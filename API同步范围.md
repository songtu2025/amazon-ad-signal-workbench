# API同步范围

更新日期：2026-06-13

## 1. 目标

本文件定义第一阶段积加开放平台 API 同步范围。

原型阶段不做高频同步，真实数据通过 `真实数据快照方案.md` 中定义的低频快照使用。

第一阶段目标是支撑 AI 信号最小闭环：

```text
同步基础广告与销售数据
→ 生成可解释 AI 信号
→ 展示证据
→ 人工处理
→ 复盘效果
```

第一阶段不做完整广告管理系统，不同步自动操作类接口。

## 2. 第一阶段必须同步

| 数据方向 | 目标表 | 主要用途 | 支撑信号 |
| --- | --- | --- | --- |
| 店铺 / 站点信息 | `shops`、`marketplaces` | 提供多店铺、站点上下文 | 店铺或站点数据缺失、所有信号筛选 |
| 销售表现 | `sales_products`、`sales_product_daily_metrics` | 判断销售承接和自然销售表现 | 产品销售强但广告弱、产品广告强但销售承接弱、处理后订单改善 |
| SP 广告活动 | `ad_campaigns` | 识别广告活动上下文 | 广告效率异常、花费突然放大 |
| SP 广告组 | `ad_groups` | 识别广告组容器 | 一个广告组内多商品表现分化、广告组结构影响诊断可信度 |
| SP 广告商品 | `advertised_products`、`ad_product_daily_metrics` | 判断广告商品表现 | 高花费无订单、广告商品高消耗低产出 |
| SP 用户搜索词-投放 | `ad_search_term_daily_metrics` | 判断搜索词消耗和转化 | 搜索词高花费无订单、高点击低 CVR |
| SP 用户搜索词-关键词 | `ad_search_term_daily_metrics` | 补充搜索词与关键词来源 | 搜索词机会、ABA 热词广告覆盖判断 |
| SP 广告位表现 | `ad_placement_daily_metrics` | 判断广告位效率 | Top of Search 花费高但转化差、广告位流量结构变化 |

## 3. 第一阶段暂缓同步

| 暂缓对象 | 暂缓原因 | 后续触发条件 |
| --- | --- | --- |
| SP 关键词详情 | 第一阶段先用搜索词表现判断覆盖，不做完整关键词库管理 | 需要判断关键词库是否完整覆盖 ABA 热词时补齐 |
| SP 商品定向详情 | P0 信号暂不依赖完整商品定向结构 | 开始做商品定向机会或竞品 ASIN 定向诊断时补齐 |
| 预算 / 竞价历史 | 第一阶段不做竞价诊断和自动调价 | 需要解释 CPC、预算、竞价变化原因时补齐 |
| SB / SD 广告 | 当前范围聚焦 SP 广告 | SP 闭环稳定后再扩展 |
| 自动操作类接口 | 项目禁止自动执行广告动作 | 不进入同步和执行范围 |

## 4. 不走 API 同步的数据

以下数据不进入积加 API 自动同步链路：

| 数据 | 来源 | 规则 |
| --- | --- | --- |
| ABA 搜索词 | 积加 ERP 人工导出 | 只按 `站点 + 周期 + 标准化搜索词` 关联 |
| 关键词排名监控 | 积加 ERP 人工导出 | 用于自然排名、广告排名、视频位判断 |
| 产品语义资料 | 人工维护 | 用于判断产品与搜索词语义相关性 |
| 投放策略资料 | 人工维护 | 用于标记主推款 ASIN、策略标签和生效周期 |
| 人工处理记录 | 系统内记录 | 用于 7 天 / 14 天复盘 |

## 5. 同步批次字段

每次 API 同步必须写入 `api_sync_runs`：

```text
api_name
shop_id
marketplace_id
start_date
end_date
synced_at
row_count
status
error_message
```

每条同步入库记录优先保留：

```text
api_sync_run_id
shop_id
marketplace_id
date 或 start_date / end_date
source_type = api
```

## 6. 信号降级规则

如果第一阶段暂缓的数据会影响信号判断，必须降低置信度，不允许假装数据已存在。

具体规则：

- 暂缓 SP 关键词详情时，`ABA 热词未覆盖` 只能判断“广告搜索词未出现或覆盖弱”，不能高置信判断“关键词库未覆盖”。
- 暂缓预算 / 竞价历史时，`CPC 异常升高` 只能提示现象，不能断言原因是竞价调整。
- 暂缓 SP 商品定向详情时，不能生成完整商品定向机会信号。
- 未同步销售表现时，不能生成产品销售承接类信号。

## 7. 禁止范围

第一阶段禁止：

```text
自动改竞价
自动暂停广告
自动开启广告
自动新增关键词
自动新增否定关键词
调用广告操作类接口
```

AI 只能生成建议和证据，所有广告动作必须人工确认。

## 8. 验收标准

API 同步范围确认后，第一阶段开发必须能回答：

```text
这个数据来自哪个 API？
同步到了哪张表？
同步周期是什么？
这条 AI 信号用了哪些 API 证据？
哪些结论因为数据暂缓而降低置信度？
```
