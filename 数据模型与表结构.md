# 数据模型与表结构

更新日期：2026-06-15

## 1. 设计目标

本文件定义项目第一阶段的数据模型和逻辑表结构。

目标不是一次性覆盖所有 Amazon 广告场景，而是支撑 v1 最小闭环：

```text
店铺 / 站点
→ 销售商品
→ 广告商品
→ 广告活动 / 广告组
→ 广告搜索词 / 广告位
→ ABA 搜索词 / 关键词排名
→ AI 信号
→ 证据
→ 人工处理
→ 7 天 / 14 天复盘
```

## 2. 建模原则

- 广告组是投放容器，不是产品。
- 经营商品和广告商品必须分开建模。
- ABA 是站点级市场数据，不归属店铺。
- 人工导入数据必须保留来源文件和导入批次。
- API 同步数据必须保留同步批次。
- AI 信号必须能回到证据。
- 证据必须能回到 API 同步或人工导入来源。
- AI 只记录建议和人工确认，不记录自动执行广告动作。
- 深度分析必须区分分析对象、经营问题、证据链、判断边界、建议和复盘路径。

## 3. 通用字段规范

核心表优先使用以下字段：

```text
id
shop_id
marketplace_id
date
start_date
end_date
source_type
source_file
import_batch_id
api_sync_run_id
created_at
updated_at
```

说明：

- `shop_id`：店铺上下文。
- `marketplace_id`：站点上下文。
- `date`：日粒度事实表使用。
- `start_date` / `end_date`：周、月或自定义周期表使用。
- `source_type`：`api`、`erp_export`、`manual`、`ai`。
- `source_file`：人工导入文件名。
- `import_batch_id`：关联人工导入批次。
- `api_sync_run_id`：关联 API 同步批次。

## 4. 基础对象层

### 4.1 shops

店铺表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| external_shop_id | 积加店铺 ID |
| shop_name | 店铺名称 |
| status | 店铺状态 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### 4.2 marketplaces

站点表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| marketplace_code | 站点代码，例如 US |
| country | 国家 |
| currency | 币种 |
| timezone | 时区 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### 4.3 sales_products

经营商品 / 销售商品表。

第一阶段可以从销售表现导出、积加 API 或人工产品资料生成，不把旧项目 `products` 表当作商品主数据。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| parent_asin | 父 ASIN |
| asin | 子 ASIN |
| msku | MSKU |
| sku | SKU |
| product_name | 商品名称 |
| brand | 品牌 |
| product_line | 产品线 |
| status | 商品状态 |
| source_type | 来源类型 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### 4.4 advertised_products

广告商品表。

代表广告中实际投放的商品对象。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| campaign_id | 广告活动 ID |
| ad_group_id | 广告组 ID |
| ad_id | 广告商品 ID |
| asin | ASIN |
| msku | MSKU |
| sku | SKU |
| ad_product_name | 广告商品名称 |
| status | 投放状态 |
| api_sync_run_id | API 同步批次 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### 4.5 ad_campaigns

广告活动表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| campaign_id | 积加 / Amazon 广告活动 ID |
| campaign_name | 广告活动名称 |
| campaign_type | 广告类型，例如 SP |
| targeting_type | 投放类型 |
| status | 状态 |
| api_sync_run_id | API 同步批次 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### 4.6 ad_groups

广告组表。

广告组只作为容器。一个广告组至少包含一个广告商品，也可能包含多个广告商品。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| campaign_id | 广告活动 ID |
| ad_group_id | 广告组 ID |
| ad_group_name | 广告组名称 |
| status | 状态 |
| api_sync_run_id | API 同步批次 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### 4.7 keywords

广告关键词表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| campaign_id | 广告活动 ID |
| ad_group_id | 广告组 ID |
| keyword_id | 关键词 ID |
| keyword_text | 关键词文本 |
| match_type | 匹配方式 |
| status | 状态 |
| api_sync_run_id | API 同步批次 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### 4.8 product_targetings

商品定向表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| campaign_id | 广告活动 ID |
| ad_group_id | 广告组 ID |
| target_id | 定向 ID |
| target_type | 定向类型 |
| target_value | 定向值 |
| status | 状态 |
| api_sync_run_id | API 同步批次 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

## 5. 表现事实层

### 5.1 sales_product_daily_metrics

销售商品每日表现表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| sales_product_id | 销售商品 |
| date | 日期 |
| asin | ASIN |
| msku | MSKU |
| sessions | 访问量 |
| page_views | 浏览量 |
| orders | 订单数 |
| units | 销量 |
| sales_amount | 销售额 |
| buy_box_rate | 购物车占比 |
| source_type | 来源类型 |
| import_batch_id | 导入批次 |
| api_sync_run_id | API 同步批次 |

### 5.2 ad_product_daily_metrics

广告商品每日表现表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| advertised_product_id | 广告商品 |
| campaign_id | 广告活动 ID |
| ad_group_id | 广告组 ID |
| ad_id | 广告商品 ID |
| date | 日期 |
| impressions | 曝光 |
| clicks | 点击 |
| spend | 花费 |
| orders | 广告订单 |
| sales | 广告销售额 |
| cpc | 单次点击成本 |
| ctr | 点击率 |
| cvr | 转化率 |
| acos | ACOS |
| roas | ROAS |
| api_sync_run_id | API 同步批次 |

### 5.3 ad_search_term_daily_metrics

广告搜索词每日表现表。

搜索词表现通常落到广告活动、广告组、关键词或投放对象，不强行归属到单个商品。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| campaign_id | 广告活动 ID |
| ad_group_id | 广告组 ID |
| keyword_id | 关键词 ID，可为空 |
| target_id | 商品定向 ID，可为空 |
| search_term | 原始搜索词 |
| normalized_query | 标准化搜索词 |
| date | 日期 |
| impressions | 曝光 |
| clicks | 点击 |
| spend | 花费 |
| orders | 订单 |
| sales | 销售额 |
| cvr | 转化率 |
| acos | ACOS |
| source_type | 来源类型 |
| import_batch_id | 导入批次 |
| api_sync_run_id | API 同步批次 |

### 5.4 ad_placement_daily_metrics

广告位每日表现表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| campaign_id | 广告活动 ID |
| ad_group_id | 广告组 ID，可为空 |
| placement | 广告位 |
| date | 日期 |
| impressions | 曝光 |
| clicks | 点击 |
| spend | 花费 |
| orders | 订单 |
| sales | 销售额 |
| cvr | 转化率 |
| acos | ACOS |
| roas | ROAS |
| api_sync_run_id | API 同步批次 |

### 5.5 keyword_rank_snapshots

关键词排名快照表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| sales_product_id | 销售商品，可为空 |
| asin | ASIN |
| keyword_text | 关键词 |
| normalized_query | 标准化搜索词 |
| date | 日期 |
| organic_rank | 自然排名 |
| ad_rank | 广告排名 |
| video_rank | 视频位置 |
| source_file | 来源文件 |
| import_batch_id | 导入批次 |

### 5.6 aba_search_term_snapshots

ABA 搜索词快照表。

ABA 不带 `shop_id`，只带站点和周期。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| marketplace_id | 站点 |
| marketplace_code | 站点代码 |
| start_date | 周期开始 |
| end_date | 周期结束 |
| search_term | 原始搜索词 |
| normalized_query | 标准化搜索词 |
| search_frequency_rank | 搜索频率排名 |
| rank_change_type | 排名变化类型 |
| rank_change_value | 排名变化值 |
| top1_asin | 第一 ASIN |
| top1_title | 第一商品标题 |
| top1_click_share | 第一点击份额 |
| top1_conversion_share | 第一转化份额 |
| top2_asin | 第二 ASIN |
| top2_title | 第二商品标题 |
| top2_click_share | 第二点击份额 |
| top2_conversion_share | 第二转化份额 |
| top3_asin | 第三 ASIN |
| top3_title | 第三商品标题 |
| top3_click_share | 第三点击份额 |
| top3_conversion_share | 第三转化份额 |
| source_file | 来源文件 |
| source_sheet | 来源工作表 |
| source_row_number | 来源行号 |
| import_batch_id | 导入批次 |

## 6. 语义与关联层

### 6.1 product_semantic_profiles

产品语义档案表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| sales_product_id | 销售商品 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| product_line | 产品线 |
| target_audience | 目标人群 |
| use_cases | 使用场景 |
| core_keywords | 核心词 |
| forbidden_keywords | 禁投词 |
| competitor_keywords | 竞品词 |
| source_type | 来源类型 |
| updated_at | 更新时间 |

### 6.2 search_term_semantic_groups

搜索词语义聚合表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| marketplace_id | 站点 |
| normalized_query | 标准化搜索词 |
| semantic_group | 语义分组 |
| intent_type | 意图类型 |
| confidence | 分组置信度 |
| source_type | 来源类型 |
| updated_at | 更新时间 |

### 6.3 product_ad_product_links

经营商品与广告商品关联表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| sales_product_id | 销售商品 |
| advertised_product_id | 广告商品 |
| match_key | 关联字段，例如 asin / msku / sku |
| confidence | 关联置信度 |
| source_type | 来源类型 |
| created_at | 创建时间 |

### 6.4 ad_group_product_members

广告组内投放商品关系表。

用于识别“一个广告组投多个商品”的结构问题。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| campaign_id | 广告活动 ID |
| ad_group_id | 广告组 ID |
| advertised_product_id | 广告商品 |
| asin | ASIN |
| status | 投放状态 |
| api_sync_run_id | API 同步批次 |

### 6.5 promotion_strategy_profiles

人工维护的投放策略资料。

第一阶段用本地运行文件保存，不进入 API 自动同步。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键，可选 |
| shop_id | 店铺 |
| marketplace_id / market_id | 站点 |
| ad_group_id | 广告组 ID |
| asin | 主推或策略相关 ASIN |
| strategy_role | 策略角色，例如 `main_push` |
| strategy_label | 策略展示名，例如 `主推款` |
| start_date | 生效开始 |
| end_date | 生效结束，可为空 |
| source_type | 来源类型，第一阶段为人工语义 |
| note | 备注 |
| updated_at | 更新时间 |

## 7. AI 信号层

### 7.0 深度分析逻辑字段

第一阶段可以由 `signal_category`、`object_type`、`uncertainty` 和 `suggestion` 推导这些逻辑字段；后续如果进入结构化存储，应按以下口径固化：

| 字段 | 说明 |
| --- | --- |
| analysis_object_level | 分析对象层级：`parent_asin`、`asin`、`advertised_product`、`search_term`、`placement`、`ad_group`、`campaign`、`data_quality`、`cross` |
| problem_type | 经营问题类型：花费浪费、销售承接不足、机会扩量、投放结构失衡、市场竞争压力、数据质量缺口、处理后复盘 |
| evidence_strength | 证据强度：强、中、弱；由证据源数量、样本量、时间窗口和新鲜度决定 |
| attribution_boundary | 归因边界，例如搜索词不能自动归属 ASIN、广告组只是容器、ABA 不是店铺数据 |
| counter_evidence | 反证或不确定性，例如主推款策略、低样本、过期数据、来源冲突 |
| review_path | 复盘路径：不适用、待人工动作、待 7d、待 14d、ready |

硬规则：

- 不能只保存指标异常而缺少 `problem_type`。
- 不能把一次性快照行 ID 当成长期复盘对象 ID。
- 数据质量信号的 `review_path` 默认不是广告指标复盘。

### 7.1 ai_signals

AI 信号主表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| signal_type | 信号类型 |
| signal_category | 信号大类 |
| priority | P0 / P1 / P2 |
| severity | 严重度 |
| confidence | 置信度 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| object_type | 判断对象类型 |
| object_id | 判断对象 ID |
| problem_type | 经营问题类型 |
| evidence_strength | 证据强度 |
| attribution_boundary | 归因边界 |
| title | 信号标题 |
| summary | 简要结论 |
| reason | 原因说明 |
| suggestion | 建议动作 |
| uncertainty | 不确定性 |
| review_path | 复盘路径 |
| status | `new`、`observing`、`handled`、`ignored`、`reviewing` |
| detected_at | 发现时间 |
| start_date | 判断周期开始 |
| end_date | 判断周期结束 |

### 7.2 ai_signal_evidences

信号证据表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| signal_id | AI 信号 |
| evidence_type | 证据类型 |
| source_type | 来源类型 |
| source_table | 来源表 |
| source_record_id | 来源记录 ID |
| metric_name | 指标名 |
| metric_value | 指标值 |
| comparison_value | 对比值 |
| time_range | 证据周期 |
| object_type | 证据对象类型 |
| object_id | 证据对象 ID |
| explanation | 证据解释 |

### 7.3 ai_signal_actions

人工处理记录表。

只记录人工动作，不记录自动广告动作。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| signal_id | AI 信号 |
| action_type | `observe`、`handled`、`add_to_review`、`ignore` |
| action_note | 处理备注 |
| operator_name | 操作人 |
| acted_at | 操作时间 |
| snapshot_id | 处理时对应的快照 |
| shop_id | 店铺 |
| market_id | 站点 |
| object_type | 处理对象类型 |
| object_id | 处理对象 ID |
| object_label | 处理对象展示名 |
| evidence_snapshot | 人工点击时的证据块摘要快照，仅用于后续复盘回看，不代表自动执行广告动作 |

`ReviewTodo` 由人工动作派生时继承 `evidence_snapshot`，用于 7d / 14d 待办回看当时判断依据；该字段不是复盘结果，也不触发广告执行。

`ReviewTodo.evidence_snapshot` 不只校验条数和证据类型，还必须能回看当前复盘对象本身，例如 `object_id`、`object_label` 或搜索词稳定文本。若快照内容指向其他候选对象，即使包含排查路径、AI 准入、搜索词边界和广告位边界，也视为对象不一致，不得进入 ReviewRecord 保存链路。

### 7.4 review_todo_decisions

复盘待办治理记录表。

只记录人工对历史复盘待办的治理结论，不记录广告动作，不补写历史证据。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| decision_type | 当前为 `void_legacy_missing_evidence` |
| action_id | 被治理的人工动作 ID |
| signal_id | AI 信号 |
| review_window | `7d` / `14d`，为空表示该 action 派生的全部复盘窗口 |
| reason | 作废原因 |
| operator_name | 操作人 |
| decided_at | 操作时间 |
| shop_id | 店铺 |
| market_id | 站点 |
| object_type | 复盘对象类型 |
| object_id | 复盘对象 ID |
| object_label | 复盘对象展示名 |
| can_auto_change_rules | 固定为 false |
| can_auto_execute_ads | 固定为 false |

`ReviewTodoDecision` 只用于让历史缺少 `evidence_snapshot` 的待办退出 ReviewRecord 保存链路；它不删除 `ManualAction`，不修改 `manual_actions.jsonl`，也不能把当前证据伪装成历史点击证据。

### 7.5 ai_signal_reviews

复盘结果表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| signal_id | AI 信号 |
| review_window | `7d` / `14d` |
| before_start_date | 处理前周期开始 |
| before_end_date | 处理前周期结束 |
| after_start_date | 处理后周期开始 |
| after_end_date | 处理后周期结束 |
| before_metrics | 处理前指标摘要 |
| after_metrics | 处理后指标摘要 |
| evidence_snapshot | 保存复盘时已校验的人工动作证据快照，必须能回看“排查路径”“AI 准入”“搜索词边界”和“广告位边界”，不代表自动执行广告动作 |
| result | `improved`、`no_change`、`worse`、`unclear` |
| review_note | 复盘备注 |
| reviewed_at | 复盘时间 |

`ReviewRecord.evidence_snapshot` 来源于对应 `ReviewTodo.evidence_snapshot` 的保存前校验，用于后续规则反馈样本回看当时诊断路径、AI 准入原因、搜索词边界、广告位边界和复盘对象身份；它不是新的广告事实，也不能触发自动改规则或自动执行广告动作。

## 8. 数据来源层

### 8.1 import_batches

人工导入批次表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| source_type | 导入类型，例如 ABA导出 / 关键词监控 / 销售表现 |
| source_file | 文件名 |
| file_hash | 文件哈希 |
| marketplace_id | 站点，可为空 |
| shop_id | 店铺，可为空 |
| start_date | 数据周期开始 |
| end_date | 数据周期结束 |
| imported_at | 导入时间 |
| row_count | 行数 |
| status | 状态 |
| error_message | 错误信息 |

### 8.2 api_sync_runs

API 同步批次表。

| 字段 | 说明 |
| --- | --- |
| id | 内部主键 |
| api_name | API 名称 |
| shop_id | 店铺 |
| marketplace_id | 站点 |
| start_date | 同步周期开始 |
| end_date | 同步周期结束 |
| synced_at | 同步时间 |
| row_count | 行数 |
| status | 状态 |
| error_message | 错误信息 |

## 9. 核心关联关系

```text
shops
  → sales_products
  → advertised_products
  → ad_campaigns / ad_groups
  → ad_search_term_daily_metrics
  → ai_signals

marketplaces
  → aba_search_term_snapshots
  → keyword_rank_snapshots
  → ai_signal_evidences

ai_signals
  → ai_signal_evidences
  → ai_signal_actions
  → ai_signal_reviews
```

重点关系：

- `sales_products` 和 `advertised_products` 通过 `asin / msku / sku` 或人工确认关系连接。
- `ad_groups` 和 `advertised_products` 是一对多关系。
- `ad_search_term_daily_metrics` 不强制归属到单个商品。
- `aba_search_term_snapshots` 通过 `marketplace_id + normalized_query + start_date + end_date` 参与机会识别。
- `ai_signal_evidences` 是所有 AI 信号可信度的核心依据。

## 10. 第一阶段必须优先落地的表

第一阶段先落这些表：

```text
shops
marketplaces
sales_products
advertised_products
ad_campaigns
ad_groups
sales_product_daily_metrics
ad_product_daily_metrics
ad_search_term_daily_metrics
ad_placement_daily_metrics
keyword_rank_snapshots
aba_search_term_snapshots
product_semantic_profiles
promotion_strategy_profiles
ai_signals
ai_signal_evidences
ai_signal_actions
ai_signal_reviews
import_batches
api_sync_runs
```

暂缓：

```text
keywords
product_targetings
search_term_semantic_groups
product_ad_product_links
ad_group_product_members
```

暂缓原因：这些表重要，但第一阶段可以先从导入数据和广告搜索词样本生成可验证信号，等 API 同步和产品关联稳定后再补齐。
