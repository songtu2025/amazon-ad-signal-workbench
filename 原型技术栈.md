# 原型技术栈

更新日期：2026-06-14

## 1. 结论

第一阶段原型采用现有技术栈继续推进：

```text
前端：React 19 + TypeScript + Vite + lucide-react + 普通 CSS
后端：Python + FastAPI + Pydantic
测试：pytest
数据层：低频真实 API 快照 + 本地 JSON 快照 + mock 兜底
```

不更换技术栈。

## 2. 当前项目事实

前端当前配置：

```text
React 19
TypeScript 5
Vite 6
lucide-react
普通 CSS
```

后端当前配置：

```text
FastAPI
Pydantic
pytest
uvicorn
```

因此第一阶段不要引入新框架重做项目。

## 3. 为什么后端用 Python

Python 适合本项目后端，原因：

- 处理 Excel / CSV / ABA 导入更直接。
- 写异常规则、证据构建、阈值判断更简单。
- 后续接语义聚合、embedding、AI 总结更自然。
- 项目不是高并发 C 端系统，Python 性能足够。
- 当前项目已经是 FastAPI，继续使用成本最低。

## 4. 前端边界

前端只负责：

```text
顶部筛选器
AI 信号队列
信号详情
证据链展示
建议处理
人工状态切换
数据新鲜度提示
```

前端不负责：

```text
直接请求积加 API
持有 AppKey 或 accessToken
生成 AI 信号
执行广告动作
复杂 BI 报表
```

## 5. 后端边界

后端负责：

```text
读取真实 API 快照
读取 ERP 导入文件
标准化数据
生成 AI 信号
构建证据链
保存人工处理和复盘记录
向前端提供只读 API
```

后端不负责：

```text
自动改竞价
自动暂停广告
自动新增关键词
自动新增否定关键词
页面打开时实时高频拉取积加 API
```

## 6. 数据层策略

第一阶段按顺序使用：

```text
真实 API 快照
ERP 导入文件
明确标记的 mock 样例数据
```

第一阶段优先使用本地 JSON 快照：

```text
data/api_snapshots/
```

暂不强制接入生产级数据库。

MVP 人工处理留痕先使用本地轻量文件，例如 JSONL。

后续需要多用户、并发写入、复杂查询或长期复盘时，再引入 SQLite。

## 7. 依赖策略

当前不主动增加依赖。

实现真实快照时允许按需新增：

```text
httpx：请求积加 API
openpyxl：读取 Excel
```

暂不引入：

```text
Next.js
Tailwind
shadcn
Redux
ORM
Celery
Airflow
复杂权限系统
```

## 8. 测试策略

第一阶段测试重点：

```text
信号规则
证据来源
字段映射
搜索词标准化
真实快照读取
mock 与真实数据标记
人工动作不自动执行
```

测试工具：

```text
pytest
前端构建检查
必要时浏览器手动验证
```

## 9. 验收标准

原型技术栈确认后，开发必须满足：

```text
前端能运行工作台页面
后端能提供信号 API
信号优先读真实快照
mock 数据必须显示为样例数据
不在前端暴露积加凭据
不引入自动广告执行能力
```
