# 开发原型

更新日期：2026-06-17

## 1. 原型定位

本原型用于验证 AI Copilot 工作台的信息架构和核心交互。

原型目标不是做完整系统，而是验证：

- 信号队列是否易读
- 信号详情是否能解释清楚异常和机会
- 证据链是否能支撑 AI 结论
- 建议处理是否符合人工确认流程
- 多店铺、站点、周期筛选是否清晰

## 2. 视觉参考

已选方向：AI Copilot 工作台。

已选 UI 视觉策略：信号分诊工作台。

参考图：

```text
design/ai-copilot-workbench-v1.png
```

该图只作为信息架构和布局参考，不代表最终视觉规范。

目标版原型补充：

```text
design/ai-copilot-workbench-v2.png
design/ai-copilot-workbench-v2.html
```

v2 用于纠正“信号列表优先导致口径混乱”的问题。

核心变化：

```text
从信号队列优先
改为广告诊断路径优先：

Parent ASIN 经营盘
→ 广告 ASIN 覆盖
→ 广告组结构
→ 广告位 / 投放词
→ 搜索词 / ABA 证据
→ AI 信号准入
→ 人工确认 / 复盘
```

v2 的判断规则：

```text
没有主对象、经营问题、证据强度、归因边界和复盘指标时，
不能把信息包装成机会或广告调整建议。
```

信号分诊工作台的核心判断：

```text
优先帮助运营判断今天先处理什么。
再解释为什么异常、证据是什么、建议怎么处理。
```

## 3. 原型第一屏

第一屏直接进入工作台，不做单独店铺选择页。

页面结构：

```text
顶部筛选器
------------------------------------------------
店铺 / 站点 / 产品线 / 产品 / 周期 / 数据新鲜度

左侧：AI 信号队列
------------------------------------------------
异常 / 机会 / 数据质量 / ABA机会 / 观察 / 待确认

中间：信号诊断
------------------------------------------------
结论
原因
证据链
影响范围
不确定性

右侧：建议处理
------------------------------------------------
建议动作
风险说明
置信度
人工确认
复盘记录
```

## 4. 原型必须体现的业务边界

- 店铺是顶部筛选器
- 信号必须带店铺和站点上下文
- ABA 是人工导入增强数据
- ABA 不直接归属店铺
- 广告组是容器，不是产品
- 销售商品和广告商品必须分开
- AI 只建议，不自动执行

## 5. 原型中的信号样例

原型至少要包含以下样例信号：

```text
1. 高花费无订单
2. 高点击低转化
3. ABA 热词未覆盖
4. 自然排名强但广告消耗高
5. 广告组内多商品表现分化
6. 广告位异常
```

这些样例用于验证界面，不代表第一阶段全部实现范围。

## 6. 原型数据来源

原型优先使用真实 API 快照，不再以纯 mock 作为正式演示数据。

真实数据方案详见：`真实数据快照方案.md`。

mock 只允许作为凭据缺失、交互开发和自动化测试的兜底，并且必须明确标记为样例数据。

建议以 RBK004 样本作为原型语境：

```text
店铺：rivbos
站点：US
产品线：儿童太阳镜
产品：RBK004
ABA 周期：2026-05-10 到 2026-05-16
```

可参考的数据文件：

```text
D:\DataProject\coedx_project\Amazon广告分析-RBK004\src_data\ABA搜索词报表-Top30万搜索词.xlsx
D:\DataProject\coedx_project\Amazon广告分析-RBK004\src_data\RBK004\用户搜索词报表-按天.xlsx
D:\DataProject\coedx_project\Amazon广告分析-RBK004\src_data\RBK004\关键词监控报表-000047955-20260521140845.xlsx
D:\DataProject\coedx_project\Amazon广告分析-RBK004\src_data\RBK004\销售表现-父ASIN.xlsx
D:\DataProject\coedx_project\Amazon广告分析-RBK004\src_data\RBK004\产品介绍.md
```

## 7. 原型交互范围

v1 原型需要可交互：

- 切换店铺筛选器
- 切换站点筛选器
- 切换周期筛选器
- 点击左侧信号
- 中间详情随信号切换
- 右侧建议和人工动作随信号切换
- 切换证据来源标签
- 标记信号状态

v1 原型不需要实现：

- 真实登录
- 高频实时 API 同步
- 真实 ABA 导入
- 生产级真实数据库写入
- 自动广告操作
- 跨店铺总览

说明：

```text
生产级数据库写入暂不做。
但人工处理状态必须支持本地轻量留痕，否则 MVP 的“人工确认 / 记录 / 复盘”闭环不成立。
```

## 8. 人工动作

原型中只允许出现以下人工动作：

```text
记录观察
标记已处理
加入复盘
忽略本次
```

不能出现：

```text
自动调价
自动暂停
自动否词
自动新增关键词
```

### 8.1 MVP 人工闭环

MVP 阶段右侧人工动作不能只改前端本地状态。

必须做到：

```text
用户点击人工动作
→ 前端提交信号 ID、动作类型、备注或默认说明、操作时间
→ 后端保存为人工处理记录
→ 信号详情能读回最近处理状态
```

第一阶段只要求本地轻量保存，不要求生产级账号、权限、审批流或数据库迁移。

## 9. 原型验收标准

原型通过标准：

- 第一屏能看出这是 AI 信号工作台，不是普通报表
- 用户能快速理解信号优先级
- 点开信号后能看到结论、原因、证据和建议
- 证据来源清楚
- 店铺和站点上下文清楚
- 没有自动执行广告动作
- 没有把广告组展示成产品
- 没有把 ABA 展示成店铺专属数据

## 10. 可开发 UI 规格

第一版 UI 按“信号分诊工作台”实现。

### 10.1 页面结构

```text
顶部筛选区
信号健康概览
左侧：信号分诊队列
中间：选中信号诊断
右侧：建议处理和人工确认
```

### 10.2 顶部筛选区

必须包含：

```text
店铺
站点
产品线
产品
周期
数据新鲜度
```

规则：

- 店铺只是筛选器，不做单独店铺首页。
- 诊断入口优先使用 Parent ASIN / ASIN；`全量排查` 只能作为混合视图排查入口，不能作为默认经营诊断口径。
- 商品经营诊断的阅读顺序固定为：Parent ASIN / ASIN 商品组 -> 广告 ASIN 对比 -> 广告组 / 搜索词 / 广告位证据下钻。
- 诊断入口边界区必须展示“当前分析路径”，把店铺 / 站点、Parent ASIN / ASIN、广告 ASIN 和搜索词 / 广告位上下文分层展示。
- 广告组、搜索词、广告位不能和商品组放在同一层筛选，否则会把经营对象和投放证据混在一起。
- 数据新鲜度必须显示 API 快照、ABA 导入、关键词监控的更新时间。
- 刷新真实 API 快照必须是人工明确触发，不能页面打开自动高频拉取。

### 10.3 信号健康概览

放在顶部筛选区下方，用于快速判断当天处理压力。

建议指标：

```text
高优先级信号数
异常信号数
机会信号数
数据质量信号数
数据过期信号数
待人工确认数
```

这些指标只做概览，不替代左侧信号队列。

### 10.4 左侧信号分诊队列

左侧是第一操作入口，名称使用：

```text
信号分诊
```

队列分组：

```text
高优先级
机会点
数据质量
数据新鲜度
观察中
已处理
```

每条信号行必须展示：

```text
严重度
信号类型
对象类型
对象名称
核心问题一句话
影响指标
置信度
数据新鲜度
人工状态
```

规则：

- 信号列表用行分隔，不做大量独立卡片。
- 默认按严重度和发现时间排序。
- 选中态必须清晰，但不要用强装饰。
- 左侧队列标题和说明必须跟随当前诊断入口变化；Parent ASIN / ASIN 入口下必须表达为“当前商品组 / 当前 ASIN AI 信号”，不能让用户误以为仍在看全量广告对象。
- 多商品广告组信号必须明确写出“广告组是容器”或“归因不确定”。
- 队列层必须展示复盘待办摘要；当前 Parent ASIN / ASIN 范围下只统计当前商品范围，全量排查入口下统计全局复盘待办。
- 复盘待办只表示已进入 7 天 / 14 天复盘窗口，不能直接展示成“处理已改善”的结论。

### 10.5 中间选中信号诊断

中间是核心阅读区，名称使用：

```text
选中信号
```

固定展示顺序：

```text
诊断摘要
分析框架
为什么异常 / 为什么值得关注
证据时间线
关键指标对比
影响范围
不确定性
```

规则：

- 诊断摘要必须先说结论，不先堆图表。
- 分析框架必须展示：分析对象、经营问题、证据强度、归因边界、复盘路径。
- 经营问题必须使用固定分类：花费浪费、销售承接不足、机会扩量、投放结构失衡、市场竞争压力、数据质量缺口、处理后复盘。
- 数据质量信号必须先展示“判断边界”，说明当前不能直接下经营结论。
- 证据时间线前必须先展示关键证据摘要，优先露出最能支持结论的 3 条证据。
- 证据时间线优先级高于图表。
- 证据链必须支持按实际来源筛选，至少包含“全部”和当前信号实际出现的证据来源。
- 选中信号必须展示对象上下文，说明广告活动、广告组、搜索词、广告位、广告商品或销售商品等判断层级。
- 对象上下文必须说明对象边界，例如广告组是投放容器、搜索词不能强行归属到单个商品、广告位只代表流量位置。
- 广告组结构信号如果已有 advertised_products 证据，必须展示广告商品清单；如果没有，必须明确说明商品清单证据不足。
- 广告组结构信号如果 source_rows 已包含广告商品表现，必须展示广告商品对比，至少包含花费、订单、销售额、ACOS、CVR。
- 广告组结构信号必须展示“为什么建议拆开看”的差异解释，说明广告组汇总会掩盖哪些商品级消耗和承接差异。
- 广告商品信号必须提供商品下钻切换，至少区分商品指标、搜索词上下文、广告位上下文和边界说明；搜索词和广告位只能作为上下文，不写成 ASIN 归因证据。
- 商品下钻每个分区必须展示一句边界说明：商品指标可直接落到当前 ASIN；搜索词和广告位只说明同广告组上下文；边界说明解释不能自动归因和需要人工复核的部分。
- Parent ASIN 入口必须先展示商品组概览，说明子 ASIN、广告 ASIN、主推款策略说明和搜索词 / 广告位归因边界，再进入具体信号详情。
- Parent ASIN 商品组概览下必须展示广告 ASIN 对比表，至少包含广告 ASIN、花费、订单、销售额、ACOS 和策略说明；该表只使用广告商品粒度指标。
- 广告 ASIN 对比表中的广告 ASIN 必须能作为轻量入口，点击后切换到对应 ASIN 诊断范围。
- 影响范围必须说明影响性质，例如数据判断能力、投放结构、搜索词投放判断、广告位流量分配或经营表现，不能只堆标签。
- 图表只用于解释趋势，不做普通 BI 大盘。
- 所有证据必须展示来源：积加API / ABA导出 / 销售表现 / 关键词监控 / 人工语义。
- 没有 ready 复盘时，只能展示“待复盘 / 证据不足”，不能展示“已改善 / 无效果 / 恶化”。
- 使用 mock 时必须标记为“样例数据”。

### 10.6 右侧建议处理

右侧是窄操作栏，名称使用：

```text
建议处理
```

必须包含：

```text
建议动作
风险提示
置信度
人工确认
复盘记录
```

允许按钮：

```text
记录观察
标记已处理
加入复盘
忽略本次
```

禁止按钮：

```text
自动调价
自动暂停
自动否词
自动新增关键词
```

### 10.7 视觉风格

第一版使用轻量运营后台风格：

```text
白色 / 浅灰底色
深灰正文
蓝色表示选中和主操作
橙红表示风险
绿色表示置信度或改善
8px 以内圆角
少阴影
多用间距、分组、分割线
```

禁止：

```text
营销页 hero
大面积渐变
装饰性背景
卡片套卡片
聊天框优先
普通 BI 大盘首页
```

## 11. 开发原型实现方案

第一阶段实现目标：

```text
按已确认的 Product Design 方向独立实现“信号分诊工作台”原型。
当前代码只作为技术壳，不作为产品结构、UI 布局或业务语境参考。
先跑通原型主链路，再接入真实 API 快照。
```

### 11.1 原型隔离原则

开发原型不得被当前已有样例页面、RB831 样例数据、现有组件结构或现有样式布局带偏。

允许参考：

```text
React / Vite 运行方式
FastAPI 接口组织方式
Pydantic response_model 用法
pytest 测试方式
```

禁止参考：

```text
旧 TodaySignals 页面布局
旧 RB831 mock 业务语境
旧样例信号字段缺口
旧样例组件拆分方式
旧样例样式层级
```

旧样例路径不得重新作为原型入口：

```text
frontend/src/pages/TodaySignals/
backend/app/services/sample_data.py
```

### 11.2 技术壳参考范围

当前项目可借用的工程能力：

```text
frontend/src/App.tsx
frontend/src/api.ts
frontend/src/styles.css
backend/app/api/routes.py
backend/app/models/signals.py
backend/app/services/signal_detection.py
backend/tests/test_signal_detection.py
```

不能沿用的问题：

```text
旧页面不包含已确认顶部筛选区
旧左侧不是信号分诊队列
旧中间不是证据时间线优先
旧右侧缺少人工确认动作区
旧 mock 语境不是 RBK004 / rivbos / US
旧样例字段不完整，不能作为 API 合同事实
```

### 11.3 第一批允许修改文件

开发第一版 UI 原型时，建议只允许修改：

```text
frontend/src/App.tsx
frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx
frontend/src/api.ts
frontend/src/styles.css
backend/app/models/signals.py
backend/app/services/signal_detection.py
backend/app/api/routes.py
backend/tests/test_signal_detection.py
```

如需新增前端组件，只允许新增在：

```text
frontend/src/components/
```

如需新增后端测试，只允许新增在：

```text
backend/tests/
```

暂不修改：

```text
真实 API 快照脚本
ABA 导入脚本
数据库持久化
登录 / 权限
旧项目任何文件
```

### 11.4 后端实现顺序

第一步：补齐 API 合同。

`AiSignal` 至少补齐：

```text
signal_category
priority
confidence
shop_id
shop_name
market_id
marketplace
freshness_status
data_sources
detected_at
uncertainty
manual_status
```

`EvidenceItem` 至少补齐：

```text
source_type
source_name
metric_name
metric_value
time_range
explanation
```

第二步：样例数据必须明确标记。

当前还可以继续使用 `sample_data.py` 支撑页面交互，但必须让前端能显示：

```text
样例数据
```

不能显示为：

```text
积加API
```

第三步：保留只读 API。

第一批接口建议保持简单：

```text
GET /api/signals
GET /api/signals/{signal_id}
GET /api/search-intents
```

暂不新增广告操作接口。

### 11.5 前端实现顺序

第一步：新建隔离页面。

```text
frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx
```

`App.tsx` 只指向新页面，不再指向旧样例页面。

第二步：顶部筛选区。

先做静态可交互筛选器：

```text
店铺：rivbos
站点：US
产品线：儿童太阳镜
产品：RBK004
周期：近 7 天 / 近 14 天
数据新鲜度：样例数据 / API 快照 / ABA 导入
```

第三步：信号健康概览。

从信号列表聚合：

```text
高优先级
异常
机会
数据质量
数据过期
待确认
```

第四步：左侧信号分诊队列。

按以下分组展示：

```text
高优先级
机会点
数据质量
数据新鲜度
观察中
已处理
```

第五步：中间选中信号诊断。

按固定顺序展示：

```text
诊断摘要
为什么异常 / 为什么值得关注
证据时间线
关键指标对比
影响范围
不确定性
```

第六步：右侧建议处理。

只允许以下按钮：

```text
记录观察
标记已处理
加入复盘
忽略本次
```

第一版人工状态可以先做前端状态切换，不写入数据库。

### 11.6 真实数据接入顺序

UI 原型跑通后，再单独进入真实 API 快照任务。

真实数据接入不得破坏 UI 合同：

```text
真实 API 快照
→ 标准化为同一 AiSignal / Evidence 结构
→ 前端不用关心数据来自 sample 还是真实快照
→ 前端只根据 source_type 显示来源和可信度
```

如果没有真实快照：

```text
允许展示样例数据
但必须显式标记为样例数据
不能把样例数据当真实业务结论
```

### 11.7 最小验收命令

后端：

```text
cd backend
python -m pytest
```

前端：

```text
cd frontend
npm run build
```

浏览器验收：

```text
打开前端页面
确认顶部筛选、信号分诊、选中信号、证据时间线、右侧人工动作可见
确认没有自动广告执行按钮
确认样例数据有明确标记
```

## 12. 后续进入开发前必须确认

进入正式开发前，还需要确认：

- AI 信号类型矩阵已确认：`AI信号类型矩阵.md`
- 数据模型与表结构已确认：`数据模型与表结构.md`
- API 同步范围已确认：`API同步范围.md`
- ABA 导入流程已确认：`ABA导入流程.md`
- 真实数据快照方案已确认：`真实数据快照方案.md`
- 原型技术栈已确认：`原型技术栈.md`
- UI 视觉策略已确认：信号分诊工作台
- 第一批允许修改文件范围已在本文件中列出

## 13. v3 原型补充：真实运营诊断路径

### 13.1 原型文件

```text
design/ai-copilot-workbench-v3.html
```

v3 原型用于修正 v2 仍偏对象矩阵的问题，把第一屏收敛到真实运营路径：

```text
Parent ASIN 销售盘
→ 广告 ASIN 覆盖
→ 广告组结构
→ 广告位 / 投放词
→ 搜索词 / ABA 证据
→ AI 信号准入
→ 人工确认 / 7 天与 14 天复盘
```

### 13.2 v3 首屏重点

```text
顶部：店铺、站点、Parent ASIN、周期、快照、数据源、禁止自动动作。
左侧：运营决策路径，不再把全量机会列表作为默认入口。
中间：Parent ASIN 销售盘、广告 ASIN、广告组、有效搜索词、无订单花费词、广告位边界。
右侧：AI 准入门禁、允许和禁用的人工动作、7/14 天复盘口径。
```

### 13.3 v3 业务边界

```text
B00K4W4AAA 当前有广告数据，但 candidate_count=0。
因此 v3 只展示诊断态和下钻路径，不生成广告调整建议。

销售表现中的 12 个子 ASIN 只能作为经营背景。
只有 3 个广告 ASIN 能进入广告诊断。
搜索词和广告位只能说明同广告组或同广告活动上下文，不能自动归因到单个 ASIN。
ABA 只能作为站点级市场背景，不能替代广告搜索词证据。
```
