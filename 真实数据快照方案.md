# 真实数据快照方案

更新日期：2026-06-16

## 1. 目标

本文件定义第一阶段原型如何使用真实积加 API 数据。

采用：

```text
低频真实 API 拉取
→ 本地快照保存
→ AI 信号从快照生成
→ 前端读取快照结果
```

不采用：

```text
页面每次打开都请求积加 API
定时高频同步
自动广告操作
只用 mock 数据做正式原型
```

## 2. 第一阶段真实样本

第一阶段先用一个真实店铺站点做闭环验证：

```text
店铺：rivbos
站点：US
market_id：1
周期：最近 7 天或最近 14 天
```

说明：

- 该样本用于验证数据链路、信号证据和页面展示。
- 不代表系统只支持单店铺。
- 后续多店铺仍通过顶部筛选器扩展。

## 3. 数据来源

真实快照优先拉取 `API同步范围.md` 中第一阶段必须同步的数据：

| 数据方向 | 来源 | 目标 |
| --- | --- | --- |
| 店铺 / 站点信息 | 积加 API | 识别 `market_id` 对应店铺站点 |
| 销售表现 | 积加 API | 支撑销售商品、销售承接、复盘 |
| SP 广告活动 | 积加 API | 支撑广告结构 |
| SP 广告组 | 积加 API | 支撑广告组容器分析 |
| SP 广告商品 | 积加 API | 支撑广告商品表现分析 |
| SP 用户搜索词-投放 | 积加 API | 支撑搜索词异常和机会 |
| SP 用户搜索词-关键词 | 积加 API | 支撑搜索词来源证据 |
| SP 广告位表现 | 积加 API | 支撑广告位异常 |

仍然不走 API 的数据：

| 数据 | 来源 |
| --- | --- |
| ABA 搜索词 | 积加 ERP 人工导出 |
| 关键词排名监控 | 积加 ERP 人工导出 |
| 产品语义资料 | 人工维护 |

## 4. 快照保存规则

建议快照目录：

```text
data/api_snapshots/
```

单次快照建议结构：

```text
data/api_snapshots/
  gerpgo_market_1_YYYYMMDD_HHMMSS/
    manifest.json
    raw/
      market_names.json
      sales_performance.json
      sp_campaigns.json
      sp_advertised_products.json
      sp_search_targeting_terms.json
      sp_search_keyword_terms.json
      sp_placements.json
    normalized/
      shops.json
      marketplaces.json
      sales_products.json
      sales_product_daily_metrics.json
      advertised_products.json
      ad_campaigns.json
      ad_groups.json
      ad_search_term_daily_metrics.json
      ad_placement_daily_metrics.json
```

`manifest.json` 必须记录：

```text
snapshot_id
source = gerpgo
market_id
shop_name
marketplace_code
start_date
end_date
created_at
api_list
row_counts
status
error_message
```

## 5. 拉取规则

第一阶段只允许手动触发。

默认限制：

```text
days = 7 / 14 / 30
count <= 50
max_pages <= 3
sales_max_pages <= 5（只用于销售表现补齐）
page_delay_seconds >= 1.2
sales_api_delay_seconds >= 5.2
```

规则：

- 请求积加 API 前必须先查询限流规则，命令：

```text
python scripts\create_api_snapshot.py --check-rate-limits
```

- `/open/api_token` 当前可查到公开规则：每 1 秒 10 次。
- 当前业务接口暂未查到公开单接口限流规则，只能使用项目保守规则，不得把经验值写成官方规则。
- 代码请求层必须以 `GERPGO_RATE_LIMIT_RULES` 的 `min_interval_seconds` 统一执行节流等待，不在分页函数里散落硬编码等待时间。
- 拉取前检查本地是否已有同店铺、同站点、同周期快照。
- 已有新鲜快照时默认复用，不重复请求 API。
- 需要刷新时由人工明确触发。
- API 失败时保留上一次成功快照。
- API 失败不能自动改用伪造数据冒充真实数据。

手动拉取入口：

```text
python scripts\create_api_snapshot.py --market-id 1 --days 7 --count 10 --max-pages 1
python scripts\create_api_snapshot.py --market-id 1 --days 30 --count 50 --max-pages 3
python scripts\run_snapshot_pipeline.py --market-id 1 --days 30 --count 50 --max-pages 3 --sales-max-pages 5 --force
```

工作台人工触发入口：

```text
GET /api/market-options
POST /api/snapshot/probe
POST /api/snapshot/create
POST /api/snapshot/pipeline
```

工作台顶部店铺 / 站点筛选器必须优先读取：

```text
GET /api/market-options
```

该接口只返回安全筛选选项，来源优先级：

```text
成功快照 manifest
→ 探测成功后的安全 market option 缓存
→ 新项目 .env / 进程环境变量中的 GERPGO_MARKET_IDS
→ 第一阶段项目样本 market_id 1
```

工作台页面的“探测 API”按钮必须优先调用：

```text
POST /api/snapshot/probe
```

工作台页面的“拉取快照”按钮必须优先调用：

```text
POST /api/snapshot/pipeline
```

`POST /api/snapshot/probe` 只做轻量权限探测，不写入快照，不拉取广告明细。
`POST /api/snapshot/probe` 和 `--probe-api` 探测成功后可以写入 `data/runtime/market_options.json`，只保存 `market_id`、店铺名和站点代码等安全筛选摘要，不保存密钥、原始响应或广告明细。
`POST /api/snapshot/create` 只作为底层创建或复用快照入口，不作为页面最终验收口径。

请求参数：

```text
market_id
days = 7 / 14 / 30
count <= 50
max_pages <= 3
sales_max_pages <= 5
force = false
```

规则：

- 该接口只能由人工点击触发。
- `days=30` 只用于人工明确授权的低频深度分析补数，不作为页面自动刷新默认值。
- `sales_max_pages` 只允许用于销售表现 `salesAnalysis/page` 补齐 Parent ASIN / ASIN 证据，不放大广告商品、搜索词或广告位接口分页。
- 顶部筛选器不能在前端写死真实店铺事实；真实店铺文案只能来自成功快照或探测成功后的安全缓存，未探测时只能显示 `market-{id}` 和 `待探测`。
- 缺少新项目 `.env` 或进程环境变量时返回 `status = not_ready` 和缺失配置键名，不请求积加 API。
- 已有同店铺、同周期成功快照时默认复用。
- 创建或复用成功时，响应必须带 `inspection`，说明 `ready_for_signals`、可分析行数和 issues。
- `/api/snapshot/pipeline` 必须同时返回 `create`、`inspection` 和 `signal_scan`，用于工作台一次性确认真实快照是否能进入 AI 信号。
- 完整快照拉取必须先请求 `marketNames/query` 并匹配目标 `market_id`；未匹配时立即停止，不继续请求销售或广告明细，不写成功快照。
- 不得从前端传递或暴露 `AppKey`、`AppSecret`、`accessToken`。

拉取前配置检查：

```text
python scripts\create_api_snapshot.py --check-config
python scripts\create_api_snapshot.py --market-id 1 --check-config
```

拉取前轻量 API 探测：

```text
python scripts\create_api_snapshot.py --market-id 1 --probe-api
python scripts\create_api_snapshot.py --probe-config-markets
```

规则：

- 脚本只读取新项目 `.env` 或当前进程环境变量。
- 脚本不读取、不复制旧项目 `.env`。
- 缺少 `GERPGO_MARKET_IDS` 或凭据时直接失败，不生成伪快照。
- 缺少凭据时，`--check-config` 和 `/api/snapshot/readiness` 必须返回 `next_action`，说明只允许在新项目 `.env` 或进程环境变量中补齐凭据。
- `--check-config` 只检查配置，不请求积加 API，不写入快照。
- `--probe-api` 只请求 `marketNames/query` 做轻量权限探测，不写入快照，不拉取广告明细。
- `--probe-api` 只输出店铺站点摘要和行数，不输出原始凭据或原始响应中的敏感字段。
- `--probe-api` 只有在 `marketNames/query` 返回行中匹配到目标 `market_id` 时才允许返回 `ready`。
- `marketNames/query` 的 `data` 可能是字符串，例如 `rivbos:US`，也可能是数组；解析器必须同时支持这两种形态。
- `salesAnalysis/page` 的 `data` 可能不是数组，而是分页对象，例如 `{total, page, rows}`；标准化时必须读取 `data.rows`，不能把它误判为空数据。
- `marketNames/query` HTTP 成功但返回 0 行或未匹配目标 `market_id` 时，必须返回 `not_ready`，不能写 probe cache，不能继续当作真实店铺站点已确认。
- `--probe-api` 探测成功后必须复用同一份安全 market option 缓存，保证命令行探测和工作台顶部筛选口径一致。
- `--probe-config-markets` 只读取新项目 `.env` 或当前进程环境变量中的 `GERPGO_MARKET_IDS`，逐个做 `marketNames/query` 只读探测。
- `--probe-config-markets` 只输出候选 `market_id` 的 ready / not_ready / failed 汇总，不写快照，不写 `data/runtime/market_options.json`。
- `POST /api/snapshot/probe` 和 `--probe-api` 使用同一套安全输出口径；页面只展示状态、店铺站点摘要、缺失配置和下一步动作。
- 已有同店铺、同周期成功快照时默认复用；需要刷新时显式加 `--force`。
- 创建或复用成功时，CLI JSON 输出也必须带 `inspection`，和 `POST /api/snapshot/create` 保持同一套验收合同。

快照只读验收入口：

```text
python scripts\inspect_api_snapshot.py
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
GET /api/snapshot/inspection
GET /api/snapshot/readiness?market_id=1
```

规则：

- 只读取本地快照，不请求积加 API。
- 输出最新快照状态、manifest 行数、normalized 实际行数、可进入信号规则的行数。
- API 入口返回同一套验收结果，供工作台展示 `ready_for_signals`、可分析行数和 issues。
- 缺少配置时，API 入口必须返回不泄密的 `next_action`，供工作台顶部状态直接提示下一步动作。
- `status = missing` 表示没有本地快照。
- `status = empty` 表示成功快照存在，但没有可进入信号规则的搜索词或广告位行。
- `status = partial` 表示 manifest 与 normalized 文件存在不一致或缺失。
- `ready_for_signals = true` 才能继续判断真实业务异常和机会。

真实信号扫描入口：

```text
python scripts\run_signal_scan.py --market-id 1
GET /api/signals?market_id=1
```

规则：

- 只读取本地 API 快照和本地 ABA Top1000 索引，不请求积加 API。
- `--market-id` 用于保留本次人工选择的店铺站点上下文；不传时依赖 `.env` 中的 `GERPGO_MARKET_IDS`。
- 工作台 API 入口使用 `market_id` 查询参数保留当前顶部筛选器上下文。
- 前端读取 `/api/signals` 和 `/api/snapshot/readiness` 时必须传入当前筛选器的 `market_id`。
- 输出必须包含 `inspection`、`ready_for_signals`、`signal_row_count`、`aba_row_count`、`signal_count` 和完整 `signals`。
- 无成功 API 快照时，不能返回空数组掩盖问题，必须返回数据质量信号。
- `ready_for_signals = false` 时，只能解释数据链路状态，不能声称发现了真实业务异常或机会。
- 有成功 API 快照后，扫描入口必须和 `/api/signals` 使用同一套信号生成链路。

## 6. 凭据规则

新项目使用本地 `.env` 配置积加凭据。

允许配置键：

```text
GERPGO_BASE_URL
GERPGO_APP_ID
GERPGO_APP_KEY
GERPGO_ACCESS_TOKEN
GERPGO_MARKET_IDS
```

禁止：

```text
在代码中写死 AppKey
在文档中粘贴 AppKey
在日志中打印 accessToken
把 .env 提交到仓库
自动复制旧项目 .env 到新项目
```

可以只读参考旧项目的 API 客户端和接口路径，但不能复用旧项目的产品中心模型。

## 7. 原型使用规则

前端和 AI 信号生成优先使用真实快照。

数据优先级：

```text
真实 API 快照
→ ERP 导入文件
→ 明确标记的 mock 兜底数据
```

mock 只能用于：

- API 凭据缺失时的页面结构预览。
- 本地开发时验证交互。
- 自动化测试中的固定样本。

mock 不能用于：

- 证明真实业务结论。
- 证明某个信号真实存在。
- 替代 API 快照作为正式演示证据。

## 8. 信号证据规则

使用真实快照生成 AI 信号时，每条证据必须能回到：

```text
snapshot_id
api_name
source_file 或 raw_payload_file
source_table
source_record_id
market_id
start_date
end_date
```

如果某个信号使用 mock 数据，前端必须显示为：

```text
样例数据
```

不能显示为：

```text
积加API
```

## 9. 与旧项目的边界

允许从旧项目只读参考：

- 积加 API 请求地址。
- 请求参数。
- 分页方式。
- 限流经验。
- 字段映射经验。

禁止从旧项目复用：

- 产品中心模型。
- 把广告来源草稿当商品主数据的逻辑。
- 旧项目页面结构。
- 旧项目产品归因逻辑。

## 10. 一键验收入口

真实 API 快照的人工低频拉取、验收和信号扫描统一使用：

```text
python scripts\create_api_snapshot.py --market-id 1 --probe-api
python scripts\run_snapshot_pipeline.py --market-id 1 --days 7 --count 10 --max-pages 1
POST /api/snapshot/pipeline
```

输出必须同时包含：

```text
pipeline_status
create
inspection
signal_scan
```

规则：

- 缺少积加 API 凭据时返回 `pipeline_status=not_ready`，不能请求 API，并在 `create.next_action` 中说明下一步配置动作。
- 完整快照拉取前，优先用 `--probe-api` 确认 token、`market_id` 和开放平台接口权限。
- 凭据存在但积加 API 请求失败时返回 `pipeline_status=failed`，不得暴露密钥值，仍必须返回当前本地 `inspection` 和 `signal_scan`。
- `pipeline_status=failed` 的 `create.message` 必须优先保留可安全展示的根因，例如目标店铺站点未命中；如果失败文本包含 token / appKey / appId / secret 等敏感标记，只能返回泛化安全文案。
- 创建或复用快照后必须立刻执行 inspection。
- inspection 后必须执行 signal_scan。
- signal_scan 必须继承本次传入的 `market_id`，避免配置排查时误报店铺站点缺失。
- ABA 只能作为增强数据，不能替代 API 快照让 `ready_for_signals=true`。

## 11. 验收标准

真实数据快照方案落地后，第一阶段原型必须能回答：

```text
当前页面使用的是哪次快照？
快照来自哪个店铺和站点？
快照周期是什么？
每类 API 拉到了多少行？
某条 AI 信号用了哪些真实 API 证据？
如果 API 失败，当前展示的是上一次快照还是样例数据？
```
