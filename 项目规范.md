# 项目规范

更新日期：2026-06-14

## 1. 规范定位

本文件用于约束项目开发过程中的代码、数据、页面、测试和文档规范。

如果本文件与 `AGENTS.md` 冲突，优先遵守 `AGENTS.md`。

如果本文件与实际数据或 API 结果冲突，以实际数据和 API 结果为准，并更新规范。

## 2. 基本原则

- 所有开发以最小可验证闭环为目标。
- 每次只解决一个明确问题。
- 不为未来不确定需求提前过度设计。
- 所有 AI 信号必须能追溯到证据。
- 所有证据必须能追溯到数据来源。
- 所有自动化都不能绕过人工确认。
- 所有数据模型必须先定义对象边界，再写代码。
- 每一轮任务都必须先做 Product Design 简报校准，并把本轮进度回写到现有管理文档。

## 3. 目录规范

建议目录结构：

```text
backend/                 后端服务
frontend/                前端应用
scripts/                 数据同步、导入、检查脚本
data/                    本地开发数据和样例数据
tmp/                     临时文件，不作为长期资产
design/                  原型图、设计参考
项目规划.md              项目定位和总体方案
项目规范.md              开发、数据、UI、测试规范
开发原型.md              原型范围和验收标准
开发经验记录.md          开发过程经验和高频错误
AI信号类型矩阵.md        AI 信号触发、证据、建议和测试样例
数据模型与表结构.md      核心对象和逻辑表结构
API同步范围.md           积加开放平台 API 同步范围和暂缓规则
ABA导入流程.md           ABA 人工导入流程和字段映射
真实数据快照方案.md      低频真实 API 快照和 mock 兜底规则
原型技术栈.md            前端、后端、数据层和测试技术选型
问题解决记录.md          问题、原因、解决方案和后续防错规则
AGENTS.md                AI 协作和执行规则
```

规则：

- `tmp/` 里的内容不能作为正式数据源。
- `design/` 只放原型和设计参考，不放业务数据。
- `scripts/` 里的脚本必须可重复执行，不能隐藏写入副作用。
- 任何新增长期目录，都必须有明确用途。

## 4. 命名规范

### 4.1 代码命名

- Python 文件：`snake_case.py`
- TypeScript / React 文件：按项目技术栈保持一致，组件优先使用 `PascalCase`
- 测试文件：与被测对象对应，清晰表达测试目标
- 脚本文件：动词开头，表达动作，例如 `sync_`、`import_`、`smoke_`、`check_`

### 4.2 数据对象命名

统一使用清晰对象名，不使用模糊缩写。

推荐对象名：

```text
Shop
Marketplace
SalesProduct
AdvertisedProduct
Campaign
AdGroup
Keyword
ProductTargeting
SearchTerm
AbaSearchTermSnapshot
KeywordRankSnapshot
ProductSemanticProfile
AiSignal
Evidence
ReviewRecord
```

禁止使用容易误导的命名：

```text
Product 但实际存广告组
ProductAttribution 但实际是广告组归因
Source 但不说明来源类型
Item 但不说明对象边界
```

## 5. 数据模型规范

核心逻辑表结构以 `数据模型与表结构.md` 为准。

### 5.1 必带上下文字段

所有核心事实表必须优先考虑：

```text
shop_id
shop_name
market_id
marketplace
country
date
start_date
end_date
source_type
source_file
imported_at
```

不是所有表都必须全部拥有这些字段，但缺失时必须有明确原因。

### 5.2 对象边界

必须严格区分：

```text
SalesProduct      经营商品 / 销售商品
AdvertisedProduct 广告商品
AdGroup           广告组容器
SearchTerm        用户搜索词
AbaSearchTerm     ABA 市场搜索词
```

广告组不能直接代表产品。

ABA 不能直接代表店铺数据。

广告搜索词不能直接等同于关键词。

广告组内单个 ASIN 消耗集中不能直接判定为异常。若该 ASIN 是主推款，集中消耗属于正常投放策略；缺少主推款标记时，只能生成策略待确认信号；已有主推款标记时，不再生成该待确认信号。

### 5.3 关联键规范

常用关联键：

```text
店铺：shop_id
站点：market_id / marketplace / country
经营商品：asin / msku / sku
广告商品：campaign_id / group_id / ad_id / asin / msku
广告组：campaign_id / group_id
关键词：campaign_id / group_id / keyword_id
商品定向：campaign_id / group_id / target_id
搜索词：normalized_query + market_id + date/week
ABA：normalized_query + marketplace/country + start_date + end_date
```

任何跨表关联必须说明：

- 关联字段
- 关联粒度
- 是否可能一对多
- 是否可能缺失

## 6. 数据源规范

### 6.1 开放平台 API

第一阶段同步范围以 `API同步范围.md` 为准。

原型真实数据使用方式以 `真实数据快照方案.md` 为准。

开放平台 API 进入同步链路前，必须记录：

```text
接口名称
接口路径
请求参数
返回字段
主键或准主键
关联键
数据周期
分页方式
真实请求验证结果
```

禁止只根据接口名称或页面名称判断数据含义。

### 6.2 ERP 导出文件

导出文件进入导入链路前，必须记录：

```text
文件名
文件来源
导出时间
工作表名
表头字段
数据行数
数据周期
站点
店铺归属规则
字段映射
```

导入脚本必须保留：

```text
source_file
source_sheet
imported_at
source_row_number
```

### 6.3 ABA 导入

ABA 导入流程以 `ABA导入流程.md` 为准。

ABA 必须作为独立快照导入。

最低字段：

```text
country
keyword
normalized_query
start_date
end_date
search_rank
rank_change_type
rank_change_value
top3_click_share
top3_conversion_share
top1_asin
top1_title
top1_click_share
top1_conversion_share
top2_asin
top2_title
top2_click_share
top2_conversion_share
top3_asin
top3_title
top3_click_share
top3_conversion_share
source_file
imported_at
```

如果某列为空，例如当前样本里的 `搜索查询量`，不能在模型中假装可用。

## 7. AI 信号规范

新增或调整信号类型时，必须同步更新 `AI信号类型矩阵.md`。

### 7.1 信号生成原则

每个信号必须包含：

```text
判断对象
判断周期
触发条件
异常或机会描述
证据列表
建议动作
置信度
严重度
不确定性
人工状态
```

禁止生成没有证据的 AI 信号。

禁止生成不可解释的黑箱结论。

### 7.2 证据规范

每条证据必须包含：

```text
evidence_type
source_type
source_name
metric_name
metric_value
comparison_value
time_range
object_type
object_id
explanation
```

证据来源必须明确标记为：

```text
积加API
ABA导出
关键词监控
销售表现
人工语义
```

### 7.3 建议动作规范

建议动作必须是人工可判断动作，例如：

```text
建议观察
建议拆分广告组
建议检查 Listing 承接
建议加入复盘
建议降低优先级
建议人工判断是否否词
建议人工判断是否加词
```

禁止建议直接执行：

```text
立即调价
立即暂停
立即否词
立即新增关键词
```

如果建议涉及广告操作，必须写成：

```text
建议人工检查后处理
```

### 7.4 深度分析框架规范

新增或调整任何 AI 信号时，必须先回答六个问题：

```text
分析对象是什么？
经营问题是什么？
使用哪些证据源？
是否存在反证或归因边界？
为什么是当前置信度和严重度？
后续如何人工确认和复盘？
```

信号生成必须遵守：

- 每条信号只能有一个主分析对象。
- 每条信号必须归入一个经营问题类型：花费浪费、销售承接不足、机会扩量、投放结构失衡、市场竞争压力、数据质量缺口、处理后复盘。
- 搜索词、广告位、广告组默认是证据和上下文；只有对象边界明确时，才能上升为商品经营判断。
- 数据质量信号不能包装成经营异常。
- 样本量不足、数据过期、证据源单一或归因不确定时，必须降低置信度或说明不确定性。
- 没有人工动作和完整 7/14 天窗口时，不能生成处理有效或无效的复盘结论。

## 8. 前端规范

前端技术栈以 `原型技术栈.md` 为准。

### 8.1 信息架构

v1 前端必须围绕 AI Copilot 工作台：

```text
顶部筛选器
左侧信号队列
中间诊断证据
右侧人工处理
```

不要做成普通报表首页。

不要把聊天框作为第一入口。

不要复刻旧项目产品中心。

### 8.2 交互规范

必须支持：

- 切换店铺
- 切换站点
- 切换周期
- 点击信号查看详情
- 查看证据来源
- 人工标记状态

不得出现：

- 自动执行按钮
- 自动调价按钮
- 自动暂停按钮
- 自动否词按钮
- 自动新增关键词按钮

### 8.3 展示规范

信号卡必须突出：

```text
严重度
信号类型
对象
核心问题
影响指标
置信度
状态
数据新鲜度
```

详情页必须按：

```text
结论 → 原因 → 证据链 → 影响范围 → 不确定性
```

### 8.4 原型隔离规范

当前已有前端样例页面不能作为新原型的信息架构依据。

规则：

- 可以复用 React / Vite 工程能力。
- 不复用 `TodaySignals` 页面布局。
- 不复用 RB831 样例业务语境。
- 不复用旧样例组件拆分方式。
- 第一版原型入口使用 `SignalTriageWorkbench`。
- 旧样例文件被删除后不得重新创建，除非用户明确要求做兼容迁移。

## 9. 后端规范

后端技术栈以 `原型技术栈.md` 为准。

### 9.1 服务边界

后端模块建议按职责拆分：

```text
数据同步
文件导入
数据模型
信号检测
证据构建
建议生成
人工确认
复盘分析
```

不要把 API 调用、清洗、规则判断、AI 文案生成全部写在一个文件里。

### 9.2 脚本规范

脚本必须明确是否写入数据。

只读检查脚本命名建议：

```text
check_*
inspect_*
smoke_*
```

写入或同步脚本命名建议：

```text
sync_*
import_*
backfill_*
```

写入脚本必须具备：

- 输入参数说明
- 数据范围说明
- 幂等策略
- 最小日志输出
- 失败停止机制

### 9.3 后端目录职责

第一阶段后端按现有 FastAPI 结构推进，不为了形式重构目录。

```text
backend/app/main.py          创建 FastAPI app、注册中间件和路由
backend/app/api/             路由层，只处理请求参数、响应模型和 HTTP 错误
backend/app/models/          Pydantic 模型，不作为数据库 ORM 模型使用
backend/app/services/        业务服务、信号检测、证据构建、样例数据读取
backend/tests/               后端 pytest 测试
scripts/                     可手动执行的数据检查、同步、导入脚本
```

后续需要新增目录时，按职责添加：

```text
backend/app/core/            配置、环境变量、通用常量
backend/app/data_sources/    真实 API 快照、ERP 导出文件、mock 样例数据读取
backend/app/rules/           信号触发规则和阈值判断
backend/app/repositories/    后续引入数据库后再添加
```

规则：

- `api/` 不写信号规则。
- `models/` 只定义请求、响应和领域对象结构。
- `services/` 可以组合数据读取、规则判断和证据构建，但不能直接持有前端状态。
- `data_sources/` 只负责读取和标准化来源数据，不生成 AI 信号。
- `rules/` 中的规则函数必须尽量是纯函数：输入数据，输出信号或证据，不做文件读写。
- `scripts/` 只作为命令入口，复杂逻辑必须放回 `app/` 下复用。

## 10. 测试规范

测试工具和范围以 `原型技术栈.md` 为准。

测试按风险选择，不追求形式化大而全。

优先覆盖：

- 数据字段映射
- 关键关联关系
- ABA 导入
- 搜索词标准化
- 信号触发条件
- 证据来源标记
- 人工动作不能自动执行

每个新增信号类型至少要有：

- 一个会触发的样例
- 一个不会触发的样例
- 一个数据不足导致低置信度的样例

## 11. 数据新鲜度规范

所有信号必须显示数据新鲜度。

至少区分：

```text
API 最新同步时间
ABA 最新导入时间
关键词监控最新导入时间
销售表现最新更新时间
```

数据过期时：

- 不隐藏信号
- 必须降低置信度
- 必须在不确定性中说明

## 12. 复盘规范

人工处理后，必须支持后续复盘。

复盘周期：

```text
7 天
14 天
```

复盘重点：

- 花费是否下降
- 订单是否改善
- ACOS 是否改善
- 信号是否重复出现
- 建议是否有效

复盘不能只记录动作，必须关联处理前后的指标变化。

### 12.1 MVP 人工留痕规则

MVP 阶段先做最小可验证留痕，不引入复杂工作流。

人工处理记录必须包含：

```text
signal_id
action_type
action_note
operator_name
acted_at
snapshot_id
shop_id
market_id
object_type
object_id
object_label
```

第一阶段允许用本地 JSONL 或轻量文件保存人工动作。

后续需要多用户、并发写入、查询筛选和长期复盘时，再升级 SQLite 或正式数据库。

禁止：

```text
只在前端 useState 保存人工状态
把人工处理记录写成自动广告执行记录
点击人工动作后自动调用广告操作 API
```

## 13. 安全和权限规范

- 不在代码中写死 API key、accessToken、店铺敏感凭据。
- 不在文档中粘贴完整密钥。
- 不把真实敏感数据提交到仓库。
- 不做绕过平台权限的抓取。
- 不做自动执行广告动作。

## 14. 文档维护规范

修改以下内容时，必须同步更新文档：

```text
项目定位变化 → 项目规划.md
对象模型变化 → 项目规划.md / 项目规范.md
表结构变化 → 数据模型与表结构.md / 项目规范.md
信号类型变化 → AI信号类型矩阵.md / 项目规划.md
API 同步范围变化 → API同步范围.md / 项目规划.md / 项目规范.md
ABA 导入流程变化 → ABA导入流程.md / 项目规划.md / 项目规范.md
真实数据快照规则变化 → 真实数据快照方案.md / 项目规划.md / 开发原型.md
原型技术栈变化 → 原型技术栈.md / 项目规范.md / 开发原型.md
代码结构或编程风格变化 → 项目规范.md / 开发经验记录.md
原型范围变化 → 开发原型.md
开发踩坑或决策变化 → 开发经验记录.md
问题、错误或重复风险变化 → 问题解决记录.md / 开发经验记录.md
执行规则变化 → AGENTS.md
```

文档必须简洁，不写空泛内容。

### 14.1 每轮进度回写

每轮任务必须在 `开发经验记录.md` 记录最小进度：

```text
本轮任务
Product Design 校准结论
已完成事实
下一步
```

如果本轮发现新问题或重复风险，同步更新 `问题解决记录.md`。

### 14.2 每轮三问复盘

每轮任务开始和结束时必须用以下三问校准方向：

```text
当前的数据口径真的清晰吗？
数据关系梳理清楚了吗？
真的能落地到业务上吗？
```

三问的最低判断标准：

- 数据口径：必须能说明来源、周期、粒度、指标定义和是否可复现。
- 数据关系：必须能说明 Parent ASIN、Child ASIN、SalesProduct、AdvertisedProduct、AdGroup、SearchTerm、signal_id 和 stable_object 的关系。
- 业务落地：必须能说明运营看完能做什么人工动作、何时复盘、复盘结果如何反哺规则。

如果三问中任一项答不清楚，本轮不得继续扩展页面或新增浅层分析，必须先收口数据口径、关系或复盘闭环。

### 14.3 每轮多视角复盘与规划

每轮复盘和下一步规划必须至少覆盖四个视角：

```text
开发工程师视角：实现范围是否最小、可维护、可验证？
架构师视角：对象边界、数据关系和闭环是否稳定？
前端设计师视角：信息层级、交互路径和工作台结构是否清楚？
用户视角：运营是否能基于证据做人工动作，并在 7/14 天后复盘？
```

多视角复盘不能替代三问复盘。三问用于校准业务事实，多视角用于校准实现、结构、体验和落地。

## 15. 开发完成标准

一个任务完成至少满足：

- 当前任务目标已完成
- 修改范围可解释
- 最小验证已执行
- 失败项已明确说明
- 必要文档已更新
- 没有引入自动广告执行能力
- 没有混入旧项目错误模型

## 16. 代码结构与编程风格规范

本节用于约束进入正式开发后的代码写法，目标是简单、可读、可测试。

### 16.1 后端编程风格

- 路由函数保持薄层，只做参数接收、调用 service、返回 response。
- 信号规则函数必须有明确输入和输出，不在规则内部读取文件、请求 API 或写数据库。
- 真实 API 快照读取、ERP 文件读取、mock 样例数据读取必须分层，不能混在一个函数里。
- 阈值优先集中配置，不在循环中散落硬编码。
- Pydantic 模型字段使用英文 `snake_case`，用户可见文案使用中文。
- 枚举值使用稳定英文值，例如 `pending`、`handled`、`ignored`。
- 函数和变量命名直接表达业务对象，不使用 `item`、`data`、`obj` 这类长期模糊命名。
- 异常处理只处理当前能解释的问题，不做宽泛吞错。
- 不写“看起来像 AI”的黑箱结论，每条信号必须带证据。
- 后端不得出现任何自动广告执行能力。

### 16.2 前端目录职责

第一阶段前端按现有 Vite 结构推进：

```text
frontend/src/main.tsx        React 挂载入口
frontend/src/App.tsx         应用入口，只组合页面
frontend/src/api.ts          第一阶段 API client 和接口类型
frontend/src/pages/          页面级容器，负责加载数据和页面状态
frontend/src/components/     可复用展示组件，不直接请求后端
frontend/src/styles.css      全局样式和页面样式
```

后续文件变多时再拆分：

```text
frontend/src/api/client.ts
frontend/src/api/signals.ts
frontend/src/types/signals.ts
frontend/src/constants/
frontend/src/components/signal/
```

规则：

- 页面容器可以请求数据，展示组件只接收 props。
- 前端类型必须和后端 Pydantic 响应字段保持一致。
- API 字段默认保持后端 `snake_case`，不要在前端私自改名导致证据字段断链。
- 前端不能直接请求积加 API，不能持有 `AppKey` 或 `accessToken`。
- mock 数据必须明确标记为样例数据，不能伪装成真实 API 数据。

### 16.3 前端编程风格

- 只使用函数组件和 React Hooks。
- 页面状态优先使用 `useState`、`useMemo`，第一阶段不引入 Redux。
- 数据请求集中在页面容器或 `api.ts`，不要散落在多个小组件里。
- 事件函数使用 `handle*` 命名，数据加载函数使用 `load*` 命名。
- label 映射、状态映射、枚举映射集中放置，不在 JSX 中重复写复杂判断。
- 按钮优先使用 lucide 图标，复杂业务按钮必须有清晰 `aria-label`。
- CSS 使用普通 class，避免大段内联样式。
- 卡片、面板、按钮保持稳定尺寸，避免数据加载后布局跳动。
- 页面文案直接服务运营判断，不写功能说明式废话。

### 16.4 API 合同规则

后端 Pydantic 模型是前后端接口合同。

新增或调整 API 时必须同步检查：

```text
后端 response_model
前端 TypeScript 类型
页面使用字段
测试样例
```

AI 信号接口最低必须表达：

```text
信号身份
店铺和站点上下文
判断对象
严重度和置信度
结论和原因
证据列表
建议动作
人工状态
数据来源和新鲜度
```

禁止前端根据展示需要虚构后端没有返回的业务事实。

### 16.5 mock 和真实数据边界

- `sample_*`、`mock_*` 只能用于页面开发和自动化测试。
- 真实业务结论只能来自真实 API 快照、ERP 导入文件或人工维护资料。
- 主推款 ASIN、策略标签和生效周期属于人工维护资料，第一阶段保存在 `data/runtime/promotion_strategy_profiles.json`，不进入 API 自动同步。
- mock 数据参与前端展示时必须显示为 `样例数据`。
- 信号证据中的 `source_type` 不能把 mock 写成 `积加API`。

### 16.6 测试放置规则

后端测试放在 `backend/tests/`：

```text
test_signal_detection.py     信号规则测试
test_snapshot_loader.py      真实快照读取测试
test_aba_import.py           ABA 导入测试
test_api_routes.py           路由 smoke 测试
```

前端第一阶段以构建检查和必要浏览器检查为主。若新增纯函数格式化、筛选或映射逻辑，再补对应测试。

测试优先覆盖：

- 信号触发和不触发。
- 证据来源标记。
- mock 与真实数据区分。
- 人工动作不能变成自动广告动作。
- API 响应字段和前端类型一致。
