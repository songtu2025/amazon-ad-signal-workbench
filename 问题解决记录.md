# 问题解决记录

更新日期：2026-06-26

## 2026-06-26：ReviewRecord 保存后读回不能漏默认核对层

```text
问题：
ReviewRecord 保存前已经要求默认核对层，但保存后读回文案没有列出默认核对层，也没有显示“默认先查哪层”的值。
用户回看历史复盘记录时，仍可能只看到效果结论和长证据列表，不能快速确认当时默认复核入口。

原因：
1. 保存前门槛和保存后读回是两段逻辑。
2. reviewRecordEvidenceSnapshotReadbackText 的搜索词 readbackLabels 没有默认核对层。
3. hasReviewRecordEvidenceSnapshot 对搜索词记录没有要求 label=默认核对层，可能让缺默认入口的记录被视为证据一致。

解决：
1. 搜索词 ReviewRecord 保存后读回标签加入“默认核对层”。
2. 保存后读回文案显示“默认核对层：默认先查...”的值。
3. 搜索词 ReviewRecord 匹配读回必须包含 label=默认核对层。
4. reviewUi 测试覆盖保存后可读回默认核对层和缺失默认核对层的缺口提示。

防错规则：
1. 保存前必查的关键证据，保存后也必须可读回。
2. 读回不能只列标签，关键入口需要显示值，用户才知道当时默认先查哪层。
3. ReviewRecord 读回只证明保存时证据快照可追溯，不代表自动改规则或执行广告动作。
4. 修复闭环断点时只补对应链路，不扩成新的报表或规则反馈系统。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-26：复盘保存门槛不能漏掉默认核对层

```text
问题：
复盘待办读回已经能显示“默认核对层”，但 ReviewRecord 保存前检查没有把它作为必备项。
如果处理后指标 ready，系统可能允许保存缺少默认核对层的搜索词复盘记录，导致 ReviewRecord 只像效果数字记录。

原因：
1. 展示读回和保存门槛是两套 helper，上一轮只补了读回，没有补保存检查。
2. requiredReviewRecordPreflightCheckIdsForEffect 的搜索词链路缺少 default_review_layer。
3. buildReviewRecordPreflightChecklist 没有从 ReviewTodo.evidence_snapshot 检查 label=默认核对层。

解决：
1. 新增 default_review_layer / default_review_layer_missing 检查项。
2. 搜索词 ReviewRecord 保存前必备项加入 default_review_layer。
3. buildReviewRecordPreflightChecklist 读取 label=默认核对层，缺失时返回 default_review_layer_missing。
4. reviewUi 测试覆盖完整链路可保存、缺少默认核对层不可保存。

防错规则：
1. 复盘读回可见的关键业务入口，也必须进入保存门槛，否则 ReviewRecord 仍可能变成裸指标记录。
2. 默认核对层仍只是人工复盘入口，不是筛选器、归因结论或自动广告动作。
3. 保存门槛优先补现有 evidence_snapshot label，不新增字段、不改后端模型。
4. 每轮发现断点时先判断它是否影响主链路闭环，再做最小修复，避免矫枉过正。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-26：复盘读回不能丢掉默认核对层

```text
问题：
ReviewTodo.evidence_snapshot 已经保存“默认核对层”，但复盘待办业务判断读回没有把它作为独立行展示。
用户到期复盘时仍要从完整证据串里找“当时默认先核对哪一层”，决策成本会上升。

原因：
1. 证据快照保存完整，不等于复盘阅读路径清晰。
2. 复盘读回更关注“到期先回看哪里”，不能只按完整审计材料展开。
3. 这个问题不需要新增 API 字段或模型，只需要读回已有 label=默认核对层。

解决：
1. buildReviewTodoDecisionReadbackSummary 读取“默认核对层”，作为独立业务判断行。
2. 搜索词复盘待办缺少默认核对层时，读回状态变为 blocked。
3. 复盘待办诊断路径预览加入“默认核对层”，让主复盘区也能看到默认入口。
4. reviewUi 和结构测试增加断言，防止该层再次被藏回完整证据串。

防错规则：
1. 默认核对层必须贯穿中间诊断、右侧人工确认和 7/14 天复盘读回。
2. 默认核对层是人工阅读入口，不是筛选器、归因结论或广告动作。
3. 修阅读断点时优先读回已有证据，不重画页面、不新增指标、不改数据模型。
4. 每轮复盘不要矫枉过正，围绕总目标、MVP 链路、数据口径、对象关系、证据链和人工动作判断。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-26：默认核对层不能只藏在完整预检证据里

```text
问题：
后端 preflight 和 reviewUi 已经能保存“默认核对层”，但右侧人工确认默认阅读顺序先展示诊断路径、关键证据和人工确认四问。
如果默认核对层主要出现在折叠的完整预检证据里，用户点击前仍可能不知道应该先核对哪一层。

原因：
1. 保存合同完整不等于默认阅读路径清晰。
2. “默认核对层”是按钮前人工复核入口，不应该只作为审计证据藏在折叠区。
3. 这个问题不需要新增后端字段或广告动作，只需要调整右侧展示顺序。

解决：
1. 右侧人工确认区从 selectedManualActionPreflightPriorityEvidenceRows 中读取 label=默认核对层。
2. 在“人工按钮前诊断路径核对”和“点击前关键证据核对”之间展示“人工确认默认核对层”。
3. 该层只读后端 preflight 返回的 value / detail / source，不在前端编造判断。
4. 结构测试锁定默认核对层必须先于关键证据、人工确认四问和人工动作按钮。

防错规则：
1. 点击前默认层必须可见，不能只藏在完整预检、证据账本或审计折叠区。
2. 默认核对层只是人工复核入口，不是筛选器、归因结论或广告动作对象。
3. 前置默认层不等于扩大右侧；完整证据仍留在折叠审计区。
4. 发现阅读断点时优先桥接现有证据，不重画页面、不改模型、不新增自动动作。

验证方式：
npm run test:workbench-structure
npm run test:review-ui
npm run build
git diff --check
```

## 2026-06-26：中间单屏卡不能丢掉首页默认下钻层

```text
问题：
左侧 Parent ASIN 首页摘要已经能回答“打开后看哪层”，但中间 Parent ASIN 单屏作战卡仍先展示四步诊断路径和六块明细判断。
用户进入 Parent ASIN 后还要自己判断先看搜索词、广告组、复盘、补证还是观察层，阅读成本会重新上升。

原因：
1. 首页摘要和中间作战卡之间缺少默认下钻层桥接。
2. 单屏卡虽然比长报表更收敛，但如果先铺路径和明细，用户仍要二次选择入口。
3. 这个问题不需要重画页面或改数据模型，只需要复用已有默认下钻规则。

解决：
1. ProductScopeSingleScreenCommandCard 复用 productScopePriorityDefaultDrilldown。
2. 在四步诊断路径前新增“默认先看”层，显示默认下钻值和人工核对说明。
3. 移动端默认下钻层改为单列，保证长路径文本不挤压。
4. 结构测试锁定默认下钻层必须先于诊断路径和明细判断网格。

防错规则：
1. 解决阅读成本时不要矫枉过正；优先桥接下一步，不要推翻三栏工作台。
2. 默认下钻层只是阅读引导，不是筛选器、归因结论或广告动作对象。
3. 收敛默认入口不等于删除完整证据；完整销售、广告 ASIN、广告组、投放词、搜索词和广告位仍要可追溯。
4. 每轮复盘必须围绕最终目标、MVP 链路、数据口径、对象关系、证据链和用户下一步动作结构化推进。

验证方式：
npm run test:workbench-structure
npm run build
```

## 2026-06-26：首页摘要不能只说打开哪个 Parent ASIN

```text
问题：
左侧 Parent ASIN 首页摘要已经能告诉用户“今日先看哪个 Parent ASIN”，但“打开后看哪层”仍使用通用四层路径。
用户进入 Parent ASIN 后还要自己判断先看搜索词、复盘、结构、补证还是观察层，容易重新回到读多张报表的路径。

原因：
1. 首页摘要的排序能力已经存在，但默认下钻层没有跟随当前优先对象变化。
2. 对搜索词扩量 / 止损类对象来说，默认入口应先落到具体 SearchTerm，再回看广告组、投放词、广告 ASIN 和广告位边界。
3. 通用四层路径能证明链路完整，但不能降低下一步选择成本。

解决：
1. 新增 productScopePriorityDefaultDrilldown，根据当前优先对象推导默认下钻层。
2. 搜索词类优先对象显示“搜索词 -> 广告组 / 投放词 / 广告 ASIN / 广告位边界”。
3. 到期复盘、结构问题、证据缺口、观察层分别显示对应默认入口。
4. 结构测试锁定首页摘要必须使用 defaultDrilldown，不退回固定四层说明。

防错规则：
1. 首页摘要必须回答两个问题：今天先看谁，打开后先看哪层。
2. 默认下钻层只是阅读引导，不是归因结论、筛选口径或广告动作。
3. 收窄默认层不等于删除完整证据；完整销售、广告 ASIN、广告组、投放词、搜索词和广告位仍保留回查。
4. 发现阅读成本高时，优先把下一步讲清楚，不扩成新的报表页。

验证方式：
npm run test:workbench-structure
npm run build
```

## 2026-06-26：人工证据快照缺少默认核对层会断开复盘读法

```text
问题：
中间诊断区已经能告诉用户当前默认先查哪一层，但右侧人工确认的 evidence_snapshot_preview 没有保存“默认核对层”。
用户点击人工动作前后，仍可能需要回到中间诊断区重新找证据入口；7/14 天复盘也无法稳定读回当时的默认入口。

原因：
1. manual_action_preflight 已保存诊断合同里的判断、能证明、不能证明和人工下一步，但缺少默认复核入口。
2. 前端人工确认前优先证据没有把默认核对层排在显眼位置。
3. 首次实现如果按 current_judgement 全文模糊匹配，会把 Parent ASIN 总览中提到的“搜索词”误判成搜索词层，默认入口会选错。

解决：
1. 后端 preflight 在 diagnosis_contract 快照里新增“默认核对层”。
2. 默认层只按 diagnosis_contract 的 section_id / title 识别，不按整段 current_judgement 里的泛文本匹配。
3. 前端人工确认前优先证据把“默认核对层”放在 AI 准入之后，同时保持默认 8 条，避免右侧增加阅读负担。
4. 前后端测试锁定：搜索词信号应保存“默认先查 3. 搜索词”，复盘快照条数同步更新。

防错规则：
1. 默认核对层是人工复核入口，不是新的筛选器、归因结论或广告动作。
2. 选择默认层时必须基于真实诊断块 ID / 标题，不能用任意文本包含关系做层级判断。
3. 补链路不等于扩页面；右侧仍只保留人工可执行动作和关键证据。
4. 每轮复盘先回到最终目标、MVP 链路、数据口径、对象关系和可落地动作，避免因为局部断点而整体推翻方向。

验证方式：
pytest backend/tests/test_manual_action_preflight_cli.py -q
npm run test:review-ui
npm run build
真实数据只读检查：B00K4W4AAA + b01fay0yl0 的 preflight 里“默认核对层”指向“搜索词机会”。
```

## 2026-06-26：广告组具体数据不能让用户重新找入口

```text
问题：
广告组下方已经有四类证据切换器和完整复核顺序，但五段诊断框架里的“广告组下具体数据”仍主要展示证据路径摘要。
用户在上方框架里不能直接知道当前广告组内部应该先查投放商品、投放词、搜索词还是广告位。

原因：
1. 下方组件已经能推导“本次优先证据层”，但上方诊断框架没有承接这个判断。
2. 证据路径摘要能证明链路存在，但不能减少用户从四类明细里重新找入口的成本。
3. 当前真实数据里第一广告组同时有有效词和无订单花费词，默认层应该优先提示先查搜索词。

解决：
1. 在 signalUi 中新增广告组具体数据默认核对层推导。
2. 五段诊断框架第 3 段改为“默认先查 X 层 + 问题落点 + 证据合流 + 保留完整证据路径”。
3. 证明边界补充“不能证明其他三层没问题，也不能生成自动广告动作”。
4. 前端测试锁定“默认先查 / 证据路径仍保留 / 避免用户从四类明细里重新找入口”。

防错规则：
1. 广告组具体数据默认层必须先给一个可执行核对入口，不能只展示完整证据路径。
2. 收敛默认核对层不等于删除四层证据；完整投放商品、投放词、搜索词和广告位仍要可追溯。
3. 搜索词和广告位只能作为广告组上下文证据，不能自动归因到单个广告 ASIN。
4. 发现具体数据层阅读成本高时，优先桥接默认判断，不重画页面、不新增自动广告动作、不扩成 BI 大盘。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-26：复盘未到期时快照按钮不能继续显示拉取动作

```text
问题：
复盘窗口未到期时，快照按钮已经被 disabled，边界文案也说明未到期前不拉取快照。
但按钮可见文字仍显示“拉取快照”，用户会看到一个不可点的拉取动作，容易误解为下一步应该继续拉快照。

原因：
1. shouldPauseSnapshotPullForReview 只控制 disabled 和 aria-label，没有控制按钮可见文案。
2. snapshotActionBoundaryText 已经表达“只刷新本地信号和查看人工留痕”，按钮文字却仍像数据拉取入口。
3. UI 默认层不一致会增加用户决策成本，让复盘未到期状态看起来像系统阻塞。

解决：
1. 新增 snapshotCreateButtonText helper。
2. 未到期暂停拉取时按钮显示“等待复盘到期”。
3. 拉取中和普通状态仍分别显示“拉取中”“拉取快照”。
4. 测试锁定 helper 输出和工作台页面使用 helper。

防错规则：
1. disabled 只是交互状态，不足以表达业务边界；可见按钮文字也必须同步业务状态。
2. 未到期复盘只能引导等待和刷新本地信号，不能在默认层继续露出“拉取快照”动作语义。
3. 不要因为一个按钮文案冲突就改快照 API 或复盘模型，优先收口信息层级。

验证方式：
npm run test:snapshot-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-26：未到期复盘默认层不能只说已排程

```text
问题：
右侧“人工确认复盘闭环”能说明已有 ReviewTodo，也能在待办行显示下一项到期日。
但默认摘要和“复盘结论”行没有直接把到期日与“不能保存 ReviewRecord”绑定，用户仍可能继续猜 0 条复盘结论是否异常。

原因：
1. ReviewTodo 是排程对象，ReviewRecord 是人工保存后的结论对象。
2. 只展示“已生成待办”不足以回答“什么时候再看”和“现在为什么不能保存结论”。
3. 未到期状态若不前置到默认层，会增加用户打开细节账本的成本。

解决：
1. buildManualReviewClosureLedger 摘要在有 nextReviewDueAt 时显示“下一项到期 YYYY-MM-DD，未 ready 前不保存 ReviewRecord”。
2. “复盘结论”行 detail 同步显示下一项到期日，并说明未到完整窗口前不能判断改善、无变化或恶化，也不能保存 ReviewRecord。
3. 测试覆盖摘要和结论行，避免后续文案回退成泛化空状态。

防错规则：
1. 未到期时默认层必须说明下一项到期日和禁止保存结论。
2. 不要把 ReviewTodo 的排程状态包装成 ReviewRecord 结论。
3. 发现用户仍需猜下一步时，优先调整默认信息层级，不要补造历史数据或改变数据模型。

验证方式：
npm run test:review-ui
npm run build
```

## 2026-06-26：暂无复盘记录不能只显示空状态

```text
问题：
当前运行态有 search_term 复盘待办，但 7/14 天窗口未到期，因此 review-records 为 0。
如果界面只显示“暂无复盘记录”，用户容易误以为复盘链路断开、数据没写入或需要手动补记录。

原因：
1. ReviewTodo 是复盘排程，不代表已经可以下效果结论。
2. ReviewRecord 必须等 7/14 天窗口到期、处理前后指标完整，并由人工保存后才生成。
3. 空状态如果不解释前置条件，会增加用户决策成本，也可能诱导错误补写历史记录。

解决：
1. 将 reviewRecordStatusText(null) 改为说明保存前置条件。
2. 测试锁定空记录状态必须包含“窗口到期”和“人工保存”。
3. 不改后端、不补写历史 ReviewRecord、不把未到期待办伪装成复盘结论。

防错规则：
1. 0 条 ReviewRecord 不一定是错误；先看是否有 ReviewTodo、是否到期、是否 ready。
2. 未到期前只能提示等待复盘窗口和补齐指标，不能保存结论、不能自动改规则、不能执行广告动作。
3. 每轮复盘围绕目标链路结构化判断，不要因为空记录就矫枉过正地补造数据或改模型。

验证方式：
npm run test:review-ui
npm run build
```

## 2026-06-26：写后默认摘要不能只显示流程状态

```text
问题：
右侧人工确认点击后，默认摘要能回答写入了吗、证据留了吗、复盘待办生成了吗。
但用户仍需要展开完整审计材料，才能确认当时关键证据是否真的进入 evidence_snapshot。
这会让点击前关键证据、写后保存结果和 7/14 天复盘之间出现阅读断点。

原因：
1. 写后默认层偏 ManualAction / ReviewTodo 流程状态，没有直接读回已保存的关键证据。
2. 完整证据账本是审计材料，不应该成为确认“关键证据是否保存”的第一阅读路径。
3. 复盘只能沿点击时 evidence_snapshot 回看，因此写后第一眼必须说明保存了哪些关键证据。

解决：
1. 在人工动作写后默认摘要后新增“写后关键证据快照”。
2. 该层只读取 ReviewTodo.evidence_snapshot 或 ManualAction.evidence_snapshot，不读当前实时信号重新生成判断。
3. 优先展示搜索词表现分组、投放上下文数、合并边界、人工确认判断依据、能证明 / 不能证明等关键证据。
4. 完整点击后读回路径和证据账本继续保留在折叠审计层。

防错规则：
1. 写后第一眼必须能确认关键证据是否进入保存快照，不能只展示流程状态。
2. 写后关键证据只能来自 evidence_snapshot，不能用当前页面实时数据补写或伪造。
3. 写后读回不代表建议有效；效果结论必须等 7/14 天复盘窗口和人工保存 ReviewRecord。
4. 解决写后断点时不要矫枉过正；只补读回层，不改保存模型、不新增自动广告动作。

验证方式：
npm run test:workbench-structure
npm run build
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA，打开 beach essentials，确认右侧显示“写后关键证据快照”，来源为 evidence_snapshot，并位于“人工动作写后完整审计材料”之前。
```

## 2026-06-26：广告组优先排序不能只输出指标摘要

```text
问题：
广告组优先排序默认层原先只输出广告组名、状态和花费/订单/ACOS。
这对工程上是正确数据，但对运营用户来说仍需要自己推理“为什么先看这个广告组”，容易回到逐个广告组读报表的低效路径。

原因：
1. 广告组是投放容器，不是最终业务动作对象。
2. 同一个广告组可能有多个广告 ASIN、搜索词和广告位证据。
3. 如果默认层只给指标，不给问题落点和证据合流，用户仍然要自己判断排序依据。
4. 但这个问题不需要重画页面或新增模型，只需要把已有诊断 helper 合成为业务判断。

解决：
1. 广告组优先排序默认判断改为：优先看哪个广告组、优先原因、证据合流、核心指标。
2. Parent ASIN 决策导览同步展示优先原因。
3. 今日焦点补充证据合流状态。
4. 前端测试锁定“优先原因 / 证据合流 / 不是扫完整广告组列表”。

防错规则：
1. 广告组层默认必须回答“为什么先看这个广告组”，不能只展示裸指标。
2. 证明对象边界不等于把用户推回完整报表；默认层要先给判断，再把完整证据折叠保留。
3. 不要因为广告组层不够清晰就重画页面、改数据模型或新增自动动作。
4. 每轮围绕最终目标、MVP 链路、数据口径、对象关系、证据链和用户下一步动作结构化推进，避免矫枉过正。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-26：人工确认点击前不能让用户回头找关键证据

```text
问题：
右侧已经有“人工确认前四问”，但用户点击前仍可能需要回到中间证据链查“到底是哪几条关键证据支撑这次人工动作”。
当用户连续查看多个 Parent ASIN 时，这会再次增加来回扫读成本。

原因：
1. 中间区已有“关键证据先读”，但右侧按钮前没有同样的关键证据核对层。
2. 人工确认四问说明了能证明、不能证明和人工下一步，但没有直接列出前三条触发证据。
3. 完整预检和 evidence_snapshot 是审计材料，不能替代点击前的业务证据核对。

解决：
1. 右侧人工确认区新增“点击前关键证据核对”。
2. 该层复用 buildKeyEvidenceFacts(selectedSignal.evidence.facts, 3, selectedSignal)，只读前三条触发证据。
3. “能支撑 / 不能支撑 / 人工下一步”复用 selectedDiagnosisEvidenceSummary。
4. 结构测试锁定该层必须位于诊断路径之后、人工确认前四问之前、人工动作按钮之前。

防错规则：
1. 右侧人工确认按钮前必须能读到同一份关键证据，不要让用户回中间区重新拼证据。
2. 关键证据核对只是阅读层，不能新增指标口径、改变证据生成或绕过后端预检。
3. 任何点击前摘要都不能包装成自动加词、自动调价、自动否词或暂停广告依据。
4. 解决阅读成本时不要矫枉过正；保留完整预检、证据快照和写后合同作为折叠审计层。

验证方式：
npm run test:workbench-structure
npm run build
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA，打开 beach essentials，确认右侧显示“点击前关键证据核对”，并在“人工确认前四问”之前读到关键证据、能支撑、不能支撑和人工下一步。
```

## 2026-06-26：证据链不能让用户先读完整审计材料

```text
问题：
中间诊断区已经有“业务判断与指标目的”“原因”“关键指标判断目的”，但证据链区仍容易让用户从来源覆盖、触发证据数量、AI 推理路径和完整证据开始逐段阅读。
当某个信号有 10 条以上证据时，用户会再次进入“看报纸式”阅读，需要自己判断哪几条证据最关键。

原因：
1. keyEvidenceFacts 已经选出触发证据，但默认摘要只显示数量，没有说明这些证据的阅读目的。
2. 完整证据是审计和复盘必需材料，但不应该成为第一阅读任务。
3. 证据链区没有直接复用 diagnosisEvidenceSummary 的“能证明 / 不能证明”，导致用户需要在上方判断合同和下方证据明细之间来回对照。

解决：
1. 在证据链摘要后新增“关键证据先读”层。
2. 固定回答：先读哪几条、能支撑什么、仍不能证明什么。
3. “能支撑什么 / 仍不能证明”直接读 diagnosisEvidenceSummary，不新增前端虚构判断。
4. 完整证据、商品下钻、触发证据明细和来源筛选全部保留，只下沉为回查和审计材料。

防错规则：
1. 证据链默认层必须先帮用户识别关键证据，不要只展示证据总数和完整列表。
2. 压缩阅读路径不等于删证据；完整证据必须能继续回查来源、对象边界和复盘审计。
3. 关键证据摘要不能包装成自动加词、自动调价、自动否词或暂停广告依据。
4. 解决阅读成本时不要矫枉过正；只调整阅读层，不改证据生成、对象模型或人工动作范围。

验证方式：
npm run test:workbench-structure
npm run build
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA，打开 beach essentials 具体信号，确认“关键证据先读”位于“触发证据”之前，并说明完整证据只用于回查来源、对象边界和复盘审计。
```

## 2026-06-26：中间诊断不能先让用户读关键指标再反推业务判断

```text
问题：
中间诊断区的关键指标卡已经有“服务判断 / 能证明 / 不能证明 / 人工下一步”，但它原来出现在“业务判断与指标目的”之前。
用户会先看到花费、订单、销售额、ACOS 等数值，再往下找这些指标到底服务什么业务判断。
这会增加二次推理成本，尤其在连续查看多个 Parent ASIN 或多个 SearchTerm 时更明显。
补充发现：即使业务判断前置后，“原因”仍排在关键指标之后，用户还是会先读指标再补读触发原因。

原因：
1. 指标解释已经存在，但阅读顺序仍偏数值优先。
2. diagnosis_contract 是业务判断合同，却被放在关键指标之后，用户需要倒读。
3. 原因段落应该承接业务判断，不应该被关键指标隔开。
4. 解决这个问题不需要改后端或新增指标，只需要把判断合同和原因前置，并给关键指标加边界提示。

解决：
1. 中间诊断区先展示“业务判断与指标目的”，再展示“原因”，最后展示“关键指标判断目的”。
2. 关键指标区保留“只服务上方业务判断”提示。
3. 明确写出这些指标只用于判断证据强度、对象边界和人工复核优先级，不能直接推出自动广告动作。
4. 结构测试锁定业务判断、原因、关键指标的顺序。

防错规则：
1. 中间区不能先堆指标，再让用户反推业务问题。
2. 指标必须服务明确判断：业务问题、对象粒度、能证明、不能证明、人工下一步。
3. 指标解释不能包装成自动调价、自动加词、自动否词或暂停广告依据。
4. 调整阅读成本时不要矫枉过正；指标和证据都保留，只调整业务判断优先级。
5. 原因要紧跟业务判断，不要被关键指标隔断。

验证方式：
npm run test:workbench-structure
npm run build
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA，确认具体 SearchTerm 诊断中“业务判断与指标目的”先于“原因”，“原因”先于“关键指标判断目的”，且关键指标区写明不能直接推出自动广告动作。
```

## 2026-06-26：Parent ASIN 左侧分诊不能让用户重新排序

```text
问题：
左侧 Parent ASIN 分诊摘要已有“今日先看什么”、决策卡、分诊桶和排序依据，但阅读策略与人工下一步埋在普通段落里。
当用户连续查看多个 Parent ASIN 时，仍容易先读完整摘要，再自己判断先打开谁。
这会把 AI 信号工作台重新推回“看报纸式阅读”，增加决策时间和成本。

原因：
1. 左侧第一层同时承担解释、排序、筛选和动作提示，最关键的“本轮只打开谁”不够突出。
2. 阅读策略和人工下一步是用户真正要执行的动作，但原来视觉权重低于决策卡和分诊桶。
3. 解决这个问题不需要重做页面或改数据模型，只需要把默认动作从解释文本中抬出来。

解决：
1. Parent ASIN 首页分诊摘要新增“本轮默认动作”块。
2. 默认动作固定回答：只打开哪个 Parent ASIN、现在人工做什么、打开后按什么顺序读。
3. 原有扫一眼决策卡、分诊桶、排序依据和证据链不删除，只放在默认动作之后。
4. 结构测试锁定默认动作必须出现在扫一眼决策卡之前。

防错规则：
1. 左侧第一眼必须先帮用户排序和收口动作，不能只罗列指标、证据和解释。
2. 解决决策成本问题时，不要矫枉过正地重画成 BI 大盘或新增独立入口。
3. Parent ASIN 只能作为经营入口；广告分析仍要下钻到有广告数据的广告 ASIN、广告组、投放词、搜索词和广告位上下文。
4. 每次优化信息架构时都要回到总目标：AI 识别信号、解释证据、支持人工确认和复盘，而不是替代人工自动执行广告动作。

验证方式：
npm run test:workbench-structure
npm run build
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA，确认左侧显示“今日先看什么”“本轮默认动作”“只打开”，且仍不出现自动广告动作。
```

## 2026-06-26：复盘状态不能只展示流程状态

```text
问题：
右侧复盘状态默认层原来先展示“已经记录了吗、什么时候复盘、现在能下结论吗、下一步做什么”。
这些问题能说明流程状态，但不能帮助用户回看当时为什么进入复盘、证据能证明什么、不能证明什么。
到 7/14 天复盘时，用户仍需要从 evidence_snapshot 和状态账本里重新拼业务判断。

原因：
1. 复盘默认层偏 ManualAction / ReviewTodo / ReviewRecord 流程门禁，没有直接承接人工确认前四问。
2. ReviewTodo 业务判断读回已有当时判断、路径、下一步和动作边界，但缺少默认层可读的“能证明 / 不能证明”。
3. 用户复盘时最需要先确认原始判断边界，再看是否能保存 ReviewRecord；流程状态不应取代业务判断。

解决：
1. ReviewTodo 判断读回补充“证据能证明”和“证据不能证明”。
2. 右侧复盘默认层改为“复盘回看四问”。
3. 四问固定为：当时确认什么、当时证据证明什么、当时证据不能证明什么、现在复盘做什么。
4. 完整 ReviewTodo 诊断路径、ReviewRecord 保存判断和规则反馈继续保留，不删除审计层。

防错规则：
1. 复盘第一层必须先回看业务判断，不要只展示流程状态。
2. ReviewTodo 只能继承点击时 evidence_snapshot；不能把当前实时广告事实伪装成当时判断。
3. 没有 ReviewTodo 或证据快照时，只能显示等待和证据缺口，不能保存 ReviewRecord 或判断效果。
4. 发现复盘区增加决策成本时，先围绕总目标重排信息，不要矫枉过正地改成复盘报表页。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
http://127.0.0.1:5175/ 确认页面显示“复盘回看四问”，并仍只允许人工复盘记录，不执行广告动作。
```

## 2026-06-26：右侧人工确认不能只展示按钮执行合同

```text
问题：
右侧人工确认区虽然已有“按钮前先看这四件事”，但内容偏执行合同：当前可点什么、为什么可点、点后写什么、何时复盘。
这些内容对系统安全有用，但用户仍需要自己把中间诊断证据重新翻译成人工判断。
当用户连续查看多个 Parent ASIN 时，右侧会再次增加决策成本。

原因：
1. 右侧默认层没有直接使用中间诊断的 businessQuestion、proves、doesNotProve、nextManualStep。
2. 预检、写入和复盘合同属于审计层，不应取代用户点击前的业务判断层。
3. 人工动作按钮如果先展示“能点什么”，容易让用户先想操作，而不是先确认对象边界和证据作用。

解决：
1. 将右侧默认层改为“人工确认前四问”。
2. 四问固定为：当前在确认什么、证据能证明什么、证据不能证明什么、现在人工做什么。
3. 四问直接回读 selectedDiagnosisEvidenceSummary，动作准入和复盘合同只作为 detail 或折叠审计材料。
4. 结构测试锁定四问文案和诊断证据字段，避免回退成按钮合同展示。

防错规则：
1. 右侧人工确认第一层必须先回答业务判断问题，不能只展示可点击状态。
2. 按钮准入、evidence_snapshot 和写后合同必须保留，但应放在默认判断层之后或折叠审计层。
3. 人工动作只能是记录观察、标记已处理、加入复盘、忽略本次，不能包装成自动广告动作。
4. 发现右侧增加决策成本时，先调整信息组织，不要矫枉过正地推翻三栏工作台或改成 BI 大盘。

验证方式：
npm run test:workbench-structure
npm run build
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA，确认右侧显示“人工确认前四问”，并能读到确认对象、证据证明、反证边界和人工下一步。
```

## 2026-06-26：推荐搜索词证据罗列会增加决策成本

```text
问题：
B00K4W4AAA / beach essentials 的“推荐搜索词承接证据”原来按表现、承接、广告 ASIN、投放词、广告位、样本、边界展示 7 行信息。
这些信息本身都正确，但用户需要自己把 7 行信息重新拼成业务判断。
当用户连续查看多个 Parent ASIN 时，这种展示方式会变成“看 10 张报纸”，增加决策时间和成本。

原因：
1. 证据展示按数据层罗列，而不是按业务问题组织。
2. 指标没有先回答“这个指标服务什么判断”，用户需要自己推理下一步。
3. 广告组、投放词、搜索词和广告位都属于上下文证据，如果平铺展示，容易让用户误以为每一层都需要单独读完整报表。

解决：
1. 将推荐搜索词承接证据压缩成四问复核：是否值得复核、从哪里下钻、能否落到 ASIN、广告位能否判断。
2. 原始指标和证据不删除，只下沉到每个问题的 detail。
3. 广告位继续明确为证据缺口：当前不能判断广告位影响，需要补 ad_placement_daily_metrics 的 campaign_id + ad_group_id 级证据。
4. 结构测试锁定四问复核，避免回退成散点指标堆叠。

防错规则：
1. 工作台第一屏不要先堆指标，必须先回答业务判断问题。
2. 搜索词机会链路至少要先回答：是否值得复核、从哪里下钻、能否落到 ASIN、广告位能否判断。
3. 指标、广告组、投放词、搜索词样本和广告位缺口可以作为 detail，但不能重新变成用户第一眼的阅读负担。
4. 压缩信息不等于删证据；证据链必须仍能回看来源、对象边界和人工下一步。

验证方式：
npm run test:workbench-structure
npm run build
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA，确认“推荐搜索词承接证据”显示四问复核，并保留广告位证据缺口和 ad_placement_daily_metrics 补证要求。
```

## 2026-06-26：推荐搜索词承接证据不能漏掉广告位缺口

```text
问题：
B00K4W4AAA / beach essentials 的后端诊断合同已返回 placement_gap：
推荐对象直连广告位 0、广告组级广告位 0、同广告活动广告位背景 6。
但单屏“推荐搜索词承接证据”只展示表现、承接广告组、广告 ASIN、投放词、样本和搜索词边界，没有单独展示广告位证据缺口。
用户会看到“核对广告位”的泛化提示，却不知道当前不能判断广告位影响、也不知道需要补什么证据。

原因：
1. recommendedSearchTermCarryEvidence 已消费搜索词、广告组、广告 ASIN 和投放词证据，但没有消费 placement_context_gap。
2. 广告位缺口虽然存在于 diagnosis_contract 和 business_evidence_blocks，但没有进入单屏第一阅读路径。
3. “广告位边界”如果只作为一句归因边界出现，无法回答用户下一步要补什么数据。

解决：
1. 推荐搜索词承接证据新增“广告位”行。
2. 优先读取 recommended_evidence_drilldown.business_evidence_blocks.placement_context_gap。
3. 文案固定说明：广告位证据缺口、当前不能判断广告位影响、需要补同周期 ad_placement_daily_metrics 的 campaign_id + ad_group_id 级广告位表现。
4. 结构测试锁定该行不能缺失。

防错规则：
1. 搜索词机会链路必须同时展示搜索词表现、广告组承接、广告 ASIN 承接、投放词、广告位缺口和归因边界。
2. placement_context_count = 0 或只有活动级广告位背景时，页面只能说“不能判断广告位影响”，不能包装成广告位问题或广告位优化建议。
3. 判断广告组级广告位影响必须有 campaign_id + ad_group_id 对齐的 ad_placement_daily_metrics。
4. 广告位缺口不能只放在折叠审计、诊断合同或复盘门禁里；当前单屏证据也必须可读。

验证方式：
npm run test:workbench-structure
npm run build
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA，确认“推荐搜索词承接证据”包含广告位证据缺口、不能判断广告位影响、ad_placement_daily_metrics 和 campaign_id + ad_group_id。
```

## 2026-06-26：推荐 SearchTerm 有广告组证据时不能显示暂无广告组

```text
问题：
B00K4W4AAA 的推荐候选 beach essentials 在 recommended_evidence_drilldown 中已有 2 个关联广告组、2 条搜索词表现行和 5 行同组广告 ASIN 承接样本。
但诊断 brief 在没有独立广告组排序行时固定显示“暂无可排序广告组”，容易让用户误以为没有广告组可下钻。

原因：
1. buildProductScopeDiagnosisBrief 只识别 productScopeAdGroupDiagnosisRows 生成的独立广告组诊断行。
2. recommended_evidence_drilldown.ad_groups 已经在单屏卡用于“推荐搜索词承接证据”，但没有传入上层五段诊断框架。
3. “无独立广告组异常排序”和“推荐 SearchTerm 已有关联广告组证据”是两种不同状态，被合并成了普通空状态。

解决：
1. 新增 ProductScopeRecommendedAdGroupEvidence 作为诊断 brief 的轻量输入，不建新表、不改 API。
2. 没有独立广告组排序行但有推荐广告组证据时，显示为“证据下钻入口”，不再显示“暂无可排序广告组”。
3. 文案明确这些广告组不代表广告组本身异常，也不能证明 SearchTerm 已自动归因到单个广告 ASIN。
4. 结构测试和 signalUi 测试锁定该分支，避免后续回退。

防错规则：
1. 空状态必须先区分“真的没有证据”和“没有某类排序行但有下钻证据”。
2. recommended_evidence_drilldown 里的广告组只能作为 SearchTerm 证据入口，不得包装成广告组异常结论。
3. SearchTerm 只能先回到广告组和 advertised_products 上下文，再判断承接复核路径，不能直接归因到单个 ASIN。
4. 缺广告位证据时，页面只能说明广告位影响不可判断，不能用搜索词或广告组证据替代广告位结论。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA，确认页面显示 beach essentials 关联 2 个广告组作为承接证据，且不出现“暂无可排序广告组 / 暂无可聚焦广告组”。
```

## 2026-06-26：Parent ASIN 切换读取态不能伪装成无候选

```text
问题：
用户切换到 parent_asin:B00K4W4AAA 后，后端 /api/signal-triage 已返回 ready_for_manual_confirmation、3 个候选和推荐 beach essentials。
但前端在 signalTriageSummary 尚未读回的 1-2 秒空窗里，首屏曾显示“AI 候选等待扫描”和“诊断 MVP”。
这容易让运营误判为该 Parent ASIN 没有可处理候选，继续增加决策成本。

原因：
1. clearProductScopeTransientState 会在切换入口时清空 signalTriageSummary。
2. buildProductScopeFirstScreenSummary 将 triageSummary 为空的状态当成候选等待扫描，而不是当前入口正在读回。
3. unknown / loading / ready 三种状态没有分开表达，导致读取空窗被包装成业务判断。

解决：
1. triageSummary 为空且存在广告 ASIN 证据时，首屏显示“读取 AI 准入中”。
2. AI 准入门槛显示“正在读取 AI 准入”，复盘门槛显示“等待人工留痕读回”。
3. 读取态明确说明读回前不能判断无候选、不能保存人工动作、不能说建议有效或无效。
4. signalUi 测试锁定读取态不能再出现“AI 候选等待扫描”。

防错规则：
1. 前端切换 Parent ASIN、ASIN、广告下钻入口时，unknown / loading / ready 必须分开表达。
2. 后端分诊未读回前，不能展示成“0 候选”“无候选”或“诊断完成”。
3. 读取态只能说明正在按当前经营入口读回 AI 准入、对象边界和人工门禁。
4. 读回后才允许展示候选数、推荐对象、人工门槛和复盘门槛。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
浏览器刷新 http://127.0.0.1:5175/ 后切到 parent_asin:B00K4W4AAA，确认切换瞬间显示“读取 AI 准入中”，读回后显示“人工留痕 MVP / 3 个候选可人工复核”。
```

## 2026-06-26：已留痕推荐对象和当前按钮对象不能埋在折叠区里

```text
问题：
B00K4W4AAA 下 recommended_candidate 是 beach essentials，但它已经有人工留痕和复盘待办；当前可继续处理的是 next_unhandled b01fay0yl0。
页面原来把“人工动作目标切换”和“人工确认双轨分流”放在完整预检折叠区里，默认层先显示广告组承接卡。
同时广告组卡写成“动作对象：RBK004-扩展-beach essentials”，容易让用户误以为当前按钮对象是广告组，而不是后端预检里的搜索词候选。

原因：
1. 推荐对象、已留痕状态、next_unhandled 候选和广告组上下文属于四个不同层级。
2. 双轨提示虽然已有，但默认不可见，用户必须展开预检才知道为什么从 beach essentials 转到 b01fay0yl0。
3. 广告组是承接上下文，不是当前人工动作对象；文案使用“动作对象”会破坏对象边界。

解决：
1. 把人工动作目标切换和人工确认双轨分流上移到右侧默认层。
2. 默认层直接展示 beach essentials 已留痕、b01fay0yl0 待授权，以及两者不能互借证据。
3. 将广告组卡改为“当前广告组证据上下文”，字段改为“广告组上下文”。
4. 选中 next candidate 但预检未 ready 时，人工下一步显示“等待预检”，不再显示“先确认选中对象”。

防错规则：
1. 已留痕推荐对象和当前按钮对象必须在右侧默认层可见，不能只放在折叠审计区。
2. 广告组只能作为证据上下文，不得在右侧默认层称为“动作对象”。
3. 当前人工按钮对象必须以后端 manual_action_preview / preflight 读回为准。
4. 推荐对象已有人工留痕时，不要重复写同一对象；next_unhandled 只有人工点击后才写入 manual_actions 和 review_todos。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA，确认右侧默认层展示 beach essentials 已留痕、b01fay0yl0 待授权、等待预检，且广告组只作为证据上下文。
```

## 2026-06-26：切换 Parent ASIN 后右侧不能残留上一入口对象

```text
问题：
选择 parent_asin:B00K4W4AAA 后，页面中间已经开始展示 B00 链路，但右侧建议处理区曾混入上一入口的 RB833 / B0GL8QDHW5 / sunglasses womens 对象。
这会让用户无法判断当前证据到底属于哪个 Parent ASIN，等于增加决策成本。

原因：
1. 经营诊断入口切换时，只更新 selectedProductScopeId，没有同步清空 selectedSignal、selectedAdGroupDiagnosis、signalTriageSummary、searchIntents 和 manualActionPreflight。
2. 旧入口的异步 signal-triage / review-evidence / search-intents 请求可能晚返回，覆盖当前入口的展示状态。
3. 右侧人工确认区依赖 selectedSignal 和 signalTriageSummary，如果二者来自不同入口，就会形成跨 Parent 混态。

解决：
1. 新增 clearProductScopeTransientState，入口切换时立即清空旧选中信号、旧广告组焦点、旧后端摘要、旧搜索词意图、旧人工预检和旧消息。
2. 新增 handleSelectProductScope，所有经营入口切换动作统一走同一个状态隔离入口。
3. 新增 productScopeRequestSeq，请求返回时校验序号，慢返回不能覆盖当前入口。
4. 结构测试增加约束，筛选器和入口跳转按钮不得直接绕过 handleSelectProductScope。

防错规则：
1. Parent ASIN 是当前屏幕的业务口径边界；左侧信号、中间诊断、右侧人工确认必须属于同一个入口。
2. 入口切换时必须先清空旧的诊断上下文，再等待新入口数据填充。
3. 异步请求返回必须校验当前入口，不能把旧入口数据写回页面。
4. 发现混态问题时先修状态边界，不要矫枉过正地推翻三栏工作台或推荐候选逻辑。

验证方式：
npm run test:workbench-structure
npm run build
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA，确认中间和右侧不再出现 RB833、B0GL8QDHW5、sunglasses womens
```

## 2026-06-26：推荐 SearchTerm 不能只展示词和广告组，必须说明承接证据

```text
问题：
B00K4W4AAA 的推荐候选是 beach essentials，默认广告组也已对齐到 RBK004-扩展-beach essentials。
但单屏作战卡如果只展示推荐词和广告组名，用户仍要进入多处报表判断：这个词表现如何、由哪些广告 ASIN 承接、投放词是什么、有没有样本、不能证明什么。
这会继续增加决策成本，和 AI 信号工作台“先判断，不先读报表”的目标冲突。

原因：
1. recommended_candidate 只告诉用户推荐对象，不足以解释为什么值得复核。
2. recommended_evidence_drilldown 已有业务证据块，但单屏卡没有消费这些证据。
3. 搜索词机会必须同时看表现、广告组承接、广告 ASIN 承接、投放词结构和归因边界，不能只看 ACOS、CVR 或广告组名。

解决：
1. 单屏作战卡新增 recommendedDrilldown。
2. 新增 recommendedSearchTermCarryEvidence helper，从推荐证据下钻和当前广告组组合出表现、承接、广告 ASIN、投放词、样本和边界。
3. 搜索词复核区新增“推荐搜索词承接证据”清单。
4. 结构测试锁定 recommended_evidence_drilldown 传入单屏卡，以及“不证明单个 ASIN 归因 / 不自动加词调价否词”的边界表达。

防错规则：
1. 推荐 SearchTerm 进入单屏时，必须说明它服务的业务判断，而不是只展示裸指标。
2. SearchTerm 证据至少要交代广告组承接、广告 ASIN 承接、投放词或证据缺口、样本和反证边界。
3. 搜索词不能自动归因到单个广告 ASIN；缺广告位证据时不能判断广告位影响。
4. 所有建议必须落到人工复核动作，不能包装成自动加词、自动调价或自动否词。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run build
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA 后查看“推荐搜索词承接证据”
```

## 2026-06-26：经营诊断入口不能被异步默认优先级覆盖用户选择

```text
问题：
浏览器验证时，用户选择 parent_asin:B00K4W4AAA 后，页面会短暂显示 B00K4W4AAA，但很快回跳到默认优先 Parent B07C32KY1R。
这会让用户误以为筛选器失效，也会导致后续证据链读错对象。

原因：
1. loadSignals 启动时 selectedProductScopeId 可能还是 null。
2. 用户在 loadSignals 尚未完全完成时手动选择 B00K4W4AAA。
3. loadSignals 返回后仍按启动时的默认优先入口直接 setSelectedProductScopeId(nextActiveProductScopeId)，覆盖了用户手动选择。

解决：
1. setSelectedProductScopeId 改为函数式更新。
2. 如果 current 仍是有效 product scope，就保留 current。
3. 只有当前没有有效选择时，才写入默认优先入口 nextActiveProductScopeId。
4. 结构测试增加反向约束，禁止再直接 setSelectedProductScopeId(nextActiveProductScopeId)。

防错规则：
1. 默认推荐入口只能用于初始化，不得覆盖用户已经手动选择的有效经营入口。
2. 优先队列按钮可以主动切换入口，但异步数据加载不能替用户改筛选器。
3. 任何 Parent ASIN 下钻验证都必须先确认筛选器 value 和单屏卡对象一致。
4. 修筛选器状态时只处理状态覆盖，不顺手改优先队列排序。

验证方式：
npm run test:workbench-structure
http://127.0.0.1:5175/ 选择 parent_asin:B00K4W4AAA，确认筛选器不回跳 B07C32KY1R
```

## 2026-06-26：推荐 SearchTerm 链路不能被广告组列表第一行带偏

```text
问题：
B00K4W4AAA 的推荐候选是 beach essentials，但默认广告组原来取 product_scope_drilldown.ad_group_diagnosis 的第一行。
第一行是 RBK004-kids sunglasses-广泛，会让用户从 beach essentials 链路跳到另一个广告组，继续像读广告组报表一样判断。

原因：
1. selectedAdGroupDiagnosis 只按用户手动选择或广告组列表第一行取值，没有消费 recommended_evidence_drilldown.ad_groups。
2. 广告组列表排序服务整体广告组诊断，不一定服务当前推荐 SearchTerm 的人工复核链路。
3. 单屏作战卡已经显示 beach essentials，但广告组定位不跟随，会形成同屏两个下一步。

解决：
1. 新增 recommendedAdGroupNames / recommendedAdGroupDiagnosis helper。
2. 默认广告组选择顺序改为：用户手动选择 > 推荐 SearchTerm 关联广告组 > 原广告组第一行。
3. 单屏作战卡和诊断简介使用当前焦点广告组，并显示“推荐 SearchTerm beach essentials 的承接广告组”。
4. 广告组列表本身不改排序，避免把局部推荐链路误改成全局广告组排序规则。

防错规则：
1. Parent ASIN 下存在 recommended_candidate 时，默认广告组焦点必须优先服务推荐候选的证据链。
2. 广告组列表第一行不能自动等同于当前推荐 SearchTerm 的复核广告组。
3. 广告组仍是投放容器，只能说明承接上下文，不能自动归因到单个广告 ASIN。
4. 用户手动选择广告组后必须尊重用户选择，不再强行回到推荐广告组。
5. 纠偏时只修默认焦点，不把广告组表改成普通 BI 大盘。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run build
http://127.0.0.1:5175/ 浏览器稳定加载后选择 parent_asin:B00K4W4AAA
```

## 2026-06-26：单屏搜索词证据不能把聚合 Top 词当成人工动作入口

```text
问题：
B00K4W4AAA 进入后，后端推荐候选是 beach essentials，但单屏作战卡的“搜索词证据”可能优先展示搜索词聚合 Top 词 adult sunglasses womens sporty。
用户会在同一屏看到两个不同的“先打开搜索词”，增加判断成本，也容易误把聚合分析当成人工动作对象。

原因：
1. 单屏作战卡原来只读取 searchIntentSummary，没有读取 signal-triage 的 recommended_candidate。
2. 搜索词聚合是 Parent ASIN 视角的分析资产，不等于右侧人工动作入口。
3. 后端推荐候选已经完成准入排序，前端如果不用它，会破坏“分诊 -> 证据 -> 人工确认”的路径。

解决：
1. 单屏作战卡新增 recommendedCandidate。
2. 当 recommended_candidate.object_type 是 search_term 时，“搜索词证据”优先显示“后端推荐候选：beach essentials”。
3. 搜索词聚合仍保留为 Parent ASIN 视角参考，并明确不替代右侧人工动作对象。
4. 结构测试锁定 recommendedCandidate 传入和“搜索词聚合只做 Parent ASIN 视角参考”的表达。

防错规则：
1. 人工动作入口优先使用后端 recommended_candidate，不得用聚合 Top 词替代。
2. 搜索词聚合只能辅助理解 Parent ASIN 广告搜索词表现，不能变成人工动作对象。
3. 当同一屏出现推荐候选和聚合搜索词时，必须明确谁是“先打开”的对象。
4. 纠偏时围绕目标链路减少决策成本，不删除仍有业务价值的辅助分析层。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run build
http://127.0.0.1:5175/ 浏览器稳定加载后选择 parent_asin:B00K4W4AAA
```

## 2026-06-26：Parent ASIN 优先清单不能让用户读长句做第一判断

```text
问题：
Parent ASIN 优先清单已经展示判断、证据和下一步，但用户看多个 Parent ASIN 时仍要逐行读长句。
这会把 AI 信号工作台退化成“看 10 张报纸”，增加运营决策时间。

原因：
1. 清单行里有优先级、工作流状态、判断、证据和下一步，但缺少第一眼可扫的动作提示。
2. 用户进入工作台时先要决定“今天先打开谁”，不是马上阅读每个 Parent ASIN 的完整证据链。
3. 如果只继续堆指标和解释，会偏离 AI Copilot 的分诊目标。

解决：
1. ProductScopePriorityQueueItem 增加 actionCue。
2. 按复盘待办、信号类型和广告证据生成短提示：先复盘、先扩量、先止损、先查结构、先补证、先确认、只观察、暂不展开。
3. 前端在 Parent ASIN 清单行首展示动作提示，辅助用户先扫再下钻。
4. 后端候选提示无法判断具体类型时使用“先确认”，避免泛化成“先处理”。

防错规则：
1. Parent ASIN 首页清单必须先帮用户排序和分诊，不能让用户靠读完整报表做第一判断。
2. 动作提示只能来自现有信号类型、复盘状态和广告证据，不得新增自动广告动作含义。
3. 没有广告证据的 Parent ASIN 只能提示暂不展开，不能强行包装成广告诊断对象。
4. 纠偏时围绕目标链路减少决策成本，不把工作台改成新的 BI 大盘。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
http://127.0.0.1:5175/ 浏览器复核动作提示
```

## 2026-06-26：经营商品入口不能无视顶部市场筛选

```text
问题：
顶部筛选器已经有 market_id，signal-triage、search-intents、review-todos 也会按市场查询。
但 product-scope 经营商品入口原来只读取全局最新成功快照，没有接收 market_id。
当存在多个市场快照时，用户切换站点后可能仍看到另一个市场的 Parent ASIN / ASIN 入口。

原因：
1. 经营入口接口早期只服务单市场 MVP，默认取最新成功快照。
2. 后续信号分诊已经市场化，但 product-scope 没有同步补 query 参数。
3. Parent ASIN 优先处理清单依赖 product-scope，如果入口市场错了，后续广告组、搜索词和人工复盘都会从错误经营对象展开。

解决：
1. /api/product-scope 增加 market_id 查询参数。
2. build_product_scope_summary 增加 selected_market_id，只选择该市场成功快照。
3. Parent ASIN 人工导入映射和主推策略行在选定市场下按 market_id / marketplace_id 过滤。
4. 前端 fetchProductScope(selectedMarketId)，让经营入口跟随顶部站点筛选。

防错规则：
1. 顶部市场筛选一旦存在，经营入口、信号分诊、搜索词聚合和复盘待办都必须共用同一 market_id。
2. 不得用“最新成功快照”替代“当前用户选择的市场快照”。
3. Parent ASIN 优先队列只负责该市场内排序，不能跨市场合并。
4. 纠偏时优先修数据入口口径，不用更多页面说明掩盖口径漂移。

验证方式：
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_product_scope.py backend/tests/test_api_routes.py::test_product_scope_route_passes_market_id_query
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
http://127.0.0.1:8000/api/product-scope?market_id=1 运行态读回
```

## 2026-06-26：复盘效果路由必须继承 SearchTerm 快照口径

```text
问题：
服务层已经验证 SearchTerm 复盘只使用 ad_search_term_daily_metrics。
但 API 路由如果未来改动时没有测试约束，可能把成功快照里的广告商品、广告位或其他搜索词行直接传成复盘指标。

原因：
1. review-effect 是前端读取复盘效果的真实入口，不能只依赖服务层测试。
2. 成功快照包含多层广告对象，SearchTerm、AdvertisedProduct、Placement 会同时存在。
3. 用户要判断人工动作是否有效，必须看同一个广告搜索词处理前后变化，不能混入上下文证据层。

解决：
1. 新增 API 路由测试，模拟 beach essentials 复盘效果请求。
2. 测试数据中混入 pool towels、ad_product_daily_metrics、ad_placement_daily_metrics。
3. 断言路由返回的 before/after 指标只来自 beach essentials 的 ad_search_term_daily_metrics。
4. 不改前端和生产逻辑，避免因补测试而扩大改动范围。

防错规则：
1. 服务层和 API 层都要锁定同一对象口径。
2. SearchTerm 复盘指标来源只能是广告搜索词指标行。
3. 广告商品和广告位可以作为证据链上下文，但不能直接变成搜索词复盘效果。
4. 纠偏时围绕目标链路补薄弱点，不因为单点风险推翻整体工作台结构。

验证方式：
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_api_routes.py::test_review_effect_route_uses_snapshot_rows_for_matching_search_term_only
npm run test:signal-ui-runtime
python scripts\inspect_review_readiness.py --market-id 1 --compact
git diff --check
```

## 2026-06-26：SearchTerm 复盘指标不能混入广告商品或广告位行

```text
问题：
SearchTerm 复盘到期后需要比较处理前后广告指标。
如果匹配 signal_rows 时只看搜索词文本或宽泛对象 ID，可能把其他搜索词、广告商品指标或广告位指标混进当前搜索词复盘。

原因：
1. 搜索词对象的稳定 ID 形如 search_term:1:beach essentials，既包含对象类型，也包含展示文本。
2. 广告商品、广告组、广告位都是搜索词诊断上下文，但不能作为 SearchTerm 复盘指标来源。
3. 复盘结果会进入规则反馈候选；一旦指标来源混乱，后续会误判人工处理是否有效。

解决：
1. 审查 _row_matches_action 确认 SearchTerm 先按 object_type/source_table 限定为 ad_search_term_daily_metrics。
2. 新增测试覆盖 beach essentials 到期复盘，只允许同一 SearchTerm 的 ad_search_term_daily_metrics 进入前后窗口。
3. 测试明确排除 pool towels、ad_product_daily_metrics 和 ad_placement_daily_metrics 行。
4. 不改生产逻辑，只把当前正确边界用测试锁住。

防错规则：
1. SearchTerm 复盘指标只来自广告搜索词指标行。
2. 广告商品、广告位和广告组只作为证据上下文，不得替代 SearchTerm 的复盘指标。
3. 到期前只允许 waiting_review_window，不得为了测试或演示写真实 ReviewRecord。
4. 到期后也必须先确认前后窗口覆盖完整，再保存人工复盘结论。

验证方式：
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_actions.py::test_review_effect_for_search_term_uses_only_matching_ad_search_term_rows
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_actions.py
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-26：复盘保存总提示不能只说缺少字段

```text
问题：
ReviewRecord 保存前检查已经能识别待办证据一致性和对象引用缺口。
但总 gate 如果只提示“缺少：待办证据对象引用”，用户会把它理解成字段缺失或系统故障，而不是复盘样本保护规则。

原因：
1. 单项检查文案说明了对象错配，但总状态是用户第一眼看到的判断。
2. ReviewRecord 的业务风险不是“少一个字段”，而是可能把其他 action、其他对象或当前重新计算证据错存成当前复盘。
3. 用户需要知道为什么不能保存，才能在 7/14 天后正确补证或等待，而不是误以为按钮坏了。

解决：
1. buildReviewRecordSaveGateSummary 在缺少 todo_evidence_signature 或 todo_object_reference 时追加历史 ReviewTodo 证据门禁解释。
2. 明确该门禁用于防止把其他 action、其他对象或当前重新计算证据错存成当前复盘。
3. 测试补充广告商品对象错配和 beach essentials 搜索词对象错配的总 gate 断言。
4. 保持 canSave 逻辑不变，不放宽 ReviewRecord 保存规则。

防错规则：
1. 第一层 UI 不能只暴露内部字段名，要说明字段背后的业务门禁。
2. 复盘保存失败时优先解释“为什么不能落地到业务复盘”，而不是只列缺口。
3. 历史 evidence_snapshot 门禁只解释，不替用户补写当前页面证据。
4. 收口文案时不能改成自动修复、自动调价、自动加词、自动否词。

验证方式：
npm run test:review-ui
npm run build
git diff --check
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-26：ReviewRecord 不能只校验证据类型，还必须校验对象引用

```text
问题：
ReviewRecord 保存前已经校验 expected_evidence_snapshot 来自当前 ReviewEffect，也校验排查路径、AI 准入、搜索词边界、广告位边界等证据类型。
但服务层 save_review_record 本身没有确认 evidence_snapshot 内容能回看 expected_object_id。
如果脚本或测试绕过 API，只要快照类型完整但内容指向其他对象，仍可能污染复盘样本。

原因：
1. API 路由层有对象引用校验，但真正写入函数缺少同等门禁。
2. 证据类型完整只能证明“链路字段齐”，不能证明“链路属于当前复盘对象”。
3. 7/14 天复盘样本一旦混入其他对象，后续规则反馈会误判建议是否有效。

解决：
1. save_review_record 增加 _review_record_evidence_snapshot_has_expected_object 校验。
2. expected_object_id 支持稳定尾部对象文本，例如 search_term:1:beach essentials 可用 beach essentials 回看。
3. 新增服务层测试，验证对象是 kids sunglasses 但快照指向 boys sunglasses 时必须阻断保存。
4. 保持缺证据错误优先级，先提示缺少诊断路径 / AI 准入 / 广告位表现，再判断对象引用不一致。

防错规则：
1. ReviewRecord 写入前必须同时满足：对象预期一致、快照签名继承、证据类型齐全、快照内容可回看当前对象。
2. 不要把“有完整 evidence_snapshot”当成“能保存复盘记录”。
3. API 层有门禁不代表服务层可以没有门禁；真正写入点必须自带底线。
4. 未到 7/14 天窗口时只能显示 waiting_review_window，不保存复盘结论，不自动改规则。

验证方式：
python -m py_compile backend/app/services/manual_actions.py backend/tests/test_manual_actions.py
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_actions.py
$env:PYTHONPATH='backend'; python scripts\inspect_review_readiness.py --market-id 1 --compact
git diff --check
```

## 2026-06-26：右侧人工动作区不能变成第二个诊断区

```text
问题：
右侧人工动作承接原本默认展示“人工动作默认证据层”，并直接铺开当前判断、能证明、不能证明、人工下一步。
同时 Parent ASIN 单屏证明边界也默认展开，导致用户点击人工动作前又读一遍中间诊断区的证明材料。

原因：
1. 为了保证人工动作证据完整，把诊断证明字段放进了右侧默认层。
2. 中间诊断区已经负责解释证据，右侧重复解释会增加动作前决策成本。
3. 右侧真正职责是保存准入、动作边界和复盘回读，不是重新诊断。

解决：
1. 将右侧默认卡改为“人工动作保存准入”。
2. 默认只展示动作对象、留痕证据、复盘窗口和动作边界。
3. 不再在默认卡展示 defaultEvidenceItem.purpose / judgement / proves / doesNotProve。
4. Parent ASIN 单屏证明边界改为折叠审计，保留来源追溯但不默认铺开。

防错规则：
1. 右侧人工动作区只回答“是否能保存、保存什么、怎么复盘”，不要重复中间诊断证明。
2. 完整证据链可以折叠审计，但不能从默认层删除到不可追溯。
3. 7/14 天复盘必须沿点击时 evidence_snapshot 回看，不能用当前页面重新生成证据替代历史证据。
4. 收窄右侧时不能改变允许动作范围，也不能包装成自动广告动作。

验证方式：
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-26：广告组四层复核步骤不能默认重复铺开

```text
问题：
当前广告组复核路径已经有优先证据层、证据切换器、完整运营检查清单和各层明细，但“当前广告组复核顺序”仍默认铺开四层逐项步骤。
用户从 Parent ASIN 下钻后，会在默认层反复读到投放商品、投放词、搜索词和广告位说明，增加决策时间。

原因：
1. 为了保证证据链完整，把审计步骤放进了默认阅读层。
2. 证据切换器和完整检查清单已经承担四层复核说明，再默认展开一次会形成重复。
3. 降低阅读成本不等于删证据，正确做法是区分默认判断层和展开审计层。

解决：
1. 保留“本次优先证据层”和“先按顺序复核”作为默认层。
2. 将四层逐项复核步骤收进 productScopeAdGroupReviewOrderDetails 展开项。
3. 保留“搜索词或广告位不能归因到单个广告 ASIN，也不执行广告动作”的边界说明。
4. 用结构测试锁定默认层先给路径，再把四层步骤放进展开审计。

防错规则：
1. 广告组复核默认层只回答“本次先看哪层、复核路径是什么”。
2. 投放商品、投放词、搜索词、广告位的逐项说明可以展开审计，但不要在多个默认层重复铺开。
3. 降噪时不能删除证据链，也不能把搜索词或广告位写成单个 ASIN 的归因证据。
4. 不要为了反复解释对象边界而牺牲用户的扫描效率。

验证方式：
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-26：广告证据下钻按钮不能只滚到三栏容器

```text
问题：
Parent ASIN 单屏作战卡已经有“进入广告证据下钻”按钮，但原交互只是滚动并聚焦到整个广告诊断工作台。
用户点击后仍要在左侧队列、中间诊断和右侧确认之间自己寻找广告组证据，路径仍然偏长。

原因：
1. 按钮文案承诺进入广告证据，但落点是大容器，不是具体证据层。
2. Parent ASIN 进入广告证据后的第一判断层是广告组优先判断，而不是三栏工作台顶部。
3. 如果只滚到容器，单屏作战卡减少阅读成本的价值会在下钻时被抵消。

解决：
1. 增加广告组优先判断锚点 adGroupPriorityGateRef。
2. “进入广告证据下钻”优先滚动并聚焦到广告组优先判断。
3. 没有可聚焦广告组时，退回广告诊断工作台容器，并提示需要补齐广告组、投放商品、投放词、搜索词和广告位证据。
4. 下钻状态提示读回默认广告组名称和证据层，不生成任何自动广告动作。

防错规则：
1. 写着“进入某层证据”的按钮，落点必须尽量对齐该证据层，而不是只滚到页面大容器。
2. Parent ASIN 单屏卡进入广告证据时，默认第一锚点是广告组优先判断。
3. 缺广告组证据时必须说明补证路径，不能包装成广告组结论。
4. 焦点优化不能改变对象模型，也不能触发人工动作写入。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
```

## 2026-06-26：Parent ASIN 优先清单行不能变成小报表

```text
问题：
左侧已经有 Parent ASIN 首页分诊摘要、分诊桶和优先清单，但优先清单每一行仍默认展示工作流、主问题、证据、默认下钻、排序依据、decisionBadge、下一步和边界。
当用户需要扫多个 Parent ASIN 时，每一行都会像一小段报表，仍然增加决策时间。

原因：
1. 为了解释排序依据，把审计字段直接铺在默认列表行里。
2. 左侧清单的职责是帮助用户选择 Parent ASIN，不是承载完整诊断证据。
3. 单屏作战卡和中间诊断区已经负责完整链路，左侧重复铺开会造成信息噪音。

解决：
1. 优先清单行默认只展示状态、判断、证据、下一步和默认下钻。
2. 排序依据、decisionBadge、边界不再默认铺在行内。
3. 按钮 aria-label 保留优先级、工作流状态、下一步和边界，避免可访问读回和审计语义丢失。
4. 对行内判断摘要做两行限制，防止长文案撑开左侧队列。

防错规则：
1. 左侧 Parent ASIN 清单只回答“选谁、为什么大致先看、下一步去哪层”。
2. 完整排序依据和对象边界放到单屏作战卡、诊断区或审计层，不在每个列表行默认铺开。
3. 降低阅读成本时不能删除证据链，也不能把清单行包装成自动广告动作入口。
4. 不要为了修“读报纸感”继续新增摘要卡；先减少重复说明。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-26：搜索词表现复核不能写得像切换经营诊断入口

```text
问题：
Parent ASIN 广告搜索词表现复核虽然已经不是“语义组复核”，但第一层仍出现“筛选”“核心指标”“聚合下的诊断”等表达。
用户点击卡片后会看到左侧队列和中间信号变化，容易误以为经营诊断入口、Parent ASIN 或广告对象被切换。

原因：
1. SearchIntent 是当前 Parent ASIN 广告上下文下的二级证据聚焦，不是 ProductScope。
2. 点击卡片会聚焦同类 SearchTerm 信号并打开具体证据链，但不会改变顶部经营诊断入口。
3. 如果页面使用“筛选”或“核心指标”这类表述，用户会把它理解成报表筛选或入口切换。

解决：
1. 激活态改为“当前 Parent ASIN 广告搜索词证据聚焦”。
2. 点击说明改为“临时聚焦同类 SearchTerm 信号，打开具体搜索词证据链”。
3. 卡片默认摘要改为“判断依据”，摘要文案说明是“广告表现判断依据”。
4. 入口锁定提示改为“经营入口未切换”，并把第二行改成“二级搜索词聚焦”。

防错规则：
1. 搜索词表现复核只能表达二级证据聚焦，不能写成经营诊断入口切换。
2. SearchIntent 不能升级成 ProductScope、人工动作对象或商品归因对象。
3. 默认层应该告诉用户“先看哪条具体 SearchTerm 证据链”，而不是让用户读一组裸指标。
4. 发现阅读成本高时，优先调整信息层级和文案，不要推翻三栏工作台或重写数据模型。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-26：右侧预检一致性不能只校验三项搜索词字段

```text
问题：
SearchTerm 人工处理前，右侧预检一致性原本只检查搜索词、Parent ASIN 广告搜索词表现复核、搜索词表现判断三项。
这会让页面看起来已经可保存，但实际缺少广告组、同组投放商品、逐投放上下文、投放词证据、广告位边界和补证边界时，7/14 天后无法复盘当时为什么处理。

原因：
1. 前端一致性校验过度聚焦“搜索词字段是否存在”，没有覆盖 Amazon 广告实际分析链路。
2. 广告组是投放容器，一个广告组可投放多个商品，SearchTerm 表现必须结合广告组、投放商品和投放词复核。
3. 广告位、ABA 和证据缺口不能被当成结论，但必须作为人工留痕边界保存。

解决：
1. SearchTerm 人工预检一致性新增完整复核链校验。
2. 必须包含 SearchTerm、Parent ASIN 搜索词表现聚合、搜索词表现判断、广告组合流、同组投放商品、逐投放上下文、投放词证据、广告位边界、ABA 背景、证据缺口、需要补证和动作边界。
3. 缺少任一关键边界时，前端显示 blocked，提示不能把前端读回当成复盘证据。
4. 不新增自动广告动作，仍只允许记录观察、标记已处理、加入复盘和忽略本次。

防错规则：
1. 右侧人工确认区必须回答“会保存什么复盘证据”，不能只回答“能不能点按钮”。
2. SearchTerm 信号的人工留痕必须保存广告组、同组商品、逐投放、投放词和广告位边界。
3. ABA 只能作为站点级背景，不能写成店铺、产品或广告归因证据。
4. 不要因为补证据链就矫枉过正成 BI 大盘；只补支撑当前业务判断的最小链路。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id 'parent_asin:B00K4W4AAA' --expected-object-id 'search_term:1:beach essentials' --expected-object-type search_term --action-type add_to_review --top 5 --compact
```

## 2026-06-26：待复盘来源不能按日志顺序机械读第一条

```text
问题：
规则反馈默认摘要已经能显示“待复盘来源”，但如果直接读取 pendingSources[0]，可能优先展示最新却证据不完整的来源。
真实运行态里 boys sunglasses 排在第一，但 Parent ASIN 经营入口仍待补；beach essentials 虽排在第二，却有更完整的 Parent ASIN、广告 ASIN、广告组、投放词和 SearchTerm 上下文。

原因：
1. pending_source_candidates 的顺序来自待复盘来源读回，不等于业务复核优先级。
2. 默认摘要是用户第一眼判断入口，不能把日志顺序当成可落地顺序。
3. 如果默认展示证据不完整来源，用户会先处理缺口，而不是先处理可复核链路。

解决：
1. RuleFeedbackPrioritySummary 增加 defaultPendingSource。
2. defaultPendingSource 按 Parent ASIN 来源、当前商品范围广告 ASIN、SearchTerm 样本、逐投放来源和广告组上下文评分。
3. 包含“经营入口待补”的来源降低默认优先级，但不从完整列表删除。
4. 工作台默认摘要读取 defaultPendingSource，完整审计材料仍展示全部 pendingSources。

防错规则：
1. 默认待复盘来源不能机械读取第一条。
2. 优先级只影响默认展示，不改变 pending_source_candidates 原始列表。
3. defaultPendingSource 不是 ReviewRecord，也不能当作规则反馈候选。
4. 禁止把证据完整度评分包装成自动改规则或自动广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
```

## 2026-06-26：没有 ReviewRecord 时不能伪造规则反馈候选

```text
问题：
真实运行态 review_feedback.total=0，candidate_groups 为空。
如果为了让页面更“有内容”而把 ManualAction 或 ReviewTodo 直接塞进 candidate_groups，就会把未到期人工留痕误包装成已保存规则反馈样本。

原因：
1. 规则反馈候选组的业务含义是已保存 ReviewRecord 的样本池，不是所有人工动作。
2. 当前只有 ManualAction / ReviewTodo，最早到 2026-06-30 才能进入 7 天复盘判断。
3. 用户仍需要知道哪些人工留痕未来可能成为复盘样本，但这个信息必须和已保存候选组分开。

解决：
1. 新增 pending_source_candidates，只展示待复盘人工留痕来源。
2. candidate_groups 在没有 ReviewRecord 时继续为空。
3. pending_source_candidates 显示 Parent ASIN来源、SearchTerm样本、逐投放来源和 ABA 边界。
4. 同一 object_id + group_label 的重复人工留痕去重，只保留最新来源。

防错规则：
1. ManualAction / ReviewTodo 不能直接当作规则反馈候选。
2. 未保存 ReviewRecord 前，只能展示待复盘来源和保存门槛，不能判断规则有效、无效或需要调整。
3. 待复盘来源必须明确“不形成规则反馈候选、不自动改规则、不自动执行广告动作”。
4. 真实运行数据有重复人工留痕时，默认读回要去重降噪，避免用户看重复样本。

验证方式：
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_triage_service.py::test_review_readiness_shows_manual_action_context_coverage_without_saved_review_records backend/tests/test_signal_triage_service.py::test_review_readiness_summarizes_saved_review_records_as_rule_feedback -q
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_triage_cli.py::test_review_readiness_cli_summarizes_saved_review_records_as_rule_feedback backend/tests/test_api_routes.py::test_signal_triage_rule_feedback_after_saving_ready_review_record -q
$env:PYTHONPATH='backend'; python -m py_compile backend/app/services/signal_triage.py
cd frontend
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-26：规则反馈候选组不能只显示抽象语义组

```text
问题：
规则反馈候选组已经能按语义组 / ABA 参考词聚合已保存 ReviewRecord。
但如果默认只显示“规则语义：海滩出行用品 / worse 1 / beach essentials”，用户仍然不知道这个反馈样本来自哪个 Parent ASIN、哪个具体 SearchTerm、哪条逐投放链路，复核时还要再翻完整证据。

原因：
1. candidate_groups 的原始目标是规则反馈样本归类，容易被压缩成内部统计口径。
2. 用户实际复核规则时，先要确认样本业务来源，而不是先读分组 ID 或结果数量。
3. ABA 参考词如果不和 Parent ASIN、SearchTerm、逐投放来源并列展示，容易被误读成店铺或广告归因证据。

解决：
1. candidate_groups 增加 sample_parent_scopes，说明 Parent ASIN 来源。
2. candidate_groups 增加 sample_search_terms，说明具体 SearchTerm 样本。
3. candidate_groups 增加 sample_ad_contexts，说明逐投放来源。
4. 前端候选组文案同步展示 Parent ASIN来源、SearchTerm样本、逐投放来源，并保留 ABA 边界。

防错规则：
1. 规则反馈候选组必须服务人工复核优先级，不能只服务内部统计。
2. SearchTerm 规则反馈样本必须尽量读出 Parent ASIN、SearchTerm、逐投放和 ABA 背景。
3. ABA 永远只是站点级背景，不能因为出现在候选组里就升级成广告归因证据。
4. 补来源不能矫枉过正成新页面或新分析层；先用现有候选组轻量表达。

验证方式：
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_triage_service.py::test_review_readiness_summarizes_saved_review_records_as_rule_feedback backend/tests/test_signal_triage_service.py::test_review_readiness_groups_saved_review_record_snapshot_without_manual_action -q
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_triage_cli.py::test_review_readiness_cli_summarizes_saved_review_records_as_rule_feedback backend/tests/test_api_routes.py::test_signal_triage_rule_feedback_after_saving_ready_review_record -q
$env:PYTHONPATH='backend'; python -m py_compile backend/app/services/signal_triage.py
cd frontend
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-26：规则反馈样本不能缺少逐投放上下文覆盖

```text
问题：
ReviewRecord 已保存 evidence_snapshot，规则反馈记录也会展示复盘证据链覆盖。
但 SearchTerm 样本的覆盖口径没有把逐投放上下文列为必备项，导致规则反馈可能只显示结果分布、广告组合流、投放词和广告位边界，而没有说明当时是否看过逐投放复核顺序。

原因：
1. 规则反馈层读取的是 ReviewRecord 样本池，容易被简化成 improved / worse 分布。
2. SearchTerm 复盘的关键不是结果标签本身，而是当时是否沿广告组、投放词和广告 ASIN 承接顺序完成复核。
3. 如果规则反馈没有显性回读逐投放上下文，后续人工复核阈值和建议动作时会缺少样本证据边界。

解决：
1. 将“逐投放上下文”加入 SearchTerm 规则反馈样本覆盖口径。
2. 样本包含该标签时显示已覆盖逐投放上下文。
3. 样本缺少该标签时显示缺口：逐投放上下文。
4. 前端规则反馈只展示后端 evidence_groups，不自行发明覆盖结论。

防错规则：
1. 规则反馈样本池不能只看复盘结果分布，必须回看保存时的 evidence_snapshot。
2. SearchTerm 规则反馈样本必须显性校验逐投放上下文。
3. 缺少逐投放上下文的样本不能被当作完整 SearchTerm 复盘样本。
4. 规则反馈只进入解释层和人工复核优先级，不自动改规则，不自动执行广告动作。

验证方式：
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_triage_service.py::test_review_readiness_summarizes_saved_review_records_as_rule_feedback -q
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_triage_cli.py::test_review_readiness_cli_summarizes_saved_review_records_as_rule_feedback -q
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_api_routes.py::test_signal_triage_rule_feedback_after_saving_ready_review_record -q
$env:PYTHONPATH='backend'; python -m py_compile backend/app/services/signal_triage.py
cd frontend
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-26：复盘默认读回不能缺少逐投放顺序

```text
问题：
逐投放上下文已经进入人工点击时 evidence_snapshot，完整证据回读也会检查该标签。
但复盘待办默认业务读回主要展示原始诊断路径、默认展开焦点、复核证据层、搜索词表现判断和人工下一步，没有把逐投放复核顺序作为默认层必须回看的内容。

原因：
1. 完整证据回读回答“快照里有没有逐投放上下文”，但默认业务读回还没有直接回答“到期后先按哪条逐投放顺序回看”。
2. SearchTerm 复盘如果只看搜索词合计指标，容易把处理后变化误解成单个广告组、广告 ASIN 或投放词的确定归因。
3. 如果把逐投放顺序强加给所有对象，又会污染广告商品、广告位等非 SearchTerm 复盘。

解决：
1. SearchTerm 复盘待办默认业务读回新增“逐投放复核顺序”。
2. SearchTerm ready 门槛要求有逐投放上下文、投放词证据、搜索词边界和广告位边界。
3. 非 SearchTerm 待办不强制逐投放上下文。
4. 缺少逐投放上下文时默认业务读回 blocked，提示不能知道当时先看哪个广告组、投放词和广告 ASIN 承接顺序。

防错规则：
1. 复盘默认层必须能回看当时的关键判断顺序，不能只把关键证据藏在完整账本里。
2. SearchTerm 复盘不能只看搜索词合计指标，必须回看广告组、投放词、广告 ASIN 承接和广告位边界。
3. 逐投放顺序只适用于 SearchTerm 复盘，不要套到所有对象。
4. ReviewTodo 只代表排程和证据快照读回，不代表建议已经有效，也不自动执行广告动作。

验证方式：
cd frontend
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
git diff --check
```

## 2026-06-26：逐投放上下文不能只藏在展开表现行里

```text
问题：
SearchTerm 详情里已经有逐投放上下文表现行，但默认层没有直接告诉用户先看哪个广告组、为什么先看、能证明什么、不能证明什么、人工下一步是什么。
用户需要展开并自己比较多行指标，容易重新变成“看报纸式”的广告数据阅读。

原因：
1. 逐投放表现行回答“每行指标是什么”，没有默认回答“哪行最值得先复核”。
2. 同一个 SearchTerm 可能跨多个广告组和投放词，缺少优先摘要时用户要自己推理排序原因。
3. 如果把逐投放上下文升级成主入口，会偏离 AI Copilot 主链路；它应该是 SearchTerm 复核链路里的证据层。

解决：
1. 为逐投放表现行补充能证明、不能证明和人工下一步。
2. 从已排序表现行生成逐投放上下文优先摘要。
3. 默认先展示“先看哪个广告组 / 为什么先看 / 能证明 / 不能证明 / 人工下一步”，完整表现行继续折叠保留。
4. 明确禁止把逐投放表现解释为自动归因、自动加词、否词、调价、广告位加价或暂停广告依据。

防错规则：
1. 降低用户决策成本时，必须围绕当前目标链路收束，不把证据层改成新的主入口。
2. 逐投放上下文默认层必须说明复核顺序和动作边界，不能只展示裸指标。
3. 逐投放上下文不能替代 Parent ASIN、广告 ASIN、广告组或广告位完整判断。
4. 复盘时需要能回看当时的逐投放复核顺序，不能只保存搜索词聚合指标。

验证方式：
cd frontend
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-26：广告位证据不能只藏在展开明细里

```text
问题：
广告位证据判断已经在当前广告组复核路径中展示，能说明广告组级广告位、活动级广告位和证据缺口。
但优先广告组和广告组列表默认层主要展示广告位数量，用户仍要展开明细才能知道当前能不能下广告位结论。

原因：
1. 数量指标回答“有几条”，没有直接回答“证据层级够不够”。
2. 搜索词风险会优先占用广告组闭环状态，广告位缺口容易被压到下层。
3. 如果为了突出广告位而改掉主诊断排序，会矫枉过正；广告位只是证据层，不是所有问题的主结论。

解决：
1. 在 ProductScopePlacementEvidenceDecision 增加 status。
2. status 只复用 placement_count、campaign_placement_count 和人工门禁状态。
3. 优先广告组、广告组列表行、当前广告组复核路径和广告位判断块都展示广告位状态。
4. 状态只表达“广告位可复核 / 只有活动背景 / 广告位缺口”，不生成优化动作。

防错规则：
1. 广告位数量必须翻译成证据层级，不能只展示裸指标。
2. 只有活动级广告位背景时，必须明确不能替代广告组级判断。
3. 广告位状态不能变成自动调价、广告位加价、预算调整或暂停建议。
4. 降低阅读成本时不能改掉主诊断排序，广告位状态应作为并列证据提示。

验证方式：
cd frontend
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-26：搜索词复核卡不能只给运营判断而缺少闭环状态

```text
问题：
Parent ASIN 广告搜索词表现复核已经展示运营判断、核心指标、优先 SearchTerm、指标目的和证据边界。
但用户默认仍要从多段信息里自己判断当前这组搜索词到底该扩量复核、止损复核、补证还是仅观察，增加了决策成本。

原因：
1. 运营判断说明“指标偏向什么”，但没有直接说明“当前复核闭环处于哪一步”。
2. 搜索词层天然跨广告组、投放词、广告位和 ABA 背景，缺少状态翻译时容易变成读报表。
3. 如果为了降低阅读成本而删掉指标目的和证据边界，又会丢失可追溯证据，属于矫枉过正。

解决：
1. 在 SearchIntentReviewCard 增加 reviewStatus。
2. reviewStatus 只复用现有 metrics、primarySearchTerm、evidence_gap 和表现行数。
3. 搜索词复核卡默认在核心指标前展示闭环状态、原因和人工下一步。
4. 展开证据中同步回显闭环状态，保证默认层和证据层一致。

防错规则：
1. 搜索词表现分组默认层必须先说明闭环状态，不能只给运营判断或裸指标。
2. 搜索词表现分组不是人工动作对象，最终人工动作仍只能落到具体 SearchTerm 信号和右侧人工确认。
3. 证据不足时只能显示证据补齐或仅观察，不能包装成自动加词、否词、调价或暂停广告。
4. 降低用户决策成本时必须保留指标目的和证据边界，不能删除追溯材料。

验证方式：
cd frontend
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-26：广告组层不能只展示三段判断而缺少闭环状态

```text
问题：
广告组层已经展示问题类型、证据强度、人工下一步，也有完整运营检查清单和证据切换器。
但用户从 Parent ASIN 下钻到广告组后，仍需要自己判断当前广告组到底是待人工复核、只读复核、证据缺口、广告位缺口、容器边界还是仅观察。

原因：
1. 三段判断回答的是“问题/证据/下一步”，但没有直接回答“当前广告组处于哪个闭环阶段”。
2. 广告组下数据层很多，缺少状态翻译时用户仍会回到读报表模式。
3. 如果为了解决这个问题而重做广告组页或删除明细，会丢掉证据追溯，属于矫枉过正。

解决：
1. 在 ProductScopeAdGroupDiagnosisRow 增加 diagnosisStatus。
2. diagnosisStatus 只复用现有广告 ASIN、搜索词、广告位、候选门禁和证据合流结果。
3. 广告组优先判断、广告组列表、当前广告组复核路径都展示状态、原因和下一步。
4. 保留完整运营检查清单、投放商品、投放词、搜索词、广告位和推理细节作为审计材料。

防错规则：
1. 广告组默认层必须先说明闭环状态，不能只给三段判断或裸指标。
2. 广告组仍是投放容器，不是产品；状态不能把广告组升级成商品动作对象。
3. 只读复核不能写人工动作；待人工复核也只能由右侧人工按钮触发。
4. 降低阅读成本时必须保留完整证据链，不删除追溯材料。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-26：Parent ASIN 队列不能只给排序不说明闭环状态

```text
问题：
左侧 Parent ASIN 队列已经能按复盘待办、高优先信号、广告证据和搜索词复核排序。
但队列行主要展示优先级、排序依据和下一步，用户看多个 Parent ASIN 时仍需要逐条判断当前对象到底是待人工确认、等待复盘、到期复盘、仅观察还是证据缺口。

原因：
1. 排序回答“谁排前面”，没有直接回答“这个对象现在处于哪个业务阶段”。
2. 用户真正要降低的是决策成本，不是获得更多裸指标。
3. 如果为了解决阅读成本而删除排序、证据链或分诊桶，又会矫枉过正，导致业务证据不可追溯。

解决：
1. 在 ProductScopePriorityQueueItem 上新增 workflowStatus。
2. workflowStatus 只复用现有信号、ReviewTodo 和广告证据，不新增后端字段。
3. 左侧队列行展示状态、原因和下一步。
4. 到期复盘只表示 ReviewTodo 到期，是否可保存仍以后续 ready effect 和保存门禁为准。

防错规则：
1. Parent ASIN 队列第一眼必须说明闭环状态，不能只显示排序依据。
2. 降低阅读成本时保留证据链和分诊桶，不删除可追溯材料。
3. 无广告证据的 Parent ASIN 只能显示证据缺口，不进入广告动作对象。
4. 复盘状态不能越权包装成自动保存、自动调价、自动加词、自动否词或自动暂停。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-26：规则反馈默认层不能只依赖候选组审计材料

```text
问题：
规则反馈区已经有规则反馈候选、样本池、候选组、closure checklist 和 records。
但样本来源、复核优先级和动作边界主要藏在一行 detail 或折叠审计材料里，用户保存复盘后仍要展开细节判断这条样本为什么能反馈规则。

原因：
1. 规则反馈的业务问题不是“有哪些样本明细”，而是“样本从哪里来、先复核什么、不能自动改什么”。
2. 如果默认只显示长 detail，用户会继续像读审计表一样找重点。
3. 如果删除候选组和 records，又会丢失证据回看和样本追溯，属于矫枉过正。

解决：
1. 在规则反馈默认摘要中新增“规则反馈样本来源读回”。
2. 默认展示样本来源、复核优先级、动作边界。
3. 只复用现有 latestReviewRecord、规则反馈候选、样本池摘要和规则改进门槛。
4. 完整候选组、样本池和 records 继续保留在折叠审计材料中。

防错规则：
1. 规则反馈默认层必须读回样本来源，不能只让用户展开候选组。
2. ReviewRecord 是人工复盘事实，规则反馈只是解释层和人工复核优先级。
3. 规则反馈不能自动改规则，不能自动执行广告动作。
4. 降低阅读成本时必须保留完整样本追溯材料，不能删除证据链。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
git diff --check
```

## 2026-06-26：复盘保存区不能默认让用户读审计清单

```text
问题：
复盘待办区已经有复盘保存顺序核对、效果窗口、保存门槛、指标表和保存前检查清单。
但这些内容主要藏在完整状态账本里，默认摘要只简略说明能否保存，用户仍需要展开后自行判断保存依据和下一步。

原因：
1. ReviewRecord 保存前的业务问题是“能不能保存、依据是什么、下一步人工做什么”。
2. 完整检查清单是审计材料，不应该默认承担第一眼判断。
3. 如果把检查清单删除或合并进单句结论，又会丢失保存门禁证据，属于矫枉过正。

解决：
1. 在复盘读回默认摘要中新增“复盘保存业务判断”。
2. 复用现有保存门槛、指标窗口、保存前检查和指标行数量。
3. 默认只展示能不能保存、保存依据、下一步人工动作。
4. 完整保存路径、指标窗口、指标表和检查清单继续保留在完整状态账本中。

防错规则：
1. 复盘保存默认层必须先回答业务判断，不能只给保存门槛标题。
2. 保存依据必须来自 ready 指标窗口和保存前检查，不能用当前页面缓存或其他对象证据替代。
3. ReviewRecord 只保存人工复盘结论，不自动改规则，不执行广告动作。
4. 降低阅读成本时保留完整审计材料，不能删除门禁证据。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
git diff --check
```

## 2026-06-26：复盘默认摘要不能只展示排程和效果门槛

```text
问题：
右侧复盘状态区已经有“复盘读回默认摘要”，能说明是否已记录、什么时候复盘、现在能否保存和下一步。
但默认层没有直接读回“这条复盘待办到期后先沿哪条诊断路径回看”，用户仍可能展开完整状态账本，或只看处理后指标涨跌。

原因：
1. ReviewTodo 的业务价值不是排程本身，而是保留人工点击时的诊断路径和证据边界。
2. 如果默认层只说排程和保存门槛，7/14 天后用户容易脱离 Parent ASIN、广告组和搜索词证据链判断效果。
3. 不能把完整证据全部提前默认展开，否则会重新增加阅读成本。

解决：
1. 复盘读回默认摘要新增“复盘待办诊断路径读回”。
2. 只复用现有 selectedReviewTodoDecisionReadback 的原始诊断路径、默认展开焦点和复核证据层。
3. 默认文案明确：到期复盘先沿 Parent ASIN、广告组、搜索词证据回看，再判断处理后指标。
4. 结构测试约束该摘要必须先于完整状态账本。

防错规则：
1. 复盘默认层必须回答“到期后先回看哪条诊断路径”，不能只展示排程状态。
2. ReviewTodo 只能表示点击时证据快照和 7/14 天排程，不代表效果结论。
3. 降低阅读成本时不能删掉完整证据链，完整证据继续放在折叠账本和待办明细中。
4. 任何复盘文案都不能暗示自动调价、加词、否词或暂停广告。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
git diff --check
```

## 2026-06-26：右侧人工按钮前不能丢失诊断路径

```text
问题：
Parent ASIN 第一屏已经展示四步诊断路径。
但右侧人工确认区默认层仍先展示按钮执行摘要，用户点按钮前不一定能确认该按钮承接的是同一条 Parent ASIN -> 广告组 -> 搜索词路径。

原因：
1. 人工动作不是孤立按钮，它必须承接上游诊断路径和证据边界。
2. 如果右侧只展示“当前可点什么 / 为什么可点 / 点后写什么 / 何时复盘”，用户仍需要回头找经营入口、广告组和搜索词上下文。
3. 不能把完整预检证据全部铺开，否则会把右侧变成长审计页。

解决：
1. 在 manualActionButtonCommandSummary 前新增“人工按钮前诊断路径核对”。
2. 固定读回四步：经营入口、广告组定位、搜索词证据、按钮前核对。
3. 路径只使用当前页面已有状态，不新增 API，不改变人工动作写入。
4. 结构测试约束路径核对必须在人工按钮执行摘要和人工动作按钮之前。

防错规则：
1. 右侧人工按钮前必须读回诊断路径，不能只展示按钮说明。
2. 默认层只展示路径摘要，完整 preflight 和证据快照继续放在折叠审计区。
3. 人工动作仍以后端 preflight 的稳定对象为准，搜索词聚合不是人工动作对象。
4. 任何文案都不能暗示自动调价、加词、否词或暂停广告。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
git diff --check
```

## 2026-06-26：Parent ASIN 第一屏不能只有模块没有路径

```text
问题：
Parent ASIN 单屏作战卡已经展示广告证据、优先级、广告组、搜索词、AI 准入和复盘门槛。
但这些信息仍是并列模块，用户需要自己判断先看经营入口、广告组、搜索词，还是右侧人工动作。

原因：
1. 降低决策成本不是删除证据，而是把阅读顺序讲清楚。
2. 如果第一屏只有多个模块，连续看多个 Parent ASIN 时仍像逐张报纸扫描。
3. 不能把完整销售、广告组、搜索词和复盘证据全搬到第一屏，否则会重新变成普通长报表。

解决：
1. ProductScopeSingleScreenCommandCard 新增“Parent ASIN 单屏诊断路径”。
2. 固定四步：经营入口 -> 广告组定位 -> 搜索词证据 -> 人工动作 / 复盘。
3. 每一步只读回当前对象、判断边界和下一步，不新增 API，不新增自动广告动作。
4. 结构测试约束该路径必须存在。

防错规则：
1. 第一屏默认必须回答“先按什么顺序看”，不能只堆模块。
2. 路径表达只能复用当前页面已有证据，不用泛化指标填充。
3. 搜索词仍只能说明广告上下文，不能自动归因到单个广告 ASIN。
4. 人工动作仍只允许记录观察、标记已处理、加入复盘或忽略本次。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
git diff --check
```

## 2026-06-26：搜索词复核顺序不能只留在下层复核区

```text
问题：
上一轮已经为 Parent ASIN 广告搜索词表现复核生成前三优先顺序。
但 Parent ASIN 单屏作战卡的“搜索词复核”格子仍只显示第一组搜索词聚合，用户需要继续下滑到搜索词复核区才能看到前三顺序。

原因：
1. 第一屏负责回答“今天先处理什么”，下层复核区负责展开证据和卡片。
2. 搜索词前三优先顺序如果只出现在下层，连续处理多个 Parent ASIN 时仍会增加一次下滑和阅读成本。
3. 不能把完整搜索词证据搬到第一屏，否则会重新变成长报表。

解决：
1. ProductScopeSingleScreenCommandCard 复用 searchIntentSummary.priorityPathItems。
2. 在第一屏“搜索词复核”格子中展示前三个搜索词复核对象、判断和优先原因。
3. 新增 productScopeSingleScreenSearchIntentPath 紧凑样式，避免扩大第一屏信息量。
4. 结构测试约束第一屏必须渲染“单屏搜索词复核顺序”。

防错规则：
1. 下层搜索词复核区有优先顺序时，Parent ASIN 第一屏也应读回该顺序。
2. 第一屏只展示前三优先对象和原因，不展示完整证据卡片。
3. 搜索词聚合仍不是人工动作对象；人工动作必须继续以后端 preflight 的稳定对象为准。
4. 降低决策成本时必须围绕路径承接，不要重复搬运全部证据。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-25：Parent ASIN 搜索词复核不能让用户逐卡自己排序

```text
问题：
当前“Parent ASIN 广告搜索词表现复核”已经能从 Parent ASIN 视角聚合广告用户搜索词表现。
但默认摘要只说明首要判断和分布，用户如果连续查看多个 Parent ASIN，仍可能需要逐张聚合卡片阅读后自己判断先看哪一组。

原因：
1. 搜索词复核卡片已经有业务问题、指标目的、证明边界和人工下一步，但跨卡片的优先顺序不够显性。
2. 对运营来说，第一眼最重要的是“先看哪几个 SearchTerm 聚合、为什么”，不是先读完整卡片列表。
3. 如果为了降噪删除卡片和证据，又会破坏搜索词、广告组、投放词和广告位的证据链，属于矫枉过正。

解决：
1. SearchIntentReviewDecisionSummary 增加 priorityPathItems。
2. 默认从已排序的搜索词复核卡片中取前 3 个对象，展示对象、判断和优先原因。
3. 在 SearchIntentReviewDecisionSummaryPanel 中新增“搜索词复核优先顺序”。
4. 保留原卡片、指标目的、证明边界、证据缺口和人工下一步，不新增自动广告动作。

防错规则：
1. 搜索词复核默认层必须先告诉用户“先看哪几个”，不能只给分布和完整卡片列表。
2. 优先顺序只能来自当前 Parent ASIN 广告上下文下的搜索词表现聚合，不能覆盖自然搜索或 ABA 店铺归因。
3. 搜索词表现分组仍只是分析视角，不能写成 ProductScope、广告组或人工动作对象。
4. 降低阅读成本时必须保留证据链，不能把必要证据删除成无依据结论。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-25：广告组内投放商品表现不能只展示裸指标

```text
问题：
当前广告组复核路径中的“广告组内投放商品表现”只展示 ASIN、MSKU 和花费 / 点击 / 订单 / 销售额 / ACOS / CVR。
用户需要自己判断这些指标到底服务于商品承接、多商品边界，还是广告组结构诊断。

原因：
1. 广告组内投放商品表现是必要证据层，不适合简单折叠成审计材料。
2. 原展示只有指标，没有业务问题、能证明、不能证明和人工下一步。
3. 对用户来说，裸指标会把 AI 工作台重新拉回普通报表，增加判断成本。

解决：
1. 新增 ProductScopeAdGroupAdvertisedProductsPanel。
2. 默认展示“投放商品业务判断”，包含当前判断、能证明、不能证明、人工下一步。
3. 保留逐广告 ASIN 指标清单，不删除原证据。
4. 结构测试约束该板块必须包含业务判断层，不能退回裸指标列表。

防错规则：
1. 必要证据不能因为降噪而被机械折叠。
2. 投放商品表现必须明确只代表当前广告组下有广告数据的 advertised_products，不代表 Parent ASIN 下所有子 ASIN。
3. 广告商品指标不能自动证明应拆广告组、调价、否词或加词。
4. 每个默认展示的指标块都要说明它服务哪个业务判断。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-25：投放词证据明细不能默认铺开成审计表

```text
问题：
当前广告组复核路径中，“投放词证据”默认铺开每个 keyword_text / target_id 上下文、搜索词样本、指标和边界。
但同屏的搜索词业务判断和证据切换器已经回答投放词证据用于判断什么、能证明什么、不能证明什么和人工下一步。

原因：
1. 投放词明细是必要审计材料，但不是运营默认第一眼要读的业务判断层。
2. 默认铺开会重复搜索词业务判断里的投放词证据结论，增加连续处理多个 Parent ASIN 的阅读成本。
3. 如果为了降噪删除投放词证据，又会矫枉过正，破坏搜索词表现和广告组投放上下文的证据链。

解决：
1. 将 ProductScopeTargetingEvidencePanel 从默认 div 改为 details 折叠区。
2. 默认只显示“展开投放词证据明细”和投放上下文数量。
3. 保留 keyword_text / target_id、搜索词样本、指标、边界和禁止自动加词 / 否词 / 调价说明。
4. 结构测试约束投放词证据必须作为折叠审计材料存在。

防错规则：
1. 投放词证据不能删除，也不能包装成完整关键词库证明。
2. 搜索词业务判断默认层必须先回答当前判断、能证明、不能证明和人工下一步。
3. 逐投放上下文属于审计回看材料，默认应折叠。
4. 信息层级治理不能矫枉过正；每轮围绕最终目标判断是否降低用户决策成本，同时保留落地证据链。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-25：后端端口不应做前端跳转入口

```text
问题：
上一轮为避免用户打开 8000 看到 Not Found，给后端 / 根路径增加了 HTML 入口页，并提供跳转到 5175 的链接。
用户纠正：这个入口很多余，项目应保留原本访问方式，直接访问前端工作台即可。

原因：
1. 8000 是后端 API 端口，不是产品入口。
2. 在后端端口做 5175 跳转，会让用户误以为存在两个前端入口。
3. 项目真正需要防错的是确认 5175 前端和 8000 API 是否指向当前项目，而不是给后端根路径加页面。

解决：
1. 删除 backend/app/main.py 中的 / 根路径 HTML 引导页。
2. 删除 FRONTEND_URL 常量和 HTMLResponse 依赖。
3. 后端测试改为确认 / 仍不是前端入口，且不包含 5175 跳转链接。
4. 保留 /health 作为后端服务身份检查入口。

防错规则：
1. 用户访问入口应保持为 5175 前端工作台，不通过 8000 或相邻后端端口跳转。
2. 8000 根路径出现 Not Found 不等于前端坏了；先检查 /health 和 5175 页面。
3. 不要为了缓解截图误解而给后端 API 端口增加产品入口页。
4. 如需排查旧进程，检查当前 5175 的 API_BASE 和 8000 /health，而不是改变访问路径。

验证方式：
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py::test_root_route_is_not_frontend_entrypoint backend\tests\test_api_routes.py::test_health_route_identifies_current_project -q
```

## 2026-06-25：广告组完整运营检查清单不能默认铺开

```text
问题：
当前广告组复核路径已经默认展示三段复核判断、广告组下具体数据四要素摘要、证据切换器和复核顺序。
但“运营检查清单”仍默认铺开同一批证据项，重复展示目的、当前判断、能证明、不能证明和人工下一步。

原因：
1. 证据切换器已经承担了默认层的“当前证据层为什么存在、证明什么、不能证明什么、下一步做什么”。
2. 完整检查清单属于审计材料，不是运营第一眼必须阅读的判断层。
3. 默认铺开会让中间区重新变成长报表，用户处理多个 Parent ASIN 时仍像读报纸。

解决：
1. 将 ProductScopeAdGroupOperationalChecklistPanel 从默认 div 改为 details 折叠区。
2. 默认只显示“展开完整运营检查清单”和折叠说明。
3. 完整四层证据、证明边界和人工下一步继续保留在折叠区。
4. 结构测试约束完整运营检查清单必须作为折叠审计材料存在。

防错规则：
1. 当前广告组默认层必须先给问题落点、证据缺口、人工下一步、四要素摘要和当前证据层。
2. 完整清单、证明边界和审计材料不能删除，但不应默认铺开。
3. 新增广告组内容时，先判断是否和证据切换器或复核顺序重复。
4. 折叠清单不代表降低证据要求；广告组、投放商品、投放词、搜索词和广告位边界仍必须保留。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-25：Parent ASIN 完整诊断框架不能默认铺开

```text
问题：
Parent ASIN 单屏作战卡已经回答“先判断什么”，但后续 Parent ASIN 运营诊断路径仍默认铺开完整五段诊断框架。
用户连续查看多个 Parent ASIN 时，仍可能被迫阅读筛选器、销售入口、广告组排序、明细数据和 AI 判断五段长说明。

原因：
1. 完整五段框架是审计和解释材料，不是运营第一眼必须阅读的默认层。
2. 默认层同时展示单屏作战卡、决策导览、体检结论和完整框架，会把同一条路径重复解释。
3. 用户真正需要先知道是否进入广告下钻、默认看哪个广告组、下一步做什么人工动作，而不是先读完整框架。

解决：
1. 保留 Parent ASIN 决策导览、体检结论、路径顺序、允许人工动作和边界说明作为默认层。
2. 将完整五段诊断框架放入“Parent ASIN 完整诊断框架”折叠区。
3. 结构测试约束默认路径顺序必须先于完整框架，完整框架必须作为折叠审计材料存在。

防错规则：
1. Parent ASIN 默认层必须先回答业务判断，不应默认铺开完整分析框架。
2. 完整框架、证明边界和人工下一步不能删除，只能折叠为审计材料。
3. 新增 Parent ASIN 分析内容时，必须检查是否和单屏作战卡、决策导览、广告组定位重复。
4. 折叠完整框架不代表降低数据口径要求；Parent ASIN、广告 ASIN、广告组、搜索词、广告位和复盘边界仍必须保留。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-25：人工动作写后不能默认展示成三层技术账本

```text
问题：
人工动作按钮后已经能读回点击后路径、ManualAction、ReviewTodo、ReviewRecord 和 evidence_snapshot 状态。
但这些内容默认全部展开时，用户点击后仍需要从三层技术账本里判断“写入了吗、证据留了吗、待办生成了吗、下一步做什么”。

原因：
1. ManualAction / ReviewTodo / ReviewRecord 是审计和防错链路，不是运营点击后第一眼需要看的业务判断。
2. 右侧栏空间窄，三层证据路径默认展开会继续推高阅读成本。
3. 当前真实状态仍是 waiting_review_window；如果默认展示过多账本，容易弱化“还不能保存复盘结论”的主信息。

解决：
1. 新增“人工动作写后默认摘要”，默认只展示写入了吗、证据留了吗、复盘待办生成了吗、下一步做什么。
2. 将点击后读回路径、读回一致性和人工留痕证据路径读回放入“人工动作写后完整审计材料”折叠区。
3. 结构测试约束默认摘要必须先于完整审计材料，点击后读回路径必须保留在折叠区。

防错规则：
1. 人工动作写后默认层必须先回答业务读回问题，不应先展示 ManualAction / ReviewTodo / ReviewRecord 三层账本。
2. 完整证据路径必须保留为审计材料，不能为了降噪删除。
3. ReviewTodo 只表示复盘排程；ReviewRecord 才是复盘结论；waiting_review_window 不能包装成已改善或规则有效。
4. 写后读回只验证人工留痕和复盘待办，不代表自动执行广告动作。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:review-ui
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-25：规则反馈区不能默认展示成内部样本池账本

```text
问题：
右侧“规则改进门槛”已经能展示规则反馈候选、候选汇总、candidateGroups、closureChecklist 和 records。
但这些内容默认全部展开时，用户需要先读内部规则反馈账本，才能判断当前到底有没有复盘结论、能不能反馈规则、下一步该做什么。

原因：
1. 规则反馈样本池是开发和后续规则复核需要的审计材料，不是运营第一眼需要处理的业务判断。
2. ReviewRecord、review_feedback、candidate_groups 等概念对规则闭环有价值，但默认暴露会增加连续处理多个 Parent ASIN 的阅读成本。
3. 当前真实状态仍是 waiting_review_window，如果默认展示样本池和候选信息，容易弱化“当前不能自动改规则，也不能输出规则已生效结论”的主信息。

解决：
1. 新增“规则反馈默认摘要”，默认只展示有没有复盘结论、能不能反馈规则、反馈依据是什么、下一步人工动作。
2. 将规则改进 readiness、规则反馈候选、候选汇总、候选组、闭环清单和记录列表放入“规则反馈完整审计材料”折叠区。
3. 结构测试约束默认摘要必须先于完整审计材料、规则反馈候选和样本池明细。

防错规则：
1. 规则反馈默认层必须先回答业务判断，不要先展示候选组、样本池或内部规则账本。
2. 没有已保存 ReviewRecord 时，不能把复盘待办、ready 门槛或 waiting_review_window 包装成规则反馈结论。
3. 规则反馈只能进入解释层和人工复核优先级，不能自动改规则，不能自动执行广告动作。
4. 完整候选、样本池和闭环清单必须保留为审计材料，但默认应折叠。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:review-ui
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-25：复盘状态不能默认展示成长账本

```text
问题：
右侧复盘状态区已经能展示全局复盘门禁、历史待办治理、三层闭环、处理留痕、复盘待办、保存门槛和指标表。
但这些内容默认纵向展开时，用户仍需要从状态账本里自行判断“现在到底能不能复盘、下一步做什么”。

原因：
1. 复盘链路为了防错保留了大量门禁和审计信息，但默认阅读层没有先回答业务问题。
2. ReviewTodo、ReviewEffect、ReviewRecord 的状态边界很重要，但直接展开会让运营误以为待办、ready 和结论是同一层。
3. 当前真实接口处于 waiting_review_window，如果默认展示太多技术状态，容易弱化“现在还不能下结论”的主信息。

解决：
1. 新增“复盘读回默认摘要”，先展示已经记录了吗、什么时候复盘、现在能下结论吗、下一步做什么。
2. 将完整复盘门禁、历史治理、三层闭环、待办证据和保存细节放入“复盘完整状态账本”折叠区。
3. 结构测试约束复盘默认摘要必须先于完整状态账本和复盘待办明细。

防错规则：
1. 复盘状态默认层必须先回答业务判断，不应先展示技术账本。
2. ReviewTodo 只能表达 7/14 天排程，不能表达改善结论。
3. ReviewRecord 只有人工保存后才是复盘结论；waiting_review_window 不能包装成 ready 或 saved。
4. 完整门禁和证据必须保留为审计材料，不能为了降噪删除。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:review-ui
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-25：人工按钮前不能默认铺满完整预检材料

```text
问题：
右侧人工确认区已经能展示目标切换、双轨分流、搜索词读回、准入核对、证据快照和点击后验收合同，但这些内容默认全部排在按钮前。
用户真正准备点击人工动作时，第一眼仍要读一段技术预检长链，才能判断当前应该点什么。

原因：
1. 之前把“可审计证据完整”放到了“默认决策可读”之前。
2. 右侧栏空间窄，完整预检、证据快照和写后合同默认展开会把按钮推得很远。
3. 对运营来说，按钮前最重要的是“点什么 / 为什么 / 写什么 / 何时复盘”，不是先读所有合同字段。

解决：
1. 新增“人工按钮执行摘要”，默认只展示当前可点什么、为什么可点、点后写什么、何时复盘。
2. 将完整预检、证据快照和点击后验收合同放入“人工按钮前完整预检证据”折叠区。
3. 结构测试约束摘要必须先于完整预检，完整预检必须保留在按钮前折叠审计区。

防错规则：
1. 右侧人工按钮前默认层必须先回答业务动作问题，不能先铺满技术预检。
2. 完整证据不能删除，但应该作为可展开审计材料。
3. 点击后写入说明只能表达人工留痕和复盘待办，不能包装成自动广告操作。
4. 新增人工动作相关内容时，必须检查是否增加按钮前默认阅读成本。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-25：右侧建议处理不能只展示泛化动作说明

```text
问题：
中间“广告搜索词表现复核链”已经给出当前判断、人工下一步、证据缺口和动作边界，但右侧建议处理区顶部仍主要展示 selectedSignal.suggested_action。
用户看完中间诊断后，还需要在右侧通用建议、预检、按钮和复盘状态之间自行拼接人工路径。

原因：
1. 中间诊断区和右侧建议处理区虽然使用同一批数据，但第一阅读位不同。
2. 当 recommended_candidate 已留痕、manual_action_preview 指向 next_unhandled_candidate 时，右侧更需要明确“当前选中对象的人工承接”。
3. 如果右侧先展示泛化动作说明，容易让用户误以为按钮区和中间诊断不是同一条路径。

解决：
1. 右侧 actionDecisionCard 新增“人工处理承接”。
2. 直接复用 selectedSearchTermOpportunityReviewChain 的当前判断、人工下一步、证据缺口和动作边界。
3. 结构测试要求该承接块先于通用“建议处理边界”展示。

防错规则：
1. 中间诊断区出现“人工下一步”时，右侧建议处理区必须同步展示同源人工路径。
2. 右侧按钮区不能只展示通用建议或技术预检，必须先告诉运营当前要人工处理什么。
3. 推荐对象已留痕、下一候选待授权时，页面必须持续区分两条轨道，不能混用证据或状态。
4. 右侧承接只能引导人工留痕和复盘待办，不能包装成自动加词、否词、调价或暂停广告。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-25：搜索词复核链不能默认铺满完整证据

```text
问题：
“广告搜索词表现复核链”虽然覆盖了对象粒度、复核路径、广告 ASIN、广告组、投放词、广告位、ABA、证明边界和人工下一步，但默认全部展开。
用户需要先读一长串证据，才能找到真正要执行的人工下一步，决策成本过高。

原因：
1. 设计上把完整证据链当成第一阅读对象，而不是把业务判断当成第一阅读对象。
2. 搜索词机会本身涉及广告组、广告 ASIN、投放词、广告位和 ABA，多层证据全部展开后会像报表。
3. 逐投放上下文表现行是复核细节，不应该默认抢占“当前判断 / 人工下一步 / 证据缺口 / 动作边界”的位置。

解决：
1. 默认只展示当前判断、人工下一步、证据缺口、动作边界。
2. 复核分层改为折叠项。
3. 完整证据、证明边界和复核路径改为折叠项。
4. 逐投放上下文表现行改为折叠项。
5. 结构测试约束默认判断摘要必须先于完整证据。

防错规则：
1. AI 信号工作台默认阅读位必须先回答业务判断和人工动作，不应先铺满所有字段。
2. 完整证据必须保留，但默认进入展开项。
3. 搜索词机会不能包装成自动加词、自动否词或自动调价；动作边界要默认可见。
4. 逐投放上下文是复核明细，不是第一屏结论。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-25：推荐对象和诊断合同不能指向不同搜索词

```text
问题：
真实接口中 recommended_candidate 指向 beach essentials，但顶层 diagnosis_contract 指向 b01fay0yl0。
这会让用户在同一屏看到两个不同判断对象，无法确认当前应该复核哪个搜索词、广告组和投放词。

原因：
1. 当推荐候选已有人工留痕时，后端把顶层 diagnosis_contract 切换到 next_unhandled_diagnosis_contract。
2. recommended_candidate、recommended_evidence_drilldown 仍然保留原推荐对象，导致同屏同名字段混用两套对象口径。
3. 前端广告诊断路径只识别旧标签“综合判断 / 广告组问题定位”，没有兼容当前接口真实返回的“排查路径 / 投放上下文 / 同组投放商品表现 / 对象边界”。
4. 业务证据默认截取前 6 个块时，search_term_boundary 可能被截掉，削弱“搜索词不能自动归因到单个 ASIN”的边界提示。

解决：
1. 顶层 diagnosis_contract 默认跟 recommended_diagnosis_contract 对齐。
2. next_unhandled_diagnosis_contract 继续独立返回，只服务下一未处理候选和人工动作预检。
3. 前端广告诊断路径兼容当前接口标签，并映射成综合判断、广告组问题定位、广告 ASIN 承接、投放词结构、广告位边界、对象边界。
4. search_term_boundary 像 context_boundary 一样保留，避免对象边界被证据截取隐藏。

防错规则：
1. 同一屏默认展示的 recommended_candidate、recommended_evidence_drilldown、diagnosis_contract 必须是同一个对象。
2. next_unhandled_candidate 可以存在，但不能替换当前推荐对象的诊断合同。
3. 前端路径映射必须以真实 API block_id 为准，不能只认旧原型标签。
4. 搜索词对象必须持续展示对象边界：它是投放证据，不是广告商品本身，不能自动归因到单个广告 ASIN。

验证方式：
cd backend
python -m pytest tests\test_signal_triage_service.py -q
cd frontend
npm run test:signal-ui
npm run test:signal-ui-runtime
http://127.0.0.1:8000/api/signal-triage?market_id=1&top=5&product_scope_id=parent_asin%3AB00K4W4AAA 返回 recommended=beach essentials、contract=beach essentials、next_contract=b01fay0yl0。
```

## 2026-06-25：打开 8000 根地址不能继续显示 Not Found

```text
问题：
用户打开 http://127.0.0.1:8000/ 时看到 {"detail":"Not Found"}。
这会让用户误以为项目服务未启动，实际 8000 是后端 API，前端工作台在 5175。

原因：
1. FastAPI 只注册了 /health 和 /api/*，没有 / 根路由。
2. 本地开发时用户容易直接打开后端端口；默认 404 没有告诉用户应进入哪个前端地址。
3. 旧 8000 / 5175 进程占用时，启动脚本会自动避让到 8010 / 5174，容易让浏览器继续访问旧进程。

解决：
1. backend/app/main.py 新增 / 根路由，返回轻量 HTML 入口页。
2. 根页明确说明“当前地址是后端 API，不是前端工作台页面”。
3. 根页提供前端入口 http://127.0.0.1:5175/、健康检查 /health 和接口文档 /docs。
4. 后端测试新增 test_root_route_guides_to_frontend_workbench。
5. 已停止旧本项目开发进程并重新启动默认 8000 / 5175。

防错规则：
1. 用户截图出现 8000 Not Found 时，先判断是否打开了后端 API 根地址，不要直接判断前端坏了。
2. 本地验证页面前，必须确认 tmp/local-dev/last-start.json 中 frontend_url 和 api_base 是否为当前浏览器地址。
3. 8000 根路径只做入口提示，不承载业务诊断，也不替代 5175 前端工作台。
4. 启动脚本避让到相邻端口时，要检查旧进程归属，不能让浏览器继续访问旧后端。

验证方式：
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py::test_root_route_guides_to_frontend_workbench backend\tests\test_api_routes.py::test_health_route_identifies_current_project -q
http://127.0.0.1:8000/ 返回 200 text/html，包含前端入口。
http://127.0.0.1:8000/health 返回 ai-ads-signal-workbench。
http://127.0.0.1:5175/ 返回 200。
```

## 2026-06-25：Parent ASIN 扫一眼决策卡不能固定两列挤压左侧栏

```text
问题：
扫一眼决策卡解决了“先看谁”的问题，但左侧栏宽度有限，如果固定两列，长文案会在窄宽度下过度换行，变成新的阅读负担。

原因：
1. 左侧栏在桌面布局里约 300px 起步。
2. 固定 repeat(2, minmax(0, 1fr)) 会让每个卡片只有很窄宽度。
3. 这和“减少看报纸成本”的目标相冲突。

解决：
1. productScopePriorityDecisionShortcuts 改为 repeat(auto-fit, minmax(min(170px, 100%), 1fr))。
2. 空间不足时自动单列，空间足够时再并排。
3. 结构测试增加该响应式规则断言。

防错规则：
1. 左侧首屏卡片不能为了密度牺牲可读性。
2. 跨 Parent ASIN 摘要必须先帮助用户扫描，不应制造新的长文本拥堵。
3. 固定两列布局只适合短指标，不适合带解释句的判断卡。

验证方式：
cd frontend
npm run test:workbench-structure
npm run build
```

## 2026-06-25：Parent ASIN 首页分诊摘要不能先让用户读列表

```text
问题：
左侧已经有 Parent ASIN 首页分诊摘要、分诊桶和优先处理清单，但如果用户第一眼仍然先读 Parent ASIN 列表，就会回到“10 个 Parent ASIN 像看 10 张报纸”的问题。
首页摘要必须先回答决策问题，而不是只把对象排好序。

原因：
1. 原摘要能告诉用户今日优先 Parent ASIN 和分诊桶，但关键判断分散在标题、阅读策略、排序依据和列表行里。
2. 用户连续处理多个 Parent ASIN 时，需要先知道“先看谁 / 为什么 / 打开后看哪层 / 不要做什么”，再决定是否展开。
3. 如果先展示分桶和列表，用户仍会被迫从多个对象中自行推理优先级和下钻层。

解决：
1. 在 Parent ASIN 首页分诊摘要中新增“扫一眼决策卡”。
2. 固定展示四个判断点：先看谁、为什么、打开后看哪层、不要做什么。
3. 分诊桶和优先处理清单继续保留，但必须排在扫一眼决策卡之后。
4. 结构测试约束该卡片必须先于分诊桶出现。

防错规则：
1. 首页摘要要先回答判断问题，再展示对象列表。
2. 跨 Parent ASIN 排序只做导航，不得替代单个 Parent ASIN 下的广告证据链。
3. “打开后看哪层”只能指向广告组、投放商品、投放词、搜索词和广告位证据，不能直接生成广告动作。
4. 不得把首页摘要包装成自动加词、否词、调价或暂停广告。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-25：人工确认后三层读回不能只展示技术状态

```text
问题：
页面已经能展示人工留痕、复盘待办和复盘结论三层状态，但用户真正需要知道的是：这三层是否都能沿同一条点击时证据路径回看。
如果只展示 manual_actions / review_todos / review_records 的数量和状态，运营仍然不知道后续复盘是否还能回到当时的广告证据。

原因：
1. 原有“人工确认复盘闭环”更像状态账本，能说明三层是否存在，但没有把 evidence_snapshot 的继承关系作为第一阅读对象。
2. ReviewTodo 只是 7/14 天排程，不是复盘结论；如果不强调证据路径，容易把待办误读成效果判断。
3. ReviewRecord 只能沿已验证 evidence_snapshot 保存人工结论，不能从处理后指标反推当时判断。

解决：
1. 新增“同一证据路径读回” helper。
2. 右侧人工处理区新增“人工留痕证据路径读回”卡。
3. 先展示 ManualAction 点击时 evidence_snapshot 是否留存，再展示 ReviewTodo 是否继承 7/14 天路径，最后展示 ReviewRecord 是否沿用已验证快照。
4. 对搜索词复盘链路增加更深标签核对，避免只靠搜索词裸指标进入复盘。
5. 保留原有三层状态账本，但放在证据路径读回之后阅读。

防错规则：
1. 人工确认后的第一层读回必须解释证据路径，而不只是状态数量。
2. ReviewTodo 只代表排程，不能代表效果改善、无变化或恶化。
3. ReviewRecord 只来自人工保存，并且必须沿用已验证 evidence_snapshot。
4. 任何读回卡都不能暗示自动改规则、自动调价、自动暂停、自动加词或自动否词。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:review-ui
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
git diff --check
```

## 2026-06-25：Parent ASIN 诊断路径不能把同一套判断重复铺开

```text
问题：
用户给出的 U 框架方向正确，但如果页面把筛选器、销售、广告组、明细和 AI 分析全部纵向展开，连续看 10 个 Parent ASIN 仍然像看 10 张报纸。
当前 Parent ASIN 运营诊断路径里，同一套五层判断既在单屏框架中出现，又在下方完整分层说明中再次出现，增加了阅读成本。

原因：
1. 为了保证数据口径和证明边界清晰，前端把每层的业务问题、当前判断、能证明、不能证明、人工下一步全部默认展示。
2. 这些字段本身必要，但默认铺开会把 AI Copilot 工作台推回普通报表阅读流。
3. 证据边界没有错，错在第一屏展示密度过高，没有区分“默认判断”和“按需审计”。

解决：
1. 保留 Parent ASIN 单屏诊断框架。
2. 默认展示业务问题、当前判断和人工下一步。
3. 将“能证明 / 不能证明”放入每层可展开的证明边界。
4. 删除重复的五层完整说明卡片。
5. 结构测试约束第一屏必须先给当前判断和人工下一步，再把证明边界收进可展开区。

防错规则：
1. U 框架只能作为业务路径，不应直接实现成纵向报表。
2. 第一屏先帮助用户判断“该看哪里、为什么、下一步做什么”。
3. 证明边界必须保留，但可以默认折叠；不能因为降噪而删除证据链。
4. AI 分析不能作为最后一张大报表，应该贯穿各层业务判断。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
```

## 2026-06-26：无复盘结论时规则反馈摘要不能只讲抽象门槛

```text
问题：
当 ReviewRecord=0 但 pending_source_candidates 已经存在时，规则反馈默认摘要如果仍显示“反馈依据是什么 / 等待复盘样本池”，用户看不出当前有什么可以人工处理的来源。

原因：
1. ReviewRecord 和待复盘来源是两条不同链路。
2. candidate_groups 为空是事实，但不代表没有任何下一步。
3. 只展示抽象门槛会增加用户判断成本，让用户误以为规则反馈区完全没有可读信息。

解决：
1. 规则反馈默认四件事的第三项改为状态感知。
2. 没有 ReviewRecord 但存在 pending_source_candidates 时，直接显示“待复盘来源 / N 条待复盘来源”。
3. 详情读取第一条待复盘来源，帮助用户知道下一步复盘对象。
4. 有真实 ReviewRecord 或没有待复盘来源时，再回退到“反馈依据是什么”。

防错规则：
1. ReviewRecord 缺失时不能伪造规则反馈样本。
2. pending_source_candidates 只能作为待复盘来源提示，不能当作已保存复盘结论。
3. 默认摘要必须优先展示当前能人工推进的信息，不能只展示抽象门槛。
4. 规则反馈区仍不得生成自动改规则或自动广告动作。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
只读运行 build_review_readiness_payload(selected_market_id=1)
```

## 2026-06-25：右侧人工确认不能只放动作按钮和完整长证据

```text
问题：
右侧人工确认区已经能承接当前广告组，但默认仍容易把四层证据完整铺开。
用户真正要点击人工动作时，第一眼需要知道“本次动作依据哪一层证据”，而不是重新读一遍完整证据清单。

原因：
1. 中间广告组诊断已经有默认证据层，但右侧人工动作承接区没有直接读回。
2. 右侧完整四层证据虽然必要，但默认铺开会增加连续处理多个 Parent ASIN 的阅读成本。
3. 人工动作如果没有证据层提示，容易变成孤立按钮，削弱“证据 -> 人工确认 -> 复盘”的闭环。

解决：
1. 右侧人工动作承接区复用 buildProductScopeAdGroupReviewPriority。
2. 新增“人工动作默认证据层”，展示当前判断、能证明、不能证明和人工下一步。
3. 将完整四层证据保留为折叠区，仍可审计，但不默认占据主要阅读位置。
4. 结构测试约束默认证据层必须先于完整四层证据和复盘回读证据链。

防错规则：
1. 右侧人工确认必须先说明当前动作依据的证据层，不能只放按钮。
2. 完整证据可以折叠保留，但不能删除证据链和边界。
3. 默认证据层只是当前阅读优先级，不能当成新的后端事实字段。
4. 任何人工动作仍只保存留痕或复盘待办，不能自动调价、暂停、否词或加词。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
git diff --check
```

## 2026-06-25：Parent ASIN 优先清单不能只告诉用户先看谁

```text
问题：
Parent ASIN 优先清单已经能告诉用户先处理哪个经营入口，但用户仍要点击后才知道这个 Parent ASIN 下默认应该先看广告组的哪一层证据。
如果要连续看 10 个 Parent ASIN，这会继续增加决策时间和阅读成本。

原因：
1. 默认证据层原本只存在于打开广告组诊断面板之后。
2. 未选中的 Parent ASIN 没有准确的当前广告组诊断上下文，不能借用当前选中广告组的结论。
3. 如果列表层不区分“当前可准确判断”和“打开后读取”，容易造成跨 Parent ASIN 的错误推断。

解决：
1. 新增 Parent ASIN 优先清单证据预览 helper。
2. 当前选中的 Parent ASIN 显示真实默认下钻证据层和人工下一步。
3. 未选中的 Parent ASIN 显示“打开后读取”，明确不跨 Parent ASIN 套用当前广告组结论。
4. 经营入口桥接区同步读回默认下钻证据层，避免用户进入后再次迷路。

防错规则：
1. 优先清单必须区分当前 Parent ASIN 的准确证据和未打开 Parent ASIN 的待读取状态。
2. 禁止用当前广告组解释另一个 Parent ASIN。
3. 列表层只能提示证据层和人工下一步，不能新增指标，不能执行广告动作。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
git diff --check
```

## 2026-06-25：广告组下具体数据不能只做纵向清单

```text
问题：
用户给出的业务链路里，“广告组数据 -> 广告组下具体数据”是正确顺序，但如果投放商品、投放词、搜索词和广告位继续纵向铺开，用户看多个 Parent ASIN 时仍然像读多份报表，决策成本没有真正下降。

原因：
1. 四类证据已经存在于运营检查清单里，但默认阅读方式仍偏长清单。
2. 系统已经能算出当前优先证据层，但优先级没有变成第一阅读入口。
3. 广告组是投放容器，如果用户先读明细清单，容易把搜索词或广告位上下文误读成单个广告 ASIN 归因。

解决：
1. 新增广告组证据层切换器。
2. 切换器复用已有 buildProductScopeAdGroupChecklistItems，不新增指标和业务口径。
3. 默认激活 buildProductScopeAdGroupReviewPriority 给出的优先证据层。
4. 每个证据层展示指标目的、当前判断、能证明、不能证明和人工下一步。
5. 完整运营检查清单继续保留，但放在切换器之后作为详细证据。

防错规则：
1. 广告组下具体数据必须先提供证据层切换或优先阅读入口，不能默认只铺长清单。
2. 切换器只能复用既有证据和优先级，不能临时虚构新指标。
3. 切换证据层不能改变 Parent ASIN 经营入口，不能写人工动作，不能执行广告动作。
4. 广告组仍是容器；搜索词和广告位只能作为上下文证据，不能自动归因到单个广告 ASIN。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-25：搜索词复核卡片不能默认铺成长证据

```text
问题：
搜索词复核已经有作战条，但每张卡片仍默认展示业务问题、指标目的、数据口径、能证明、不能证明、Top Terms 和边界说明。
当用户查看多个 Parent ASIN 时，这会重新变成“每个 Parent 看一份小报表”，增加决策时间和成本。

原因：
1. 原卡片把“选择 SearchTerm 的操作入口”和“完整证据解释”放在同一个 button 中。
2. 指标目的和证明边界是必要内容，但不应该默认全部铺在列表层。
3. 用户第一眼需要的是核心判断、优先 SearchTerm 和人工下一步，而不是先读完所有证据。

解决：
1. 将搜索词复核卡片改为 article 容器。
2. 主按钮只保留运营判断、核心指标、优先 SearchTerm 和人工下一步。
3. 完整业务问题、指标目的、证明边界和 Top Terms 放入 details“展开证据与边界”。
4. 结构测试锁定默认摘要先于展开证据，指标目的必须在展开证据内。

防错规则：
1. 搜索词复核列表层默认只放能帮助选择下一步的摘要，不默认铺完整证据。
2. 完整证据必须可展开查看，不能删除业务问题、数据口径、能证明和不能证明。
3. 搜索词复核卡片点击只定位 SearchTerm 信号，不能切换 Parent ASIN 经营入口。
4. 搜索词复核仍只允许人工记录观察、标记已处理、加入复盘或忽略本次，不得自动加词、否词、调价或暂停广告。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-25：搜索词复核不能只给分布和长卡片

```text
问题：
Parent ASIN 广告搜索词表现复核已经能按扩量、止损和观察分层，但如果摘要只展示分布数字，下面继续铺长卡片，用户仍要像读小报表一样自己判断先打开哪个 SearchTerm、为什么打开、下一步做什么。

原因：
1. 分布数字只能说明有多少组，不直接回答当前业务问题。
2. 搜索词复核卡片包含指标目的、口径、能证明、不能证明和人工下一步，信息正确但默认全展开会增加扫描成本。
3. 用户看多个 Parent ASIN 时，需要先扫出首要判断和优先 SearchTerm，再决定是否下钻。

解决：
1. SearchIntentReviewDecisionSummary 增加业务问题、首要判断原因、复核路径和证明边界。
2. 页面在搜索词复核判断摘要中新增“广告搜索词复核作战条”。
3. 作战条先于扩量 / 止损 / 观察分布和卡片列表渲染。
4. 测试锁定作战条字段、渲染顺序和样式存在。

防错规则：
1. 搜索词复核摘要必须先回答“今天先扩量、止损还是观察”，不能只给分布数字。
2. 首要 SearchTerm、复核路径和证明边界必须在卡片列表前可见。
3. 搜索词复核只服务人工复核优先级，不能改变 Parent ASIN 诊断入口，也不能自动加词、否词、调价或暂停广告。
4. ABA 仍只能作为站点级市场背景，不能当店铺、广告组、广告 ASIN 或自然搜索数据。

验证方式：
cd frontend
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-25：右侧人工确认不能丢失 Parent ASIN 单屏证明边界

```text
问题：
中间区已经把用户草图中的筛选器、Parent ASIN 销售表现、广告组、具体数据和 AI 判断压缩成单屏诊断框架。
但右侧人工确认区如果只展示当前广告组承接和动作按钮，用户在最终点击前仍需要回到中间区核对 Parent ASIN 层面的业务问题、能证明和不能证明。

原因：
1. 右侧是人工动作前最后一层门禁，不能只显示按钮和广告组检查清单。
2. Parent ASIN 销售入口、广告组、搜索词、广告位和 AI 判断的证明范围不同，右侧若不读回，会把中间证据链截断。
3. 多 Parent ASIN 场景下，任何最终点击前需要来回核对的设计都会继续增加决策时间。

解决：
1. ProductScopeAdGroupActionBridgeCard 接入 productScopeDiagnosisBrief。
2. 右侧新增 Parent ASIN 单屏判断读回。
3. 读回销售表现入口、广告组优先排序、广告组下具体数据和 AI 人工动作判断四层。
4. 每层展示业务问题、能证明、不能证明和人工下一步。

防错规则：
1. 中间区新增会影响人工动作的 Parent ASIN 判断后，右侧也必须读回同源边界。
2. 右侧不能只展示广告组层证据，必须保留 Parent ASIN 经营入口和广告动作边界。
3. 读回只能复用已有 ProductScopeDiagnosisBrief，不能临时虚构新指标或新归因。
4. 右侧仍只允许记录观察、标记已处理、加入复盘或忽略本次，不得出现自动广告动作。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-25：单屏诊断框架不能缺少证明边界

```text
问题：
Parent ASIN 单屏诊断框架已经按筛选器、销售入口、广告组、具体数据和 AI 判断组织，但如果卡片只展示业务问题、当前判断和人工下一步，用户仍需要下翻长详情才能确认“能证明什么、不能证明什么”。

原因：
1. 纵向框架能说明阅读顺序，但不能自动降低判断成本。
2. Parent ASIN、广告 ASIN、广告组、搜索词、广告位和 AI 判断的证明范围不同，如果证明边界不前置，用户容易把上下文证据当成广告动作依据。
3. 看多个 Parent ASIN 时，任何需要下翻核边界的设计都会重新变成“看报纸”。

解决：
1. 在 ProductScopeDiagnosisBriefPanel 的单屏诊断框架卡中新增证明边界区。
2. 每层卡片默认展示“能证明”和“不能证明”。
3. 结构测试锁定 productScopeDiagnosisFrameworkEvidence 和 section.proves / section.doesNotProve 必须存在。

防错规则：
1. 单屏框架卡不能只展示当前判断和下一步，必须同步展示证明边界。
2. 证明边界只能复用已有诊断合同或前端已计算结论，不能临时虚构指标。
3. 搜索词、广告位、ABA 和广告组证据仍不能自动归因到单个广告 ASIN，也不能包装成自动广告动作。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-25：中间证据链不能只做长路线说明

```text
问题：
广告证据链导览已经有 7 层步骤，但如果用户必须逐层读完，才能知道哪些证据可直接判断、哪些只是上下文、是否能人工动作和怎么复盘，中间区仍然会增加决策成本。

原因：
1. 路线说明解决了顺序问题，但没有先给证据层级状态。
2. 运营需要先判断“能不能落地”，再决定是否继续看广告组、搜索词和广告位细节。
3. 搜索词、广告位、ABA 等上下文如果不先标边界，容易被误读成单个广告 ASIN 归因。

解决：
1. 在广告证据链导览顶部新增层级摘要。
2. 摘要固定展示直接证据、上下文证据、AI 准入和复盘路径。
3. 继续保留 7 层步骤作为下钻解释，不用摘要替代证据链。

防错规则：
1. 中间区必须先回答证据层状态，再展示详细步骤。
2. 直接证据和上下文证据必须分开表达，不能把搜索词或广告位写成单 ASIN 归因。
3. AI 准入和复盘路径只能表达人工门槛，不得包装成自动广告动作。

验证方式：
cd frontend
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-25：Parent ASIN 分诊桶不能只是静态摘要

```text
问题：
左侧已有 Parent ASIN 首页分诊摘要和优先处理清单，但如果分诊桶只能展示“复盘优先 / 人工确认 / 保持观察 / 暂不展开”的数量，用户仍需要向下读完整清单，无法真正解决多个 Parent ASIN 像看多张报纸的问题。

原因：
1. 摘要只告诉用户有哪些类型，但没有把阅读面收窄。
2. 用户真实工作流是先选今天要处理的对象类型，再打开少数 Parent ASIN 诊断链路。
3. 如果分诊桶不可点击，排序和摘要会变成另一层信息，而不是降低决策成本。

解决：
1. 将分诊桶改为可点击过滤按钮。
2. 今日 Parent ASIN 优先处理清单只展示当前桶命中的 Parent。
3. 空桶展示原因说明，不用泛化指标填充。

防错规则：
1. 多对象首页不能只展示汇总，还必须帮助用户收窄下一步阅读对象。
2. 分诊筛选只能改变阅读范围，不能改变数据口径，不能写人工动作，不能执行广告动作。
3. “全部”入口必须保留，避免过滤状态让用户误以为其他 Parent ASIN 不存在。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run build
```

## 2026-06-25：Parent ASIN 核心判断不能藏在折叠长详情里

```text
问题：
用户提出如果按“筛选器 -> Parent ASIN 销售表现 -> 广告组数据 -> 广告组下明细 -> AI 广告分析”的纵向框架设计，业务路径是对的，但如果全部纵向堆叠，用户看 10 个 Parent ASIN 仍会像看 10 张报纸。

原因：
1. Parent ASIN 详情链路可以很长，但用户第一眼需要的是“是否值得下钻、先看哪里、能不能人工动作和复盘”。
2. 当前代码已有 Parent ASIN 首屏摘要，但主要放在折叠详情里，默认可见区域仍不足以完成第一眼判断。
3. 如果把 AI 分析放在最后，AI 就变成报表总结，而不是贯穿每层的分诊和证据解释。

解决：
1. 新增默认可见的 Parent ASIN 单屏作战卡。
2. 单屏卡只展示广告证据、今日处理优先级、默认聚焦广告组、搜索词复核、AI 准入、7/14 天复盘六个判断点。
3. 完整证据继续留在折叠详情和三栏诊断区，避免第一屏变成长报表。

防错规则：
1. Parent ASIN 详情可以有完整链路，但第一眼必须先回答业务判断，不能让用户先读完所有数据再理解。
2. AI 分析不能只放在最后当总结，必须在入口、广告组、搜索词、人工确认和复盘门槛中持续说明下一步。
3. 单屏卡只能做分诊和下钻入口，不得写入人工动作，不得执行广告操作。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run build
```

## 2026-06-25：未选中 Parent 的搜索词机会不能藏在下钻里

```text
问题：
上一轮只把当前已选 Parent 的 signal-triage 候选补回优先清单。这样 B00K4W4AAA 被选中时能看到 beach essentials，但用户扫多个 Parent 时，未选中的 Parent 仍可能把搜索词扩量或止损线索藏在详情页里。

原因：
1. Parent ASIN 首页分诊原本主要依赖普通 AI 信号、复盘待办和广告花费。
2. 搜索词复核摘要来自 /api/search-intents，页面只读取当前 Parent，未参与首页多个 Parent 的排序理由。
3. 用户真正需要先判断“哪个 Parent 值得打开”，不能先逐个打开 Parent 才发现搜索词机会。

解决：
1. 新增 mergeProductScopePrioritySearchIntentHints，把搜索词复核摘要并入 Parent 优先清单。
2. 页面初始化时只读取优先 Parent 的搜索词摘要，切换 Parent 时补当前 Parent 摘要，不新增后端接口。
3. 搜索词扩量 / 止损线索可提升 Parent 行展示和排序，但到期复盘仍优先。

防错规则：
1. Parent 优先清单不能只看普通 AI 信号数，还要露出当前可复核搜索词摘要。
2. 搜索词复核只能作为首页排序和下钻提示，不能把搜索词表现分组当作人工动作对象。
3. 任何搜索词线索都必须落到具体 SearchTerm 后再人工确认，不得自动加词、否词、调价或暂停广告。

验证方式：
cd frontend
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-25：Parent ASIN 优先清单不能只看普通 AI 信号数

```text
问题：
B00K4W4AAA 当前有 12 个子 ASIN、3 个广告 ASIN 和 beach essentials 搜索词候选，但如果左侧 Parent ASIN 优先清单只展示普通 AI 信号数，用户会误以为该 Parent 没有值得复核的线索。

原因：
1. signal-triage 的可人工复核候选可能来自当前 Parent 的搜索词候选，不一定表现为广告 ASIN 异常信号。
2. Parent 优先清单是用户看 10 个 Parent 时的第一道分诊入口，如果候选线索藏在下钻页，会增加决策时间。
3. 当前只知道已选 Parent 的后端候选，不能把局部候选结果拿来重排全部 Parent，否则会造成全局排序口径不清。

解决：
1. 新增 mergeActiveProductScopePriorityTriageHint，只增强当前已选 Parent 的优先行展示。
2. 当前 Parent 有 candidate_pool_count 时，优先行展示后端候选数量和推荐对象，例如 beach essentials。
3. 不改变原有全局排序权重，不新增后端请求，不把候选包装成自动广告动作。

防错规则：
1. Parent 优先清单必须露出当前 Parent 的后端候选线索，不能只展示 AI 信号条数。
2. 只掌握当前 Parent 候选时，不能据此重排全部 Parent。
3. 搜索词候选只能引导人工复核投放词、广告组、广告 ASIN 和边界，不能自动加词、否词、调价或暂停广告。

验证方式：
cd frontend
npm run test:signal-ui
npm run test:workbench-structure
```

## 2026-06-25：右侧人工动作建议不能脱离中间证据链

```text
问题：
右侧“本次建议人工动作”能告诉用户本次建议点哪个按钮，但如果不说明这个建议来自中间哪条业务问题、能证明什么、不能证明什么，用户仍要回到中间诊断区自己拼证据。

原因：
1. 人工动作按钮解决的是“能不能点”，不等于解决“为什么点”。
2. 中间诊断区已经有业务问题、证据强度和判断边界，右侧若不复用，会在最终点击前产生新的理解成本。
3. 只展示推荐动作和按钮门禁，容易让 AI Copilot 工作台退回成操作面板，而不是证据驱动的人工决策台。

解决：
1. manualActionChoiceRecommendation 新增 evidenceLink 和 evidenceBoundary。
2. 右侧推荐动作卡接入 selectedDiagnosisEvidenceSummary。
3. 推荐动作旁直接展示证据回链和边界回链，缺少诊断摘要时提示先回到中间诊断区核对。

防错规则：
1. 右侧人工动作推荐必须能回到中间诊断摘要，不能只展示按钮可用状态。
2. 推荐动作必须同时说明能证明什么和不能证明什么，不能包装成自动广告动作。
3. 没有中间诊断摘要时，只能提示补齐证据核对，不能生成看似完整的动作理由。

验证方式：
cd frontend
npm run test:review-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-25：广告组具体数据不能平均展开

```text
问题：
Parent ASIN 层已经有首页分诊和单屏判断框架，但进入某个广告组后，如果投放商品、投放词、搜索词、广告位四层都平均展开，用户仍要自己判断先看哪里。

原因：
1. 广告组是投放容器，具体数据层包含多个证明范围不同的证据。
2. 投放商品能证明广告 ASIN 范围，投放词说明投放上下文，搜索词说明用户搜索表现，广告位说明流量位置或证据缺口。
3. 如果不先提示本次优先证据层，用户会把广告组详情继续当成报表阅读。

解决：
1. 在当前广告组复核顺序内新增“本次优先证据层”。
2. 根据问题落点、证据合流状态和证据缺口，优先指向搜索词、广告位、投放词或投放商品。
3. 优先层只降低阅读成本，不替代四层完整复核，也不生成自动广告动作。

防错规则：
1. 广告组详情必须先给本次优先证据层，再展开投放商品、投放词、搜索词、广告位。
2. 优先证据层只能来自已有诊断字段，不能新增未验证指标或新归因。
3. 搜索词和广告位仍不能自动归因到单个广告 ASIN；右侧仍只允许人工留痕和复盘。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui
npm run build
```

## 2026-06-25：Parent ASIN 四层 UI 框架不能直译成四块长报表

```text
问题：
用户草图按筛选器、Parent ASIN 销售表现、广告组数据、广告组下具体数据、AI 广告数据分析组织，业务顺序正确。
但如果页面直接做成四个大块，用户看 10 个 Parent ASIN 仍会像看 10 张报纸。

原因：
1. 四个大块只说明信息分类，不直接回答业务判断。
2. Parent ASIN 销售表现、广告组、投放商品 / 投放词 / 搜索词 / 广告位和 AI 判断的证明范围不同，不能同等铺成报表。
3. 用户真正需要的是先判断是否展开、先看哪个广告组、问题落在哪层证据、下一步能做什么人工动作。

解决：
1. 在 Parent ASIN 体检结论之后新增“单屏诊断框架”。
2. 将四层路径压缩成四张判断卡：Parent ASIN 销售表现入口、广告组优先排序、当前广告组复核路径、AI 人工动作判断。
3. 每张卡固定展示业务问题、当前结论和人工下一步，再让用户进入详细路径。

防错规则：
1. 后续新增 Parent ASIN 主路径区块时，必须先说明业务问题，再展示指标。
2. 销售、广告组、具体证据和 AI 判断可以按顺序组织，但必须压缩成判断卡后再展开明细。
3. AI 判断只能输出人工动作和复盘路径，不能包装成自动调价、自动否词、自动加词或自动暂停。

验证方式：
cd frontend
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-25：优先证据行不能因为重复标签丢掉动作边界

```text
问题：
运行态证据快照增加到 31 条后，manualActionPreflightPriorityEvidenceRows 在截取前 8 条优先证据时，重复的“同组投放商品表现”等标签会挤掉“动作边界”。

原因：
1. 证据快照可以增长，固定条数不是业务合同。
2. 优先展示逻辑原来只按优先级排序和截取，没有按 label 去重。
3. “动作边界”是右侧人工确认的安全提醒，不能因为证据变多而被挤出优先视图。

解决：
1. 优先证据行排序后按 label 去重，再截取展示。
2. 运行态测试从“必须等于 30 条”改为“至少 30 条，并保留关键证据”。
3. 保留对“动作边界”的断言，确保不自动执行广告动作的提醒仍在优先证据里。

防错规则：
1. 证据快照条数不能作为精确业务合同，除非后端明确固定。
2. 关键安全边界应按标签校验存在，而不是依赖数组位置。
3. 新增证据时必须确认优先证据视图仍包含 AI 准入、搜索词表现判断、人工下一步、证据缺口、需要补证和动作边界。

验证方式：
cd frontend
npm run test:signal-ui-runtime
npm run test:review-ui
```

## 2026-06-25：Parent ASIN 主路径不能只有四段明细

```text
问题：
Parent ASIN 四段路径已经先展示业务问题，但如果用户仍要读完四段才能知道是否进入广告诊断、先看哪个广告组、证据能否下钻和右侧能做什么动作，页面仍然有报表阅读成本。

原因：
1. 四段路径解决的是结构清晰，不等于第一眼就能决策。
2. 多 Parent ASIN 场景下，用户需要先扫结论，再决定是否展开。
3. 没有压缩结论层时，销售入口、广告组排序、证据下钻和人工边界仍分散在不同区块。

解决：
1. 在 Parent ASIN 决策导览和四段路径之间新增“Parent ASIN 体检结论”。
2. 固定压缩为准入结论、今日焦点、证据下钻、人工边界。
3. 结构测试锁定体检结论必须先于四段路径出现。

防错规则：
1. Parent ASIN 主路径必须先给一眼结论，再展示明细路径。
2. 体检结论只能复用已有证据和边界，不能新增未验证指标或归因。
3. 人工边界必须继续表达只允许人工留痕和复盘，不得自动执行广告动作。

验证方式：
cd frontend
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-25：Parent ASIN 四段框架不能缺少业务问题

```text
问题：
用户草图按筛选器、Parent ASIN 销售表现、广告组数据、广告组下具体数据、AI 广告分析组织，业务顺序正确。
但如果每段只展示目的、指标或判断，用户仍要自己推理“这段到底帮我决定什么”，页面会重新变成长报表。

原因：
1. 区块标题只能说明内容类型，不能直接说明业务判断。
2. 销售表现、广告组、投放商品、投放词、搜索词、广告位的证明范围不同，不能混成“具体数据”。
3. AI 如果只在最后总结，而前面没有业务问题导向，用户仍要先读完整页再决定下一步。

解决：
1. ProductScopeDiagnosisBriefSection 增加 businessQuestion。
2. 四段路径先展示业务问题，再展示当前判断、人工下一步、能证明和不能证明。
3. 结构测试锁定“业务问题”必须早于“当前判断”出现，防止回退成指标报表。

防错规则：
1. Parent ASIN 诊断路径的每段必须先回答业务问题。
2. 新增销售、广告组、搜索词或广告位区块时，必须说明它服务哪个人工判断。
3. AI 段只能把证据收敛为人工动作和复盘路径，不能包装成自动广告动作。
4. 任何区块如果不能回答业务问题，只能作为补充证据，不应进入主路径。

验证方式：
cd frontend
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-24：右侧人工准入不能把后端预检当主语言

```text
问题：
右侧人工确认区仍以“后端只读预检 / 后端预检通过 / will_write=false / review_records”等技术语言解释人工动作准入。
这些字段能保护接口合同，但会让运营在最终点击前先理解后端状态，而不是直接判断是否能人工留痕、是否生成复盘排程。

原因：
1. 后端预检是实现机制，不是用户的业务任务。
2. will_write=false 是安全合同，应该留在测试和底层校验里，不应该作为主文案。
3. review_records 是存储对象，用户真正关心的是是否已有复盘结论。
4. Parent ASIN 诊断链路前面已经在降低报表阅读成本，如果右侧动作区继续暴露技术字段，会把成本加回最后一步。

解决：
1. 右侧面板标题改为“人工留痕准入”。
2. 人工动作空态、准入状态、按钮阻断、写后读回统一改成“准入核对 / 复盘排程 / 复盘结论”的业务读法。
3. 非搜索词对象在写后读回中优先展示对象名或 ASIN；SearchTerm 仍保留稳定对象提示，防止复盘对象错配。
4. 更新 review-ui、signal-ui 和结构测试，防止旧的后端预检主语言回退。

防错规则：
1. 用户第一层只读业务判断：能否人工留痕、保存什么证据、生成什么复盘排程、是否不执行广告动作。
2. 后端预检、will_write、review_records 可以出现在合同、测试和底层审计里，但不能成为右侧人工动作主标题或主句。
3. 写后读回应展示“复盘结论 0 条 / N 条”，不要直接展示 review_records。

验证方式：
cd frontend
npm run test:review-ui
npm run test:signal-ui
npm run test:workbench-structure
```

## 2026-06-24：人工预检摘要不能把对象ID和窗口代码作为主展示

```text
问题：
推荐候选、下一个候选和右侧人工动作对象链路读回中，仍直接展示“对象ID”和“窗口：7d / 14d”。
虽然这些字段对接口合同有用，但对运营来说会增加理解成本。

原因：
1. 对象 ID 是机器稳定标识，不等于用户第一眼需要理解的业务对象。
2. 7d / 14d 是复盘窗口代码，用户可见层应显示为 7 天 / 14 天。
3. Parent ASIN 诊断路径已经从报表改为决策导览，但最后的人工预检摘要如果继续暴露技术字段，会把决策成本重新加回来。

解决：
1. 新增 ManualActionPreviewStrip，统一展示人工动作、复盘对象、对象身份和复盘窗口。
2. 推荐候选和下一个候选的只读预检改用该业务读法。
3. 右侧人工动作对象链路读回和 manualActionTargetSummary 同步改成复盘对象 / 对象身份 / 复盘窗口。
4. 补充测试，防止对象ID和 7d / 14d 窗口代码重新成为候选预检主展示。

防错规则：
1. 用户可见的人工预检主展示必须优先使用业务语言，机器字段只能作为底层合同。
2. 复盘窗口在用户可见层写 7 天 / 14 天，不直接写 7d / 14d。
3. 新增候选摘要、右侧动作读回或复盘入口时，必须同时检查是否把 object_id、action_type、review_window 当成主展示。

验证方式：
cd frontend
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-24：人工动作预检不能裸露 add_to_review

```text
问题：
推荐对象和下一个未留痕候选的只读预检里直接展示 add_to_review。后端 next_action 中如果包含该动作码，前端理由也会原样展示。

原因：
1. action_type 是前后端机器合同，不是运营可读语言。
2. 预检摘要是用户决定是否人工留痕 / 加入复盘的入口，裸露动作码会增加理解成本。
3. 如果页面让运营读后端枚举，就会偏离 AI Copilot 工作台的业务判断定位。

解决：
1. 用户可见文案中将 add_to_review 翻译为“加入复盘”。
2. 推荐对象和下一个未留痕候选的预检摘要改用 manualActionDisplayLabel。
3. 保留 actionType 机器字段，不改变 ManualAction / ReviewTodo 写入合同。
4. 补充测试断言，防止 add_to_review 再出现在用户可见理由和预检摘要里。

防错规则：
1. 后端枚举可以保留在接口合同和测试对象里，但不能直接展示给运营。
2. 所有人工动作用户可见文案必须使用：记录观察、标记已处理、加入复盘、忽略本次。
3. 新增人工动作预检、读回、复盘卡片时，必须同时检查机器字段和展示文案是否分离。

验证方式：
cd frontend
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
cd ..
git diff --check
```

## 2026-06-24：首页 Parent ASIN 分诊不能只给排序结果

```text
问题：
左侧已经有“今日先看什么”和 Parent ASIN 优先处理清单，但如果摘要只展示排序结果、待处理规模、分诊桶、排序依据和人工下一步，用户仍可能把每个 Parent ASIN 逐个点开阅读完整链路。

原因：
1. 排序只能回答“谁排第一”，不直接回答“其他对象暂时怎么处理”。
2. 多 Parent ASIN 场景下，用户需要明确阅读策略，否则还是会把列表当作 10 份报表入口。
3. 首页如果不说明只展开第一优先对象，后续销售表现、广告组、明细和 AI 判断仍会被当成纵向报表逐个阅读。

解决：
1. ProductScopePriorityDecisionSummary 新增 readingStrategy。
2. 首页 Parent ASIN 分诊摘要展示“阅读策略”。
3. 阅读策略明确只展开今日优先 Parent ASIN；其余对象先按分诊桶观察，除非出现到期复盘或高优先信号，不逐个打开完整报表。
4. 结构测试锁定阅读策略必须存在。

防错规则：
1. 首页 Parent ASIN 分诊必须同时回答“先看谁、为什么、其他对象怎么先不看”。
2. 只给排序依据不够，必须明确减少阅读成本的策略。
3. 详情链路可以纵向打深，但首页不能把每个 Parent ASIN 都变成完整报表入口。

验证方式：
npm run test:workbench-structure
npm run build
```

## 2026-06-24：复盘待办业务读回不能只显示判断和补证

```text
问题：
ReviewTodo 已经能通过 evidence_snapshot 回读完整证据链。
但“复盘待办业务判断读回”如果只展示搜索词表现判断、人工下一步和需要补证，用户第一眼仍然看不到当初从哪个 Parent ASIN / 广告对象进入、默认聚焦哪个广告组、复核哪些广告证据层。

原因：
1. 完整 evidence_snapshot 在下一层核对区，第一眼业务读回仍偏短。
2. 复盘到期时用户需要先恢复当初的决策路径，再看处理前后指标。
3. 如果路径、焦点和动作边界不前置，用户仍可能把 ReviewTodo 当成普通到期提醒或效果结论。

解决：
1. buildReviewTodoDecisionReadbackSummary 新增原始诊断路径、默认展开焦点、复核证据层、动作边界。
2. search_term 待办要求读回 Parent ASIN 入口、广告 ASIN 承接、广告组合流判断、投放词、搜索词边界和广告位边界。
3. advertised_product 待办要求读回排查路径、广告组合流判断和广告商品 / 广告位证据层。
4. 结构测试和 review-ui 测试锁定这些业务读回项。

防错规则：
1. ReviewTodo 第一眼必须讲清“为什么复盘、从哪里展开、看哪层证据、不能自动做什么”。
2. 完整证据核对不能替代业务判断读回，两层都要服务用户复盘。
3. ReviewTodo 只表示进入 7/14 天排程，不得展示成建议有效或广告动作已执行。
4. 缺少路径、广告组合流或动作边界时，保存 ReviewRecord 前必须阻断或要求补证。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-26：复盘 ready 不能被读成系统已判断有效

```text
问题：
复盘待办到期后，页面存在“ready / 复盘效果可人工保存 / 保存结论”等表达，用户可能误以为系统已经判断本次人工动作有效。

原因：
1. ready 是数据窗口状态，不是业务结论状态。
2. ReviewTodo 只是待办和证据快照，不能替代人工保存的 ReviewRecord。
3. 如果把 ready 写成“复盘效果”，会让 AI Copilot 看起来像自动归因或自动判断广告动作效果。

解决：
1. 将 ready 默认层收口为“处理前后指标可人工复核”。
2. 保存门槛明确未 ready 前不保存 ReviewRecord，也不判断改善、无变化或恶化。
3. 保存路径明确最后一步是“人工保存结论”，ready 不等于已判断有效。
4. 保留现有 ReviewTodo / ReviewRecord 链路，不新增自动动作，不重构数据模型。

防错规则：
1. ready 只能描述指标窗口或数据是否可复核，不能描述效果好坏。
2. 效果结论必须来自人工保存 ReviewRecord。
3. 遇到局部口径问题时，先围绕总目标做结构化收口，不要矫枉过正地推翻已有工作台模型。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
rg ready 高风险旧表述
```

## 2026-06-24：Parent ASIN 纵向框架不能直译成长报表

```text
问题：
用户草图按筛选器 -> Parent ASIN 销售表现 -> 广告组数据 -> 广告组下具体数据 -> AI 广告数据分析组织，业务顺序是对的。
但如果页面照图直译成四个大块，用户看 10 个 Parent ASIN 仍然像看 10 张报纸，决策时间和成本不会下降。

原因：
1. 销售、广告组、明细和 AI 分析如果只是纵向堆叠，本质仍是报表阅读，不是 AI 分诊。
2. AI 如果只放在最后，用户需要先读完所有区块再自己拼问题落点。
3. 广告组、投放商品、投放词、搜索词和广告位不是同一种对象，混成“具体数据”会再次制造口径混乱。

解决：
1. 在 Parent ASIN 运营诊断路径顶部新增“Parent ASIN 决策导览”。
2. 导览先回答：当前是否展开、展开哪个广告组、按什么顺序读、哪些不能证明、人工下一步是什么。
3. 导览明确只展开问题广告组，不逐个读完整报表；搜索词和广告位不能自动归因到单个广告 ASIN；AI 不自动执行广告动作。
4. 结构测试锁定导览必须先于四段路径出现。

防错规则：
1. 用户提供纵向业务框架时，先保留业务链路，再把展示方式改成决策导览和问题分诊。
2. Parent ASIN 页面不能让用户先读完整销售和广告组报表，再自己找结论。
3. AI 分析必须承接证据链，但页面第一眼要给用户明确读法和人工边界。
4. 新增任何 Parent ASIN 区块前，都要先问它是否减少用户决策成本。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-24：复盘待办不能丢失 Parent ASIN 决策漏斗顺序

```text
问题：
如果人工动作保存后只保留 action_type、object_id 和复盘窗口，用户 7/14 天后看 ReviewTodo / ReviewRecord 时会丢失当初的 Parent ASIN 经营入口、广告 ASIN 承接、广告组上下文、投放词、搜索词和广告位边界。

原因：
1. 页面上的纵向决策漏斗如果没有进入 evidence_snapshot，复盘时只能看到动作结果，不能解释当时为什么这么判断。
2. 搜索词是人工动作对象，但 Parent ASIN、广告 ASIN、广告组、投放词和广告位是证据上下文，必须按顺序保留。
3. 多 Parent ASIN 场景下，如果复盘只剩动作状态，用户仍要重新翻回原始报表拼证据。

解决：
1. 只读验证 beach essentials 的 preflight evidence_snapshot_preview 已包含完整证据链。
2. 补强后端回归测试，锁定 ReviewTodo 保存后证据顺序：Parent ASIN入口 -> 广告 ASIN承接 -> 广告组合流判断 -> 同组投放商品表现 -> 逐投放上下文 -> 投放词证据 -> 搜索词边界 -> 广告位边界 -> 动作边界。
3. 不新增自动广告动作，不改变人工动作和复盘对象。

防错规则：
1. ReviewTodo / ReviewRecord 必须能回读当时的决策漏斗，不能只保存最终动作。
2. Parent ASIN 只能作为经营入口，不得默认变成人工动作对象。
3. 搜索词、广告位和广告组只能作为证据上下文，不能在复盘中被自动归因到单个广告 ASIN。
4. 后续改 evidence_snapshot 排序、去重或标签时，必须运行该回归测试。

验证方式：
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_post_write_review_todos_keep_search_term_ad_evidence_chain -q
```

## 2026-06-24：AI 诊断不能提前出现在广告组证据之前

```text
问题：
单 Parent ASIN 运营诊断路径原顺序是“销售入口 -> AI 广告诊断摘要 -> 广告组优先排序 -> 当前广告组复核路径”。
这会让用户在还没看到先看哪个广告组、广告组下有哪些具体证据前，就先读到 AI 结论。

原因：
1. 用户草图里的 AI 广告数据分析应该承接销售、广告组和具体广告证据，不应该提前成为第二段。
2. AI 如果提前出现，容易把工作台重新带回“先看 AI 文案，再自己找证据”的模式。
3. 本项目的业务本质是证据链驱动人工动作，AI 只能做诊断汇总和人工下一步，不是提前裁判。

解决：
1. 将 Parent ASIN 运营诊断路径调整为“销售表现入口 -> 广告组优先排序 -> 当前广告组复核路径 -> AI 人工动作判断”。
2. 将“AI 广告诊断摘要”改名为“AI 人工动作判断”。
3. AI 段只汇总为记录观察、标记已处理、加入复盘或忽略本次，并继续明确不自动执行广告动作。
4. 结构测试锁定 AI 人工动作判断必须在广告组排序和具体证据之后。

防错规则：
1. 单 Parent ASIN 详情页必须先展示经营入口和广告证据，再展示 AI 人工动作判断。
2. AI 诊断不得插在广告组优先排序和广告组下具体证据之前。
3. AI 段不得包装成自动加词、否词、调价或暂停广告。
4. 如果未来调整诊断路径顺序，必须同步检查 signalUi helper 和结构测试。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run test:review-ui
npm run build
```

## 2026-06-24：右侧人工动作前不能缺少证据读回

```text
问题：
右侧人工动作承接如果只展示动作对象、来源读回、允许动作和复盘回读证据链，用户点击记录观察或加入复盘前仍可能需要回到中间诊断区确认复核顺序、证据缺口和动作边界。

原因：
1. 人工动作是复盘闭环的起点，不只是按钮。
2. 点击前必须确认当前动作依据来自哪条复核路径。
3. 证据缺口和动作边界如果只在中间或复盘区展示，人工动作容易脱离原始证据。

解决：
1. 在右侧当前广告组人工动作承接中新增“人工点击前证据读回”。
2. 读回复核顺序、证据缺口和动作边界。
3. 复核顺序复用当前广告组四层检查清单，证据缺口复用证据合流判断，动作边界复用 forbiddenActions。

防错规则：
1. 右侧人工动作前必须能读回复核顺序、证据缺口和动作边界。
2. 右侧只能做人工留痕和复盘待办确认，不能包装成自动广告动作。
3. 右侧读回必须复用中间证据，不得新增未验证归因或新排序口径。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：当前广告组具体数据不能先让用户读明细

```text
问题：
当前广告组具体数据虽然已经有运营检查清单和四层证据边界，但如果用户进入该区后先看到广告组指标和明细，仍要自己判断应该先看投放商品、投放词、搜索词还是广告位。

原因：
1. 广告组下的具体数据层级多，缺少复核顺序时容易变成明细清单。
2. 投放商品、投放词、搜索词和广告位的证明范围不同，不能让用户自己从指标里猜读法。
3. 如果复核顺序不前置，后续人工确认也容易脱离原始证据路径。

解决：
1. 新增“当前广告组复核顺序”。
2. 复用现有四层检查清单，按投放商品 -> 投放词 -> 搜索词 -> 广告位展示。
3. 将复核顺序放在广告组指标和运营检查清单之前。
4. 明确该顺序只服务人工复核，不证明搜索词或广告位已归因到单个广告 ASIN，也不执行广告动作。

防错规则：
1. 当前广告组具体数据必须先展示复核顺序，再展示指标和明细。
2. 复核顺序只能复用已验证证据层，不得新增未验证归因。
3. 搜索词和广告位只能作为上下文证据，不能被包装成单 ASIN 归因或自动动作依据。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：广告组数据区不能让用户从列表里自己找重点

```text
问题：
广告组行内虽然已经展示问题落点、证据合流、证据缺口和人工下一步，但区域顶部如果只显示“几个广告组，点击聚焦”，用户仍要先扫完整广告组列表，才能判断最该看哪一组。

原因：
1. 广告组是投放容器，列表天然容易变成广告组报表。
2. 行内判断解决单行理解问题，但没有解决区域第一眼“先看哪一组”的问题。
3. 多 Parent ASIN 场景下，每个 Parent ASIN 再展示多行广告组，会继续增加阅读成本。

解决：
1. 在广告组问题定位区顶部增加“广告组优先判断”。
2. 复用当前排序第一条广告组，展示优先广告组、为什么先看、能证明、不能证明和人工下一步。
3. 保留下方可点击广告组列表，只作为切换焦点，不作为第一判断入口。

防错规则：
1. 广告组数据区必须先给区域级优先判断，再展示广告组列表。
2. 优先判断只能复用已有排序和证据合流，不得新增未验证排序口径。
3. 广告组摘要不得暗示自动拆广告组、自动否词、自动调价或单 ASIN 自动归因。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：Parent ASIN 销售入口不能退化成销售报表

```text
问题：
中间诊断摘要第一段如果只叫“销售表现摘要”，用户仍要自己判断这个 Parent ASIN 是否值得进入广告诊断。多 Parent ASIN 场景下，这会继续把工作台变成多张报表。

原因：
1. 销售表现回答经营背景，不直接回答广告诊断准入。
2. Parent ASIN 下所有销售子 ASIN 不等于都有 advertised_products 广告证据。
3. 如果第一段不先给进入或暂不进入广告诊断的结论，用户还要自己从销售和广告覆盖指标里推理下一步。

解决：
1. 将 Parent ASIN 诊断详情摘要第一段改为“销售入口准入判断”。
2. 基于广告证据门禁和 advertised_products 覆盖输出“可以进入广告诊断 / 暂不进入广告诊断”。
3. 明确未投放销售子 ASIN 只作为经营背景，不进入广告组和搜索词诊断。

防错规则：
1. Parent ASIN 销售入口必须回答“是否值得进入广告诊断”，不能只展示销售指标。
2. 只有有 advertised_products 证据的广告 ASIN 可以进入广告诊断。
3. 缺广告对象证据时，页面必须说明要补什么证据，不能用销售表现硬撑广告分析。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：Parent ASIN 优先清单不能只给排序依据

```text
问题：
左侧“今日 Parent ASIN 优先处理清单”已经能说明排序依据，但如果每行缺少当前人工动作和复盘状态，用户仍要读主问题、证据摘要和下一步，才能判断当前对象是先复盘、待人工确认、保持观察还是暂不展开。

原因：
1. 排序依据回答“为什么排在这里”，但不直接回答“现在处于什么处理阶段”。
2. 多 Parent ASIN 场景下，用户要先扫状态，再决定是否打开详情。
3. 如果任务状态缺失，首页仍可能变成 10 个 Parent ASIN 的逐行阅读列表。

解决：
1. ProductScopePriorityQueueItem 新增 decisionBadge。
2. 基于已有 ReviewTodo、到期状态、高优先信号、待复核信号和广告证据生成“人工动作 / 复盘状态”。
3. 左侧队列行直接渲染 decisionBadge，不新增后端接口、不改排序公式、不新增广告动作。

防错规则：
1. Parent ASIN 优先清单必须同时回答“先看谁、为什么先看、当前处理状态是什么”。
2. 任务状态只能来自现有信号、复盘待办和广告证据，不得伪造业务归因。
3. 状态文案只能指向记录观察、标记已处理、加入复盘、忽略本次或等待复盘，不能暗示自动广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：右侧人工动作前必须读回入口、信号和保存对象

```text
问题：
首页默认进入今日优先 Parent ASIN 后，如果右侧人工动作只展示按钮、证据快照或后端预检状态，用户仍可能不知道点击后会保存到哪个稳定对象。
这会造成“左侧看的是 Parent ASIN，中心看的是某条信号，右侧保存到另一个 SearchTerm / 广告 ASIN / 广告组”的误解风险。

原因：
1. Parent ASIN 是经营入口，不是所有人工动作的保存对象。
2. 选中信号可能是 SearchTerm、广告 ASIN、广告组或数据质量对象。
3. 真正能写入 ManualAction / ReviewTodo 的对象以后端 preflight target 为准。
4. 如果三者不并排读回，用户需要自己跨三栏推断对象关系。

解决：
1. 新增“人工动作对象链路读回”。
2. 在人工动作按钮前展示今日优先入口、选中信号、前端预览对象和后端预检对象。
3. 当前端预览和后端 preflight target 不一致时显示 blocked。
4. 保存逻辑仍以后端 preflight 和按钮门禁为准，不新增自动广告动作。

防错规则：
1. 右侧人工动作按钮前必须能读回将保存的稳定对象。
2. Parent ASIN 只能作为分诊和诊断入口，不能被默认写成人工动作对象。
3. 后端 preflight target 与当前选中信号不一致时，必须阻断或明确显示 blocked，不能静默保存。
4. 任何对象读回都不能暗示自动加词、否词、调价或暂停广告。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run test:review-ui
npm run build
```

## 2026-06-24：人工动作不能脱离复盘回读证据链

```text
问题：
右侧人工动作如果只保存动作对象、允许动作和下一步，7/14 天复盘时容易只看最终指标涨跌，无法回读当初为什么基于投放商品、投放词、搜索词和广告位做人工处理。

原因：
1. 中间广告组检查清单已经有四层证据边界，但右侧承接没有完整带过去。
2. 人工动作是复盘入口，不是广告执行按钮。
3. 如果动作和证据链脱节，后续 ReviewTodo / ReviewRecord 只能读到动作结果，不能读到原始判断边界。

解决：
1. 在当前广告组人工动作承接中新增“复盘回读证据链”。
2. 复用投放商品、投放词、搜索词、广告位四层证据。
3. 每层展示目的、能证明和不能证明，明确复盘不能只看最终指标涨跌。

防错规则：
1. 人工动作承接必须说明未来复盘要回读哪些证据。
2. 右侧不能只展示动作按钮和动作对象。
3. 复盘读回只能使用原始证据边界，不允许自动推断广告归因或补写历史证据快照。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：当前广告组具体数据不能只列明细

```text
问题：
当前广告组具体数据如果只展示投放商品、投放词、搜索词和广告位明细，用户仍要自己判断这些数据为什么存在、能证明什么、不能证明什么和下一步做什么。

原因：
1. 广告组是容器，明细层很多，但用户真正需要的是复核路径。
2. 投放商品、投放词、搜索词和广告位的证明范围不同，混在一起容易造成错误归因。
3. 如果明细只给指标，用户会重新进入“读广告组报表”的状态。
4. 搜索词和广告位已有证明 / 反证字段，投放商品和投放词也需要在清单层说清用途。

解决：
1. 运营检查清单新增目的、能证明、不能证明。
2. 四层分别覆盖投放商品、投放词、搜索词和广告位。
3. 搜索词和广告位复用已有 decision / placementDecision，投放商品和投放词补充边界说明。

防错规则：
1. 当前广告组具体数据的职责是组织复核路径，不是堆明细报表。
2. 每一层证据都必须回答为什么看、能证明什么、不能证明什么、人工下一步。
3. 未投放子 ASIN、完整关键词库、搜索词归因和广告位归因都不能被自动推断。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：广告组列表不能只展示裸指标

```text
问题：
广告组问题定位如果只展示花费、订单、流量上下文和下一步复核提示，用户仍然需要逐个点开广告组才能知道问题落点、证据是否合流、缺什么证据和下一步能做什么。

原因：
1. 广告组是投放容器，不是最终业务判断对象。
2. 裸指标只能说明表现结果，不能直接回答“问题在哪里”。
3. problemLocator 和 evidenceSynthesis 已经生成，但如果只藏在深层推理详情里，列表层仍像报表。
4. 多 Parent ASIN 场景下，广告组列表越像报表，用户越像在读多张报纸。

解决：
1. 在广告组列表行新增“广告组业务判断”区。
2. 前置问题落点、证据合流、证据缺口和人工下一步。
3. 保留点击聚焦当前广告组具体数据的交互，不新增页面。

防错规则：
1. 广告组列表的职责是缩小范围，不是堆广告组报表。
2. 每个广告组行必须回答“先看哪里、为什么、缺什么、下一步人工做什么”。
3. 搜索词、广告位和广告商品证据只能作为上下文合流判断，不能自动归因到单个广告 ASIN。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：右侧人工动作区不能重复解释 Parent ASIN 进入理由

```text
问题：
左侧已经说明 Parent ASIN 首页分诊排序，中间已经说明证据路径后，右侧人工动作区如果继续展示 Parent ASIN 进入理由和主问题，会让用户在点击人工动作前再次阅读诊断解释，增加决策成本。

原因：
1. 右侧建议区的职责不是解释为什么进入，而是确认能否人工留痕或加入复盘。
2. Parent ASIN 进入理由属于左侧分诊摘要和中间证据路径承接，不应在右侧重复。
3. 如果右侧重复诊断解释，人工动作按钮会被说明文案淹没，用户更难判断该点哪个动作。

解决：
1. 右侧 adGroupActionBridgeScope 改为“人工动作前核对”。
2. 移除右侧“进入理由”和“主问题”。
3. 右侧展示动作对象、来源读回、允许动作和留痕边界。

防错规则：
1. 右侧只回答“能记录什么、复盘什么、不能自动做什么”。
2. 右侧不能重复展示 Parent ASIN 排序依据、进入理由或主问题。
3. 右侧必须保留禁止自动调价、暂停、否词、加词的边界。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：左中右不能重复解释同一段排序判断

```text
问题：
左侧“Parent ASIN 首页分诊摘要”已经展示今日先看什么、排序依据和人工下一步后，中间“当前 Parent ASIN 进入理由”如果继续展示排序依据、主问题和人工下一步，会让用户在左侧和中间重复读同一段判断。

原因：
1. 左侧摘要的职责是分诊排序，回答“先看谁”。
2. 中间诊断区的职责是承接证据路径，回答“沿什么广告证据链看”。
3. 右侧建议区的职责是人工动作前核对，回答“能记录什么、复盘什么、不能自动做什么”。
4. 三栏如果重复解释排序依据，会增加阅读成本，偏离 AI Copilot 工作台降低决策成本的目标。

解决：
1. 中间面板标题改为“当前 Parent ASIN 证据路径承接”。
2. 中间面板移除重复的排序依据和主问题展示。
3. 中间面板改为展示首页摘要读回、证据路径、广告证据、默认聚焦广告组、聚焦原因和进入右侧前核对。

防错规则：
1. 左侧只负责排序和入口摘要，中间只负责证据路径承接，右侧只负责人工动作前核对。
2. 同一段排序依据不要在左中右三栏重复展示。
3. 如果需要补充解释，优先说明对象边界、证据缺口和下一层检查路径，而不是重复指标。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：Parent ASIN 首页不能只给列表让用户逐行读

```text
问题：
左侧已经有“今日 Parent ASIN 优先处理清单”，但如果首页只有列表，用户仍要逐行读每个 Parent ASIN 的主问题、证据摘要和排序依据。10 个 Parent ASIN 场景下，页面仍容易变成 10 张报表。

原因：
1. 列表回答“有哪些对象”，但首页还需要先回答“今天先看哪一个”。
2. 用户目标是降低广告诊断决策成本，不是把 Parent ASIN 数据拆成更多卡片。
3. 如果没有浓缩摘要，完整销售表现、广告组明细和 AI 证据会在首页产生阅读压力，而不是下沉到单个 Parent ASIN 详情链路。

解决：
1. 新增 productScopePriorityDecisionSummary，基于现有 ProductScopePriorityQueueItem 生成首页摘要。
2. 左侧队列前新增“Parent ASIN 首页分诊摘要”，展示今日先看什么、待处理规模、排序依据、人工下一步和边界。
3. 摘要按钮只切换当前 Parent ASIN 诊断入口，不执行广告动作。

防错规则：
1. 首页 Parent ASIN 区域必须先给一个浓缩分诊判断，再展示优先清单。
2. 首页摘要只能复用已有排序事实，不能新增未经证据证明的归因或业务指标。
3. 详细销售表现、广告组、投放词、搜索词和广告位证据应下沉到单个 Parent ASIN 诊断链路，不能把首页改成长报表。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：右侧人工动作不能脱离 Parent ASIN 分诊路径

```text
问题：
右侧“当前广告组人工动作承接”已经提示先按中间检查清单复核，但如果它只展示广告组四层检查项，用户点击记录观察、标记已处理或加入复盘前，仍需要回到左侧和中间确认这个广告组来自哪个 Parent ASIN 分诊理由。

原因：
1. Parent ASIN 是经营入口，广告组是投放容器，人工动作必须承接这条路径。
2. 用户真实操作不是孤立处理广告组，而是先看 Parent ASIN 销售和广告表现，再定位广告组问题。
3. 如果右侧动作缺少来源和边界，页面会增加跨栏阅读成本，尤其在 10 个 Parent ASIN 场景下会变成逐张报表翻看。

解决：
1. ProductScopeAdGroupActionBridgeCard 接收 activeProductScopePriorityItem。
2. 右侧动作卡片新增 Parent ASIN 分诊路径承接区。
3. 展示 Parent ASIN 来源、进入理由、主问题、当前广告组和边界。
4. 缺少 Parent ASIN 来源时降级为当前广告组证据复核，不伪造经营入口。

防错规则：
1. 右侧人工动作必须能读出当前广告组来自哪个 Parent ASIN 分诊路径。
2. 人工动作卡片只能做动作前核对和留痕入口，不能生成自动调价、暂停、否词或加词暗示。
3. Parent ASIN 来源只能复用当前优先队列事实，不能前端新增未被证据证明的排序或归因。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：Parent ASIN 进入理由必须承接默认广告组焦点

```text
问题：
中间诊断区已经展示“当前 Parent ASIN 进入理由”，但如果它只展示排序依据、主问题和人工下一步，用户仍需要继续到广告组问题定位列表中判断默认该看哪个广告组。

原因：
1. Parent ASIN 是经营入口，广告组才是投放容器；两层之间必须有明确承接。
2. 用户真实排查路径是 Parent ASIN -> 广告 ASIN -> 广告组 -> 投放商品 / 投放词 / 搜索词 / 广告位。
3. 如果默认广告组焦点只藏在下方 active 状态里，用户仍会跨面板来回确认，决策成本没有降到底。

解决：
1. ProductScopePriorityEntryBridgePanel 接收 selectedAdGroupDiagnosis。
2. 进入理由面板新增“默认聚焦广告组”和“聚焦原因”。
3. 有广告组行时读取当前广告组名称、问题类型、状态、指标、nextReviewFocus 和 boundary。
4. 无广告组行时明确展示证据缺口，不把 Parent ASIN 包装成广告组问题。

防错规则：
1. Parent ASIN 进入理由必须承接默认广告组焦点，不能只停留在排序依据。
2. 默认广告组焦点只能来自当前 ad_group_diagnosis 运行态，不能前端伪造排序或广告问题。
3. 缺少广告组诊断行时必须展示证据缺口，不能生成自动拆广告组、自动调价、自动否词或自动加词暗示。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
```

## 2026-06-24：Parent ASIN 分诊理由不能只停留在左侧队列

```text
问题：
左侧 Parent ASIN 今日优先处理清单已经能说明排序依据，但用户点击某个 Parent ASIN 后，中间诊断区如果直接进入诊断详情摘要、广告组排序和证据链，用户仍需要把左侧的排序理由记在脑子里，再重新读中间链路。

原因：
1. 左侧队列回答“先看谁”，中间诊断回答“看什么证据”，两者之间缺少进入理由承接。
2. 用户提供的纵向草图本质是运营路径，不是让页面变成长报表；点入后必须先解释为什么进入当前 Parent ASIN。
3. 如果中间第一眼不承接排序依据，10 个 Parent ASIN 场景仍会让用户反复跨栏阅读，决策成本回升。

解决：
1. 新增 activeProductScopePriorityItem，按 activeProductScopeId 找到当前 Parent ASIN 优先队列项。
2. 中间诊断区新增“当前 Parent ASIN 进入理由”，展示排序依据、主问题、当前证据、人工下一步和边界说明。
3. 该面板只解释进入理由，不写入人工动作，不执行广告操作。
4. 结构测试锁定该面板必须在 diagnosisPanel 内，并且先于 Parent ASIN 诊断详情摘要。

防错规则：
1. 左侧 Parent ASIN 分诊理由必须被中间诊断首屏承接，不能让用户点入后重新从头读。
2. 进入理由只能复用已有优先队列事实，不能新增未经后端证明的排序或业务指标。
3. 进入理由不能替代广告证据链，也不能暗示自动加词、否词、调价、暂停或拆广告组。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：Parent ASIN 优先队列不能让排序依据变成黑箱

```text
问题：
左侧已经有 Parent ASIN 今日优先处理清单，可以避免多 Parent ASIN 场景变成长报表。
但如果每行只展示优先级标签、主问题和证据摘要，用户仍需要猜为什么这个 Parent ASIN 排在前面，容易回到逐个点开读的旧路径。

原因：
1. “今日优先 / 等复盘 / 观察”只能表达结果，不能解释排序依据。
2. 多 Parent ASIN 分诊的本质是减少决策成本，排序理由必须可被运营复核。
3. 如果排序依据不透明，用户会把队列当成黑箱推荐，而不是业务分诊入口。

解决：
1. ProductScopePriorityQueueItem 增加 rankReason。
2. 排序依据显式展示复盘到期、待复盘、高优先级信号、待复核信号、广告证据和广告花费。
3. 左侧每个 Parent ASIN 行展示“排序依据”，不改变排序公式、不改变后端接口。

防错规则：
1. Parent ASIN 优先队列必须同时回答“先看谁”和“为什么先看”。
2. 排序依据只能来自现有信号、复盘待办和广告证据，不得伪造业务事实。
3. 点击 Parent ASIN 只切换经营诊断入口，不写人工动作，不执行广告操作。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：右侧人工动作不能脱离当前广告组检查项

```text
问题：
当前广告组焦点已经把中间诊断区压缩成投放商品、投放词、搜索词和广告位四层检查清单，但右侧建议处理区仍主要围绕选中信号、后端预检和人工动作按钮展开。
用户看完中间检查清单后，还需要自己把四层证据和右侧“记录观察 / 标记已处理 / 加入复盘 / 忽略本次”对应起来，决策成本仍偏高。

原因：
1. 中间诊断区回答“这个广告组先查哪层证据”，右侧动作区回答“人工下一步怎么留痕”，两者之前缺少直接承接。
2. 右侧按钮虽然有选择依据，但没有先展示当前广告组四层检查结果，用户需要跨栏回看。
3. 如果右侧不承接广告组检查项，页面会再次变成“中间读证据、右侧找动作”的长流程。

解决：
1. 新增当前广告组人工动作承接卡片，展示在右侧建议处理顶部。
2. 卡片复用中间广告组运营检查清单的四层结果：投放商品、投放词、搜索词、广告位。
3. 明确该区只保存人工留痕或复盘待办，不自动调价、暂停、否词或加词。
4. 结构测试锁定该承接卡片必须在右侧 actionPanel 内，并且先于建议动作摘要。

防错规则：
1. 当前广告组有检查清单时，右侧人工动作区必须能承接检查结果，不能只显示按钮和后端预检。
2. 右侧承接只能帮助人工判断，不得新增自动广告动作或自动执行暗示。
3. 如果中间检查项新增或调整层级，右侧承接必须复用同一 helper，避免两套口径漂移。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：广告组焦点不能让 AI 推理和证据层混排

```text
问题：
当前广告组焦点已经补出了投放商品、投放词、搜索词和广告位证据层，但“问题归属、问题落点、证据合流、人工复核判断”等 AI 推理块仍默认展开，并夹在证据层之间。
用户进入广告组后仍要先读多段解释，再自己判断应该看投放商品、投放词、搜索词还是广告位，读报表感没有完全消除。

原因：
1. 广告组焦点的第一问题是运营先查哪里，不是系统推理过程有多完整。
2. 证据层和 AI 推理层混排，会让用户在“事实证据”和“系统判断”之间来回切换。
3. 多广告组场景下，每个广告组都展开完整推理文本，会再次形成长页面。

解决：
1. 在当前广告组焦点顶部新增运营检查清单，固定按投放商品、投放词、搜索词、广告位四层展示。
2. 保持投放商品、投放词、搜索词和广告位证据层可见。
3. 将问题归属、问题落点、证据合流、人工复核判断和边界说明折叠为“AI 推理细节”。
4. 结构测试锁定阅读顺序：运营检查清单 -> 投放商品 -> 投放词 -> 搜索词 -> 广告位 -> AI 推理细节。

防错规则：
1. 当前广告组焦点第一眼必须回答“先查哪层证据”，不能先铺系统推理过程。
2. AI 推理细节可以展开查看，但不能打断投放商品、投放词、搜索词、广告位的证据阅读顺序。
3. 任何折叠和压缩都不能删除对象边界和禁止自动广告动作说明。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：投放词证据不能藏在搜索词定位里

```text
问题：
当前广告组焦点已经能展示投放商品、搜索词和广告位，但投放词证据主要藏在“搜索词问题定位”的判断项和搜索词行小字里。
用户按照 Amazon 广告架构排查广告组时，会先看广告组内投放商品，再看投放词或投放对象，再判断搜索词表现。如果投放词不是独立层，页面仍像把多个广告证据混在一起，增加用户推理成本。

原因：
1. 广告组是投放容器，投放商品、投放词、搜索词和广告位是不同证据层。
2. 搜索词表现行可以带出 keyword_text / target_id，但这只能说明当前广告组投放上下文，不等于完整关键词库。
3. 第一阶段 SP 关键词详情和商品定向详情暂缓同步，不能把搜索词行里的投放词证据升级成完整覆盖判断。

解决：
1. 在当前广告组具体数据中新增“投放词证据”独立层。
2. 投放词层从搜索词表现行聚合 targetingText，放在投放商品之后、搜索词问题定位之前。
3. 无明确投放词时展示证据缺口，说明只能按搜索词表现复核，不能判断关键词库覆盖。
4. 页面明确禁止把该层解释成自动加词、否词、调价或完整商品定向诊断。

防错规则：
1. 当前广告组焦点必须按投放商品 -> 投放词 -> 搜索词 -> 广告位顺序表达。
2. 投放词证据必须标明来源和边界：来自搜索词表现行的 keyword_text / target_id，不代表完整关键词库。
3. 缺 SP 关键词详情或商品定向详情时，不能生成高置信“关键词库未覆盖”或“商品定向机会”结论。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-24：广告组具体数据不能全部铺在同一层

```text
问题：
用户草图明确提出 Parent ASIN 后应先看广告组数据，再看广告组下的具体数据。
如果页面把每个广告组的投放商品、搜索词、广告位、问题归属和证据边界全部纵向铺开，单个 Parent ASIN 下多个广告组仍会形成长报表，用户需要逐组阅读后自己判断优先级，决策成本没有降低。

原因：
1. 广告组是投放容器，不是经营商品；一个 Parent ASIN 可能对应多个广告组和多个广告商品。
2. “广告组数据”回答的是先看哪个广告组，“广告组下具体数据”回答的是当前广告组内看哪些证据，两者不是同一层信息。
3. 如果没有当前广告组焦点，投放商品、投放词、搜索词和广告位证据会同时堆在多个广告组下，用户很难快速定位人工下一步。

解决：
1. 将广告组问题定位改为可点击的优先级列表，只保留问题类型、关键指标、流量上下文和下一步复核重点。
2. 新增“当前广告组具体数据”焦点面板，集中展示一个广告组内的投放商品表现、问题归属、问题落点、证据合流、搜索词定位、广告位边界和禁止自动动作。
3. 默认选中优先级最高的广告组，用户点击其他广告组时只切换焦点，不改变 Parent ASIN 诊断入口，不写人工动作。

防错规则：
1. 多广告组、多搜索词、多广告 ASIN 场景必须先给优先级列表，再给当前焦点详情，不能直接全量展开。
2. 广告组详情必须继续标明搜索词和广告位只说明同广告组上下文，不能自动归因到单个广告 ASIN。
3. 当前广告组焦点只能服务人工复核和复盘判断，不能出现自动拆广告组、自动加词、自动否词、自动调价等动作。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 1. 使用规则

本文件只记录开发过程中已经暴露的问题、事实原因、解决方案和后续防错规则。

每次遇到以下情况，必须追加记录：

- 用户明确纠偏。
- 设计方向被现有代码带偏。
- 数据来源、对象边界或 API 能力判断错误。
- 测试或验证暴露出可复用问题。
- 同类问题有重复出现风险。

记录格式：

```text
问题
原因
解决方案
后续防错规则
验证方式
```

## 2026-06-24：选中 Parent ASIN 后不能把 AI 诊断藏在长链路底部

```text
问题：
用户提供的草图把 Parent ASIN 销售表现、广告组数据、广告组下具体数据和 AI 广告数据分析按纵向顺序排列。
这个方向符合真实运营链路，但如果直接实现成长页面，用户仍需要先读完销售盘、广告组和明细，最后才看到 AI 判断，决策成本没有真正下降。

原因：
1. 运营点击 Parent ASIN 后的第一问题是“当前应该先判断什么”，不是“所有数据从上到下有哪些”。
2. AI 信号工作台的价值在于前置判断、证据边界和人工动作，而不是把 AI 分析放在报表底部。
3. 如果没有明确“能证明 / 不能证明 / 人工下一步”，用户仍会把指标当成普通 BI 数据阅读。

解决：
1. 新增 Parent ASIN 诊断详情摘要，放在中间诊断区第一层。
2. 摘要固定四段：销售表现摘要、广告组问题排序、广告组下具体数据、AI 诊断摘要。
3. 每段都展示目的、当前判断、能证明、不能证明和人工下一步。
4. 完整广告证据链和广告组详情保留在摘要后面，不作为第一眼阅读负担。

防错规则：
1. 选中 Parent ASIN 后，AI 判断必须前置，不能藏在销售盘和广告组长报表后面。
2. 销售表现只能作为经营背景，广告诊断必须继续落到有广告证据的广告 ASIN、广告组、投放词、搜索词和广告位。
3. 所有建议都必须回到人工动作和 7/14 天复盘，不能包装成自动广告操作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：多 Parent ASIN 入口不能变成看报纸式工作流

```text
问题：
如果用户要看 10 个 Parent ASIN 的广告数据，页面直接按 Parent ASIN 销售表现、广告组数据、广告组下具体数据和 AI 分析纵向铺开，会让用户像读 10 张报纸一样逐个判断，增加决策时间和成本。

原因：
1. 用户的第一问题不是“每个 Parent ASIN 有哪些数据”，而是“今天先处理哪个 Parent ASIN”。
2. 纵向业务链路适合选中一个 Parent ASIN 后深挖，不适合作为多个 Parent ASIN 的首页入口。
3. 如果 AI 分析放在长链路底部，用户必须先读完报表才知道系统建议关注什么。

解决：
1. 新增 Parent ASIN 今日优先处理清单，先按复盘到期、高优先级信号、待复核信号和广告证据排序。
2. 每行只展示优先级、主要业务问题、证据摘要、人工下一步和对象边界。
3. 点击清单只切换诊断入口，不写人工动作，不执行广告动作；完整证据链仍在中间和右侧下钻。

防错规则：
1. 多 Parent ASIN 场景必须先有优先级入口，不能把所有 Parent ASIN 详情直接铺成报表。
2. 首页只回答先看谁、为什么看、点进去做什么；完整销售表现、广告组和搜索词证据放到选中后的详情区。
3. AI 诊断摘要必须前置到决策入口附近，不能藏在长页面底部。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：首屏路径不能把投放商品藏在广告组里

```text
问题：
首屏闭环路径已经展示 Parent ASIN、广告 ASIN、广告组、投放词、搜索词、广告位、AI 信号、人工确认和复盘。
但如果不显式展示“投放商品”，用户仍可能把广告组理解成单商品对象，或把 Parent ASIN 下全部子 ASIN 都带入广告诊断。

原因：
1. Amazon 广告组是投放容器，一个广告组至少包含一个广告商品，也可能包含多个广告商品。
2. Parent ASIN 下销售子 ASIN 是经营背景；只有 advertised_products 里实际投放的商品才能进入广告诊断。
3. 投放商品如果只藏在广告组说明里，首屏路径无法完整表达广告结构。

解决：
1. 首屏 pathSummary 增加“投放商品 / 投放词 / 搜索词 / 广告位”。
2. pathSteps 增加投放商品边界说明：投放商品来自广告组内 advertised_products 证据，只代表实际参与广告投放的商品。
3. signalUi、runtime、structure 测试锁定首屏必须显式展示投放商品层。

防错规则：
1. 任何 Parent ASIN 广告诊断路径都必须区分销售子 ASIN、广告 ASIN、广告组和投放商品。
2. 广告组不能被当作产品；投放商品必须来自广告商品证据。
3. 未投放子 ASIN 只能作为经营背景，不能进入广告 AI 信号队列或人工动作对象。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
```

## 2026-06-24：搜索词表现分组点击不能改变经营诊断入口

```text
问题：
用户确认“语义组复核”的本质应是从 Parent ASIN 角度聚合广告中的用户搜索词表现。
如果点击搜索词表现分组时改变诊断入口，或文案让用户以为正在处理一个语义组对象，就会再次偏离广告数据工作台的业务本质。

原因：
1. 搜索词表现分组只是当前 Parent ASIN 下的二级分析视角，不是 ProductScope。
2. 具体诊断和人工留痕对象应落到 SearchTerm 或后端 preflight 确认的稳定广告对象。
3. “已聚合复核”这类文案不够明确，容易被理解成已处理状态或语义标签对象。

解决：
1. 将激活态文案改为“当前搜索词表现分组”。
2. 结构测试锁定 handleSelectSearchIntent 只能设置 selectedSearchIntentLabel、selectedSearchIntentScopeId 和 selectedId。
3. 结构测试禁止 handleSelectSearchIntent 调用 setSelectedProductScopeId。

防错规则：
1. 搜索词表现分组只能做当前 Parent ASIN 下的二级筛选，不得写回经营诊断入口。
2. 任何人工动作必须落到具体 SearchTerm 或后端 preflight 确认对象，不能落到分组标签。
3. 页面主语优先使用“Parent ASIN 广告搜索词表现复核 / 搜索词表现分组”，避免重新使用“语义组复核”作为用户可见主概念。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui-runtime
```

## 2026-06-24：人工动作按钮不能只展示可点状态

```text
问题：
右侧人工确认区已经有后端预检、对象身份门禁和按钮 disabled 状态。
但如果按钮前不说明每个动作的适用场景，用户仍可能把“加入复盘 / 标记已处理 / 记录观察 / 忽略本次”理解成普通状态按钮，甚至误以为系统会执行广告动作。

原因：
1. disabled 状态只能说明能不能点，不能说明为什么该选这个动作。
2. 四个动作写入结果不同：复盘类会生成 7/14 天待办，忽略本次不生成当前复盘待办。
3. 如果动作含义只藏在 title 或长证据链里，运营操作前需要自己推断。

解决：
1. 新增 manualActionChoiceGuideItems，明确四个动作的适用场景、写入结果和动作边界。
2. 右侧人工确认区在按钮前展示“人工动作选择依据”。
3. 选择依据复用 manualActionButtonGate，显示当前可点击或阻塞原因，避免解释和按钮状态不一致。

防错规则：
1. 人工动作按钮前必须可见地解释动作语义，不能只依赖 hover title。
2. 前端解释层必须复用后端 preflight / action gate 结果，不能自己判断是否可写。
3. 忽略本次必须明确不生成当前 7/14 天复盘待办，但信号和证据仍保留。

验证方式：
npm run test:workbench-structure
npm run test:review-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：SearchTerm 证据链必须翻译成可执行复核路径

```text
问题：
Parent ASIN 广告搜索词表现复核已经能展示聚合承接、具体 SearchTerm 理由和诊断证据链。
但仅展示“为什么”仍不够，运营需要知道点击聚合卡片后具体按什么顺序核对，否则容易继续把它当成解释性信息，而不是可执行工作台。

原因：
1. 证据链回答的是数据关系，执行路径回答的是用户下一步动作，两者不是一回事。
2. SearchTerm 聚合、具体 SearchTerm、广告 ASIN / 广告组 / 投放词 / 广告位、人工动作对象分属不同层级，用户需要显式路径。
3. 如果没有执行步骤，用户会自己推断操作顺序，容易再次把搜索词表现分组误读成诊断入口或人工动作对象。

解决：
1. SearchIntentSelectedTermReasonSummary 增加 executionSteps。
2. “具体 SearchTerm 复核理由”渲染 SearchTerm 复核执行路径。
3. 执行路径固定为：锁定入口、锁定 SearchTerm、核对广告承接、选择人工动作。
4. 结构测试和 signalUi 测试锁定这 4 步和禁止自动广告动作边界。

防错规则：
1. 新增诊断证据时，必须同时说明用户下一步如何人工核对。
2. 聚合视角只服务筛选和优先级，不能成为人工动作对象。
3. 人工动作前必须先确认后端 preflight 的稳定对象，不能直接从聚合标签生成动作。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：经营入口摘要不能把搜索词表现复核推出首屏

```text
问题：
用户再次确认“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
运行态曾发现经营入口摘要承载过多 MVP 状态、门禁、路径、广告 ASIN 表格和证据下钻内容，容易把三栏工作台和左侧 Parent ASIN 广告搜索词表现复核推到页面下方。

原因：
1. 经营入口摘要是帮助用户进入工作台的上方引导，不应该承载完整证据链。
2. 搜索词表现复核需要贴近信号队列，才能让用户理解它只是当前 Parent ASIN 下的 SearchTerm 聚合视角。
3. 如果首屏先展示长经营摘要，用户会再次把页面理解成经营报表，而不是广告 AI 信号工作台。

解决：
1. 将 productScopeBusinessPreview 从默认展开 div 改为 details 折叠摘要。
2. 首屏只展示 Parent ASIN 标题、摘要和关键事实，MVP 状态、准入门禁、广告 ASIN 表格和下钻入口放入“展开证据”。
3. 结构测试锁定经营摘要必须是可折叠 details，禁止退回大块默认展开 div。

防错规则：
1. 首屏经营摘要只能帮助用户确认当前 Parent ASIN 诊断入口，不能替代三栏工作台。
2. 搜索词表现复核必须贴近左侧信号队列，不能被长经营证据块隔开。
3. 完整证据链应放到中间诊断区、右侧人工确认区或可展开详情里。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
cd backend && python -m pytest tests/test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope
```

## 2026-06-24：1280 桌面不能把右侧人工确认挤到诊断区下方

```text
问题：
1280x720 桌面宽度下，工作台被 @media (max-width: 1320px) 提前切成两列。
左侧队列和中间诊断仍可见，但右侧 actionPanel 被放到第二行，实际 y=24335，manualActions y=27605，用户必须滚过很长诊断区才能看到人工确认。

原因：
1. 响应式断点把 1280 这类常见桌面宽度当成窄屏处理。
2. 两栏布局使用 grid-template-areas: "queue diagnosis" / "queue action"，当 diagnosis 很长时 action 会被排在长诊断之后。
3. 这破坏了 AI Copilot 工作台的第一屏合同：左侧信号队列 / 中间证据链 / 右侧人工确认必须并列承接。

解决：
1. 将桌面三栏最小宽度收紧为 300 / 500 / 300。
2. 将两栏断点从 1320px 下调到 1180px，让 1280 桌面继续保持三栏。
3. 在 signalWorkbenchStructure.test.ts 增加 CSS 合同断言，防止断点退回 1320px。

防错规则：
1. 后续调整工作台响应式布局时，1280 桌面必须保留三栏。
2. 右侧人工确认不能被放到长诊断区下方，否则用户会把它误读成详情页尾部，而不是人工决策区。
3. 如果确实要做两栏或单栏，必须只在更窄屏触发，并确认人工动作区有清晰入口。

验证方式：
npm run test:workbench-structure
npm run build
浏览器运行态复核 1280x720：workbenchGrid=312.25px 576.484px 312.266px，rightActionVisibleInFirstRow=true。
```

## 2026-06-24：搜索词表现复核不能残留搜索意图主语

```text
问题：
用户再次明确“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
虽然页面主标题已经收口，但后端 current_judgement 和前端广告组搜索词判断里仍残留“搜索意图 / 搜索意图表现分化”的用户可见表达。

原因：
1. 历史实现把搜索词归类称为 search_intent，容易把聚合维度说成业务对象。
2. 用户真实任务不是复核语义意图，而是判断广告用户搜索词表现、广告商品承接和人工复核优先级。
3. 如果继续展示“搜索意图”，用户会误以为系统在做抽象语义分析，而不是广告链路诊断。

解决：
1. 后端 SearchIntentSummary.current_judgement 改为“广告用户搜索词表现和广告商品承接”。
2. 前端广告组搜索词判断改为“广告搜索词表现分化”。
3. 旧“语义组 / 搜索意图分组”只作为历史 evidence label 兼容和展示归一化输入保留。

防错规则：
1. 用户可见主语优先使用 Parent ASIN 广告搜索词表现复核、搜索词表现分组、具体 SearchTerm。
2. search_intent 只能作为代码内部命名或历史兼容概念，不能在新文案里变成业务主语。
3. 新增 SearchTerm 相关文案时，必须回答它服务哪个广告业务判断，而不是只说明语义分类。

验证方式：
python -m pytest tests\test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope tests\test_signal_triage_service.py::test_search_intent_current_judgement_uses_ad_search_term_performance_subject -q
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
```

## 2026-06-24：复盘待办不能只显示排程状态

```text
问题：
右侧复盘待办已经能展示完整“复盘待办证据回读核对”，但第一层仍主要是到期状态、对象和原始证据快照文本。
对于 SearchTerm 待办，用户第一眼仍不容易看到当时为什么把它判为扩量、止损或观察候选。

原因：
1. ReviewTodo 的核心价值是继承人工点击时证据，并在 7/14 天后支持复盘。
2. 完整证据门禁信息较长，适合保存前核对，但不适合作为第一眼行动摘要。
3. 如果不先读回“搜索词表现判断 / 人工下一步 / 需要补证 / 动作边界”，用户容易把待办状态误读成效果结论，或从处理后指标反推当时原因。

解决：
1. 新增 buildReviewTodoDecisionReadbackSummary，只从 ReviewTodo.evidence_snapshot 抽取业务判断摘要。
2. 右侧复盘待办区在完整证据门禁前展示“复盘待办业务判断读回”。
3. 摘要明确待办只表示进入 7/14 天排程，未到期不判断效果，不自动执行广告动作。
4. review-ui、workbench-structure 和运行态 smoke 测试同步锁定该读回。

防错规则：
1. ReviewTodo 第一眼必须能回答“当时为什么进入复盘”，不能只显示到期状态。
2. ReviewTodo 摘要不能替代完整 ReviewRecord 保存前门禁。
3. 待办只表示排程，不能被表达成建议有效、无效或广告动作已执行。
```

## 2026-06-24：搜索词表现分组不能按接口返回顺序直接展示

```text
问题：
搜索词表现分组卡片已经能说明扩量、止损和观察判断，但多组同时存在时，如果直接按接口返回顺序展示，运营可能先看到低样本观察组，错过高花费无订单或低 ACOS 有订单的优先组。

原因：
1. 接口返回顺序不是人工处理优先级。
2. 搜索词表现分组服务的是 AI 信号分诊，第一屏应该优先暴露更确定的风险和机会。
3. 如果排序只靠数据源顺序，页面看起来像普通列表，而不是 Copilot 工作台。

解决：
1. 前端 buildSearchIntentReviewCards 先计算业务判断，再按人工处理优先级排序。
2. 排序优先级固定为：止损复核 -> 扩量复核 -> 观察复核。
3. 同类中按对应业务指标排序，并保留原始顺序作为最终兜底。
4. 测试覆盖排序后再截断，防止 limit 先截掉真正高优先级组。

防错规则：
1. 搜索词表现分组展示顺序必须服务人工分诊，而不是接口返回顺序。
2. 排序只能决定展示优先级，不能解释成自动否词、调价、暂停或扩量。
3. 后续新增分组类型时，必须明确它在止损、扩量、观察中的优先级位置。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-24：搜索词表现分组指标不能停留在裸指标说明

```text
问题：
搜索词表现分组卡片已经展示花费、点击、订单、ACOS、CVR，也已经说明它从 Parent ASIN 角度聚合广告用户搜索词。
但如果指标说明只写“扩量机会指标 / 浪费风险指标 / 证据缺口提示”，用户仍要自己判断这些指标到底服务扩量、止损还是观察，业务落地感不足。

原因：
1. 指标名称偏分析报表语言，没有直接回答运营下一步判断。
2. 扩量、止损、观察三类动作边界虽然存在于信号规则中，但卡片指标没有把“目标、证据、边界”放在同一句里。
3. 用户已经多次指出当前项目不能做浅显分析，必须说明指标为什么服务业务目标。

解决：
1. 指标说明改为“扩量判断 / 止损判断 / 观察门槛”。
2. 每条说明固定包含判断目标、当前证据和人工边界。
3. 测试锁定三类判断名称，并要求扩量说明落到具体 SearchTerm，止损说明禁止自动否词、调价或暂停广告。

防错规则：
1. 搜索词表现分组卡片不允许只展示裸指标，必须说明指标服务的业务判断。
2. 扩量、止损、观察只能作为人工复核判断，不能包装成自动执行动作。
3. 后续新增指标时，必须同时回答“为什么看这个指标”和“它不能证明什么”。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-24：SearchTerm 复核链不能缺少投放词证据分层

```text
问题：
用户再次明确“语义组复核”的作用应从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
当前页面主语已经收口为 Parent ASIN 广告搜索词表现复核，但具体 SearchTerm 的分层判断只有广告组合流、同组投放商品和广告位边界，投放词证据虽然在长证据列表和保存门禁中存在，却没有作为中间诊断的独立判断层。

原因：
1. SearchTerm 表现能否落地复核，必须知道它由关键词、商品定向还是自动投放上下文承接。
2. 如果投放词证据只藏在长列表里，用户仍会先看到裸指标，难以判断后续该核对关键词、商品定向还是广告组策略。
3. 缺少投放词分层时，页面容易让“搜索词表现分组”看起来像抽象语义分析，而不是广告投放链路分析。

解决：
1. SearchTerm 机会复核链增加“投放词证据”层。
2. 四层顺序固定为：广告组合流判断 / 同组投放商品表现 / 投放词证据 / 广告位边界。
3. 测试要求真实推荐链和 helper 样例都必须包含投放词证据，并说明不能证明关键词库完整覆盖、不能自动加词否词调价。

防错规则：
1. Parent ASIN 广告搜索词表现复核必须能一路读到投放词承接证据。
2. 搜索词表现分组不是人工动作对象；人工处理必须落到具体 SearchTerm 和可回看的广告投放上下文。
3. 后续调整中间诊断区时，不能只保留广告组、同组商品和广告位，必须同时保留投放词证据。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-24：Parent ASIN 概览不能把销售子 ASIN 和广告 ASIN 放在同一诊断层

```text
问题：
用户多次纠偏当前项目必须以广告数据为核心。中间 Parent ASIN 概览虽然已经说明未投放子 ASIN 不进广告队列，但标题和摘要仍使用“商品组 / 经营盘子”等宽泛词，容易让运营误以为 Parent ASIN 下全部销售子 ASIN 都是广告诊断对象。

原因：
1. SalesProduct 与 AdvertisedProduct 数据模型已分开，但前端首屏概览表达不够分层。
2. “12 个销售表现子 ASIN / 3 个当前投放广告 ASIN”放在同一句里，用户需要自己推理哪些能进入广告诊断。
3. 广告 ASIN 表头只写“广告 ASIN”，没有强调它来自投放证据，而不是完整变体清单。

解决：
1. Parent ASIN 概览改为“经营背景与广告证据”。
2. 摘要拆成“经营背景：销售表现子 ASIN”和“广告诊断：有投放证据的广告 ASIN”。
3. 关系摘要显式标注“销售背景（不直接诊断）”和“广告诊断对象（有投放证据）”。
4. 广告 ASIN 表头改为“广告 ASIN（有投放证据）”。

防错规则：
1. 销售子 ASIN 只能作为经营背景、覆盖缺口或销售承接参考，不能直接进入广告诊断队列。
2. 广告诊断对象必须来自 advertised_products 或明确广告证据，不能用 Parent ASIN 下全部子 ASIN 替代。
3. 用户可见概览必须同时说明“能进入诊断的对象”和“不能证明的边界”。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-24：搜索词表现分组新 label 必须进入关键证据优先级

```text
问题：
用户明确“语义组复核”的作用是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析广告搜索词表现。
当前后端新证据已经使用“搜索词表现分组”，但前端 search_term_opportunity 的关键证据排序只把“搜索意图分组 / 语义组 / 广告搜索词聚合上下文”等旧 label 放在最高优先级，没有包含新标准 label。

原因：
1. 旧 label 兼容层持续存在，容易让测试和排序规则停留在历史命名。
2. 新写入链路改成“搜索词表现分组”后，关键证据排序没有同步更新。
3. 如果不修，用户可能先看到“投放上下文数”等辅助指标，而不是搜索词表现分组，页面主线会被弱化。

解决：
1. search_term_opportunity 关键证据优先级新增“搜索词表现分组”。
2. 当前搜索词机会测试样例改用“搜索词表现分组”，断言它是关键证据第一项。
3. 完整 SearchTerm 复盘链测试 fixture 改用“搜索词表现分组”，旧“语义组”只保留在历史兼容和坏文案归一化测试中。

防错规则：
1. 新写入证据 label 一旦替换旧口径，关键证据排序、人工预检、复盘读回和运行态测试必须同步检查。
2. “语义组”只能作为旧数据兼容或文本归一化输入，不能作为当前测试样例的标准用户主语。
3. Parent ASIN 广告搜索词表现复核的第一关键证据应优先露出搜索词表现分组，再展示投放上下文、订单、ACOS 等指标。

验证方式：
npm run test:signal-ui
npm run test:review-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：搜索意图分组过滤不能看起来像切换了经营诊断入口

```text
问题：
用户观察到点击“语义组复核 / 搜索意图分组”后，像是“诊断入口筛选器”的值发生了变化。
运行态复核确认真实 select value 没有变，仍是 parent_asin:B00K4W4AAA；但旧标签“诊断入口”过宽，容易让用户把经营入口、搜索意图过滤和选中 SearchTerm 对象混在一起理解。

原因：
1. Parent ASIN 是经营诊断入口，搜索意图分组只是当前入口下广告 SearchTerm 的过滤维度。
2. 点击搜索意图分组会选中具体 SearchTerm，导致中间诊断对象变化，肉眼上容易被误解成入口变化。
3. 顶部下拉旧名“诊断入口”没有明确它是经营层入口，不够区分左侧的搜索意图分组过滤。

解决：
1. 顶部下拉改名为“经营诊断入口”。
2. 下拉新增 aria-label="经营诊断入口筛选器"。
3. 结构测试锁定该下拉只绑定 activeProductScopeId，不允许 activeSearchIntentLabel 或 selectedSearchIntentLabel 驱动。
4. 浏览器复核点击搜索意图分组后，经营诊断入口仍为 parent_asin:B00K4W4AAA。

防错规则：
1. 经营诊断入口只能由 ProductScope 状态驱动。
2. 搜索意图分组只能作为当前经营入口下的 SearchTerm 过滤态。
3. 选中具体 SearchTerm 只能改变中间诊断和右侧人工确认对象，不能暗示 Parent ASIN / ASIN 入口变化。
4. 后续如果新增面包屑，必须同时展示经营诊断入口、搜索意图分组过滤和选中信号对象三层。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
浏览器运行态复核：http://localhost:5175/ 点击“规则语义：海滩出行用品”后，经营诊断入口 selectedText=经营入口：Parent ASIN B00K4W4AAA，value=parent_asin:B00K4W4AAA。
```

## 2026-06-23：后端新 evidence_snapshot 不能继续写入“语义组”

```text
问题：
上一轮前端已经把“语义组复核”收口成 Parent ASIN 广告搜索词表现复核，但后端 preflight、信号生成和部分测试仍会在新 evidence_snapshot 中写入“语义组”。
这会造成前端显示和后端证据口径不一致，用户看起来像已经收口，实际人工留痕仍在沉淀旧对象名。

原因：
1. 前端先做了展示归一化，但后端生成源头没有同步修改。
2. “语义组”既被当作旧 evidence label，又被当作用户可见业务主语，导致兼容层和新口径边界不清。
3. 测试只验证能读回旧 label，没有强制新生成链路写入“搜索意图分组”。

解决：
1. SearchTerm 人工动作预检的新证据快照改写“搜索意图分组”。
2. 信号检测中的搜索词机会、ABA 语义背景和聚合信号事实 label 改写“搜索意图分组”。
3. manual_actions、signal_triage 增加新旧搜索意图上下文 label 兼容，历史“语义组”仍可读。
4. 后端测试改为断言新生成证据写“搜索意图分组”，旧 fixture 保留“语义组”验证兼容。

防错规则：
1. 新写入 evidence_snapshot 禁止再使用“语义组”作为 SearchTerm 聚合证据 label。
2. “语义组”只能出现在历史数据兼容、旧 fixture 或文本归一化规则中。
3. 用户可见主语必须是 Parent ASIN 广告搜索词表现复核，分析维度是搜索意图分组，人工动作对象是具体 SearchTerm。
4. 后续新增测试必须同时覆盖“新写入用新 label”和“旧留痕可读”。

验证方式：
python -m py_compile backend\app\models\signals.py backend\app\services\manual_action_preflight.py backend\app\services\manual_actions.py backend\app\services\signal_detection.py backend\app\services\signal_triage.py
python -m pytest tests\test_manual_action_preflight_cli.py tests\test_manual_action_apply_cli.py tests\test_manual_actions.py tests\test_api_routes.py tests\test_signal_detection.py tests\test_signal_triage_service.py -q
npm run test:review-ui
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-23：语义组复核不能抢占 Parent ASIN 广告搜索词表现主语

```text
问题：
用户进一步明确：“语义组复核”的作用应该是从 Parent ASIN 的角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果页面继续把“语义组 / 聚合标签 / Parent ASIN 搜索词表现聚合”作为主语，用户会误以为系统在处理一个语义资产，而不是在分析广告 SearchTerm 表现。

原因：
1. 后端历史 evidence_snapshot 和测试夹具里存在“语义组”旧 label。
2. 前端部分读回文本沿用“Parent ASIN 搜索词表现聚合”，不够强调广告数据来源和业务用途。
3. “聚合标签”是实现层概念，不是运营用户能执行或复盘的广告对象。

解决：
1. 右侧人工确认核对区改为“Parent ASIN 广告搜索词表现复核背景核对”。
2. 用户可见层把“语义组 / 广告搜索词聚合上下文”展示为“搜索意图分组”。
3. 搜索词分组卡、运行态接口文案和复盘读回统一表达为“搜索意图分组不是人工动作对象”。
4. ReviewTodo / ReviewRecord 读回显示“Parent ASIN 广告搜索词表现复核”，同时保留旧 evidence label 兼容。

防错规则：
1. 用户可见主语必须是 Parent ASIN 广告搜索词表现复核或具体 SearchTerm。
2. 搜索意图分组只能作为当前 Parent ASIN 广告搜索词表现的聚合维度，不能作为人工动作对象。
3. 后端旧 label 可以兼容识别，但前端展示必须翻译成业务口径。
4. 任何新增复盘读回文案都必须说明广告搜索词、广告组、投放词、广告 ASIN 和广告位的证据边界。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:review-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-23：中间承接条不能继续使用“聚合标签”做用户主语

```text
问题：
左侧板块已经收口为“Parent ASIN 广告搜索词表现复核”，但用户点击聚合卡进入具体 SearchTerm 后，中间承接条仍显示“Parent ASIN 搜索词表现聚合”和“聚合标签”。
这会让用户误以为当前处理对象是一个抽象聚合标签，而不是从 Parent ASIN 广告搜索词表现复核进入的具体 SearchTerm。

原因：
1. 左侧板块、运行态接口和右侧复盘链已经收口，但中间承接条仍沿用旧展示文案。
2. “聚合标签”是实现层表达，不是运营用户的业务对象。
3. 如果中间区主语不一致，用户会再次把语义分组误读成诊断入口或人工动作对象。

解决：
1. 中间承接条标题改为“Parent ASIN 广告搜索词表现复核承接”。
2. 字段名从“聚合标签”改为“搜索意图分组”。
3. 边界说明明确该分组只是分析分组，不是经营商品、广告组或人工动作对象。
4. 前端结构测试禁止 `<b>聚合标签</b>` 回流。

防错规则：
1. 用户可见层出现语义分组时，必须绑定 Parent ASIN 广告搜索词表现复核口径。
2. “聚合标签”只能作为内部概念，不应作为页面字段名。
3. 点击聚合卡后，中间诊断和右侧人工确认必须继续落到具体 SearchTerm。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-23：Parent ASIN 搜索词聚合必须用运行态接口锁口径

```text
问题：
用户再次明确“语义组复核”的作用是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
前端 helper 和结构测试已经有正确文案，但如果运行态 /api/search-intents 退回站点级聚合、自然搜索词池或语义资产口径，用户仍会看到错误业务主语。
同时，完整 evidence_snapshot 已超过默认展示 10 条，测试如果只读默认截断列表，会误判“搜索词边界”等关键证据缺失。

原因：
1. “语义组”只是聚合维度，容易被误读成独立业务对象。
2. 仅验证前端兜底文案不够，必须验证真实接口带 product_scope_id 后的返回口径。
3. evidence_snapshot_preview 条目增多后，默认截断不适合用来判断完整证据是否存在。

解决：
1. 运行态测试新增 /api/search-intents?product_scope_id=parent_asin:B00K4W4AAA 校验。
2. 测试锁定搜索词聚合必须包含当前 Parent ASIN、推荐 SearchTerm 所属搜索词行、诊断入口不变、不能当人工动作对象。
3. 完整证据快照校验改为读取 30 条预览。
4. 模拟 ready ReviewTodo 补齐“逐投放上下文”，保持 SearchTerm 复盘链完整。

防错规则：
1. 语义组复核的用户可见主语必须是 Parent ASIN 广告搜索词表现复核。
2. product_scope_id 存在时，/api/search-intents 不得返回全站语义池口径。
3. 判断 evidence_snapshot 完整性时不能只看默认截断列表。
4. SearchTerm 复盘链必须保留逐投放上下文，不能退回搜索词裸指标。

验证方式：
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run test:workbench-structure
python -m pytest tests\test_signal_triage_service.py -k "search_intent_summaries_respect_parent_asin_product_scope or review_candidates"
```

## 2026-06-23：SearchTerm 复盘读回不能丢失逐投放复核顺序

```text
问题：
用户明确“语义组复核”的作用是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
该定义要求用户从聚合卡进入具体 SearchTerm 后，复盘读回必须保留广告组和投放词层面的逐投放复核顺序。
前端 ReviewTodo / ReviewRecord 读回门禁原先没有把“逐投放上下文”当成 SearchTerm 必备证据，可能让 7/14 天复盘退回搜索词合计指标。

原因：
1. 后端 preflight 已经保存“逐投放上下文”，但前端 reviewUi 的必备标签链尚未同步。
2. 旧测试夹具只覆盖广告组合流判断、同组投放商品表现、投放词、搜索词边界、广告位边界和 ABA 背景。
3. 用户真正需要的是复核顺序：先看哪个广告组、哪个投放词、再核对广告 ASIN 承接。

解决：
1. SearchTerm ReviewTodo 读回链新增“逐投放复核顺序”。
2. ReviewRecord 保存前门禁新增 ad_context_rows / ad_context_rows_missing。
3. 已保存 ReviewRecord 读回要求新增“逐投放上下文”。
4. 前端测试补齐完整 SearchTerm 复盘夹具，并覆盖缺少“逐投放上下文”时 blocked。

防错规则：
1. Parent ASIN 搜索词表现聚合只能筛选同类 SearchTerm，不能替代具体 SearchTerm 复盘对象。
2. 逐投放上下文必须表示人工核对顺序，不能表示自动加词、否词、调价或暂停广告。
3. 后端 evidence_snapshot_preview 新增关键判断后，前端 ReviewTodo / ReviewRecord 读回门禁必须同步。

验证方式：
npm run test:review-ui
npm run build
python -m pytest tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot tests\test_api_routes.py::test_beach_essentials_manual_action_post_write_readback_distinguishes_action_types -q
```

## 2026-06-23：人工确认快照不能丢失逐投放复核优先级

```text
问题：
中间诊断区已经在 beach essentials 的逐投放上下文里标记“优先复核广告组 / 对照复核广告组”，但右侧人工确认预检快照如果仍只保存广告组明细，后续 ManualAction、ReviewTodo 和 ReviewRecord 读回会丢失当时的人工排查顺序。

原因：
1. 前端 buildSearchTermAdContextRows 已补充 reviewPriority 和 reviewReason。
2. 后端 manual_action_preflight 的“逐投放上下文”快照仍只拼广告活动、广告组、投放词和指标。
3. 人工留痕保存的是后端 evidence_snapshot_preview，不是前端中间区展示文本。

解决：
1. 后端 SearchTerm 逐投放快照按订单优先、花费其次排序。
2. 快照行写入“优先复核广告组 / 对照复核广告组 / 止损复核候选 / 观察补证”。
3. 测试同时覆盖人工预检和授权写入后的 evidence_snapshot。

防错规则：
1. 中间诊断区出现的关键人工判断，如果会影响复盘读回，必须同步进入后端 evidence_snapshot_preview。
2. 复核优先级只能表示人工排查顺序，不能成为自动加词、否词、调价或暂停广告的依据。
3. 历史旧快照缺少该判断时不能静默补写，只能按旧证据边界读回或重新人工留痕。

验证方式：
python -m py_compile backend\app\services\manual_action_preflight.py
python -m pytest backend\tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_snapshots_diagnosis_contract_gaps backend\tests\test_manual_action_apply_cli.py::test_manual_action_apply_rewrites_after_voided_legacy_todo_with_isolated_roots -q
npm run test:signal-ui
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id "search_term:1:beach essentials" --expected-object-type search_term --action-type add_to_review --top 5 --compact
```

## 2026-06-23：后端 preflight 证据快照不能缺具体 SearchTerm 身份

```text
问题：
用户明确“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
前端已经把语义组定位为聚合分析视角，但后端 preflight evidence_snapshot_preview 如果不显式返回具体 SearchTerm 身份，人工确认和复盘读回仍可能只看到 Parent ASIN、广告 ASIN 或语义聚合上下文，用户无法第一眼确认处理对象。

原因：
1. 语义组是分析聚合维度，不是广告架构里的可写动作对象。
2. SearchTerm 人工动作必须落到具体 search_term object_id。
3. 后端真实写入路径如果少了 SearchTerm 身份块，会和前端兜底展示口径不一致。

解决方案：
1. SearchTerm preflight evidence_snapshot_preview 新增“搜索词”对象身份块。
2. 顺序固定为排查路径、AI 准入、搜索词、Parent ASIN入口、广告 ASIN承接，再进入广告组、投放词、广告位、ABA 和动作边界。
3. 后端测试锁定 B00K4W4AAA + beach essentials 的 ManualAction / ReviewTodo 都能读回具体 SearchTerm 身份。

后续防错规则：
1. 语义组只能用于 Parent ASIN 下广告用户搜索词表现聚合，不能成为诊断入口或人工动作对象。
2. SearchTerm 人工动作和 7/14 天复盘的第一主语必须是具体 SearchTerm。
3. 点击语义组只允许筛选当前 Parent ASIN 下同类 SearchTerm 信号，不允许改变顶部诊断入口筛选器。

验证方式：
cd backend; python -m pytest tests/test_manual_action_preflight_cli.py -q
cd backend; python -m pytest tests/test_manual_action_apply_cli.py tests/test_api_routes.py -q
cd frontend; npm run test:signal-ui
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id "boys sunglasses" --expected-object-type search_term --action-type add_to_review --top 5 --compact
```

## 2026-06-23：人工留痕证据快照不能让聚合标签抢占 SearchTerm 主语

```text
问题：
Parent ASIN 广告搜索词表现复核已经明确语义组只作为聚合维度。
但前端 buildSearchIntentManualActionEvidenceSnapshot 生成证据快照时，第一条仍是“广告搜索词聚合上下文”，第二条才是“搜索词”。
复盘摘要只展示前几条证据时，用户容易先看到聚合标签，从而误以为人工动作对象是语义组。

原因：
1. 聚合上下文在早期用于解释 SearchTerm 分组，因此被放在证据快照开头。
2. 当前业务口径已经升级为“具体 SearchTerm 人工复核，聚合上下文辅助回看”，证据顺序没有同步调整。
3. manualActionEvidenceSnapshotText 默认读取前 4 条证据，顺序会直接影响用户对动作对象的理解。

解决方案：
1. 有具体 SearchTerm 时，证据快照第一条改为“搜索词”。
2. “广告搜索词聚合上下文”改为第二条辅助证据，继续说明同类 SearchTerm 判断链。
3. review-ui 测试锁定留痕摘要必须先出现具体 SearchTerm。

后续防错规则：
1. 人工留痕和复盘读回的第一主语必须是具体处理对象。
2. SearchTerm 场景下，聚合上下文只能作为辅助证据，不能排在具体搜索词之前。
3. 任何证据摘要如果只截取前几条，必须优先保留对象身份、AI 准入和关键边界。

验证方式：
cd frontend; npm run test:review-ui
cd frontend; npm run test:workbench-structure
cd frontend; npm run build
```

## 2026-06-23：搜索词表现指标目的不能停留在一句总括

```text
问题：
用户明确“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
如果卡片只显示花费、点击、订单、ACOS 或一句指标目的，用户仍要自己推导这组词到底是扩量、止损还是补证观察。

原因：
1. SearchTerm 聚合卡片已经绑定 Parent ASIN 广告上下文，但指标表达仍偏“展示数据”。
2. 扩量机会、浪费风险和证据缺口服务的是不同业务判断，不能混成同一段文案。
3. 如果不拆开指标目的，“语义组复核”容易被误读成抽象语义分析，而不是广告搜索词表现分析。

解决方案：
1. SearchIntentReviewCard 增加 metricPurposeItems。
2. 指标目的拆成扩量机会指标、浪费风险指标、证据缺口提示。
3. 前端卡片直接渲染三类指标目的，明确哪些指标用于判断承接质量，哪些用于判断消耗浪费，哪些只提示补证。

后续防错规则：
1. Parent ASIN 广告搜索词表现复核必须说明指标为什么存在，不能只展示裸指标。
2. 语义组只允许作为 SearchTerm 表现聚合维度，不允许成为人工动作对象。
3. 缺广告组、投放词、广告位、同组 ASIN 或 ABA 周期证据时，只能提示补证或降低置信度，不能包装成自动广告动作。

验证方式：
cd frontend; npm run test:signal-ui
cd frontend; npm run test:workbench-structure
cd frontend; npm run build
```

## 2026-06-23：广告搜索词表现复核不能缺 Parent ASIN 入口和广告 ASIN 承接

```text
问题：
用户再次明确：“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
此前人工确认和复盘读回虽然有搜索词、投放词、广告组、广告位、ABA 等证据，但没有强制保存 Parent ASIN 入口和广告 ASIN 承接。
这样会导致复盘时只剩搜索词裸指标，无法证明当时分析的是哪个经营商品盘、哪些已广告 ASIN 在承接。

原因：
1. 语义组是聚合维度，不是人工动作对象。
2. SearchTerm 是广告表现对象，但必须从 Parent ASIN 经营入口和广告 ASIN 承接范围进入。
3. 旧 ReviewTodo 证据快照缺 Parent ASIN 入口和广告 ASIN 承接时，仍可能被页面误读成完整复盘链。

解决方案：
1. SearchTerm evidence_snapshot 新增“Parent ASIN入口”和“广告 ASIN承接”。
2. ReviewRecord 保存前强制校验这两个标签，缺失时拒绝保存。
3. 复盘待办审计和修复预检新增 has_parent_asin_scope、has_ad_asin_coverage。
4. 前端人工确认、预览、复盘读回统一显示为“广告搜索词表现复核链”，并识别“Parent ASIN 入口 / Parent ASIN入口”等同义标签。

后续防错规则：
1. SearchTerm 人工确认快照必须能回答：从哪个 Parent ASIN 入口进入、哪些广告 ASIN 承接、在哪些广告组和投放词下产生搜索词表现。
2. 语义组只允许作为搜索词表现聚合维度，不允许作为人工动作对象。
3. 历史旧待办缺 Parent ASIN 入口或广告 ASIN 承接时，只能提示修复或重新留痕，不能标记为完整 ready。

验证方式：
cd backend; python -m pytest tests/test_manual_action_preflight_cli.py tests/test_manual_action_apply_cli.py tests/test_api_routes.py tests/test_review_evidence_repair_cli.py
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run test:signal-ui-runtime
cd frontend; npm run test:workbench-structure
cd frontend; npm run build
```

## 2026-06-23：SearchTerm 诊断合同不能继续用内部分类当用户主语

```text
问题：
后端 diagnosis_contract 的 section_id=search_term_opportunity 属于内部信号分类，标题可能是“搜索词机会”。
但页面专用复核链已经表达为“广告搜索词表现复核”，如果通用诊断合同区仍显示“搜索词机会”，用户会以为这是另一个对象或另一个入口。

原因：
1. 内部分类用于规则和候选排序，用户可见主语用于运营理解，两者不是同一层。
2. 早期直接展示后端 title，导致通用诊断合同区和专用 SearchTerm 复核链主语不一致。
3. 用户已经多次纠偏“语义组复核”的本质是 Parent ASIN 下广告用户搜索词表现分析，不是抽象语义或机会标签。

解决方案：
1. 前端 signalTriageDiagnosisContractItems 增加展示层标题映射。
2. section_id=search_term_opportunity 或 title=搜索词机会 时，用户可见标题统一显示为“广告搜索词表现复核”。
3. 保留后端 signal_category 和规则分类，不为文案改数据模型。

后续防错规则：
1. 内部信号分类可以用于代码和规则，但面向运营的诊断块标题必须表达业务任务。
2. SearchTerm 场景的用户可见主语统一为广告搜索词表现复核，不回退成语义组、搜索词机会或二级筛选。
3. 如果同时存在 recommended 和 next_unhandled 合同，前端必须按 selectedSignal ID 选对应合同，不能混用证据。

验证方式：
cd frontend; npm run test:signal-ui
cd frontend; npm run test:signal-ui-runtime
cd frontend; npm run test:workbench-structure
```

## 2026-06-23：搜索词聚合卡片指标不能和具体 AI 信号混成一个口径

```text
问题：
用户确认“语义组复核”的作用是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
实际运行中，Parent ASIN 广告搜索词表现复核卡片会统计同类搜索词的全部广告表现行，而点开后的具体 AI 信号只展示通过规则准入或合并后的可行动证据子集。
如果页面不说明这层关系，用户会看到卡片合计指标和信号详情指标不一致，并判断系统数据口径混乱。

原因：
1. 搜索词表现复核是分析入口，应该覆盖当前 Parent ASIN 广告上下文中的同类 SearchTerm 表现行。
2. 具体 AI 信号是处理入口，只保留满足机会、浪费、异常等规则的可行动证据。
3. 两层对象不同：前者是聚合分析层，后者是可人工复核的 SearchTerm 信号层。

解决方案：
1. SearchIntentReviewCard 和 SearchIntentPanelContext 增加 signalMetricBoundary。
2. 面板新增“与具体信号关系”，说明聚合卡片覆盖全部同类广告搜索词表现行，具体 AI 信号只展示可行动证据子集。
3. Top SearchTerm 增加“表现行 N 条”，让用户知道聚合数字来自多少条广告搜索词表现行。

后续防错规则：
1. Parent ASIN 广告搜索词表现复核的卡片指标不得被解释为某条具体 AI 信号的完整指标。
2. 具体 AI 信号的指标不得反向定义 Parent ASIN 下同类广告搜索词的全部表现范围。
3. 页面只要同时展示聚合层和信号层，就必须说明二者的数据口径关系。

验证方式：
cd frontend; npm run test:signal-ui
cd frontend; npm run test:workbench-structure
cd frontend; npm run build
```

## 2026-06-23：广告搜索词表现复核点击目标不能和运营判断脱节

```text
问题：
用户再次明确“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
当前卡片已经显示扩量复核、止损复核和观察复核，但点击时仍默认打开 top_search_terms 第一条，可能不是最符合当前运营判断的具体 SearchTerm。

原因：
1. 之前 primarySearchTerm 只是取第一条 top_search_terms，偏展示顺序，不是业务优先级。
2. 扩量、止损、观察三个判断需要不同的优先词选择逻辑。
3. 如果点击目标和运营判断脱节，用户会以为系统只是做语义组展示，而不是帮助分析广告搜索词表现。

解决方案：
1. SearchIntentReviewCard 增加 primarySearchTermReason。
2. 扩量复核优先打开有订单、订单更多且 ACOS 更低的具体 SearchTerm。
3. 止损复核优先打开无订单且花费最高的具体 SearchTerm。
4. 观察复核优先打开样本行数和消耗更有代表性的具体 SearchTerm。
5. 页面直接展示“优先打开：SearchTerm + 原因”。

后续防错规则：
1. Parent ASIN 广告搜索词表现复核卡片点击必须落到具体 SearchTerm，不得停留在语义聚合上下文。
2. 聚合卡片的运营判断、优先打开词和中间诊断详情必须一致。
3. 语义组 / 搜索意图只能作为聚合和筛选方法，不能成为人工动作对象。

验证方式：
cd frontend; npm run test:signal-ui
cd frontend; npm run test:workbench-structure
cd frontend; npm run build
```

## 2026-06-23：搜索词表现复核卡片不能只靠用户读长文判断优先级

```text
问题：
Parent ASIN 广告搜索词表现复核已经使用广告搜索词表现行聚合，但卡片顶部如果只展示语义标签、指标摘要和长段判断文本，用户仍要自己从订单、花费、ACOS 中推导这组词到底应该先扩量复核、止损复核还是观察。

原因：
1. 当前卡片已经有 current_judgement，但它是句子，不适合左侧队列快速扫读。
2. 用户真实工作路径是先快速判断处理优先级，再打开具体 SearchTerm 证据链。
3. 如果没有明确运营判断标签，搜索词表现复核容易退回普通指标列表，削弱 AI 信号分诊价值。

解决方案：
1. SearchIntentReviewCard 增加 operationDecisionLabel、operationDecisionReason、operationDecisionTone。
2. 用已有订单、花费和 ACOS 派生“扩量复核 / 止损复核 / 观察复核”。
3. 前端卡片顶部展示“运营判断”，并明确仍需打开具体 SearchTerm 核对投放词、广告组和广告位后再人工处理。

后续防错规则：
1. Parent ASIN 广告搜索词表现复核卡片必须先给出运营判断，再展示指标和证据边界。
2. 运营判断只能作为人工复核方向，不能变成自动加词、自动否词、自动调价或自动暂停。
3. 扩量、止损和观察都必须落回具体 SearchTerm 证据链，不能让聚合上下文成为人工动作对象。

验证方式：
cd frontend; npm run test:signal-ui
cd frontend; npm run test:workbench-structure
cd frontend; npm run build
```

## 2026-06-23：具体 SearchTerm 复核链不能跳过 Parent ASIN 和广告 ASIN

```text
问题：
用户点击语义组后进入具体 SearchTerm 信号详情时，如果复核链直接从投放词或广告组开始，用户仍然看不清这条搜索词属于哪个 Parent ASIN 经营盘、由哪些广告 ASIN 承接。

原因：
1. 后端 diagnosis_contract 已经包含 parent_asin_scope 和 ad_asin_coverage，但前端 SearchTermOpportunityReviewChain 没有把这两个前置节点纳入链路。
2. 搜索词复核链更关注投放词、广告组、同组商品、广告位和 ABA，容易让用户误以为 SearchTerm 可以脱离 Parent ASIN 主对象单独处理。
3. 人工确认前证据项也缺少 Parent ASIN 入口和广告 ASIN 承接，复盘回看时证据链不完整。

解决方案：
1. SearchTermOpportunityReviewChain 增加 Parent ASIN 入口和广告 ASIN 承接字段。
2. 从 diagnosis_contract 的 parent_asin_scope 和 ad_asin_coverage 生成这两个字段。
3. 中间详情和右侧人工确认前证据项同步展示这两个节点。

后续防错规则：
1. 具体 SearchTerm 详情必须按 Parent ASIN -> 广告 ASIN -> 广告组 -> 投放词 / 搜索词 / 广告位 -> ABA -> 人工动作的路径展示。
2. SearchTerm 不能脱离 Parent ASIN 经营入口和广告 ASIN 承接直接生成动作判断。
3. 人工留痕前证据项必须保留对象链前置节点，避免复盘时只剩搜索词指标。

验证方式：
cd frontend; npm run test:signal-ui
cd frontend; npm run test:workbench-structure
```

## 2026-06-23：语义组复核必须绑定 Parent ASIN 广告搜索词表现

```text
问题：
用户指出“语义组复核”的作用应该是从 Parent ASIN 的角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果页面把语义组复核表现成新的经营入口、广告组入口或动作对象，用户会误以为点击语义组会改变诊断对象。

原因：
1. 页面文案仍有“当前诊断入口”这类泛化表达，弱化了 Parent ASIN 主对象。
2. 语义组点击虽然没有改变顶部诊断入口，但视觉上容易被误读成入口筛选。
3. 后端顶层 diagnosis_contract 曾优先指向 next_unhandled_diagnosis_contract，导致推荐信号和中间诊断合同可能不是同一个对象。

解决方案：
1. 前端标题改为“Parent ASIN 搜索词表现复核”，说明这是当前 Parent ASIN 下广告用户搜索词表现的语义聚合。
2. SearchIntentSummary 业务问题统一改成“当前 Parent ASIN 广告上下文”。
3. 顶层 diagnosis_contract 改为优先跟随 recommended_diagnosis_contract，next_unhandled_diagnosis_contract 保持独立字段。

后续防错规则：
1. 语义组复核只能筛选当前 Parent ASIN 下同类 SearchTerm 信号，不得改变诊断入口。
2. 语义组不是人工动作对象，人工动作必须落到具体 SearchTerm、广告 ASIN 或其他稳定广告对象。
3. 顶层诊断合同必须与 recommended_candidate 一致；下一条待处理对象只能通过 next_unhandled_* 字段展示。

验证方式：
python -m pytest backend/tests/test_signal_triage_service.py::test_signal_triage_top_level_diagnosis_contract_tracks_recommended_candidate backend/tests/test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope -q
cd frontend; npm run test:signal-ui
cd frontend; npm run test:workbench-structure
```

## 2026-06-23：搜索词表现复核不能只展示裸指标

```text
问题：
搜索词表现复核已经按 Parent ASIN 广告上下文聚合搜索词，但卡片如果只展示花费、点击、订单、ACOS、ABA 命中，用户仍然无法判断这组词为什么值得处理、应该扩量还是止损、下一步要查哪层证据。

原因：
1. 指标展示没有说明业务问题。
2. 指标没有解释服务的判断目的。
3. 广告活动 / 广告组覆盖范围和证据缺口没有直接露出，用户难以判断能否落到具体人工动作。

解决方案：
1. SearchIntentSummary 增加业务判断合同：business_question、current_judgement、metric_purpose、ad_context、evidence_gap。
2. 后端从聚合指标、广告活动 / 广告组数量、投放词字段和 ABA 命中生成这些字段。
3. 前端搜索词表现复核卡片直接展示业务问题、当前判断、指标目的、广告上下文和证据缺口。

后续防错规则：
1. 搜索词表现复核卡片不得只展示指标，必须说明指标为什么存在。
2. “当前判断”只能给人工复核方向，不得包装成自动执行广告动作。
3. 语义聚合卡片只能引导用户打开具体 SearchTerm 信号，不能直接成为人工动作对象。

验证方式：
python -m pytest backend/tests/test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope -q
cd frontend; npm run test:signal-ui
cd frontend; npm run test:workbench-structure
```

## 2026-06-23：搜索词表现复核不能只取机会队列

```text
问题：
用户指出“语义组复核”的作用应该是从 Parent ASIN 的角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
当前实现把 /api/search-intents 限定为已进入 SearchTerm 机会队列的 source_rows，导致未触发 AI 信号但仍属于当前 Parent ASIN 广告上下文的搜索词不会展示。

原因：
1. 之前为了避免“语义组”被误读成人工动作对象，把模块收口成 SearchTerm 机会筛选。
2. 后端从 search_term_opportunity 信号反推搜索词表现，等于用结果层代替原始广告搜索词表现层。
3. 前端标题和数据口径也跟着机会队列走，弱化了 Parent ASIN 搜索词表现分析价值。

解决方案：
1. /api/search-intents 带 product_scope_id 时，改为通过 advertised_products 找到当前 Parent ASIN / ASIN 的广告上下文。
2. 聚合对应 ad_search_term_daily_metrics，而不是只聚合已进入 AI 机会信号的 source_rows。
3. 前端主标题改为“搜索词表现复核”，说明这是 Parent ASIN / 诊断入口下的广告用户搜索词表现语义聚合。
4. 点击语义聚合卡片只筛同类 SearchTerm 信号，不改变诊断入口，也不生成语义组人工动作。

后续防错规则：
1. 搜索词表现复核的输入层必须是广告搜索词表现行，不是 AI 信号结果层。
2. 机会队列只能作为可行动子集，不能反过来定义 Parent ASIN 的搜索词表现范围。
3. ABA 仍是站点级背景，不能当作店铺或 Parent ASIN 自有搜索词数据。
4. 语义聚合只是一种阅读和筛选方法，人工动作仍必须落到具体 SearchTerm 等稳定对象。

验证方式：
python -m pytest backend/tests/test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope -q
cd frontend; npm run test:signal-ui
cd frontend; npm run test:workbench-structure
cd frontend; npm run build
```

## 2026-06-23：SearchIntentSummary 默认合同不能残留语义组人工动作

```text
问题：
SearchTerm 机会筛选板块标题、边界说明和前端兜底已经收口为 SearchTerm 机会筛选，但运行态 /api/search-intents 的 does_not_prove 仍返回“不能生成语义组人工动作”。
用户会继续把语义组误读成可处理、可留痕或可复盘对象。

原因：
1. backend/app/models/signals.py 中 SearchIntentSummary 默认 does_not_prove 仍是旧文案。
2. search_intent_summaries 没有显式覆盖 does_not_prove，会使用模型默认值。
3. 前端 buildSearchIntentReviewCards 优先展示后端返回值，导致前端正确兜底无法覆盖旧后端合同。

解决方案：
1. SearchIntentSummary 默认 does_not_prove 改为“不能生成 SearchTerm 筛选上下文人工动作”。
2. 搜索意图信号里的“语义类目”用户可见文案改为“这组广告搜索词 / SearchTerm 筛选上下文”。
3. 前端 SearchTerm 机会卡增加展示层兼容，把旧文案中的“语义组人工动作 / 语义类目”转换为 SearchTerm 机会口径。
4. 后端和前端测试同时锁定不再把“语义组人工动作”展示给用户。

后续防错规则：
1. SearchTerm 机会筛选相关 API 合同、前端展示和测试必须使用同一个主语：SearchTerm 机会 / SearchTerm 筛选上下文。
2. 语义聚合只能是筛选方法，不能出现在人工动作对象、复盘对象或可保存证据主语中。
3. 如果后端历史数据必须保留 label=语义组，前端展示层必须映射为 SearchTerm 筛选上下文。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
python -m pytest backend/tests/test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope -q
python -m pytest backend/tests/test_signal_detection.py -q -k "search_intent_summary"
```

## 2026-06-23：SearchTerm 机会复核链不能漏掉广告位边界和同组商品承接

```text
问题：
beach essentials 的后端诊断合同已经包含广告组边界、同组投放商品、广告位证据缺口和 ABA 背景，但前端“搜索词机会复核链”只展示投放词证据、广告组合流判断和 ABA 背景。
用户会看到“搜索词机会”结论，却不一定同时看到为什么不能归因到单个广告 ASIN、为什么不能下广告位结论。

原因：
1. SearchTermOpportunityReviewChain 已构造 adGroupProductPerformance，但 SearchTermOpportunityReviewChainPanel 没有渲染。
2. placement_gap 诊断合同没有进入 SearchTerm 机会复核链。
3. 右侧人工确认虽然有其他证据快照逻辑，但中间诊断链和点击前人工确认卡片没有统一展示广告位边界。

解决方案：
1. SearchTermOpportunityReviewChain 增加 placementBoundary。
2. 从 placement_gap 诊断合同生成广告位边界。
3. 中间“搜索词机会复核链”新增同组投放商品表现和广告位边界。
4. 右侧人工确认证据项新增广告位边界。
5. 测试锁定展示顺序：投放词证据 -> 广告组合流判断 -> 同组投放商品表现 -> 广告位边界 -> ABA 背景。

后续防错规则：
1. SearchTerm 机会不能只讲搜索词指标，必须同步展示广告组、投放商品和广告位边界。
2. 有活动级广告位背景不等于有广告组级广告位归因，页面必须明确“不能下广告位结论”。
3. 人工确认前的中间复核链和右侧证据项必须保持一致，避免用户点击前后看到的证据层级不一致。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：语义组复核不能把分析标签当成业务对象

```text
问题：
用户明确指出“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果页面继续把“语义组 / 广告搜索词聚合上下文”作为用户主语，用户会误以为这是一个新的诊断入口或人工动作对象。

原因：
1. 语义标签只是对 ad_search_term_daily_metrics 用户搜索词表现行的分析分组。
2. Parent ASIN 才是经营入口，SearchTerm 才是具体诊断和人工复盘对象。
3. “聚合上下文人工动作”这类文案会暗示系统可以对聚合标签执行广告动作，偏离 AI Copilot 工作台边界。

解决：
1. 前端展示层把“语义组 / 广告搜索词聚合上下文”统一显示为“Parent ASIN 搜索词表现聚合”。
2. SearchIntent 聚合卡、选中关系提示、人工确认核对和复盘读回统一强调 Parent ASIN 搜索词表现口径。
3. 后端默认文案和复盘重复上下文说明改成 Parent ASIN 搜索词表现聚合。
4. 保留历史标签兼容，避免旧 evidence_snapshot 读不回来。

防错规则：
1. 语义标签只能用于聚合同类 SearchTerm，不能成为经营入口、广告组入口或人工动作对象。
2. 用户可见主语优先使用“Parent ASIN 广告搜索词表现复核 / Parent ASIN 搜索词表现聚合”。
3. 人工动作必须落到后端 preflight 确认的稳定对象，不能写入语义组动作。

验证方式：
npm run test:signal-ui
npm run test:review-ui
npm run test:workbench-structure
npm run build
python -m pytest tests\test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope tests\test_manual_actions.py::test_review_todos_extract_search_intent_and_aba_context tests\test_manual_actions.py::test_review_todos_infer_search_intent_for_legacy_search_term_action -q
```

## 2026-06-23：旧 SearchTerm 复盘测试夹具缺 Parent ASIN 入口

```text
问题：
运行整份 backend/tests/test_manual_actions.py 时，7 个 SearchTerm ReviewRecord 相关用例失败，错误集中为 review_record_missing_parent_asin_scope。

原因：
1. 当前服务层已经要求 SearchTerm 复盘证据快照保留 Parent ASIN 入口。
2. 部分旧测试夹具 diagnosis_evidence_snapshot() 仍是旧版搜索词证据链，没有补 Parent ASIN 入口和广告 ASIN 承接。
3. 这些失败不是本轮 Parent ASIN 搜索词表现聚合文案改动导致，但会影响后续全量后端测试稳定性。

解决：
本轮只记录风险，不顺手大改旧测试夹具。
下一轮应单独修复 diagnosis_evidence_snapshot() 或相关 SearchTerm 夹具，让服务层门禁顺序和预期错误重新对齐。

防错规则：
1. SearchTerm 复盘夹具必须包含 Parent ASIN 入口、广告 ASIN 承接、广告组合流判断、同组投放商品、投放词、搜索词边界、广告位、ABA、证据缺口、需要补证和动作边界。
2. 修改复盘门禁时必须同步更新旧测试夹具，否则全量测试会被旧证据链阻断。
3. 发现全量测试失败时，要区分本轮影响范围和既有夹具债务，不把无关问题混进当前小任务。

验证事实：
python -m pytest tests\test_manual_actions.py -q（backend 目录）：7 failed, 43 passed。
定向相关用例已通过。
```

## 2026-06-23：旧 SearchTerm 复盘测试夹具 Parent ASIN 入口缺口已修复

```text
问题：
上一轮记录的 backend/tests/test_manual_actions.py 全量失败，根因是通用 diagnosis_evidence_snapshot() 没有 Parent ASIN入口 和 广告 ASIN承接。

原因：
1. SearchTerm ReviewRecord 服务层已经升级为完整对象链门禁。
2. 旧夹具仍停留在搜索词边界、广告位边界和投放词证据层，导致还没验证目标错误，就先被 review_record_missing_parent_asin_scope 阻断。
3. 单个测试局部手工拼接 Parent ASIN 入口，说明合同已经存在，但通用夹具没有同步。

解决：
1. diagnosis_evidence_snapshot() 默认包含 Parent ASIN入口。
2. diagnosis_evidence_snapshot() 默认包含 广告 ASIN承接。
3. 局部测试改为直接复用完整快照，避免重复拼接同类证据。

防错规则：
1. 通用 SearchTerm 复盘夹具必须跟服务层证据合同同步。
2. 不要为了让旧测试通过而放宽 ReviewRecord 门禁。
3. 如果某个测试要验证缺失标签，必须通过 diagnosis_without_label_snapshot(label) 明确移除目标标签。

验证方式：
python -m pytest tests\test_manual_actions.py -q
python -m pytest tests\test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope -q
```

## 2026-06-23：SearchTerm 聚合点击不能被误读成诊断入口变化

```text
问题：
用户点击 SearchTerm 机会筛选卡片后，左侧队列和中间详情会切到对应 SearchTerm 信号，容易误以为“诊断入口筛选器的值变了”或系统进入了语义组对象。

原因：
1. 真实交互是 selectedSearchIntentLabel / selectedSearchIntentScopeId 二级筛选，不是 selectedProductScopeId 变化。
2. 视觉上队列和中间详情同步变化，如果页面不直接说明点击后影响范围，用户会把筛选结果变化理解成入口变化。
3. “语义组”作为证据标签仍存在于后端证据快照里，前端主语如果不够明确，会放大误读。

解决方案：
1. SearchTerm 机会筛选板块增加“点击后”说明。
2. 固定说明点击只改变左侧信号队列筛选和中间选中 SearchTerm，不改变顶部诊断入口筛选器。
3. 测试锁定 interactionBoundary 和板块渲染字段。

后续防错规则：
1. 辅助筛选维度的点击行为必须说明影响范围。
2. 任何 SearchTerm 聚合点击都不能调用 setSelectedProductScopeId。
3. 如果需要人工动作，必须进入具体 SearchTerm 信号，并以后端 preflight 的稳定对象为准。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：本地 8000 端口不能默认视为当前项目后端

```text
问题：
本项目和旧项目都可能使用 localhost:8000 或相邻端口。本地浏览器即使打开了正确的前端，也可能因为 API_BASE 默认值或旧进程残留，连到错误后端，导致信号数量、数据口径和证据链被污染。

原因：
1. 原 /health 只返回 status=ok，无法识别当前端口属于哪个项目。
2. 前端默认 API_BASE 是 http://127.0.0.1:8000，但端口占用时没有项目身份校验。
3. 手工启动时容易混入旧 uvicorn / Vite 进程，页面看起来正常，实际接口来源不可信。

解决方案：
1. /health 增加 service=ai-ads-signal-workbench。
2. scripts/start_local_dev.ps1 启动后校验 /health.service 和 /openapi.json title。
3. 脚本启动前不杀未知进程，端口被占用时自动换到备用端口。
4. 前端启动时显式注入 VITE_API_BASE_URL，绑定本轮启动出来的后端。
5. 写入 tmp/local-dev/last-start.json，记录本轮 frontend_url、api_base、PID 和日志路径。

后续防错规则：
1. 判断页面数据前先确认 /health.service 是否为 ai-ads-signal-workbench。
2. 如果 8000 被占用，不要直接杀进程；先识别进程归属，必要时让脚本自动避让。
3. 本地启动结果以 tmp/local-dev/last-start.json 为准，不凭浏览器地址栏猜后端来源。
4. Windows PowerShell 启动脚本保持 ASCII 内容，避免无 BOM UTF-8 中文字符串导致解析异常。

验证方式：
PowerShell AST 解析 scripts/start_local_dev.ps1：FULL_PARSE_OK
python -m pytest tests/test_api_routes.py::test_health_route_identifies_current_project tests/test_api_routes.py::test_cors_allows_local_vite_fallback_port -q
Invoke-RestMethod http://127.0.0.1:8000/health
读取 tmp/local-dev/last-start.json
```

## 2026-06-23：SearchTerm 机会筛选命名不能再回退成语义组主语

```text
问题：
用户追问“语义组复核板块的作用是什么、数据口径是什么”，说明只要页面或历史记录把主语放在“语义组 / 语义聚焦”上，运营就会自然把它误读成诊断入口、广告处理对象或复盘对象。
当前源码已经使用“SearchTerm 机会筛选（语义聚合）”，但历史记录里存在把标题改回“搜索词语义聚焦”的决策残留，后续开发有回退风险。

原因：
1. 语义聚合只是 SearchTerm 机会队列的筛选方法，不是稳定业务对象。
2. 该接口在带 product_scope_id 时，数据来自当前诊断入口内已进入 SearchTerm 机会队列的广告搜索词表现行，不是 Parent ASIN 下全部搜索词画像。
3. 如果标题或按钮把“语义组”放成主语，用户会误以为点击后会改变诊断入口或生成语义组人工动作。

解决方案：
1. 保持主标题为“SearchTerm 机会筛选”，aria-label 为“SearchTerm 机会筛选（语义聚合）”。
2. 语义聚合只作为方法说明，不能作为模块主对象、人工动作对象或复盘对象。
3. 结构测试继续锁定不出现“搜索词语义聚焦”“聚焦语义组”“搜索词机会二级筛选”，并锁定点击语义卡片不能 setSelectedProductScopeId。
4. 如果运行页面仍显示旧主语，优先排查本地 dev server、浏览器缓存或旧构建产物，而不是继续改业务模型。

后续防错规则：
1. 辅助聚合维度不能抢业务对象主语；标题先写可复核对象，再写聚合方法。
2. SearchTerm 机会筛选只能缩小当前诊断入口内的 SearchTerm 信号队列，不能改变 ProductScope。
3. 人工动作写入必须以后端 preflight 确认的稳定对象和 evidence_snapshot_preview 为准，不能写入语义组。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run build
```

## 2026-06-24：复盘待办不能把缺证据链的 SearchTerm 显示成 ready

```text
问题：
用户再次明确“语义组复核”应从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
前端 ReviewTodo 读回旧逻辑只要读到 7d / 14d 待办，就把“待办读回”显示成 ready；即使证据快照缺少同组投放商品表现、逐投放上下文等关键链路，也容易被用户理解为到期后可以直接复盘。

原因：
1. “语义组 / 搜索词表现分组”只是分析聚合维度，不是人工动作对象。
2. SearchTerm 复盘需要能回看 Parent ASIN、广告 ASIN、广告组、同组商品、逐投放上下文、投放词、搜索词边界、广告位边界和 ABA 背景。
3. 旧读回逻辑把“待办已生成”和“证据链足够复盘”混成一个 ready 状态。

解决：
1. ReviewTodo 读回增加搜索词表现分组新旧标签兼容，历史“语义组”只作为搜索词表现分组来源读取。
2. SearchTerm 待办读回要求完整广告搜索词表现复核链；缺少同组投放商品表现或逐投放上下文时显示待核对。
3. 待办已生成但证据链缺口未补齐时，待办读回 tone 改为 blocked，并提示不能保存复盘结论。

防错规则：
1. 搜索词表现分组只能帮助用户从 Parent ASIN 角度聚合广告 SearchTerm 表现，不能成为人工动作对象。
2. ReviewTodo ready 不能只看 7d / 14d 是否存在，还必须看 evidence_snapshot 是否保留 SearchTerm 复核链。
3. 历史缺证据待办不能静默补写，只能 dry-run 作废后重新人工留痕生成新的 ReviewTodo。

验证方式：
npm run test:review-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：ReviewTodo 不能只验证对象继承，必须验证完整证据快照继承

```text
问题：
后端人工动作路由已有 preflight 签名比对，也会让 ReviewTodo 从 ManualAction 继承 evidence_snapshot。
但旧路由测试只验证 action_id、object_type、object_id 等身份字段，没有验证 7d / 14d ReviewTodo 是否完整继承 SearchTerm 复核链。

原因：
1. SearchTerm 复盘能否落地，不只取决于对象 ID，还取决于能否回看 Parent ASIN、广告 ASIN、广告组、同组商品、逐投放上下文、投放词、广告位和 ABA 背景。
2. 如果测试只验证对象身份，后续即使 evidence_snapshot 被截断或漏关键标签，也可能不会失败。
3. build_review_todos 只继承 ManualAction 证据，不负责补证据；写入入口必须被测试锁住。

解决：
1. 新增完整 SearchTerm 复核链测试夹具 search_term_full_review_chain_snapshot。
2. 新增路由测试，POST 人工动作后断言 ManualAction.evidence_snapshot 与 ReviewTodo.evidence_snapshot 完全等于 preflight preview。
3. 测试同时断言搜索词表现分组、广告组合流判断、同组投放商品表现、逐投放上下文、投放词证据、广告位边界、ABA 背景和动作边界都存在。
4. CLI 写入脚本 post-write smoke 改为完整签名比对，并输出 SearchTerm 关键链路标签是否齐全。

防错规则：
1. 任何人工动作写入测试不能只看对象身份，复盘类动作还必须看 evidence_snapshot 完整继承。
2. ReviewTodo 不补写历史证据；缺证据的历史待办只能治理作废后重新留痕。
3. SearchTerm 复盘链缺同组商品或逐投放上下文时，不能展示为可直接保存复盘结论。
4. CLI smoke 不能只比对证据条数或 label，必须比对 label / value / detail / source 完整签名。

验证方式：
python -m pytest tests\test_manual_actions.py::test_manual_action_route_review_todos_inherit_full_search_term_snapshot -q
python -m pytest tests\test_manual_actions.py -q
python -m pytest tests\test_manual_action_apply_cli.py -q
```

## 2026-06-23：SearchTerm 详情不能只展示合计链路

```text
问题：
中间诊断区已有“广告搜索词表现复核链”，但主要是文本总结。
当同一个 SearchTerm 跨多个广告活动、广告组或投放词出现时，只看合计链路会让用户误以为该词只有一个投放上下文。

原因：
1. SearchTerm 机会信号的 source_rows 已包含逐广告组表现行，但前端没有把这些行结构化展示。
2. ABA 行和广告搜索词表现行混在 source_rows 中，如果不明确过滤，容易把站点级市场背景误读成广告组表现。
3. 广告组是投放容器，搜索词表现必须回到广告组和投放词上下文里判断。

解决：
1. 新增 buildSearchTermAdContextRows，只读取 ad_search_term_daily_metrics。
2. 按当前 SearchTerm 过滤 source_rows，排除 ABA 行和其他搜索词行。
3. 在“广告搜索词表现复核链”下方新增“逐投放上下文复核”，展示广告活动、广告组、投放词、周期、点击、花费、订单、销售额、ACOS、CVR 和边界。

防错规则：
1. SearchTerm 详情必须能看到广告活动、广告组和投放词上下文。
2. ABA 只能作为站点级市场背景，不能被渲染成广告组表现行。
3. 多广告组同词表现不能被合计值覆盖，必须保留逐上下文差异。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：广告搜索词聚合卡片点击不能只停留在语义组筛选

```text
问题：
左侧 Parent ASIN 广告搜索词表现复核卡片点击后，如果只按语义组 / intentLabel 筛选，再选中同组第一条信号，就可能出现用户看到的是 baby sunglasses，但中间诊断打开的是同组另一个 SearchTerm。

原因：
1. 语义组只是组织广告用户搜索词的分析维度，不是最终可复核对象。
2. 原点击逻辑没有保留卡片主搜索词，只知道 intentLabel。
3. 同一 Parent ASIN 下同一语义组可能包含多个 SearchTerm，列表顺序不能代表用户点击意图。

解决：
1. SearchIntentReviewCard 增加 primarySearchTerm。
2. buildSearchIntentReviewCards 从 top_search_terms 或 search_terms 提取优先打开的具体搜索词。
3. handleSelectSearchIntent 使用 selectSearchIntentSignalId，在当前诊断入口内优先选中具体 SearchTerm 信号，找不到时才回退同组第一条。

防错规则：
1. “语义组”只能作为 Parent ASIN 广告搜索词表现的聚合维度，不能作为人工动作或复盘对象。
2. 卡片展示了某个 Top SearchTerm，点击时就必须优先打开该 SearchTerm 的诊断。
3. 顶部诊断入口不能因为点击聚合卡片而切换 Parent ASIN / 广告 ASIN / 广告组。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：不可行动 product_scope 不能合并后端候选信号

```text
问题：
后端 product_scope_gate 已经能把 sales_asin 标记为 requires_product_scope / is_actionable=false。
但前端 mergeBackendTriageSignals 原先只要 summary 中出现 recommended_candidate、manual_action_preview 或 next_unhandled_candidate 的 signal_id，就会把该信号合并进当前队列。
如果后端摘要或旧服务进程残留候选，sales_asin 背景入口仍可能显示广告候选，破坏对象边界。

原因：
1. 右侧候选 helper 已用 summaryRequiresProductScope 阻断，但左侧队列合并 helper 没有使用同一门禁。
2. mergeBackendTriageSignals 的职责是补齐后端推荐信号，但没有先判断当前 product_scope 是否允许推荐候选进入队列。
3. sales_asin 是销售背景，不是广告下钻入口；队列层混入广告候选会让用户误以为可以处理广告动作。

解决方案：
1. mergeBackendTriageSignals 在 product_scope_gate.is_actionable=false 时直接返回当前 scope 内信号。
2. 增加 sales_asin 运行态测试：当前 scope 只有销售背景信号，即使 summary 残留广告 recommended_candidate，也不能合并广告候选。

后续防错规则：
1. 所有后端推荐候选、下一个候选、人工动作预览在进入前端队列或右侧操作前，都必须先看 product_scope_gate.is_actionable。
2. sales_asin、all、unattributed 只能做只读排查或背景说明，不能通过候选合并绕回广告动作路径。
3. 如果后端旧进程或摘要残留候选，前端也必须保留 product_scope 边界，不得用候选 ID 覆盖当前入口。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
python -m pytest backend/tests/test_signal_triage_service.py -q -k "sales_asin or product_scope"
```

## 2026-06-23：诊断入口摘要不能把 sales_asin 算成广告可下钻 ASIN

```text
问题：
顶部诊断入口的首屏引导原先把 advertised_asin 和 sales_asin 一起统计成“ASIN 可下钻 / ASIN 可观察”。
同时 sales_asin 的选择摘要复用了 advertised_asin 的“广告 ASIN 范围已同步”文案，容易让用户把销售背景 ASIN 当成广告诊断对象。

原因：
1. buildProductScopeEntryGuidance 用同一个 asinScopeCount 统计 advertised_asin 和 sales_asin。
2. buildProductScopeSelectionSummary 把 advertised_asin 和 sales_asin 放在同一分支。
3. 旧文案使用“经营商品优先入口”，没有把经营入口、广告下钻入口和销售背景入口拆开。

解决方案：
1. 诊断入口引导拆分 Parent ASIN 经营入口、广告 ASIN 可下钻、销售背景 ASIN 仅辅助。
2. sales_asin 单独显示为“销售 ASIN 背景入口”，只说明销售表现和广告覆盖缺口。
3. advertised_asin 才显示“广告 ASIN 范围已同步”和广告商品指标。
4. 页面 aria-label 从“经营商品优先入口说明”改为“诊断入口路径说明”。

后续防错规则：
1. sales_asin 只能作为销售背景、经营盘和覆盖缺口，不能被包装成广告下钻入口。
2. 任何“可下钻 / 可观察 ASIN”摘要必须说明是否来自广告商品数据。
3. 只有 advertised_asin 能直接进入广告 ASIN 下钻；没有广告投放行的销售 ASIN 不生成广告动作对象。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
python -m pytest backend/tests/test_signal_triage_service.py -q -k "search_intent_summaries_respect_parent_asin_product_scope or product_scope"
```

## 2026-06-23：规则反馈分组和 ABA 参考词仍容易被误读成处理对象

```text
问题：
规则反馈区和复盘候选区已经说明“不自动改规则”，但用户可见文案仍使用“规则反馈分组（搜索词语义）”“规则反馈分组（ABA参考词）”“语义组 N 条”“ABA参考 N 条”“证据分组”。
这些表达容易让运营误以为 search_intent、ABA reference 或 evidence group 是广告处理对象。

原因：
1. review_feedback.candidate_groups 的 group_type 本来只是样本归类字段，但前端直接翻译成“分组”。
2. manual_action_context 统计的是证据快照覆盖情况，却用“语义组 / ABA参考”作为裸指标名。
3. ReviewTodo repeat_summary 和 review_context 把 ABA reference 写成“参考词”，没有强调它是站点级市场背景。
4. 只读诊断脚本和 AI 信号类型矩阵也保留旧口径，容易让后续开发继续沿用。

解决方案：
1. 规则反馈候选统一改为“规则反馈样本上下文（SearchTerm 筛选 / ABA 站点级参考）”。
2. 复盘输入证据统计统一改为“SearchTerm 筛选上下文 / ABA 站点级参考”。
3. 证据追溯从“证据分组”改为“证据上下文”。
4. ReviewTodo 复盘上下文里的 ABA 表达改为“ABA 站点级参考”。
5. AI 信号矩阵和只读诊断脚本同步更新，避免规则定义与实现脱节。

后续防错规则：
1. 任何规则反馈样本池都只能说明“样本归类和人工复核优先级”，不能输出广告动作建议。
2. ABA reference 必须带站点级边界，不能写成店铺、广告组、商品或搜索词归因证据。
3. 复盘样本上下文不能替代稳定 object_type / object_id，人工动作和复盘保存仍必须落到稳定业务对象。

验证方式：
npm run test:review-ui
npm run test:signal-ui
npm run test:workbench-structure
npm run build
python -m pytest tests\test_signal_triage_service.py -q -k "review_readiness"
python -m pytest tests\test_manual_actions.py -q
python -m pytest tests\test_api_routes.py::test_search_term_manual_action_api_readback_preserves_aba_review_context -q
python -m pytest tests\test_signal_detection.py -q -k "grouped_search_term_opportunity"
python -m py_compile app\services\signal_triage.py app\services\manual_actions.py app\services\signal_detection.py
```

## 2026-06-23：搜索词语义筛选缺少诊断入口绑定

```text
问题：
搜索词机会二级筛选虽然已经说明“不改变诊断入口”，但前端状态只保存 selectedSearchIntentLabel，没有同时保存它属于哪个 product_scope。
用户切换或感知诊断入口变化时，旧语义组可能继续影响当前队列展示，容易被误读成点击语义组改写了诊断入口。

原因：
1. 语义筛选是当前入口内 SearchTerm 机会队列的二级筛选，但状态没有绑定 activeProductScopeId。
2. 页面展示、队列过滤、中间承接和右侧语义上下文核对都直接读取 selectedSearchIntentLabel。
3. 如果语义焦点跨入口残留，会把局部搜索词上下文带到新的经营入口里。

解决方案：
1. 新增 selectedSearchIntentScopeId，点击语义卡片时记录当前 activeProductScopeId。
2. 新增 activeSearchIntentLabel，只有绑定入口等于当前入口时才允许过滤队列和展示语义焦点。
3. 当前诊断入口变化时自动清空旧语义焦点。
4. 已聚焦提示展示“绑定诊断入口”，并用结构测试锁定点击语义卡片不能 setSelectedProductScopeId。

后续防错规则：
1. 下游辅助筛选必须绑定上游诊断入口，不能做隐式全局状态。
2. 任何二级筛选都只能缩小当前入口内的数据，不能反向改写 ProductScope。
3. 页面只要出现“已聚焦 / 已筛选”，必须说明它绑定的是哪个业务入口，以及不能证明什么。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run build
```

## 2026-06-23：语义聚合板块主标题仍容易被读成语义组复核

```text
问题：
左侧语义相关板块虽然已经说明“不改变诊断入口”和“不生成语义组人工动作”，但主标题仍写成“搜索词语义聚焦”。
用户会自然追问“语义组复核板块的作用是什么”，说明页面主语仍把注意力带到语义组，而不是 SearchTerm 机会。

原因：
1. 该板块的数据来自当前诊断入口内的 SearchTerm 机会队列，不是独立语义组对象。
2. “语义聚焦”强调了聚合方式，却没有在标题层说明业务对象和用途。
3. 按钮 aria-label 使用“聚焦语义组”，进一步强化了语义组像可点击处理对象的误读。

解决方案：
1. 主标题改为“SearchTerm 机会筛选”。
2. aria-label 改为“SearchTerm 机会筛选（语义聚合）”，把语义聚合作为方法而不是主对象。
3. 已筛选状态、空态和按钮 aria-label 统一使用“筛选 SearchTerm 机会”。
4. 中间承接标题改为“SearchTerm 机会筛选承接”，继续说明右侧人工动作必须落到具体 SearchTerm 稳定对象。

后续防错规则：
1. 辅助聚合维度不能作为模块主对象命名，主标题必须优先说明可复核的业务对象。
2. 页面可以展示“语义组”作为上下文字段，但按钮、标题和空态不得把它包装成广告处理对象。
3. SearchTerm 机会筛选只能缩小当前入口内的 SearchTerm 队列，不能改写诊断入口，不能生成语义组人工动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：SearchTerm 机会筛选口径不能只藏在卡片里

```text
问题：
SearchTerm 机会筛选虽然已经在单张卡片里显示用途、数据口径、能证明、不能证明和人工下一步，但板块本身没有固定口径摘要。
当当前入口没有 SearchTerm 机会聚合时，整个板块可能不出现；用户会继续疑惑这个板块的作用是什么、数据口径是什么，甚至误以为它是经营商品入口、广告组入口或可写人工动作对象。

原因：
1. 卡片级说明只有在 searchIntentReviewCards.length > 0 时才可见。
2. SearchTerm 机会筛选的业务边界是板块级事实，不应该依赖用户点开某个语义聚合卡片才理解。
3. 无聚合空态也有业务含义：它只代表当前诊断入口下没有已进入 SearchTerm 机会队列的广告搜索词行，不代表 Parent ASIN 没有搜索词。

解决：
1. 新增 buildSearchIntentPanelContext，集中返回板块级用途、数据口径、能证明、不能证明、人工下一步、边界和空态解释。
2. SignalTriageWorkbench 固定渲染 SearchTerm 机会筛选板块。
3. 有聚合时显示筛选卡，无聚合时显示“暂无 SearchTerm 机会聚合”及证据不足解释。
4. 前端结构测试锁定 SearchTerm 机会筛选数据口径区和空态必须存在。

防错规则：
1. SearchTerm 机会筛选的板块级口径必须可见，不能只藏在具体卡片里。
2. 无聚合时必须说明是当前诊断入口下没有进入 SearchTerm 机会队列，不得暗示 Parent ASIN 没有搜索词。
3. 语义聚合只能作为 SearchTerm 机会的筛选方法，不能包装成经营商品入口、广告组入口或人工动作对象。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：中间证据和右侧处理依据仍残留语义组主语

```text
问题：
左侧主标题已收口为 SearchTerm 机会筛选，但左侧卡片边界、中间触发证据、商品下钻、完整证据、右侧上下文核对和不可保存预检仍可能展示“语义组”作为主语。
用户会把“语义组”误读成可处理、可留痕或可复盘对象。

原因：
1. 底层 evidence_snapshot 和信号事实历史上使用 label=语义组，需要兼容读回。
2. 前端展示层直接输出 fact.label 或临时上下文 label，没有做用户可读对象映射。
3. 后端建议文案仍使用“先按语义组复核”，会进入中间或右侧建议区域。
4. 左侧 SearchTerm 机会卡片和选中筛选上下文说明仍有“不生成语义组人工动作”等旧表达。

解决方案：
1. 中间证据展示增加 evidenceFactDisplayLabel，将历史 label=语义组 显示为 SearchTerm 筛选上下文。
2. 右侧上下文核对、不可保存预检和临时人工动作上下文统一改为 SearchTerm 筛选上下文。
3. 后端搜索词机会建议文案改为“先按 SearchTerm 筛选上下文复核同类搜索词表现”。
4. ABA 短语背景改为“同类 SearchTerm 市场热度背景”，不再说“同语义组”。
5. 左侧卡片边界和筛选上下文说明统一改为 SearchTerm 筛选上下文，不再生成“语义组人工动作”的表达。

后续防错规则：
1. 底层历史字段可以保留 label=语义组，但用户可见主语必须映射成 SearchTerm 筛选上下文。
2. 任何处理依据、人工动作、复盘上下文都不能把语义组、ABA 参考词或规则反馈分组包装成广告处理对象。
3. 新增搜索词机会或规则反馈文案时，建议动作必须落到具体 SearchTerm 或稳定复盘对象。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui
npm run build
python -m pytest tests\test_signal_detection.py -q -k "low_spend_high_conversion or long_tail"
python -m pytest tests\test_api_routes.py::test_search_term_manual_action_api_readback_preserves_aba_review_context -q
python -m py_compile app\services\signal_detection.py
```

## 2026-06-23：广告覆盖和广告组数量摘要被误读成归因充分

```text
问题：
广告覆盖原先写成“X/Y 个子 ASIN 有广告投放行”，广告组问题定位顶部只展示“广告 ASIN N 个 / 搜索词 N 条 / 广告位 N 条”。
用户容易把这些数量误读成广告证据已经充分，甚至误以为搜索词、广告位或广告组表现可以直接归因到单个广告 ASIN。

原因：
1. 广告覆盖数量来自当前 SP advertised_products / 广告商品表现快照，不代表全部广告来源或完整子商品事实。
2. 广告组数量上下文只说明同广告组或同活动下有多少搜索词、广告位和广告 ASIN，不代表归因链路成立。
3. 边界说明原先在后续证据合流里才出现，用户第一眼先看到裸数量。

解决方案：
1. 广告覆盖文案改为“当前 SP 广告投放行 / 当前 SP 广告覆盖率”。
2. 广告组问题定位新增 trafficContextBoundary，紧跟数量摘要展示。
3. 测试锁定数量摘要必须说明“不能证明归因到单个广告 ASIN，也不能替代人工动作门禁”。

后续防错规则：
1. 数量型上下文必须同时展示数据来源和不能证明的边界。
2. 广告组内搜索词数、广告位数、广告 ASIN 数只能作为排查入口，不能作为归因结论。
3. 广告覆盖率只能说明当前 SP 广告覆盖，不代表 Parent ASIN 下全部广告来源或完整经营商品事实。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：搜索词语义聚合被误读成全量搜索词画像

```text
问题：
搜索词机会二级筛选卡片原先主要展示订单、花费、ACOS 和 ABA 命中。
虽然已经说明“不改变诊断入口”，但没有直接告诉用户它的数据来自当前机会队列，而不是 Parent ASIN 下全部广告搜索词表现。

原因：
1. /api/search-intents 在带 product_scope_id 时，会先过滤当前诊断入口内的 search_term_opportunity 信号，再从 source_rows 抽取广告搜索词行。
2. 前端卡片没有展示数据粒度、能证明什么、不能证明什么和人工下一步。
3. insight 仍使用“语义类目”作为主语，容易让用户把语义组当成可处理对象。

解决方案：
1. SearchIntentSummary 新增 data_grain、proves、does_not_prove、next_manual_step。
2. 带 product_scope_id 时，data_grain 明确为“当前诊断入口内已进入 SearchTerm 机会队列的广告搜索词表现行”。
3. 前端卡片展示数据口径、能证明、不能证明和人工下一步。
4. insight 主语改成“这组广告搜索词”，避免继续把语义类目当动作对象。

后续防错规则：
1. 局部候选集合不能包装成全量业务画像。
2. 任何聚合卡片必须说明数据粒度和不能证明的边界。
3. 语义组只能用于筛 SearchTerm 机会，不能作为人工动作对象或 Parent ASIN 全量结论。

验证方式：
python -m pytest tests\test_signal_triage_service.py -q -k "search_intent_summaries_respect_parent_asin_product_scope"
npm run test:signal-ui
npm run build
```

## 2026-06-23：candidate_count 被误读成可写人工候选

```text
问题：
首屏摘要原先只要 candidate_count 大于 0，就容易输出“可写人工候选 / 候选可人工复核”。
这会让用户误以为 AI 候选已经通过人工动作写入门禁，可以直接进入广告处理动作。

原因：
1. candidate_count 只来自 signal_status，表达的是 AI 识别到的候选数量。
2. 是否能写人工动作由 actionability_status.can_write_manual_action 决定。
3. 前端展示 helper 混用了“候选存在”和“可写动作”两个口径。

解决方案：
1. 首屏摘要改为同时读取 candidate_count 和 can_write_manual_action。
2. candidate_count > 0 但 can_write_manual_action=false 时，显示“候选未通过人工写入门禁”。
3. AI 准入卡片显示“只能诊断不能写动作”，不再输出“候选可人工复核”。
4. 测试补充候选存在但不可写动作的场景，锁定不能出现误导文案。

后续防错规则：
1. 数量型字段只能说明规模，不能直接升级为动作结论。
2. 可写人工动作必须以 actionability_status.can_write_manual_action 为准。
3. 页面只要出现“可处理 / 可复核 / 可写入”，必须能追到明确准入门禁字段。

验证方式：
npm run test:signal-ui
npm run build
```

## 2026-06-23：复盘反馈语义分组被误读成广告处理对象

```text
问题：
review_feedback.candidate_groups 和 review_context 原先会输出“复核该语义组”“同一语义组已有 N 次人工留痕”“语义组：xxx”。
用户容易把这些内容理解成可以直接处理的广告对象，进而误以为系统能对“语义组”自动改规则或执行广告动作。

原因：
1. 后端把 search_intent / ABA reference 作为样本归类时，展示主语仍是“该语义组 / 该 ABA 参考词”。
2. 前端规则反馈样本池直接把 group_type 翻译成“语义组 / ABA参考词”，没有说明它只是规则反馈分组。
3. 复盘上下文直接展示原始 evidence_snapshot 标签“语义组”，没有转换成运营可读的“搜索词语义上下文”。

解决方案：
1. 后端 review_feedback.candidate_groups 改为“规则反馈分组（搜索词语义 / ABA参考词）”。
2. candidate group boundary 明确“只用于规则反馈样本归类和人工复核优先级，不是广告处理对象”。
3. review_context repeat_summary 改为“同一搜索词语义分组”，强调复核的是规则反馈口径。
4. 前端 reviewUi 对 evidence_snapshot 的“语义组”做展示层映射，显示为“搜索词语义上下文”。

后续防错规则：
1. 语义组和 ABA 参考词只能作为搜索词上下文、ABA 背景或规则反馈样本归类，不能作为广告处理对象。
2. 任何规则反馈候选都必须同时展示“不自动改规则、不自动执行广告动作、不是广告处理对象”。
3. 人工动作对象必须继续落到稳定对象：SearchTerm、AdvertisedProduct、Placement、AdGroup 或明确的销售 / 广告商品对象。

验证方式：
python -m pytest tests\test_signal_triage_service.py -q -k "review_readiness"
python -m pytest tests\test_manual_actions.py tests\test_api_routes.py -q -k "review_context or review_todos"
npm run test:signal-ui
npm run test:review-ui
```

## 2026-06-23：搜索词语义聚焦被误读成独立业务对象

```text
问题：
左侧“搜索词语义聚焦”虽然文案已经说明不改变诊断入口，但它原先展示在队列筛选之前，视觉上像一个独立主板块。
用户容易把语义组理解成可处理对象，进而误以为可以直接对“语义组”做广告动作或复盘结论。

原因：
1. 语义聚合本质是 SearchTerm 机会的辅助筛选，但页面层级高于队列筛选。
2. 空状态仍使用“语义组聚焦”表述，容易把语义组说成业务对象。
3. helper boundary 没有直接写出“不生成语义组人工动作”。

解决方案：
1. 将该模块移动到队列筛选之后、信号队列之前。
2. 标题改为“搜索词机会二级筛选”，强调只缩小 SearchTerm 机会队列。
3. 文案补充“不生成语义组人工动作”，空状态改成“搜索词语义筛选”。
4. 结构测试锁定该模块必须跟在队列筛选之后，不能回到独立主入口位置。

后续防错规则：
1. 语义组只能作为 SearchTerm 聚合上下文、规则反馈分组或 ABA 背景解释，不能作为广告处理对象。
2. 所有可点击的筛选型模块必须说明是否改变诊断入口；如果不改变入口，视觉层级不能高于主队列筛选。
3. 人工动作对象必须仍是稳定业务对象：SearchTerm、AdvertisedProduct、Placement 或其他明确对象，不能写成语义组。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：中间诊断看到同组投放商品表现但人工复盘留痕缺失

```text
问题：
广告组诊断已展示同组广告 ASIN 的花费、点击、订单、销售额、ACOS、CVR，但右侧 manual_action preflight 和 ReviewTodo / ReviewRecord 门禁原先没有强制保存“同组投放商品表现”。
这会导致运营复盘时看不到当时是否已回看同组投放商品承接差异。

原因：
1. 中间诊断新增了 ad_group_diagnosis.advertised_product_performance，但人工动作证据快照仍停留在广告组合流判断、搜索词边界、广告位边界等旧标签。
2. ReviewRecord 保存前门禁和前端 checklist 没有把同组广告商品表现作为必需证据。
3. 历史 22 条 evidence_snapshot 不能静默补写，否则会伪造当时点击证据。

解决方案：
1. preflight evidence_snapshot_preview 新增“同组投放商品表现”，来源为 advertised_products + ad_product_daily_metrics。
2. SearchTerm / AdvertisedProduct 的 ReviewRecord 后端门禁、API 预检、前端 checklist 都新增同组投放商品表现要求。
3. review readiness、repair 工具和前端队列摘要识别 missing_ad_group_product_performance，并提示 dry-run 作废旧待办后重新人工留痕。
4. 前端对象引用门禁改为优先使用稳定 object_id，避免“广告商品”等泛标签误放行。

后续防错规则：
1. 中间诊断用于人工决策的关键证据，必须同步进入 manual_action evidence_snapshot_preview 和 ReviewRecord 保存前门禁。
2. 历史证据快照缺口只能治理或重新留痕，不能静默补写历史 evidence_snapshot。
3. 搜索词仍不能自动归因到单个广告 ASIN；同组投放商品表现只说明同广告组承接差异。

验证方式：
python -m pytest tests\test_manual_action_preflight_cli.py tests\test_manual_actions.py tests\test_manual_action_apply_cli.py tests\test_api_routes.py tests\test_review_evidence_repair_cli.py tests\test_review_readiness_cli.py tests\test_review_todo_void_cli.py -q
npm run test:review-ui
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --action-type add_to_review --compact
```

## 2026-06-22：广告组诊断不能只展示广告 ASIN 数量和列表

```text
问题：
Parent ASIN 下钻里的 ad_group_diagnosis 已经能显示广告组、广告 ASIN 数量、搜索词和广告位上下文。
但如果只展示广告 ASIN 数量或 ASIN 列表，运营仍然看不出同一广告组内每个投放商品的花费、订单、销售额、ACOS、CVR 差异，无法判断“为什么要拆开看”。

原因：
1. 后端 ad_group_structure 信号已有“广告商品清单”和“商品表现差异”，但 product_scope_drilldown.ad_group_diagnosis 没有返回结构化投放商品表现。
2. 前端广告组面板已有归属判断、问题落点和证据合流，但缺少商品级事实区块。
3. 广告组是投放容器，如果不展示商品级表现，用户容易继续把广告组汇总指标当成单产品判断。

解决方案：
1. ad_group_diagnosis 新增 advertised_product_performance，按同广告组广告商品聚合花费、点击、订单、销售额、ACOS、CVR 和样本边界。
2. 前端 ProductScopeAdGroupDiagnosisRow 新增 advertisedProductPerformance。
3. 广告组问题定位面板新增“广告组内投放商品表现”，在归属判断前展示商品级事实。

后续防错规则：
1. 广告组结构诊断必须同时展示广告组汇总和同组广告商品表现，不能只显示数量。
2. 搜索词和广告位仍只能作为同广告组上下文，不能因为展示了商品表现就自动归因到单个广告 ASIN。
3. 如果广告组内商品样本不足，必须展示样本边界，只能作为观察提示。

验证方式：
python -m pytest backend\tests\test_signal_triage_service.py -q -k "empty_parent_scope_includes_ad_drilldown"
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-22：广告位复盘不能只保存基础边界

```text
问题：
placement 已经是可复盘对象，但如果 ReviewTodo / ReviewRecord 只保存排查路径、AI 准入、搜索词边界和广告位边界，运营在复盘时只能看到“有广告位口径”，看不到广告位表现、证据缺口、需要补证和动作边界。
这会让广告位层变成浅层指标展示，甚至被误读成可以自动调整广告位加价、自动调价或归因到单个 ASIN。

原因：
1. 原有 ReviewRecord 门禁对 search_term 和 advertised_product 有专属证据链，对 placement 只套用了基础证据标签。
2. 广告位是流量位置层级对象，天然容易被误读为可直接操作的投放开关。
3. 缺少 placement 专属测试时，前后端可能再次只校验基础边界，漏掉真正能落地的人工复盘证据。

解决方案：
1. 后端 preflight 为 placement 补齐“广告位表现 / 证据缺口 / 需要补证 / 动作边界”。
2. 后端 ReviewRecord 保存和 API 预检同步校验 placement 专属证据。
3. 前端保存前 checklist 展示 placement 复核链，缺任一项都不能保存 ReviewRecord。

后续防错规则：
1. 每个可复盘对象都必须有对象专属证据链，不能只依赖基础标签。
2. 广告位表现只能说明流量位置层级差异，不能自动归因到搜索词、广告组或广告 ASIN。
3. 广告位相关建议只能是记录观察、标记已处理、加入复盘或忽略本次，不能出现自动调整广告位加价、自动调价、自动暂停、自动加词或自动否词。

验证方式：
python -m pytest backend\tests\test_manual_actions.py backend\tests\test_manual_action_preflight_cli.py -q
npm run test:review-ui
python -m pytest backend\tests\test_api_routes.py -q
npm run build
```

## 2026-06-22：搜索词机会复核链不能漏展示广告组合流判断

```text
问题：
buildSearchTermOpportunityReviewChain 已经生成 adGroupSynthesis，用于解释搜索词、广告组、同组广告 ASIN 和广告位之间的边界。
但中间“搜索词机会复核链”原先只展示投放词证据、ABA 背景、当前判断、能证明和不能证明，没有展示广告组合流判断。
用户会看到搜索词机会指标和 ABA 背景，却看不到“搜索词不能自动归因到单个广告 ASIN、缺广告组级广告位时不能判断广告位影响”的关键解释。

原因：
1. helper 层和右侧人工确认已经承接广告组合流判断，但中间阅读区漏渲染同一字段。
2. 结构测试只锁定了搜索词复核链组件存在，没有锁定链路内部必须展示 adGroupSynthesis。
3. 搜索词机会的业务判断需要“投放词 -> 广告组合流 -> ABA 背景”连续阅读，缺任一层都会变成浅层指标展示。

解决方案：
1. 在 SearchTermOpportunityReviewChainPanel 中新增“广告组合流判断”行。
2. 展示顺序固定为“投放词证据 -> 广告组合流判断 -> ABA 背景”。
3. 结构测试锁定 <b>广告组合流判断</b>、chain.adGroupSynthesis 和展示顺序。

后续防错规则：
1. helper 生成的关键业务判断必须在用户主阅读区展示，不能只保留在右侧或证据快照里。
2. 搜索词机会链必须同时包含投放词证据、广告组合流判断、ABA 背景、证据缺口和动作边界。
3. 任何搜索词机会 UI 调整都要确认用户能读出：搜索词不能自动归因到单个广告 ASIN，广告位缺口不能被包装成广告位调整建议。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:review-ui
npm run build
```

## 2026-06-22：搜索词语义聚焦不能被读成右侧人工动作对象

```text
问题：
左侧“搜索词语义聚焦”已经说明只做当前入口内 SearchTerm 机会聚合，但中间诊断和右侧人工确认如果不继续承接这层关系，用户仍可能把“语义组”当成要留痕或复盘的对象。

原因：
1. 中间诊断区原先只解释当前入口和选中信号关系，没有单独说明当前语义聚焦如何落到单条 SearchTerm。
2. 右侧原先写“当前语义组留痕”，容易让用户误以为语义组本身是人工动作对象。
3. 前端展示证据和后端 preflight evidence_snapshot_preview 的持久化合同必须保持清楚，否则会再次混淆页面上下文和实际写入对象。

解决方案：
1. 新增 buildSearchIntentFocusContext，只有当前选中 SearchTerm 信号命中语义聚焦时才生成承接提示。
2. 中间诊断区在 SignalDiagnosis 前展示“搜索词语义聚焦承接”，说明左侧只缩小队列，中间仍诊断单条 SearchTerm，右侧人工动作必须落到这条 SearchTerm 稳定对象。
3. 右侧“当前语义组留痕”改为“搜索词语义上下文核对”。
4. 右侧文案明确实际写入以后端 preflight evidence_snapshot_preview 为准，语义组不是人工动作对象。

后续防错规则：
1. 任何语义聚焦上下文都只能作为页面核对和复盘背景，不能替代 SearchTerm / AdGroup / AdvertisedProduct 等真实动作对象。
2. 中间诊断和右侧人工确认必须同时说明筛选上下文与实际动作对象的关系。
3. 前端临时上下文不能被写成持久化证据来源；持久化仍以后端 preflight 证据快照为准。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:review-ui
npm run build
```

## 2026-06-22：语义组复核命名容易被误读成商品语义判断

```text
问题：
左侧板块原名“语义组复核”，用户容易把它理解成 Parent ASIN / 子 ASIN 的商品语义符合度判断。
但当前真实口径是当前诊断入口下的广告 SearchTerm 机会聚合，只能帮助运营聚焦同类搜索词，再进入单条搜索词证据链。

原因：
1. “语义组”没有直接说明数据来自广告搜索词表现。
2. 卡片原先主要展示订单、花费、ACOS、ABA 命中，缺少“这些指标为什么存在”和“不能证明什么”。
3. ABA 命中如果没有边界说明，容易被误读成店铺、商品或广告组归因。

解决方案：
1. 页面标题改为“搜索词语义聚焦”。
2. 标题说明固定为“当前诊断入口内的 SearchTerm 机会聚合”。
3. 卡片新增用途：聚焦当前诊断入口内同类广告搜索词，判断是否值得逐条进入人工复核。
4. 卡片新增边界：只筛 SearchTerm 机会，不改变诊断入口，不证明单个 ASIN 归因，ABA 仅作站点级背景。

后续防错规则：
1. 搜索词语义聚合必须先说明广告搜索词口径，不能包装成商品语义判断。
2. 语义聚焦只能作为当前诊断入口下的二级筛选，不允许反向改写 ProductScope。
3. ABA 命中只能作为站点级背景，不得写成店铺、商品、广告组或 ASIN 归因。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:review-ui
npm run build
```

## 2026-06-22：选中信号详情不能弱化当前诊断入口

```text
问题：
顶部“诊断入口”已经明确是 Parent ASIN / ASIN / 辅助排查等上游业务口径。
但用户点击左侧队列里的具体信号后，中间详情原先直接展示信号证据，缺少“当前入口”和“选中信号”的关系说明。
对于 SearchTerm、AdGroup、Placement 这类下游对象，用户容易误以为选中信号本身改变了诊断入口，或可以直接归因到某个 ASIN。

原因：
1. 语义组复核和候选定位已经收口为不改写 product_scope，但信号详情层还缺一个显式上下文读法。
2. SignalDiagnosis 解释信号本身，却没有先解释该信号与当前 ProductScope 的上下游关系。
3. 搜索词、广告组、广告位都不是经营商品入口；如果先看指标再看边界，用户容易把它们误读成广告动作对象。

解决方案：
1. 新增 buildSelectedSignalScopeContext，按信号对象类型生成入口关系、能看什么、不能证明什么。
2. 中间诊断区在 SignalDiagnosis 前展示“选中信号与当前入口”。
3. 搜索词文案固定说明不能自动归因到当前入口下某个单一 ASIN。
4. 广告组文案固定说明广告组是投放容器，经营入口仍是当前 ProductScope。
5. 数据质量文案固定说明它只解释快照、字段或新鲜度，不代表经营异常。

后续防错规则：
1. 任何信号详情页都必须先说明当前 ProductScope 和选中 signal object 的关系。
2. 下游诊断对象只能决定中间证据和右侧人工确认对象，不能反向改变诊断入口。
3. SearchTerm / AdGroup / Placement 不能被包装成商品归因入口或自动广告动作入口。
4. 结构测试必须锁定关系提示出现在 SignalDiagnosis 前。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:review-ui
npm run build
```

## 2026-06-22：人工动作按钮不能只按推荐候选校验目标

```text
问题：
右侧人工确认区已经展示“人工动作对象身份门禁”，能说明当前信号、preflight target、留痕读回和复盘待办是否一致。
但按钮禁用逻辑原先只用 selectedBackendManualActionPreview 的 object_type / object_id 作为 expected target。
如果推荐候选或下一候选与当前详情信号错位，页面可能展示身份问题，但按钮层仍只按推荐候选和 preflight 是否一致来判断能否点击。

原因：
1. selectedBackendManualActionPreview 是推荐候选/下一候选/当前信号 fallback 的混合结果。
2. buildManualActionIdentityGateItems 使用当前信号稳定对象校验，但 manualActionButtonGate 没有复用同一口径。
3. 展示层身份门禁和按钮写入门禁分离，容易出现“看的是当前信号，写的是另一个候选”的风险。

解决方案：
1. 新增 manualActionExpectedTargetForSignal，从当前选中信号提取稳定 object_type / object_id / action_type。
2. SignalTriageWorkbench 的按钮渲染和 handleManualAction 都使用当前信号 expected target 调用 manualActionButtonGate。
3. manualActionButtonGate 在 expected target 缺 object_type 或 object_id 时直接 blocked。
4. 增加测试覆盖当前信号对象缺失和 preflight 目标与当前信号不一致两类场景。

后续防错规则：
1. 人工动作按钮必须以当前选中信号稳定对象为写入目标口径。
2. 推荐候选只能用于定位和解释，不能越过当前详情对象成为按钮写入依据。
3. 展示的对象身份门禁如果会出现 blocked，按钮写入门禁也必须以同一对象口径阻断。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-24：固定端口被旧服务占用会让页面继续看到旧口径

```text
问题：
源码和测试已经把“语义组复核”收口为“Parent ASIN 广告搜索词表现复核”，但运行中的 8000 / 5175 旧进程仍可能返回旧 search-intents 口径，导致用户浏览器继续看到旧概念。

原因：
1. start_local_dev.ps1 在首选端口被占用时会启用 fallback 端口。
2. 用户当前浏览器固定打开 localhost:5175，若 5175 仍绑定旧 Vite，页面就会继续请求旧 8000。
3. 仅看源码或重新运行脚本不等于当前浏览器已经切到最新后端。

解决：
1. 先检查 8000 / 5175 端口归属和 uvicorn / Vite 进程。
2. 停止本项目旧 uvicorn / Vite 进程，再用固定 8000 + 5175 重启。
3. 用 /api/search-intents?market_id=1&product_scope_id=parent_asin:B00K4W4AAA 验证 data_grain 已是 Parent ASIN 广告搜索词表现口径。

防错规则：
1. 用户反馈页面文案或口径不对时，先区分源码问题和运行态旧进程问题。
2. 固定浏览器地址为 localhost:5175 时，必须确保前端 Vite 的 VITE_API_BASE_URL 指向当前 8000。
3. 不要把旧服务返回的旧口径误判成当前源码仍未修正。
```

## 2026-06-24：搜索词表现聚合不能被“语义组复核”命名带偏

```text
问题：
用户明确指出“语义组复核”的作用应该是从 Parent ASIN 的角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果页面或 API 合同继续强调“语义标签聚合”，用户会误以为该板块是在复核语义资产，而不是分析广告 SearchTerm 表现。

原因：
1. 规则语义只是搜索词归类来源，不是用户要处理的业务对象。
2. 搜索词表现聚合必须从 Parent ASIN 经营入口出发，只看广告中实际产生表现的用户搜索词行。
3. ABA 是站点级背景，不能混进广告搜索词表现数据口径。

解决：
1. 后端 SearchIntentSummary 和 signal_triage 搜索词聚合口径改为“按标准化用户搜索词及规则归类聚合”。
2. Parent ASIN data_grain 明确排除未投放子 ASIN、自然搜索词和 ABA 站点数据。
3. 前端 SearchIntent 展示统一强调“Parent ASIN 视角聚合广告中实际产生表现的用户搜索词”。
4. 兼容旧快照文案，把旧“按标准化搜索词/语义标签聚合”显示规整为新口径。

防错规则：
1. 用户可见主概念必须是“Parent ASIN 广告搜索词表现复核”或“广告搜索词表现聚合”。
2. “规则语义”只能作为来源字段，不能作为复核对象、诊断入口或人工动作对象。
3. 搜索词表现聚合的数据口径必须说明来自 ad_search_term_daily_metrics，不包含未投放子 ASIN、自然搜索词或 ABA 站点数据。

验证方式：
python -m py_compile backend\app\services\signal_detection.py backend\app\services\signal_triage.py backend\app\models\signals.py backend\app\services\manual_action_preflight.py
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope -q
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
```

## 2026-06-22：语义组复核不能强制改队列分类

```text
问题：
点击“语义组复核”卡片后，前端原先会执行 setFilter("opportunity_expansion")。
虽然 selectedProductScopeId 没变，但队列分类 tab 被自动切到“机会扩量”，用户容易误以为诊断入口或业务对象被切换。

原因：
1. 语义组复核和队列分类被混在同一个筛选状态体验里。
2. 语义组本质是当前诊断入口内的 SearchTerm 机会聚焦，不是信号主分类。
3. 自动切换 tab 缺少解释，会放大“语义组是否是新业务入口”的误解。

解决方案：
1. handleSelectSearchIntent 不再修改 filter。
2. selectedSearchIntentLabel 激活时直接从当前 displayProductScopedSignals 中筛同组 SearchTerm 机会。
3. 用户点击普通队列分类时清除语义组聚焦，避免两层筛选静默叠加。
4. 激活态文案明确写出“诊断入口保持不变，仅显示同组 SearchTerm 机会；ABA 只作站点级背景”。

后续防错规则：
1. ProductScope / 诊断入口是经营对象口径，不能被语义组、ABA 或队列 tab 隐式改写。
2. 语义组只做当前入口内的 SearchTerm 复核导航，不作为广告组、商品或自动动作入口。
3. 两层筛选如果同时存在，必须在 UI 上说明关系，或在切换主筛选时清除辅助筛选。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:review-ui
npm run build
```

## 2026-06-22：搜索词复核链不能依赖上游证据块自带完整边界

```text
问题：
搜索词机会复核链会从后端 business_evidence 读取投放词、广告组和 ABA 背景。
如果上游块已经存在但内容较薄，前端原先可能直接展示该块，缺少“不代表完整关键词库”“不能自动归因到单个广告 ASIN”“ABA 不能当店铺数据”等边界。

原因：
1. business_evidence 的证据块是局部事实，不保证每个块都自带完整反证边界。
2. 右侧人工确认区依赖搜索词复核链的结果，如果复核链边界薄，人工动作前的认知也会变薄。
3. 语义组复核容易被误解为广告 ASIN 或广告组归因，因此边界不能只靠后端偶然返回的文案。

解决方案：
1. 搜索词机会复核链对投放词证据、广告组合流判断、ABA 背景统一追加必要边界。
2. 对已有简短证据块使用 appendBoundaryIfMissing 补齐缺失口径。
3. 增加薄证据测试，确认中间诊断和右侧人工确认都能读到同一套边界。

后续防错规则：
1. 证据块可以提供事实，但最终给运营看的诊断链必须补齐对象粒度、能证明和不能证明。
2. 搜索词相关判断必须明确：不能自动归因到单个广告 ASIN，不能据此判断广告位影响，ABA 只能作为站点级背景。
3. 语义组只做同类搜索词复核导航，不应被包装成商品、广告组或自动动作入口。

验证方式：
npm run test:signal-ui
npm run test:review-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-22：人工确认证据核对必须同时检查页面缺口和快照缺口

```text
问题：
右侧人工确认区会同时展示页面证据依据，并读取后端只读预检中的 evidence_snapshot_preview。
旧逻辑只在页面证据已经出现搜索词复核链标签时，才检查投放词证据、广告组合流判断、ABA 背景、证据缺口和动作边界。
如果后端预检快照包含这些搜索词复核链证据，但页面没有展示，核对区可能不会报警。

原因：
1. manualConfirmationEvidenceReadinessSummary 的搜索词复核链开关只看 manualLabels。
2. 这只覆盖了“页面有、快照缺”的方向，没有覆盖“快照有、页面缺”的方向。
3. 人工确认按钮前如果页面少讲证据，运营就无法知道授权点击后实际保存了哪些复盘依据。

解决方案：
1. 搜索词复核链开关同时检查 manualLabels 和 snapshotLabels。
2. 只要任一侧出现搜索词复核链标签，就要求页面证据和 evidence_snapshot_preview 双向完整。
3. 增加测试：后端预检快照包含搜索词复核链、但页面证据缺少该链路时，必须返回 blocked。

后续防错规则：
1. 人工动作写入前的证据核对必须双向检查，不能只检查后端是否包含页面证据。
2. 后端将保存的 evidence_snapshot_preview 如果比页面展示更完整，页面也必须提示缺口，不能静默通过。
3. 搜索词复核链缺投放词、广告组合流、ABA 背景、证据缺口或动作边界时，不得当作可复盘留痕。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-22：复盘待办范围外提示不能弱化当前诊断入口边界

```text
问题：
当前 Parent ASIN / ASIN 入口下，复盘待办提示原先使用“全局还有”“全局共”这类表达。
虽然按钮是显式切到全量排查，但文案容易让用户把范围外 ReviewTodo 理解成当前诊断入口的一部分。

原因：
1. buildReviewTodoScopeHint 同时接收 currentTotal 和 globalTotal。
2. 旧文案强调全局数量，没有明确“范围外待办不属于当前入口”。
3. 复盘待办是人工动作后的对象排程，如果和当前 product_scope 混淆，会影响运营判断当前商品组是否真的待复盘。

解决：
1. currentTotal=0 时显示“当前诊断入口暂无复盘待办；范围外辅助排查还有 N 条”。
2. currentTotal>0 时显示“当前诊断入口有 N 条复盘待办；范围外辅助排查还有 M 条，不属于当前入口”。
3. 复盘待办范围外按钮 aria-label 改为“切到全量排查查看范围外复盘待办”。
4. 更新 reviewUi 测试锁定新口径。

防错规则：
1. 当前入口复盘待办只能表达当前 product_scope 内的 ReviewTodo。
2. all / 范围外 ReviewTodo 只能作为辅助排查提示，必须明确“不属于当前入口”。
3. 复盘待办只表示进入 7/14 天排程，不代表效果改善，也不触发自动广告动作。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
```

## 2026-06-22：商品组空态不能把全量排查包装成当前商品组信号

```text
问题：
当前商品组暂无信号时，空态解释会提示“全量排查还有 N 条范围外信号”。
这句话容易让用户把 all / 未归因 / 数据质量层误解成当前商品组的 AI 信号延伸。

原因：
1. buildProductScopeSignalExplanation 同时知道当前范围信号数和全量信号数。
2. 旧文案只强调全量排查还有多少条，没有明确这些信号不属于当前商品组广告诊断。
3. 用户已经多次指出项目的问题是数据口径混乱，而不是界面缺少指标。

解决：
1. 将提示改为“范围外辅助排查”。
2. 明确范围外信号“不属于当前商品组 / ASIN 广告诊断”，只用于未归因广告数据或数据质量事项。
3. frontend/tests/signalUi.test.ts 增加断言，防止恢复成弱边界文案。

防错规则：
1. 当前商品组 AI 信号只能指当前 product_scope 内的信号。
2. all / unattributed / data_quality 只能作为辅助排查，不得被包装成当前商品组机会。
3. 空态文案必须同时说明“为什么没有当前范围信号”和“范围外事项不属于当前诊断入口”。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
```

## 2026-06-24：复盘待办不能只回读搜索词分组和广告组

```text
问题：
用户明确指出“语义组复核”的作用应是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
如果复盘待办只回读搜索词表现判断、广告组合流判断和逐投放上下文，用户仍可能看不到同组投放商品承接差异、投放词证据和广告位边界为什么重要。

原因：
1. 搜索词表现分组只是聚合视角，不是人工动作对象。
2. SearchTerm 指标不能自动归因到单个广告 ASIN。
3. 广告组可以投放多个商品，不回看同组投放商品就无法判断承接差异。
4. 投放词和广告位只提供广告结构上下文，不能直接生成自动加词、否词或调价动作。

解决：
1. ReviewTodo 证据读回新增“同组投放商品表现”独立判断行。
2. ReviewTodo 证据读回新增“投放词与广告位边界”独立判断行。
3. 缺少同组投放商品或广告位边界时，读回状态 blocked，并提示先补证，不能只凭 SearchTerm 指标保存复盘结论。

防错规则：
1. Parent ASIN 广告搜索词表现复核必须沿广告结构读回，不允许只展示搜索词裸指标。
2. 同组投放商品是广告组复核的必要证据，不能把未投放子 ASIN 拉入复盘。
3. 投放词和广告位边界只能说明上下文，不能包装成自动广告动作。

验证方式：
frontend 目录执行：
npm run test:review-ui
npm run test:workbench-structure
```

## 2026-06-24：搜索词分诊不能缺少同广告组投放商品承接

```text
问题：
Parent ASIN 广告搜索词表现复核的中间诊断区已经展示搜索词表现、投放词结构和广告位证据缺口，但搜索词分诊下钻里的 ad_product_rows 为空，用户在人工留痕前看不到该 SearchTerm 所在广告组里实际投放了哪些 ASIN。

原因：
1. 搜索词候选自身的 source_rows 通常只包含 ad_search_term_daily_metrics。
2. build_review_candidates_payload 已经提前生成 recommended_evidence_drilldown，后续 summary 增强没有作用到实际推荐下钻。
3. 没有按 campaign/ad_group 从真实 advertised_products 快照补齐同组投放商品承接。

解决：
1. 在 signal-triage 汇总阶段，对 search_term 推荐下钻按 campaign/ad_group 关联真实 advertised_products。
2. 新增 ad_group_product_performance 业务证据块；有证据时展示同广告组广告 ASIN 花费和订单，没证据时明确显示缺口。
3. 前端业务证据展示顺序补入搜索词表现、投放上下文和同组投放商品表现。

防错规则：
1. 搜索词分诊不能只展示 SearchTerm 裸指标，必须尽量回到同广告组投放商品承接。
2. 同组投放商品只能来自真实 advertised_products 同 campaign/ad_group 匹配，不能把 Parent ASIN 下全部子 ASIN 拉进分析。
3. 该证据只能说明广告组内承接差异，不能自动归因到单个 ASIN，也不能自动执行加词、否词或调价。

验证方式：
backend 目录执行：
python -m pytest tests\test_signal_triage_service.py -q
python -m compileall app\services\signal_triage.py

frontend 目录执行：
npm run test:workbench-structure
npm run test:signal-ui
```

## 2026-06-24：右侧人工确认不能把复核上下文读成动作对象

```text
问题：
左侧和中间区已经把“语义组复核”收口为 Parent ASIN 广告搜索词表现复核，但右侧人工确认区如果仍用“搜索词表现分组”作为动作边界主语，用户可能误以为要保存的是分组，而不是具体 SearchTerm。

原因：
1. 右侧人工确认区是用户真正点击记录观察、标记已处理、加入复盘或忽略本次的位置。
2. 如果这里主语含糊，会比左侧文案更容易造成错误操作理解。
3. 复核上下文、具体 SearchTerm、后端 preflight evidence_snapshot_preview 是三层不同对象。

解决：
1. 右侧背景核对改为“复核上下文不是人工动作对象”。
2. 人工留痕对象读回和后端预检一致性提示统一使用“Parent ASIN 广告搜索词表现复核”。
3. 测试锁定右侧 boundary 不能把搜索词分组当成动作对象。

防错规则：
1. 右侧人工确认区的动作对象只能是具体 SearchTerm 或后端 preflight 确认的稳定对象。
2. Parent ASIN 广告搜索词表现复核只能作为上下文和证据快照读回。
3. 新增右侧文案时，必须同时回答“保存对象是谁”和“哪些上下文只用于回看”。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
```

## 2026-06-22：语义组复核不能使用全局搜索词池

```text
问题：
语义组复核原先从 /api/search-intents 读取最新快照中的全局搜索词语义汇总。
当用户选择 Parent ASIN 或广告 ASIN 诊断入口时，该板块仍可能展示当前入口之外的搜索词机会，造成广告诊断口径混乱。

原因：
1. /api/search-intents 原先没有 market_id 和 product_scope_id 参数。
2. 前端 loadSignals 初始化时提前拉取 fetchSearchIntents()，发生在 activeProductScopeId 确认之前。
3. 语义组本质是 SearchTerm 复核辅助，但接口口径更像全局搜索词候选池。

解决：
1. backend/app/services/signal_triage.py 增加 build_search_intent_summaries，先按 market_id 过滤，再按 product_scope_id 提取当前范围内的 search_term_opportunity 源搜索词行。
2. backend/app/api/routes.py 让 /api/search-intents 接收 market_id 和 product_scope_id。
3. frontend/src/apiPaths.ts 和 api.ts 让 fetchSearchIntents 传递当前诊断入口。
4. SignalTriageWorkbench 在初始化和诊断入口变化时都按 activeProductScopeId 重新拉取语义组。
5. 前端结构测试锁定不允许回到无参数 fetchSearchIntents()。

防错规则：
1. 语义组复核只能服务当前诊断入口，不能默认展示全局搜索词池。
2. 点击语义组只能筛选 search_term_opportunity，不能改变 product_scope。
3. ABA 只做站点级搜索词背景，不能把 ABA 匹配结果当成店铺、商品或广告组归因。
4. 未投放、无广告数据的 ASIN 不应进入广告搜索词语义组分析。

验证方式：
python -m pytest tests\test_api_routes.py -q -k search_intents
python -m pytest tests\test_signal_triage_service.py -q -k "search_intent_summaries_respect_parent_asin_product_scope or parent_scope_includes_search_term"
npm run test:snapshot-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-17：广告下钻数据被误读为可行动机会

### 问题

```text
用户指出当前 Web 结果数据口径混乱，给出的信息、机会、证据链没有实际落地意义。
在 parent_asin:B00K4W4AAA 上复核发现：
- 系统能读取 3 个广告 ASIN、广告组、搜索词和广告位上下文。
- 但 candidate_count=0，manual_action_preview=null。
- 页面和脚本仍容易把“有下钻数据”表达成“有处理机会”。
```

### 原因

```text
product_scope_drilldown 和 candidate 的边界没有成为统一合同。
product_scope_drilldown 只能说明经营对象下有哪些广告数据。
candidate 才能说明哪个广告对象可以进入人工确认。

同时 scripts/inspect_signal_triage.py 复制了一份旧分诊逻辑，导致 CLI 真实检查脚本仍输出 status=empty，和后端服务新口径分叉。
```

### 解决

```text
新增 actionability_status：
- requires_product_scope：需要先选择 Parent ASIN / ASIN。
- no_actionable_candidate：已有诊断数据，但没有可进入人工确认的候选。
- ready_for_manual_confirmation：有候选且有 manual_action_preview。
- needs_manual_preview：有候选但缺少预检。

前端规则：
- can_write_manual_action=false 时，不允许 fallback 推荐卡生成候选。
- 商品范围空态显示“暂无可行动候选”。
- 明确提示“只能作为诊断视图，不能写人工动作”。

CLI 规则：
- inspect_signal_triage.py 必须输出同一份 actionability_status，不能继续输出旧 status=empty 误导排查。
```

### 防错规则

```text
以后所有分诊入口都必须先回答：
1. 当前对象是什么：Parent ASIN、广告 ASIN、销售 ASIN、广告组、搜索词还是广告位？
2. 当前数据是诊断上下文，还是可处理候选？
3. 是否有稳定 object_id 和 manual_action_preview？
4. 是否能写入人工动作并进入 7/14 天复盘？

如果 candidate_count=0：
- 不得显示“建议优先留痕”。
- 不得生成人工动作按钮目标。
- 只能显示诊断视图和下一步复核路径。

如果脚本、API、前端任一入口输出不同 status，必须先修合同分叉，再继续做 UI 或规则优化。
```

### 验证方式

```text
后端：
python -m pytest tests\test_signal_triage_service.py tests\test_signal_triage_cli.py -q

前端：
npm run test:signal-ui
npm run build

真实范围：
python scripts\inspect_signal_triage.py --market-id 1 --top 10 --product-scope-id parent_asin:B00K4W4AAA
期望：status=no_actionable_candidate，can_write_manual_action=false。

预检：
python scripts\inspect_manual_action_preflight.py --market-id 1 --top 10 --product-scope-id parent_asin:B00K4W4AAA
期望：blocked，missing_target，will_write=false。
```

### 问题：规则反馈清单没有检查复盘记录来源完整性
#### 现象

```text
review_feedback.records 已经能显示 review_record_id、action_id 和指标窗口。
但 closure_checklist 仍只有复盘结果分布、样本优先级、证据追溯和动作边界。
这会让规则反馈区看起来已具备复核条件，却没有说明有多少样本真的能追溯到 ReviewRecord、ManualAction 和指标窗口。
```

#### 原因

```text
上一轮只把来源字段补到 records 明细层。
_review_closure_checklist 和 scripts/inspect_review_readiness.py 的同名逻辑没有把来源字段纳入闭环检查。
因此 API 和 CLI 的复核清单都缺少“复盘记录来源”这一项。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- closure_checklist 增加 review_record_trace。
- 统计同时具备 review_record_id、action_id、metric_window.before 和 metric_window.after 的样本数。

scripts/inspect_review_readiness.py：
- 同步增加 review_record_trace。
- CLI records 透传 review_record_id、action_id 和 metric_window。
```

#### 后续防错规则

```text
规则反馈候选不能只看复盘结果数量。
每轮规则反馈闭环至少检查：
1. 复盘结果分布。
2. 样本优先级。
3. 证据追溯。
4. 复盘记录来源。
5. 人工动作边界。

没有完整 review_record_id / action_id / 指标窗口时，只能标记 partial 或 blocked，不能当作完整规则复核样本。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py backend\tests\test_review_readiness_cli.py -q
结果：37 passed。

npm run test:signal-ui
npm run test:review-ui
npm run build
结果：通过。

GET /api/signal-triage?market_id=1&top=5&product_scope_id=parent_asin:B00K4W4AAA
结果：review_records=0，closure_checklist=5 项，包含 review_record_trace。
```

### 问题：规则反馈候选只显示复盘汇总，缺少具体 ReviewRecord 来源
#### 现象

```text
review_feedback.records 原先能展示 result、signal_type、object、review_window、review_note 和证据边界。
但规则反馈候选没有稳定显示 review_record_id、action_id 和处理前后指标窗口。
这会让“规则反馈来自哪条已保存复盘记录”不够清楚，后续人工复核很难直接回查来源。
```

#### 原因

```text
_review_feedback_record_items 只从 ReviewRecord 读取结果、对象和复盘窗口，没有把 ReviewRecord.id、action_id、before/after 窗口和指标摘要透传给前端。
前端 ruleFeedbackRecordText 也只拼 result/object/window，因此页面看起来像抽象汇总，而不是来源明确的人工规则复核候选。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- review_feedback.records 增加 review_record_id。
- review_feedback.records 增加 action_id。
- review_feedback.records 增加 metric_window 和 metric_snapshot。

frontend/src/pages/SignalTriageWorkbench/signalUi.ts：
- RuleFeedbackRecordForUi 增加 review_record_id、action_id、metric_window。
- 规则复核记录行展示复盘记录来源和指标窗口。
```

#### 后续防错

```text
规则反馈候选必须从已保存 ReviewRecord 出发。
候选最少要能说明：
1. 哪条 review_record_id。
2. 对应哪次 action_id。
3. 对象、result、review_window。
4. 处理前后指标窗口。
5. 证据边界和动作边界。

没有 ReviewRecord 来源的内容只能叫复盘状态或 blocked 清单，不能叫规则反馈候选。
规则反馈仍只进入人工复核材料，不自动改规则，不自动执行广告动作。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; pytest backend\tests\test_signal_triage_service.py::test_review_readiness_summarizes_saved_review_records_as_rule_feedback -q
结果：通过。

$env:PYTHONPATH='backend'; pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
结果：30 passed。

$env:PYTHONPATH='backend'; pytest backend\tests\test_api_routes.py::test_signal_triage_rule_feedback_after_saving_ready_review_record -q
结果：通过。

cd frontend
npm run test:signal-ui
npm run test:review-ui
npm run build
结果：通过。

页面 smoke：
http://127.0.0.1:5175/ 首屏可见规则改进门槛和规则复核优先级，控制台无业务 error / warn。
```

### 问题 239：复盘待办摘要不能只显示下一项而不显示到期日
#### 现象

```text
当前 Parent ASIN 作用域内有 6 条复盘待办、到期 0 条。
队列摘要原先显示“下一项：B016EXMVZS”，但不显示 due_at。
运营能知道有下一项，却不知道应该等到哪一天复盘。
```

#### 原因

```text
buildReviewTodoQueueSummary 已经选出 next todo，但返回结构只包含 nextLabel。
页面摘要没有消费 review_todos.due_at，导致“何时复盘”这个关键业务信息只能进入详情后再找。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- ReviewTodoQueueSummary 增加 nextDueDate。
- buildReviewTodoQueueSummary 从 next.due_at 派生日期。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- 队列摘要显示“下一项：对象 / 到期 YYYY-MM-DD”。
```

#### 后续防错规则

```text
凡是展示复盘待办摘要，都必须同时说明数量、是否到期、下一项对象和到期日。
不能只展示“有待办”或“下一项”，否则运营无法判断当前该处理、等待还是复盘。
ready 前仍不能保存 review_records，也不能输出改善 / 无变化 / 恶化结论。
```

#### 验证方式

```text
npm run test:review-ui
npm run test:signal-ui
npm run build
浏览器运行态检查复盘摘要显示“下一项：B016EXMVZS / 到期 2026-06-22”，且 console error / warn 为空。
```

### 问题 238：前端人工动作目标优先级不能和后端预检不一致
#### 现象

```text
B06VW5SQ97 已经人工留痕并生成 7d / 14d 复盘待办后，后端分诊把 next_unhandled_candidate 切到 B016EXMW1G。
但前端仍可能默认选中或预览已留痕推荐对象 B06VW5SQ97。
此时调用 /api/manual-action/preflight 时，后端目标为 B016EXMW1G，前端 expected_object_id 为 B06VW5SQ97，结果返回 object_id_mismatch。
```

#### 原因

```text
后端 manual_action_preflight 的目标选择规则是 next_unhandled_candidate 优先，其次 recommended_candidate。
前端 resolveSignalSelectionId 和 manualActionPreviewForSelectedSignal 原先仍优先推荐对象，且会对选中对象生成 fallback 预检预览。
这会让安全阻塞成为正常路径，削弱“已处理对象等待复盘、下一个候选进入可操作位”的业务闭环。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/signalUi.ts：
- 当推荐对象已有人工留痕且存在 next_unhandled_candidate 时，默认选中 next_unhandled_candidate。
- 人工动作预览只对后端当前目标生成；非当前目标不再生成 fallback 预检预览。
```

#### 后续防错规则

```text
凡是页面要调用 /api/manual-action/preflight，前端 expected_object_id 必须和后端当前人工动作目标优先级一致。
如果推荐对象已有人工留痕并等待复盘，右侧人工处理区必须面向下一个未留痕候选，而不是继续展示已留痕对象的可写入口。
object_id_mismatch 应当代表真实目标错误，不应成为正常工作流里的常态提示。
```

#### 验证方式

```text
npm run test:signal-ui
npm run test:review-ui
npm run build
浏览器运行态检查右侧人工处理目标为 B016EXMW1G，后端只读预检通过且 console error / warn 为空。
```

### 问题 237：截图空态不能直接当作真实数据缺失
#### 现象

```text
用户截图显示工作台左侧为 0 条信号，并出现“后端服务未连接”提示。
如果只按截图判断，会误以为 B00K4W4AAA 没有真实候选信号，或者后端服务仍不可用。
```

#### 原因

```text
截图只能证明某一刻的浏览器可见状态，不能证明当前后端接口、当前筛选作用域和当前 DOM 状态。
本轮复核发现：后端 /health 正常，B00K4W4AAA 分诊接口返回 15 条候选，浏览器当前 DOM 也已经显示 15 条队列。
因此截图中的空态属于旧状态或加载过程中的临时状态，不是当前数据事实。
```

#### 解决方案

```text
遇到页面空态时，先按三层事实链复核：
1. 服务层：GET /health。
2. 业务层：直接请求当前筛选作用域的业务 API，例如 /api/signal-triage?market_id=1&product_scope_id=parent_asin:B00K4W4AAA。
3. 页面层：用浏览器 DOM 读取队列容器和行数，例如 .queuePanel 和 .signalRows .signalRow。

只有三层都指向同一问题时，才进入代码修复；如果接口和 DOM 已恢复，不要为旧截图状态改代码。
```

#### 后续防错规则

```text
不要把截图里的“0 条”“读取中”“后端未连接”直接当作业务结论。
Parent ASIN / ASIN 作用域必须以后端 API 返回和当前 DOM 行数共同校验。
如果后端分诊有候选但前端无行数，才排查前端筛选或展示层；如果 DOM 已有行数，则不做无效修复。
```

#### 验证方式

```text
Invoke-RestMethod http://127.0.0.1:8000/health
Invoke-RestMethod http://127.0.0.1:8000/api/signal-triage?top=5&market_id=1&product_scope_id=parent_asin%3AB00K4W4AAA
浏览器 DOM 检查 .signalRows .signalRow 数量。
```

### 问题 236：后端业务证据补齐后必须进入中间诊断可见区
#### 现象

```text
后端 recommended_evidence_drilldown 已经能返回 sales_product 的销售表现、广告承接、广告商品覆盖和同父体位置。
但前端只在左侧队列的“诊断依据”折叠区展示 recommendedEvidenceDrilldownText。
中间 SignalDiagnosis 仍只展示 selectedSignal 自身 facts，运营在第一屏诊断区看不到推荐对象的完整业务证据链。
```

#### 原因

```text
后端分诊摘要 signalTriageSummary 和中间诊断组件 SignalDiagnosis 的数据入口分离。
如果只把推荐证据放进折叠辅助区，就会出现“API 有证据、页面中间诊断看不到”的产品断层。
这会削弱 AI Copilot 工作台的本质价值：人工确认前必须先看到证据和边界。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/signalUi.ts：
- 新增 signalTriageBusinessEvidenceItems。
- 从 business_evidence_blocks 中提取首屏关键证据：销售表现、广告承接、广告商品覆盖、同父体位置。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- 仅当当前选中信号命中推荐候选或下一个未留痕候选时，把业务证据传给 SignalDiagnosis。
- 在中间“证据链”区域可见展示业务证据块。
```

#### 后续防错规则

```text
后端证据补齐不等于产品闭环补齐。
用于支撑人工确认的证据必须进入中间诊断可见区，不能只放在折叠辅助区、CLI 或 API payload 中。
前端不能重新计算业务证据，只消费后端 business_evidence_blocks。
推荐对象证据不能误显示到非推荐选中信号上。
```

#### 验证方式

```text
npm run test:signal-ui
npm run build

浏览器运行态必须能在中间证据链看到：
- 销售表现
- 广告承接
- 广告商品覆盖
- 同父体位置
```

### 问题 235：sales_product 推荐证据合并不能丢失销售商品证据
#### 现象

```text
B00K4W4AAA 范围内推荐对象是 P0 sales_product / B06VW5SQ97。
候选结论是“销售强但广告弱”，但 recommended_evidence_drilldown 经过同 ASIN 合并后变成 recommended_evidence_rows，广告商品投放行 0 条，订单 / 销售额也显示为 0。
这会让运营只能看到结论，看不到销售表现、广告承接、同父体位置和广告商品覆盖证据。
```

#### 原因

```text
_merge_recommended_evidence_drilldown 原先默认按广告商品行合并推荐证据。
sales_product 候选的原始 evidence_drilldown 已经包含 sales_product_summary 和 business_evidence_blocks，但合并函数没有保留这些字段。
当 sales_product 没有直接广告商品行时，合并结果会把销售商品证据降级为空广告指标。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- 推荐对象为 sales_product、存在 sales_product_summary 且没有广告商品行可合并时，保留原 sales_product drilldown。
- 复用 _sales_product_sibling_enriched_drilldown，补齐同父体销售位置和广告商品覆盖证据块。

backend/tests/test_signal_triage_service.py：
- 新增 recommended sales_product merge 回归测试，要求保留 sales_product_daily_metrics、sales_product_summary 和 business_evidence_blocks。
```

#### 后续防错规则

```text
推荐证据合并不能只按广告商品行建模。
sales_product 是经营商品入口，必须保留 sales_product_daily_metrics 证据。
广告商品覆盖、同父体位置、销售表现和人工下一步属于同一条 sales_product 推荐证据链，不能被合并层丢弃。
如果证据行为空，不能直接输出 0 花费 / 0 订单伪装成完整推荐证据。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; pytest backend\tests\test_signal_triage_service.py -q
$env:PYTHONPATH='backend'; pytest backend\tests\test_signal_triage_cli.py -q

真实运行态必须能看到：
- recommended_evidence_drilldown.metric_summary.basis=sales_product_daily_metrics
- sales_product_summary.orders > 0
- business_evidence_blocks 包含 sales_performance、ad_coverage、ad_product_coverage、parent_sibling_position
```

### 问题 234：人工动作按钮不能绕过后端预检状态
#### 现象

```text
页面已展示 /api/manual-action/preflight 的后端只读预检结果。
但四个人工动作按钮原先只受 savingManualAction 和“已有复盘待办不重复加入复盘”影响。
如果后端预检读取中、读取失败或返回 blocked，按钮仍可能保持可点。
后续补查还发现，即使按钮层已经禁用，写入函数 handleManualAction 如果被其他入口调用，仍可能绕过按钮门槛。
```

#### 原因

```text
“展示预检结果”和“用预检结果约束操作”是两个不同层级。
只展示 will_write=false、expected_after_write 和 blockers，并不能阻止运营在阻塞状态下点击人工动作。
这会让页面行为和“凡是页面要展示人工动作是否可写，必须先调用只读预检 API”的规则不一致。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- 新增 manualActionButtonGate。
- 统一返回按钮 disabled、reason、compactReason。
- 预检读取中、读取失败、blocked、will_write 未确认为 false 时禁用人工按钮。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- 四个人工按钮统一使用 manualActionButtonGate。
- disabled、title、aria-label 和按钮副文案与同一门槛结果对齐。
- handleManualAction 入口也复用 manualActionButtonGate。
- 门槛禁用时只显示“未保存”提示，不调用 createSignalManualAction。
```

#### 后续防错规则

```text
只读预检不能只作为文字展示。
凡是预检结果用于说明人工动作是否可写，按钮状态也必须使用同一预检结果。
后端预检未完成、失败、blocked 或未明确 will_write=false 时，不允许人工按钮可点。
已有 7d / 14d 复盘待办时，不允许重复点击“加入复盘”。
写入函数入口也必须复用同一 gate，不能只依赖按钮 disabled。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run build

测试必须覆盖：
- 预检读取中禁用。
- 预检 blocked 禁用。
- 预检 ready 且 will_write=false 时允许。
- 已有复盘待办时禁用“加入复盘”。
- 写入 guard 在预检未完成时返回“未保存”，预检通过时返回 null。
```

### 问题 233：人工动作预检不能只停留在 CLI 脚本
#### 现象

```text
scripts/inspect_manual_action_preflight.py 已能判断人工动作写前 / 写后是否符合预期。
但工作台页面只能消费后端 API。
如果预检只停留在 CLI，前端会继续靠 signal-triage 和本地推断拼接口径，容易出现“脚本可验收、页面不可验收”的分裂。
```

#### 原因

```text
人工动作闭环涉及三类事实：
- 分诊推荐对象。
- manual_actions 人工留痕。
- 由人工动作派生的 7d / 14d review_todos。

这些事实必须由后端只读接口统一返回。
前端不能自行推断“写后应该有几条待办”，也不能把点击按钮等同于闭环成立。
```

#### 解决方案

```text
backend/app/services/manual_action_preflight.py：
- 新增人工动作预检服务，统一返回 will_write=false、当前数量、写后预期数量、写后验收项和禁止副作用。

backend/app/api/routes.py：
- 新增 GET /api/manual-action/preflight。
- 参数包含 market_id、product_scope_id、expected_object_id、expected_object_type、action_type、expect_written。
- 接口只读，不写 manual_actions，不写 review_records，不请求积加 API，不执行广告动作。
```

#### 后续防错规则

```text
凡是页面要展示“人工动作是否可写 / 写后是否符合预期”，必须先调用只读预检 API。
不允许前端自己推断 expected_after_write。
不允许把 CLI 脚本作为页面运行时依赖。
不允许把预检接口变成写入接口。
```

#### 验证方式

```text
PYTHONPATH=backend pytest backend/tests/test_api_routes.py::test_manual_action_preflight_route_returns_readonly_validation backend/tests/test_manual_action_preflight_cli.py backend/tests/test_manual_action_apply_cli.py -q
python -m py_compile backend/app/api/routes.py backend/app/services/manual_action_preflight.py scripts/inspect_manual_action_preflight.py scripts/apply_manual_action_once.py

只读 smoke：
GET /api/manual-action/preflight?market_id=1&top=3&product_scope_id=parent_asin%3AB00K4W4AAA&expected_object_id=B06VW5SQ97&expected_object_type=sales_product&action_type=add_to_review&expect_written=false
返回 status=ready_for_explicit_manual_write、will_write=false、expected_target_review_todo_count=2、blocker_count=0。
```

### 问题 232：人工动作成功提示不能只说已记录
#### 现象

```text
右侧人工动作按钮点击成功后，页面原先只提示“已记录：加入复盘 / 记录观察 / 标记已处理 / 忽略本次”。
但 MVP 闭环要求人工动作后必须能读回复盘待办。
如果成功提示不说明 7d / 14d 是否生成，运营容易误以为只要动作写入就等于复盘闭环成立。
```

#### 原因

```text
人工动作记录和复盘待办是两个不同事实。
“已记录”只能证明 ai_signal_actions 有留痕，不能证明 review todo 已按预期生成。
页面已有读回一致性逻辑，但成功消息没有直接复用这套逻辑。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- 新增 manualActionPostWriteReadbackMessage。
- 成功消息统一包含“已记录”和“点击后读回一致 / 待读回核对”。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- loadManualActions 返回读回记录。
- handleManualAction 写入成功后使用读回记录生成消息。
- 读回失败时明确显示“已记录但复盘待办读取失败”。
```

#### 后续防错规则

```text
人工动作成功不等于复盘闭环成功。
所有复盘类人工动作都必须读回 7d / 14d 待办后，才允许展示“读回一致”。
忽略动作必须读回 0 条待办。
任何读取失败或窗口缺失都只能显示“待读回核对”，不能展示成闭环完成。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run build

只读接口：
http://127.0.0.1:8000/api/signal-triage?market_id=1&top=5&product_scope_id=parent_asin%3AB00K4W4AAA
确认本轮没有写入人工动作：recommended_manual_status.has_manual_action=false，review_todo_count=0。
```

### 问题 231：页面默认选中第一条信号会遮蔽后端推荐人工候选
#### 现象

```text
后端 /api/signal-triage 已经在 parent_asin:B00K4W4AAA 范围内推荐 B06VW5SQ97 / RBK004-RBK004-2 深蓝。
但前端 loadSignals 原逻辑只执行 current ?? nextSignals[0]?.id。
如果第一条信号不是后端推荐候选，运营进入页面后会先看到非推荐对象，右侧人工确认区也不一定聚焦到最应处理的 P0 候选。
```

#### 原因

```text
页面默认选中逻辑只看 signals 列表顺序，没有消费 signal-triage 的 recommended_candidate / manual_action_preview。
这会让“后端推荐候选 -> 人工确认”的业务链路在前端被第一条列表项打断。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/signalUi.ts：
- 新增 resolveSignalSelectionId。
- 当前选择仍在当前商品范围内时保留用户选择。
- 当前选择为空或不在范围内时，优先选择后端推荐人工候选，其次选择 next_unhandled_candidate，最后才选第一条信号。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- 初始加载时先按真实 product_scope 计算当前商品范围，再读取该范围的 signal-triage。
- 初始加载和范围切换后都使用 resolveSignalSelectionId。
```

#### 后续防错规则

```text
前端不能把列表第一项当作推荐对象。
凡是后端已经给出 recommended_candidate、manual_action_preview 或 next_unhandled_candidate，页面聚焦和右侧人工确认都必须优先服从后端分诊结果。
但如果用户当前选择仍在当前范围内，应保留用户选择，避免页面频繁抢焦点。
```

#### 验证方式

```text
cd frontend; npm run test:signal-ui
cd frontend; npm run build

只读接口：
http://127.0.0.1:8000/api/signal-triage?market_id=1&top=5&product_scope_id=parent_asin%3AB00K4W4AAA
返回 recommended_candidate.stable_object_id=B06VW5SQ97。
```

### 问题 230：空复盘反馈时不能隐藏 blocked 复核清单
#### 现象

```text
后端 review_feedback.total=0 时仍然会返回 closure_checklist，明确 result_distribution 和 evidence_trace 为 blocked。
旧前端 buildRuleFeedbackPrioritySummary 在 total <= 0 时直接返回 null，导致右侧规则复核区隐藏 blocked 清单。
```

#### 原因

```text
total=0 只说明没有已保存复盘记录，不等于没有规则复核状态。
当 closure_checklist 存在时，blocked 本身就是重要业务结论：当前证据和样本不足，不能进入规则改进。
前端把“没有复盘记录”和“没有需要展示的复核门槛”混成了同一个条件。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/signalUi.ts：
- buildRuleFeedbackPrioritySummary 先读取 closure_checklist。
- 只有 total <= 0 且 closure_checklist 为空时才隐藏。
- total=0 但 closure_checklist 存在时展示 blocked 清单。

frontend/tests/signalUi.test.ts：
- 增加 total=0 + closure_checklist blocked 的测试。
```

#### 后续防错规则

```text
规则复核入口不能只用复盘记录数量判断是否显示。
如果后端返回 readiness / checklist / boundary 类状态，即使没有样本，也要展示 blocked 或 next_action。
隐藏 blocked 状态会让运营失去“先补证据”的判断依据。
```

#### 验证方式

```text
npm run test:signal-ui
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
python -m py_compile backend\app\services\signal_triage.py scripts\inspect_review_readiness.py
npm run build
```

### 问题 229：规则复核材料必须有闭环确认清单
#### 现象

```text
规则复核层已经有复盘结果分布、样本排序、证据分组和动作边界。
但这些信息分散展示时，运营仍然需要自己判断是否已经具备进入人工规则改进会议的最小材料，容易跳过证据追溯或动作边界检查。
```

#### 原因

```text
规则改进不是看到 worse / no_change 就可以直接调整规则。
它至少需要确认结果分布、样本优先级、证据追溯和越权边界。
缺少闭环确认清单时，系统容易从“复盘反馈”过早跳到“规则改进”，偏离 AI 只做诊断、证据整理、建议和复盘的边界。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- review_feedback 增加 closure_checklist。
- 基于 by_result、records、evidence_groups 和 action_boundaries 只读派生复核清单。

scripts/inspect_review_readiness.py：
- CLI readiness 同步 closure_checklist。

frontend：
- 规则复核优先级展示 closureChecklist，让右侧工作台直接呈现复核材料是否 ready / partial / blocked。
```

#### 后续防错规则

```text
任何“复盘反馈进入规则改进”的功能，都必须先展示闭环确认清单。
不能只凭复盘结果数量或 worse / no_change 文案判断可以改规则。
证据追溯只能来自真实 evidence_groups，不能从 review_note 或前端文案反推。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
python -m py_compile backend\app\services\signal_triage.py scripts\inspect_review_readiness.py
npm run test:signal-ui
npm run build
```

### 问题 228：规则复核动作建议必须有越权边界
#### 现象

```text
规则复核层已经能展示样本排序和证据分组。
但如果只说“复核阈值、证据来源、建议动作”，没有同时说明禁止自动行为，运营或后续开发可能把规则复核误解成系统可以自动改规则或自动执行广告动作。
```

#### 原因

```text
AI 信号工作台只允许诊断、证据整理、建议生成和人工复盘。
规则复核建议属于解释层和人工复核材料，不是广告执行指令。
缺少动作边界时，建议文案容易越权。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- review_feedback 增加 action_boundaries。
- review_feedback.records 每条增加 action_boundary。
- 每类复盘结果都明确 allowed_reviews 和 forbidden_actions。

frontend：
- 规则复核优先级展示总体动作边界。
- 每条复核记录展示对应动作边界。
```

#### 后续防错规则

```text
任何规则复核建议都必须同时给出 allowed_reviews 和 forbidden_actions。
禁止只写“建议动作”而不写“不自动改规则、不自动执行广告动作”。
规则复核只能进入人工复核材料，不能直接进入广告执行链路。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
python -m py_compile backend\app\services\signal_triage.py scripts\inspect_review_readiness.py
npm run test:signal-ui
npm run build
```

### 问题 227：规则复核样本排序必须说明业务理由
#### 现象

```text
规则复核样本列表已经按 worse -> no_change -> unclear -> improved 展示。
但如果界面只展示排序后的结果，不说明为什么这样排，运营可能误以为列表是时间排序、随机排序，或把 improved 样本当成优先复核对象。
```

#### 原因

```text
样本排序是业务风险排序，不是数据库默认排序。
worse / no_change / unclear 分别代表变差、建议无效风险、证据不足，优先级高于 improved。
这个判断必须进入解释层，否则规则复核优先级会被误读。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- review_feedback 增加 sample_sort。
- review_feedback.records 每条增加 sort_reason。
- 排序原因明确说明 worse / no_change / unclear / improved 的业务优先级。

frontend：
- 规则复核优先级展示样本排序说明。
- 每条复核记录展示排序依据。
```

#### 后续防错规则

```text
凡是进入规则复核层的排序，都必须同时给出排序字段、排序顺序和业务理由。
不能只返回排序后的列表，不能让运营从列表顺序猜测优先级。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
python -m py_compile backend\app\services\signal_triage.py scripts\inspect_review_readiness.py
npm run test:signal-ui
npm run build
```

### 问题 226：规则复核证据不能只给长摘要
#### 现象

```text
规则复核对象已经能回到 evidence_drilldown.summary。
但 summary 是一整段文字，运营复核时仍然需要自己拆解哪些是商品指标、哪些是搜索词上下文、哪些是广告位上下文、哪些是归因边界。
这会降低复核效率，也容易漏看边界提示。
```

#### 原因

```text
证据摘要适合解释完整上下文，但不适合快速排序和复核。
规则复核场景需要先按证据类别扫读，再决定复核阈值、证据来源或建议动作。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- 从 evidence_drilldown 提炼 evidence_groups。
- 固定输出商品指标、搜索词上下文、广告位上下文、边界提示。
- 没有 evidence_drilldown 时返回空数组。

frontend：
- 规则复核优先级记录展示“证据分组”。
- 分组只读展示，不新增规则变更或广告动作入口。
```

#### 后续防错规则

```text
进入规则复核层的证据，必须同时有完整摘要和可扫读分组。
分组只能来自真实 evidence_drilldown，不能从 review_note、前端文案或人工备注反推。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
python -m py_compile backend\app\services\signal_triage.py scripts\inspect_review_readiness.py
npm run test:signal-ui
npm run build
```

### 问题 225：规则复核对象必须能回到原始证据链
#### 现象

```text
规则复核优先级已经可以展示具体复核对象。
但如果只看到对象、窗口和备注，运营仍然不知道该对象当初为什么被判定为 anomaly / opportunity。
这会导致复核仍停留在对象名单层，而不是证据层。
```

#### 原因

```text
ReviewRecord 保存的是人工复盘事实，不保存原始诊断证据。
原始证据属于 AI signal 的 evidence.source_rows 和 evidence_drilldown。
两者必须通过 signal_id 只读关联，不能把证据复制进复盘记录，也不能在映射失败时伪造证据。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- review_feedback.records 增加 evidence_drilldown。
- 通过 review_record.signal_id 映射当前 AI signal。
- 复用 _candidate_evidence_drilldown 生成原始证据摘要。
- 映射不到或没有可下钻证据时返回 null。

frontend：
- 规则复核优先级记录文案展示“证据回看”摘要。
- 没有 evidence_drilldown 时不展示证据，不编造解释。
```

#### 后续防错规则

```text
任何规则复核、规则反馈或 AI 建议有效性分析，都必须能说明对象来源和证据来源。
只有对象没有证据，不算完成业务闭环。
证据回看只能来自真实 signal evidence，不允许从复盘备注或前端文案反推。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
python -m py_compile backend\app\services\signal_triage.py scripts\inspect_review_readiness.py
npm run test:signal-ui
npm run build
```

### 问题 224：规则复核优先级不能只停留在数量汇总
#### 现象

```text
规则复核优先级已经能按复盘结果和信号类型汇总。
但如果只展示 by_result / by_signal_type 数量，运营仍然不知道具体是哪条复盘记录、哪个对象、哪个窗口造成了优先级。
这种展示容易重新变成浅层分析，不能支撑业务复核。
```

#### 原因

```text
数量汇总只能说明“哪类问题多”，不能说明“先复核哪个对象”。
AI Copilot 工作台的业务闭环要求每个结论能回到证据和对象，规则复核优先级也必须能回到 saved review record。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- review_feedback 增加 records。
- records 按 worse / no_change / unclear / improved 优先级排序。
- 每条记录携带 signal_id、signal_type、result、object_type、object_id、object_label、review_window、review_note。

frontend：
- 规则复核优先级展示对象记录列表。
- 只读展示，不新增自动改规则或广告动作。
```

#### 后续防错规则

```text
任何“规则复核优先级”“规则反馈”“AI 建议有效性”类展示，必须能追溯到对象级记录。
只给数量、不交代对象来源的分析，不算完成业务闭环。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
python -m py_compile backend\app\services\signal_triage.py scripts\inspect_review_readiness.py
npm run test:signal-ui
npm run build
```

### 问题 223：规则反馈按信号类型汇总不能凭前端猜测
#### 现象

```text
复盘反馈已能按 improved / no_change / worse / unclear 汇总。
但规则复核优先级还需要知道反馈来自 anomaly 还是 opportunity。
ReviewRecord 当前不直接保存 signal_type，如果只在前端按 result 文案推断信号类型，会制造错误数据口径。
```

#### 原因

```text
review record 保存的是人工复盘事实：signal_id、对象、窗口、result、指标和备注。
signal_type 属于 AI signal 的诊断属性，不是 review record 原有字段。
两者必须通过 signal_id 关联，不能凭前端文案或对象类型猜测。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- review_feedback 增加 by_signal_type。
- 通过 review_record.signal_id 只读映射当前信号快照的 signal_type。
- 映射失败归入 unknown。
- 没有 review_records 时不构建当前信号映射，避免无关副作用。

frontend：
- 只消费后端 by_signal_type。
- 规则复核优先级明确展示信号类型分布、复盘结果优先级和边界。
```

#### 后续防错规则

```text
任何按信号类型的复盘反馈汇总，都必须有 signal_id -> signal_type 的可解释来源。
映射不到时必须归入 unknown，不允许前端猜测。
by_signal_type 只用于解释层和人工复核优先级，不自动改规则，不自动执行广告动作。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
npm run test:signal-ui
npm run build
```

### 问题 222：规则反馈候选缺少结构化解释
#### 现象

```text
保存复盘记录后，右侧规则改进门槛可以显示“规则反馈已沉淀”。
但如果只有状态文案，运营容易不知道这个反馈到底基于哪个对象、哪个窗口、哪个复盘结果，也可能误读成系统已经自动改了规则。
```

#### 原因

```text
saved review record 原先主要通过 buildRuleImprovementReadiness 汇总成一段说明。
它能表达“已沉淀”，但没有把依据、建议复核方向和边界分开展示。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- 增加 buildRuleFeedbackCandidate。
- 基于最近 review record 和复盘窗口生成 basis、recommendation、boundary。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- 在“规则改进门槛”下展示“规则反馈候选”。
- 只读展示，不新增按钮，不触发规则或广告动作。
```

#### 后续防错规则

```text
任何规则反馈都必须说明：依据是什么、建议复核什么、边界是什么。
规则反馈候选只能用于解释层和人工复核优先级，不能自动改规则，不能自动执行广告动作。
```

#### 验证方式

```text
npm run test:review-ui
npm run build
```

### 问题 221：复盘记录保存前缺少口径和规则边界检查
#### 现象

```text
右侧复盘待办在复盘效果 ready 后可以保存复盘记录。
但保存按钮前原先没有结构化展示对象、窗口、指标口径和规则反馈边界。
运营可能把 7d / 14d 的局部指标变化误读为“规则已经可以自动改”或“广告动作可以自动执行”。
```

#### 原因

```text
canSaveReviewEffect 只判断 effect.status === ready。
该条件能说明“可以保存复盘记录”，但不能说明“保存前必须核对什么”。
复盘记录和规则改进之间缺少最后一道业务边界提示。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- 增加 buildReviewRecordPreflightChecklist。
- ready 时输出复盘对象、复盘窗口、指标口径、规则反馈边界 4 项检查。
- not_ready 时不展示保存前检查。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- 在“保存复盘记录”按钮前展示“保存前检查”。
```

#### 后续防错规则

```text
任何复盘记录保存入口前，都必须展示对象、窗口、指标口径和规则边界。
复盘记录只能进入规则反馈候选或解释层，不能自动改规则，不能自动执行广告动作。
处理前后窗口指标变化不等同于完整业务归因。
```

#### 验证方式

```text
npm run test:review-ui
npm run build
```

### 问题 220：人工确认按钮缺少点击前复核清单
#### 现象

```text
右侧人工确认区已有“加入复盘 / 记录观察 / 标记已处理 / 忽略本次”按钮和只读预检目标。
但按钮前没有结构化展示必须核对的事实，运营可能只看到 AI 建议和对象 ID，就直接点击加入复盘。
在 B06VW5SQ97 场景下，这会放大两个误读风险：
1. 把销售表现广告订单当成广告商品投放行。
2. 把销售商品强直接理解为可以自动加投。
```

#### 原因

```text
manual_action_preview 原先只包含 action_type、object_type、object_id、review_windows 等写入目标字段。
它能说明“写谁”，但不能说明“写之前必须确认什么”。
证据链和人工按钮之间缺少最后一道业务复核门槛。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- manual_action_preview 增加 preflight_checklist。
- 销售商品对象默认包含复盘对象、广告 ASIN 覆盖、库存/价格/策略边界、人工动作边界 4 项检查。

scripts/inspect_signal_triage.py：
- CLI 同步输出 preflight_checklist。

frontend：
- 右侧人工确认区在按钮前展示“确认前检查”。
- 本地 fallback 也保留最小检查项，防止后端预检缺失时无提示。
```

#### 后续防错规则

```text
任何人工确认按钮前都必须能看到对象身份和动作边界。
销售商品类人工动作必须提示核对广告 ASIN 覆盖、库存/价格和策略边界。
检查清单只能作为只读复核提示，不能自动执行广告动作，也不能替代人工判断。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
npm run test:signal-ui
npm run build

真实只读回查 parent_asin:B00K4W4AAA：
- manual_action_preview.object_id=B06VW5SQ97
- manual_action_preview.preflight_checklist 包含 target_identity、ad_product_coverage、business_boundary、manual_action_boundary。
```

### 问题 219：销售表现广告订单和广告商品投放行口径不一致
#### 现象

```text
B06VW5SQ97 在 sales_product_daily_metrics 中有广告订单 18、广告销售额 170.76。
但同一真实快照的 advertised_products 中，B06VW5SQ97 直接广告商品行为 0。
如果只看广告商品行，容易误判为“没有广告”；如果只看销售表现广告订单，又容易误判为“广告商品已覆盖目标 ASIN”。
```

#### 原因

```text
sales_product_daily_metrics 是销售商品表现口径，包含该销售商品维度上的广告订单和广告销售字段。
advertised_products 是广告商品投放行口径，只说明当前广告商品快照中有哪些 ASIN 投放行。
两个口径不是同一张事实表，不能互相替代。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- 为销售商品证据增加 ad_product_coverage。
- 先用 parent_asin 下按 asin 聚合的销售商品集合确定同父体范围。
- 再统计 advertised_products 中同父体已覆盖的广告 ASIN 和目标 ASIN 直接广告商品行数。
- B06VW5SQ97 的真实结果是目标广告商品行 0 / 同父体已投 ASIN 3/12，同时销售表现仍有广告订单 18。

frontend：
- 证据块增加“广告商品覆盖”，用于解释该口径差异。
```

#### 后续防错规则

```text
不能把 advertised_products 中目标 ASIN 行数为 0 直接解释为“没有广告”。
不能把 sales_product_daily_metrics 的广告订单直接解释为“广告商品快照已覆盖该 ASIN”。
遇到两者不一致时，只能标记为人工复核点：广告 ASIN 覆盖、非 SP 来源、同步缺口或对象映射缺口。
AI 不允许据此自动加投、自动暂停或自动调价。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
npm run test:signal-ui
npm run build

真实只读回查 parent_asin:B00K4W4AAA：
- target_asin=B06VW5SQ97
- target_direct_ad_product_row_count=0
- parent_advertised_asin_count=3
- sibling_asin_count=12
- target_sales_ad_orders=18
```

### 问题 218：同父体销售行数被误读为子 ASIN 数
#### 现象

```text
Parent ASIN B00K4W4AAA 的销售表现数据只读检查中，sales_product_daily_metrics 原始命中 14 行。
如果直接把行数当子 ASIN 数，会得出“父体下有 14 个子商品”的错误结论。
用户已指出该 Parent ASIN 下仅有 12 个子 ASIN，且可从积加开放平台销售表现求证。
```

#### 原因

```text
销售表现快照中同一个 asin 可能出现多行，例如不同来源或重复快照行。
同父体子 ASIN 对比的业务口径不是原始行数，而是 parent_asin 下按 asin 聚合后的唯一 ASIN 数。
广告组中投放 2 个变体也不能反推父体只有 2 个子 ASIN；广告组是投放容器，不是父体商品结构。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- 增加 sibling_comparison，只在 sales_product_daily_metrics 中按 parent_asin 找同父体销售商品。
- 先按 asin 聚合，再计算 sibling_asin_count、订单排名、销售额排名和广告订单占比排名。
- B06VW5SQ97 的真实只读回查结果为 12 个子 ASIN 中订单排名 12/12，销售额排名 12/12。

scripts/inspect_signal_triage.py：
- 同步输出同父体比较，避免 CLI 检查继续显示旧证据结构。
```

#### 后续防错规则

```text
任何“父 ASIN 下有多少子 ASIN”的判断，都必须按 parent_asin + asin 去重或聚合。
不得用广告组投放商品数量、原始行数、页面展示行数替代子 ASIN 数。
请求积加 API 前必须先查限流规则；本轮未请求 API，只使用已有真实快照只读验证。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
npm run test:signal-ui

真实只读回查 parent_asin:B00K4W4AAA：
- sibling_asin_count=12
- target_asin=B06VW5SQ97
- target_order_rank=12
- target_sales_rank=12
- parent_orders=24762
- parent_ad_order_share=0.3242
```

### 问题 217：下一个候选对象有销售商品证据但分诊 payload 没有返回证据下钻
#### 现象

```text
Parent ASIN B00K4W4AAA 范围内，next_unhandled_candidate 已正确指向 B06VW5SQ97 / RBK004-RBK004-2 深蓝。
但 next_unhandled_evidence_drilldown 初次只读回查为 null，页面只能看到“下一个未留痕候选”，看不到为什么是这个 ASIN、证据来自哪里、为什么只能人工复核。
```

#### 原因

```text
_candidate_evidence_drilldown 原先只处理 advertised_products、ad_search_term_daily_metrics、ad_placement_daily_metrics，遇到 sales_product_daily_metrics 会直接返回 None。
补第一版 next_unhandled_evidence_drilldown 后，又暴露出对象身份匹配问题：候选摘要里 object_label 是商品名“RBK004-RBK004-2 深蓝”，stable_object_id 才是 ASIN B06VW5SQ97；helper 复用 _stable_object_id(candidate_summary) 时把商品名当稳定 ID，导致回查原始候选证据失败。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- sales_product_daily_metrics 生成销售商品证据摘要，展示订单、销售额、广告订单占比、广告销售占比。
- ad_spend 只按原始快照展示，不单独作为异常结论。
- build_signal_triage_payload 增加 next_unhandled_evidence_drilldown。
- 下一个候选证据回查优先用 signal_id；没有 signal_id 时再退回 stable_object_id。

scripts/inspect_signal_triage.py：
- 同步 next_unhandled_evidence_drilldown 和 signal_id 优先匹配逻辑。

frontend：
- “下一个未留痕候选”候选预检展示 next_unhandled_evidence_drilldown。
```

#### 后续防错规则

```text
任何信号证据回查不得把 object_label 当对象身份键。
页面展示名使用 object_label；业务身份使用 stable_object_id 或人工动作 object_id。
销售商品证据必须允许来自 sales_product_daily_metrics；广告承接结论必须结合广告商品、库存、价格和策略人工复核，不允许从销售强直接跳到自动加投。
```

#### 验证方式

```text
python -m py_compile backend\app\services\signal_triage.py scripts\inspect_signal_triage.py
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
npm run test:signal-ui
npm run build

真实只读回查 parent_asin:B00K4W4AAA：
- next_object=B06VW5SQ97
- next_label=RBK004-RBK004-2 深蓝
- next_drilldown_object=RBK004-RBK004-2 深蓝
- next_drilldown_summary 包含订单 251、广告订单 18、广告订单占比 7.2%、广告销售占比 7.0% 和“不自动执行广告动作”。
```

### 问题 216：推荐对象已留痕时根级人工动作预览仍指向已处理对象

#### 现象

```text
在 Parent ASIN B00K4W4AAA 范围内，推荐对象 B016EXMW02 已经人工留痕并生成 7d / 14d 复盘待办。
系统也正确找到了下一未处理 P0 候选 B06VW5SQ97。
但 /api/signal-triage 和 inspect_signal_triage.py 的根级 manual_action_preview 仍指向 B016EXMW02。
这会让页面或预检把“已处理等待复盘的推荐对象”误读成“下一次应写入的对象”。
```

#### 原因

```text
build_signal_triage_payload 先从 build_review_candidates_payload 读取推荐对象的 manual_action_preview。
随后虽然计算了 next_unhandled_candidate，但没有把根级 manual_action_preview 切换到下一未处理对象。
recommended_manual_status 和 next_unhandled_candidate 两层语义正确，但根级写入预览仍停在旧对象。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- 增加 _next_action_manual_preview。
- 当 recommended_manual_status.has_manual_action=true 且存在 next_unhandled_candidate 时，根级 manual_action_preview 使用 next_unhandled_candidate.manual_action_preview。

scripts/inspect_signal_triage.py：
- 同步相同逻辑，保持 CLI 与 API 口径一致。

测试：
- test_signal_triage_points_to_next_unhandled_candidate_when_recommended_is_waiting 增加根级 manual_action_preview 断言。
- CLI 同名测试同步增加断言。
```

#### 后续防错规则

```text
分诊 payload 中必须区分三类对象：
1. recommended_candidate：算法认为最值得解释的对象。
2. recommended_manual_status：推荐对象当前是否已人工留痕、是否等待复盘。
3. manual_action_preview：下一次人工按钮会写入的对象。

当推荐对象已人工留痕并等待复盘时，manual_action_preview 必须指向 next_unhandled_candidate，而不是继续指向推荐对象。
任何人工动作预检、页面按钮、CLI apply 脚本都必须以 manual_action_preview 作为下一次写入目标。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py::test_signal_triage_points_to_next_unhandled_candidate_when_recommended_is_waiting backend\tests\test_signal_triage_cli.py::test_signal_triage_cli_points_to_next_unhandled_candidate_when_recommended_is_waiting -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py backend\tests\test_manual_action_preflight_cli.py backend\tests\test_manual_action_apply_cli.py -q
npm run build

真实只读验证：
Parent ASIN B00K4W4AAA 下 root_preview_object、next_unhandled_object、preflight_target_object 都为 B06VW5SQ97。
```

### 问题 215：深度分析与分诊脚本的经营问题分类映射不一致

#### 现象

```text
真实近 30 天快照已经能扫描出 178 条信号。
但 inspect_analysis_depth.py 的问题层里出现 140 条“未分类问题”。
其中 136 条来自 product_ad_coverage，4 条来自 advertised_product_efficiency。
这会让深度分析看起来数据量足够，但业务分类仍停留在浅层，运营无法按固定经营问题分诊。
```

#### 原因

```text
inspect_signal_triage.py 已把 product_ad_coverage 归为“销售承接不足”。
inspect_analysis_depth.py 缺少 product_ad_coverage 映射。
两个脚本都缺少 advertised_product_efficiency 映射。
新增信号类型后，经营问题分类映射没有同步到所有分析入口，造成口径漂移。
```

#### 解决方案

```text
scripts/inspect_analysis_depth.py：
- product_ad_coverage -> 销售承接不足。
- advertised_product_efficiency -> 销售承接不足。

scripts/inspect_signal_triage.py：
- advertised_product_efficiency -> 销售承接不足。

backend/tests/test_analysis_depth_cli.py：
- 增加 product_ad_coverage 和 advertised_product_efficiency 的分类回归。
- 断言问题层不出现“未分类问题”。
```

#### 后续防错规则

```text
新增或调整 signal_category 时，必须同步检查所有经营问题分类入口：
- scripts/inspect_analysis_depth.py
- scripts/inspect_signal_triage.py
- 前端展示中涉及经营问题或深度分析的映射

真实快照深度验收不能只看 signal_count 和 depth_score。
必须检查 problem_type_counts 中是否存在“未分类问题”。
只要有“未分类问题”，不能继续扩展页面或新增浅层分析，必须先收口分类口径。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_analysis_depth_cli.py -q
python -m py_compile scripts\inspect_analysis_depth.py scripts\inspect_signal_triage.py
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_analysis_depth_cli.py backend\tests\test_signal_triage_cli.py backend\tests\test_snapshot_expansion_plan_cli.py -q

真实快照只读验证：
inspect_analysis_depth.py --market-id 1 的 problem_type_counts 不包含“未分类问题”。
```

### 问题 214：浏览器验收 fixture 绕开真实候选逻辑会造成浅层通过

#### 现象

```text
构造 ready 复盘隔离样本时，最初手写了 build_review_candidates_payload 的返回值。
接口能返回 ready_count=1 和 rule_improvement.ready_for_manual_review_record。
但 candidate 列表、recommended_candidate 和 manual_action_preview 为空，页面保存按钮依赖的真实前端链路没有被完整覆盖。
```

#### 原因

```text
ready 复盘状态属于复盘层，但页面能否保存复盘记录还依赖候选层：
AI 信号必须先成为可处理候选，候选必须生成只读 manual_action_preview，页面才有明确对象和按钮上下文。
如果 fixture 直接手写聚合 payload，就绕过了候选构建、对象归一化和前端选择逻辑。
```

#### 解决方案

```text
scripts/serve_isolated_runtime_api.py 的 --ready-review-fixture 改为只替换底层数据来源：
- 注入 fixture signal。
- 注入处理前后 signal_rows。
- 注入 product_scope 和 snapshot 状态。
- 复用真实 build_review_candidates_payload 生成候选、推荐对象和 manual_action_preview。
```

#### 后续防错规则

```text
浏览器验收 fixture 只能替换底层数据源，不能绕开要验证的业务组合逻辑。
只要页面按钮依赖 recommended_candidate、manual_action_preview、review_todos 或 review_effect，就必须让这些字段由真实服务函数生成。
验收保存复盘记录时，必须同时确认：
1. 保存前存在可点击的“保存复盘记录”按钮。
2. 保存前规则状态是 ready_for_manual_review_record。
3. 保存后出现 review_record 读回状态。
4. 保存后规则状态是 saved_feedback。
5. 页面仍明确不自动执行广告动作或自动改规则。
```

#### 验证方式

```text
python -m py_compile scripts\serve_isolated_runtime_api.py
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py::test_signal_triage_rule_feedback_after_saving_ready_review_record backend\tests\test_signal_triage_service.py::test_review_readiness_summarizes_saved_review_records_as_rule_feedback -q
npm run build
浏览器打开 http://127.0.0.1:5189/，点击“保存复盘记录”，确认“规则反馈已沉淀”。
```

### 问题 213：/api/signal-triage 未继承隔离运行态根目录

#### 现象

```text
隔离 API 启动脚本会把 routes.MANUAL_ACTION_ROOT / REVIEW_RECORD_ROOT 指向临时目录。
右侧 manual-actions、review-todos、review-records 路由能读写临时目录。
但 /api/signal-triage 的 review_status 原来仍通过 build_review_readiness_payload 读取默认 data/runtime 目录。
测试中临时 action_root 已有 1 条 add_to_review，但 /api/signal-triage 返回 manual_action_count=0。
```

#### 原因

```text
routes.py 只有局部人工动作路由把 MANUAL_ACTION_ROOT / REVIEW_RECORD_ROOT 传给 service。
get_signal_triage 调用 build_signal_triage_payload 时没有传运行态根目录。
signal_triage.build_review_readiness_payload 内部也没有 action_root / review_root 参数，只能读取默认目录。
这会让浏览器隔离验收出现“按钮写入临时目录，分诊摘要读默认目录”的口径漂移。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- build_signal_triage_payload 增加可选 action_root / review_root。
- build_review_readiness_payload 增加可选 action_root / review_root。
- load_manual_actions、load_review_records、build_review_todos、build_review_effect_result 统一使用传入根目录。

backend/app/api/routes.py：
- /api/signal-triage 在 MANUAL_ACTION_ROOT / REVIEW_RECORD_ROOT 被改写时，把根目录传给 build_signal_triage_payload。

为保持默认运行态兼容，只有根目录不是默认值时才显式传参。
```

#### 后续防错规则

```text
凡是本地隔离验收或测试改写 routes.MANUAL_ACTION_ROOT / REVIEW_RECORD_ROOT，所有读取人工动作、复盘待办、复盘记录和规则改进状态的入口必须共享同一根目录。
不能只验证局部 /manual-actions 或 /review-todos；必须同时验证 /api/signal-triage.review_status。
ready 复盘保存前只能显示 ready_for_manual_review_record；保存 review_records 后才能显示 saved_feedback。
saved_feedback 只能进入规则反馈解释层，不能自动执行广告动作或自动改规则。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py::test_signal_triage_route_reads_review_status_from_route_runtime_roots -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py::test_signal_triage_rule_feedback_after_saving_ready_review_record -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
```

### 问题 212：浏览器验收预选 API 信号可能和页面实际选中信号不一致

#### 现象

```text
隔离浏览器验收时，脚本先从 /api/signals 预选了一个 sales_product 信号。
但页面默认选中的第一条队列信号不是这个预选 ID。
浏览器点击“加入复盘”后，页面状态行正确变为“读回一致”，但如果继续用预选 ID 查 /manual-actions，会误得到 0 条，造成假失败。
```

#### 原因

```text
前端队列会按当前商品范围、筛选、排序和 selectedId 回退逻辑决定实际选中信号。
API 预选信号只是候选，不等于页面当前选中对象。
浏览器验收如果不从页面或写后读回反查实际对象，就会把验证目标和真实点击对象错配。
```

#### 解决方案

```text
本轮改用临时 manual_actions.jsonl 和 /api/review-todos 反查实际点击对象。
实际写入对象为：
sales_product / B06VW5SQ97
signal_id=sig-sales-product-ad-weak-gerpgo_market_1_20260615_162042:sales:1:826-Dark Blue:2026-05-17:2026-06-15

验收以实际写后读回为准，而不是以预选 API 信号为准。
```

#### 后续防错规则

```text
浏览器点击验收必须记录三类对象：
1. 页面点击前实际选中对象。
2. 写入后的 manual_actions 对象。
3. review_todos / review_records 读回对象。

三者一致才算闭环验收通过。
不能只用脚本预选的 API 信号 ID 作为验收对象。
```

#### 验证方式

```text
Playwright + 隔离后端 8031 + 隔离前端 5188：
点击“加入复盘”后右侧状态行显示“读回一致”。
tmp/isolated-runtime/manual-actions/manual_actions.jsonl 写入 1 条 add_to_review。
/api/review-todos?market_id=1 返回同一 signal_id 的 7d / 14d 两条待办。
/api/signals/{signal_id}/review-records?... 返回 0 条。
```

### 问题 211：人工动作 API 写后读回缺少同一对象合同测试

#### 现象

```text
前端已经能在点击人工动作后刷新当前 signal 的人工留痕、复盘待办、全局复盘队列和分诊摘要。
但后端 API 层原来缺少一个完整合同测试来证明：
POST manual-actions 后，GET signal review-todos 和 GET global review-todos 读回的是同一 shop / market / object。
```

#### 原因

```text
之前的测试更多覆盖 service 层和 CLI 预检。
这些测试能证明写入逻辑和 post-write 检查，但不能直接覆盖前端真实点击依赖的 API 路由组合。
如果后续改 routes.py 或对象身份映射，可能出现 service 测试通过、前端 API 读回口径断裂的问题。
```

#### 解决方案

```text
在 backend/tests/test_api_routes.py 增加隔离合同测试。
使用 tmp_path 临时替换 MANUAL_ACTION_ROOT / REVIEW_RECORD_ROOT。
通过 TestClient 真实调用：
- POST /api/signals/{signal_id}/manual-actions
- GET /api/signals/{signal_id}/review-todos
- GET /api/review-todos
- GET /api/signals/{signal_id}/review-records

验证 observe / handled / add_to_review 读回 7d / 14d，ignore 读回 0 条待办，并且对象身份一致。
```

#### 后续防错规则

```text
凡是修改 manual-actions、review-todos、review-records 路由或对象身份映射，都必须跑 API 合同测试。
不能只依赖 service 层测试证明前端点击后的读回闭环。
写入后 ready 前仍必须保持 review_records=0，不能提前保存复盘结论。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py::test_manual_action_api_write_then_readback_uses_same_object_identity -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_actions.py backend\tests\test_manual_action_preflight_cli.py -q
```

### 问题 210：点击后读回摘要会和人工按钮争夺首屏空间

#### 现象

```text
为了把点击后真实读回状态接到右侧人工确认区，最初在写后预期摘要下面追加第二条读回摘要。
Playwright 检查显示桌面 1440x900 下读回摘要可以出现，但四个人工按钮被推到首屏外。
这会让人工确认区变成“先看说明、再滚动找按钮”，不利于运营落地。
```

#### 原因

```text
右侧人工确认区同时承载动作入口、写后预期、目标对象和读回状态。
如果每个信息都用独立块展开，会让说明文字抢占动作入口空间。
从业务本质看，按钮是执行人工留痕的入口，读回状态是辅助判断，不应该压过按钮。
```

#### 解决方案

```text
四个人工动作按钮改为一行紧凑按钮，只显示动作和短状态。
完整动作边界保留在 button title 中。
写后预期和点击后读回合并成一行紧凑状态：
预期：复盘类 7d/14d，忽略 0；读回：暂无留痕。

完整读回判断仍由 manualActionReadbackConsistencyText 保留，供测试和 title 使用。
```

#### 后续防错规则

```text
右侧人工确认区新增状态时，必须先保证四个人工按钮在 1440x900 首屏内可见。
按钮只显示命令和短状态，跨按钮共同口径放入紧凑状态行或 title。
凡是涉及 manual_actions / review_todos / review_records 的 UI 文案，必须同时说明是否保存复盘结论，不能暗示 ready 前已有改善判断。
```

#### 验证方式

```text
npm run test:review-ui
npm run test:signal-ui
npm run build

Playwright + 本机 Chrome：
http://127.0.0.1:5175/
桌面 1440x900：4 个人工按钮全部可见，top=820，bottom=862；读回状态行可见，top=870，bottom=896。
移动端 390x844：manualButtonCount=4，无横向溢出。
console error/warn=0。
```

### 问题 209：人工按钮内追加过多说明会把写后预期推到首屏外

#### 现象

```text
为了把四个人工动作的写后结果带回页面，最初尝试在每个按钮内追加第三行“写后预期”。
Playwright 检查显示桌面 1440x900 下人工动作按钮位于首屏下方，摘要和按钮内容不能在运营点击前稳定可见。
```

#### 原因

```text
右侧人工确认区空间有限。
按钮本身已经承载动作名称和动作含义，再把写后预期逐按钮展开，会拉高按钮组高度。
这偏离了 AI Copilot 工作台的第一屏原则：右侧应快速说明人工确认边界，而不是把关键说明藏到滚动后。
```

#### 解决方案

```text
保留按钮内部原有动作含义。
在人工确认区顶部、目标对象信息之前增加一条只读写后预期摘要：
记录观察 / 标记已处理 / 加入复盘生成 7d / 14d 待办；忽略本次不生成待办；均只留痕，不保存复盘结论。

该摘要不是新操作入口，只用于点击前说明 manual_actions / review_todos / review_records 的边界。
```

#### 后续防错规则

```text
右侧人工确认区新增说明时优先使用摘要条，不要把长说明塞进四个按钮。
按钮只承载动作名称和直接意图；跨按钮共同口径放在区域摘要。
任何新增说明都必须用 1440x900 首屏检查是否可见，并用 390px 移动端检查无横向溢出。
```

#### 验证方式

```text
npm run test:review-ui
npm run test:signal-ui
npm run build

Playwright + 本机 Chrome：
http://127.0.0.1:5175/
桌面 1440x900：manualActionWriteExpectationSummary 可见，manualButtonCount=4，console error/warn=0。
移动端 390x844：manualButtonCount=4，无横向溢出，console error/warn=0。
```

### 问题 208：CLI 写后验收默认 add_to_review，无法隔离覆盖四个人工动作口径

#### 现象

```text
apply_manual_action_once.py 原本只能使用分诊 payload 里的 action_type，实际主要是 add_to_review。
这能验证“加入复盘”写后生成 7d / 14d 待办，但不能直接验证：
- 记录观察 observe 写后也应生成 7d / 14d 待办。
- 标记已处理 handled 写后也应生成 7d / 14d 待办。
- 忽略本次 ignore 写后不应进入当前 7d / 14d 复盘待办。

inspect_manual_action_preflight.py 原 post_write 验收默认要求目标对象必须有 2 条复盘待办。
该默认规则会把 ignore 误判成写后阻塞，或让外层 next_action 继续提示等待 7d / 14d 窗口。
```

#### 原因

```text
CLI 写入验收最初只围绕“推荐对象加入复盘”设计，没有把右侧四个人工按钮都视为独立业务动作。
preflight 没有显式 action_type 参数，post_write 验收也没有区分可复盘动作和 ignore。
```

#### 解决方案

```text
scripts/inspect_manual_action_preflight.py：
- 新增 expected_action_type / --action-type。
- observe / handled / add_to_review 写后必须验收 1 条人工留痕和 2 条 7d / 14d 复盘待办。
- ignore 写后必须验收 1 条人工留痕和 0 条当前复盘待办。
- ignore 成功提示不再说等待 7d / 14d 窗口。

scripts/apply_manual_action_once.py：
- 新增 action_type / --action-type。
- 写入前和写入后都把显式动作类型传入 preflight。
- 外层 next_action 复用 post_write 验收结果，避免两层提示漂移。
```

#### 后续防错规则

```text
凡是涉及人工动作写入或写后验收，必须显式回答 action_type。
不能把 add_to_review 当成所有人工按钮的代表。

人工动作写后口径固定为：
- observe / handled / add_to_review：人工留痕 + 7d / 14d 复盘待办 + 0 条 review_records。
- ignore：人工留痕 + 0 条当前复盘待办 + 0 条 review_records。

CLI、页面文案和后端服务必须共享同一口径。
任何一层提示“ignore 等待 7d / 14d 复盘”都视为错误。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_action_apply_cli.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_action_preflight_cli.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_actions.py -q
$env:PYTHONPATH='backend'; python -m py_compile scripts\inspect_manual_action_preflight.py scripts\apply_manual_action_once.py

隔离脚本真实入口：
--action-type observe 写入临时 action_root 后 target_review_todo_count=2，target_review_record_count=0。
--action-type ignore 写入临时 action_root 后 target_review_todo_count=0，target_review_record_count=0。
```

### 问题 207：人工动作按钮说明没有完整承接 7/14 天复盘和不自动执行边界

#### 现象

```text
右侧人工确认区有四个按钮：加入复盘、记录观察、标记已处理、忽略本次。
页面总标题已经说明“只记录运营判断，不自动执行广告动作”，但单个按钮说明仍偏简略：
- 记录观察：只记录当前观察；暂不判断处理效果。
- 标记已处理：记录人工已处理；效果要等 ready 复盘。
- 加入复盘：生成 7/14 天复盘待办；不代表已有改善结论。
- 忽略本次：不进入当前复盘待办；信号和证据保留。

这些说明没有全部直接说明是否进入 7/14 天复盘，也没有逐项说明不会自动执行广告动作。
```

#### 原因

```text
按钮文案只从前端交互角度描述动作，没有回到后端真实行为：
observe、handled、add_to_review 都属于可复盘人工动作，会生成 7d / 14d 复盘待办；ignore 不进入当前复盘待办。
如果按钮说明不写清楚，运营容易把“标记已处理”或“加入复盘”误读成自动广告操作或立即产生效果结论。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- manualActionIntentText 的四个动作全部补齐人工动作边界。
- add_to_review / handled / observe 明确生成 7/14 天复盘待办。
- ignore 明确不进入当前 7/14 天复盘待办。
- 四个动作都明确不自动执行广告动作。
```

#### 后续防错规则

```text
任何人工动作按钮文案都必须同时回答：
1. 这是人工动作，不是自动广告动作。
2. 是否进入 7/14 天复盘待办。
3. 是否已经能判断改善、无变化或恶化。

如果按钮说明答不清楚，不允许进入页面交付。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
结果：通过。

cd frontend; npm run test:signal-ui
结果：通过。

cd frontend; npm run build
结果：通过。

浏览器打开 http://127.0.0.1:5179/：
右侧人工确认区 4 个按钮均显示“不自动执行广告动作”；加入复盘、记录观察、标记已处理显示生成 7/14 天复盘待办；忽略本次显示不进入当前 7/14 天复盘待办。
```

### 问题 206：已留痕推荐对象卡片仍使用“建议优先留痕”口径

#### 现象

```text
B016EXMW02 已经人工留痕并生成复盘待办，页面状态文本也说明“推荐对象已人工留痕并等待复盘”。
但左侧卡片标题仍显示“建议优先留痕”，按钮 aria-label 仍是“查看建议优先留痕对象 B016EXMW02”。
这会让运营误以为 B016EXMW02 仍是当前要重复加入复盘的对象。
```

#### 原因

```text
推荐对象卡片文案没有读取 recommended_manual_status.has_manual_action。
卡片标题和按钮语义固定使用未处理状态，没有跟随人工留痕 / 复盘等待状态变化。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/signalUi.ts 新增 recommendedManualActionCardCopy：
- 推荐对象已有人工动作时，输出“推荐对象复盘等待 / 查看复盘对象”。
- 推荐对象尚未人工动作时，保留“建议优先留痕 / 查看信号”。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx 使用该 helper 驱动卡片 aria-label、标题、按钮文本和按钮 aria-label。
```

#### 后续防错规则

```text
任何推荐对象卡片都必须先判断 manual status：
1. has_manual_action=false：可以表达为建议优先留痕。
2. has_manual_action=true：必须表达为复盘等待或复盘对象，不能再表达为待留痕对象。

推荐对象和下一个未留痕候选必须分开展示。
```

#### 验证方式

```text
cd frontend; npm run test:signal-ui
结果：通过。

cd frontend; npm run build
结果：通过。

浏览器打开 http://127.0.0.1:5179/：
首屏显示“推荐对象复盘等待 / 查看复盘对象”，不再显示“建议优先留痕”作为 B016EXMW02 的当前卡片口径。
```

### 问题 205：推荐对象已留痕时深度分析仍解释已处理对象

#### 现象

```text
真实分诊状态中，推荐对象 B016EXMW02 已经人工留痕并生成 7d / 14d 复盘待办。
同时，Parent ASIN B00K4W4AAA 范围下仍存在下一个未留痕候选 B06VW5SQ97。
前端“诊断依据”的深度分析原本只读取 recommended_candidate，因此仍输出 B016EXMW02 的“机会扩量”，没有解释当前真正可处理的 B06VW5SQ97。
```

#### 原因

```text
signalTriageDepthText 把“系统推荐对象”和“当前可处理候选”当成同一个对象。
在推荐对象已进入等待复盘状态后，深度分析继续解释推荐对象，会削弱下一步行动判断。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/signalUi.ts：
- next_unhandled_candidate 类型补齐深度分析字段。
- 当 recommended_manual_status.has_manual_action=true 且存在 next_unhandled_candidate 时，signalTriageDepthText 优先解释下一个未留痕候选，并加上“待处理候选”前缀。
```

#### 后续防错规则

```text
分诊摘要必须区分两类对象：
1. 推荐对象：可能已经人工留痕并等待复盘。
2. 待处理候选：当前仍可由运营人工确认的下一个对象。

当二者不一致时，行动相关的深度分析必须优先解释待处理候选；已留痕推荐对象只能作为复盘等待状态展示。
```

#### 验证方式

```text
cd frontend; npm run test:signal-ui
结果：通过。

cd frontend; npm run build
结果：通过。

浏览器打开 http://127.0.0.1:5179/，展开“诊断依据”：
深度分析显示“待处理候选 RBK004-RBK004-2 深蓝 / 经营问题 销售承接不足”，不再显示已留痕推荐对象的“机会扩量”作为当前行动口径。
```

### 问题 204：浏览器验证容易误看旧 Vite 或旧后端端口

#### 现象

```text
本轮后端 8014 已经返回 problem_type、evidence_strength、attribution_boundary、review_path，但浏览器打开 5177 时仍显示“深度分析：暂无完整口径”。
进一步检查发现 5177 并不是新启动的前端实例；Vite 因端口占用自动递增到了 5179，而 5177 仍连接旧后端 8010。
```

#### 原因

```text
开发环境同时存在多个 Vite / FastAPI 进程。
如果只按请求端口验证，而不读取 Vite 实际输出的 Local URL，也不检查前端请求的 API base，就会把旧进程状态误判成新代码问题。
```

#### 解决方案

```text
本轮改用实际可访问的 http://127.0.0.1:5179/ 做渲染验证，并确认页面请求走的是 http://127.0.0.1:8014/api/...。
展开“诊断依据”后，页面能显示深度分析文本。
```

#### 后续防错规则

```text
浏览器验证前必须确认三件事：
1. Vite 实际 Local URL。
2. 页面真实请求的 API base。
3. 目标字段在接口和页面中是否同时出现。

不能只凭“我打开了某个端口”判断前端是否已使用新代码。
```

#### 验证方式

```text
http://127.0.0.1:5179/ 请求 http://127.0.0.1:8014/api/signal-triage?...
展开“诊断依据”后显示：
深度分析：经营问题 机会扩量 / 证据强度 中 / 复盘路径 待人工动作 / 归因边界 搜索词和广告位只说明同广告组上下文，不能自动归因到该广告 ASIN。
```

### 问题 203：分诊候选摘要没有承接深度分析五要素

#### 现象

```text
项目规划和深度分析框架已经要求每条候选能回答经营问题、证据强度、归因边界和复盘路径。
但 signal triage 的 recommended_candidate / top_candidates 摘要原本只返回 priority、confidence、severity、evidence_count、freshness_status 等字段。
前端“诊断依据”可以展示分诊层级和证据，但不能稳定回答“这到底是什么业务问题、证据够不够、能归因到哪里、下一步如何复盘”。
```

#### 原因

```text
深度分析框架停留在规划和局部 inspection 口径里，_candidate_summary 没有把固定分析字段作为候选摘要契约输出。
前端类型和展示函数也没有消费这些字段，导致 UI 容易退化成浅层信号列表。
```

#### 解决方案

```text
backend/app/services/signal_triage.py 和 scripts/inspect_signal_triage.py 同步补充：
- problem_type
- evidence_strength
- attribution_boundary
- review_path

frontend/src/api.ts、frontend/src/pages/SignalTriageWorkbench/signalUi.ts 和 SignalTriageWorkbench.tsx 同步承接，在“诊断依据”折叠区展示深度分析文本。
```

#### 后续防错规则

```text
任何用于推荐、排序或人工动作预检的候选摘要，必须能回答：
1. 经营问题是什么。
2. 证据强度是多少。
3. 归因边界在哪里。
4. 复盘路径是什么。

如果这四项缺失，不允许把它当成完整的 AI 信号候选输出给运营。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
结果：27 passed。

cd frontend; npm run test:signal-ui
结果：通过。

cd frontend; npm run build
结果：通过。

$env:PYTHONPATH='backend'; python scripts\inspect_signal_triage.py --market-id 1 --top 5 --product-scope-id parent_asin:B00K4W4AAA
结果：recommended_candidate B016EXMW02 输出 problem_type=机会扩量、evidence_strength=中、review_path=待人工动作、attribution_boundary=搜索词和广告位只说明同广告组上下文。
```

### 问题 202：复盘 readiness 长 JSON 不利于每轮目标复核

#### 现象

```text
scripts/inspect_review_readiness.py 只能输出完整 JSON。
目标模式每轮复盘时，运营真正需要先看的是 status、manual_actions、review_records、ready/not_ready、最新快照、规则改进状态、下一步和待复盘对象。
完整 JSON 信息量大，容易漏看“窗口未到期”这一业务结论，从而误以为需要继续拉数据或保存复盘。
```

#### 原因

```text
脚本原先服务于深度诊断，没有给目标模式的高频复盘提供精简只读视图。
复盘闭环现在需要先判断能不能落地，而不是每次都展开所有字段。
```

#### 解决方案

```text
scripts/inspect_review_readiness.py 新增 --compact。
compact 只改变输出形态，不改变 build_review_readiness_payload 的计算逻辑，不写入 manual_actions，不写入 review_records，不触发 API。
```

#### 后续防错规则

```text
每轮目标复盘先运行 compact 视图回答三问：
1. 当前的数据口径真的清晰吗？
2. 数据关系梳理清楚了吗？
3. 真的能落地到业务上吗？

只有 compact 指出 ready 或具体数据缺口时，再进入完整 JSON 或数据链路诊断。
如果 reason 是 waiting_review_window，未到期前不拉快照、不保存复盘结论。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_review_readiness_cli.py -q
结果：6 passed。

$env:PYTHONPATH='backend'; python -m py_compile scripts\inspect_review_readiness.py
结果：通过。

$env:PYTHONPATH='backend'; python scripts\inspect_review_readiness.py --market-id 1 --compact
结果：status=not_ready，manual_actions=4，review_records=0，ready=0，not_ready=6，rule_improvement=waiting_review_window。
```

### 问题 201：规则改进读回未承接复盘记录对象身份
#### 现象

```text
API ReviewRecord 已经包含 action_id、action_type、shop_id、market_id、object_type、object_id、object_label 和 review_window。
但前端 ReviewRecordForUi 只定义 result 和 review_note。
buildRuleImprovementReadiness 在“规则反馈已沉淀”状态下只说明 improved / no_change / worse 的处理方向，无法说明这条规则反馈来自哪条人工动作、哪个店铺站点、哪个稳定业务对象和哪个复盘窗口。
```

#### 原因

```text
复盘效果对象身份已经补齐到 review-effect，但保存后的 review_record 前端类型没有同步承接。
规则改进层只消费结果枚举，忽略了结果背后的业务对象边界。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- ReviewRecordForUi 补齐保存记录身份字段。
- 新增 reviewRecordTargetReadbackText。
- buildRuleImprovementReadiness 的 saved 状态追加复盘记录读回。

frontend/tests/reviewUi.test.ts：
- 增加 saved review_record 到规则改进的回归测试。
```

#### 后续防错规则

```text
任何进入“规则反馈 / 规则改进 / 同类信号校准”的复盘记录，都必须能读回：
1. action_type / action_id。
2. shop_id / market_id。
3. object_type / object_id / object_label。
4. review_window。

不能只用 improved / no_change / worse 这类结果枚举驱动规则解释。
规则改进只进入解释层，不能自动改广告动作，也不能自动改规则。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
```

### 问题 200：前端复盘效果未承接后端对象身份字段
#### 现象

```text
后端 ReviewEffectResult 已经返回 action_id、action_type、shop_id、market_id、object_type、object_id、object_label。
但前端 ReviewEffectForUi 仍只定义 status、result、message、窗口和指标字段。
右侧“复盘待办”区只能通过 nextReviewTodo 间接看到对象，不能确认当前 review effect 自身来自哪条人工动作和哪个稳定业务对象。
```

#### 原因

```text
后端 readiness / review-effect 字段补齐后，前端 UI 辅助类型没有同步扩展。
这会让页面展示重新退化成“状态 + 窗口 + 指标”，缺少人工动作和对象身份读回。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- ReviewEffectForUi 补齐 action_id、action_type、shop_id、market_id、object_type、object_id、object_label 等字段。
- 新增 reviewEffectTargetReadbackText，用 effect 自身输出店铺、站点、对象、人工动作和窗口。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- 右侧“复盘待办”区展示 reviewEffectTargetReadbackText(selectedReviewEffect)。
```

#### 后续防错规则

```text
任何复盘效果只要进入前端展示，都必须同时能读回：
1. 人工动作 action_type / action_id。
2. 店铺与站点 shop_id / market_id。
3. 稳定业务对象 object_type / object_id / object_label。
4. review_window。

不能只依赖当前 selectedSignal 或 nextReviewTodo 推断 review effect 的业务对象。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build

浏览器验证：
http://127.0.0.1:5175/ 切到“查看建议优先留痕对象 B016EXMW02”后，右侧复盘待办显示 advertised_product / B016EXMW02、add_to_review / manual-action...、窗口 7 天。
```

### 问题 199：review readiness effect 压缩输出丢失复盘对象身份
#### 现象

```text
build_review_effect_result 已经返回 action_id、action_type、shop_id、market_id、object_type、object_id、object_label 和 review_window。
但 scripts/inspect_review_readiness.py 与 backend/app/services/signal_triage.py 在构造 readiness effects 时，只保留了 market_id、object_type 和 object_id。
CLI、后端分诊摘要或后续前端读取 readiness effects 时，无法直接判断某条 ready / not_ready 属于哪次人工动作、哪个店铺和哪个对象标签。
```

#### 原因

```text
effect 从领域对象压缩为 payload 时，只保留了状态、窗口和部分对象字段。
这让复盘对象身份在跨层传递时被削弱，容易回到只看 signal_id 或 object_id 的旧问题。
```

#### 解决方案

```text
readiness effects 统一补齐：
- action_id
- action_type
- shop_id
- object_label

保留原有 market_id、review_window、object_type、object_id、状态、窗口和日期字段。
```

#### 后续防错规则

```text
任何 ready / not_ready 复盘效果输出，都必须能回答：
1. 来自哪条人工动作 action_id？
2. 属于哪个 shop_id / market_id？
3. 属于哪个 object_type / object_id / object_label？
4. 对应哪个 review_window？

不能只用 signal_id 或 object_id 作为复盘展示和判断依据。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_review_readiness_cli.py::test_review_readiness_reports_ready_effect -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py::test_review_readiness_next_action_waits_when_review_effects_are_not_due -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_review_readiness_cli.py backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py backend\tests\test_api_routes.py -q
```

### 问题 198：复盘效果 ready 必须同时满足窗口到期和数据窗口完整
#### 现象

```text
build_review_effect_result 原本只根据处理前后数据窗口是否完整来判断 ready。
如果测试数据或未来补数已经覆盖了处理后 7 天 / 14 天窗口，但当前时间还没到 due_at，函数也可能返回 ready。
随后 save_review_record 会允许保存复盘记录，导致运营提前沉淀“处理有效 / 无效”结论。
```

#### 原因

```text
复盘 ready 的业务口径缺少“当前时间 >= due_at”的硬门槛。
数据窗口完整只能证明样本覆盖，不能证明复盘周期已经自然完成。
```

#### 解决方案

```text
build_review_effect_result 在计算处理前后指标前先检查 current_time < due_at。
未到期时返回 not_ready，并提示复盘窗口尚未到期。
save_review_record 继续只接受 ready effect，因此未到期复盘不能保存 review_records。
```

#### 后续防错规则

```text
复盘记录保存前必须同时满足：
1. 当前时间已经到达 review_window 对应 due_at。
2. 处理前窗口完整。
3. 处理后窗口完整。
4. 复盘对象身份字段明确。

不能只因为测试数据或快照数据覆盖了日期范围，就提前输出 improved / no_change / worse。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_actions.py::test_review_effect_waits_until_review_window_due_even_when_rows_exist -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_actions.py backend\tests\test_review_readiness_cli.py backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
```

### 问题 197：post-write 只返回数量不能证明读回身份一致
#### 现象

```text
人工动作写入后的 post-write 验收原本只返回：
- target_manual_action_count
- target_review_todo_count
- target_review_windows
- target_review_record_count

这些字段能说明“数量看起来对”，但不能直接证明读回的 manual_actions 和 review_todos 落在同一 shop_id、market_id、object_type、object_id 和 object_label 上。
```

#### 原因

```text
写入链路已经把目标身份写进 manual_actions，build_review_todos 也会继承这些字段。
但 inspect_manual_action_preflight.py 的 post_write_checks 没有把读回记录的身份字段暴露出来，验证人员只能靠数量推断对象是否正确。
```

#### 解决方案

```text
post_write_checks 增加以下只读身份明细：
- target_manual_action_identities
- target_review_todo_identities
- target_review_record_identities

每条身份明细至少包含：
signal_id / shop_id / market_id / object_type / object_id / object_label。
复盘待办和复盘记录额外包含 review_window。
```

#### 后续防错规则

```text
人工留痕写入后的验收不能只看 count。
凡是判断“已写入 / 已生成复盘待办 / 已可复盘”，都必须能读回目标身份字段。
如果数量正确但目标身份不一致，本轮仍然视为失败。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_action_preflight_cli.py backend\tests\test_manual_action_apply_cli.py -q
隔离运行 scripts\apply_manual_action_once.py，使用临时 action_root / review_root，确认 post_write_validation.post_write_checks 返回同一 sales_product / B06VW5SQ97、shop_id=market:1、market_id=1 的 manual_actions 和 7d / 14d review_todos。
```

### 问题 196：复盘待办读回不能只按稳定对象 ID 匹配
#### 现象

```text
前端 selectReviewTodosForSignal 在没有直接 signal_id 待办时，会从当前商品范围的复盘待办中按稳定对象 ID 回连历史待办。
原逻辑只检查 object_id 是否命中当前信号的 ASIN / label / object_id，没有检查 shop_id、market_id 和 object_type。
在同一 market 下多店铺存在同 ASIN 时，当前信号可能误读到其他店铺的人工动作或复盘待办。
```

#### 原因

```text
ReviewTodoForUi 和 ReviewSignalForUi 之前没有把 shop_id / market_id 作为前端读回匹配的一等字段。
后端 ReviewTodo 已经携带 shop_id、market_id、object_type、object_id，但前端工具函数没有使用这些字段做一致性门槛。
```

#### 解决方案

```text
ReviewTodoForUi 增加 shop_id / market_id / object_type。
ReviewSignalForUi 增加 shop_id / market_id。
selectReviewTodosForSignal 除稳定 object_id 外，同时排除 shop_id、market_id、object_type 冲突的复盘待办。
处理留痕和复盘待办区统一使用 reviewTargetReadbackText 展示读回目标，避免按钮前目标和写入后目标脱节。
```

#### 后续防错规则

```text
人工动作、复盘待办、复盘记录的前端读回匹配，不能只看 object_id。
至少要检查：
1. shop_id 不冲突。
2. market_id 不冲突。
3. object_type 不冲突。
4. object_id 命中稳定业务对象。

缺字段时可以兼容旧记录，但只要两侧都有字段且发生冲突，就必须排除。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
浏览器打开 http://127.0.0.1:5175/，只读检查右侧人工确认区没有控制台 error。
```

### 问题 195：右侧人工确认按钮前缺少稳定店铺和目标摘要
#### 现象

```text
右侧人工确认区域原本有“复盘对象 / 对象ID / 窗口”等片段信息，但没有在按钮前集中展示 shop_id / shop_name / market_id / object_type / object_id。
浏览器检查还发现，当选中信号不是后端推荐候选或下一个候选时，摘要会退化为“当前信号暂无后端目标预检”，运营仍看不到最终留痕店铺和对象。
```

#### 原因

```text
前端只把 manual_action_preview 当作目标摘要来源。
但真实页面存在三种来源优先级：
1. 后端推荐候选的 manual_action_preview。
2. next_unhandled_candidate 的 manual_action_preview。
3. 当前选中信号自身的 evidence.primary_object 和 signal.shop_id。

第三种回退缺失，导致普通选中信号无法显示完整人工目标。
```

#### 解决方案

```text
RecommendedManualActionPreview 增加 shopId / shopName / marketId。
manualActionPreviewForSelectedSignal 增加 selectedSignal 回退来源。
没有后端预检时，从当前信号 evidence.primary_object 和 shop_id 生成只读目标预览。
右侧人工确认区新增 manualActionTargetSummary，点击按钮前统一显示店铺、站点、对象、窗口和不自动执行边界。
```

#### 后续防错规则

```text
每个可点击人工按钮前必须先可见回答：
1. 写到哪个 shop_id？
2. 写到哪个 market_id？
3. 写到哪个 object_type / object_id？
4. 是否只是人工留痕，不会自动执行广告动作？

如果任一项不可见，本轮不能算通过。
```

#### 验证方式

```text
cd frontend; npm run test:signal-ui
cd frontend; npm run build
浏览器打开 http://127.0.0.1:5175/，检查 .manualActionTargetSummary 是否包含店铺、站点、对象、窗口和“不自动执行广告动作”，且控制台无 error。
```

### 问题 194：前端只说明 Parent ASIN 筛选范围会弱化人工处理目标边界
#### 现象

```text
工作台顶部已经能按 Parent ASIN 收敛商品范围，但原提示主要解释“先按 Parent ASIN 收敛到经营商品组”，没有直接说明人工处理目标不能使用 Parent ASIN 范围 ID，也不能把子 ASIN 当作 product_scope_id。
```

#### 原因

```text
前端 ProductScopeSelectionSummary 只有 title / description / tone，缺少独立的 targetBoundary。
这会让“分析范围”和“人工动作目标”在运营视角上混在一起。
```

#### 解决方案

```text
ProductScopeSelectionSummary 增加 targetBoundary。
Parent ASIN 入口显示：分析范围是 Parent ASIN；人工处理目标必须落到子 ASIN / 销售商品 / 广告 ASIN，不要把子 ASIN 当作 product_scope_id。
同时修正商品范围提示条 flex 压缩，避免新增边界文案后标题逐字换行。
```

#### 后续防错规则

```text
任何商品范围入口都必须同时回答两个问题：
1. 当前 product_scope_id 代表什么分析范围？
2. 人工确认和复盘最终写到哪个 object_type / object_id？

如果只说明范围，不说明处理目标，本轮口径不算通过。
```

#### 验证方式

```text
cd frontend; npm run test:signal-ui
cd frontend; npm run build
浏览器打开 http://127.0.0.1:5175/，检查商品范围条是否包含“人工处理目标必须落到子 ASIN / 销售商品 / 广告 ASIN”，且提示条没有明显换行异常。
```

### 问题 193：不能把子 ASIN 当作 Parent ASIN 范围筛选
#### 现象

```text
使用 --product-scope-id B06VW5SQ97 检查人工动作预检时，分诊 payload 返回 target={}，preflight 状态为 blocked，错误原因是 missing_target。看起来像写入器无法定位 B06VW5SQ97。
```

#### 原因

```text
B06VW5SQ97 是目标销售商品的稳定对象 ID，不是产品范围 ID。当前分诊入口按 Parent ASIN 范围聚合候选，正确筛选范围是 product_scope_id=parent_asin:B00K4W4AAA；在这个范围内，next_unhandled_candidate 才会落到 sales_product / B06VW5SQ97。
```

#### 解决方案

```text
隔离写入和 preflight 验证统一使用：
--product-scope-id parent_asin:B00K4W4AAA
--expected-object-id B06VW5SQ97
--expected-object-type sales_product

同时在 apply / preflight 脚本里增加 action_root / review_root，用临时目录验证真实写入门禁，不污染默认运行数据。
```

#### 后续防错规则

```text
每轮涉及商品候选或人工动作时必须先回答：
1. 当前的数据口径真的清晰吗？特别区分 Parent ASIN 范围、子 ASIN、销售商品、广告商品。
2. 数据关系梳理清楚了吗？特别区分 product_scope_id、object_type、object_id、shop_id、market_id。
3. 真的能落地到业务上吗？运营看到的是范围内的业务候选，不是底层快照行 ID 或错误筛选 ID。

如果 preflight missing_target，先检查 product_scope_id 是否是范围 ID，再检查候选生成，不要直接怀疑写入器。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id B06VW5SQ97 --expected-object-type sales_product --top 10 --compact
$env:PYTHONPATH='backend'; python scripts\apply_manual_action_once.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id B06VW5SQ97 --expected-object-type sales_product --top 10 --execute --authorization-code WRITE_ONCE:sales_product:B06VW5SQ97 --action-root <TEMP_ACTION_ROOT> --review-root <TEMP_REVIEW_ROOT> --compact
```

### 问题 192：真实候选源缺少 shop_id 会削弱同店铺人工状态匹配

#### 现象

```text
服务层和 inspect_signal_triage.py 已经增加同店铺匹配门槛。
但真实运行 inspect_signal_triage.py 时，recommended_manual_status.shop_id 仍为 None。
这会让人工留痕、复盘待办和下一个未留痕候选缺少明确店铺上下文。
```

#### 原因

```text
inspect_signal_triage.py 的推荐对象来自 scripts/inspect_review_candidates.py。
该脚本的 candidate_payload 和 manual_action_preview 只传 market_id，没有传 shop_id / shop_name。
下游即使有 _matches_shop，也缺少目标店铺输入。
```

#### 解决方案

```text
scripts/inspect_review_candidates.py：
- candidate_payload 增加 shop_id / shop_name。
- manual_action_preview 增加 shop_id / shop_name。

frontend/src/api.ts 和 frontend/src/pages/SignalTriageWorkbench/signalUi.ts：
- recommended_manual_status 类型补齐 shop_id / shop_name。

backend/tests/test_review_candidates_cli.py：
- 增加候选和 manual_action_preview 店铺字段透传断言。
```

#### 后续防错规则

```text
凡是进入人工动作、复盘待办或复盘效果匹配的对象，都必须同时带 object_type / object_id / market_id / shop_id。
market_id 只能表达站点或市场筛选，不能替代店铺上下文。
修复下游匹配逻辑后，必须同时检查上游候选源是否传递 shop_id。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_review_candidates_cli.py backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_action_preflight_cli.py backend\tests\test_manual_action_apply_cli.py -q
$env:PYTHONPATH='backend'; python -m compileall backend\app\services scripts backend\tests -q
cd frontend; npm run test:signal-ui
cd frontend; npm run build
$env:PYTHONPATH='backend'; python scripts\inspect_signal_triage.py --market-id 1 --top 5 --product-scope-id parent_asin:B00K4W4AAA
```

### 问题 191：post-write 验收不能用跨店铺人工动作满足目标对象

#### 现象

```text
inspect_manual_action_preflight.py 的 post-write 验收按 object_type / object_id / market_id 匹配目标记录。
如果其他店铺存在同 ASIN、同 market_id 的 manual_action 或 review_todo，或者历史记录缺少 shop_id，脚本可能把它误算为当前 shop_id=market:1 的写入结果。
```

#### 原因

```text
_matches_target 没有比较 shop_id。
market_id 只能表示站点或市场筛选，不能唯一表达店铺上下文。
多店铺项目里，同一个 ASIN 可以在多个店铺出现，缺 shop_id 的历史记录也不能证明当前店铺闭环已完成。
```

#### 解决方案

```text
scripts/inspect_manual_action_preflight.py：
- 当 target 带 shop_id 时，_matches_target 要求 record.shop_id 与 target.shop_id 完全一致。

backend/tests/test_manual_action_preflight_cli.py：
- 增加错误 shop_id 的 post-write 失败用例。
- 增加缺 shop_id 的 post-write 失败用例。
- 合法 post-write 成功用例补齐 shop_id。
```

#### 后续防错

```text
人工动作、复盘待办、复盘记录的目标匹配必须同时看稳定对象 ID、market_id 和 shop_id。
跨店铺记录不能满足当前店铺验收。
缺 shop_id 的记录不能作为多店铺复盘闭环完成证据。
```

#### 验证方式

```text
PYTHONPATH=backend python -m pytest backend\tests\test_manual_action_preflight_cli.py -q
PYTHONPATH=backend python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_manual_action_preflight_cli.py backend\tests\test_manual_action_apply_cli.py -q
PYTHONPATH=backend python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id B06VW5SQ97 --expected-object-type sales_product --expect-written --compact
```

### 问题 190：人工动作预检不能只带 market_id，必须保留 shop_id

#### 现象

```text
apply_manual_action_once.py 的 dry-run 能定位 B06VW5SQ97，并且 preflight 能返回 market_id=1。
但 target 里没有 shop_id / shop_name。
如果真实写入，save_manual_action 会收到 shop_id=None，导致 manual_actions 后续复盘缺少店铺上下文。
```

#### 原因

```text
signal_triage 的候选 payload 只透出了 market_id / marketplace，没有透出 AiSignal 自身的 shop_id / shop_name。
后续 manual_action_preview、preflight target 和 apply 执行器都只能沿用不完整上下文。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- candidate、candidate summary、manual_action_preview 全部透出 shop_id / shop_name。

scripts/inspect_manual_action_preflight.py：
- target 透出 shop_id / shop_name。
- pre_write 和 post_write 都在缺 shop_id 时 blocked。

scripts/apply_manual_action_once.py：
- 沿用 preflight target 的 shop_id 写入 save_manual_action。
```

#### 后续防错

```text
人工动作和复盘待办必须同时具备稳定对象 ID、shop_id、market_id。
market_id 不能替代 shop_id；它只是站点 / market 过滤键，不是完整店铺上下文。
后续新增任何人工动作入口，都必须检查 shop_id 是否从 AiSignal 透传到 ManualActionRecord。
```

#### 验证方式

```text
PYTHONPATH=backend python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_manual_action_preflight_cli.py backend\tests\test_manual_action_apply_cli.py -q
PYTHONPATH=backend python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id B06VW5SQ97 --expected-object-type sales_product --compact
PYTHONPATH=backend python scripts\apply_manual_action_once.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id B06VW5SQ97 --expected-object-type sales_product --compact
```

### 问题 189：真实人工留痕不能靠手工调用保存函数绕过门禁

#### 现象

```text
写入前和写入后验收脚本已经能证明 B06VW5SQ97 的对象口径和复盘待办预期。
但如果后续真实写入时由开发者手工调用 save_manual_action，仍可能绕过 preflight、重复写入检查和 post-write 验收。
```

#### 原因

```text
save_manual_action 是底层持久化函数，只负责追加 JSONL。
它本身不知道当前目标是否是下一条未留痕候选、是否匹配 Parent ASIN 范围、是否具备完整 7d / 14d 复盘窗口，也不会自动做写后验收。
```

#### 解决方案

```text
新增 scripts/apply_manual_action_once.py：
- 默认只 dry-run，不写入。
- 写入前必须复用 inspect_manual_action_preflight.py 的 ready_for_explicit_manual_write 结果。
- 真实写入必须提供对象绑定授权码 WRITE_ONCE:{object_type}:{object_id}。
- 写入后立即复用 --expect-written 口径做 post-write 验收。
```

#### 后续防错

```text
真实人工留痕只能走受控执行器，不直接手工调用 save_manual_action。
没有明确授权码时，即使带 --execute 也必须 blocked。
写入后验收通过只表示进入等待 7d / 14d 复盘窗口，不能生成处理改善结论。
```

#### 验证方式

```text
PYTHONPATH=backend python -m pytest backend\tests\test_manual_action_apply_cli.py -q
PYTHONPATH=backend python scripts\apply_manual_action_once.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id B06VW5SQ97 --expected-object-type sales_product --compact
PYTHONPATH=backend python scripts\apply_manual_action_once.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id B06VW5SQ97 --expected-object-type sales_product --execute --compact
```

### 问题 188：授权写入后不能只看请求成功，必须做复盘闭环验收

#### 现象

```text
写入前预检能证明 B06VW5SQ97 可以进入人工留痕，但这只能证明“允许写”。
如果真实写入后只看请求成功，不检查目标对象人工留痕和 7d / 14d 复盘待办，就无法证明闭环真的进入复盘阶段。
```

#### 原因

```text
项目目标不是保存一条 manual_action，而是跑通“人工确认 -> 7 天 / 14 天复盘 -> 规则改进”。
写入请求成功、前端状态变化、按钮反馈都不是稳定业务证据。
```

#### 解决方案

```text
scripts/inspect_manual_action_preflight.py：
- 增加 --expect-written 只读验收模式。
- 写入后要求目标对象有且仅有 1 条人工留痕。
- 写入后要求目标对象生成完整 7d / 14d 复盘待办。
- 写入后验收阶段要求目标复盘记录仍为 0，避免未到期前保存复盘结论。

backend/tests/test_manual_action_preflight_cli.py：
- 增加写入后通过用例。
- 增加写入后缺待办阻断用例。
```

#### 后续防错

```text
任何真实人工留痕写入后，都必须立刻运行 --expect-written 验收。
验收通过只表示进入等待复盘窗口，不表示处理已改善。
未到 7d / 14d 窗口前，不能保存 review_records，不能生成规则改进结论。
```

#### 验证方式

```text
PYTHONPATH=backend python -m pytest backend\tests\test_manual_action_preflight_cli.py -q
PYTHONPATH=backend python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id B06VW5SQ97 --expected-object-type sales_product --expect-written --compact
```

### 问题 185：到期缺快照测试前提不能使用未到期待办

### 问题 187：写入前预检不能只看对象匹配，还必须确认复盘窗口完整

#### 现象

```text
inspect_manual_action_preflight.py 原先会检查目标对象是否匹配、是否存在身份风险、是否重复人工留痕。
但如果 manual_action_preview.review_windows 只包含 7d，脚本仍会返回 ready_for_explicit_manual_write。
这会导致真实写入后无法生成完整 7d / 14d 复盘待办，闭环缺一半。
```

#### 原因

```text
预检把“能写入一条 manual_action”和“能进入完整复盘闭环”混在一起。
从项目目标看，人工确认不是终点，必须能继续进入 7 天 / 14 天复盘和规则改进。
```

#### 解决方案

```text
scripts/inspect_manual_action_preflight.py：
- _blockers 增加 missing_review_windows。
- 目标人工动作缺少完整 7d / 14d 复盘窗口时，返回 blocked。

backend/tests/test_manual_action_preflight_cli.py：
- 新增缺少 14d 窗口的回归测试。
```

#### 后续防错

```text
任何真实人工留痕写入前都必须同时检查：
- 稳定业务对象 ID。
- 是否已有重复人工留痕。
- 是否存在历史对象身份风险。
- 是否具备完整 7d / 14d 复盘窗口。

只能证明“能写 manual_actions”不等于证明“能跑通复盘闭环”。
```

#### 验证方式

```text
PYTHONPATH=backend python -m pytest backend\tests\test_manual_action_preflight_cli.py -q
PYTHONPATH=backend python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id B06VW5SQ97 --expected-object-type sales_product
```

### 问题 186：范围级规则改进状态不能覆盖单条信号复盘事实

#### 现象

```text
后端 review_status.rule_improvement 已经能说明当前 Parent ASIN / ASIN 范围的规则改进 readiness。
如果前端右侧“规则改进门槛”无条件优先展示该范围级状态，会覆盖选中信号自身的 ready review effect、review_todo 或已保存 review_record。
这样可能出现单条信号已经可以保存复盘记录，但页面仍显示“范围级复盘窗口尚未到期”的错误引导。
```

#### 原因

```text
review_status.rule_improvement 是当前筛选范围的聚合状态。
selectedReviewEffect、latestReviewRecord、nextReviewTodo 是选中信号或稳定业务对象的局部事实。
两者粒度不同，不能简单互相替代。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- buildRuleImprovementReadiness 增加 backendRuleImprovement 可选参数。
- 只有 effect / record / todo 都为空时，才使用后端 rule_improvement 作为兜底。
- 如果后端字段带 can_auto_change_rules=true 或 can_auto_execute_ads=true，前端忽略该字段并回退安全文案。
```

#### 后续防错

```text
前端消费后端聚合状态前必须先确认粒度：
- 范围级状态只能用于范围摘要或无局部事实时的兜底。
- 单条信号详情必须优先使用该信号或稳定业务对象自己的复盘事实。
- 任何规则改进提示都不得暗示自动改规则或自动执行广告动作。
```

#### 验证方式

```text
npm run test:review-ui
npm run test:signal-ui
npm run build
浏览器运行态确认右侧“规则改进门槛”显示后端 waiting_review_window 兜底文案，且不显示自动广告动作文本。
```

### 问题 185：到期缺快照测试前提不能使用未到期待办

#### 现象

```text
扩大运行 review readiness CLI 测试时，test_review_readiness_prioritizes_actionable_ad_product_gap 期望 next_action 优先提示“缺少处理后 7 天快照”。
但测试输入里的所有 review_todo 都是 is_due=false。
当前实现返回“复盘窗口尚未到期；未到期前不拉取快照、不保存复盘结论”。
```

#### 原因

```text
测试名称和断言是在验证“到期后缺快照的优先级”。
但 fixture 把待办设成未到期，和项目复盘规则冲突。
未到期待办不能提示运营拉快照，否则会误导用户在 7/14 天窗口完整前请求积加 API。
```

#### 解决方案

```text
修正 backend/tests/test_review_readiness_cli.py 中该用例的前提：
- 数据质量 7d 待办 is_due=true。
- 广告商品 7d 待办 is_due=true。
- 广告商品 14d 待办 is_due=true。

这样该用例才真正验证“到期但没有 ready 复盘效果时，广告商品处理后 7 天快照缺口优先于 14 天窗口不足和数据质量边界”。
```

#### 后续防错

```text
复盘状态测试必须先明确待办是否到期：
- is_due=false：只能等待复盘窗口，不提示拉快照，不保存复盘结论。
- is_due=true 且缺数据：才提示缺快照或窗口不足。
- ready=true：才允许人工保存复盘记录。

测试名、fixture 和断言必须在这三类状态上保持一致。
```

#### 验证方式

```text
PYTHONPATH=backend python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py backend\tests\test_review_readiness_cli.py -q
结果：27 passed。
```

### 问题 184：下一个未留痕候选定位后右侧预检对象缺失

#### 现象

```text
页面左侧已经能展示并定位下一个未留痕候选 RBK004-RBK004-2 深蓝 / B06VW5SQ97。
中间诊断也能显示该对象是销售商品。
但右侧建议处理仍显示“当前信号暂无后端推荐预检；真实留痕仍由人工按钮触发”。
运营在点击加入复盘前，看不到稳定复盘对象是 sales_product / B06VW5SQ97。
```

#### 原因

```text
右侧 selectedBackendManualActionPreview 只读取 summary.manual_action_preview。
summary.manual_action_preview 只对应推荐对象 B016EXMW02。
后端 next_unhandled_candidate.manual_action_preview 已经返回 B06VW5SQ97，但页面右侧没有消费它。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/signalUi.ts：
- 新增 manualActionPreviewForSelectedSignal。
- 先匹配推荐对象 manual_action_preview，再匹配 next_unhandled_candidate.manual_action_preview。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- 右侧建议处理统一使用 manualActionPreviewForSelectedSignal。
- 预检文案显示复盘对象类型、稳定对象 ID 和 7d / 14d 窗口。
```

#### 后续防错

```text
只要后端提供可处理候选，就不能只在左侧显示定位入口。
选中候选后，右侧人工处理区必须能显示同一稳定业务对象的只读预检。
next_unhandled_candidate 仍然只能作为只读预检，不得自动写 manual_actions、review_records 或执行广告动作。
```

#### 验证方式

```text
npm run test:signal-ui
结果：RED 后 GREEN。

npm run build
结果：通过。

浏览器 DOM 验证：
选中 RBK004-RBK004-2 深蓝 后，右侧预检显示 add_to_review / 复盘对象：销售商品 / 对象ID：B06VW5SQ97 / 窗口：7d / 14d。
控制台 error/warn 为空。
```

### 问题 183：下一个未留痕候选只有文案没有队列定位入口

#### 现象

```text
页面首屏已经能展示：
可继续人工确认下一个未留痕候选 RBK004-RBK004-2 深蓝。

但该候选只出现在推荐对象状态文案里。
运营仍需要自己在 15 条队列中查找对应行，容易停留在已留痕的 B016EXMW02 或误点其他候选。
```

#### 原因

```text
前端只实现了 buildBackendRecommendedManualActionCandidate，用于定位推荐对象 B016EXMW02。
后端新增的 next_unhandled_candidate 只被 recommendedManualStatusText 消费成文案，没有生成独立的队列定位候选。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/signalUi.ts：
- 新增 buildNextUnhandledManualActionCandidate，按 next_unhandled_candidate.manual_action_preview.signal_id 定位当前信号。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- 左侧优先处理提示新增“下一个未留痕候选”卡片。
- “定位候选”只切换选中信号，不调用人工动作保存接口。

frontend/src/api.ts：
- SignalTriageSummary 类型补齐 next_unhandled_candidate。
```

#### 后续防错

```text
如果后端给出 next_unhandled_candidate，页面不能只把它塞进长文案。
应当提供明确但只读的定位入口，帮助运营继续人工分诊。
定位入口不得写 manual_actions，不得保存 review_records，不得执行广告操作。
```

#### 验证方式

```text
npm run test:signal-ui
结果：RED 后 GREEN。

npm run build
结果：通过。

浏览器 DOM 验证：
页面可见“下一个未留痕候选 / RBK004-RBK004-2 深蓝 / 定位候选”。
点击后左侧选中行切换到 RBK004-RBK004-2 深蓝，控制台 error/warn 为空。
```

### 问题 182：页面可见摘要没有消费后端顶层 next_action

#### 现象

```text
后端和 CLI 已经返回：
推荐对象已人工留痕并等待复盘；可继续人工确认下一个未留痕候选 RBK004-RBK004-2 深蓝。

但页面首屏可见摘要仍显示：
B016EXMW02：推荐对象已人工留痕并生成复盘待办；等待 7 天 / 14 天完整窗口后再判断效果。

运营在页面上看不到“下一个未留痕候选”，会继续被 B016EXMW02 的等待复盘状态卡住。
```

#### 原因

```text
recommendedManualStatusText 只读取 recommended_manual_status.next_action。
这个字段描述的是推荐对象 B016EXMW02 自身状态。
顶层 summary.next_action 才描述当前分诊队列的下一步，但页面可见摘要没有优先使用它。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/signalUi.ts：
- 当 recommended_manual_status.has_manual_action=true 时，recommendedManualStatusText 优先展示 summary.next_action。
- 如果没有 summary.next_action，再回退到 recommended_manual_status.next_action 或本地 fallback。

frontend/tests/signalUi.test.ts：
- 增加断言：可见推荐状态文本必须包含“下一个未留痕候选 RBK004-RBK004-2 深蓝”。
```

#### 后续防错

```text
recommended_manual_status.next_action 只说明推荐对象自身状态。
summary.next_action 才说明当前分诊队列下一步。
页面首屏可见区域必须优先展示对运营最可执行的 next_action，不能只展示对象局部状态。
```

#### 验证方式

```text
npm run test:signal-ui
结果：通过。

npm run build
结果：通过。

浏览器 DOM 验证：
页面可见摘要包含“下一个未留痕候选 RBK004-RBK004-2 深蓝”和“不要自动执行广告动作”。
点击“查看建议优先留痕对象 B016EXMW02”后，控制台 warn/error 为空。
```

### 问题 181：推荐对象等待复盘时分诊队列缺少下一个可处理候选

#### 现象

```text
当前 Parent ASIN B00K4W4AAA 范围内，推荐对象 B016EXMW02 已经人工留痕并生成 7d / 14d 复盘待办。
由于复盘窗口未到期，顶层 next_action 只提示等待 7 天 / 14 天完整窗口。
但同一商品组仍有其他候选尚未人工留痕，运营不应该只能停在 B016EXMW02 上等待。
```

#### 原因

```text
分诊推荐只维护一个 recommended_candidate。
当该推荐对象已经进入复盘等待期时，系统没有继续扫描候选队列中尚未人工留痕的下一个对象。
这会让“人工确认 -> 复盘等待”和“继续处理队列中其他候选”两个动作混在一起，降低运营推进效率。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- 新增 next_unhandled_candidate。
- 只有 recommended_manual_status.has_manual_action=true 时，才查找下一个未留痕候选。
- 按现有候选排序和稳定对象口径判断是否已有人工动作。
- 顶层 next_action 在存在未留痕候选时提示继续人工确认该候选，真实写入仍只能由人工按钮触发。

scripts/inspect_signal_triage.py：
- 同步 CLI 只读预检字段和文案。
```

#### 后续防错

```text
推荐对象进入复盘等待，不代表整个分诊队列暂停。
队列下一步必须区分：
1. 推荐对象的复盘状态。
2. 商品组内其他候选是否尚未人工留痕。

next_unhandled_candidate 只能作为只读运营建议，不得自动写 manual_actions，不得自动执行广告动作。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
结果：22 passed。

python -m compileall backend\app scripts backend\tests -q
结果：通过。

只读 CLI smoke：
recommended=B016EXMW02，recommended_has_manual_action=true。
next_unhandled=RBK004-RBK004-2 深蓝，next_unhandled_object=B06VW5SQ97。
next_action 提示继续人工确认下一个未留痕候选，且明确不要自动执行广告动作。
```

### 问题 180：到期缺快照的复盘提示没有统一提醒限流预检

#### 现象

```text
前端复盘详情已经能在缺少处理后快照时提示“先查询积加 API 限流规则，再由人工触发低频快照”。
但 review readiness 服务、readiness CLI 和 signal triage CLI 的下一步提示只说“缺少处理后快照”或“优先处理数据缺口”。
这会让运营在后端/CLI 预检时看不到真实可执行顺序。
```

#### 原因

```text
复盘阻塞文案分散在服务层、readiness CLI、分诊 CLI 和前端。
上一轮已经修正未到期状态不能误导拉快照，但到期缺快照状态没有把“限流预检 -> 人工触发低频快照”统一到所有入口。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- no_ready_review_effect blocker 和 review readiness next_action 在检测到“缺少处理后”时追加限流预检提示。

scripts/inspect_review_readiness.py：
- readiness CLI 的 next_action 使用同一到期缺快照下一步。

scripts/inspect_signal_triage.py：
- 分诊 CLI 的 no_ready_review_effect blocker 使用同一到期缺快照下一步。

测试中固定断言“先查询积加 API 限流规则”和“人工触发低频快照”必须同时出现。
```

#### 后续防错

```text
复盘状态提示必须先区分两类情况：
1. is_due=false：只提示等待窗口完整，不能提示拉快照或限流预检。
2. is_due=true 且缺少处理后快照：必须提示先查询积加 API 限流规则，再由人工触发低频快照。

服务层、CLI 和前端不能各自发明复盘下一步口径。
每轮复盘固定检查三问：数据口径是否清晰、数据关系是否梳理清楚、是否真的能落地到业务上。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
结果：20 passed。

python -m compileall backend\app scripts backend\tests -q
结果：通过。

只读 CLI smoke：
当前真实状态最早到 2026-06-21 后再复核，未到期前不拉取快照、不保存复盘结论。
```

### 问题 179：积加 API 限流规则被查询但未统一执行等待

#### 现象

```text
项目已有 GERPGO_RATE_LIMIT_RULES、--check-rate-limits 和未知接口拒绝逻辑。
但 GerpgoSnapshotClient.post 原先只调用 require_gerpgo_rate_limit_rule(path)，没有按 min_interval_seconds 等待。
分页函数里另有固定 sleep(1.25 / 5.2)，导致限流口径散落在调用层，token 请求也没有统一节流。
```

#### 原因

```text
“查询限流规则”和“按规则节流请求”被拆成了两件事。
旧实现只保证未知接口不会请求，但不能证明已知接口会按规则等待。
分页层硬编码等待时间还会让规则表和执行代码出现两套口径。
```

#### 解决方案

```text
backend/app/services/gerpgo_snapshot.py：
- 新增 _sleep_for_gerpgo_rate_limit(rule)。
- get_access_token 在 token 请求前按 /open/api_token 的 0.1 秒规则等待。
- GerpgoSnapshotClient.post 在业务接口请求前按对应 path 的 min_interval_seconds 等待。
- 分页函数移除固定 sleep，统一由请求层执行。

backend/tests/test_gerpgo_snapshot.py：
- 覆盖业务接口请求前 sleep 1.25。
- 覆盖 token 请求前 sleep 0.1。
```

#### 后续防错

```text
新增积加 API path 时必须同时做三件事：
1. 在 GERPGO_RATE_LIMIT_RULES 中登记限流规则或项目保守规则。
2. 通过 require_gerpgo_rate_limit_rule 阻断未知接口。
3. 让真实请求通过 GerpgoSnapshotClient.post 或 get_access_token，不能绕过统一节流层。

不要在分页函数、业务函数或页面里散落 sleep 秒数。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_gerpgo_snapshot.py backend\tests\test_snapshot_cli.py backend\tests\test_snapshot_request.py -q
结果：35 passed。

python -m compileall backend\app scripts backend\tests -q
结果：通过。

GERPGO_SKIP_DOTENV=1 python scripts\create_api_snapshot.py --check-rate-limits
结果：status=ready，规则数 7，销售表现分页保守规则为 5.2 秒。
```

### 问题 178：分诊摘要把未到期复盘误写成数据缺口

#### 现象

```text
真实分诊摘要中已有人工处理记录和 7d / 14d 复盘待办，ready_count=0。
但 review readiness 顶层 next_action 仍把 not_ready effect 写成“优先处理数据缺口：缺少处理后快照”。
分诊 blocker 也写成“复盘阻塞：缺少处理后快照”。
这会误导运营在复盘窗口未到期前就去拉快照或查询限流。
```

#### 原因

```text
旧逻辑只看 review_effect.status 是否 ready。
只要 not_ready 且 message 包含“缺少处理后快照”，就归为数据缺口。
但 not_ready 有两个阶段：
- is_due=false：复盘窗口尚未到期，应该等待。
- is_due=true 或缺少 is_due：到期后仍缺窗口或指标，才是可处理的数据缺口。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- _review_next_action 先判断全部 not_ready effect 是否未到期。
- _no_ready_review_message 在未到期时返回等待到期文案。
- 到期或未知 is_due 的 effect 仍按缺处理后快照、处理前窗口不足、数据质量等优先级排序。

scripts/inspect_review_readiness.py 和 scripts/inspect_signal_triage.py 同步相同口径。
```

#### 后续防错

```text
看到 review_effect.status=not_ready 时，必须先问 is_due：
- is_due=false：未到期，只能提示等待到期，不拉快照，不保存结论。
- is_due=true：到期后仍 not_ready，才进入补快照或补数据动作。

分诊摘要、右侧复盘详情和 CLI 预检必须使用同一口径。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
结果：19 passed。

python -m compileall backend\app scripts backend\tests -q
结果：通过。

$env:PYTHONPATH='backend'; python scripts\inspect_review_readiness.py --market-id 1
结果：next_action 提示“7 天 / 14 天复盘窗口尚未到期；最早到 2026-06-21 后再复核处理后指标”。

$env:PYTHONPATH='backend'; python scripts\inspect_signal_triage.py --market-id 1 --top 5 --product-scope-id parent_asin:B00K4W4AAA
结果：no_ready_review_effect blocker 提示等待到期，不提示缺快照。
```

### 问题 177：运行态验证使用旧后端会误判页面未接入新字段

#### 现象

```text
命令行脚本已经输出 review_feedback，但 http://127.0.0.1:8010/api/signal-triage 仍没有 review_feedback。
如果直接用指向 8010 的前端页面验收，会误以为前端或后端字段没有接通。
```

#### 原因

```text
8010 端口存在旧 Python 后端进程，启动时间早于本轮代码变更。
前端默认 API_BASE 指向 8010；如果不确认实际 API 响应，就会用旧运行态验证新代码。
PowerShell 启动前端时，如果没有转义 $env:VITE_API_BASE_URL，环境变量会被外层命令提前展开，导致新前端仍指向默认旧端口。
```

#### 解决方案

```text
不杀旧进程，另启当前代码后端到 8011。
前端另启到 5177，并显式设置 VITE_API_BASE_URL=http://127.0.0.1:8011。
启动后先用 /api/signal-triage 验证 review_status.review_feedback 是否真实返回，再用浏览器验证页面。
```

#### 后续防错

```text
运行态验收前必须确认三件事：
1. 当前前端实际 API base 指向哪个后端端口。
2. 该后端端口是否返回本轮新增字段。
3. 浏览器页面没有“后端服务未连接”和 console error/warn。

不要把旧端口页面当成本轮验收依据。
PowerShell 中设置前端环境变量时要转义 $env:VITE_API_BASE_URL，或使用不会被外层提前展开的启动方式。
```

#### 验证方式

```text
http://127.0.0.1:8011/api/signal-triage?market_id=1&top=5 返回 review_status.review_feedback。
http://127.0.0.1:5177/ 页面加载成功，无“后端服务未连接”，console error/warn 为 0。
当前真实数据 review_record_count=0，页面不显示非空“复盘反馈”。
```

### 问题 176：复盘记录只计数会让规则反馈闭环不可见

#### 现象

```text
后端分诊摘要能返回 review_record_count，但没有返回 review_result 结构。
运营只能知道“有几条复盘记录”，看不到这些记录是 improved、no_change、worse 还是 unclear。
这样会让“7 天 / 14 天复盘 -> 规则改进解释层”的闭环在摘要层不可见。
```

#### 原因

```text
build_review_readiness_payload 原先只统计 review_record_count。
build_signal_triage_payload 的 review_status 也只透出计数和 ready_count。
前端 signalTriageSummaryText 只能展示 ready 复盘数量，不能展示已保存复盘结果对同类信号解释的反馈。
```

#### 解决方案

```text
新增 review_feedback 派生字段，按 review_result 汇总 improved / no_change / worse / unclear。
readiness API、triage API 和两个只读 CLI 脚本统一透出 review_feedback。
前端分诊摘要在存在已保存复盘记录时展示复盘反馈摘要。
该反馈只进入解释层，不自动调整广告动作或规则。
```

#### 后续防错

```text
复盘记录不能只做数量统计。
凡是进入规则改进或同类信号解释的入口，都必须同时说明 review_result 分布和“只解释、不自动执行”的边界。
如果已有保存复盘记录，不要再把“当前没有新的 ready review effect”展示成规则反馈完全阻塞。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_cli.py -q
python -m compileall backend\app scripts backend\tests -q
cd frontend; npm run test:signal-ui
cd frontend; npm run build
结果：全部通过。
```

### 问题 175：复盘记录只按 signal_id 读回会丢失同对象历史结论

#### 现象

```text
同一个广告 ASIN 或销售 ASIN 在不同快照中可能生成新的 signal_id。
如果复盘记录只按当前 signal_id 查询，新信号会读不到旧信号已经保存的复盘结果。
这样会让规则改进层误以为该对象还没有复盘记录。
```

#### 原因

```text
signal_id 是快照内信号定位，不是长期业务对象 ID。
复盘记录真实归属的是同一市场下的稳定业务对象，例如 advertised_product / B016EXMW02。
原来的 load_review_records 和 apply_latest_review_records 只按 signal_id 匹配，缺少 stable object 回退匹配。
```

#### 解决方案

```text
复盘记录读回增加 market_id + object_type + object_id 口径。
后端查询先兼容原 signal_id，再支持稳定对象过滤。
信号 enrichment 先按 signal_id 匹配，找不到时按稳定对象匹配最近复盘记录。
前端读取 /review-records 时带上当前信号的稳定对象参数。
```

#### 后续防错

```text
人工动作、复盘待办、复盘结果和规则反馈都不能只依赖一次性 signal_id。
凡是需要跨快照延续的状态，都必须同时说明 stable object 口径。
产品类对象优先使用 ASIN 作为复盘对象 ID；广告组仍然只能作为投放容器，不得替代商品对象。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_actions.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q
cd frontend; npm run test:snapshot-ui
cd frontend; npm run test:review-ui
cd frontend; npm run build
python -m compileall backend\app backend\tests -q
结果：全部通过。
```

### 问题 174：处理留痕只看当前 signal_id 会和复盘待办状态矛盾

#### 现象

```text
B016EXMW02 已经通过稳定对象匹配读回 7d / 14d 复盘待办，右侧人工处理按钮也正确显示等待复盘窗口。
但同一右栏的“处理留痕”仍显示“暂无人工处理记录”。
这会让运营误以为系统既说已经加入复盘，又说没有处理记录。
```

#### 原因

```text
复盘待办已经按 stable_object_type / stable_object_id 跨快照匹配。
处理留痕仍只读取当前 signal_id 下的 manual_actions。
同一个业务对象在不同快照里可能生成不同 signal_id，只按 signal_id 读取会丢失上一轮人工动作。
```

#### 解决方案

```text
新增 selectManualActionsForSignal。
优先使用当前 signal_id 的直接人工动作；如果没有直接动作，则从当前稳定对象匹配到的复盘待办中回退读取 action_type / operator_name / acted_at。
右侧处理留痕和复盘待办使用同一稳定对象口径。
```

#### 后续防错

```text
人工处理、复盘待办、复盘效果和规则改进必须围绕同一稳定对象读回。
不能一处用 stable_object 匹配，另一处只按当前 signal_id 匹配。
涉及跨快照状态时，优先检查 stable_object_type / stable_object_id，再把 signal_id 作为当前快照定位信息。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
结果：全部通过。
Browser 插件访问本地 URL 被 URL policy 阻止，页面级最终复核未完成，后续需在浏览器可访问时补验。
```

### 问题 173：未到期复盘提前显示缺快照会误导运营拉数据

#### 现象

```text
B016EXMW02 已有 7d / 14d 复盘待办，但待办未到期。
如果前端已经读取到 not_ready review effect，复盘待办主文案会直接显示“复盘效果暂不可计算：缺少处理后 7 天快照”。
这会让运营误以为现在就应该补快照，而不是先等复盘窗口到期。
```

#### 原因

```text
reviewCheckpointText 在 effect 存在时优先展示 effect.message，没有先判断 todo.is_due。
未到期和到期后缺快照是两个不同阶段：未到期是等待，已到期后缺快照才是补数据动作。
```

#### 解决方案

```text
reviewCheckpointText 先判断待办是否到期。
未到期且 effect 不是 ready 时，主文案固定提示到期日和“当前复盘效果仅说明窗口尚未完整”。
只有待办到期后，not_ready effect 才直接展示具体缺口并进入补快照判断。
```

#### 后续防错

```text
复盘 UI 必须先区分未到期、到期但 not_ready、ready、已保存复盘记录。
未到期时不得引导查询限流或拉取低频快照。
任何需要请求积加 API 的动作，都必须在到期后且确认缺快照时，先查询限流规则，再由人工触发。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
结果：全部通过。
```

### 问题 172：review readiness next_action 字典序排序会误导复盘优先级

#### 现象

```text
readiness CLI/API 在没有 ready 复盘效果时，会汇总多个 not_ready 原因。
旧逻辑把 message 放进 set 后直接 sorted，导致“处理前 14 天窗口不足”和“数据质量或交叉信号不适用”排在“缺少处理后 7 天快照”前面。
运营看到的第一动作不是最可执行的数据缺口。
```

#### 原因

```text
next_action 使用文本字典序排序，不理解业务优先级。
广告商品的处理后 7 天快照缺口是当前可推进的数据动作；14 天窗口不足更多是等待；data_quality / cross 类原因通常是边界说明，不应该抢在对象级快照缺口前。
```

#### 解决方案

```text
新增 _prioritized_review_messages 和 _review_effect_gap_priority。
按对象类型、缺口类型、复盘窗口和 message 做稳定排序并去重。
CLI 的 _next_action 和后端服务的 _review_next_action 使用同一套排序口径。
```

#### 后续防错

```text
复盘 next_action 不能只按文本排序。
广告商品 / 销售商品的 7 天处理后快照缺口优先于长期窗口不足和数据质量边界说明。
广告组仍然只是投放容器；即使一个广告组包含一个或多个广告商品，商品级复盘对象也必须落到 AdvertisedProduct / SalesProduct 或稳定 ASIN。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_review_readiness_cli.py -q
结果：5 passed。

$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
结果：9 passed。

$env:PYTHONPATH='backend'; python scripts\inspect_review_readiness.py --market-id 1
结果：next_action 已先提示“缺少处理后 7 天快照”，再提示“处理前 14 天窗口不足”，最后提示“数据质量或交叉信号不适用”。
```

### 问题 171：到期待办无复盘效果时被误导回人工动作阶段

#### 现象

```text
当复盘待办已经到期，但前端暂时没有读取到 review effect 时，规则改进门槛会落入 effect 为空的通用分支。
旧文案提示“先记录人工动作并生成 7/14 天复盘待办”，这会让运营误以为还没有进入复盘流程。
```

#### 原因

```text
buildRuleImprovementReadiness 只区分未到期待办和 effect 是否存在。
没有单独处理 todo.is_due=true 且 effect=null 的状态。
这个状态实际代表“待办已到期，但复盘效果还未读取或未生成”，不是“还没开始人工处理”。
```

#### 解决方案

```text
在 effect 为空的通用分支前增加到期待办判断。
到期待办无 effect 时展示“规则改进等待复盘效果”。
下一步提示先读取复盘效果；如果提示缺少处理后快照，再先查询积加 API 限流规则，并由人工触发低频快照。
```

#### 后续防错

```text
判断规则改进状态时，不能只看 effect 是否存在。
必须先看是否已有复盘待办，以及待办是否到期。
已有到期待办时，不得提示重新记录人工动作或重新加入复盘。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
Browser：当前真实 B016EXMW02 仍显示未到期等待到 2026-06-22，新分支没有误伤真实页面，console warn/error 为 0。
```

### 问题 170：not_ready 复盘只说不能规则改进会让下一步不清楚

#### 现象

```text
右侧规则改进门槛已经能说明 not_ready 时不能输出规则改进结论。
但如果不展示下一步，运营无法区分当前是“未到期继续等待”、“到期后缺处理后快照需要补数据”，还是“ready 后该保存复盘记录”。
```

#### 原因

```text
规则改进门槛只表达了是否满足条件，没有把复盘待办 due_at / is_due 和 effect.status 合成运营动作指引。
这容易导致两类错误：未到期就拉快照或保存结论；到期缺快照时忘记先查询积加 API 限流规则。
```

#### 解决方案

```text
RuleImprovementReadiness 增加 nextStep。
未到期待办：提示到期日期，并明确未到期前不保存复盘结论。
not_ready 且需要补快照：提示先查询积加 API 限流规则，再人工触发低频快照。
ready：提示先保存复盘记录，再进入规则反馈解释层。
```

#### 后续防错

```text
规则改进区必须同时展示状态、原因和下一步。
未到期不能提示拉取快照或保存复盘。
任何需要请求积加 API 的下一步，都必须先提示查询限流规则。
规则反馈只能进入解释层，不能自动调整广告动作或自动改规则。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
Browser：切到 ad_asin:B016EXMW02 后，右侧显示“下一步：到 2026-06-22 后再复核处理后窗口；未到期前不保存复盘结论。”，console warn/error 为 0。
```

### 问题 169：广告组内广告 ASIN 数容易被误读为 Parent ASIN 子商品数

#### 现象

```text
用户再次指出 Amazon 广告架构中，一个广告组至少包含一个商品，也可能包含多个商品。
如果系统把某个广告组下看到的 2 个或 3 个广告 ASIN 当成 Parent ASIN 的完整子商品集合，就会误判商品范围。
```

#### 原因

```text
广告组是投放容器，广告组内广告 ASIN 只代表当前投放覆盖。
Parent ASIN 的完整子商品数必须来自销售表现 parent_asin / variationAsin、销售商品主数据或明确人工映射。
广告组数量、广告覆盖数量、广告组内出现的广告 ASIN 数，都不是 Parent ASIN 完整子商品数。
```

#### 解决方案

```text
保持当前对象模型：AdGroup 只作为容器，AdvertisedProduct 是广告投放商品，SalesProduct 是经营商品。
Parent ASIN / ASIN 作为经营诊断入口，广告组、搜索词和广告位只作为结构信号或上下文证据。
本轮只读复核现有实现和测试，没有修改业务代码。
```

#### 后续防错

```text
任何 Parent ASIN 子 ASIN 数判断，都必须引用销售表现或稳定商品映射证据。
广告组结构信号可以分析“一个广告组投多个商品”，但不能用广告组反推商品组完整性。
看到“广告组下只有 N 个商品”时，只能表述为当前广告组投放覆盖 N 个广告商品。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python scripts\inspect_parent_asin_children.py --parent-asin B00K4W4AAA --expected-count 12
结果：complete，child_asin_count=12。

$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_product_scope.py backend\tests\test_parent_asin_children_inspection.py -q
结果：6 passed。

$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py::test_detects_ad_group_spend_concentration_across_multiple_ad_products backend\tests\test_signal_detection.py::test_does_not_detect_ad_group_spend_concentration_for_single_ad_product -q
结果：2 passed。
```

### 问题 168：ready 前规则改进入口空白会误导复盘闭环状态

#### 现象

```text
当前 B016EXMW02 已有人工留痕和 7d / 14d 复盘待办，但 review effect 仍是 not_ready。
如果右侧只显示复盘待办，不显示规则改进门槛，运营无法判断“规则改进”是已经可做、还没开始，还是缺少处理后快照。
```

#### 原因

```text
已有 reviewRuleFeedback 主要服务“保存复盘记录之后”的解释层反馈。
当前真实运行态更常见的是 ready 前状态：有待办、有处理前指标，但缺少处理后完整窗口。
这个阶段不能输出规则改进结论，但必须说明为什么还不能进入规则反馈。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts 新增 buildRuleImprovementReadiness。
frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx 在右侧新增“规则改进门槛”只读区块。
not_ready 时展示具体 message，并明确缺少完整前后指标时不能调整规则或输出效果结论。
ready 时提示先人工保存复盘记录；已保存复盘记录时只进入解释层反馈。
```

#### 后续防错

```text
规则改进必须由 ready 复盘效果和人工保存的复盘记录驱动。
not_ready 阶段只能展示门槛和缺口，不能输出 improved / no_change / worse。
任何规则反馈都不能自动调整广告动作，也不能自动改规则。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
Browser：点击 B016EXMW02 后，规则改进门槛显示“规则改进暂未满足条件”，原因包含“缺少处理后 7 天快照”，console warn/error 为 0。
```

### 问题 167：同一稳定对象多条 signal_id 容易被误读为多次待处理

#### 现象

```text
Parent ASIN B00K4W4AAA 范围下，B016EXMW02 在左侧队列中出现 3 条信号。
该对象已经有人工留痕和 2 条 7d / 14d 复盘待办，但队列如果只展示单条 signal_id，会让运营误以为三条都需要分别加入复盘。
```

#### 原因

```text
signal_id 是一次信号快照或规则命中的行级标识，不是长期人工处理和复盘对象。
人工动作、复盘待办和复盘效果应优先回到稳定业务对象，例如广告 ASIN、销售 ASIN、搜索词或广告位。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/signalUi.ts 新增 buildSignalQueueObjectStatus。
左侧队列按稳定业务键识别同一对象，展示“同对象 N 条信号 / 复盘待办 N 条”。
当前只做提示，不做对象折叠，不自动写入人工动作。
```

#### 后续防错

```text
队列展示可以保留多条信号，但必须让运营知道哪些信号属于同一稳定业务对象。
涉及人工状态和复盘状态时，不能只看 signal_id。
广告组是容器，一个广告组可以包含一个或多个商品；商品级处理对象必须落到 AdvertisedProduct 或 SalesProduct。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
Browser：B016EXMW02 三条队列行均显示“同对象 3 条信号 / 复盘待办 2 条”，console warn/error 为 0。
```

### 问题 165：对象已有复盘待办时 UI 仍提示重复加入复盘

#### 现象

```text
Browser 验证 B016EXMW02 时，右侧已经展示 7 天复盘待办和处理前指标，但主操作按钮仍显示“加入复盘”。
这会让运营误以为需要再次留痕，偏离“人工处理 -> 等待 7/14 天窗口 -> 复盘”的闭环。
```

#### 原因

```text
右侧复盘待办已经支持按稳定 object_id 兜底匹配。
但动作按钮仍只按当前 signal_id 的人工处理记录判断，没有考虑同一广告 ASIN 在旧 signal_id 下已经生成复盘待办。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
当 selectedReviewTodos 已经存在时，把 add_to_review 主按钮改为禁用态“等待复盘窗口”。
按钮文案提示等待 7/14 天完整窗口，不再引导重复生成复盘待办。
```

#### 后续防错

```text
复盘状态展示必须优先按稳定业务对象判断，不只按 signal_id 判断。
同一广告 ASIN 多条信号共存时，UI 不能重复引导“加入复盘”。
```

#### 验证方式

```text
cd frontend; npm run test:signal-ui
cd frontend; npm run test:review-ui
cd frontend; npm run build
Browser：选中 B016EXMW02 后，主按钮为禁用态“等待复盘窗口”，复盘待办显示缺少处理后 7 天快照，console warn/error 为 0。
```

### 问题 166：Vite 进程继承旧 API 地址导致页面误报后端断开

#### 现象

```text
后端 8010 的 /api/market-options、/api/review-todos 等接口均返回 200。
但 5175 页面仍显示“后端服务未连接”。
```

#### 原因

```text
运行中的 Vite 进程继承了旧环境变量 VITE_API_BASE_URL=http://127.0.0.1:8012。
当前实际后端服务监听在 http://127.0.0.1:8010。
```

#### 解决方案

```text
重启 5175 前端进程，并显式设置 VITE_API_BASE_URL=http://127.0.0.1:8010。
frontend/src/api.ts 增加 fetch 缺失时的 XMLHttpRequest 兜底，降低内置浏览器环境差异导致的误报。
```

#### 后续防错

```text
页面报“后端服务未连接”时，先检查编译后的 /src/api.ts 中 VITE_API_BASE_URL，再判断后端是否真的不可用。
不要只凭页面报错判断 API 失败。
```

#### 验证方式

```text
http://127.0.0.1:5175/src/api.ts 显示 VITE_API_BASE_URL=http://127.0.0.1:8010。
Browser 刷新 5175 页面后，后端服务未连接=false，B00K4W4AAA 和 B016EXMW02 均可见。
```

## 2. 当前高优先级问题

### 问题 1：旧项目对象模型带偏

问题：

```text
旧项目曾把广告对象、产品中心、产品归因混在一起，容易把广告组或广告来源误当成经营商品。
```

原因：

```text
旧项目是从已有数据和页面继续堆功能，没有先明确对象边界。
```

解决方案：

```text
新项目独立建模，严格区分 SalesProduct、AdvertisedProduct、AdGroup、SearchTerm、Evidence。
```

后续防错规则：

- 旧项目只能只读参考 API 路径、字段和踩坑经验。
- 不复用旧项目页面结构、产品中心逻辑和产品归因逻辑。
- 看到“产品”字样时，必须先确认真实数据来源和对象粒度。

验证方式：

```text
检查 数据模型与表结构.md / 项目规划.md / API同步范围.md。
```

### 问题 138：推荐对象缺少人工状态会让复盘闭环断在“建议”阶段

问题：

```text
工作台能显示推荐优先留痕对象 B016EXMW02，但如果不显示该对象是否已经人工留痕、是否生成复盘待办，运营容易误以为推荐已经进入复盘链路。
```

原因：

```text
recommended_candidate 和 manual_action_preview 只说明“建议对哪个对象做什么动作”，不说明“这个对象现在处于人工确认 / 复盘链路的哪一步”。
```

解决方案：

```text
/api/signal-triage 和 inspect_signal_triage.py 增加 recommended_manual_status。
该字段只读返回 has_manual_action、review_todo_count、ready_review_count 和 next_action。
前端推荐卡展示该状态，但真实写入仍必须由人工按钮触发。
```

后续防错规则：

- 推荐对象不能只展示对象和原因，还必须展示当前人工状态。
- 状态字段必须保持只读，不得因为推荐对象存在就自动写 manual_actions。
- 前端展示推荐状态时，必须继续提示“真实写入由人工按钮触发”。

验证方式：

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
npm run test:signal-ui
浏览器检查推荐卡是否显示 B016EXMW02 尚未人工留痕。
```

### 问题 139：人工留痕后推荐对象状态不刷新会误导运营

问题：

```text
用户手动点击“加入复盘”后，如果前端只刷新复盘待办列表，不刷新 /api/signal-triage，推荐卡可能仍显示“推荐对象尚未人工留痕”。
```

原因：

```text
推荐对象状态来自 signal-triage 的 recommended_manual_status，不是单个 signal 的本地状态。
人工动作保存成功后，handleManualAction 原来只刷新右侧处理记录和 review-todos，没有刷新 signalTriageSummary。
```

解决方案：

```text
handleManualAction 保存成功后，并行刷新 fetchReviewTodos 和 fetchSignalTriageSummary。
recommendedManualStatusText 对 has_review_todo=true 的状态显示“已生成复盘待办，等待 7 天 / 14 天完整窗口”。
```

后续防错规则：

- 任何会改变人工动作或复盘待办的前端操作，都必须同步刷新依赖这些事实的摘要区。
- 推荐卡状态不能依赖本地推测，必须以后端分诊摘要为准。
- 已生成复盘待办不等于 ready 复盘结论，仍要等待处理后快照。

验证方式：

```text
npm run test:signal-ui
npm run build
只读检查 inspect_signal_triage.py 中 recommended_manual_status。
```

### 问题 140：把广告投放覆盖的 2 个 ASIN 误当成 Parent ASIN 完整子商品

问题：

```text
Parent ASIN B00K4W4AAA 页面只显示 2 个子 ASIN，容易被理解成该 Parent ASIN 只有 2 个子商品。
用户指出实际应有 12 个子 ASIN，且可以从积加开放平台销售表现求证。
```

原因：

```text
当前系统使用人工映射快照和广告投放覆盖来构建 Parent ASIN 入口。
销售表现 API raw 中已有 variationAsin 字段，但规范化时没有写入 parent_asin。
广告商品只能说明当前投放覆盖，不能代表完整变体关系。
```

解决方案：

```text
销售表现规范化保留 variationAsin / parentAsin 到 parent_asin。
sales_products 和 sales_product_daily_metrics 同步写入 parent_asin。
前端商品组概览改为“已识别销售表现子 ASIN / 当前投放广告 ASIN”，不再暗示广告覆盖等于完整子商品。
```

后续防错规则：

- Parent ASIN 完整子 ASIN 数必须来自销售表现 variationAsin/parent_asin 或稳定主数据源。
- 广告 ASIN 只代表当前投放覆盖，不能作为完整变体关系。
- 如果接口限流或分页不完整，只能标记为“已识别”，不能写成“完整”。

验证方式：

```text
$env:PYTHONPATH='.'; python -m pytest tests\test_gerpgo_snapshot.py tests\test_product_scope.py -q
python -m py_compile app\services\gerpgo_snapshot.py
npm run test:signal-ui
限流恢复后，再用销售表现完整分页验证 B00K4W4AAA 的唯一子 ASIN 数。
```

### 问题 141：请求积加 API 前没有显式查询限流规则

问题：

```text
为了求证 Parent ASIN 子 ASIN 数，如果直接继续请求销售表现分页，容易再次触发 509 / 90008 限流。
用户明确要求请求积加 API 时必须先查询限流规则。
```

原因：

```text
项目已有 page_delay_seconds 和 sales_api_delay_seconds 经验值，但代码没有把“已查询限流规则”作为请求前置门禁。
测试命令还可能误读本地 .env，导致本应无凭据的测试实际请求积加 API。
```

解决方案：

```text
增加积加 API 限流规则查询表和请求前置校验。
已登记路径才能请求；未登记路径直接停止。
新增 create_api_snapshot.py --check-rate-limits 只读输出当前规则。
新增 GERPGO_SKIP_DOTENV 测试隔离开关，避免测试误触真实 API。
```

后续防错规则：

- 拉取真实快照前先运行 `python scripts\create_api_snapshot.py --check-rate-limits`。
- 公开文档未确认的业务接口只能标记为 conservative，不能写成官方限流规则。
- 新增积加 API 路径时，必须先补限流规则测试和规则表，再允许请求。
- 测试里需要模拟缺凭据时，必须设置 `GERPGO_SKIP_DOTENV=1`。

验证方式：

```text
$env:PYTHONPATH='.'; python -m pytest tests\test_gerpgo_snapshot.py tests\test_snapshot_cli.py tests\test_snapshot_request.py -q
python -m py_compile app\services\gerpgo_snapshot.py ..\scripts\create_api_snapshot.py
$env:GERPGO_SKIP_DOTENV='1'; python scripts\create_api_snapshot.py --check-rate-limits
```

### 问题 2：把广告组当产品

问题：

```text
一个广告组可能投放多个商品，不能把广告组直接归属为一个产品。
```

原因：

```text
广告组是投放容器，不是经营商品，也不是广告商品。
```

解决方案：

```text
广告组只作为容器分析；商品表现必须落到 AdvertisedProduct 或 SalesProduct。
```

后续防错规则：

- 广告组级搜索词无法自动归属到单个商品时，必须降低置信度。
- 多商品广告组信号必须说明“广告组是容器”或“归因不确定”。

验证方式：

```text
检查 AI信号类型矩阵.md 中广告组结构信号。
```

### 问题 3：把 ERP 页面功能当开放平台 API 能力

问题：

```text
ERP 页面能展示某类数据，不代表开放平台 API 可以获取该数据。
```

原因：

```text
页面能力和开放平台 API 能力不是同一个边界。
```

解决方案：

```text
进入同步链路前，必须用开放平台文档或真实请求验证 API 路径、参数、字段和返回结果。
```

后续防错规则：

- 不根据页面名称判断 API 能力。
- 不根据旧项目脚本假设新项目可用。
- 未验证 API 的信号必须降级或暂缓实现。

验证方式：

```text
检查 API同步范围.md 和真实请求输出。
```

### 问题 4：把 ABA 当店铺或产品数据

问题：

```text
ABA 搜索词数据不是店铺数据，也不是 RBK004 专属数据。
```

原因：

```text
当前 ABA 来源是 ERP 人工导出的站点级周快照，开放平台暂不能直接获取。
```

解决方案：

```text
ABA 独立入库，只按 站点 + 周期 + 标准化搜索词 参与机会匹配。
```

后续防错规则：

- ABA 不写 `shop_id`。
- ABA 不进入自动 API 同步主链路。
- ABA 周期过期或与广告周期不一致时，相关信号降置信度。
- `搜索查询量` 为空时，不能估算搜索量或市场容量。

验证方式：

```text
检查 ABA导入流程.md 和样本文件字段。
```

### 问题 5：当前项目旧样例页面带偏

问题：

```text
当前项目原有 TodaySignals 页面和 RB831 样例数据会让原型继续沿用旧样例结构。
```

原因：

```text
样例代码虽然可运行，但它不是经过 Product Design 和数据模型确认后的原型。
```

解决方案：

```text
删除 TodaySignals 原型入口和 RB831 内置样例数据，新建 SignalTriageWorkbench 作为独立入口。
```

后续防错规则：

- 不恢复 `TodaySignals` 作为原型入口。
- 不恢复 RB831 内置样例数据。
- 默认信号 API 不返回内置业务样例。
- 测试 fixture 可以存在，但不能伪装成真实业务数据。

验证方式：

```text
rg -n "TodaySignals|RB831|sample_data|AGGREGATED_ROWS" frontend/src backend/app backend/tests -S
```

### 问题 6：mock 数据伪装成真实结论

问题：

```text
如果页面直接展示 mock 信号，用户会误以为这是实际店铺诊断结果。
```

原因：

```text
mock 只适合交互开发和测试，不具备真实业务证据效力。
```

解决方案：

```text
正式原型优先读取低频真实 API 快照；mock 兜底时必须显式标记为样例数据。
```

后续防错规则：

- mock 不允许标记为 `积加API`。
- mock 不用于证明真实业务结论。
- 真实 API 失败时保留上一次成功快照，不用伪造数据顶替。

验证方式：

```text
检查 真实数据快照方案.md 和前端数据新鲜度显示。
```

### 问题 7：先写页面再补数据模型

问题：

```text
先做页面容易让 UI 字段反推业务对象，最终模型不稳定。
```

原因：

```text
页面只表达用户看到什么，不能替代数据来源、对象边界和关联关系。
```

解决方案：

```text
先确认数据源、对象模型、关联键和信号证据，再开发页面。
```

后续防错规则：

- 页面新增字段前，先确认后端 API 合同。
- 前端不能为了展示虚构后端没有返回的业务事实。
- 新增信号类型必须同步 AI信号类型矩阵.md。

验证方式：

```text
检查 response_model、frontend/src/api.ts、AI信号类型矩阵.md 是否一致。
```

### 问题 8：真实 API 拉取频率失控

问题：

```text
原型需要真实数据，但不能每次打开页面都请求积加 API。
```

原因：

```text
积加 API 有限流和凭据风险，页面实时拉取会不稳定，也不利于复现问题。
```

解决方案：

```text
采用低频真实 API 快照 + 本地缓存 + 手动刷新。
```

后续防错规则：

- 页面打开不自动高频请求积加 API。
- 快照必须记录 manifest。
- 已有新鲜快照时默认复用。
- 刷新必须由人工明确触发。

验证方式：

```text
检查 真实数据快照方案.md 和 data/api_snapshots/manifest.json。
```

### 问题 9：新项目缺少独立积加凭据配置

问题：

```text
真实 API 快照脚本已经具备最小拉取能力，但新项目当前没有 .env，也没有 GERPGO_* 进程环境变量。
```

原因：

```text
旧项目有 .env，但新项目隔离规则禁止自动读取或复制旧项目凭据。
```

解决方案：

```text
脚本只读取新项目 .env 或当前进程环境变量；缺少 GERPGO_MARKET_IDS 或凭据时直接失败。
```

后续防错规则：

- 不从旧项目自动复制 `.env`。
- 不在日志、文档或前端输出密钥。
- 没有真实凭据时不能生成伪快照冒充真实 API 数据。

验证方式：

```text
python scripts\create_api_snapshot.py --check-config
python scripts\create_api_snapshot.py --market-id 1 --check-config
python scripts\create_api_snapshot.py --market-id 1 --days 7 --count 10 --max-pages 1
```

当前结果：

```text
--check-config：not_ready，缺少 GERPGO_MARKET_IDS 和凭据
--market-id 1 --check-config：not_ready，缺少凭据
真实拉取：failed，缺少 GERPGO_APP_ID / GERPGO_APP_KEY / GERPGO_ACCESS_TOKEN
接口状态：/api/snapshot/status 返回 has_snapshot = false，status = missing
```

### 问题 10：AI 信号实现字段落后于信号规范

问题：

```text
后端 AiSignal 早期实现只包含 signal_type、object_type、severity、summary、why、evidence 等轻量字段，
不足以支撑真实业务里的证据链、置信度、数据新鲜度和多店铺站点上下文。
```

原因：

```text
早期代码先跑通了规则和页面交互，但还没有完全对齐 AGENTS.md、项目规范.md 和开发原型.md 中定义的信号合同。
```

解决方案：

```text
补齐 AiSignal、EvidenceItem 和 DataSourceRef 字段；
快照行读取时带上 manifest 上下文；
信号生成时带上数据来源、新鲜度、置信度和不确定性。
```

后续防错规则：

- 新增或调整 AI 信号字段时，必须同步检查后端 Pydantic、前端 TypeScript 类型、页面使用字段和测试。
- 快照数据进入信号前，必须先带上店铺、站点、周期、来源表和来源记录 ID。
- 不允许生成没有数据来源和不确定性说明的真实业务信号。

验证方式：

```text
python -m pytest tests\test_snapshot_store.py tests\test_signal_detection.py
cd backend && python -m pytest
cd frontend && npm run build
```

### 问题 11：补丁工具默认目录误写旧项目

问题：

```text
本轮新增测试时，apply_patch 使用线程初始目录，第一次把 test_snapshot_readiness.py 写到了旧项目 Amazon广告分析。
```

原因：

```text
当前 Codex 线程的环境目录仍显示旧项目，但目标 goal 和实际开发目录是新项目 Amazon广告AI信号工作台。
```

解决方案：

```text
立即删除旧项目误写的未跟踪测试文件；
后续修改新项目文件时使用绝对路径，或在修改前先用 Test-Path 验证目标路径。
```

后续防错规则：

- 任何 `apply_patch` 修改新项目时，优先使用新项目绝对路径。
- 修改后立刻确认旧项目没有新增未跟踪任务文件。
- 旧项目只读参考，不允许留下本项目开发产物。

验证方式：

```text
Test-Path D:\DataProject\coedx_project\Amazon广告分析\backend\tests\test_snapshot_readiness.py
结果：False

Test-Path D:\DataProject\coedx_project\Amazon广告AI信号工作台\backend\tests\test_snapshot_readiness.py
结果：True
```

### 问题 12：本地备用前端端口被 CORS 拦截

问题：

```text
默认 8010 后端端口存在旧进程时，本轮使用 8011 后端和 5176 前端验证当前代码。
但后端 CORS 只允许 5174，导致备用前端无法请求当前后端。
```

原因：

```text
本地开发端口可能因旧进程占用而切换，但 CORS 白名单没有覆盖备用 Vite 端口。
```

解决方案：

```text
后端本地开发 CORS 白名单补充 5175 / 5176。
```

后续防错规则：

- 浏览器显示“后端服务未连接”时，先查实际接口响应和 CORS，不要直接判断前端逻辑坏了。
- 如果默认端口被旧进程占用，优先换端口验证当前代码，不直接杀未知进程。
- 本地备用端口必须进入测试覆盖。

验证方式：

```text
python -m pytest tests\test_api_routes.py::test_cors_allows_local_vite_fallback_port
结果：通过

浏览器打开 http://127.0.0.1:5176/
结果：顶部显示 无真实快照：缺配置 2 项，控制台 error/warn 为 0
```

### 问题 13：真实快照表已保存但未进入信号输入

问题：

```text
真实快照会保存 normalized/ad_placement_daily_metrics.json，但早期信号输入只读取 ad_search_term_daily_metrics.json。
结果是即使积加 API 成功拉到广告位数据，也不会生成广告位 P0 信号。
```

原因：

```text
快照落盘、快照状态和信号生成分别完成后，没有补一个“快照表 -> 信号输入 -> AI 信号”的端到端测试。
```

解决方案：

```text
新增真实快照广告位信号验收用例。
load_signal_rows_from_latest_snapshot 同时读取 ad_search_term_daily_metrics 和 ad_placement_daily_metrics。
广告位行统一带上 snapshot_id、api_name、source_table、source_record_id、店铺站点和周期上下文。
```

后续防错规则：

- 新增任何 normalized 快照表后，必须确认是否需要进入 AI 信号输入。
- 如果该表支撑 P0/P1 信号，必须补“快照 fixture -> detect_signals”的测试。
- 不只检查 manifest row_counts，还要检查该表是否被真实规则消费。

验证方式：

```text
python -m pytest tests\test_snapshot_signal_flow.py
结果：1 passed

python -m pytest
结果：19 passed
```

### 问题 14：快照状态与信号实际来源可能不一致

问题：

```text
如果先有成功快照，再有一次更新的 API 拉取失败，早期 load_snapshot_status 会返回最新 failed manifest。
但 load_signal_rows_from_latest_snapshot 会读取上一份成功快照，导致页面数据新鲜度状态和 AI 信号实际来源不一致。
```

原因：

```text
状态读取默认取最新 manifest，信号读取取最新成功 manifest，两者选择规则不一致。
```

解决方案：

```text
load_snapshot_status 优先返回最新成功快照。
只有没有任何成功快照时，才返回最新失败或 unknown manifest。
```

后续防错规则：

- 展示给前端的快照状态必须优先表达“当前可用于信号的快照”。
- 最新失败尝试不能覆盖上一份可用成功快照的展示状态。
- 新增快照状态字段前，必须明确它表示“当前使用快照”还是“最新拉取尝试”。

验证方式：

```text
python -m pytest tests\test_snapshot_store.py::test_snapshot_status_prefers_latest_success_when_newer_attempt_failed
结果：1 passed

python -m pytest
结果：20 passed
```

### 问题 15：只测 service 未测 API 路由会漏掉前端入口断链

问题：

```text
快照读取和信号生成的 service 测试通过，不代表 GET /api/signals 这个前端入口一定能返回同样完整的信号合同。
如果路由层导入、response_model、字段序列化或依赖组合出错，前端仍可能拿不到证据链。
```

原因：

```text
早期验证重点放在 snapshot_store 和 detect_signals，缺少真实快照 fixture 经过 API 路由入口的回归测试。
```

解决方案：

```text
新增 GET /api/signals 路由级测试。
测试用成功 API 快照 fixture 验证返回信号包含 snapshot_id、api_name、source_table、source_record_id、积加API来源和人工确认要求。
```

后续防错规则：

- 新增或调整前端依赖的 API 合同时，必须补路由级测试。
- service 层测试只证明内部逻辑，不能替代前端入口验收。
- AI 信号接口必须断言证据来源和人工确认字段，不能只断言返回 200。

验证方式：

```text
python -m pytest tests\test_api_routes.py::test_signals_route_returns_explainable_signals_from_api_snapshot
结果：1 passed

python -m pytest
结果：21 passed
```

### 问题 16：ABA 原始搜索词和标准化搜索词混用

问题：

```text
ABA 导入时如果把 search_term 也写成 normalized_query，会丢失原始导出词的大小写和空格形态。
这会削弱证据回查能力，也和 ABA导入流程.md 中“search_term 原始搜索词、normalized_query 标准化搜索词”的定义不一致。
```

原因：

```text
第一版实现为了方便匹配，把展示字段和匹配字段合并成了同一个标准化值。
```

解决方案：

```text
search_term 保留原始词去首尾空格后的值。
normalized_query 单独执行小写和连续空格合并。
重复判断只使用 normalized_query。
```

后续防错规则：

- 人工导出数据必须保留原始字段和标准化字段两套值。
- 用于匹配的字段不能覆盖用于证据回查的字段。
- 导入测试必须同时断言 `search_term` 和 `normalized_query`。

验证方式：

```text
python -m pytest tests\test_aba_import.py::test_import_aba_workbook_normalizes_and_deduplicates_rows
结果：1 passed
```

### 问题 17：真实快照文件可能被误提交

问题：

```text
真实 ABA 快照导入后会生成较大的本地 JSON 文件。
本轮真实快照 normalized/aba_search_term_snapshots.json 约 451MB，如果没有忽略规则，后续很容易被误提交。
```

原因：

```text
新项目还没有 .gitignore，真实数据、构建产物、依赖目录和缓存目录都处于未跟踪状态。
```

解决方案：

```text
新增 .gitignore，忽略 .env、data/api_snapshots、data/aba_snapshots、tmp、frontend/node_modules、frontend/dist 和缓存文件。
```

后续防错规则：

- 真实 API 快照和 ERP 导出快照只作为本地数据资产，不进入版本管理。
- 任务结束前用 git status 确认真实数据目录没有出现在可提交变更里。
- 新增任何真实数据落盘目录时，同步检查 .gitignore。

验证方式：

```text
git status --short -- .gitignore data backend/app/services/aba_import.py backend/tests/test_aba_import.py scripts/import_aba_snapshot.py backend/requirements.txt
结果：data/aba_snapshots 未出现在可提交变更中
```

### 问题 18：信号接口不应默认读取 ABA 全量大文件

问题：

```text
真实 ABA 快照全量文件 normalized/aba_search_term_snapshots.json 约 451MB。
如果 /api/signals 每次生成信号都读取全量文件，页面接口会变慢，也容易造成内存浪费。
```

原因：

```text
ABA 文件用于市场热度增强，但第一阶段信号只需要高排名热词。
全量快照适合回查，不适合作为实时页面信号输入。
```

解决方案：

```text
ABA 导入时额外写入 normalized/aba_search_term_top1000.json。
load_aba_rows_from_latest_snapshot 只读取 Top1000 索引。
如果旧快照缺少索引，读取器直接返回空，不回退读取全量文件。
```

后续防错规则：

- 页面信号接口只读轻量索引或已聚合数据。
- 全量 ERP 导出快照只用于导入、回查或离线分析，不直接进入高频接口。
- 新增任何大文件快照参与信号前，必须先设计小索引或聚合文件。

验证方式：

```text
python -m pytest tests\test_aba_import.py::test_write_aba_snapshot_preserves_manifest_and_normalized_rows
结果：1 passed

python -m pytest tests\test_snapshot_signal_flow.py::test_api_search_term_and_aba_snapshot_generate_market_opportunity_signal
结果：1 passed

load_aba_rows_from_latest_snapshot()
结果：1000 行，只读取 ABA Top1000 索引
```

### 问题 19：缺少 API 凭据时快照创建接口不可解释

问题：

```text
POST /api/snapshot/create 在缺少积加 API 凭据时如果返回普通 400，前端 createSnapshot 会直接抛 request_failed_400。
用户无法在工作台看到缺少哪些配置，也容易误判为系统异常。
```

原因：

```text
缺配置是可预期的业务状态，不是无法解释的服务错误。
第一阶段真实 API 快照必须由人工触发，缺配置时需要明确说明未请求 API、未生成快照。
```

解决方案：

```text
SnapshotCreateResult 增加 can_request_api 和 missing。
缺配置时 POST /api/snapshot/create 返回 200 + status = not_ready。
响应只包含缺失配置键名，不包含 AppKey、accessToken 或其他密钥值。
```

后续防错规则：

- 人工触发真实 API 快照的结果必须可展示。
- 缺配置、缺授权、未选择 market_id 这类可预期状态优先返回结构化状态，不返回前端不可消费的普通错误。
- 任何快照创建失败都不能生成伪快照，也不能自动切换成假真实数据。

验证方式：

```text
python -m pytest tests\test_api_routes.py::test_snapshot_create_route_returns_not_ready_without_secret_values
结果：1 passed

POST /api/snapshot/create
结果：200，status = not_ready，can_request_api = false
```

### 问题 20：前端只显示缺配置数量，用户仍不知道该补什么

问题：

```text
工作台顶部数据新鲜度只显示“无真实快照：缺配置 N 项”。
这能说明系统没有真实快照，但不能指导下一步该补哪个配置。
```

原因：

```text
前端把 readiness.missing 聚合成数量，丢掉了后端已经返回的安全配置键名。
缺配置键名不是密钥值，可以安全展示。
```

解决方案：

```text
新增 snapshotUi.ts，把快照状态文案做成纯函数。
无真实快照且缺配置时，直接展示缺失配置键名。
not_ready 创建结果也复用同一套 missing 文案。
```

后续防错规则：

- 缺配置状态必须显示安全键名，不只显示数量。
- 前端文案转换优先做成可测试纯函数。
- 不展示任何 AppKey、accessToken 或密钥值。

验证方式：

```text
npm run test:snapshot-ui
结果：通过

Browser DOM smoke
结果：展示“无真实快照：缺少 GERPGO_MARKET_IDS、GERPGO_APP_ID_AND_GERPGO_APP_KEY_OR_GERPGO_ACCESS_TOKEN”
```

### 问题 21：本地旧 dev server 容易让前端连到错误后端

问题：

```text
8010、5174、5175、5176、5177 等端口可能已有旧进程。
如果前端仍使用默认 API_BASE 8010，页面会显示“后端服务未连接”，并掩盖真实的缺配置状态。
```

原因：

```text
Vite dev server 的 VITE_API_BASE_URL 只在启动时注入。
本地端口被旧进程占用时，新服务会失败或被迫换端口。
后端 CORS 原本只列了少数固定 Vite 端口。
```

解决方案：

```text
验证时使用当前代码后端 8013 和前端 5179。
前端启动前明确设置 VITE_API_BASE_URL。
后端 CORS 增加本机 51xx Vite 开发端口规则。
```

后续防错规则：

- 浏览器 smoke 前先确认前端源码中的 VITE_API_BASE_URL 是否指向当前后端。
- 不杀不明旧进程，优先换端口启动当前服务。
- 如果页面显示“后端服务未连接”，先查实际 API_BASE 和端口进程，不直接改业务代码。
- Browser 截图命令可能超时，不能把截图失败误判为页面失败；同时保留 DOM、文本、按钮状态和控制台结果。

验证方式：

```text
http://127.0.0.1:5179/src/api.ts
结果：VITE_API_BASE_URL = http://127.0.0.1:8013

Browser smoke
结果：页面加载、刷新按钮可点击、拉取快照按钮禁用、控制台 warning/error 为空
```

### 问题 22：错误的 GERPGO_MARKET_IDS 会让配置检查直接崩溃

问题：

```text
如果 .env 中 GERPGO_MARKET_IDS 写成 abc 这类非数字值，load_gerpgo_config 会直接抛 ValueError。
脚本和后端无法返回结构化 not_ready，用户会看到普通异常。
```

原因：

```text
market_id 解析使用 int(...) 列表推导，没有把错误配置转换为可解释状态。
真实 API 快照链路里，配置错误属于预期业务状态，应该可展示、可修正。
```

解决方案：

```text
GerpgoConfig 增加 invalid_market_ids 标记。
load_gerpgo_config 遇到非法 market_id 时记录标记，不直接抛异常。
check_snapshot_readiness 返回 VALID_GERPGO_MARKET_IDS。
显式传入 market_id 时允许覆盖 .env market 列表。
```

后续防错规则：

- 配置检查不能因为用户填错 `.env` 而直接崩溃。
- 只返回安全配置键名，不返回密钥值。
- 命令行显式参数优先级高于 `.env` 中的 market 列表。
- 新增配置项时必须补读取测试和错误配置测试。

验证方式：

```text
python -m pytest tests\test_gerpgo_snapshot.py tests\test_snapshot_readiness.py tests\test_snapshot_request.py
结果：11 passed

python -m pytest
结果：29 passed
```

### 问题 23：无真实 API 快照时信号队列为空会掩盖真实阻塞点

问题：

```text
当前没有成功 API 快照或缺少 Gerpgo 凭据时，/api/signals 会返回空列表。
用户看到空信号队列时，容易误以为业务没有异常，而不是数据链路未就绪。
```

原因：

```text
原信号生成只处理广告商品、搜索词、广告位等业务对象。
真实 API 快照状态只出现在顶部状态提示，没有进入 AI 信号队列。
这不符合“哪里异常、为什么异常、证据是什么、建议怎么处理”的工作台定位。
```

解决方案：

```text
新增数据质量信号生成逻辑。
当没有成功 API 快照时，生成 sig-data-quality-api-snapshot-missing。
信号证据只暴露快照状态、API 是否可请求、缺失配置键名，不暴露密钥值。
信号建议只引导人工补齐配置并手动拉取快照，不自动请求 API 或执行广告动作。
```

后续防错规则：

- 数据链路阻塞也必须作为 AI 信号呈现，不能只做页面状态提示。
- 缺配置、缺快照、快照失败都属于数据质量问题，必须带证据和人工建议。
- 空信号队列只能表示“有数据但无触发信号”，不能表示“主数据不可用”。
- 任何配置相关证据只能展示安全键名，不能展示密钥值。

验证方式：

```text
python -m pytest tests\test_api_routes.py::test_signals_route_reports_missing_api_snapshot_as_data_quality_signal -q
结果：1 passed

python -m pytest
结果：30 passed

TestClient GET /api/signals
结果：返回 sig-data-quality-api-snapshot-missing / data_quality
```

### 问题 24：手动快照脚本把缺凭据表现成 failed

问题：

```text
直接运行 scripts/create_api_snapshot.py 拉取快照时，如果缺少 Gerpgo 凭据，脚本返回 exit code 1 和 status=failed。
这会让操作者把“缺配置”误判成脚本故障。
```

原因：

```text
脚本只在 --check-config 模式下做 readiness 检查。
真实拉取模式直接进入 create_gerpgo_snapshot，再由 client.ensure_configured 抛异常。
```

解决方案：

```text
脚本在真实拉取前统一执行 check_snapshot_readiness。
如果 readiness.ready=false，直接输出 status=not_ready 和安全缺失配置键名。
不继续请求积加 API，不打印密钥值。
```

后续防错规则：

- CLI、后端接口和前端状态对“缺配置”必须统一使用 `not_ready`。
- `failed` 只用于真实执行过程中发生的不可预期错误，不能用于可修正的配置缺失。
- 手动拉取入口必须先做 readiness gate，再做网络请求。
- 新增手动脚本时必须补 subprocess 级 smoke 测试。

验证方式：

```text
python -m pytest tests\test_snapshot_cli.py::test_create_api_snapshot_cli_returns_not_ready_without_credentials -q
结果：1 passed

python -m pytest
结果：31 passed

python scripts\create_api_snapshot.py --market-id 1 --count 1 --max-pages 1
结果：not_ready，缺少 Gerpgo 鉴权配置
```

### 问题 25：成功快照但无可分析行会被误判为没有业务信号

问题：

```text
如果快照 manifest 状态为 success，但 normalized 中广告搜索词和广告位可分析行数为 0，
/api/signals 仍可能返回空数组。
用户会把空信号队列误解成“没有异常或机会”，而不是“快照覆盖不足”。
```

原因：

```text
原数据质量信号只判断“没有成功快照”。
成功快照是否真的包含信号规则需要的可分析行，没有进入数据质量判断。
```

解决方案：

```text
/api/signals 只加载一次 signal_rows，并把行数传入数据质量规则。
当快照 success 且 signal_row_count=0 时，生成 sig-data-quality-api-snapshot-empty。
证据展示 snapshot_id、row_counts、api_list、周期、店铺和站点。
```

后续防错规则：

- 快照 `status=success` 不等于信号输入可用。
- 空信号队列必须先排除数据覆盖不足，再解释为业务无异常。
- 真实快照验收必须检查 `manifest.row_counts` 和 `normalized` 可分析行数。
- 数据覆盖不足只能建议人工检查快照和接口范围，不能自动请求更多 API 或执行广告动作。

验证方式：

```text
python -m pytest tests\test_api_routes.py::test_signals_route_reports_empty_success_snapshot_as_data_quality_signal -q
结果：1 passed

python -m pytest
结果：32 passed
```

### 问题 26：缺少快照只读验收入口会让真实拉取后的判断靠人工猜

问题：

```text
真实 API 快照拉取后，如果只看 manifest 或页面状态，仍不能快速判断快照是否能进入 AI 信号规则。
操作者需要手工打开多个 normalized 文件核对行数，容易漏掉 row_counts 不一致、文件缺失或 signal_row_count=0。
```

原因：

```text
原项目有 create_api_snapshot 负责拉取，有 /api/signals 负责生成信号，但缺少中间的只读验收入口。
真实数据闭环需要先证明快照可用，再证明信号可解释。
```

解决方案：

```text
新增 snapshot_inspection 服务和 scripts/inspect_api_snapshot.py。
脚本只读取本地快照，不请求积加 API，不写入数据。
输出 status、row_counts、normalized_tables、signal_row_count、ready_for_signals 和 issues。
```

后续防错规则：

- 真实拉取后先运行 `inspect_api_snapshot.py`，再解释业务信号。
- `ready_for_signals=true` 才能进入真实业务异常和机会判断。
- `status=partial` 时先修复快照或字段映射，不直接改信号规则。
- `status=empty` 时先检查 API 范围、参数和 normalized 文件，不把空信号当作无异常。

验证方式：

```text
python -m pytest tests\test_snapshot_inspection.py -q
结果：2 passed

python -m pytest
结果：34 passed

python scripts\inspect_api_snapshot.py
结果：missing，NO_API_SNAPSHOT
```

### 问题 27：快照验收只停留在 CLI 会让工作台看不到 ready_for_signals

问题：

```text
新增 inspect_api_snapshot.py 后，只能在命令行看到快照是否 ready_for_signals。
前端工作台仍只能读取 snapshot/status 和 readiness，无法展示 normalized 行数、可分析行数和验收 issues。
```

原因：

```text
CLI 验收解决了开发者检查问题，但没有形成前端可消费的 API 合同。
真实数据闭环要求工作台本身能回答“当前快照是否可进入信号规则”。
```

解决方案：

```text
新增 GET /api/snapshot/inspection。
后端用 SnapshotInspectionResult 固定响应模型。
前端新增 SnapshotInspectionResult 类型和 snapshotInspectionText 文案函数。
工作台顶部数据新鲜度同步展示快照验收状态。
```

后续防错规则：

- 任何关键数据验收脚本都要判断是否需要对应 API 入口。
- 前端不能只显示“有无快照”，还要能显示 `ready_for_signals` 和 issues。
- 只读验收入口不能请求积加 API，不能写入数据。
- 缺少真实快照时必须明确显示 `NO_API_SNAPSHOT`，不能让空信号队列误导为业务无异常。

验证方式：

```text
python -m pytest tests\test_api_routes.py::test_snapshot_inspection_route_returns_ready_state_without_secret_values -q
结果：1 passed

npm run test:snapshot-ui
结果：通过

GET /api/snapshot/inspection
结果：missing，ready_for_signals=false，issues=NO_API_SNAPSHOT
```

### 问题 28：快照创建成功后不返回验收结果会形成新的判断断点

问题：

```text
POST /api/snapshot/create 创建或复用快照后，如果只返回 manifest 基本信息，
前端和操作者仍要再调用 /api/snapshot/inspection 才知道该快照能不能进入信号规则。
```

原因：

```text
创建快照和验收快照是连续操作。
如果响应不携带 ready_for_signals、signal_row_count 和 issues，用户容易把“创建成功”误认为“可生成业务信号”。
```

解决方案：

```text
SnapshotCreateResult 增加 inspection。
创建或复用成功时，路由层立即调用 inspect_api_snapshot，把验收结果随创建响应返回。
缺配置 not_ready 时不生成伪 inspection，保持 inspection = null。
```

后续防错规则：

- 任何“创建 / 导入 / 同步”类接口成功后，都要考虑是否需要返回只读验收摘要。
- `status=success` 或 `status=reused` 不等于可进入 AI 信号规则。
- `ready_for_signals` 才是进入真实业务异常和机会判断的门槛。
- 缺配置或失败状态不能伪造 inspection。

验证方式：

```text
python -m pytest tests\test_api_routes.py::test_snapshot_create_route_returns_manual_request_result -q
结果：1 passed

npm run test:snapshot-ui
结果：通过

POST /api/snapshot/create
结果：当前缺配置时 status=not_ready，inspection=None
```

### 问题 29：CLI 创建成功后不输出验收结果会让命令行操作者误判

问题：

```text
POST /api/snapshot/create 已经会在成功创建或复用快照后返回 inspection，
但 scripts/create_api_snapshot.py 的成功输出仍只包含 manifest。
命令行操作者可能把 success / reused 误判为“可进入 AI 信号规则”。
```

原因：

```text
API 入口和 CLI 入口是同一个人工拉取动作的两种使用方式。
如果两边输出合同不一致，真实数据闭环会在命令行路径上再次断开。
```

解决方案：

```text
create_api_snapshot.py 成功创建或复用快照后，立即调用 inspect_api_snapshot。
CLI JSON 输出增加 inspection。
缺配置 not_ready 时仍不生成伪 inspection。
```

后续防错规则：

- 同一个人工动作的 API 和 CLI 输出合同必须一致。
- `success` / `reused` 只表示快照创建动作完成，不表示信号输入可用。
- CLI 成功输出必须能直接看到 `ready_for_signals`、`signal_row_count` 和 `issues`。
- 没有真实凭据时只能返回 `not_ready`，不能为了演示生成伪快照。

验证方式：

```text
python -m pytest tests\test_snapshot_cli.py::test_create_api_snapshot_cli_success_prints_inspection -q
结果：1 passed

python -m pytest
结果：36 passed

npm run test:snapshot-ui
结果：通过

npm run build
结果：通过

python scripts\create_api_snapshot.py --market-id 1 --count 1 --max-pages 1
结果：not_ready，缺少 GERPGO_APP_ID_AND_GERPGO_APP_KEY_OR_GERPGO_ACCESS_TOKEN，未请求积加 API
```

### 问题 30：CLI 信号扫描没有复用真实信号数据链路

问题：

```text
scripts/run_signal_scan.py 只调用 detect_signals()，没有读取最新 API 快照、ABA Top1000、快照状态和配置 readiness。
在无 API 快照时会输出空数组，容易被误判为“没有业务异常或机会”。
```

原因：

```text
/api/signals 已经组合了快照行、ABA 行和数据质量信号，但 CLI 验收入口没有同步复用这条链路。
真实数据闭环需要命令行和前端入口都能回答“是否有可解释 AI 信号”。
```

解决方案：

```text
run_signal_scan.py 新增 build_signal_scan_payload。
脚本读取 API 快照行、ABA Top1000、snapshot_status、snapshot_readiness 和 inspection。
输出 inspection、ready_for_signals、输入行数、信号数量和完整 signals。
无成功 API 快照时返回数据质量信号，不返回空数组。
```

后续防错规则：

- `/api/signals` 和 `run_signal_scan.py` 必须使用同一套信号生成链路。
- 信号扫描入口不能只调用空的 `detect_signals()`。
- 无 API 快照时空信号队列不是正常业务状态，必须输出数据质量信号。
- ABA Top1000 存在但 API 快照缺失时，不能生成店铺业务机会结论。

验证方式：

```text
python -m pytest tests\test_signal_scan_cli.py -q
结果：2 passed

python -m pytest
结果：38 passed

python scripts\run_signal_scan.py
结果：ready_for_signals=false，NO_API_SNAPSHOT，返回 sig-data-quality-api-snapshot-missing
```

### 问题 31：真实快照拉取、验收和信号扫描分散成多条命令

问题：

```text
真实 API 快照链路需要先拉取，再 inspect，再 run_signal_scan。
如果人工只执行其中一条命令，容易误判“已经有真实数据”或“没有业务信号”。
```

原因：

```text
真实数据闭环的本质不是拉到文件，而是能证明数据可用于 AI 信号。
拉取成功、快照结构可用、信号扫描可解释必须在同一条验收链路里出现。
```

解决方案：

```text
新增 scripts/run_snapshot_pipeline.py。
该脚本统一执行 request_api_snapshot、inspect_api_snapshot 和 build_signal_scan_payload。
缺配置时返回 not_ready，不请求积加 API，并输出数据质量信号。
```

后续防错规则：

- 真实 API 验收优先运行 `run_snapshot_pipeline.py`。
- `create.status=success/reused` 不能单独证明 AI 信号可用。
- `inspection.ready_for_signals=true` 才能进入真实业务信号判断。
- 无 API 快照时只能输出数据质量信号，不能伪造业务机会点。

验证方式：

```text
python -m pytest tests\test_snapshot_pipeline_cli.py -q
结果：2 passed

python scripts\run_snapshot_pipeline.py --market-id 1 --count 1 --max-pages 1
结果：pipeline_status=not_ready，未请求积加 API，返回数据质量信号
```

### 问题 32：流水线已传 market_id，但信号扫描 readiness 重新误判市场配置

问题：

```text
run_snapshot_pipeline.py 已传入 --market-id 1。
create 阶段只缺真实 API 凭据。
但 signal_scan 阶段重新读取 readiness 时没有继承 market_id，可能额外报 GERPGO_MARKET_IDS 缺失。
```

原因：

```text
build_signal_scan_payload 原来没有 selected_market_id 参数。
流水线里的 create、inspection、signal_scan 三段虽然在同一条命令里，但 readiness 上下文没有完全传递。
```

解决方案：

```text
build_signal_scan_payload 增加 selected_market_id。
run_snapshot_pipeline.py 调用 signal_scan 时传入本次 market_id。
测试覆盖信号扫描和流水线两处参数传递。
```

后续防错规则：

- 同一条流水线的 `market_id` 必须贯穿 create、inspection 和 signal_scan。
- 配置缺失信号必须区分“未选市场”和“缺少凭据”。
- 用户已经通过参数选择市场时，不能在数据质量信号里再次误报市场缺失。

验证方式：

```text
python -m pytest tests\test_signal_scan_cli.py tests\test_snapshot_pipeline_cli.py -q
结果：5 passed

python scripts\run_snapshot_pipeline.py --market-id 1 --count 1 --max-pages 1
结果：create_missing 和 signal_missing 一致，只缺 GERPGO_APP_ID_AND_GERPGO_APP_KEY_OR_GERPGO_ACCESS_TOKEN
```

### 问题 33：直接运行 run_signal_scan 时无法传入市场上下文

问题：

```text
run_snapshot_pipeline.py 已经能把 --market-id 传给 signal_scan。
但直接运行 run_signal_scan.py 时没有 --market-id 参数。
如果 .env 不存在，直接扫描会同时提示 GERPGO_MARKET_IDS 和凭据缺失。
```

原因：

```text
build_signal_scan_payload 已支持 selected_market_id，但 CLI main() 没有暴露参数。
流水线入口和直接扫描入口的验收口径不一致。
```

解决方案：

```text
run_signal_scan.py 增加 --market-id 参数。
main() 调用 build_signal_scan_payload(selected_market_id=args.market_id)。
真实数据快照方案.md 的推荐扫描命令改为 python scripts\run_signal_scan.py --market-id 1。
```

后续防错规则：

- 直接扫描真实信号时推荐显式传入 `--market-id`。
- 流水线入口和直接扫描入口的市场上下文必须一致。
- 不带市场参数时出现 `GERPGO_MARKET_IDS` 是配置缺失提示，不代表业务数据异常。

验证方式：

```text
python -m pytest tests\test_signal_scan_cli.py -q
结果：4 passed

python scripts\run_signal_scan.py --market-id 1
结果：signal_market_id=1，只缺 GERPGO_APP_ID_AND_GERPGO_APP_KEY_OR_GERPGO_ACCESS_TOKEN
```

### 问题 34：后端 API 入口没有继承顶部筛选 market_id

问题：

```text
命令行入口已经支持 --market-id 1。
但工作台后端 API 的 /api/snapshot/readiness 和 /api/signals 没有接收 market_id 查询参数。
如果前端已经选择店铺站点，后端仍可能按未选择市场来判断 readiness。
```

原因：

```text
CLI、流水线和 API 三类入口的上下文传递没有统一。
之前只修了命令行链路，没有覆盖工作台页面读取的 API 链路。
```

解决方案：

```text
/api/snapshot/readiness 增加 market_id 查询参数。
/api/signals 增加 market_id 查询参数，并传给数据质量 readiness。
/api/signals/{signal_id} 和列表入口保持同一 market_id 上下文。
新增 API 路由测试覆盖参数传递。
```

后续防错规则：

- 顶部筛选器里的 `market_id` 必须贯穿页面、API、CLI 和流水线。
- 数据质量信号必须区分“未选择市场”和“缺少凭据”。
- 修 CLI 链路时要同步检查页面 API 链路是否存在同类断点。

验证方式：

```text
python -m pytest tests\test_api_routes.py::test_snapshot_readiness_route_passes_market_id_query tests\test_api_routes.py::test_signals_route_passes_market_id_to_data_quality_readiness -q
结果：2 passed

python -m pytest
结果：44 passed

GET /api/snapshot/readiness?market_id=1 和 GET /api/signals?market_id=1
结果：market_id=1，只缺 GERPGO_APP_ID_AND_GERPGO_APP_KEY_OR_GERPGO_ACCESS_TOKEN
```

### 问题 35：前端读取信号时没有传递顶部筛选 market_id

问题：

```text
后端 API 已支持 /api/signals?market_id=1 和 /api/snapshot/readiness?market_id=1。
但前端仍调用 /api/signals 和 /api/snapshot/readiness。
这会让页面层继续丢失顶部筛选器上下文。
```

原因：

```text
后端、CLI、流水线已补齐 market_id，但前端 API client 没有同步升级。
顶部筛选器只是展示，没有成为请求上下文。
```

解决方案：

```text
新增 marketScopedPath 统一构造 market_id 查询参数。
fetchSignals 和 fetchSnapshotReadiness 支持 marketId 参数。
SignalTriageWorkbench 使用 selectedMarketId=1 调用信号、readiness 和创建快照。
```

后续防错规则：

- 顶部筛选器不能只展示，必须成为 API 请求上下文。
- 后端新增查询参数后，要同步检查前端 API client 和页面调用点。
- 店铺站点上下文必须从页面贯穿到 API、CLI 和流水线。

验证方式：

```text
npm run test:snapshot-ui
结果：通过

npm run build
结果：通过

python -m pytest
结果：44 passed
```

### 问题 36：工作台后端缺少与脚本一致的一键流水线入口

问题：

```text
脚本 run_snapshot_pipeline.py 已能一次返回 create、inspection、signal_scan。
但后端 API 只有 /api/snapshot/create、/api/snapshot/inspection、/api/signals 等分散入口。
工作台如果只调用 create，不能在一个响应中确认快照是否可进入 AI 信号。
```

原因：

```text
真实 API 闭环一开始先在 CLI 脚本中打通，后端 API 合同没有同步补齐。
这会导致脚本验收和页面验收口径不一致。
```

解决方案：

```text
新增 POST /api/snapshot/pipeline。
响应同时包含 pipeline_status、create、inspection、signal_scan。
缺凭据时返回 not_ready，不请求积加 API，并返回数据质量信号。
```

后续防错规则：

- 真实快照验收不要只看 create 是否成功。
- 工作台后端入口必须能证明 inspection 和 signal_scan 的结果。
- 缺凭据、无快照、空快照都只能生成数据质量信号，不能冒充真实业务异常。

验证方式：

```text
python -m pytest tests\test_api_routes.py::test_snapshot_pipeline_route_returns_not_ready_with_signal_scan -q
结果：1 passed

python -m pytest
结果：45 passed

POST /api/snapshot/pipeline
结果：pipeline_status=not_ready，signal_market_id=1，只缺 GERPGO_APP_ID_AND_GERPGO_APP_KEY_OR_GERPGO_ACCESS_TOKEN
```

### 问题 37：前端按钮仍调用底层 create，页面不能一次看到完整流水线结果

问题：

```text
后端已经提供 /api/snapshot/pipeline。
但工作台“拉取快照”按钮仍调用 /api/snapshot/create。
页面只能看到 create 结果，不能在同一个响应里看到 inspection 和 signal_scan。
```

原因：

```text
后端流水线入口补齐后，前端 API client 和页面事件没有同步迁移。
这会让 CLI 一键验收和页面人工触发验收口径不一致。
```

解决方案：

```text
新增 runSnapshotPipeline。
新增 snapshotPipelineMessage。
工作台按钮改为调用 /api/snapshot/pipeline。
页面消息统一展示 create / inspection / signal_scan。
```

后续防错规则：

- 工作台人工拉取按钮必须调用 pipeline，而不是只调用 create。
- 新增后端一键合同后，必须同步检查前端 API client、页面事件和 UI 消息。
- 页面不直接请求积加 API，也不传递任何凭据。

验证方式：

```text
npm run test:snapshot-ui
结果：通过

npm run build
结果：通过

python -m pytest
结果：45 passed

python scripts\run_snapshot_pipeline.py --market-id 1 --days 7 --count 1 --max-pages 1
结果：pipeline_status=not_ready，只缺 GERPGO_APP_ID_AND_GERPGO_APP_KEY_OR_GERPGO_ACCESS_TOKEN
```

### 问题 38：not_ready 只返回缺失键名，缺少可执行下一步

问题：

```text
真实快照流水线在缺少凭据时只返回 missing。
这能证明没有请求积加 API，但对用户下一步配置动作不够直接。
```

原因：

```text
缺凭据状态最初只作为安全保护和测试断言处理，没有把运营可见的恢复动作作为 API 合同返回。
```

解决方案：

```text
SnapshotCreateResult 新增 next_action。
缺少 Gerpgo 凭据时，/api/snapshot/create、/api/snapshot/pipeline 和 run_snapshot_pipeline.py 都返回同一条下一步配置动作。
前端 snapshotPipelineMessage 显示 next_action。
```

后续防错规则：

- 数据链路状态不能只给错误码或缺失键名，必须给不泄密的恢复动作。
- `next_action` 只能描述人工配置或人工触发，不得自动复制旧项目凭据。
- 缺凭据时仍不能请求积加 API，也不能生成真实业务异常结论。

验证方式：

```text
python -m pytest tests\test_api_routes.py::test_snapshot_pipeline_route_returns_not_ready_with_signal_scan -q
结果：1 passed

npm run test:snapshot-ui
结果：通过

python -m pytest
结果：45 passed

npm run build
结果：通过

python scripts\run_snapshot_pipeline.py --market-id 1 --days 7 --count 1 --max-pages 1
结果：create.next_action 已返回
```

### 问题 39：check-config 与 readiness 缺少 next_action，配置诊断入口和流水线反馈不一致

问题：

```text
create 和 pipeline 已经在缺凭据时返回 next_action。
但 --check-config、/api/snapshot/readiness 和工作台顶部数据状态仍只展示 missing / not_ready。
用户从配置诊断入口进入时，看不到同一条恢复动作。
```

原因：

```text
上一轮只补了快照创建结果 SnapshotCreateResult，没有把同一条配置恢复动作下沉到 SnapshotReadiness。
这会让不同入口对同一类缺凭据问题给出不同反馈。
```

解决方案：

```text
新增 snapshot_guidance.py 统一保存缺凭据 next_action。
SnapshotReadiness 新增 next_action。
check_snapshot_readiness、snapshot_readiness service、create_api_snapshot.py --check-config 和前端 snapshotFreshnessText 共用同一条提示。
```

后续防错规则：

- 同一类数据链路问题只能有一套用户可见恢复动作。
- `create`、`pipeline`、`check-config`、`readiness` 和工作台状态必须一起检查。
- 缺凭据提示只能指向新项目 `.env` 或当前进程环境变量，不能引导复制旧项目凭据。

验证方式：

```text
python -m pytest tests\test_snapshot_readiness.py::test_snapshot_readiness_reports_missing_config_without_secret_values -q
结果：1 passed

python -m pytest tests\test_snapshot_cli.py::test_create_api_snapshot_cli_returns_not_ready_without_credentials tests\test_snapshot_cli.py::test_create_api_snapshot_check_config_returns_next_action_without_credentials -q
结果：2 passed

npm run test:snapshot-ui
结果：通过

python -m pytest
结果：46 passed

npm run build
结果：通过

python scripts\create_api_snapshot.py --market-id 1 --check-config
结果：readiness.next_action 已返回
```

### 问题 40：凭据存在但 API 请求失败时，pipeline 直接中断

问题：

```text
缺凭据场景已经能返回 not_ready。
但如果用户配置了凭据，真实积加 API 请求返回 HTTP 错误、权限错误或 token 错误，pipeline 会直接抛异常。
页面或脚本拿不到 inspection 和 signal_scan，无法判断当前是否还有可复用快照或数据质量信号。
```

原因：

```text
之前只把配置缺失作为可预期状态处理。
真实 API 运行期失败没有被纳入流水线合同。
```

解决方案：

```text
SnapshotRequestFailed 表示配置已通过但 API 请求失败。
build_snapshot_failure_result 统一返回不泄密的 failed 结果。
run_snapshot_pipeline.py 和 POST /api/snapshot/pipeline 在 failed 时继续返回 inspection 和 signal_scan。
```

后续防错规则：

- 真实 API 拉取失败不能让 pipeline 直接中断。
- `not_ready` 表示配置未就绪，`failed` 表示配置已过检查但 API 请求失败。
- API 失败响应不得包含原始 accessToken、AppKey 或其它密钥值。
- API 失败时只能生成数据链路级信号，不能生成真实业务异常结论。

验证方式：

```text
python -m pytest tests\test_snapshot_pipeline_cli.py::test_snapshot_pipeline_returns_failed_and_keeps_inspection_and_signal_scan -q
结果：1 passed

python -m pytest tests\test_api_routes.py::test_snapshot_pipeline_route_returns_failed_with_signal_scan -q
结果：1 passed

python -m pytest
结果：48 passed
```

### 问题 41：缺少轻量 API 探测，首次真实拉取风险过高

问题：

```text
填好 Gerpgo 凭据后，之前只能直接运行完整快照流水线。
如果 token、market_id 或接口权限有问题，第一次真实请求就会进入完整拉取路径，排查成本高。
```

原因：

```text
check-config 只检查配置是否存在，不验证真实开放平台 token 和 market_id 是否可用。
完整快照会连续请求多个 API，不适合作为凭据接入后的第一步验证。
```

解决方案：

```text
create_api_snapshot.py 新增 --probe-api。
该入口只请求 marketNames/query，返回店铺站点安全摘要，不写入快照，不拉取广告明细。
缺凭据时仍复用 not_ready 合同。
```

后续防错规则：

- 真实凭据接入后，先跑 `--probe-api`，再跑完整 `run_snapshot_pipeline.py`。
- 探测入口不得输出 accessToken、AppKey 或原始响应中的敏感字段。
- 探测入口不得写入 `data/api_snapshots`，不能被当作真实快照。
- 探测成功只证明 token、market_id 和轻量接口可用，不代表广告明细 API 都可用。

验证方式：

```text
python -m pytest tests\test_gerpgo_snapshot.py::test_probe_gerpgo_market_access_returns_safe_market_summary -q
结果：1 passed

python -m pytest tests\test_snapshot_cli.py::test_create_api_snapshot_probe_api_returns_not_ready_without_credentials tests\test_snapshot_cli.py::test_create_api_snapshot_probe_api_success -q
结果：2 passed

python -m pytest
结果：51 passed
```

### 问题 42：轻量 API 探测停留在 CLI，工作台仍可能直接拉完整快照

问题：

```text
CLI 已有 --probe-api，但运营主要在工作台操作。
如果页面只有“拉取快照”，用户填好凭据后仍容易直接进入完整快照流水线。
这会让 token、market_id 或接口权限问题在重请求路径里暴露，排查成本高。
```

原因：

```text
数据链路能力和工作台操作路径没有同步。
只完成脚本能力不等于完成可用产品入口。
```

解决方案：

```text
新增 POST /api/snapshot/probe。
工作台顶部新增“探测 API”按钮，调用 probeSnapshotApi。
页面用 snapshotProbeMessage 展示 ready、not_ready 或 failed，并保留 next_action。
```

后续防错规则：

- 每个真实数据能力都要同时检查 CLI、后端 API、工作台入口。
- “探测 API”不得写入快照，不得拉取广告明细。
- 缺少凭据时页面只能提示配置动作，不能生成业务异常或机会结论。
- “拉取快照”仍必须走人工点击，不允许 probe 成功后自动执行。

验证方式：

```text
python -m pytest tests\test_api_routes.py::test_snapshot_probe_route_returns_not_ready_without_secret_values tests\test_api_routes.py::test_snapshot_probe_route_returns_safe_market_summary -q
结果：2 passed

npm run test:snapshot-ui
结果：通过

python -m pytest
结果：53 passed

npm run build
结果：通过

http://127.0.0.1:5180 页面 smoke
结果：“探测 API”按钮可点击，点击后展示 API 探测未就绪和缺失凭据键
```

### 问题 43：顶部筛选硬编码 market_id，容易把样本上下文当真实店铺事实

问题：

```text
工作台顶部店铺 / 站点筛选之前在前端固定显示 market_id 1。
这能支撑第一阶段样本，但在真实 API 接入后容易误导：
页面看起来像已经知道真实店铺和站点，实际可能只是样本默认值。
```

原因：

```text
顶部筛选器没有独立读取真实快照 manifest 或 Gerpgo 配置。
店铺站点上下文被写在前端展示里，而不是来自数据链路。
```

解决方案：

```text
新增 GET /api/market-options。
选项优先来自成功快照 manifest，其次来自新项目 GERPGO_MARKET_IDS。
完全没有配置时只返回 project_sample 选项，显示 market-1 / 待探测。
前端店铺和站点 select 改为读取 fetchMarketOptions。
```

后续防错规则：

- 前端不能把样本店铺名写成真实事实。
- 真实店铺名和站点代码只能来自成功快照或真实 API 探测结果。
- 没有真实快照时，页面只能显示 `待探测`，不能显示成已确认店铺。
- 多店铺能力先做筛选上下文，不做跨店铺总览。

验证方式：

```text
python -m pytest tests\test_market_options.py -q
结果：2 passed

python -m pytest tests\test_api_routes.py::test_market_options_route_returns_safe_filter_options -q
结果：1 passed

npm run test:snapshot-ui
结果：通过

python -m pytest
结果：56 passed

npm run build
结果：通过

GET http://127.0.0.1:8023/api/market-options
结果：返回 project_sample 选项，不伪装成真实店铺

http://127.0.0.1:5181 页面 smoke
结果：顶部筛选显示 market-1 / market_id 1，API 探测按钮可点击
```

### 问题 44：探测成功后页面仍停留待探测

问题：

```text
轻量 API 探测成功能拿到店铺站点摘要，但如果不缓存该安全摘要，完整快照成功前顶部筛选仍只能显示 market-1 / 待探测。
```

原因：

```text
market option 来源之前只有成功快照 manifest、Gerpgo 配置 market_id 和 project_sample。
probe 成功结果没有进入筛选选项来源。
```

解决方案：

```text
新增 data/runtime/market_options.json。
POST /api/snapshot/probe 返回 ready 后，只缓存 market_id、shop_name、marketplace_code、country 等安全筛选字段。
GET /api/market-options 在成功快照之后、配置 market_id 之前读取 probe_cache。
前端 probe ready 后重新读取 market options。
```

后续防错规则：

- probe cache 不能写入 `data/api_snapshots`。
- probe cache 不能保存 accessToken、AppKey、原始响应或广告明细。
- probe cache 只能证明店铺站点上下文已探测，不能证明广告明细 API 可用。
- 缺凭据或探测失败时不能创建 probe cache。

验证方式：

```text
python -m pytest tests\test_market_options.py -q
结果：3 passed

python -m pytest tests\test_api_routes.py::test_snapshot_probe_route_returns_safe_market_summary -q
结果：1 passed

python scripts\create_api_snapshot.py --market-id 1 --probe-api
结果：not_ready，缺少凭据且未创建 data/runtime/market_options.json
```

### 问题 45：CLI 探测成功后工作台仍可能看不到真实店铺站点

问题：

```text
如果用户先运行 python scripts\create_api_snapshot.py --market-id 1 --probe-api，CLI 能返回 ready 和安全店铺站点摘要。
但早期只有页面 POST /api/snapshot/probe 会写入 market option 缓存。
这会导致 CLI 探测 ready 后，工作台顶部筛选仍可能显示 market-1 / 待探测。
```

原因：

```text
CLI 探测入口和工作台探测入口没有复用同一个安全缓存写入动作。
真实 API 闭环的脚本入口和页面入口口径不一致。
```

解决方案：

```text
create_api_snapshot.py 在 --probe-api 返回 ready 后调用 save_probe_market_option。
CLI 和页面统一写 data/runtime/market_options.json。
```

后续防错规则：

- 每个真实数据能力都要同时检查 CLI、后端 API 和工作台入口。
- CLI 探测成功可以写安全筛选缓存，但不能写 `data/api_snapshots`。
- CLI 探测成功只证明 token、market_id 和 `marketNames/query` 可用，不能证明广告明细 API 可用。
- 缺凭据或探测失败时不能创建 probe cache。

验证方式：

```text
python -m pytest tests\test_snapshot_cli.py::test_create_api_snapshot_probe_api_success -q
结果：1 passed

python scripts\create_api_snapshot.py --market-id 1 --probe-api
结果：not_ready，缺少凭据且未创建 data/runtime/market_options.json
```

### 问题 46：轻量探测 HTTP 成功但 0 行被误判为 ready

问题：

```text
使用一次性旧项目 Gerpgo 凭据探测 market_id=1 时，marketNames/query HTTP 请求成功，但 data 返回 0 行。
早期 probe_gerpgo_market_access 仍返回 ready，且 CLI / 页面可能写入 market-1 / 待探测 的 probe cache。
```

原因：

```text
探测逻辑只判断接口没有报错，没有要求返回行中必须匹配目标 market_id。
这会把“接口可请求”误判为“店铺站点已确认”。
```

解决方案：

```text
probe_gerpgo_market_access 只有匹配到目标 market_id 时才返回 ready。
0 行或未匹配目标 market_id 时返回 not_ready，并给出检查凭据、market_id 和开放平台接口权限的 next_action。
POST /api/snapshot/probe 只在 ready 时写 probe cache。
GET /api/market-options 忽略历史遗留的 market-1 / 待探测 probe cache。
```

后续防错规则：

- HTTP 200 或积加 `code=0` 不等于业务数据已可用。
- `marketNames/query` 探测必须同时满足：有返回行、匹配目标 `market_id`、返回安全店铺站点摘要。
- `row_count=0` 时不能写 `data/runtime/market_options.json`。
- 旧项目 `.env` 只能作为一次性只读排查凭据来源，不能复制到新项目，也不能当作新项目配置完成。

验证方式：

```text
python -m pytest tests\test_gerpgo_snapshot.py::test_probe_gerpgo_market_access_requires_matching_market_row -q
结果：1 passed

python -m pytest tests\test_api_routes.py::test_snapshot_probe_route_does_not_cache_unmatched_market -q
结果：1 passed

python -m pytest tests\test_market_options.py::test_market_options_ignore_unconfirmed_probe_cache -q
结果：1 passed

一次性旧凭据只读探测
结果：not_ready，row_count=0，market=null
```

### 问题 47：多店铺配置只探测第一个 market_id 容易卡住

问题：

```text
当前业务是多店铺站点。如果脚本只探测默认第一个 GERPGO_MARKET_IDS，遇到第一个 market_id 返回 0 行时，无法判断其它候选 market_id 是否可用。
```

原因：

```text
单 market_id 探测适合已知店铺站点，不适合凭据接入初期的多候选排查。
但直接写 probe cache 又可能把未确认店铺站点带入工作台筛选器。
```

解决方案：

```text
create_api_snapshot.py 新增 --probe-config-markets。
该入口逐个探测配置内 GERPGO_MARKET_IDS，只输出 ready / not_ready / failed 汇总，不写快照，不写 probe cache。
```

后续防错规则：

- 多店铺凭据首次接入时，先跑 `--probe-config-markets`，不要只盯默认第一个 `market_id`。
- 只有单个 `--probe-api` 返回 ready 且 market 不为空时，才允许写安全筛选缓存。
- 多 market_id 批量诊断入口只做排查，不改变工作台当前筛选上下文。

验证方式：

```text
python -m pytest tests\test_snapshot_cli.py::test_create_api_snapshot_probe_config_markets_returns_per_market_results tests\test_snapshot_cli.py::test_create_api_snapshot_probe_config_markets_returns_not_ready_without_market_ids -q
结果：2 passed
```

### 问题 48：完整快照入口可能绕过店铺站点确认

问题：

```text
轻量探测已经要求 marketNames/query 必须返回目标 market_id。
但完整快照 create_gerpgo_snapshot 仍会在 marketNames/query 返回 0 行时继续拉销售和广告明细，并可能用 market-{id} 兜底生成快照。
```

原因：

```text
探测逻辑和完整快照逻辑没有共用同一条“目标店铺站点必须先确认”的业务闸。
```

解决方案：

```text
create_gerpgo_snapshot 先请求 marketNames/query。
只有返回行中匹配目标 market_id 时才继续拉销售和广告明细。
未匹配时直接抛出可解释错误，不写成功快照。
```

后续防错规则：

- 任何完整 API 快照拉取都必须先确认店铺站点。
- 不能用 `market-{id}` 作为真实店铺名称兜底写入成功快照。
- 轻量探测和完整快照入口必须共享同一套 market_id 匹配规则。

验证方式：

```text
python -m pytest tests\test_gerpgo_snapshot.py::test_create_gerpgo_snapshot_stops_when_market_name_does_not_match -q
结果：1 passed
```

### 问题 49：pipeline 泛化失败原因导致排查成本高

问题：

```text
create_gerpgo_snapshot 已能在 marketNames/query 未返回目标店铺站点时抛出明确原因：
积加 API 未返回目标店铺站点，已停止完整快照拉取。

但 request_api_snapshot 会把所有异常重新包装成：
积加 API 请求失败，已保留当前本地快照状态。

页面和 CLI 只能看到泛化失败，无法判断是凭据失败、接口失败，还是店铺站点未命中。
```

原因：

```text
异常包装层只考虑了“不泄露密钥”，没有区分安全业务原因和敏感 HTTP / 凭据错误。
```

解决方案：

```text
SnapshotRequestFailed 增加可选 message，并集中执行安全清洗。
不含 accessToken / appKey / appId / token / secret 等敏感标记的原因可以透传。
包含敏感标记的原因统一退回泛化安全文案。
API route 和 CLI fallback 都使用同一个 SnapshotRequestFailed 清洗逻辑。
```

后续防错规则：

- `pipeline_status=failed` 不等于只展示泛化错误；能安全展示的根因必须进入 `create.message`。
- 所有失败消息必须先过统一清洗逻辑，不能在 route / CLI 各自拼接。
- 真实 API 失败验证要同时检查：失败原因可解释、无密钥泄露、未写入伪成功快照。

验证方式：

```text
python -m pytest tests\test_snapshot_request.py::test_request_api_snapshot_preserves_safe_failure_reason tests\test_snapshot_request.py::test_request_api_snapshot_redacts_unsafe_failure_reason -q
结果：2 passed

一次性旧凭据真实 pipeline smoke
结果：pipeline_status=failed，create.message=积加 API 未返回目标店铺站点，已停止完整快照拉取，快照目录数量不变
```

### 问题 50：marketNames/query 字符串响应被误判为 0 行

问题：

```text
真实请求 /middle/base/marketNames/query，参数 {"markerIds":[1]}，返回 data 类型是字符串：
rivbos:US

新项目解析器只支持 data 为数组的情况，因此把可用店铺站点误判成 row_count=0 / not_ready。
```

原因：

```text
新项目根据理想化数组结构写了 _safe_market_rows，没有兼容旧项目已经验证过的字符串返回形态。
```

解决方案：

```text
_safe_market_rows 同时支持数组和字符串。
字符串按分号拆多店铺，按冒号拆 店铺:站点，并绑定本次请求的 market_id。
create_gerpgo_snapshot 的 shop_name / marketplace_code 也复用同一套解析口径。
```

后续防错规则：

- 积加 API 的 `data` 形态不能只按理想 JSON 表格假设，必须以真实响应为准。
- 店铺站点接口要同时覆盖字符串形态和数组形态测试。
- 不能把解析失败直接等同于店铺未授权，必须先检查真实 `data_type`。

验证方式：

```text
python -m pytest tests\test_gerpgo_snapshot.py::test_probe_gerpgo_market_access_accepts_text_market_name_response tests\test_gerpgo_snapshot.py::test_create_gerpgo_snapshot_accepts_text_market_name_response -q
结果：2 passed

python scripts\create_api_snapshot.py --probe-config-markets
结果：market_id=1 ready，market=rivbos / US，未写快照和缓存
```

### 问题 51：广告位行被误分类成搜索词机会

问题：

```text
第一份真实快照生成 signal_scan 后，source_table=ad_placement_daily_metrics 的广告位行触发了搜索词机会规则。
页面会出现“搜索词  转化稳定，存在放量机会”这种空搜索词文案。
```

原因：

```text
搜索词机会规则只看 orders 和 ACOS，没有先判断来源表和对象类型。
广告位行也有 orders / sales / ACOS，因此被错误归入 search_term_opportunity。
```

解决方案：

```text
source_table=ad_placement_daily_metrics 的行走广告位规则。
搜索词机会规则必须要求不是广告位行，且存在真实 normalized/search_term。
低 ACOS 且有订单的广告位生成 placement_efficiency 机会信号。
```

后续防错规则：

- 规则触发前必须先判断对象类型，不能只看指标。
- 广告位、搜索词、广告商品即使指标字段相同，也不能共用同一条文案。
- 真实快照生成信号后要检查 summary 是否出现空对象名。

验证方式：

```text
python -m pytest tests\test_signal_detection.py::test_detects_placement_opportunity_without_empty_search_term_signal -q
结果：1 passed

python scripts\run_signal_scan.py --market-id 1
结果：signal_count=3，全部为 object_type=placement 的广告位机会信号
```

### 问题 52：销售表现分页对象被误判为空数据

问题：

```text
第一份真实 API 快照中 raw/sales_performance.json 实际包含销售表现数据：
data.total / data.page / data.rows

但 normalized/sales_products.json 为空，manifest 显示 sales_products=0。
```

原因：

```text
通用 _rows() 只支持 data 为数组。
salesAnalysis/page 返回的是分页对象，真实行在 data.rows 中。
解析器没有展开 data.rows，导致销售商品被误判为 0 行。
```

解决方案：

```text
_rows() 增加 data.rows 解析。
新增回归测试覆盖 salesAnalysis/page 的分页对象结构。
使用本地 raw 快照重建 normalized，不再额外请求积加 API。
```

后续防错规则：

- 遇到 normalized 行数为 0 时，必须先检查 raw 的真实结构和嵌套行字段。
- 不能把 `data` 不是数组直接判断为接口无数据。
- 真实 API 探测触发 509 限流后必须停止继续请求，优先使用本地 raw 快照排查。
- 销售商品进入快照后，产品销售承接信号仍需单独实现规则，不能自动推断已有高置信产品信号。

验证方式：

```text
python -m pytest tests\test_gerpgo_snapshot.py::test_build_snapshot_payload_reads_sales_rows_from_paged_data_object -q
结果：1 passed

python scripts\inspect_api_snapshot.py
结果：ready_for_signals=true，sales_products=10，issues=[]
```

### 问题 53：销售商品主数据进入快照但销售承接信号仍缺指标输入

问题：

```text
sales_products 已经有 10 行，但它只表示销售商品对象。
产品销售强但广告弱需要销售订单、销售额、广告订单、广告花费等周期指标。
如果只读取 sales_products，产品 / 广告商品销售承接信号仍然没有足够证据。
```

原因：

```text
销售商品主数据和销售表现事实表被混在了同一轮理解里。
标准化层只输出 sales_products，没有输出 sales_product_daily_metrics。
信号输入层也只读取广告搜索词和广告位，没有读取销售商品指标和广告商品指标。
```

解决方案：

```text
build_snapshot_payload 新增 normalized/sales_product_daily_metrics.json。
load_signal_rows_from_latest_snapshot 读取 sales_product_daily_metrics 和 advertised_products。
detect_signals 新增 product_ad_coverage 规则，并限制搜索词规则只消费 ad_search_term_daily_metrics。
```

后续防错规则：

- 看到 `sales_products` 只能说明商品对象存在，不能说明销售表现已可分析。
- 产品类信号必须同时检查对象表和事实指标表。
- 销售商品与广告商品只能按同一店铺站点下的 `asin / msku / sku` 关联，不能回退到广告组归因。
- 真实快照无销售订单或无商品键命中时，不生成产品销售承接信号。
- 新增 normalized 表后必须检查是否进入信号输入链路。

验证方式：

```text
python -m pytest tests\test_gerpgo_snapshot.py::test_build_snapshot_payload_reads_sales_rows_from_paged_data_object -q
结果：1 passed

python -m pytest tests\test_snapshot_store.py::test_signal_rows_load_from_sales_and_advertised_product_metrics -q
结果：1 passed

python -m pytest tests\test_signal_detection.py::test_detects_sales_product_strong_but_ad_weak_signal tests\test_signal_detection.py::test_does_not_detect_sales_product_signal_without_sales_strength -q
结果：2 passed

python scripts\run_signal_scan.py --market-id 1
结果：signal_row_count=42，signal_count=3，当前真实快照没有生成无证据产品信号
```

### 问题 54：产品承接类信号不触发时工作台容易静默

问题：

```text
真实快照中 sales_product_daily_metrics 和 advertised_products 都已经存在。
但当前销售商品订单全为 0，销售商品与广告商品的 asin / msku / sku 也没有命中。
系统没有生成产品销售强但广告弱信号是正确的，但如果工作台只显示 3 条广告位信号，用户不知道产品承接类信号为什么没有出现。
```

原因：

```text
原规则只区分“生成产品机会信号”和“不生成产品机会信号”。
没有把“数据存在但不足以判断产品承接”建成独立的数据质量信号。
```

解决方案：

```text
detect_signals 在 product_ad_coverage 机会信号为 0 时，检查 sales_product_daily_metrics 和 advertised_products。
如果两类输入都存在，但强销售商品为 0，则生成 sig-data-quality-product-ad-coverage-unavailable。
证据明确展示销售商品指标行、强销售商品、广告商品行和商品关联命中。
```

后续防错规则：

- 某类业务信号没有出现时，必须判断是“没有异常”还是“证据不足”。
- 证据不足要优先生成数据质量信号，不能让工作台静默。
- 产品承接诊断必须同时说明销售强度和商品关联键，不得回退到广告组归因。
- 数据质量信号只能给人工检查建议，不得出现广告动作执行建议。

验证方式：

```text
python -m pytest tests\test_signal_detection.py::test_detects_product_ad_coverage_data_gap_when_sales_signal_cannot_be_evaluated -q
结果：1 passed

python -m pytest
结果：73 passed

python scripts\run_signal_scan.py --market-id 1
结果：signal_count=4，新增产品承接证据不足的数据质量信号
```

### 问题 55：广告组汇总容易掩盖组内多商品消耗集中

问题：

```text
当前真实快照中 RBK004-Auto 广告组投放 2 个广告商品。
两个广告商品都属于同一广告组，但花费明显集中在其中一个广告商品。
如果只看广告组汇总，容易把头部广告商品表现误当成整个广告组内所有商品的表现。
```

原因：

```text
广告组是投放容器，不是产品。
原规则已经能识别广告位和产品承接数据缺口，但还没有把广告组内多商品结构风险作为独立信号输出。
```

解决方案：

```text
新增 detect_ad_group_structure_signals。
只读取 advertised_products，按 ad_group_id 分组。
同一广告组至少 2 个广告商品、周期总花费 >= 50、头部商品花费占比 >= 75% 时，生成 ad_group_structure 信号。
信号对象是 ad_group，证据列出广告商品数、广告组总花费、头部商品花费占比和头部广告商品。
```

后续防错规则：

- 广告组可以作为结构诊断对象，但不能作为商品。
- 商品级判断必须落到 advertised_product 或 sales_product。
- 广告组结构信号要说明“归因不确定”，不能把搜索词、广告位或广告组汇总直接归因到单个商品。
- 建议只能是人工复核、拆分观察或调整预算分配，不得出现自动广告动作。

验证方式：

```text
python -m pytest tests\test_signal_detection.py::test_detects_ad_group_spend_concentration_across_multiple_ad_products tests\test_signal_detection.py::test_does_not_detect_ad_group_spend_concentration_for_single_ad_product -q
结果：2 passed

python -m pytest
结果：75 passed

build_signal_scan_payload(selected_market_id=1)
结果：signal_count=5，新增 sig-ad-group-spend-concentration-34612947118490
```

### 问题 56：低花费有效长尾词被主机会阈值漏掉

问题：

```text
当前真实快照中 12 month sunglasses 花费 1.12，产生 2 个订单，ACOS 约 4.88%。
原搜索词机会规则要求订单 >= 3，导致这类低花费有效长尾词不会进入信号队列。
```

原因：

```text
主机会词规则适合稳定出单搜索词，但长尾词样本小、花费低，不能和主机会词使用同一个订单阈值。
```

解决方案：

```text
新增 P2 长尾搜索词机会规则。
仅消费 ad_search_term_daily_metrics 行，要求花费 > 0 且 <= 5、订单 >= 2 且 < 3、ACOS <= 10%。
信号建议只写成人工观察或小流量测试，不直接建议执行广告动作。
```

后续防错规则：

- 主机会词和长尾机会词必须分开阈值，避免高阈值漏掉小样本机会。
- 长尾机会只能给中等置信度，必须说明订单样本少。
- 搜索词规则必须先判断 `source_table=ad_search_term_daily_metrics`，不能误吃广告位或商品行。
- 单订单长尾词暂不触发，避免样本过小导致误报。

验证方式：

```text
python -m pytest tests\test_signal_detection.py::test_detects_long_tail_search_term_low_spend_high_conversion_signal tests\test_signal_detection.py::test_does_not_detect_long_tail_search_term_signal_with_single_order -q
结果：2 passed

python -m pytest
结果：77 passed

build_signal_scan_payload(selected_market_id=1)
结果：signal_count=6，新增 sig-long-tail-opportunity-gerpgo_market_1_20260614_075325:507606169
```

### 问题 57：逐行看搜索词会漏掉同词表现分化

问题：

```text
当前真实快照中 1 year old sunglasses 分布在 3 条投放行中。
聚合后总花费 4.06、总订单 2，但其中一条无订单行花费 2.24，占总花费 55.17%。
如果只逐行生成信号，会把有订单行和无订单消耗行割裂，用户看不到同一搜索词的结构分化。
```

原因：

```text
广告搜索词表现通常按广告活动、广告组、关键词或投放对象拆行。
同一 normalized_query 可能同时出现在多条投放行里，逐行判断不能回答“这个搜索词整体怎么样、哪里分化”。
```

解决方案：

```text
新增 search_term_performance_split 聚合规则。
只读取 ad_search_term_daily_metrics，按 normalized_query 分组。
同一词同时存在有订单行和无订单消耗行，且总花费、总订单、无订单花费占比达到阈值时，生成 P2 分化信号。
```

后续防错规则：

- 搜索词诊断既要支持逐行规则，也要支持按 `normalized_query` 聚合。
- 聚合信号不能直接归因到单个商品，只能提示人工对比广告活动、广告组和投放对象。
- 精确搜索词聚合先于复杂语义聚类，后续再接人工语义或模型语义分组。
- 如果只有单行、全有订单或全无订单，不生成“表现分化”信号。

验证方式：

```text
python -m pytest tests\test_signal_detection.py::test_detects_same_search_term_performance_split_signal tests\test_signal_detection.py::test_does_not_detect_same_search_term_split_without_mixed_outcomes -q
结果：2 passed

python -m pytest
结果：79 passed

build_signal_scan_payload(selected_market_id=1)
结果：signal_count=7，新增 sig-search-term-performance-split-gerpgo_market_1_20260614_075325:507606144
```

### 问题 58：ABA Top1000 参与扫描但过期状态未显式提示

问题：

```text
当前真实信号扫描会读取本地 ABA Top1000 快照，真实 ABA 周期为 2026-05-10 至 2026-05-16。
API 分析周期结束为 2026-06-14，两者相差 29 天，已经超过 14 天阈值。
原逻辑仍然只把 ABA 作为机会增强数据参与匹配，没有先生成数据质量信号提示 ABA 已过期。
```

原因：

```text
ABA 是人工导出的站点级数据，不属于自动 API 同步主链路。
如果只在机会规则里消费 ABA，而不先判断 ABA 新鲜度，用户会误以为相关机会判断和当前 API 快照同样新。
```

解决方案：

```text
新增 sig-data-quality-aba-stale。
detect_signals 在主流程开头先检查 aba_rows，取 ABA 最新 end_date 与 API rows 最新 end_date 对比。
当 stale_days > 14 时生成 P0 数据质量信号，证据列出 ABA 行数、ABA 周期、分析周期结束、过期天数和过期阈值。
```

后续防错规则：

- ABA 只作为站点级增强数据，不能当店铺数据。
- ABA 参与任何机会判断前，必须先检查数据新鲜度。
- ABA 过期时，相关机会信号必须降置信或被数据质量信号压住优先级。
- 建议动作只能是人工重新导出和导入 ABA 文件，不得出现广告自动执行动作。

验证方式：

```text
python -m pytest tests\test_signal_detection.py::test_detects_stale_aba_data_quality_signal tests\test_signal_detection.py::test_does_not_detect_aba_stale_signal_when_period_is_current -q
结果：2 passed

python -m pytest
结果：81 passed

build_signal_scan_payload(selected_market_id=1)
结果：signal_count=8，新增 sig-data-quality-aba-stale
```

### 问题 59：ABA 周期不一致的机会信号缺少低置信联动

问题：

```text
ABA 导入流程要求：ABA 导入周期超过阈值、或 ABA 周期和广告搜索词周期不一致时，必须降低 ABA 相关信号置信度。
上一轮已生成 sig-data-quality-aba-stale，但 ABA 热词机会规则仍然只支持同周期精确匹配。
如果未来当前广告搜索词命中了旧 ABA 热词，系统要么直接跳过机会提示，要么容易被后续改动误写成中置信机会。
```

原因：

```text
detect_aba_market_signals 只有 `站点 + 周期 + normalized_query` 精确索引。
缺少“同站点 + 同搜索词 + 最新 ABA 行”的低置信兜底路径，也没有把广告周期、ABA 周期和过期天数写进机会信号证据。
```

解决方案：

```text
新增 latest_aba_rows_by_market_query。
当同周期精确匹配失败时，只在 ABA end_date 距离广告 end_date 超过 14 天的情况下，允许同站点同搜索词生成低置信 ABA 机会提示。
该信号必须设置 confidence=low、freshness_status=stale，并追加广告周期、ABA周期、ABA过期天数证据。
```

后续防错规则：

- 同周期 ABA 匹配优先，过期 ABA 只能作为低置信补充。
- 过期 ABA 机会信号必须显式展示周期差异，不能只展示 ABA 排名。
- 当前真实快照没有 ABA 热词命中时，不要硬造 ABA 机会信号。
- ABA 机会建议只能是人工复核承接和匹配方式，不得出现自动广告动作。

验证方式：

```text
python -m pytest tests\test_signal_detection.py::test_stale_aba_market_opportunity_is_low_confidence -q
结果：1 passed

python -m pytest
结果：82 passed

build_signal_scan_payload(selected_market_id=1)
结果：signal_count=8，aba_market_signal_count=0，未硬造真实 ABA 机会信号
```

### 问题 60：真实搜索词全未分组导致语义聚合不可用

问题：

```text
当前真实快照中 ad_search_term_daily_metrics 有 20 行、16 个唯一搜索词，但 intent_label 全部为 `未分组搜索词`。
如果继续直接做“同类搜索词整体表现”或“语义相关 / 不相关”判断，会把没有语义依据的精确词聚合误写成语义聚合。
```

原因：

```text
当前信号层已有 search_intent_summaries 和 detect_intent_signals，但输入层没有真实语义分组。
所有搜索词进入同一个默认 intent_label，语义类目指标失去解释意义。
```

解决方案：

```text
新增 sig-data-quality-search-intent-ungrouped。
当广告搜索词行数 >= 2 且未分组占比 >= 80% 时，生成 P1 数据质量信号。
证据列出搜索词行数、未分组行数、未分组占比和唯一搜索词数。
```

后续防错规则：

- 未接入人工语义或模型语义前，只能做精确 normalized_query 聚合，不能声称已完成语义聚合。
- 当搜索词大部分未分组时，优先输出数据质量信号。
- 语义相关、不相关、同类词整体表现等规则必须依赖明确语义标签或可解释分组来源。
- 搜索词语义补齐建议只能是人工分组或接入语义规则，不得自动执行广告动作。

验证方式：

```text
python -m pytest tests\test_signal_detection.py::test_detects_search_intent_grouping_data_quality_gap tests\test_signal_detection.py::test_does_not_detect_search_intent_grouping_gap_when_terms_have_labels -q
结果：2 passed

build_signal_scan_payload(selected_market_id=1)
结果：signal_count=9，新增 sig-data-quality-search-intent-ungrouped
```

### 问题 61：数据质量信号在前端混入普通异常

问题：

```text
后端已经生成 data_quality 信号，但前端只按 signal_type 展示。
结果是 ABA 过期、搜索词语义未分组、商品广告覆盖缺口都被显示成普通“异常”，用户无法区分经营异常和数据质量问题。
```

原因：

```text
前端筛选和标签只使用 signal_type=anomaly/opportunity。
signal_category=data_quality 没有进入 UI 分类规则。
```

解决方案：

```text
新增 signalUi.ts，把 signal_category=data_quality 映射为队列类型 data_quality。
概览增加数据质量和数据过期。
队列增加数据质量筛选。
信号标签增加 signalPill data_quality。
```

后续防错规则：

- 前端不能只看 `signal_type`，必须同时看 `signal_category`。
- 数据质量信号优先作为“可分诊问题”展示，不要混入普通异常。
- 当系统不能判断业务问题时，先展示“不能判断的原因”和证据。

验证方式：

```text
npm run test:signal-ui
结果：通过

浏览器点击“数据质量”
结果：队列 3 条，全部为 signalPill data_quality
```

### 问题 62：本地浏览器验收容易被旧项目和旧后端进程污染

问题：

```text
打开 127.0.0.1:5173 时看到的是旧项目页面，不是新项目。
8020 上也曾存在旧后端进程，OpenAPI 缺少 /api/market-options，导致新前端显示“后端服务未连接”。
```

原因：

```text
本机多个 Amazon 广告项目并行开发，常用端口可能被旧服务占用。
PowerShell Start-Process 中如果环境变量写法错误，Vite 会启动成功但仍指向默认 API。
```

解决方案：

```text
新项目前端改用 5184 验收。
后端使用 8020 前先检查 /openapi.json 是否包含当前路由。
启动 Vite 时确认转换后的 /src/api.ts 中 VITE_API_BASE_URL 指向 8020。
```

后续防错规则：

- 浏览器验收前先确认页面标题和核心文案属于新项目。
- 后端验收先查 `/openapi.json`，不要只看端口是否 Listen。
- PowerShell 循环变量不要命名为 `$pid`，这是内置变量。
- 前端环境变量用单引号或 here-string，避免被外层 PowerShell 提前展开。

验证方式：

```text
http://127.0.0.1:8020/openapi.json
结果：包含 /api/market-options

http://127.0.0.1:5184/src/api.ts
结果：VITE_API_BASE_URL=http://127.0.0.1:8020

浏览器验收
结果：渲染 9 条信号，console error 为 0
```

### 问题 63：数据质量详情缺少判断边界，容易被当成经营异常处理

问题：

```text
前端已经能把 data_quality 信号从普通异常中拆出来，但详情页仍然先展示原因、对象、指标和建议。
用户可能把 ABA 过期、语义分组缺失、商品广告覆盖不足直接理解成广告经营异常。
```

原因：

```text
详情区没有单独表达“当前不能直接下经营结论”的判断边界。
数据质量信号需要先解释数据缺口，再进入证据和建议。
```

解决方案：

```text
新增 signalDecisionBoundary。
数据质量信号详情在证据链前展示“判断边界”。
ABA 过期类信号展示“数据已过期，相关结论需要降置信复核”。
其他数据质量信号展示“先补数据，再判断经营问题”。
```

后续防错规则：

- 数据质量信号详情必须先讲判断边界。
- 不能把数据缺口包装成确定的经营异常。
- 涉及过期数据的结论必须提示降置信复核。

验证方式：

```text
npm run test:signal-ui
结果：通过

浏览器点击“数据质量”
结果：队列 3 条，详情显示“判断边界 / 数据已过期，相关结论需要降置信复核”
```

### 问题 64：证据来源混在一起，用户难以快速核验证据边界

问题：

```text
信号详情已经展示每条证据的 source_type，但所有证据默认混在一个时间线中。
当同一信号同时使用 ABA导出 和 积加API 时，用户需要逐条扫文字才能判断哪些证据来自过期 ABA，哪些来自当前 API 快照。
```

原因：

```text
详情页只有证据列表，没有按来源建立轻量筛选。
前端纯函数初版如果把完整 Evidence 降成过窄类型，还会丢失 time_range / explanation / note 等详情字段。
```

解决方案：

```text
新增 buildEvidenceSourceOptions 和 filterEvidenceBySource。
证据链顶部展示“全部”和当前信号实际出现的来源按钮。
filterEvidenceBySource 使用泛型返回原始证据项类型，避免筛选后丢失详情字段。
```

后续防错规则：

- 证据链不能只展示来源文本，必须支持按来源快速查看。
- 筛选和排序这类前端纯函数要保留原始证据项类型，不要把 API 合同字段收窄丢失。
- ABA 过期类信号必须能单独查看 ABA 证据和 API 周期证据。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验收
结果：ABA导出 4/5，积加API 1/5，console error 为 0
```

### 问题 65：关键证据被完整证据时间线淹没

问题：

```text
信号详情有完整证据链，但用户仍要从多条证据中自己找最关键的判断依据。
例如 ABA 过期信号中，“过期天数 29”排在完整证据链后面，用户需要扫完整列表才看到真正支撑结论的核心事实。
```

原因：

```text
前端只按后端证据原始顺序展示，没有把“最能解释结论的证据”提到前面。
这会让证据链完整但不够可读，尤其是数据质量信号和多来源信号。
```

解决方案：

```text
新增 buildKeyEvidenceFacts。
按标签关键词选出最多 3 条关键证据，在完整证据链前展示。
完整证据链和来源筛选仍保留，关键证据只做阅读入口，不替代证据追溯。
```

后续防错规则：

- 信号详情必须先展示关键证据，再展示完整证据链。
- 关键证据不能虚构，只能来自 signal.evidence.facts。
- 关键证据摘要不能替代来源筛选和完整证据追溯。
- 数据质量信号优先露出过期、缺失、占比等能解释判断边界的证据。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验收
结果：关键证据显示过期天数 / ABA 周期 / 分析周期结束，console error 为 0
```

### 问题 66：影响范围只展示标签，用户难以判断影响性质

问题：

```text
信号详情的“影响范围”只展示 tags。
例如 ABA 过期信号显示“数据质量 / ABA过期 / 市场机会”，但没有说明这影响的是 ABA 机会判断可信度，不是广告经营表现本身异常。
```

原因：

```text
前端把 tags 当成影响范围直接展示，没有基于 signal_category、object_type 和 freshness_status 解释影响性质。
这会让数据质量、广告组、搜索词和广告位信号在阅读上混成同一类标签。
```

解决方案：

```text
新增 signalImpactScope。
影响范围先展示解释性主文案，再保留对象类型和 tags 作为辅助上下文。
```

后续防错规则：

- 影响范围不能只堆标签。
- 数据质量信号必须说明影响的是判断能力，不直接代表经营异常。
- 广告组信号必须说明广告组是投放容器，不能直接归因为单个商品。
- 搜索词和广告位信号必须说明对应投放层级，不能泛化成产品问题。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验收
结果：影响范围显示“影响 ABA 机会判断可信度，不代表广告经营表现本身异常。”，console error 为 0
```

### 问题 67：信号详情只有对象名称，缺少对象边界上下文

问题：

```text
详情页原本只显示对象类型和对象名称。
当信号对象是广告组、搜索词或广告位时，用户仍然不知道当前判断层级，容易再次把广告组理解成商品，或把搜索词表现强行归因到单个商品。
```

原因：

```text
前端没有基于 primary_object 展示广告活动、广告组、搜索词、广告位、ASIN、SKU 等上下文字段。
对象边界只存在于文档和影响范围规则里，没有在详情页对象区域直接呈现。
```

解决方案：

```text
新增 buildSignalObjectContext。
信号详情对象区域展示对象边界说明和上下文字段。
广告组信号明确写出“广告组是投放容器，不能直接归因为单个商品”。
```

后续防错规则：

- 详情页不能只显示对象名称，必须说明对象层级。
- 广告组详情必须展示广告活动和广告组，并提示广告组是投放容器。
- 搜索词详情必须展示广告活动、广告组和搜索词，并提示不能强行归属到单个商品。
- 广告位详情必须展示广告活动和广告位，并提示广告位代表流量位置。
- 商品类信号必须区分广告商品和销售商品。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验收
结果：真实广告组信号显示“广告组是投放容器，不能直接归因为单个商品”，console error 为 0
```

### 问题 68：广告组结构信号缺少完整广告商品清单

问题：

```text
广告组结构信号已经说明“广告组是投放容器”，但证据层只展示广告商品数和头部广告商品。
用户仍然无法直接确认这个广告组内实际投放了哪些商品。
```

原因：

```text
后端只把 advertised_product_count 和 top_advertised_product 写入证据。
前端关键证据摘要也没有优先展示完整广告商品清单。
这会让“多商品消耗集中”的判断缺少可读的商品级证据。
```

解决方案：

```text
后端按广告商品花费从高到低生成 advertised_product_list。
前端关键证据摘要优先展示“广告商品清单”。
真实广告组 RBK004-Auto 当前显示：
B016EXMVZS / 004-Wayfarer Pink / 花费 78.11 / 订单 34
B016EXMW02 / 004-Wayfarer Black / 花费 19.08 / 订单 7
```

后续防错规则：

- 广告组是投放容器，不能展示成产品。
- 广告组结构信号如果有广告商品快照，必须展示广告商品清单。
- 如果当前快照没有广告商品清单，必须明确说明证据不足。
- 后端改动后，HTTP API 验收前先确认服务已重启或已加载最新代码。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run test:snapshot-ui
结果：通过

npm run build
结果：通过

cd backend && python -m pytest
结果：84 passed

浏览器验收
结果：真实广告组信号详情显示“广告商品清单”，console error 为 0
```

### 问题 69：广告商品清单是长文本，用户仍难比较商品差异

问题：

```text
广告组信号已经展示广告商品清单，但清单是一条长文本。
用户能知道广告组投放了哪些商品，却不能快速比较每个广告商品的花费、订单、销售额、ACOS 和 CVR。
```

原因：

```text
前端只把 evidence facts 展示成通用证据卡，没有利用 evidence.source_rows 里的广告商品表现行。
这会把结构化广告商品指标压扁成长文本，不利于判断“哪个商品表现更好或更差”。
```

解决方案：

```text
新增 buildAdProductComparisonRows。
广告组结构信号从 source_rows 生成广告商品对比行。
页面新增“广告商品对比”区，展示每个广告商品的花费、订单、销售额、ACOS、CVR。
source_record_id 只用于内部 key，不直接展示给运营。
```

后续防错规则：

- 商品级对比不能只用长文本表达。
- 运营判断字段和技术回溯字段要分开展示。
- 广告组仍然是容器，商品对比只能说明广告商品表现差异，不能直接等同于经营商品归因。
- 前端可基于 source_rows 做展示计算，但不能虚构后端没有返回的业务事实。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验收
结果：真实广告组信号显示 2 个广告商品对比行，花费、订单、销售额、ACOS、CVR 均可见，console error 为 0
```

### 问题 70：商品对比有指标，但缺少后端生成的拆分原因证据

问题：

```text
广告组详情已经展示广告商品对比，但用户还需要自己判断为什么这个广告组需要拆开看。
如果前端直接根据 source_rows 拼解释，会把业务诊断职责放到前端。
```

原因：

```text
商品对比区是展示层能力，不能替代 AI 信号证据。
“广告组汇总会掩盖哪些商品差异”属于信号解释，应当作为后端 evidence fact 返回。
```

解决方案：

```text
后端新增 ad_product_performance_difference 证据。
证据值列出每个广告商品的花费占比、订单、销售额、ACOS、CVR，并说明广告组汇总会掩盖商品之间的消耗和承接差异。
前端关键证据优先展示“商品表现差异”，但不自行生成该业务结论。
```

后续防错规则：

- 业务诊断解释优先由后端信号证据生成。
- 前端可以排序、筛选、格式化证据，但不能编造后端没有返回的结论。
- 广告组拆分原因必须继续说明广告组是容器，不等同于产品归因。
- 样本量不足时，差异解释必须降级为观察提示。

验证方式：

```text
python -m pytest tests/test_signal_detection.py::test_detects_ad_group_spend_concentration_across_multiple_ad_products
结果：通过

npm run test:signal-ui
结果：通过

浏览器验收
结果：关键证据显示“商品表现差异”，真实 ASIN 的花费占比、订单、销售额、ACOS、CVR 均可见，console error 为 0
```

### 问题 71：低样本商品差异容易被误读成稳定结论

问题：

```text
广告组结构信号已经能说明同一广告组内不同广告商品的花费、订单、销售额、ACOS 和 CVR 差异。
但如果某个商品点击、订单或销售额样本很少，直接输出 ACOS/CVR 差异会显得过度确定。
```

原因：

```text
花费集中是结构事实，商品表现差异是解释事实。
结构事实可以触发信号，但低样本表现差异不能被写成稳定业务结论。
```

解决方案：

```text
后端在 ad_product_performance_difference 中追加样本边界：
点击 < 20 标记为点击样本少。
订单 < 2 标记为订单样本少。
销售额 <= 0 标记为无销售额。
存在任一样本边界时，结尾明确写“只能作为观察提示，不能当作稳定结论”。
```

后续防错规则：

- 广告组花费集中可以高置信触发结构信号，但商品表现解释必须说明样本边界。
- 低样本 ACOS/CVR 不能写成稳定结论。
- 前端只展示后端证据，不在前端补写样本边界。
- 真实快照样本足够时，不要误加低样本提示。

验证方式：

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_detection.py::test_ad_group_product_difference_marks_low_sample_as_observation_only -q
结果：通过

$env:PYTHONPATH='backend'; python -m pytest backend/tests -q
结果：85 passed

真实快照扫描
结果：RBK004-Auto 两个广告商品样本不低，未误加低样本提示
```

### 问题 72：搜索词上下文行不能直接混入广告商品对比 source_rows

问题：

```text
广告组结构信号需要说明同广告组搜索词表现不能自动归属到单个广告商品。
但如果直接把搜索词行追加到 evidence.source_rows，前端广告商品对比会把搜索词行当作广告商品行展示。
```

原因：

```text
当前前端 buildAdProductComparisonRows 使用 signal.evidence.source_rows 构建广告商品对比。
source_rows 在广告组结构信号里既承担证据回溯，又承担广告商品对比输入。
搜索词表现属于广告组层级上下文，不是广告商品行。
```

解决方案：

```text
搜索词上下文行只加入 data_sources 和“归因边界”证据，不加入 evidence.source_rows。
后端新增 ad_group_attribution_boundary，说明同广告组搜索词表现只能证明广告组层级流量和转化，不能自动归属到任一广告商品。
前端关键证据排序增加“归因边界”，让该证据在详情页优先露出。
```

后续防错规则：

- 修改 source_rows 前必须检查前端是否把它作为某个结构化展示输入。
- 广告商品对比只能消费 advertised_products 行。
- 搜索词和广告位上下文可以作为证据来源，但不能伪装成商品级行。
- 多商品广告组下的搜索词表现必须说明归因边界。

验证方式：

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_detection.py::test_ad_group_structure_explains_search_term_attribution_boundary -q
结果：通过

npm run test:signal-ui
结果：通过

真实接口 /api/signals?market_id=1
结果：ad_group_attribution_boundary 已返回，source_rows 仍只有 advertised_products 行
```

### 问题 73：搜索词分化信号只有聚合证据，缺少投放行明细

问题：

```text
同一搜索词表现分化信号已经能说明总花费、总订单和无订单花费占比。
但用户仍需要自己打开 source_rows 才能看出哪条投放行出单、哪条投放行消耗无单。
```

原因：

```text
聚合证据能说明“有分化”，但不能直接说明“分化发生在哪里”。
搜索词分化的处理动作是人工对比投放结构，因此证据链必须把投放行明细前置。
```

解决方案：

```text
后端新增 search_term_split_row_detail 证据。
该证据列出每条投放行的广告活动、广告组、投放对象、花费、订单、销售额、ACOS 和 CVR。
前端关键证据排序增加“投放行表现明细”，让它优先出现在关键证据区。
```

后续防错规则：

- 聚合类搜索词信号必须同时提供聚合指标和明细证据。
- 搜索词分化不能只给总数，必须说明哪几条投放行表现不同。
- 新增 EvidenceItem 时，用户可见 label 用中文，metric_name 用稳定英文。
- 搜索词明细不能被解释成商品级归因，只能作为投放结构对比依据。

验证方式：

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_detection.py::test_detects_same_search_term_performance_split_signal -q
结果：通过

npm run test:signal-ui
结果：通过

真实快照扫描
结果：搜索词分化信号返回 search_term_split_row_detail

$env:PYTHONPATH='backend'; python -m pytest backend/tests -q
结果：86 passed

真实接口 /api/signals?market_id=1
结果：search_term_split_row_detail 已返回，evidence_count=6
```

### 问题 74：继续堆信号会让 MVP 闭环失焦

问题：

```text
真实快照已经能生成多条信号后，继续优先新增信号类型会让项目看起来更丰富，但不能证明 MVP 可真实使用。
当前人工处理按钮主要是前端本地状态，刷新后无法作为处理记录和复盘依据。
```

原因：

```text
MVP 的核心不是信号数量，而是“真实数据 → 可解释信号 → 人工处理 → 留痕 → 复盘”是否闭合。
如果人工处理不落盘，系统只能展示信号，不能形成可复用的运营决策记录。
```

解决方案：

```text
把下一阶段优先级收敛为人工处理留痕最小闭环。
第一阶段先用本地 JSONL 或轻量文件保存人工动作，不引入复杂数据库和权限系统。
```

后续防错规则：

- 已有真实信号数量足够验证工作台时，不要继续用新增信号替代闭环建设。
- 人工动作不能只保存在前端 `useState`。
- 复盘必须基于可读回的人工处理记录。
- MVP 优先做最小可验证闭环，不做复杂审批流、跨店铺总览或自动广告动作。

验证方式：

```text
检查 项目规划.md / 开发原型.md / 项目规范.md / 原型技术栈.md。
下一步实现后，必须能通过接口提交并读回人工处理记录。
```

### 问题 75：人工处理运行记录不能落到未忽略目录

问题：

```text
人工处理留痕会生成真实运营记录。
如果默认写入 data/manual_actions，而该目录没有被 .gitignore 覆盖，后续容易把真实处理记录混入版本管理。
```

原因：

```text
项目已有 data/runtime/ 忽略规则，但新增运行数据目录时如果不先查 .gitignore，就会产生可提交的真实运行数据。
```

解决方案：

```text
人工处理 JSONL 默认保存到 data/runtime/manual_actions/manual_actions.jsonl。
data/runtime/ 已在 .gitignore 中忽略。
```

后续防错规则：

- 新增任何真实运行记录目录前，必须先检查 .gitignore。
- 真实 API 快照、ABA 快照、人工处理记录和复盘运行记录都不能进入版本管理。
- 测试用 tmp_path，不写入项目 data 目录。

验证方式：

```text
Test-Path data/runtime/manual_actions/manual_actions.jsonl
结果：True

git status --short -- data
结果：未显示 data/runtime/manual_actions 运行记录
```

### 问题 76：复盘待办不能被误写成效果归因

问题：

```text
人工动作已经能落盘后，下一步很容易直接写“7 天花费下降、14 天 ACOS 改善”等复盘结论。
但当前只有人工动作时间和当前快照，没有处理后的指标对比。
```

原因：

```text
复盘效果结论必须依赖处理前后同对象、同店铺站点、同口径周期的指标变化。
如果没有后续快照或对比口径，只能生成复盘待办，不能生成效果判断。
```

解决方案：

```text
先实现只读复盘待办：
从 data/runtime/manual_actions/manual_actions.jsonl 读取人工动作。
每条动作派生 7d / 14d 两个 ReviewTodo。
只判断 due_at、is_due、days_since_action。
```

后续防错规则：

- 复盘待办只回答“什么时候该回头看”，不回答“处理是否有效”。
- 复盘效果信号必须有处理前后指标证据，不能只靠人工动作记录生成。
- 复盘对比必须保留 shop_id、market_id、snapshot_id 和对象边界。
- 没有后续真实快照时，只能提示等待复盘或手动拉取快照，不能生成改善结论。

验证方式：

```text
GET /api/signals/sig-data-quality-aba-stale/review-todos
结果：返回 7d / 14d 待办，未返回花费、订单或 ACOS 改善结论。
```

### 问题 77：主推款导致的广告组消耗集中不能直接判成异常

问题：

```text
RBK004-Auto 广告组内一个 ASIN 消耗集中。
系统原文案为“存在多商品消耗集中，商品级诊断需要拆开看”。
但用户指出该 ASIN 是主推款，因此这种集中是正常投放策略。
```

原因：

```text
规则只看 advertised_products 中的花费集中度，没有主推款 / 非主推款策略字段。
消耗集中是客观事实，但是否异常取决于投放策略。
缺少策略证据时，系统不能把“主推款消耗集中”写成结构异常。
```

解决方案：

```text
广告组消耗集中信号从 anomaly 调整为 opportunity。
优先级调整为 P2，置信度调整为 medium，严重度调整为 2。
摘要改为“多商品消耗集中，先确认头部 ASIN 是否主推款”。
新增“策略边界”证据，明确当前快照没有主推款标记。
```

后续防错规则：

- 消耗集中只能先作为结构观察或策略确认信号。
- 如果头部 ASIN 是主推款，不能生成异常结论。
- 缺少主推款标记时，建议动作必须是人工确认策略，不是直接拆分广告组。
- 后续需要维护 `主推款 ASIN / 策略标签 / 生效周期`，再让系统自动降级或关闭这类提示。

验证方式：

```text
GET /api/signals/sig-ad-group-spend-concentration-34612947118490?market_id=1
结果：signal_type=opportunity，priority=P2，summary=RBK004-Auto 多商品消耗集中，先确认头部 ASIN 是否主推款。

浏览器验收：
队列显示“机会 / RBK004-Auto 多商品消耗集中，先确认头部 ASIN 是否主推款”。
```

### 问题 78：已确认主推款后仍提示策略待确认会形成噪音

问题：

```text
用户已经确认 RBK004-Auto 广告组的头部 ASIN 是主推款后，如果系统仍继续返回“先确认头部 ASIN 是否主推款”，会把正常策略反复推给运营处理。
```

原因：

```text
前一轮只把缺少主推款标记表达为不确定性，但系统没有实际的主推款策略资料读取入口。
```

解决方案：

```text
新增 data/runtime/promotion_strategy_profiles.json 作为第一阶段轻量人工语义资料。
信号规则匹配到同 market_id、ad_group_id、asin 且周期有效的 main_push 策略时，不生成广告组消耗集中待确认信号。
```

后续防错规则：

- 用户确认正常策略后，必须进入可读取的人工语义资料，而不是只改文案。
- 主推款资料至少包含 `market_id / ad_group_id / asin / strategy_role / start_date / end_date`。
- 缺少策略资料时才能生成待确认信号；已有策略资料时不能继续制造同类噪音。
- 投放策略资料属于人工维护数据，不走积加 API 自动同步。

验证方式：

```text
GET /api/signals?market_id=1
结果：signal_count=8，ad_group_concentration_count=0。
```

### 问题 79：复盘效果只读最新快照会丢失处理前基线

问题：

```text
复盘效果需要对比人工处理前后指标。
如果接口只读取最新成功快照，后续快照成为最新后，处理前快照会被排除，系统无法可靠计算 before / after。
```

原因：

```text
早期信号生成只需要最新快照。
复盘效果的时间语义不同，需要同一对象在多个成功快照中的指标序列。
```

解决方案：

```text
新增 load_signal_rows_from_success_snapshots，复盘接口读取所有成功快照。
build_review_effect_result 按 manual action 的 object_type / object_id / market_id 匹配对象，再按 acted_at 切分处理前和处理后。
处理后快照不足时返回 not_ready / unclear，不生成 improved / worse 结论。
```

后续防错规则：

- 信号扫描可以只读最新快照，复盘效果不能只读最新快照。
- 没有处理前或处理后完整周期时，只能提示“暂不可计算”。
- 复盘效果结论必须来自同对象、同店铺站点、同周期窗口的指标对比。
- 不得把复盘待办误写成复盘效果。

验证方式：

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_actions.py::test_review_effect_waits_for_after_snapshot backend/tests/test_manual_actions.py::test_review_effect_compares_before_and_after_metrics backend/tests/test_snapshot_store.py::test_signal_rows_load_from_all_success_snapshots_for_review -q
结果：3 passed

GET /api/signals/sig-data-quality-aba-stale/review-effect?review_window=7d
结果：status=not_ready，result=unclear，未生成效果改善结论。
```

### 问题 80：代码已更新但本地后端旧进程仍返回旧路由

问题：

```text
新增 /api/signals/{signal_id}/review-effect 后，首次真实 HTTP smoke 返回 404。
```

原因：

```text
8020 端口上的 FastAPI 进程仍是代码变更前启动的旧进程。
pytest 使用当前源码，不代表运行中的本地服务已经热更新。
```

解决方案：

```text
确认 8020 Listen 进程后重启该 FastAPI 进程，再重新请求 /health 和新接口。
```

后续防错规则：

- 新增或修改后端路由后，真实 HTTP smoke 前必须确认本地服务是否需要重启。
- 测试通过不等于浏览器正在连接的新代码。
- 前端页面异常时，先区分“源码问题”和“运行进程未重启”。

验证方式：

```text
GET /health
结果：status=ok

GET /api/signals/sig-data-quality-aba-stale/review-effect?review_window=7d
结果：返回 ReviewEffectResult。
```

### 问题 81：not_ready 复盘效果不能保存为复盘记录

问题：

```text
复盘效果接口已经能返回 not_ready / unclear 后，如果前端或接口仍允许保存复盘记录，会把“证据不足”误写成人工复盘结论。
这会污染后续信号排序和复盘效果判断。
```

原因：

```text
复盘待办、复盘效果、复盘记录是三件事。
复盘待办回答“什么时候回头看”。
复盘效果回答“处理前后指标是否有证据变化”。
复盘记录回答“人工确认后把这次效果留痕”。
只有复盘效果 status=ready 时，复盘记录才有保存价值。
```

解决方案：

```text
后端 POST /api/signals/{signal_id}/review-records 保存前重新计算 review-effect。
如果 status 不是 ready，返回 409 review_effect_not_ready。
前端只在 canSaveReviewEffect(effect) 为 true 时展示“保存复盘记录”按钮。
真实 ABA 过期信号当前 status=not_ready，因此 smoke 中必须被拒绝保存。
```

后续防错规则：

- 不得把 not_ready / unclear 写入 review_records.jsonl。
- 不得为了让 UI 看起来闭环而伪造真实复盘记录。
- ready 保存链路用测试临时数据覆盖；真实数据只按真实状态保存。
- 信号诊断口径和复盘口径必须分开：诊断负责发现问题，复盘负责证明处理效果。

验证方式：

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_actions.py::test_review_record_rejects_not_ready_effect backend/tests/test_manual_actions.py::test_review_record_route_rejects_not_ready_effect -q
结果：2 passed

POST /api/signals/sig-data-quality-aba-stale/review-records?review_window=7d
结果：409，未写入复盘记录。

浏览器验收：
结果：not_ready 状态下不显示“保存复盘记录”按钮。
```

### 问题 82：信号分诊口径不显性会被误解成 AI 黑箱判断

问题：

```text
用户询问“信号分诊断是按照什么口径去实现的”时，如果只能口头解释，页面仍然看不出当前信号为什么进入数据质量、异常、机会、高优先级或观察中。
这会让运营误以为分诊是 AI 黑箱判断，或者误把复盘结果当成分诊依据。
```

原因：

```text
前端已有 signalQueueKind、signalCategoryLabel 等分桶逻辑，但没有统一输出“字段口径说明”。
队列筛选、详情展示、复盘结果各自显示，容易让用户把 signal_type、signal_category、severity、status、review_result 混为一谈。
```

解决方案：

```text
新增 buildSignalTriageRationale 纯函数，统一输出：
queueKind
queueLabel
queueReason
priorityReason
statusReason
reviewBoundary

在选中信号详情中增加“分诊口径”区块。
明确 review_result 只说明人工处理后的复盘效果，不参与信号分诊。
```

后续防错规则：

- 新增队列筛选条件时，必须同步更新 buildSignalTriageRationale。
- 分诊桶只能由 signal_category / signal_type 决定。
- 高优先级只能由 severity 阈值决定。
- 观察中只能由人工状态 status 决定。
- review_result 不能反向改变信号类型或分诊桶。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验收：
结果：页面出现“分诊口径”，并显示 review_result 不参与分诊。
```

## 问题 83：分诊口径容易被误解成置信度和严重度证据口径

发现时间：2026-06-14

现象：

```text
用户继续追问“信号分诊断是按照什么口径去实现的”时，说明页面上的“分诊口径”仍可能被理解为完整的 AI 判断依据。
但当前实现实际只解释队列归类和筛选：
signal_category / signal_type 决定数据质量、异常、机会桶；
severity>=4 决定高优先级筛选；
status=observing 决定观察中筛选；
review_result 不参与分诊。
```

原因：

```text
当前页面还没有显性解释 confidence 和 severity 为什么成立。
虽然 API 已返回 confidence、severity、evidence_count、data_sources、freshness_status、uncertainty，但前端尚未把这些字段组合成“证据支撑口径”。
```

解决方案：

```text
下一步应新增置信度和严重度证据支撑说明。
该说明必须和分诊口径分开：
分诊口径回答“进哪个队列、为什么排前面”；
证据支撑口径回答“为什么这个置信度和严重度成立”。
```

落地结果：

```text
已新增 buildSignalEvidenceSupport 纯函数。
已在选中信号详情中新增“证据支撑”区块。
该区块展示：
sourceSummary
confidenceReason
severityReason
supportWarning

数据过期时，即使高置信能证明“数据问题确定”，也会提示相关经营判断需要降置信复核。
```

后续防错规则：

- 不要把“分诊口径”写成“AI 判断全过程”。
- confidence 必须解释数据源数量、新鲜度和异常强度。
- severity 必须解释影响范围，如花费、订单、ACOS、核心产品或高热搜索词。
- 如果只有单一来源，高置信必须给出数据质量类例外或降级说明。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验收：
结果：页面出现“证据支撑”，显示高置信、高严重度、数据过期提示；console error 为 0。
```

### 问题 84：非数据质量单源信号不能保持高置信

发现时间：2026-06-14

现象：

```text
前端已经能提示“高置信但单源需要警惕”，但如果后端仍输出非数据质量 high confidence，页面提示只能补充解释，不能真正阻止经营误读。
用户会以为“高置信”代表可以直接按经营结论处理。
```

原因：

```text
数据质量高置信和经营结论高置信不是一回事。
数据质量高置信可以表示“数据过期、数据缺口、快照不可用”等事实确定。
经营类高置信必须有至少两个独立证据来源，否则最多只能是中置信观察。
```

解决方案：

```text
新增后端统一保护 enforce_signal_confidence_support：
1. 只处理 high confidence。
2. data_quality 例外。
3. 非 data_quality 必须至少有两个独立证据来源。
4. 不足时降为 medium，并把“证据来源不足”写入 uncertainty。

detect_signals 返回前统一套用该保护。
```

后续防错规则：

- 不要只靠前端文案修正高置信误导。
- 经营类 high confidence 必须在后端输出链路做证据门槛。
- data_quality high confidence 只能表示数据事实确定，不能扩展成经营结论确定。
- 新增信号类型时，必须检查 data_sources 是否足以支撑 confidence。

验证方式：

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_detection.py -q
结果：23 passed

$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_snapshot_signal_flow.py -q
结果：2 passed

$env:PYTHONPATH='backend'; python scripts/run_signal_scan.py --market-id 1
结果：signal_count=8，当前非数据质量 high confidence 数量为 0。
```

### 问题 85：信号触发条件只在矩阵文档中会让页面仍像黑箱

发现时间：2026-06-14

现象：

```text
页面已经能展示分诊口径和证据支撑，但用户仍需要知道“这条信号为什么被规则命中”。
如果触发条件只存在于 AI信号类型矩阵.md 或后端规则代码里，运营在工作台中仍会觉得信号像 AI 黑箱结论。
```

原因：

```text
分诊口径回答“进哪个队列”。
证据支撑回答“当前证据是否足够支撑置信度和严重度”。
但触发口径回答“为什么这条规则会命中”，这三件事不能混在一起。
```

解决方案：

```text
新增 buildSignalTriggerRationale 纯函数，并在选中信号详情中新增“触发口径”区块。
触发口径固定展示：
判断对象
触发条件
证据来源
置信边界
处理边界
```

后续防错规则：

- 新增信号类型时，必须同步补充触发口径说明。
- 页面不能只展示 signal_category 或 signal_type，必须解释规则命中条件。
- 触发口径不能替代证据支撑；命中规则不等于证据足够。
- 触发口径必须保留人工动作边界，不能出现自动调价、自动暂停、自动否词或自动加词。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验收：
结果：页面出现“触发口径”；当前 ABA 过期信号展示 ABA 站点级对象、超过 14 天、证据来源、置信边界和重新导入边界；console error 为 0。
```

### 问题 86：关键证据摘要不能只靠通用关键词排序

发现时间：2026-06-14

现象：

```text
关键证据摘要已经能从完整证据中挑出 3 条，但如果只按通用关键词排序，不同信号类型会露出不够关键的事实。
例如长尾搜索词机会里，“搜索词 / 花费 / 订单”会漏掉 ACOS；广告位机会也需要先说明判断对象是广告位。
```

原因：

```text
同一个证据 label 在不同信号类型里的重要性不同。
关键证据摘要回答“最先看哪 3 个事实能判断这条结论”，所以必须结合 signal_category。
只看 facts 文本会把“证据时间线摘要”做成通用列表，而不是运营诊断摘要。
```

解决方案：

```text
buildKeyEvidenceFacts 增加可选 signal 上下文。
页面调用时传入当前 signal。
按 signal_category 定义关键证据优先级：
广告位：广告位、ACOS、订单。
长尾搜索词机会：搜索词、订单、ACOS。
产品销售承接数据质量：销售商品指标行、强销售商品、广告商品行。
ABA 过期：过期天数、ABA 周期、分析周期结束。
搜索词表现分化：投放行表现明细、无订单花费占比。
```

后续防错规则：

- 新增信号类型时，必须检查关键证据摘要的前三项是否真正支撑结论。
- 不要只按“花费 / 订单 / ACOS”这类通用指标排序。
- 数据质量信号优先露出缺口事实，不能伪装成经营异常。
- 机会信号优先露出机会成立的事实，不能让辅助指标抢占摘要。
- 页面关键证据摘要必须来自真实 evidence.facts，不补造后端没有返回的业务事实。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验收：
广告位信号关键证据为：广告位 → ACOS → 订单。
长尾搜索词信号关键证据为：搜索词 → 订单 → ACOS。
产品销售承接数据质量信号关键证据为：销售商品指标行 → 强销售商品 → 广告商品行。
console error 为 0。
```

### 问题 87：历史人工动作中的问号串会污染当前 UI

发现时间：2026-06-14

现象：

```text
真实运行目录 data/runtime/manual_actions/manual_actions.jsonl 中存在历史记录：
operator_name=????
action_note=????????????

接口读回后，页面会显示“???? / 时间”，影响人工留痕可信度。
```

原因：

```text
历史记录已经写入坏文本。
save_manual_action 当前 UTF-8 写入正常，但 load_manual_actions 之前直接返回历史值，没有做读回清洗。
人工留痕属于运营判断证据，读回值不能出现明显编码损坏或占位问号串。
```

解决方案：

```text
新增 test_manual_action_load_hides_corrupted_question_mark_text，先确认旧逻辑失败。
在 load_manual_actions 读取层增加最小清洗：
operator_name 为纯问号串时回退为“本地运营”。
action_note 为纯问号串时回退为 None。
不修改原始 JSONL 文件，保留历史写入事实。
```

后续防错规则：

- 人工处理闭环验收必须检查“写入后读回”，不能只检查写入成功。
- 运行态 JSONL 里出现坏历史值时，优先在读取层清洗展示值，不直接手改留痕原始文件。
- 复盘待办继承人工动作字段，因此人工动作读回清洗必须在 `load_manual_actions` 层完成。
- UI 中不能暴露 `????` 这类明显损坏文本；这类文本应该回退为稳定默认值或空值。

验证方式：

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_actions.py -q
结果：11 passed

真实运行目录只读验证：
load_manual_actions('sig-data-quality-aba-stale')
结果：operator_name=本地运营，坏备注回退为 None。

重启后端后接口验证：
GET /api/signals/sig-data-quality-aba-stale/manual-actions
结果：不再返回 operator_name=????。
```

### 问题 88：同一信号多条人工动作会生成重复复盘待办

发现时间：2026-06-14

现象：

```text
真实运行信号 sig-data-quality-aba-stale 存在两条人工观察记录。
旧逻辑会为每条记录分别生成 7d / 14d 复盘待办，页面和接口合计出现 4 条。
这会让运营误以为同一信号有多组并行复盘任务。
```

原因：

```text
build_review_todos 直接遍历所有 manual_actions 记录。
但信号当前状态是按最新人工动作生效，复盘待办却按所有历史动作生效，两个口径不一致。
```

解决方案：

```text
新增测试固定 MVP 口径：
1. 同一信号只基于最新人工动作生成复盘待办。
2. 最新人工动作为 ignore 时，不生成复盘待办。

实现上先按 signal_id 收敛到最新人工动作，再只为 observe / handled / add_to_review 生成 7d / 14d 待办。
```

后续防错规则：

- 复盘待办必须和信号当前人工状态同口径，默认以最新人工动作作为依据。
- `ignore` 表示本次不进入后续处理，MVP 阶段不生成复盘待办。
- 如果未来要查看历史动作复盘，必须单独设计“历史处理记录”视图，不能混进当前待办队列。

验证方式：

```text
python -m pytest tests/test_manual_actions.py -q
结果：13 passed

python -m py_compile app/services/manual_actions.py
结果：通过

GET /api/signals/sig-data-quality-aba-stale/review-todos?market_id=1
结果：只返回 2 条，7d / 14d 都指向最新人工动作。

GET /api/review-todos?market_id=1
结果：count=2，没有重复历史动作。
```

### 问题 89：数据质量信号被套用广告指标复盘口径

发现时间：2026-06-14

现象：

```text
sig-data-quality-aba-stale 是 ABA 数据过期信号，object_type=cross。
旧复盘效果接口会返回“缺少处理前 7 天快照”。
这会把“数据是否补齐 / 是否更新”的问题误导成广告指标前后对比问题。
```

原因：

```text
build_review_effect_result 默认把所有人工处理后的信号都进入广告行指标匹配。
但数据质量 / 交叉信号不一定有广告对象行，也不应该用 ACOS、订单、花费前后变化来判断是否解决。
```

解决方案：

```text
新增测试固定 object_type=cross 的复盘口径。
当最新人工动作 object_type=cross 时，复盘效果返回 not_ready / unclear。
文案说明：数据质量或交叉信号不适用广告指标前后对比，请复查数据是否补齐或更新。
before_metrics / after_metrics 保持为空。
```

后续防错规则：

- 数据质量信号复盘默认检查数据补齐、新鲜度、覆盖范围，不套广告指标效果。
- 广告效果复盘只适用于能明确匹配广告对象和前后周期指标的信号。
- 如果未来要支持数据质量复盘保存，需要单独设计复盘结果类型，不能复用 `improved/no_change/worse`。

验证方式：

```text
python -m pytest tests/test_manual_actions.py -q
结果：14 passed

python -m py_compile app/services/manual_actions.py
结果：通过

GET /api/signals/sig-data-quality-aba-stale/review-effect?review_window=7d
结果：not_ready / unclear / 不适用广告指标前后对比。
```

### 问题 90：内置浏览器截图接口偶发超时

现象：

```text
使用 Browser 插件的 tab.screenshot({ fullPage: true }) 截取当前工作台 UI 时，出现 Page.captureScreenshot 超时。
页面本身可访问，DOM 检查正常，问题集中在截图接口。
```

影响：

```text
如果卡在内置截图接口，会拖慢每轮“截图给用户看”的验收节奏。
```

解决方案：

```text
先用 Browser DOM 或接口确认页面状态。
如果 tab.screenshot 超时，直接使用本地 Playwright 命令兜底：
npx playwright screenshot --channel chrome --viewport-size=1440,900 --full-page --wait-for-timeout=1500 http://127.0.0.1:5184/ <截图路径>
```

后续防错规则：

- 截图失败不要误判为前端页面失败。
- 先确认 DOM / 接口，再切换截图工具。
- 每轮截图路径写入 `开发经验记录.md`，方便回看。

### 问题 91：人工状态和复盘结果按 signal_id 归并会跨店铺污染

现象：

```text
多店铺 / 多站点场景下，同一类数据质量信号可能复用同一个 signal_id。
如果人工动作和复盘结果只按 signal_id 归并，market 1 的“观察中”或 review_result 会显示到 market 2 的同名信号上。
```

原因：

```text
早期实现默认 signal_id 全局唯一。
apply_latest_manual_actions 和 apply_latest_review_records 使用 signal_id 作为唯一归并键。
详情接口 manual-actions / review-todos / review-effect / review-records 也没有强制带 market_id 过滤。
前端 localStatus 只按 signal_id 缓存，也存在跨 market 覆盖风险。
```

解决方案：

```text
后端人工动作和复盘记录统一按 (signal_id, market_id) 归并。
详情路由支持 market_id 查询参数。
前端请求人工动作、复盘待办、复盘效果和复盘记录时传当前 selectedMarketId。
前端 localStatus 覆盖键改为 market_id:signal_id。
```

后续防错规则：

- 多店铺状态不能只看 `signal_id`，必须带 `market_id`。
- 顶部店铺 / 站点筛选器改变时，右侧处理留痕和复盘待办必须跟随过滤。
- 以后新增人工记录、复盘记录或运营备注时，默认主键口径为 `market_id + signal_id + object_id`，不要直接使用单个 `signal_id`。

验证方式：

```text
python -m pytest backend/tests/test_manual_actions.py::test_latest_manual_status_does_not_cross_market backend/tests/test_manual_actions.py::test_latest_review_result_does_not_cross_market backend/tests/test_manual_actions.py::test_signal_manual_routes_are_scoped_by_market_id -q
结果：3 passed

npm run test:snapshot-ui
结果：通过

npm run test:signal-ui
结果：通过

GET /api/signals/sig-data-quality-aba-stale/manual-actions?market_id=1
结果：2 条

GET /api/signals/sig-data-quality-aba-stale/manual-actions?market_id=2
结果：0 条
```

### 问题 92：“忽略本次”容易被误解为删除信号或判定误报

现象：

```text
右侧人工动作里有“忽略本次”按钮。
如果没有边界说明，运营可能理解成：这个信号被删除、这个信号是误报、或者以后都不需要再看。
这些理解都不准确。
```

原因：

```text
ignore 在系统里只是人工分诊状态。
它的真实语义是：最新动作为 ignore 时，不生成当前复盘待办。
它不改变信号生成规则，不删除证据，也不生成 review_result。
```

解决方案：

```text
保留后端现有语义：latest ignore 跳过 build_review_todos。
前端新增 manualActionBoundaryText。
只有最新人工动作 action_type=ignore 时，在处理留痕中展示：
“忽略本次只表示当前不进入复盘待办；信号和证据仍保留，不代表误报或已删除。”
```

后续防错规则：

- “忽略本次”只影响当前复盘待办，不影响信号生成和证据保留。
- 不要把 ignore 写成 false_positive。
- 不要为了展示 ignore 文案去点击真实运行页面，避免污染人工动作记录。

验证方式：

```text
python -m pytest backend/tests/test_manual_actions.py -q
结果：17 passed

npx tsc --module commonjs --target es2020 --moduleResolution node --esModuleInterop --skipLibCheck --outDir ../tmp/frontend-tests tests/reviewUi.test.ts; if ($LASTEXITCODE -eq 0) { node ../tmp/frontend-tests/tests/reviewUi.test.js }
结果：通过

npm run build
结果：通过
```

### 问题 93：人工动作容易被误解为效果结论

现象：

```text
“标记已处理”和“加入复盘”如果只显示动作名称，容易被误解。
标记已处理可能被理解为系统已经确认效果改善。
加入复盘可能被理解为已经有复盘结论。
```

原因：

```text
人工动作属于工作流状态，复盘结果属于效果判断。
这两个层级不能混在一起。
当前后端已经把 observe / handled / add_to_review 作为可复盘动作，但 UI 留痕缺少边界说明。
```

解决方案：

```text
不改后端动作类型。
前端 manualActionBoundaryText 增加 handled 和 add_to_review 文案：
handled：只表示已记录人工处理动作，是否改善要等复盘效果计算或人工复盘确认。
add_to_review：只表示进入后续复盘待办，不代表已经产生改善、无变化或恶化结论。
```

后续防错规则：

- 人工动作不等于效果结论。
- `handled` 不要直接写成 `improved`。
- `add_to_review` 不要直接写成已有复盘结果。
- 效果结论只能来自 `review-effect.status=ready` 后的复盘记录。

验证方式：

```text
npx tsc --module commonjs --target es2020 --moduleResolution node --esModuleInterop --skipLibCheck --outDir ../tmp/frontend-tests tests/reviewUi.test.ts; if ($LASTEXITCODE -eq 0) { node ../tmp/frontend-tests/tests/reviewUi.test.js }
结果：通过

npm run build
结果：通过

python -m pytest backend/tests/test_manual_actions.py -q
结果：17 passed
```

### 问题 94：广告数据平铺导致商品视角混乱

现象：

```text
用户指出当前广告数据直接平铺在信号队列里，看起来很乱。
第一性原理上，运营通常会先想看某个 ASIN / Parent ASIN / 商品组，再下钻到广告活动、广告组、搜索词和广告位。
```

原因：

```text
真实快照中不同广告数据的对象粒度不一致：
advertised_products 有 ASIN，可以建立广告 ASIN 视角。
sales_product_daily_metrics 有 ASIN，可以建立销售 ASIN 视角。
ad_search_term_daily_metrics 和 ad_placement_daily_metrics 当前没有 ASIN，不能直接归属到商品。
当前 normalized 快照没有 parent_asin，不能做 Parent ASIN 筛选。
```

解决方案：

```text
新增商品视角 API 和顶部轻量筛选器。
商品视角只筛选有 ASIN 证据的信号。
没有 ASIN 的搜索词和广告位归入“未归因广告数据”。
页面显式展示广告 ASIN、销售 ASIN、Parent ASIN 和未归因行数，避免用户误以为已经完成商品归因。
```

后续防错规则：

- 商品视角可以作为上层筛选，但不能替代证据归因。
- Parent ASIN 必须来自真实字段或人工导入的父 ASIN 文件，不能从 ASIN 猜。
- 搜索词和广告位只有 campaign / ad_group / placement / search_term 时，必须保留为未归因广告数据。
- 未来补商品语义或主推款策略时，也只能提升解释能力，不能反向篡改原始归因键。

验证方式：

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_product_scope.py backend/tests/test_api_routes.py -q
结果：18 passed

npm run build
结果：通过

浏览器验证：
页面显示商品视角、广告 ASIN 2、销售 ASIN 10、Parent ASIN 0、未归因搜索词 20、未归因广告位 10。
选择广告 ASIN B016EXMVZS 后，信号队列收敛为 1 条。
```

### 问题 95：商品视角筛选容易被误解为商品归因已经完成

现象：

```text
用户指出广告数据平铺很乱，进一步判断上层按 ASIN / Parent ASIN 分组筛选更合理。
这个方向是对的，但如果页面只加商品筛选，不解释搜索词和广告位的证据粒度，用户会误以为所有广告数据都已经能归到商品。
```

原因：

```text
商品视角是分析入口，不等于归因事实。
当前搜索词和广告位快照只有广告活动、广告组、搜索词或广告位粒度，没有 ASIN。
Parent ASIN 当前没有真实字段。
如果把无 ASIN 的搜索词或广告位硬放进某个商品，会制造虚假证据。
```

解决方案：

```text
新增“诊断主视角”：
有 ASIN 证据的信号显示为商品视角。
无 ASIN 的搜索词和广告位显示为未归因广告数据。
数据质量信号显示为数据质量层。
详情页明确说明：搜索词 / 广告位缺少 ASIN 时不能强行归属到商品或 Parent ASIN。
```

后续防错规则：

- 上层可以按 ASIN / Parent ASIN 分组，但只有真实字段或稳定映射才能进入该层。
- 搜索词、广告位、广告组的原始证据粒度不能被商品筛选器抹掉。
- `未归因广告数据` 不是异常，它是当前数据粒度的事实。
- UI 中只要出现商品筛选，就必须同时显示归因边界。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验证：
选择“未归因广告数据”，点击搜索词 12 month sunglasses。
详情显示“诊断主视角 / 未归因广告数据”，并说明不能强行归属到商品或 Parent ASIN。
```

### 问题 96：未归因广告数据筛选混入数据质量信号

现象：

```text
选择顶部“商品视角 = 未归因广告数据”后，左侧队列仍出现 ABA 过期、搜索词语义缺失等数据质量信号。
这会让用户误以为“无 ASIN = 未归因广告数据”，进一步混淆数据链路问题和广告经营对象问题。
```

原因：

```text
原 filterSignalsByProductScope 的 unattributed 判断只看 signalProductScopeIds(signal).length === 0。
数据质量信号通常也没有 ASIN，因此被误归入未归因广告数据。
但数据质量层是数据链路问题，不是广告搜索词、广告位或广告组这类经营对象。
```

解决方案：

```text
新增队列主视角标签 buildSignalQueueScopeBadge。
未归因筛选改为：signal_category !== data_quality 且没有商品 scope。
数据质量信号保留在“全部”和“数据质量”队列，不进入“未归因广告数据”商品视角筛选。
```

后续防错规则：

- `无 ASIN` 不等于 `未归因广告数据`。
- 数据质量信号必须先归入数据质量层。
- 未归因广告数据只用于承载无商品键的广告经营对象，例如搜索词、广告位、广告组结构。
- 任何新增商品视角筛选都必须明确排除数据链路信号，除非 UI 明确展示为数据质量层。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验证：
选择“未归因广告数据”后，信号队列显示 5 条。
队列有“未归因广告数据”标签，包含广告位和搜索词。
队列不再出现“数据质量层”或 ABA 数据质量信号。
```

### 问题 97：数据质量层排除出商品筛选后缺少入口提示

现象：

```text
修正“未归因广告数据”不再混入数据质量信号后，数据质量层虽然仍可通过队列筛选查看，但顶部商品视角区域没有提示。
用户可能误以为数据质量信号被过滤掉或丢失。
```

原因：

```text
商品视角边界条只展示商品 ASIN、销售 ASIN、Parent ASIN 和未归因搜索词/广告位。
数据质量层不参与商品归因，因此不能放进商品视角下拉。
但如果完全不在顶部解释，会造成“数据质量在哪里看”的信息断点。
```

解决方案：

```text
新增 buildSignalLayerOverview，用统一的主视角口径统计商品、未归因、数据质量和投放容器层。
在顶部商品视角边界条新增“数据质量层 N 条 / 不参与商品归因”。
文案明确：数据质量信号独立进入队列筛选，不进入商品或未归因广告数据筛选。
```

后续防错规则：

- 数据质量层不能加入商品视角下拉。
- 排除某类信号时，必须在附近说明入口和原因。
- 商品视角区域可以提示数据质量层存在，但必须写明“不参与商品归因”。
- 数据质量信号只解释数据链路、字段覆盖、新鲜度和导入问题，不能扩大成广告经营结论。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验证：
顶部边界条显示“数据质量层 3 条 / 不参与商品归因”。
切换到“未归因广告数据”后，ABA 数据质量信号没有混入队列。
```

### 问题 98：Parent ASIN 只显示计数，没有成为可筛选诊断入口

现象：

```text
顶部商品视角已经显示 Parent ASIN 计数，但下拉选项只有广告 ASIN、销售 ASIN 和未归因广告数据。
如果后续数据中出现 parent_asin，用户仍不能直接按 Parent ASIN 商品组收敛信号。
```

原因：

```text
原 build_product_scope_summary 只生成 ad_asin:* 和 sales_asin:* 选项。
前端 filterSignalsByProductScope 也只按信号自身的 ASIN scope_id 匹配，不支持 parent_asin:* 通过子 ASIN 命中信号。
```

解决方案：

```text
ProductScopeOption 增加 child_asins。
后端根据 sales_product_daily_metrics 和 advertised_products 中的真实 parent_asin 字段生成 parent_asin:* 选项。
前端筛选支持用 parent_asin:* 选项里的 child_asins 匹配信号中的 ASIN。
顶部文案改为“诊断入口”，并说明 Parent ASIN 有真实字段时优先作为商品组入口，缺失时按 ASIN 观察。
```

后续防错规则：

- Parent ASIN 必须来自真实字段或人工维护映射，不能从 ASIN 猜。
- Parent ASIN 是经营入口，不是自动归因结论。
- 搜索词和广告位没有 ASIN 时，仍然必须留在未归因广告数据。
- 只显示 Parent ASIN 计数不够；一旦有真实 parent_asin，必须能成为可筛选入口。

验证方式：

```text
python -m pytest tests\test_product_scope.py
结果：通过

npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验证：
顶部显示“诊断入口”。
顶部边界条显示“商品 / Parent ASIN”。
当前真实快照 Parent ASIN 为 0，页面明确提示缺失时按 ASIN 观察。
```

### 问题 99：总览异常数把数据质量当成经营异常

现象：

```text
总览里“异常信号”会统计 signal_type=anomaly 的数据质量信号。
当数据质量有 3 条时，页面看起来像出现 4 条经营异常，容易让用户误判业务问题数量。
```

原因：

```text
buildSignalOverview 原本只按 signal_type=anomaly 统计异常，没有排除 signal_category=data_quality。
但数据质量信号解释的是数据链路、字段覆盖或新鲜度，不代表广告经营异常。
```

解决方案：

```text
总览文案从“异常信号”改为“经营异常”。
经营异常只统计 signal_type=anomaly 且 signal_category != data_quality 的信号。
数据质量继续单独统计为“数据质量”。
```

后续防错规则：

- 数据质量可以是 anomaly 类型，但不能计入经营异常总数。
- 总览指标名称必须体现统计口径，不能用模糊“异常”混合数据链路和经营对象。
- 新增信号层级后，必须检查总览、队列、筛选器三处是否同口径。

验证方式：

```text
npm run test:signal-ui
结果：通过

npm run build
结果：通过

浏览器验证：
总览显示“经营异常 1 / 数据质量 3”。
控制台 error/warn 为 0。
```

### 问题 100：Parent ASIN 映射不能和当前 API 指标混算周期

现象：

```text
销售表现-父ASIN 导入周期是 2026-03-20 至 2026-05-18。
当前 API 快照周期是 2026-06-08 至 2026-06-14。
如果 Parent ASIN 入口同时展示 ERP 60 天销售额和 API 7 天广告花费，会让用户误以为这些指标属于同一分析周期。
```

原因：

```text
最初把 ERP 父 ASIN 行既当映射，又当入口指标来源。
但本轮 MVP 只需要父子 ASIN 映射，入口指标应按当前真实 API 快照周期解释。
```

解决方案：

```text
parent_asin_product_map 只参与 parent_asin -> child_asins 映射。
ProductScope 的 spend / orders / sales 仍来自当前 API 快照中能匹配到的广告商品或销售商品。
不同周期 ERP 指标后续要作为单独证据展示，必须标明来源周期，不能混入当前入口汇总。
```

后续防错规则：

- Parent ASIN 是诊断入口，不是自动归因结论。
- ERP 导入文件如果周期不同，只能作为带来源周期的证据，不得和当前 API 快照汇总指标直接相加。
- 入口指标必须同周期、同来源口径，不能为了看起来完整而混算。

### 问题 101：数据质量信号不能进入商品 / Parent ASIN 筛选

现象：

```text
切换到 Parent ASIN B00K4W4AAA 后，商品入口下仍出现“产品销售承接信号证据不足”这类数据质量信号。
页面文案同时写着“数据质量信号独立进入队列筛选，不进入商品或未归因广告数据筛选”，前后冲突。
```

原因：

```text
filterSignalsByProductScope 只在 unattributed 分支排除了 data_quality。
如果数据质量信号的 source_rows 里带有 advertised_products，Parent ASIN child_asins 映射会把它误匹配进商品入口。
```

解决方案：

```text
非 all 入口下，signal_category=data_quality 一律不参与商品 / Parent ASIN / 未归因广告数据筛选。
数据质量只能在 all 视图或数据质量队列筛选中查看。
```

后续防错规则：

- 数据质量解释数据链路，不解释商品经营表现。
- 商品筛选器只筛经营对象信号，不筛数据链路信号。
- 新增筛选入口时必须同时检查 all、unattributed、Parent ASIN、data_quality 四个视图口径。

### 问题 102：ERP 广告花费存在负数记账口径

现象：

```text
真实销售表现-父ASIN 文件中“广告花费”导入后为负数，例如 -37772.39。
如果直接进入后续分析，会出现负成本、负 ACOS 或错误机会判断。
```

原因：

```text
ERP 导出可能按财务支出口径把广告花费记为负数。
广告诊断中 cost / spend 应采用正数成本口径。
```

解决方案：

```text
parent_asin_import.py 导入“广告花费”时统一取绝对值。
测试样本加入负数广告花费，确保导入结果为正数。
```

后续防错规则：

- 所有 ERP 导入指标要先确认正负号业务含义。
- 成本类字段进入广告分析前必须统一为正数。
- 如果保留原始财务口径，必须另存 raw 字段，不能覆盖分析口径。

### 问题 103：Windows 本地验收容易被旧进程和 PowerShell 细节干扰

现象：

```text
8010 / 8012 等端口存在旧后端进程，OpenAPI 路由缺少新接口。
PowerShell 不支持 Bash 风格 python heredoc。
Start-Process 不能把 stdout 和 stderr 重定向到同一个文件。
中文路径传给 Python 时可能出现乱码或问号。
```

原因：

```text
Windows 本地环境里多个历史 uvicorn / Vite 进程并存。
PowerShell 语法和 Bash 不一致，默认编码也可能影响中文路径。
```

解决方案：

```text
验收新后端时使用 uvicorn --app-dir 指向当前 backend 绝对路径，并换未占用端口。
Start-Process 的 stdout / stderr 分别写入不同日志文件。
PowerShell 中先设置 $OutputEncoding 和 [Console]::OutputEncoding 为 UTF-8，再传中文路径。
不要在 PowerShell 中使用 python - <<'PY' 这类 Bash heredoc。
```

后续防错规则：

- 浏览器验收前必须先用 /openapi.json 或目标接口确认当前端口加载的是当前代码。
- 启动新服务尽量使用隔离端口，不误杀用户正在看的旧进程。
- 命令失败先判断 shell / 进程 / 编码问题，不要先归因到业务代码。

### 问题 104：Parent ASIN 空队列不一定是筛选失败

现象：

```text
选择 Parent ASIN B00K4W4AAA 后，信号队列显示 0 条。
如果只看 UI，容易误以为 Parent ASIN 筛选没有生效，或者系统没有读到广告商品数据。
```

原因：

```text
后端原始规则在没有主推款资料时会生成“多商品消耗集中，先确认头部 ASIN 是否主推款”。
但 API 路由会读取 data/runtime/promotion_strategy_profiles.json。
当前 B016EXMVZS 已被标记为主推款，规则正确压制了该广告组结构提示。
因此空队列是策略资料生效后的结果，不是筛选失败。
```

解决方案：

```text
ProductScopeOption 增加 strategy_notes。
ProductScope 读取主推款策略资料，给 Parent ASIN / 广告 ASIN 入口补充“已标记为主推款，不作为异常推送”的说明。
前端在诊断入口边界条展示选中入口的策略说明。
```

后续防错规则：

- 不要为了让 Parent ASIN 队列“有内容”而恢复已确认主推款的误报。
- 空队列必须可解释，至少说明是没有命中规则、数据不足，还是被人工策略资料压制。
- 策略事实属于解释层，不等于异常或机会信号。

### 问题 105：首屏默认 all 会让商品诊断重新变乱

现象：

```text
诊断入口虽然已经支持 Parent ASIN / ASIN，但默认仍停在“全部商品和未归因对象”时，用户第一眼会同时看到数据质量、广告位、搜索词和商品信号。
这会让“商品优先”的信息架构失效，用户仍然觉得广告数据很乱。
```

原因：

```text
all 视图适合排查全量信号，不适合作为默认经营诊断入口。
用户的主要决策对象是 Parent ASIN / ASIN，广告组、搜索词和广告位应作为证据维度或下钻维度，而不是首屏混排的主对象。
```

解决方案：

```text
新增 preferredProductScopeId。
没有手动选择诊断入口时，默认优先选择 Parent ASIN，其次广告 ASIN、销售 ASIN，最后才回退到 all。
同时补 advertised_product_opportunity，让 Parent ASIN 入口下能看到真实广告 ASIN 机会信号，而不是为了有内容恢复广告组结构误报。
```

后续防错规则：

- 首屏默认入口必须贴近经营对象，优先 Parent ASIN / ASIN。
- all 视图只能作为全量排查入口，不能作为默认诊断口径。
- 广告组信号进入商品入口前必须有广告商品 ASIN 证据或明确 child_asins 映射。
- 不要把搜索词、广告位、数据质量层强行归到 Parent ASIN。

### 问题 106：不能为了让页面出现异常而调低商品异常阈值

现象：

```text
补广告商品级异常规则后，当前真实快照仍然没有 advertised_product_efficiency 异常信号。
如果只追求页面上“有异常”，容易把正常表现的广告 ASIN 也推成异常。
```

原因：

```text
当前真实广告商品 B016EXMW02 和 B016EXMVZS 都有足够订单，ACOS 分别约 27.28% 和 19.60%，不满足商品异常条件。
强行调低阈值会制造误报，破坏用户对信号可信度的判断。
```

解决方案：

```text
advertised_product_efficiency 保持保守阈值：
点击 >= 20 且订单 <= 1；
或点击 >= 20 且 ACOS >= 45%。
当前真实快照 0 条商品异常是正确结果，测试用 fixture 验证规则触发能力。
```

后续防错规则：

- 没有异常命中时先尊重真实数据，不为了 UI 丰富度制造异常。
- 机会信号和异常信号必须分开，不能让高 ACOS 商品进入机会信号。
- 商品异常只能说明广告商品 ASIN 当前承接偏弱，不能直接断言搜索词、广告位或广告组原因。

### 问题 107：同广告组上下文不能被当成 ASIN 归因

现象：

```text
用户希望按 ASIN / Parent ASIN 查看广告数据。
如果把同广告组搜索词或广告位直接展示在广告 ASIN 证据里，容易误读为这些搜索词 / 广告位已经精确归因到该 ASIN。
```

原因：

```text
用户当前业务里常见“一个广告组投多个商品”。
广告商品报表可以落到 ASIN，但搜索词和广告位通常只落到广告组、活动或流量位置。
同广告组数据只能解释上下文，不能证明某个搜索词或广告位由某个 ASIN 单独产生。
```

解决方案：

```text
广告商品机会 / 异常信号可以补同广告组搜索词和广告位上下文。
上下文必须同时补“上下文边界”：搜索词和广告位只说明同广告组上下文，不能自动归因到该广告 ASIN。
source_rows 和 data_sources 可以包含上下文行，但信号 object_type 仍保持 advertised_product。
```

后续防错规则：

- Parent ASIN / ASIN 是诊断入口，不是强制归因容器。
- 只有 advertised_products、sales_product_daily_metrics 这类商品粒度数据才能直接作为 ASIN 证据。
- 搜索词、广告位进入商品信号时必须标注“上下文”或“边界”，不能写成商品归因证据。
- 没有 ad_group_id / group_id 的广告位行不能强行挂到广告商品信号。

### 问题 108：前端本地验收时 API base 不能重复带 `/api`

现象：

```text
本轮启动前端时设置 VITE_API_BASE_URL=http://127.0.0.1:8031/api。
页面显示“后端服务未连接”，但后端 http://127.0.0.1:8031/api/signals?market_id=1 实际可访问。
```

原因：

```text
frontend/src/api.ts 的请求 path 已经包含 /api，例如 /api/signals。
VITE_API_BASE_URL 再带 /api 后，实际请求会变成 http://127.0.0.1:8031/api/api/signals。
```

解决方案：

```text
前端 dev server 使用：
VITE_API_BASE_URL=http://127.0.0.1:8031

不要写成：
VITE_API_BASE_URL=http://127.0.0.1:8031/api
```

后续防错规则：

- 浏览器截图前先用页面实际 served module 或网络请求确认 API_BASE。
- 页面出现“后端服务未连接”时，先查 API base 和路径拼接，不要先怀疑后端规则。
- 前端 API base 只填 origin，不填业务 path。

### 问题 109：应用内浏览器截图接口可能超时

现象：

```text
商品下钻 UI 已在页面 DOM 中出现，但应用内浏览器调用 Page.captureScreenshot 连续超时。
如果继续反复截图，会浪费验收时间，并且容易误判为页面未渲染。
```

原因：

```text
本地应用内浏览器的截图能力偶发卡在 CDP 截图命令上。
这属于浏览器验收工具问题，不等于前端页面或后端 API 失败。
```

解决方案：

```text
先用 DOM 状态确认页面事实，例如是否存在“商品下钻”、默认入口是否为 Parent ASIN、是否有后端连接错误。
截图接口连续失败时，用本机 headless Chrome 针对同一个本地 URL 生成截图，并记录截图路径。
```

复现记录：

```text
2026-06-14 商品组概览验收时，应用内浏览器 Page.captureScreenshot 再次超时。
本轮先用 DOM 确认“Parent ASIN B00K4W4AAA 商品组”、2 个广告 ASIN、搜索词 / 广告位上下文边界已经渲染，再用 headless Chrome 生成同一 URL 截图。

2026-06-14 广告 ASIN 表格点击联动验收时，Chrome DevTools Protocol 的 Runtime.evaluate 可以完成点击和 DOM 断言，但 Page.captureScreenshot 仍超时。
本轮改为用 DOM 断言验证点击前后 select.value 从 parent_asin:B00K4W4AAA 变成 ad_asin:B016EXMVZS，再用普通 headless Chrome --screenshot 截取表格入口。

2026-06-14 商品视角当前分析路径验收时，应用内浏览器 DOM 断言已确认“当前分析路径”存在，但 Page.captureScreenshot 仍超时。
本轮用 headless Chrome 第一次截图过早，页面还在“正在读取信号”；第二次增加 --virtual-time-budget 后成功截到已加载页面。

2026-06-14 复盘待办 UI 验收时，页面 DOM 已确认“复盘待办”和复盘口径文案存在，但 Page.captureScreenshot 仍超时。
本轮继续按同一策略处理：用 DOM 验证页面事实，用本机 headless Chrome 对 http://127.0.0.1:5192/ 生成截图。
```

后续防错规则：

- 截图失败时先区分“页面事实未达成”和“截图工具超时”。
- 必须先保留 DOM 或接口证据，再用截图兜底。
- 使用 headless Chrome 兜底时必须指向同一个前端 URL 和同一个后端 API base。
- 使用 headless Chrome 截 React 页面时，要给足加载等待预算，避免截到“正在读取信号”的中间态。
- 交互验收优先断言稳定的 DOM value 或接口状态，不依赖中文正文匹配。

### 问题 110：全量入口容易把商品诊断重新带乱

现象：

```text
用户指出当前广告数据看起来很乱，并判断上一层按 ASIN 或 Parent ASIN 分组筛选分析会更合理。
如果“全部商品和未归因对象”看起来像主入口，用户第一眼仍会看到商品、搜索词、广告位和数据质量混排。
```

原因：

```text
all 视图适合全量排查，不适合做经营诊断主口径。
用户的日常决策对象是 Parent ASIN / ASIN。
搜索词、广告位和广告组是下钻证据或未归因广告数据，数据质量是独立数据链路层。
```

解决方案：

```text
将 all 入口文案改为“全量排查（商品 + 未归因 + 数据质量）”。
在诊断入口边界区展示当前入口说明：
Parent ASIN / ASIN 是商品诊断入口；未归因广告数据只解释上下文；全量排查只用于混合排查。
```

后续防错规则：

- 默认经营诊断优先 Parent ASIN / ASIN。
- all 入口不能再写成看起来像商品主入口的文案。
- 未归因广告数据不能强行归到商品。
- 数据质量层不能混入商品筛选。

验证方式：

```text
npm run test:signal-ui
npm run build
```

### 问题 127：人工留痕候选推荐不能只按严重度排序

#### 现象

```text
当前真实候选中，placement 信号是 P0 / severity=4。
advertised_product 信号是 P1 / severity=3。
如果只按严重度或优先级推荐，系统会先推荐广告位。
```

#### 原因

```text
长期目标要求围绕 Parent ASIN / ASIN 经营商品入口跑通闭环。
广告位虽然优先级高，但它不是商品入口对象。
第一条真实人工留痕更适合选择能落到 Parent ASIN / ASIN 的广告 ASIN，便于后续经营商品维度复盘。
```

#### 解决方案

```text
inspect_review_candidates.py 输出 recommended_candidate 和 recommendation_reason。
推荐规则优先考虑：
1. 是否能落到 Parent ASIN / ASIN 商品入口。
2. 是否没有主推款策略备注干扰。
3. 再考虑 severity / priority。
```

#### 后续防错

```text
推荐“下一条人工留痕候选”时，不能只看 severity 或 P0/P1。
必须先判断该候选是否服务当前工作台主入口：
Parent ASIN / ASIN -> 信号 -> 人工确认 -> 7 天 / 14 天复盘。
广告位、搜索词和全量排查对象可以作为辅助入口，但不应覆盖商品入口的第一条闭环样本。
```

#### 验证方式

```text
python -m pytest backend\tests\test_review_candidates_cli.py -q
python scripts\inspect_review_candidates.py --market-id 1
```

### 问题 128：推荐人工留痕对象只在 CLI 中可见会卡住工作台 MVP

#### 现象

```text
inspect_review_candidates.py 已能输出 recommended_candidate。
但如果工作台页面仍只展示普通信号队列，运营需要回到 CLI 或聊天结论里找下一条建议对象。
这会让“AI 信号 -> 人工选择 -> 留痕 -> 后续复盘”的 MVP 闭环停在页面外。
```

#### 原因

```text
MVP 的关键不是多一个脚本字段，而是运营能在工作台里完成下一步选择。
推荐结果如果不可见，页面仍无法引导用户从真实候选中选中第一条广告对象级信号。
```

#### 解决方案

```text
新增 buildRecommendedManualActionCandidate。
左侧信号队列展示“建议优先留痕”提示块，显示推荐对象和推荐原因。
按钮只负责查看对应信号：
- setFilter("all")
- setSelectedId(signal.id)

不调用 createSignalManualAction。
不写 manual_actions.jsonl。
不保存 review_records。
```

#### 后续防错

```text
任何“建议下一步处理对象”的能力，必须进入运营实际工作路径。
但推荐入口只能帮助选择和解释，不能替用户记录人工动作，更不能自动执行广告动作。
```

#### 验证方式

```text
npm run test:signal-ui
npm run test:review-ui
npm run build
```

### 问题 129：广告商品人工留痕不能使用快照行 ID 作为长期复盘对象

#### 现象

```text
advertised_product 信号的 primary_object.object_id 可能是快照行 ID。
例如 snapshot-1:ad-product:1:28792336180945。
如果人工动作把该值保存为 object_id，后续新快照的 row_id 变化后，review-effect 很可能匹配不到同一个广告 ASIN。
```

#### 原因

```text
复盘需要跨快照比较同一个业务对象。
快照行 ID 是单次快照内的技术标识，不是长期业务对象标识。
广告商品 / 销售商品复盘应优先使用 ASIN，其次才是 msku / sku，最后才回退到原 object_id。
```

#### 解决方案

```text
create_signal_manual_action 保存人工动作前，通过 manual_action_object_id 计算稳定对象 ID。
advertised_product / sales_product：
1. asin
2. msku
3. sku
4. object_id

其它对象保持原 object_id。
```

#### 后续防错

```text
任何进入 7 天 / 14 天复盘的人工动作，都必须检查 object_id 是否是跨快照稳定业务键。
不要把 source_record_id、row_id、snapshot_id 拼接 ID 当成长期复盘对象。
```

#### 验证方式

```text
python -m pytest tests\test_manual_actions.py::test_manual_action_route_uses_asin_for_advertised_product_review_identity -q
python -m pytest tests\test_manual_actions.py tests\test_api_routes.py tests\test_review_candidates_cli.py tests\test_review_readiness_cli.py -q
python -m py_compile app\api\routes.py
```

### 问题 130：推荐候选进入人工留痕前缺少只读预检

#### 现象

```text
推荐候选已经能指出优先处理对象，但在真实人工动作写入前，缺少一份只读预检结果。
运营人员无法先确认将要写入的 signal_id、action_type、稳定 object_id 和复盘窗口。
```

#### 原因

```text
人工留痕是业务确认动作，不能由 AI 自动创建。
但 MVP 闭环需要在点击前暴露写入合同，避免把快照行 ID 或错误复盘窗口写进 manual_actions.jsonl。
```

#### 解决方案

```text
inspect_review_candidates.py 输出 manual_action_preview。
该预检包含：
- will_write=false
- signal_id
- action_type=add_to_review
- object_type
- object_id
- object_label
- market_id
- review_windows=7d / 14d

预检只读，不写 manual_actions.jsonl。
真实人工动作仍必须由人工按钮触发。

前端工作台的“建议优先留痕”卡片同步展示只读预检：
- action_type
- object_id
- review_windows
```

#### 后续防错

```text
任何推荐动作都必须区分 preview 和 write。
will_write=false 的输出不得创建运行时记录。
真实写入必须保留人工确认入口，不能由脚本自动补人工动作。
```

#### 验证方式

```text
python -m pytest tests\test_review_candidates_cli.py -q
python -m pytest tests\test_manual_actions.py tests\test_review_candidates_cli.py tests\test_review_readiness_cli.py tests\test_api_routes.py -q
python -m py_compile app\services\manual_actions.py app\api\routes.py ..\scripts\inspect_review_candidates.py
python scripts\inspect_review_candidates.py --market-id 1
python scripts\inspect_review_readiness.py --market-id 1
```

### 问题 131：历史广告商品人工动作仍可能携带快照行 ID

#### 现象

```text
当前 data/runtime/manual_actions/manual_actions.jsonl 中已经存在 1 条 advertised_product 历史动作：
signal_id=sig-ad-product-stable-conversion-...:ad-product:1:173873141224215
object_label=B016EXMVZS
object_id=gerpgo_market_1_20260614_075325:ad-product:1:173873141224215

该 object_id 仍是快照行 ID，不是稳定 ASIN。
```

#### 原因

```text
该运行记录产生于稳定 object_id 规则完成之前，属于历史数据污染。
代码已经修正后续写入逻辑，但已有 runtime JSONL 不会自动迁移。
```

#### 解决方案

```text
本轮不自动改运行数据。
inspect_review_readiness.py 增加只读诊断：
- manual_action_identity_issue_count
- manual_action_identity_issues

诊断规则：
- 只检查 object_type=advertised_product / sales_product。
- 当 object_id 看起来是 snapshot row_id 时输出问题项。
- 用成功快照 signal_rows 按 row_id / source_record_id / id 匹配原始行。
- 优先建议 asin，其次 msku / sku。
- 每条问题项固定 will_write=false。

当前诊断结果：
- manual_action_identity_issue_count=1
- action_id=manual-action-36ab1ca4680846e2899f747c43c0e800
- current_object_id=gerpgo_market_1_20260614_075325:ad-product:1:173873141224215
- suggested_object_id=B016EXMVZS

用户确认后已执行迁移：
- action_id=manual-action-36ab1ca4680846e2899f747c43c0e800
- object_id 从 gerpgo_market_1_20260614_075325:ad-product:1:173873141224215 改为 B016EXMVZS
- 迁移后 manual_action_identity_issue_count=0
- review_record_count=0，未保存复盘结论
```

#### 后续防错

```text
不要把历史 runtime 记录当作当前代码仍在写错。
修复历史数据前，review readiness 里出现的广告商品 object_id 需要区分：
1. 新代码写入的稳定 ASIN。
2. 旧记录遗留的快照行 ID。

历史迁移必须显式确认后再写入，不能由 AI 自动改 manual_actions.jsonl。
```

#### 验证方式

```text
python scripts\inspect_review_readiness.py --market-id 1
python -m pytest tests\test_review_readiness_cli.py -q
python -m pytest tests\test_review_readiness_cli.py tests\test_manual_actions.py tests\test_review_candidates_cli.py tests\test_api_routes.py -q
人工检查 data/runtime/manual_actions/manual_actions.jsonl
```

### 问题 132：PowerShell UTF8 写入 BOM 会导致 JSONL 第一行被后端跳过

#### 现象

```text
迁移 manual_actions.jsonl 后，inspect_review_readiness.py 一度显示 manual_action_count 从 3 变成 2。
人工检查 JSONL 仍有 3 行，PowerShell ConvertFrom-Json 也能解析 3 行。
但后端 load_manual_actions 只加载 2 行。
```

#### 原因

```text
PowerShell 里的 Set-Content -Encoding UTF8 会在文件开头写入 UTF-8 BOM。
后端读取 JSONL 时使用 path.read_text(encoding="utf-8") 后逐行 json.loads。
第 1 行包含 BOM 后，json.loads 抛出 Unexpected UTF-8 BOM，load_manual_actions 会跳过该行。
```

#### 解决方案

```text
重新用无 BOM UTF-8 写回 manual_actions.jsonl：
[System.Text.UTF8Encoding]($false)

修复后：
- JSONL 3 行均可通过 ManualActionRecord 校验。
- load_manual_actions(market_id=1) 返回 3 条。
- inspect_review_readiness.py 显示 manual_action_count=3。
```

#### 后续防错

```text
修改 JSONL 运行记录时，不要使用 PowerShell Set-Content -Encoding UTF8。
必须使用无 BOM UTF-8 写入，或用不会写 BOM 的工具。
迁移后必须验证：
1. 每行 json.loads 成功。
2. ManualActionRecord 校验成功。
3. load_manual_actions 读取数量符合预期。
```

#### 验证方式

```text
python scripts\inspect_review_readiness.py --market-id 1
load_manual_actions(market_id=1)
逐行 ManualActionRecord.model_validate
```

### 问题 111：新项目没有旧项目任务三件套

现象：

```text
本轮按旧项目经验尝试读取 project_state.json、当前任务.md、开发任务清单.md。
新项目 Amazon广告AI信号工作台 实际没有这三个文件。
```

原因：

```text
旧项目 Amazon广告分析 使用过任务三件套，但新项目已经把执行规则集中到 AGENTS.md、项目规划.md、项目规范.md、开发原型.md 等管理文档。
把旧项目流程原样套到新项目，会造成无效检查和误判。
```

解决方案：

```text
新项目当前以 AGENTS.md 和项目管理文档为准。
不要把旧项目任务三件套当作新项目必备文件；除非后续明确新增当前任务管理文件。
```

后续防错规则：

- 开始任务前按新项目 AGENTS.md 的“每次开始任务前必须读取”清单执行。
- 旧项目流程只能作为经验参考，不能当作新项目文件事实。
- 缺文件时先核对新项目 AGENTS.md，不要直接创建同名文件补齐旧流程。

验证方式：

```text
rg --files
结果：新项目根目录不存在 project_state.json / 当前任务.md / 开发任务清单.md。
```

### 问题 112：reviewUi 测试文件存在但缺少 npm 脚本

现象：

```text
运行 npm run test:review-ui 时失败，提示 Missing script: "test:review-ui"。
但 frontend/tests/reviewUi.test.ts 已存在。
```

原因：

```text
新增 reviewUi 纯函数测试后，没有把对应测试命令写入 frontend/package.json。
```

解决方案：

```text
在 frontend/package.json 增加 test:review-ui。
```

后续防错规则：

- 新增前端纯函数测试文件时，同步检查 package.json 是否有对应脚本。
- 文档或进度记录里写验证命令前，先以 package.json 的实际 scripts 为准。
- 如果临时使用 npm exec 长命令，任务结束前评估是否应沉淀为 npm script。

验证方式：

```text
npm run test:review-ui
结果：通过。
```

## 3. 任务结束检查清单

每个任务结束前快速检查：

- 是否被旧项目对象模型带偏？
- 是否把广告组当成产品？
- 是否把 ERP 页面功能当 API 能力？
- 是否把 ABA 当店铺数据？
- 是否让 mock 数据看起来像真实结论？
- 是否绕过了人工确认？
- 是否新增了自动广告动作？
- 是否更新了相关文档和本文件？
### 问题 114：默认 API 端口被旧服务占用会误判为后端未连接

#### 现象

当前前端默认读取 `http://127.0.0.1:8010`。
本轮验证时 8010 已被一个旧 Python 进程占用，但该进程不是当前项目 API，访问 `/api/product-scope` 返回 404。
页面因此显示“后端服务未连接”，容易被误判为商品入口或信号接口坏了。

#### 原因

本机存在多个历史 dev server 和 Python 服务。
如果只看页面错误，不核对端口对应进程和 API 返回，就会把运行态污染当成业务代码问题。

#### 解决方案

```text
不直接杀旧进程。
当前项目后端临时启动到 8000。
前端用 VITE_API_BASE_URL=http://127.0.0.1:8000 在新端口 5183 启动。
启动后必须检查 /src/api.ts 中编译出的 API_BASE 是否为 8000，再做 DOM 和截图验证。
```

#### 后续防错

```text
看到“后端服务未连接”时，先检查：
1. 前端编译出的 API_BASE。
2. API_BASE 端口是否属于当前项目。
3. /api/product-scope 是否返回当前项目数据。
不能直接修改业务筛选逻辑。
```

#### 2026-06-14 复核补充

```text
当前 5183 前端指向当前项目 8000，可以作为运行态验收 URL。
当前 5184 / 5192 / 5193 / 5194 前端仍指向旧 8010，不能作为当前项目 UI 验收 URL。
截图或 DOM 验证前，必须先检查 /src/api.ts 中是否包含 127.0.0.1:8000。
```

### 问题 115：旧项目 cwd 污染会让补丁误指向旧目录

#### 现象

本轮第一次执行 `apply_patch` 时，补丁路径按默认工作目录解析到了旧项目：

```text
D:\DataProject\coedx_project\Amazon广告分析
```

实际要修改的新项目是：

```text
D:\DataProject\coedx_project\Amazon广告AI信号工作台
```

因此补丁报错找不到 `frontend/tests/reviewUi.test.ts`。

#### 原因

线程环境里的默认 cwd 仍可能来自旧项目，不能假定文件编辑工具会自动使用新项目目录。

#### 解决方案

```text
修改新项目文件时使用绝对路径。
运行 shell 命令时显式指定 workdir=D:\DataProject\coedx_project\Amazon广告AI信号工作台。
报错出现 Amazon广告分析 路径时立即停止，确认没有误改旧项目。
```

#### 后续防错

```text
每轮正式改文件前，先确认目标路径包含 Amazon广告AI信号工作台。
旧项目只允许只读参考 API 路径和字段经验，不能作为补丁目标。
```

### 问题 116：按广告组或全量广告对象作为主入口会让经营诊断变乱

#### 现象

用户指出当前广告数据如果按现有方式看会很乱，原因是实际业务里常见一个广告组投放多个商品。
如果主界面先按广告组、搜索词、广告位和数据质量混排，运营第一眼无法判断到底是哪个经营商品有问题。

#### 原因

```text
广告组是投放容器，不是经营商品。
一个广告组可以同时投放多个广告 ASIN。
搜索词和广告位很多时候只能落到广告活动、广告组或流量位置上下文，不能自动归因到单个 ASIN。
```

#### 解决方案

```text
MVP 主入口按经营对象上移：
店铺 / 站点 -> Parent ASIN / ASIN -> 广告 ASIN 对比 -> 搜索词 / 广告位上下文 -> 信号分诊。

全量排查只作为辅助视图，用于查看未归因广告数据、数据质量事项和全局复盘待办。
```

#### 后续防错

```text
默认入口必须优先 Parent ASIN / ASIN。
广告组只能作为投放容器证据或下钻上下文。
搜索词和广告位进入商品信号时必须标注“上下文”或“边界”，不能写成 ASIN 归因证据。
主推款策略属于解释层事实；主推 ASIN 消耗集中不能直接判成异常。
```

#### 验证方式

```text
npm run test:signal-ui
npm run test:review-ui
npm run build
运行态截图确认首屏仍是“当前商品组 AI 信号”和 Parent ASIN 商品组入口。
```

### 问题 117：移动端截图横向溢出不能当成已完成响应式验收

#### 现象

```text
390px 宽度截图下，商品组关系摘要已经按单列展示，但页面整体仍有横向溢出。
顶部刷新按钮和广告 ASIN 表格区域无法在当前视口内完整显示。
```

#### 原因

```text
当前工作台优先为桌面运营场景设计，部分工具区、筛选区和广告 ASIN 表格仍有较宽内容。
本轮只做商品关系摘要收敛，没有完整整理移动端所有宽度约束。
```

#### 解决方案

```text
本轮先修正顶部操作区的移动端布局，避免明显按钮挤压。
完整移动端响应式需要作为独立 UI 任务处理，重点检查筛选栏、数据新鲜度、广告 ASIN 表格和三栏工作台降级布局。
```

#### 后续防错

```text
桌面截图通过不等于移动端验收完成。
如果声明移动端完成，必须同时检查 390px 和桌面截图。
看到横向溢出时，先定位是表格、筛选器、顶部操作区还是工作台三栏造成，不能简单归因为截图工具问题。
```

#### 验证方式

```text
桌面截图：
C:\Users\11010\AppData\Local\Temp\amazon-ai-signal-workbench-product-relation-summary-5183-20260614.png

移动端风险截图：
C:\Users\11010\AppData\Local\Temp\amazon-ai-signal-workbench-product-relation-summary-mobile-grid-5183-20260614.png
```

### 问题 118：商品范围少信号不能直接解读为系统没读到数据

#### 现象

```text
切到某个 Parent ASIN 或 ASIN 后，左侧队列可能只有 0-2 条信号。
如果界面不解释原因，用户容易误以为系统没有读取广告数据，或者误以为该商品没有任何问题。
```

#### 原因

```text
商品范围内信号少，至少可能来自四类原因：
策略压制：例如主推款本来就允许更高预算或消耗集中。
暂无规则命中：真实快照扫描后没有触发更多异常或机会阈值。
数据不足：当前范围缺少订单、销售或花费样本，只能观察。
归因边界：搜索词、广告位和数据质量信号可能只属于广告上下文，不能强行写成 ASIN 证据。
```

#### 解决方案

```text
在当前商品组 AI 信号队列下方增加少信号解释卡片。
由 buildProductScopeSignalExplanation 根据当前筛选范围、范围内信号数、全量信号数、数据质量信号数和 strategy_notes 输出解释。
界面直接说明：信号少不等于系统没读到数据，需要区分策略压制、暂无规则命中、数据不足和归因边界。
```

#### 后续防错

```text
不能为了让队列看起来“有内容”，重新放回被策略压制的广告组误报。
不能把全量排查里的搜索词、广告位或数据质量信号强行塞进商品归因。
如果 Parent ASIN / ASIN 信号少，先解释扫描口径，再决定是否需要进入全量排查或广告上下文。
```

#### 验证方式

```text
npm run test:signal-ui
npm run test:review-ui
npm run test:snapshot-ui
python -m pytest tests\test_product_scope.py tests\test_manual_actions.py -q
npm run build

截图：
C:\Users\11010\AppData\Local\Temp\amazon-ai-signal-workbench-sparse-signal-explanation-5183-20260614.png
```

### 问题 119：只在下拉里提供 Parent ASIN 不足以阻止用户回到广告组混排视角

#### 现象

```text
用户看到当前广告数据仍觉得很乱，并提出是否应该在上一层按 ASIN 或 Parent ASIN 分组筛选分析。
虽然代码已优先选择 parent_asin 作为默认 scope，但如果首屏没有明确解释分析层级，用户仍会按广告组、搜索词、广告位混在一起理解。
```

#### 原因

```text
广告数据天然来源于广告活动、广告组、搜索词和广告位。
如果 UI 只放一个“诊断入口”下拉，而没有显式说明“经营对象优先”，用户会把广告组当成主分析对象。
这会回到旧项目最容易出错的模型：广告组看起来像产品，商品归因和投放容器混在同一层。
```

#### 解决方案

```text
新增 buildProductScopeEntryGuidance，把入口关系固定为：
第一层分组：Parent ASIN / ASIN。
第二层证据：广告组 / 广告活动只解释投放容器。
上下文边界：搜索词 / 广告位只做上下文或未归因排查。

工作台首屏在商品范围区直接展示“经营商品优先入口”，不让用户只靠下拉选项猜当前分析口径。
```

#### 后续防错

```text
不要把“有 Parent ASIN 下拉选项”当成信息架构完成。
只要页面还让用户第一眼感觉在看广告组混排，就说明经营入口没有显式化。
广告组相关文案必须放在证据层或上下文层，不能作为主入口或商品归因层。
```

#### 验证方式

```text
npm run test:signal-ui
npm run test:review-ui
npm run test:snapshot-ui
python -m pytest tests\test_product_scope.py -q
npm run build

截图：
C:\Users\11010\AppData\Local\Temp\amazon-ai-signal-workbench-product-first-entry-loaded-5183-20260614.png
```

### 问题 120：信号扫描摘要不能按技术排序替代业务阅读顺序

#### 现象

```text
信号扫描摘要最初按表名排序，导致 ad_placement_daily_metrics 排在 advertised_products 前面。
用户关心的是经营商品和证据链，不是数据库表名字母顺序。
```

#### 原因

```text
技术排序容易把辅助上下文提前，让页面再次回到“广告位 / 搜索词 / 广告组混排”的理解方式。
这会削弱 AI Copilot 工作台的业务主线：先看 Parent ASIN / ASIN 商品信号，再看广告上下文证据。
```

#### 解决方案

```text
扫描摘要固定业务顺序：
广告商品 -> 搜索词 -> 广告位 -> 销售商品 -> 销售表现 -> 广告活动 -> 广告组 -> 店铺 -> 站点。
未知表再排到后面。
```

#### 后续防错

```text
面向运营解释的摘要必须按业务阅读顺序组织。
不能用数据库表名、接口返回顺序或字母顺序替代业务优先级。
```

#### 验证方式

```text
python -m pytest tests\test_signal_scan_summary.py tests\test_api_routes.py::test_signal_scan_summary_route_returns_scan_boundaries -q
npm run test:signal-scan-summary-ui
python -m pytest tests -q
npm run build
```

### 问题 121：复盘结果只有边界说明会让规则改进闭环停在记录层

#### 现象

```text
信号分诊口径已经说明 review_result 不参与分诊。
但如果页面只说明“不参与分诊”，没有说明复盘结果如何反馈给后续规则解释，运营只能看到历史记录，无法判断同类信号下次应该保留、复核还是调整规则。
```

#### 原因

```text
复盘不是独立历史备注，而是“人工处理 -> 指标变化 -> 规则改进”的反馈环。
第一性原理上，复盘结果必须回答：建议是否有效、证据是否足够、下次同类信号是否需要复核阈值 / 证据来源 / 建议动作。
```

#### 解决方案

```text
在 buildSignalTriageRationale 中增加 reviewRuleFeedback。
根据 review_result 输出规则反馈提示：
improved：保留当前规则提示，但广告动作仍需人工确认。
no_change：复核证据来源或建议动作。
worse：复核阈值、证据来源和建议动作。
unclear：先补复盘样本和指标。
无 review_result：保留当前解释口径，不调整规则。

该提示只进入解释层，不改变分诊桶，不自动改规则，不触发任何广告动作。
```

#### 后续防错

```text
复盘结果不能只作为历史状态展示。
每次新增复盘结果、复盘信号或规则调整提示时，都必须同时说明：
它是否改变分诊？
它是否只是解释层反馈？
它是否会触发自动广告动作？

默认答案应是：不改变分诊，只做解释层反馈，不自动执行广告动作。
```

#### 验证方式

```text
npm run test:signal-ui
npm run test:review-ui
npm run build
```

### 问题 122：复盘效果不能只看是否存在处理前后数据行

#### 现象

```text
build_review_effect_result 早期只判断处理前是否有数据行、处理后结束日期是否到期。
如果处理前只有 1 天数据，或处理后数据从第 4 天才开始，仍可能返回 ready。
```

#### 原因

```text
复盘效果的本质是比较人工处理前后完整 7 天 / 14 天指标。
只有零散数据行时，指标变化不能证明建议有效、无效或恶化。
```

#### 解决方案

```text
复盘效果计算增加窗口覆盖校验：
处理前窗口必须覆盖 acted_at 前完整 7 天或 14 天。
处理后窗口必须覆盖 acted_at 后完整 7 天或 14 天。
窗口不足时返回 not_ready / unclear，并明确说明是处理前还是处理后窗口不足。
```

#### 后续防错

```text
复盘效果进入 ready 前，必须先证明前后窗口完整。
不能用“有一行 before 和一行 after”代替完整窗口。
窗口不足只能提示补快照或等待数据，不允许生成 improved / no_change / worse 结论。
```

#### 验证方式

```text
python -m pytest tests\test_manual_actions.py -q
python -m pytest tests\test_api_routes.py -q
python -m pytest tests -q
```

### 问题 123：复盘窗口不足时前端不能展示部分指标对比

#### 现象

```text
后端已经能在处理前或处理后窗口不足时返回 not_ready / unclear。
但如果返回体里仍带有部分 before_metrics / after_metrics，前端原先会生成指标对比表。
运营看到对比表后，容易误以为已经具备完整 7 天 / 14 天复盘证据。
```

#### 原因

```text
指标对比表的业务含义是“处理前后窗口可比较”。
只要复盘效果不是 ready，部分指标只能作为缺口上下文，不能作为效果判断证据展示。
```

#### 解决方案

```text
reviewMetricComparisonRows 增加状态门槛：
只有 status === ready 时才展示指标对比。
not_ready / unclear 时，即使存在部分指标，也只展示后端缺口文案，不展示对比表。
```

#### 后续防错

```text
前端展示复盘指标前，必须先看复盘状态。
不能用“有 before_metrics 和 after_metrics 字段”代替完整窗口校验。
窗口不足只能提示补快照或等待数据，不允许用指标表暗示处理有效、无效或恶化。
```

#### 验证方式

```text
npm run test:review-ui
npm run test:signal-ui
npm run build
```

### 问题 124：右侧复盘状态不能只按 signal_id 缓存

#### 现象

```text
后端人工动作和复盘记录已经按 market_id + signal_id 归并。
但前端右侧人工处理、复盘待办、复盘效果、复盘记录仍按 signal_id 缓存。
如果不同店铺 / 站点复用同一个 signal_id，切换筛选时可能短暂显示上一店铺的复盘状态。
```

#### 原因

```text
多店铺工作台里，signal_id 不是足够完整的 UI 状态 key。
复盘状态带有店铺和站点上下文，前端缓存口径必须和后端归并口径一致。
```

#### 解决方案

```text
新增 signalScopedStateKey。
前端右侧状态缓存统一使用 market_id + signal_id：
- manualActionsBySignal
- reviewTodosBySignal
- reviewEffectsBySignal
- reviewRecordsBySignal

保留 signalStatusOverrideKey 作为同一 helper 的别名，避免人工状态和复盘状态使用不同口径。
```

#### 后续防错

```text
新增任何信号详情侧状态时，默认使用 market_id + signal_id。
不能只用 signal_id 存人工动作、复盘记录、运营备注或详情缓存。
后端已经拒绝 not_ready 复盘保存，但前端也不能用其他店铺的 ready 状态误导当前店铺。
```

#### 验证方式

```text
npm run test:signal-ui
npm run test:review-ui
npm run test:snapshot-ui
npm run build
python -m pytest tests\test_manual_actions.py -q
python -m pytest tests\test_api_routes.py -q
```

### 问题 125：当前真实运行数据还没有广告效果 ready 复盘组合

#### 现象

```text
复盘效果计算、保存和前端展示测试都已具备。
但当前 data/runtime 下只有 2 条人工动作，且都指向 sig-data-quality-aba-stale。
最新人工动作 object_type=cross，object_id=aba_search_term_snapshot。
当前没有 review_records.jsonl。
```

#### 原因

```text
当前成功 API 快照只有 2026-06-08 到 2026-06-14 一周数据。
当前人工动作是 2026-06-14 记录的 ABA 数据质量观察动作。
数据质量 / 交叉信号不适用广告指标前后对比。
即使后续要做广告效果复盘，也必须先有广告对象级人工动作，并在后续快照覆盖处理后完整 7 天 / 14 天窗口。
```

#### 解决方案

```text
新增只读脚本 scripts/inspect_review_readiness.py。
该脚本不写入、不保存复盘，只输出当前人工动作、复盘记录、成功快照行数和每个复盘窗口的 ready / not_ready 原因。
```

#### 后续防错

```text
不能把测试 fixture 的 ready 复盘当成当前真实运行数据已经闭环。
不能为了证明 ready 手工伪造运行态复盘记录。
真实闭环验收必须同时满足：
1. 人工动作落在广告对象或搜索词等可指标复盘对象上。
2. 成功快照覆盖 acted_at 前完整 7/14 天。
3. 成功快照覆盖 acted_at 后完整 7/14 天。
4. review-effect 返回 ready 后，再由人工保存 review-record。
```

#### 验证方式

```text
python scripts\inspect_review_readiness.py --market-id 1
python -m pytest tests\test_review_readiness_cli.py -q
```

### 问题 126：人工处理区缺少复盘适用边界

#### 现象

```text
右侧人工处理区已经有“记录观察 / 标记已处理 / 加入复盘 / 忽略本次”。
但用户看到 data_quality / cross 信号时，界面没有直接说明这类信号不适用广告指标前后对比。
这会让运营误以为所有“加入复盘”的信号都能计算 7 天 / 14 天广告效果。
```

#### 原因

```text
前端只解释了动作语义：
- 标记已处理不等于已改善
- 加入复盘不等于已有复盘结论

但没有解释对象边界：
- 广告对象级信号可以等后续快照做广告指标复盘
- 数据质量 / cross 信号只能做数据补齐或新鲜度复查
```

#### 解决方案

```text
新增 reviewApplicabilityBoundaryText。
右侧人工处理按钮后展示“复盘边界”：
- search_term / advertised_product / sales_product / placement / ad_group：
  可进入广告指标复盘，等待处理前后完整 7/14 天快照。
- data_quality / cross：
  只适合留痕或复查数据是否补齐，不进入广告指标前后对比。
```

#### 后续防错

```text
新增人工处理动作、复盘入口或对象类型时，必须同时检查复盘适用边界。
不能只因为某条信号有人工动作，就默认它能做广告指标效果归因。
数据质量信号的复盘第一性原理是检查数据是否补齐、是否更新、是否仍影响判断。
```

#### 验证方式

```text
npm run test:review-ui
npm run test:signal-ui
npm run build
```

### 问题 133：当前数据量只能支撑 MVP 骨架，不能支撑高置信深度分析

#### 现象

```text
用户指出当前数据量和分析深度不够。
只读检查显示：当前只有 1 个成功 API 快照，周期为 2026-06-08 到 2026-06-14。
可进入信号扫描的真实 API 行数为 42，广告商品 2 条，搜索词指标 20 条，广告位指标 10 条，销售商品日指标 10 条。
当前信号数为 10，复盘记录为 0，ready 复盘结果为 0。
ABA top1000 虽然存在，但周期为 2026-05-10 到 2026-05-16，距离当前分析期结束已过期 29 天。
```

#### 原因

```text
当前阶段优先跑通了 MVP 闭环骨架和对象身份稳定性，但真实数据覆盖还停留在单店铺、单站点、单周快照和少量广告对象。
缺少连续 14/30 天对比窗口，缺少更多广告商品、搜索词、广告位、关键词排名、产品语义和真实复盘反馈。
因此系统可以输出低到中置信的候选信号和数据质量提示，但不能输出高置信趋势判断、因果判断或策略效果结论。
```

#### 解决方案

```text
先做只读分析深度体检，量化当前缺口。
再按缺口补数据：优先扩展 14/30 天 API 快照，扩大 SP 搜索词、广告商品和广告位覆盖，刷新 ABA，补关键词排名或产品语义资料。
补数前不新增大批 UI 页面，不把信号数量当成业务进度，也不把 not_ready 复盘包装成效果结论。
```

#### 后续防错

```text
不能把“已有 10 条信号”误认为“分析已经足够深入”。
不能为了显得有分析深度而堆低质量信号。
不能用过期 ABA 或单周广告数据输出高置信策略结论。
任何深度分析结论都必须先说明数据窗口、样本量、证据源数量和复盘状态。
```

#### 验证方式

```text
python scripts\inspect_api_snapshot.py
python scripts\run_signal_scan.py --market-id 1
python scripts\inspect_review_readiness.py --market-id 1
后续建议补一个只读 inspect_analysis_depth.py，把上述检查统一输出。
```

### 问题 134：把深度广告分析误做成浅层指标和信号堆叠

#### 现象

```text
用户明确指出希望深度构建一套 Amazon 广告分析框架，而不是浅显地做分析。
当前系统已经能生成若干信号，但如果继续只增加信号数量、图表数量或页面展示，会变成浅层分析，不能真正指导运营决策。
```

#### 原因

```text
浅层分析通常只回答“哪个指标异常”。
深度广告分析必须进一步回答：
分析对象是谁？
异常或机会属于哪类经营问题？
证据是否足够？
是否存在反证或归因边界？
建议动作是否只能人工执行？
后续能否用 7/14 天复盘验证建议是否有效？
```

#### 解决方案

```text
把 Amazon 广告分析框架拆成对象层、问题层、证据层、判断层、建议层和复盘层。
每条信号必须落到一个清晰对象和一个清晰问题，不允许只因为某个指标波动就生成结论。
搜索词、广告位、广告组默认作为证据和上下文；只有对象边界明确时，才进入商品或经营诊断。
```

#### 后续防错

```text
不能把更多信号等同于更深分析。
不能把更多图表等同于更深分析。
不能把广告组、搜索词、广告位和 ASIN 混在同一层做归因。
不能在没有证据、反证、置信边界和复盘路径时输出高置信建议。
```

#### 验证方式

```text
后续每新增一个分析能力，必须能回答：
1. 分析对象是什么？
2. 经营问题是什么？
3. 使用哪些证据源？
4. 置信度为什么是高 / 中 / 低？
5. 建议动作是否需要人工确认？
6. 后续如何复盘？
```

### 问题 135：扩展快照窗口时只改 CLI 没改底层服务校验

#### 现象

```text
用户要求拉取近 30 天数据。
第一轮只把 inspect_snapshot_expansion_plan.py、create_api_snapshot.py、run_snapshot_pipeline.py 和 snapshot_request.py 改成支持 30 天。
真实运行 run_snapshot_pipeline.py --days 30 时仍失败：
error_message=days 只能是 7 或 14。
```

#### 原因

```text
快照窗口能力有多层入口：
1. 只读预检脚本 supported days。
2. CLI argparse choices。
3. snapshot_request 复用规则。
4. gerpgo_snapshot.create_gerpgo_snapshot 底层参数校验。

本轮第一次实现只改了前 3 层，漏掉第 4 层。
```

#### 解决方案

```text
新增 backend/tests/test_gerpgo_snapshot.py::test_create_gerpgo_snapshot_accepts_30_day_window。
该测试要求 days=30 能通过底层校验，并确认 sales / ads 请求日期窗口为 30 天。

修正 backend/app/services/gerpgo_snapshot.py：
days 允许 7 / 14 / 30。
```

#### 后续防错

```text
以后修改快照窗口、count、max_pages 这类同步参数时，必须同时检查：
- 预检脚本
- CLI 参数
- API/Pydantic 请求模型
- snapshot_request 复用逻辑
- gerpgo_snapshot 底层校验
- 文档中的真实快照方案

只改 CLI 不代表真实流水线可运行。
```

#### 验证方式

```text
$env:PYTHONPATH="backend"; python -m pytest backend\tests\test_snapshot_expansion_plan_cli.py backend\tests\test_snapshot_cli.py backend\tests\test_snapshot_pipeline_cli.py backend\tests\test_gerpgo_snapshot.py backend\tests\test_snapshot_request.py -q

python scripts\run_snapshot_pipeline.py --market-id 1 --days 30 --count 50 --max-pages 3
```
### 问题 137：只读脚本输出没有接入工作台时，运营闭环仍然断在命令行

#### 现象

```text
inspect_signal_triage.py 已经能输出 30 天快照后的候选分布、推荐对象和复盘阻塞原因。
但如果能力只停留在命令行脚本，运营仍然需要依赖开发者手动运行脚本，工作台页面无法直接展示“候选过多、推荐 B016EXMW02、暂无 ready 复盘”等关键信息。
```

#### 原因

```text
脚本适合开发和只读验收，不等于产品入口。
AI Copilot 工作台的第一性原理是帮助运营分诊和人工确认，所以关键信号摘要必须进入后端只读 API 和页面队列，而不是只留在本地命令行。
```

#### 解决方案

```text
新增 backend/app/services/signal_triage.py，将分诊构造放到后端服务层。
新增 GET /api/signal-triage，供工作台读取。
前端通过 fetchSignalTriageSummary 读取该接口，并在左侧信号队列顶部展示后端分诊摘要。
```

#### 后续防错

```text
以后新增只读诊断脚本时，必须判断它属于：
- 开发验收工具：只保留 scripts。
- 工作台必要能力：必须下沉到 backend/app/services，并由 /api 暴露。

不能让工作台长期依赖 scripts 作为运行入口。
不能把脚本跑通误判为运营可用。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py::test_signal_triage_route_returns_readonly_summary -q
npm run test:signal-ui
npm run build
浏览器打开 http://127.0.0.1:5173/，确认左侧出现“后端信号分诊摘要”。
```

### 问题 136：30 天数据后直接输出全量候选会让运营分诊失焦

#### 现象

```text
近 30 天真实快照拉取成功后，inspect_review_candidates.py 返回 135 个可人工处理候选。
其中 P0 候选 112 个，sales_product 候选 95 个。
如果直接把全量候选 JSON 给工作台或运营使用，用户需要在大量信号中自行判断优先级，容易把“信号数量”误认为“分析深度”。
```

#### 原因

```text
数据量提升后，核心问题从“有没有真实信号”变成“运营先处理哪条信号”。
原有候选脚本适合调试和完整检查，但不适合作为 AI Copilot 工作台的第一分诊出口。
深度分析必须先压缩为对象层、优先级、推荐人工确认对象和复盘阻塞原因，而不是堆更多指标或更多信号。
```

#### 解决方案

```text
新增只读脚本 scripts/inspect_signal_triage.py。
该脚本复用现有候选和复盘 readiness，不重新生成信号，不写 manual_actions，不写 review_records。
输出候选分布、推荐对象、只读人工动作预览、Top 候选、复盘状态、阻塞原因和下一步。
```

#### 后续防错

```text
以后每次扩大数据范围或新增信号类型后，必须先看分诊摘要：
- candidate_count 是否过高。
- by_object_type 是否偏离 Parent ASIN / ASIN 经营入口。
- recommended_candidate 是否使用稳定对象 ID。
- review_status.ready_count 是否为 0。

不能把更多信号直接等同于更深分析。
不能在 ready_count=0 时展示处理有效或无效。
不能跳过 manual_action_preview 直接写人工动作。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_cli.py backend\tests\test_review_candidates_cli.py backend\tests\test_review_readiness_cli.py backend\tests\test_analysis_depth_cli.py -q
python scripts\inspect_signal_triage.py --market-id 1 --top 5
```

### 问题 142：缺少 Parent ASIN 子 ASIN 只读校验会让局部快照被误读为完整事实

#### 现象

```text
用户指出 B00K4W4AAA 应有 12 个子 ASIN。
当前系统历史上只识别到 2 个，如果没有只读校验入口，容易继续把这 2 个当作完整子商品数量。
```

#### 原因

```text
当前最新成功快照 gerpgo_market_1_20260615_125325 的销售表现 raw 只有 150/209 行。
旧 normalized 销售表现文件没有 parent_asin 字段。
能命中 B00K4W4AAA 的 2 个 ASIN 只来自 raw.sales_performance.variationAsin，证据范围不完整。
```

#### 解决方案

```text
新增 backend/app/services/parent_asin_children_inspection.py。
新增 scripts/inspect_parent_asin_children.py。
校验结果必须输出 status、child_asin_count、child_asins、证据来源、raw 拉取行数和 message。
当 expected-count=12 但当前只识别到 2 个，且 raw 只拉取 150/209 行时，必须返回 partial。
```

#### 后续防错

```text
Parent ASIN 子 ASIN 数量只允许用销售表现或稳定主数据证据证明。
广告商品覆盖、广告组投放覆盖和局部快照只能写“已识别”，不能写“完整”。
请求积加 API 前必须先执行限流规则查询。
raw 销售表现分页未补齐时，所有 Parent ASIN 子 ASIN 结论都必须降级为 partial。
```

#### 验证方式

```text
$env:PYTHONPATH='.'; python -m pytest tests\test_parent_asin_children_inspection.py -q
python scripts\inspect_parent_asin_children.py --parent-asin B00K4W4AAA --expected-count 12
```

### 问题 145：广告商品缺 parent_asin 时不能靠广告组推断商品组

#### 现象

```text
B07BS9754Q 是 B00K4W4AAA 的销售表现子 ASIN，广告商品数据中也有投放记录。
但该广告商品行本身缺少 parent_asin，后端商品范围服务最初只按 parent_asin 字段统计时，把它漏出 B00K4W4AAA 的当前广告覆盖。
```

#### 原因

```text
广告商品 API 记录不一定直接带 parent_asin。
销售表现已经提供 ASIN -> Parent ASIN 关系，但商品范围服务只使用人工 Parent ASIN 导入映射补广告商品 parent_asin，没有同时使用销售表现映射。
```

#### 解决方案

```text
商品范围服务在广告商品缺少 parent_asin 时，使用销售表现里的 ASIN -> parent_asin 映射补齐。
该映射只用于广告商品 ASIN 回到 Parent ASIN，不通过广告组推断商品归属。
```

#### 后续防错

```text
广告组是投放容器，一个广告组至少包含一个广告商品，也可能包含多个广告商品。
广告组不能作为 Parent ASIN 或产品归属依据。
广告商品缺 parent_asin 时，优先用销售表现、稳定主数据或人工 Parent ASIN 映射补齐。
如果这些证据都缺失，只能保留未归因或低置信上下文。
```

#### 验证方式

```text
$env:PYTHONPATH='.'; python -m pytest tests\test_product_scope.py -q
npm run test:signal-ui
应用内浏览器检查 B00K4W4AAA 商品组概览显示 3 个当前投放广告 ASIN。
```

### 问题 143：为补齐 Parent ASIN 证据而放大全局 max_pages 会扩大广告接口请求面

#### 现象

```text
B00K4W4AAA 子 ASIN 校验需要补齐销售表现 209 行。
如果直接把全局 max_pages 从 3 放大到 5，会让广告商品、搜索词、广告位等接口也一起增加请求页数。
这不符合“只补当前证据缺口”的 MVP 原则，也会增加触发积加 API 限流的风险。
```

#### 原因

```text
原快照创建链路使用同一个 max_pages 控制销售表现和所有广告接口。
当前缺口只在 salesAnalysis/page 的销售表现分页，不在广告接口分页。
```

#### 解决方案

```text
新增 sales_max_pages。
max_pages 继续限制 1 到 3，用于广告商品、搜索词和广告位等普通接口。
sales_max_pages 单独限制 1 到 5，只用于销售表现 salesAnalysis/page。
当 sales_max_pages 大于 max_pages 时，不复用旧同周期快照，必须重新低频拉取。
manifest 写入 request_options，保留本次分页策略。
```

#### 后续防错

```text
补齐某个证据缺口时，先判断缺口属于哪个 API，不要直接扩大整套快照请求面。
请求积加 API 前必须先执行 --check-rate-limits。
销售表现补齐只能人工低频触发，不能页面打开时自动执行。
如果 Parent ASIN 证据缺口已补齐，不要继续重复 force 拉取。
```

#### 验证方式

```text
$env:PYTHONPATH='.'; python -m pytest tests\test_gerpgo_snapshot.py tests\test_snapshot_cli.py tests\test_snapshot_request.py tests\test_snapshot_pipeline_cli.py tests\test_snapshot_expansion_plan_cli.py tests\test_parent_asin_children_inspection.py -q
python scripts\run_snapshot_pipeline.py --market-id 1 --days 30 --count 50 --max-pages 3 --sales-max-pages 5 --force
python scripts\inspect_parent_asin_children.py --parent-asin B00K4W4AAA --expected-count 12
```

### 问题 144：Parent ASIN 概览只显示数量会让运营无法核对子 ASIN 证据

#### 现象

```text
B00K4W4AAA 已经通过完整销售表现快照确认有 12 个销售表现子 ASIN。
如果工作台商品组概览只显示“已识别 12 个销售表现子 ASIN”，但不列出具体 ASIN，运营仍然无法直接核对这个结论来自哪些子商品。
```

#### 原因

```text
早期商品组概览重点是区分“销售表现子 ASIN”和“当前投放广告 ASIN”两个数量。
数量口径虽然正确，但缺少可核对明细，证据透明度不足。
```

#### 解决方案

```text
商品组概览的“经营入口”行展示 Parent ASIN 下已识别的销售表现子 ASIN 明细。
广告 ASIN 仍单独在直接商品证据和广告 ASIN 对比表中展示，不与完整子 ASIN 列表混在一起。
```

#### 后续防错

```text
Parent ASIN 入口展示子 ASIN 时，必须同时展示数量和具体 ASIN。
广告 ASIN 只代表当前投放覆盖，不能替代销售表现子 ASIN 明细。
若快照不完整，只能显示“已识别”，不能写成完整数量。
```

#### 验证方式

```text
npm run test:signal-ui
$env:PYTHONPATH='.'; python -m pytest tests\test_product_scope.py -q
python scripts\inspect_parent_asin_children.py --parent-asin B00K4W4AAA --expected-count 12
```

### 问题 146：当前商品组页面不能继续展示全局后端分诊推荐

#### 现象

```text
工作台选中 parent_asin:B00K4W4AAA 时，商品组概览已经显示 12 个销售表现子 ASIN / 3 个当前投放广告 ASIN。
但后端信号分诊摘要仍显示全局 178 条信号 / 176 个候选，并推荐 B0D89X1LXC。
B0D89X1LXC 属于 Parent ASIN B0D8B7K8ZP，不属于当前 B00K4W4AAA 商品组。
```

#### 原因

```text
前端左侧队列已经用 activeProductScopeId 过滤当前商品组信号。
但 fetchSignalTriageSummary 只传 market_id/top，没有传当前 product_scope_id。
后端 build_signal_triage_payload 和 GET /api/signal-triage 也只接收 market_id/top，因此推荐候选始终按全局对象池生成。
```

#### 解决方案

```text
GET /api/signal-triage 增加 product_scope_id。
build_signal_triage_payload 和 build_review_candidates_payload 增加 product_scope_id。
候选生成前先按商品范围过滤信号：
- all 或未传参：保留全量。
- parent_asin:*：只保留命中该 Parent ASIN child_asins 的销售商品 / 广告商品 / 相关上下文信号。
- unattributed：只保留无商品范围的非数据质量信号。

前端 fetchSignalTriageSummary 增加 productScopeId。
SignalTriageWorkbench 在初次加载、activeProductScopeId 变化和人工处理后都按当前商品范围刷新后端分诊摘要。
```

#### 后续防错

```text
当前诊断入口是 Parent ASIN / ASIN 时，后端分诊摘要、推荐对象、人工预检和前端队列必须使用同一个商品范围。
全量分诊推荐只允许在 all 范围下出现，不能混入当前商品组入口。
广告组是投放容器，一个广告组至少包含一个广告商品，也可能包含多个广告商品；广告组不能作为单一 Parent ASIN 或产品归属依据。
Parent ASIN 范围匹配只能来自销售表现、广告商品 ASIN、稳定主数据或人工 Parent ASIN 映射等商品证据。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py backend\tests\test_api_routes.py -q
npm run test:signal-ui
npm run test:snapshot-ui
npm run build
python scripts\inspect_signal_triage.py --market-id 1 --top 5 --product-scope-id parent_asin:B00K4W4AAA
浏览器打开 http://127.0.0.1:5175/，确认 B00K4W4AAA 下后端分诊摘要为 15 条信号 / 15 个候选，推荐 B016EXMW02，不显示 B0D89X1LXC。
```

### 问题 147：广告组候选不能默认进入广告 ASIN 机会层

#### 现象

```text
候选分层新增后，ad_group 如果没有显式分类，会被默认兜底到 advertised_asin_opportunity。
这会把广告组结构信号误读成广告 ASIN 机会。
```

#### 原因

```text
_candidate_layer_id 先判断销售商品和策略边界，其他候选统一返回 advertised_asin_opportunity。
广告组本身是投放容器，一个广告组至少包含一个广告商品，也可能包含多个广告商品。
因此广告组不能和广告 ASIN 使用同一个业务层级。
```

#### 解决方案

```text
新增 ad_group_structure_boundary 候选层。
ad_group 候选先进入广告组结构边界层，只用于结构复核和归因边界说明。
推荐理由中增加广告组结构边界数量和“广告组只作为结构和归因边界复核”的说明。
```

#### 后续防错

```text
广告组信号可以进入人工复核，但不能默认作为广告 ASIN 机会。
商品机会必须落到 advertised_product 或 sales_product。
搜索词、广告位和广告组只能在证据链里说明上下文，不能自动推断到单个 ASIN。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py::test_review_candidate_layers_keep_ad_group_as_container_boundary -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
python scripts\inspect_signal_triage.py --market-id 1 --top 15 --product-scope-id parent_asin:B00K4W4AAA
```

### 问题 148：推荐对象只有结论会削弱运营判断

#### 现象

```text
后端分诊摘要能推荐 B016EXMW02，但早期只显示候选分层和推荐理由。
运营能看到“建议先看 B016EXMW02”，却不能立即看到该推荐来自哪些广告活动、广告组和投放行。
```

#### 原因

```text
候选 payload 只保留 object_label、summary、evidence_count 等摘要字段。
推荐候选的 evidence.source_rows 没有被压缩成适合分诊摘要阅读的证据包。
这会让工作台退化成浅层推荐，而不是证据驱动的 AI 分诊。
```

#### 解决方案

```text
为推荐候选增加 recommended_evidence_drilldown。
最小字段包括：
- 推荐对象
- 广告商品投放行数量
- 广告活动
- 广告组
- 同广告组搜索词上下文数量
- 同广告组广告位上下文数量
- 归因边界说明

前端后端分诊摘要展示 recommendedEvidenceDrilldownText。
```

#### 后续防错

```text
推荐对象必须能追到 source_rows 或明确说明证据缺口。
广告商品可以作为推荐对象；广告组只能作为上下文容器。
搜索词和广告位只说明同广告组上下文，不能自动归因到推荐 ASIN。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py::test_review_candidates_include_recommended_evidence_drilldown -q
npm run test:signal-ui
python scripts\inspect_signal_triage.py --market-id 1 --top 15 --product-scope-id parent_asin:B00K4W4AAA
curl http://127.0.0.1:8012/api/signal-triage?market_id=1&top=15&product_scope_id=parent_asin%3AB00K4W4AAA
```

### 问题 149：只看单条推荐信号会低估同 ASIN 多投放行证据

#### 现象

```text
后端推荐对象为 B016EXMW02，但 recommended_evidence_drilldown 只展示推荐候选自身的一条广告商品投放行。
真实 Parent ASIN B00K4W4AAA 范围内，同一个 B016EXMW02 可能出现在多个广告活动和广告组中。
```

#### 原因

```text
早期实现把 recommended_candidate.evidence_drilldown 原样透出。
这会让“推荐对象”停留在单条信号粒度，而不是 ASIN 经营对象粒度。
从 Amazon 广告架构看，广告组是投放容器，一个广告组至少包含一个商品，也可能包含多个商品。
因此不能按广告组归因，也不能只看单个候选行。
```

#### 解决方案

```text
recommended_evidence_drilldown 按 stable_object_id 合并同 ASIN 候选。
合并字段包括广告商品投放行数量、广告活动、广告组、搜索词上下文、广告位上下文和边界说明。
CLI 和 API 复用同一个后端 helper。
```

#### 后续防错

```text
推荐对象是 ASIN / 广告商品时，证据要优先按 ASIN 聚合。
广告组只能作为上下文容器，不得自动当作单一商品。
搜索词、广告位只说明同广告组上下文，不能自动归因到某个 ASIN。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py::test_recommended_evidence_drilldown_merges_same_asin_candidates -q
python scripts\inspect_signal_triage.py --market-id 1 --top 15 --product-scope-id parent_asin:B00K4W4AAA
http://127.0.0.1:8012/api/signal-triage?market_id=1&top=15&product_scope_id=parent_asin%3AB00K4W4AAA
```

### 问题 160：复盘 blocker 只有 no_ready 会隐藏可执行下一步

#### 现象

```text
分诊摘要能返回 no_ready_review_effect，但旧文案只说“已有人工处理记录，但当前没有 ready 的 7/14 天复盘结果”。
运营无法从摘要判断是 7 天处理后快照缺失、14 天处理前窗口不足，还是数据质量 / cross 信号不适用广告指标复盘。
```

#### 原因

```text
build_review_readiness_payload 已经有 effects 明细，包括 review_window、status、message 和 object_type。
但 _blockers 没有使用这些明细，只输出固定文案。
这让复盘闭环停在状态层，没有进入可执行诊断层。
```

#### 解决方案

```text
新增 _no_ready_review_message。
no_ready_review_effect blocker 从固定文案改为：
- 保留 no_ready 事实。
- 追加最多 3 条窗口级阻塞原因。
- 广告商品 / 销售商品 / 搜索词 / 广告位原因优先。
- 7d 先于 14d。
- 数据质量或 cross 信号原因靠后展示。

服务端和 CLI 脚本同步实现，避免 API 与只读预检口径漂移。
```

#### 后续防错

```text
只要 blocker 表示复盘未 ready，就必须尽量带出具体窗口和原因。
不能把数据质量类复盘原因排在广告商品复盘原因前面。
no_ready 只能解释阻塞和下一步，不能包装成改善、无变化或恶化结论。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_cli.py -q
python -m py_compile backend\app\services\signal_triage.py scripts\inspect_signal_triage.py
```

### 问题 159：复盘记录读回只按当前 signal_id 会丢失跨快照记录

#### 现象

```text
复盘记录保存已经改为使用 nextReviewTodo.signal_id。
但页面刷新后的复盘记录读回如果仍使用当前 selectedSignal.id，就可能读不到旧快照待办 signal_id 下保存的复盘记录。
运营会看到“已保存过”，但刷新后右侧最近复盘结果消失。
```

#### 原因

```text
复盘记录属于人工动作和复盘待办来源，不一定属于当前最新快照的 signal_id。
当前 UI 选中信号用于展示 state key，但请求复盘记录时应跟随待办 signal_id。
从 Amazon 广告架构看，广告组是投放容器，一个广告组至少包含一个广告商品，也可能包含多个广告商品。
因此不能用广告组上下文或当前选中信号替代具体待复盘对象。
```

#### 解决方案

```text
新增 buildReviewRecordReadbackTarget。
该 helper 明确拆分：
- requestSignalId：用于请求复盘记录，优先取 nextReviewTodo.signal_id。
- stateSignalId：用于当前 UI 展示，取 selectedSignal.id。

前端 loadManualActions 不再读取复盘记录，避免当前 signal_id 结果异步覆盖待办 signal_id 结果。
复盘记录改由独立 useEffect 读取并写入当前 selectedSignalStateKey。
```

#### 后续防错

```text
人工动作、复盘待办、复盘效果、复盘记录必须区分“请求对象”和“展示对象”。
跨快照场景优先按待办来源请求，按当前选中业务对象展示。
不要把广告组当成商品复盘对象，也不要把当前 selectedSignal.id 当作所有复盘数据的唯一入口。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
```

### 问题 158：复盘记录保存使用当前 signal_id 会错位跨快照待办

#### 现象

```text
右侧复盘详情已经能按旧快照待办 signal_id 读取 review-effect。
但保存复盘记录时如果仍使用当前选中信号 selectedSignal.id，跨快照同一广告 ASIN 场景下可能重新按新 signal_id 计算复盘效果。
结果可能是 ready 复盘保存失败、保存到错误信号，或保存后和待办来源不一致。
```

#### 原因

```text
复盘待办的真实来源是人工动作和它所属 signal_id，不一定等于当前页面选中的最新快照 signal_id。
从 Amazon 广告架构看，广告组只是投放容器，一个广告组至少包含一个商品，也可能包含多个商品。
因此不能用当前 UI 选中的信号或广告组上下文替代待办所属的广告商品 / 搜索词复盘对象。
```

#### 解决方案

```text
新增 reviewRecordSignalIdForTodo。
保存复盘记录时优先使用 nextReviewTodo.signal_id。
没有待办 signal_id 时，才回退 selectedSignal.id。
前端测试固定跨快照同 ASIN 场景，保证保存口径跟随待办来源。
```

#### 后续防错

```text
复盘详情读取、复盘记录保存、复盘记录读取必须使用同一对象边界。
跨快照对象优先看稳定业务对象和待办来源，不直接相信当前 selectedSignal.id。
广告组只能作为上下文，不能作为商品复盘归因对象。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_actions.py::test_review_record_persists_ready_effect_and_can_read_latest backend\tests\test_manual_actions.py::test_review_record_rejects_not_ready_effect backend\tests\test_manual_actions.py::test_review_record_route_saves_ready_effect_and_signal_reads_latest_result backend\tests\test_manual_actions.py::test_review_record_route_rejects_not_ready_effect -q
```

### 问题 156：复盘待办只显示等待状态会隐藏窗口和指标口径

#### 现象

```text
review-effect 已能返回 not_ready 原因、before_start_date / before_end_date 和 before_metrics。
但前端复盘待办区如果只显示“缺少处理后 7 天快照”，运营无法看到已知处理前窗口和处理前指标。
这会让复盘待办看起来像普通状态提示，而不是可复核的证据链。
```

#### 原因

```text
旧的 reviewMetricComparisonRows 只在 effect.status=ready 时返回指标行。
not_ready 时即使后端已经返回 before_metrics，也不会展示。
前端也没有单独展示 review-effect 的 before / after 窗口。
```

#### 解决方案

```text
新增 reviewEffectWindowText，展示处理前和处理后窗口。
reviewMetricComparisonRows 改为只要存在 before_metrics 或 after_metrics 就返回指标行。
not_ready 且 after_metrics 缺失时，处理后指标显示为 -。
保存复盘记录仍只允许 ready 状态。
```

#### 后续防错

```text
not_ready 不等于没有证据。
只要后端返回了处理前窗口和 before_metrics，前端就应该展示为复盘预览证据。
但 ready 前不能保存 review_records，不能展示改善、无变化或恶化结论。
```

#### 验证方式

```text
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
```

### 问题 155：复盘指标混入销售商品行和跨窗口聚合

#### 现象

```text
B016EXMW02 已加入 7d / 14d 复盘后，review-effect 仍是 not_ready，但 before_metrics 出现负花费和异常转化：
- cost=-7922.73
- orders=4632
- cvr=8.8736

这些指标会误导后续 ready 复盘判断。
```

#### 原因

```text
人工动作对象是 advertised_product / B016EXMW02。
旧逻辑只按 ASIN / SKU / MSKU 通用匹配，导致同 ASIN 的 sales_product_daily_metrics 行被广告商品复盘命中。
该销售商品行包含 cost=-8468.97、orders=4441，直接污染广告商品复盘指标。

旧逻辑还把 2026-05-17 至 2026-06-15 的 30 天汇总行纳入 7 天复盘 before_rows，导致跨窗口和跨快照重复聚合。
```

#### 解决方案

```text
复盘行匹配先检查对象来源兼容性：
- advertised_product 只允许 advertised_products / ad_product_daily_metrics。
- sales_product 只允许 sales_product_daily_metrics。
- search_term 只允许 ad_search_term_daily_metrics。
- placement 只允许 ad_placement_daily_metrics。

复盘 before_rows / after_rows 只允许落在目标 7 天 / 14 天窗口内部的行。
窗口不足时返回 not_ready，不用 30 天汇总行冒充 7 天或 14 天窗口。
```

#### 后续防错

```text
复盘指标必须同时满足稳定对象 ID、对象类型、来源表和时间窗口四个条件。
同 ASIN 不等于同复盘口径；销售商品指标不能自动替代广告商品指标。
广告组是投放容器，一个广告组至少包含一个广告商品，也可能包含多个广告商品，不能把广告组整体当作单一商品复盘口径。
没有完整处理后窗口时，只展示 not_ready 和窗口原因，不能输出改善 / 无效 / 恶化结论。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_actions.py::test_review_effect_for_advertised_product_ignores_sales_product_rows_with_same_asin backend\tests\test_manual_actions.py::test_review_effect_uses_rows_inside_review_window_only -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_actions.py backend\tests\test_signal_triage_service.py backend\tests\test_api_routes.py -q
http://127.0.0.1:8012/api/signals/sig-ad-product-stable-conversion-gerpgo_market_1_20260615_125325%3Aad-product%3A1%3A28792336180945/review-effect?review_window=7d&market_id=1
```

### 问题 153：商品组概览未直接提示广告组多商品边界

#### 现象

```text
商品组概览已经能显示 B00K4W4AAA 有 12 个销售表现子 ASIN、当前广告覆盖 3 个广告 ASIN。
但如果边界说明只写“广告 ASIN 只代表当前投放覆盖”，仍可能让后续开发或运营把广告组数量、广告覆盖数或广告组内出现的 ASIN 数误读成 Parent ASIN 完整子商品数量。
```

#### 原因

```text
Amazon 广告架构中，广告组是投放容器。
一个广告组至少包含一个广告商品，也可能包含多个广告商品。
广告组、搜索词、广告位都只能作为结构和上下文证据，不能反推 Parent ASIN 的完整子 ASIN 关系。
```

#### 解决方案

```text
商品组概览边界说明新增：
广告组是投放容器，一个广告组至少包含一个广告商品，也可能包含多个广告商品，不能用广告组数量或广告覆盖数量推断完整子商品数量。

frontend/tests/signalUi.test.ts 增加对应回归断言。
```

#### 后续防错

```text
Parent ASIN 完整子商品数量只来自销售表现 parent_asin/variationAsin、销售商品主数据或人工映射。
AdvertisedProduct 只说明广告投放覆盖。
AdGroup 只能说明投放容器结构，不能作为商品归因或子商品完整性依据。
```

#### 2026-06-15 复核补充

```text
用户再次校准：一个广告组至少包含一个商品，也可能包含多个商品。
复核结论：当前 product_scope 实现和回归测试已经把 Parent ASIN 完整子 ASIN 与广告覆盖 ASIN 分开。
继续防错：看到某个广告组下只投放 2 个 ASIN，只能说明该广告组当前广告覆盖了 2 个广告商品，不能推断 Parent ASIN 只有 2 个子商品。
```

#### 验证方式

```text
cd frontend; npm run test:signal-ui
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_product_scope.py backend\tests\test_parent_asin_children_inspection.py -q
$env:PYTHONPATH='backend'; python scripts\inspect_parent_asin_children.py --parent-asin B00K4W4AAA --expected-count 12
```

### 问题 154：人工留痕按 signal_id 匹配会被新快照切断

#### 现象

```text
B016EXMW02 已经在 2026-06-15 12:53 快照生成的信号上人工点击“加入复盘”。
复盘待办也已生成 7d / 14d。
但 2026-06-15 16:20 新快照重新生成了同一 B016EXMW02 的新 signal_id。
推荐摘要按当前 signal_id 查询人工记录时，误判为 has_manual_action=false、has_review_todo=false。
```

#### 原因

```text
signal_id 包含快照 ID 和行 ID，适合定位一次信号实例，不适合作为长期复盘对象唯一键。
人工留痕和复盘闭环必须落到稳定业务对象。
对广告商品来说，稳定对象是 object_type=advertised_product + object_id/ASIN + market_id。
```

#### 解决方案

```text
recommended_manual_status 保留当前 signal_id，但在当前 signal_id 未命中时，按 object_type / object_id / market_id 匹配已有人工动作、复盘待办和 review effect。
Parent ASIN 当前范围复盘队列也按 child_asins / object_id 识别旧快照待办。
不迁移旧记录，不自动写 manual_actions，不生成复盘记录。
```

#### 后续防错

```text
signal_id 只能作为当前信号实例 ID。
人工处理、复盘待办、复盘效果必须能回到稳定业务对象。
新快照不应让同一 ASIN 的人工留痕消失。
ready 前只能显示待复盘或证据不足，不能显示有效、无效或恶化结论。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py::test_signal_triage_matches_recommended_manual_status_by_stable_object -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_manual_actions.py backend\tests\test_api_routes.py -q
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
http://127.0.0.1:8012/api/signal-triage?market_id=1&top=15&product_scope_id=parent_asin%3AB00K4W4AAA
```

### 问题 150：推荐证据只有覆盖行数仍然偏浅

#### 现象

```text
ASIN 级推荐证据能说明 B016EXMW02 合并了几条同 ASIN 候选、覆盖几个广告活动和广告组。
但如果没有花费、点击、订单、销售额、ACOS、CVR 和主要花费来源，运营仍然无法判断这条推荐证据的经营强度。
```

#### 原因

```text
上一轮只完成了证据对象聚合，没有把广告商品证据行压缩成经营指标。
广告运营判断至少需要投入、产出、效率和主要贡献来源。
只展示行数会让工作台接近浅层索引，而不是 AI 分诊证据解释。
```

#### 解决方案

```text
recommended_evidence_drilldown 增加 metric_summary。
metric_summary 按 recommended_evidence_rows 计算 spend、clicks、orders、sales、acos、cvr。
新增 top_spend_campaign 和 top_spend_ad_group，说明主要花费来源。
前端推荐证据摘要展示证据行合计和主要花费来源。
```

#### 后续防错

```text
证据行合计必须标明 basis，不得默认等同于 ASIN 全量投放表现。
如果要判断 B016EXMW02 的全量广告表现，必须单独对比 raw advertised_products 中同 ASIN 的全部投放行。
广告组仍只能作为投放容器上下文，不能自动归因到单个 ASIN。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py::test_recommended_evidence_drilldown_merges_same_asin_candidates -q
npm run test:signal-ui
python scripts\inspect_signal_triage.py --market-id 1 --top 15 --product-scope-id parent_asin:B00K4W4AAA
http://127.0.0.1:8012/api/signal-triage?market_id=1&top=15&product_scope_id=parent_asin%3AB00K4W4AAA
```

### 问题 162：顶层 next_action 与推荐对象人工状态不一致

#### 现象

```text
真实分诊 payload 中，recommended_manual_status 已显示 B016EXMW02 有人工留痕和 7d / 14d 复盘待办。
但顶层 next_action 仍提示“优先让运营人工确认 B016EXMW02 的 add_to_review”。
页面如果使用顶层 next_action，会误导运营重复点击加入复盘。
```

#### 原因

```text
_triage_next_action 只读取 manual_action_preview。
manual_action_preview 只说明“推荐写入形态”，不代表当前对象是否已经人工留痕。
推荐对象真实人工状态已经在 recommended_manual_status 中计算，但顶层 next_action 没有使用它。
```

#### 解决方案

```text
build_signal_triage_payload 先计算 recommended_manual_status。
_triage_next_action / scripts.inspect_signal_triage._next_action 增加 recommended_manual_status 参数。
当推荐对象已有人工留痕时，顶层 next_action 复用 recommended_manual_status.next_action。
推荐对象尚未人工留痕时，仍提示必须由人工按钮触发，不能自动执行广告动作。
```

#### 后续防错

```text
manual_action_preview 只能表示只读写入预览，不能单独决定下一步。
顶层 next_action 必须优先服从稳定业务对象的人工状态和复盘待办状态。
同一 payload 内不能同时出现“已人工留痕等待复盘”和“优先人工确认加入复盘”两种相反提示。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_cli.py -q
python -m py_compile backend\app\services\signal_triage.py scripts\inspect_signal_triage.py
build_signal_triage_payload(selected_market_id=1, top=5, product_scope_id='parent_asin:B00K4W4AAA')
```

### 问题 161：前端只取前两个 blocker 会隐藏复盘阻塞原因

#### 现象

```text
后端 no_ready_review_effect 已经带出 7d / 14d 复盘阻塞原因。
但前端工作台原来直接渲染 signalTriageSummary.blockers.slice(0, 2)。
当候选过多、缺少人工动作预览、no_ready_review_effect 同时存在时，复盘阻塞原因可能排在第三条而不显示。
```

#### 原因

```text
前端把所有 blocker 当成同优先级短提示。
从 AI Copilot 工作台的第一性原理看，复盘阻塞是“人工确认 -> 复盘 -> 规则改进”闭环的关键状态，不应被普通候选数量提示挤掉。
```

#### 解决方案

```text
新增 signalTriageBlockerTexts。
展示时优先保留 no_ready_review_effect，再补充其他 blocker，最多展示 2 条。
SignalTriageWorkbench 使用该 helper 渲染左侧后端分诊摘要。
```

#### 后续防错

```text
任何涉及复盘、人工动作或规则改进闭环的 blocker，都不能只靠数组顺序和 slice 截断。
如果页面必须限制条数，先按业务闭环优先级选择，再限制数量。
```

#### 验证方式

```text
cd frontend; npm run test:signal-ui
cd frontend; npm run build
```

### 问题 157：右侧复盘详情只按当前 signal_id 导致旧待办丢失

#### 现象

```text
后端分诊摘要已经识别 B016EXMW02 有人工留痕和 2 条复盘待办。
但当前推荐信号来自新快照 signal_id，/api/signals/{current_signal_id}/review-todos 返回 0。
旧快照 signal_id 才能返回 B016EXMW02 的 7d / 14d 复盘待办。
页面选中当前 B016EXMW02 时，右侧详情容易显示“暂无复盘待办”，或者只显示窗口和处理前指标但不显示 not_ready 原因。
```

#### 原因

```text
人工留痕和复盘待办的长期对象应该是稳定业务对象 B016EXMW02。
前端详情区原先只使用当前 signal_id 的待办结果，没有在当前 Parent ASIN 商品范围内按 object_id 回退匹配旧快照待办。
reviewCheckpointText 在待办未到期时优先返回“到期后再判断”，即使已经拿到 review-effect 的 not_ready message，也没有展示缺失处理后快照的原因。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts 新增 selectReviewTodosForSignal。
当前 signal_id 没有直接待办时，按当前信号 primary_object 的 asin / label / object_id 到当前商品范围待办里匹配稳定对象。
SignalTriageWorkbench 复盘效果读取改用 nextReviewTodo.signal_id，而不是强行使用当前 signal_id。
reviewCheckpointText 在存在 not_ready review-effect 时展示 effect.message，即使待办尚未到期。
```

#### 后续防错

```text
signal_id 是快照内信号标识，不是长期复盘对象。
右侧人工确认和复盘展示必须优先服务稳定业务对象：广告商品 ASIN、销售 ASIN、搜索词或明确对象 ID。
not_ready 不是空状态；只要后端返回原因，就必须展示原因、窗口和已有处理前指标，避免运营误以为没有待办或已有结论。
```

#### 验证方式

```text
current_signal_id 的 review-todos 返回 0。
old_signal_id 的 review-todos 返回 B016EXMW02 的 7d / 14d 待办。
cd frontend; npm run test:review-ui
cd frontend; npm run test:signal-ui
cd frontend; npm run build
Browser DOM 验证：点击“查看建议优先留痕对象 B016EXMW02”后，右侧显示 not_ready 原因、复盘窗口、花费 19.08、订单 7、销售额 69.93，且没有“暂无复盘待办”和“复盘效果读取失败”。
```

### 问题 152：全量覆盖率只有数量仍不能解释未覆盖投放行

#### 现象

```text
all_asin_ad_coverage 已能说明 B016EXMW02 推荐证据覆盖 raw 同 ASIN 投放行 3/6。
但如果只显示“未进入推荐证据 3 条”，运营仍然不知道是哪 3 条、花费和订单是多少，也无法判断是否需要单独观察。
```

#### 原因

```text
覆盖率是口径说明，不是证据解释。
AI Copilot 工作台需要回答“哪些证据支撑推荐”和“哪些反证或背景行没有进入推荐”，否则仍会退化成浅层数字提示。
```

#### 解决方案

```text
all_asin_ad_coverage 新增 missing_ad_product_rows 和 missing_reason_summary。
missing_ad_product_rows 保留广告活动、广告组、ASIN、花费、点击、订单、销售额、ACOS、CVR 和 coverage_reason。
summary 增加未覆盖原因与未覆盖投放行摘要。
前端类型和 fallback 文案同步接入。
```

#### 后续防错

```text
只要展示“覆盖率 / 未覆盖数量”，就必须能追到未覆盖对象明细。
未覆盖行不能自动判定为异常；只能按零花费、未触发推荐证据、策略边界或其他可解释原因分类。
有订单的未覆盖行不应被直接忽略，下一轮应判断是否需要单独观察或调整推荐排序。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py::test_recommended_evidence_drilldown_merges_same_asin_candidates -q
npm run test:signal-ui
http://127.0.0.1:8012/api/signal-triage?market_id=1&top=15&product_scope_id=parent_asin%3AB00K4W4AAA
```

### 问题 151：推荐证据行合计容易被误读为 ASIN 全量广告表现

#### 现象

```text
B016EXMW02 的 recommended_evidence_rows 已经能汇总花费、点击、订单、销售额、ACOS 和 CVR。
但 raw advertised_products 中同 ASIN 还有未进入推荐证据的投放行。
如果只展示推荐证据行合计，运营可能误以为这就是 B016EXMW02 的全量广告表现。
```

#### 原因

```text
recommended_evidence_rows 是信号推荐证据包，代表触发或支撑推荐的候选行。
raw advertised_products 是同 ASIN 全量广告商品投放行，可能包含低花费、零花费或未触发推荐信号的背景行。
从 Amazon 广告架构看，广告组是投放容器，一个广告组至少包含一个广告商品，也可能包含多个广告商品；不能用广告组整体替代 ASIN 全量投放口径。
```

#### 解决方案

```text
recommended_evidence_drilldown 新增 all_asin_ad_coverage。
all_asin_ad_coverage 使用 raw advertised_products 中同 ASIN 的全部去重投放行计算：
- raw_ad_product_row_count
- recommended_ad_product_row_count
- covered_raw_ad_product_row_count
- missing_ad_product_row_count
- coverage_ratio
- raw_advertised_products 口径 metric_summary

前端摘要同时展示推荐证据行口径和 raw 全量投放覆盖口径。
```

#### 后续防错

```text
任何推荐证据指标都必须标明 basis。
recommended_evidence_rows 不能命名或展示为“ASIN 全量表现”。
判断 ASIN 全量广告表现时，必须读取 raw advertised_products 中同 ASIN 的全部投放行。
广告组只能作为结构和上下文复核对象，不能作为商品归因依据。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py::test_recommended_evidence_drilldown_merges_same_asin_candidates -q
npm run test:signal-ui
python scripts\inspect_signal_triage.py --market-id 1 --top 15 --product-scope-id parent_asin:B00K4W4AAA
http://127.0.0.1:8012/api/signal-triage?market_id=1&top=15&product_scope_id=parent_asin%3AB00K4W4AAA
```

### 问题 163：CLI 分诊预检未按稳定业务对象匹配人工状态

#### 现象

```text
后端服务 build_signal_triage_payload 已能识别推荐对象 B016EXMW02 有人工留痕和 2 条 7d / 14d 复盘待办。
但 scripts/inspect_signal_triage.py 真实运行时仍返回 recommended_manual_status.has_manual_action=false，并把顶层 next_action 写成“优先让运营人工确认 B016EXMW02 的 add_to_review”。
这会让只读脚本预检和工作台 API 对同一个推荐对象给出相反下一步。
```

#### 原因

```text
服务层 _recommended_manual_status 已经按 object_type / object_id / market_id 回退匹配稳定业务对象。
CLI 脚本仍只按当前 signal_id 查询人工动作和复盘待办。
当前推荐信号来自最新快照，而人工动作和复盘待办可能来自旧快照 signal_id；signal_id 不是长期复盘对象。
```

#### 解决方案

```text
scripts/inspect_signal_triage.py 的 _recommended_manual_status 补齐稳定对象匹配：
- 先按当前 signal_id 查直接人工动作和待办。
- 再按 object_type / object_id / market_id 查同一稳定业务对象的历史人工动作和待办。
- review effects 也支持稳定业务对象匹配。

backend/tests/test_signal_triage_cli.py 增加 CLI 回归，覆盖当前 signal_id 无记录、旧快照同 ASIN 已有人工动作和待办的场景。
```

#### 后续防错

```text
CLI 只读预检和 API 服务必须共享同一业务口径。
涉及人工动作、复盘待办和复盘效果时，不能只按 signal_id 判断状态。
长期复盘对象必须优先使用广告 ASIN、销售 ASIN、搜索词或明确稳定 object_id。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
结果：12 passed。

$env:PYTHONPATH='backend'; python -m py_compile scripts\inspect_signal_triage.py
结果：通过。

服务和 CLI 真实一致性检查：
build_signal_triage_payload(selected_market_id=1, top=5, product_scope_id='parent_asin:B00K4W4AAA')
scripts/inspect_signal_triage.py --market-id 1 --top 5 --product-scope-id parent_asin:B00K4W4AAA
结果：两者均为 has_manual_action=true、review_todo_count=2，next_action 均为等待 7 天 / 14 天完整窗口后再判断效果。
```

### 问题 164：广告组 1:N 关系仍容易被误读成商品归属

#### 现象

```text
用户再次校准：从 Amazon 广告架构看，一个广告组至少包含一个商品，也可能包含多个商品。
如果后续继续把某广告组下出现的 1 个或 2 个广告商品当成 Parent ASIN 完整子商品数量，会再次偏离经营对象入口。
```

#### 原因

```text
广告组是投放容器，广告商品是容器内实际投放对象。
Parent ASIN 完整子商品数量必须来自销售表现 parent_asin / variationAsin、销售商品主数据或人工映射，不能来自广告组数量或某个广告组当前广告覆盖。
```

#### 解决方案

```text
本轮只读复核现有实现和测试：当前代码已把销售表现子 ASIN 与当前投放广告 ASIN 分开，没有发现需要修改的业务逻辑。
```

#### 后续防错

```text
看到广告组、搜索词或广告位证据时，默认只作为结构和上下文证据。
商品级判断必须落到 AdvertisedProduct 或 SalesProduct。
Parent ASIN 完整子 ASIN 数只用销售表现或稳定主数据求证。
```

#### 验证方式

```text
$env:PYTHONPATH='backend'; python scripts\inspect_parent_asin_children.py --parent-asin B00K4W4AAA --expected-count 12
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_product_scope.py backend\tests\test_parent_asin_children_inspection.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py::test_detects_ad_group_spend_concentration_across_multiple_ad_products backend\tests\test_signal_detection.py::test_does_not_detect_ad_group_spend_concentration_for_single_ad_product -q
cd frontend; npm run test:signal-ui
```

### 问题 165：销售表现 ad_spend 字段出现负值，不能直接当广告花费口径

#### 现象

```text
近 30 天真实 API 快照 gerpgo_market_1_20260616_120443 中：
- sales_product_daily_metrics 共 209 行
- 其中 153 行 ad_spend 为负值
- min_ad_spend=-12171.41，max_ad_spend=0

同一快照的 advertised_products 共 118 行，spend 字段没有负值，范围为 0 到 122.51。
```

#### 原因

```text
当前归一化逻辑把销售表现接口 /operation/sts/salesAnalysis/page 返回的 adsSpend 直接映射为 sales_product_daily_metrics.ad_spend。
但该接口中的 adsSpend 在真实返回中大量为负，口径可能不是广告商品报表中的正向 spend。
因此不能把销售表现表的 ad_spend 直接用于广告花费、ACOS 或广告承接强弱判断。
```

#### 解决方案

```text
backend/app/services/snapshot_store.py：
- sales_product_daily_metrics 行不再把 ad_spend 写入通用 cost。
- 原始 ad_spend 仍保留在行内，作为待解释辅助字段。

backend/app/services/signal_detection.py：
- metric_snapshot 遇到 sales_product_daily_metrics 时只保留经营商品订单和销售额。
- 销售商品证据包不再计算广告花费、ACOS 或 CPC。

商品广告花费判断继续优先使用 advertised_products.spend 或广告报表口径。
salesAnalysis.adsSpend 的业务含义仍需后续查 API 字段口径，但它已经不会污染通用花费指标。
```

#### 后续防错

```text
任何基于销售商品的广告花费、ACOS、广告强弱判断，都必须标明来源表。
sales_product_daily_metrics.ad_spend 出现负值时，不得直接生成“广告花费过高”或“ACOS 异常”结论。
销售商品与广告商品关联仍按同一店铺站点下 asin / msku / sku 做，但花费口径必须来自广告商品或广告报表。
```

#### 验证方式

```text
只读统计：
data\api_snapshots\gerpgo_market_1_20260616_120443\normalized\sales_product_daily_metrics.json
data\api_snapshots\gerpgo_market_1_20260616_120443\normalized\advertised_products.json

结果：
sales_product_daily_metrics.negative_ad_spend_rows=153
advertised_products.negative_spend_rows=0

修复验证：
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_snapshot_store.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py::test_sales_product_candidate_evidence_drilldown_uses_sales_product_scope backend\tests\test_signal_triage_service.py::test_recommended_evidence_drilldown_merges_same_asin_candidates -q

真实快照 smoke：
signal_row_count=777
signal_count=179
product_ad_coverage_count=135
negative_product_evidence_cost_count=0
product_evidence_non_null_acos_count=0
```

### 问题：Parent ASIN 推荐候选只看展示名，导致销售商品 ASIN 被漏匹配
#### 现象

```text
在真实 30 天快照 gerpgo_market_1_20260616_120443 中，Parent ASIN B00K4W4AAA 下有 15 个候选：
- sales_product=9
- advertised_product=6
- P0=9

但推荐候选曾落到 P1 广告 ASIN B07BS9754Q，而不是 P0 销售强广告弱商品。
```

#### 原因

```text
推荐排序会给命中 Parent ASIN 的候选加权，但 _product_context_for_candidate 只用 object_label 匹配商品范围。
真实销售商品候选的 object_label 是商品展示名，例如“RBK004-RBK004-2 绿”，不是 ASIN。
ASIN 存在 asin 或 stable_object_id 字段中。
因此销售商品候选没有被识别为 B00K4W4AAA 子 ASIN，推荐排序被广告商品候选压过。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
_product_context_for_candidate 改为读取 asin / stable_object_id / object_label / object_id，并用大小写无关的候选标识集合匹配 product_scope.options。

backend/tests/test_signal_triage_service.py：
用 label="RBK004-RBK004-2 绿"、asin="B016EXMW1G" 复现真实销售商品形态，要求 Parent ASIN 推荐候选落到 sales_product。
```

#### 后续防错

```text
商品展示名不能当 ASIN 使用。
Parent ASIN / ASIN 经营入口的匹配必须优先读取明确 ASIN 字段。
涉及人工动作、复盘待办和推荐排序时，长期稳定对象优先使用 ASIN 或稳定 object_id，不能只依赖 signal_id 或展示文案。
```

#### 验证方式

```text
python -m pytest tests\test_signal_triage_service.py::test_review_candidates_group_parent_asin_candidates_by_decision_layer -q
结果：1 passed。

python -m pytest tests\test_signal_triage_service.py tests\test_review_candidates_cli.py -q
结果：22 passed。

python scripts\inspect_signal_triage.py --market-id 1 --top 15 --product-scope-id parent_asin:B00K4W4AAA
结果：推荐候选为 sales_product / B06VW5SQ97 / RBK004-RBK004-2 深蓝 / P0，manual_action_preview.will_write=false。
```

### 问题：前端默认 API 端口为 8010，导致页面误报后端未连接
#### 现象

```text
用户截图中 http://localhost:5175 显示“后端服务未连接”。
只读检查发现后端 8000 实际存活：
- http://127.0.0.1:8000/health 返回 ok
- http://127.0.0.1:8000/api/snapshot/status 返回 30 天真实快照 gerpgo_market_1_20260616_120443
```

#### 原因

```text
frontend/src/api.ts 默认 API_BASE 曾为 http://127.0.0.1:8010。
当前后端按项目运行方式启动在 http://127.0.0.1:8000。
当前进程可通过 VITE_API_BASE_URL 修正，但如果下次不带环境变量启动前端，会再次请求 8010。
```

#### 解决方案

```text
frontend/src/api.ts：
默认 API_BASE 改为 http://127.0.0.1:8000。

本轮同时用 VITE_API_BASE_URL=http://127.0.0.1:8000 重启了前端 5175，并确认 Vite 实际注入该地址。
```

#### 后续防错

```text
启动前端后必须确认实际注入的 VITE_API_BASE_URL。
如截图出现“后端服务未连接”，先查前端 API_BASE 与后端监听端口，不要误判为 30 天数据未拉取成功。
```

#### 验证方式

```text
http://127.0.0.1:8000/api/snapshot/status
结果：返回 snapshot_id=gerpgo_market_1_20260616_120443。

http://127.0.0.1:5175/src/api.ts
结果：VITE_API_BASE_URL=http://127.0.0.1:8000。

cd frontend; npm run test:signal-ui
结果：通过。
```

### 问题：Parent ASIN 队列把无广告匹配销售变体当成广告 AI 信号
#### 现象

```text
用户指出“当前商品组 AI 信号”直接展示 Parent ASIN B00K4W4AAA 下多个销售变体不合理。
项目是 Amazon 广告 AI 信号工作台，没有广告数据的 ASIN 不应继续作为广告信号分析对象。

只读接口复现：
http://127.0.0.1:8000/api/signal-triage?market_id=1&top=20&product_scope_id=parent_asin%3AB00K4W4AAA
- candidate_count=15
- sales_product=9
- advertised_product=6
- 推荐候选为 sales_product / B06VW5SQ97
```

#### 原因

```text
detect_product_ad_coverage_signals 只要销售商品订单达到阈值且广告订单弱，就会生成 product_ad_coverage 信号。
当 matched_ads 为空时，系统仍把完全没有广告商品匹配的销售变体作为 sales_product 信号输出。
这把“销售商品背景证据”误放进了“广告 AI 信号待处理队列”。
```

#### 解决方案

```text
backend/app/services/signal_detection.py：
detect_product_ad_coverage_signals 增加 matched_ads 门槛。
没有匹配到 advertised_products 的销售商品直接跳过，不生成 product_ad_coverage 可处理信号。
匹配到广告商品后才允许生成该信号，置信度按销售表现 + 广告商品两个证据源设为 high。

backend/tests/test_signal_detection.py：
新增 test_does_not_detect_sales_product_ad_coverage_without_ad_product_match。
```

#### 后续防错

```text
广告 AI 信号队列必须以广告事实为入口。
SalesProduct 可以作为商品组背景、销售承接和兄弟变体证据，但不能在没有广告商品匹配时成为待处理信号对象。
Parent ASIN 是范围筛选，不是把所有子 ASIN 都纳入广告分析的理由。
```

#### 验证方式

```text
cd backend
python -m pytest tests/test_signal_detection.py::test_detects_sales_product_strong_but_ad_weak_signal tests/test_signal_detection.py::test_does_not_detect_sales_product_ad_coverage_without_ad_product_match tests/test_signal_detection.py::test_detects_product_ad_coverage_data_gap_when_sales_signal_cannot_be_evaluated
结果：3 passed。

重启后端后请求 B00K4W4AAA：
- candidate_count=6
- candidate_mix.by_object_type 只剩 advertised_product=6
- recommended_candidate.object_type=advertised_product

浏览器刷新 http://localhost:5175/：
- “当前商品组 AI 信号”显示 6 条。
- 队列只展示广告商品 B016EXMVZS / B016EXMW02 / B07BS9754Q。
```

### 问题：同一广告 ASIN 因多条广告商品行重复占用信号队列
#### 现象

```text
上一轮修正后，B00K4W4AAA 队列已只剩广告商品候选，但仍显示 6 条。
只读接口显示实际是 3 个广告 ASIN 各重复两条：
- B016EXMVZS
- B016EXMW02
- B07BS9754Q

页面也显示“同对象 2 条信号”，说明队列对象仍停留在 advertised_products 行级。
```

#### 原因

```text
detect_advertised_product_opportunity_signals 逐条遍历 advertised_products。
同一 ASIN 在多个广告活动或广告组中有多条广告商品行时，会生成多条 advertised_product_opportunity。
这适合数据证据，不适合作为人工待处理队列对象。
```

#### 解决方案

```text
backend/app/services/signal_detection.py：
- aggregate_ad_product_row 按 ASIN 聚合广告商品指标。
- grouped_ad_product_context_evidence 汇总同广告组搜索词和广告位上下文。
- detect_advertised_product_opportunity_signals 改为每个 ASIN 只生成一条广告商品机会信号。
- 原始 advertised_products 行保留在 source_rows，用于证据链下钻和排查。

backend/tests/test_signal_detection.py：
- 新增 test_advertised_product_opportunity_groups_rows_by_asin。
```

#### 后续防错

```text
左侧 AI 信号队列必须承载业务处理对象，不是 API 明细行。
广告 ASIN 才是当前商品组内可人工确认和复盘的稳定对象。
advertised_products 多行只能作为证据，不应重复生成同类待处理信号。
```

#### 验证方式

```text
cd backend
python -m pytest tests/test_signal_detection.py tests/test_signal_triage_service.py
结果：48 passed。

重启后端后请求 B00K4W4AAA：
- candidate_count=3
- candidate_mix.by_object_type.advertised_product=3
- top_candidates 为 B016EXMW02 / B016EXMVZS / B07BS9754Q

浏览器刷新 http://localhost:5175/：
- “当前商品组 AI 信号”显示 3 条。
- 不再出现“同对象 2 条信号”。
```

### 问题：广告 ASIN 证据块缺少结构化口径且覆盖数与证据数可能矛盾
#### 现象

```text
广告 ASIN 对象级队列完成后，中间诊断依据仍主要依赖长 summary。
运营无法一眼区分广告商品覆盖、广告指标、主要花费来源和上下文归因边界。

浏览器检查还发现 direct 场景可能显示：
- 覆盖 raw 投放行 2/2
- 但 detail 出现覆盖率未知

接口排查 B016EXMW02 时还发现合并推荐证据可能出现：
- 证据 7 条
- 覆盖 raw 投放行 6/7
这说明证据数和覆盖数不是同一业务口径。
```

#### 原因

```text
_merge_recommended_evidence_drilldown 之前用多个候选 direct_ad_product_row_count 简单相加。
_all_asin_ad_coverage 则按 raw advertised_products 投放行键去重。
同一投放行被多个信号命中时，会被证据数重复计算，但覆盖数只算一次。

direct drilldown 没有 all_asin_ad_coverage 时，覆盖率没有从 direct_count 推导，导致 2/2 场景仍可能显示未知。
```

#### 解决方案

```text
backend/app/services/signal_triage.py：
- 广告 ASIN 详情新增 business_evidence_blocks。
- 覆盖块统一展示：覆盖 raw 投放行 x/y / 证据行 n 条。
- direct drilldown 无全量覆盖对象时，用 direct_count / direct_count 推导覆盖率。
- 合并同 ASIN 候选时，用 _ad_product_row_key 对广告商品证据去重，并用去重后的行数作为 direct_count。

backend/tests/test_signal_triage_service.py：
- 增加 direct drilldown 覆盖率 100.0% 回归测试。
- 增加重复命中场景，确保同一投放行只算一条证据。

frontend/tests/signalUi.test.ts：
- 增加广告 ASIN 业务证据块消费测试。
```

#### 后续防错

```text
所有广告 ASIN 证据统计必须先明确业务粒度。
左侧队列是广告 ASIN 对象，中间证据链是 advertised_products 投放行、搜索词上下文和广告位上下文。
证据行数、覆盖行数和指标汇总必须使用同一投放行去重口径。
搜索词和广告位只能说明同广告组上下文，不能自动归因到广告 ASIN。
```

#### 验证方式

```text
cd backend
python -m pytest tests/test_signal_triage_service.py
结果：19 passed。

python -m py_compile app/services/signal_triage.py
结果：通过。

cd frontend
npm run test:signal-ui
结果：通过。

重启后端后请求 B00K4W4AAA：
- recommended=B016EXMW02
- direct_ad_product_row_count=6
- raw_ad_product_row_count=7
- covered_raw_ad_product_row_count=6
- missing_ad_product_row_count=1
- 广告商品覆盖：覆盖 raw 投放行 6/7 / 证据行 6 条

Chrome headless 页面验证：
- 当前筛选为 Parent ASIN B00K4W4AAA。
- 诊断依据展示广告商品覆盖、广告聚合指标、主要花费来源、上下文边界。
- 不再出现覆盖率未知。
- 控制台 error/warn 数量为 0。
```

### 问题：人工动作留痕只记录动作名称，缺少当时证据快照
#### 现象

```text
右侧人工动作已经可以保存 manual_actions，并生成 7d / 14d review_todos。
但 ManualActionRequest 原来只发送 action_type、action_note、operator_name。
action_note 只是“记录观察 / 标记已处理 / 加入复盘 / 忽略本次”这类动作名称。

后续复盘时能知道运营做了什么，却不能直接回看当时依据的广告商品覆盖、广告聚合指标、主要花费来源和上下文边界。
```

#### 原因

```text
人工动作模型只覆盖了动作事实和稳定业务对象。
上一轮刚补完中间证据块，但右侧写入链路没有把这些证据块作为人工判断依据保存。
这会导致“证据解释 -> 人工确认 -> 复盘”之间缺少证据快照。
```

#### 解决方案

```text
backend/app/models/manual_actions.py：
- 新增 ManualActionEvidenceSnapshot。
- ManualActionRequest / ManualActionRecord 增加 evidence_snapshot。

backend/app/services/manual_actions.py：
- save_manual_action 支持写入 evidence_snapshot。

backend/app/api/routes.py：
- POST /api/signals/{signal_id}/manual-actions 透传 evidence_snapshot。

frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- buildManualActionEvidenceSnapshot 从当前证据块生成留痕快照。
- manualActionEvidenceSnapshotText 用于处理留痕回读展示。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- 人工按钮写入时携带当前证据块快照。
- 处理留痕区展示读回的证据快照。
```

#### 后续防错

```text
人工动作不能只保存动作名称。
凡是人工动作来自某条 AI 信号，都必须能回到当时的证据摘要。
evidence_snapshot 是复盘回看的判断依据，不是复盘结论，也不是广告执行记录。
ReviewRecord 保存的是复盘结果；ManualAction 保存的是人工动作和当时证据快照，两者不能混用。
```

#### 验证方式

```text
cd backend
python -m pytest tests/test_manual_actions.py tests/test_api_routes.py
结果：49 passed。

python -m py_compile app/models/manual_actions.py app/services/manual_actions.py app/api/routes.py
结果：通过。

cd frontend
npm run test:review-ui
结果：通过。

npm run build
结果：通过。

Chrome headless 页面验证：
- 人工确认区存在。
- 后端只读预检存在。
- 复盘状态区存在。
- 页面展示不自动执行广告动作边界。
- 控制台 error/warn 数量为 0。
```

### 问题：复盘待办没有继承人工动作证据快照
#### 现象

```text
上一轮已让 manual_actions 保存 evidence_snapshot。
处理留痕区也能显示最新人工动作的证据快照。

但 review_todos 仍只包含 action_id、action_type、对象、窗口和 due_at。
复盘待办详情可以看到何时复盘、复盘对象和指标窗口，但不能直接看到当时为什么做这个人工判断。
```

#### 原因

```text
ReviewTodo 是从 ManualActionRecord 派生的等待事项。
build_review_todos 复制了动作、对象和窗口字段，但没有复制 evidence_snapshot。
前端 review todo 类型也没有 evidence_snapshot，因此页面只能在处理留痕区回看快照，不能在待办详情中回看。
```

#### 解决方案

```text
backend/app/models/manual_actions.py：
- ReviewTodo 增加 evidence_snapshot。

backend/app/services/manual_actions.py：
- build_review_todos 从 ManualActionRecord 继承 evidence_snapshot。

frontend/src/api.ts：
- ReviewTodo 类型增加 evidence_snapshot。

frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- manualActionEvidenceSnapshotText 支持任何携带 evidence_snapshot 的对象。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- 复盘待办区展示 nextReviewTodo 的证据快照文案。
```

#### 后续防错

```text
复盘待办不是普通提醒事项。
它必须能回到人工动作和当时证据，否则“人工确认 -> 复盘”中间会断证据链。
ReviewTodo 继承 evidence_snapshot 只用于回看判断依据，不是复盘结果，也不代表广告动作执行。
旧历史动作没有 evidence_snapshot 时允许为空，但新写入动作必须携带。
```

#### 验证方式

```text
cd backend
python -m pytest tests/test_manual_actions.py tests/test_api_routes.py
结果：49 passed。

python -m py_compile app/models/manual_actions.py app/services/manual_actions.py app/api/routes.py
结果：通过。

cd frontend
npm run test:review-ui
结果：通过。

npm run test:signal-ui
结果：通过。

npm run build
结果：通过。

重启后端后只读请求 /api/review-todos?market_id=1：
- 当前历史待办 10 条。
- 旧历史待办 evidence_snapshot_count=0，兼容正常。

Chrome headless 页面验证：
- 复盘状态区存在。
- 复盘待办区存在。
- 人工确认区存在。
- 页面展示不自动执行广告动作边界。
- 控制台 error/warn 数量为 0。
```

### 问题：复盘保存前检查没有强制回看人工动作证据快照
#### 现象

```text
manual_actions 已能保存 evidence_snapshot，review_todos 也能继承该快照。
但保存 ReviewRecord 前的 preflight checklist 原先只检查复盘对象、复盘窗口、指标口径和规则反馈边界。
这意味着运营可能只看处理后指标窗口就保存复盘结论，没有被提醒回看当时人工动作依据的证据。
```

#### 原因

```text
buildReviewRecordPreflightChecklist 只消费 todo、effect 和 readbackTarget 中的对象 / 指标信息。
它没有读取 todo.evidence_snapshot，也没有区分“当时证据快照”和“处理后指标变化”。
因此复盘保存前的业务闸门缺了一环。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- ReviewRecordPreflightCheck 增加 action_evidence_snapshot。
- buildReviewRecordPreflightChecklist 在确认复盘对象后插入“回看人工动作证据”。
- 有证据快照时展示留痕证据快照。
- 没有证据快照时明确只能保存指标复盘事实，不能反推当时证据或自动调整规则。

frontend/tests/reviewUi.test.ts：
- 保存前检查清单从 4 项调整为 5 项。
- 增加证据快照检查的回归断言。
```

#### 后续防错

```text
保存 ReviewRecord 前必须同时检查三类事实：
1. 人工动作当时的证据快照。
2. 处理前后复盘指标窗口。
3. 规则反馈边界。

没有 evidence_snapshot 的旧历史待办可以保存指标事实，但不能反推当时证据。
规则反馈只能形成候选解释，不自动改规则，不自动执行广告动作。
```

#### 验证方式

```text
cd frontend
npm run test:review-ui
结果：通过。
```

### 问题：保存复盘记录后只提示已保存，缺少读回匹配校验
#### 现象

```text
保存 ReviewRecord 后，页面原先只把 POST 返回记录追加到本地状态，并提示“已保存复盘记录”。
manualActionReadbackConsistencyText 也只会显示“已有复盘记录 N 条”。
这不能证明保存后的记录确实匹配当前人工动作、当前对象、当前 7d / 14d 窗口和处理前后指标窗口。
```

#### 原因

```text
ReviewRecordForUi 原先没有暴露 before_start_date、before_end_date、after_start_date、after_end_date、before_metrics、after_metrics。
reviewRecordReadbackStatus 只统计记录数量，没有使用 action_id / object_id / review_window / metric window 做匹配判断。
页面保存后也没有重新调用 fetchSignalReviewRecords 做真实读回。
```

#### 解决方案

```text
frontend/src/pages/SignalTriageWorkbench/reviewUi.ts：
- ReviewRecordForUi 补齐复盘窗口和指标字段。
- reviewRecordReadbackStatus 支持按 action_id / object_id / review_window / 指标窗口匹配。
- buildReviewRecordReadbackExpectation 从人工动作和复盘待办生成读回期望。
- manualActionReadbackConsistencyText 支持显示匹配复盘记录状态。

frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx：
- 保存 ReviewRecord 后重新调用 fetchSignalReviewRecords。
- 保存成功提示带读回状态；读回失败时提示已保存但待读回核对。
```

#### 后续防错

```text
复盘记录保存成功不等于复盘链路闭环。
保存后必须读回，并校验：
1. action_id 匹配人工动作。
2. object_type / object_id 匹配复盘对象。
3. review_window 匹配 7d / 14d。
4. before / after 指标窗口存在。

只显示“已有 N 条复盘记录”不够，不能作为规则反馈候选的可靠依据。
```

#### 验证方式

```text
cd frontend
npm run test:review-ui
结果：通过。

npm run test:signal-ui
结果：通过。

npm run build
结果：通过。

git diff --check
结果：通过。

Chrome headless 页面只读 smoke：
复盘区、人工确认区、复盘记录边界和不自动执行广告动作边界均存在，控制台无业务 error / warn。
```

### 问题：诊断区前置信息过重导致证据链不在首屏
#### 现象

```text
v4.2 工作台在 1440x900 首屏下，中间诊断区主要展示结论、诊断主视角、对象上下文和指标条。
证据链工作区被推到首屏下方，运营第一眼看不到来源覆盖、关键证据和证据筛选摘要。
这会削弱 AI Copilot 工作台的核心价值：先看结论，再看原因和证据，再做人工确认。
```

#### 原因

```text
诊断区虽然已经有 evidenceWorkbench，但视觉顺序仍让对象上下文和指标条占据证据链之前的位置。
对象边界和指标是重要辅助信息，但不应把证据链挤出第一屏。
```

#### 解决方案

```text
frontend/src/styles.css：
- 诊断区改为可控纵向视觉顺序。
- 可视顺序调整为：结论 -> 原因 -> 证据链 -> 诊断视角 / 对象 / 指标 -> 影响范围 -> 不确定性。
- 压缩结论卡、诊断视角、对象上下文和指标条的间距。
- 将响应式两栏断点从 1240px 提高到 1320px，避免 1280 左右宽度出现横向滚动。
```

#### 后续防错

```text
AI Copilot 工作台首屏必须优先露出证据链入口。
对象上下文、指标条和辅助口径可以保留，但不能压过“结论 -> 原因 -> 证据链”的阅读顺序。
视觉升级不能改变人工动作边界：仍只允许记录观察、标记已处理、加入复盘、忽略本次。
```

#### 验证方式

```text
Chrome headless 1440x900 截图：
C:\Users\11010\AppData\Local\Temp\amazon-ai-workbench-v43c-chrome-desktop.png

对比图：
C:\Users\11010\AppData\Local\Temp\amazon-ai-workbench-v43c-reference-compare.png

Browser DOM 复查：
- 证据链 y = 662，进入 900px 首屏。
- horizontalOverflow = false。
- console error / warn = 0。

cd frontend
npm run test:signal-ui
npm run test:review-ui
npm run test:snapshot-ui
npm run test:signal-scan-summary-ui
npm run build
结果：全部通过。
```
## 2026-06-16：复盘等待摘要在聚合 API 中未透传

### 现象

```text
build_review_readiness_payload 已经生成 review_wait_summary 后，直接请求 readiness 口径可以拿到结构化等待状态。
但 /api/signal-triage 的 review_status 中没有该字段，前端规则改进门槛只能继续依赖 rule_improvement 长文案。
```

### 原因

```text
build_signal_triage_payload 对 review_status 使用字段白名单组装，只透传了 review_feedback、rule_improvement 和 next_action。
新增 readiness 字段如果没有同步加入聚合白名单，页面不会获得该字段。
```

### 解决方案

```text
backend/app/services/signal_triage.py：
- build_signal_triage_payload 的 review_status 增加 review_wait_summary。

backend/tests/test_signal_triage_service.py：
- 在聚合 payload 测试中模拟 review_wait_summary，并断言 review_status 原样透传。
```

### 后续防错

```text
凡是 build_review_readiness_payload 新增字段，如果前端需要消费，必须同步检查：
1. build_signal_triage_payload.review_status 是否透传。
2. frontend/src/api.ts 类型是否声明。
3. 前端 helper 是否有不覆盖具体对象状态的消费逻辑。
```

## 2026-06-16：PowerShell 管道中的中文路径和中文列名会误导 ABA 只读检查

### 现象

```text
使用 PowerShell here-string 把 Python 脚本管道传入时，脚本里的中文文件名或中文列名可能被破坏。
表现为 openpyxl 报错找不到 ABA 文件，或用中文列名访问 header_map 时 KeyError。
但同一个 Excel 文件实际可正常读取，输出 JSON 也能显示正确中文。
```

### 原因

```text
问题来自 PowerShell 管道脚本文本编码，不是 ABA 文件内容损坏，也不是导入逻辑失败。
文件路径和表头本身是正常的；通过 glob 匹配 ASCII 编号、或使用列位置读取，可以绕开该问题。
```

### 解决方案

```text
只读检查时：
- 用 *000049101-20260616173512*.xlsx 这类 ASCII 编号 glob 定位文件。
- 对固定 ABA 表结构，可用列位置做统计检查。
- 真正导入时复用 backend/app/services/aba_import.py，避免在临时脚本里重复写中文表头逻辑。
```

### 后续防错

```text
PowerShell 显示的中文乱码不能直接作为文件内容损坏证据。
涉及 ABA Excel 的最终判断，以 openpyxl 读取结果、导入结果和 aba_store 读取结果为准。
```

## 2026-06-16：ABA 已导入但未进入搜索词机会证据

### 问题

```text
新 ABA 报告已成功导入，aba_row_count=1000。
但原先 search_term_opportunity 只使用广告搜索词自身指标，真实扫描 source_counts 仍主要只有积加API。
这会让“广告搜索词机会”缺少市场热度第二证据源，分析深度不足。
```

### 原因

```text
aba_market_opportunity 已经接入 ABA，但只覆盖“ABA 热词有点击无订单”的机会场景。
search_term_opportunity 是在 detect_signals 主循环中直接生成，未读取 latest_aba_rows_by_market_query。
所以同一个搜索词即使存在 ABA Top1000 命中，也不会进入该信号的 data_sources 和 evidence facts。
```

### 解决方案

```text
新增 matched_latest_aba_row_for_search_term，只按 market_id / marketplace + normalized_query 匹配最新 ABA Top1000。
过滤 search_frequency_rank 大于 1000 或超过 14 天的 ABA 行。
search_term_opportunity 命中 ABA 时增加 ABA导出 data_source，并补充 ABA排名、ABA周期、ABA Top1 ASIN 证据。
```

### 后续防错

```text
新增搜索词机会规则时，必须先回答：
1. 广告侧主证据是什么？
2. 是否存在 ABA / 销售表现 / 关键词监控等独立第二证据源？
3. 第二证据源是否只是市场上下文，不能被误当成店铺广告归因？
```

## 2026-06-16：搜索词机会不能按原始广告搜索词行占用队列

### 问题

```text
同一标准化搜索词 beach essentials 在两个不同广告活动 / 广告组下分别触发 search_term_opportunity。
原逻辑会在左侧信号队列展示两条高置信机会，运营需要重复判断同一个搜索词机会。
```

### 原因

```text
search_term_opportunity 原先在 detect_signals 主循环中逐行生成。
信号 id 和 primary_object.object_id 都使用 row_id，导致 SearchTerm 对象被降级成原始数据行。
但从业务对象看，搜索词机会首先应该落到 SearchTerm；广告活动、广告组、广告商品是证据上下文。
```

### 解决方案

```text
在置信度校验前增加 aggregate_search_term_opportunity_signals。
只合并 signal_category=search_term_opportunity 且 object_type=search_term 的信号。
合并键为 market_id + normalized search term。
合并后保留所有 source_rows 和 data_sources，并补充投放上下文数、合计订单、合计花费、合计 ACOS 和合并边界。
```

### 后续防错

```text
搜索词机会类规则必须先判断对象层级：
1. SearchTerm 是机会对象。
2. campaign / ad_group 是投放上下文。
3. asin / sales_product 是承接或归因对象，不能在缺少明确关系时强行绑定。
同一 SearchTerm 跨多个上下文命中时，应合并进一条信号的证据链，而不是占用多条队列。
```

## 2026-06-16：搜索词语义汇总不能混入非搜索词对象

### 问题

```text
接入规则语义分组后，/api/search-intents 一度返回 广告商品表现、广告位表现 等标签。
这些标签来自 advertised_products / ad_placement_daily_metrics，不是广告搜索词语义组。
如果不修复，运营会把广告商品或广告位对象误读成搜索意图。
```

### 原因

```text
search_intent_summaries 原先遍历传入的所有 rows，直接按 intent_label 聚合。
/api/search-intents 传入的是最新完整 signal rows，其中包含销售商品、广告商品、广告位和搜索词。
因此非搜索词对象也进入了搜索词语义汇总。
```

### 解决方案

```text
search_intent_summaries 只接受 source_table=ad_search_term_daily_metrics 的行参与聚合。
新增 resolved_search_intent_label，只在搜索词原始 intent_label 为空或未分组时应用轻量规则。
规则语义标签只作为分析层资产，不回写原始快照。
```

### 后续防错

```text
所有 search_intent / 搜索词语义汇总逻辑必须先过滤对象类型：
1. 只处理 ad_search_term_daily_metrics。
2. 不把 advertised_products、ad_placement_daily_metrics、sales_product_daily_metrics 当成搜索意图。
3. 规则语义标签必须标明是规则生成，不得当成人工语义或模型高置信结论。
```

## 2026-06-16：搜索词语义组只停留在汇总层，未接回机会信号详情

### 问题

搜索词语义分组 MVP 已经能把广告搜索词解析成语义组，但合并后的 `search_term_opportunity` 仍只展示投放上下文、合计指标和 ABA 事实。

这会导致运营在左侧队列或右侧建议里看到“搜索词有机会”，却不知道它属于哪个用户意图组，后续容易退回到按单个搜索词机械处理。

### 原因

`aggregate_search_term_opportunity_signals` 重建合并证据时，只从原信号复制 ABA 事实，并重新生成投放上下文数、合计订单、合计花费、合计 ACOS。

单条搜索词机会虽然已有语义字段，但合并信号没有把 `resolved_search_intent_label` 重新写入证据和 `primary_object.intent_label`。

前端 `search_term_opportunity` 的关键证据排序也仍按长尾词假设优先显示搜索词、订单和 ACOS，没有把语义组作为分诊首屏证据。

### 解决

新增 `search_term_semantic_group`，只从 `ad_search_term_daily_metrics` 行解析语义组。

合并搜索词机会时：

```text
1. 写入 语义组 证据。
2. 写入 primary_object.intent_label。
3. 摘要带上语义组。
4. suggested_action 改为先按语义组复核同类搜索词表现，再人工判断是否加入精准关键词候选或小流量观察。
```

前端 `search_term_opportunity` 关键证据排序改为优先展示：

```text
语义组 -> 投放上下文数 -> 搜索词 -> 订单 -> ACOS
```

### 防错规则

搜索词语义组不能只停留在 `/api/search-intents` 汇总层。

凡是搜索词机会进入 AI 信号队列，都必须回答：

```text
1. 这个搜索词属于哪个语义组？
2. 该语义组来自规则解析、人工语义还是外部语义资产？
3. 语义组是否只用于人工复核，而不是自动加词或否词？
```

## 2026-06-16：展开明细里的长信息不能继承顶部短标签不换行规则

### 问题

```text
商品边界展开后，商品组关系摘要里的长 Parent ASIN / 子 ASIN 文本会横向铺开，和“直接商品证据 / 策略事实 / 广告上下文”等信息互相遮挡。
移动端同一区域还存在分析路径标签和广告 ASIN 表格横向溢出。
```

### 原因

```text
productScopeBar strong 为顶部短标签设置了 white-space: nowrap。
商品边界展开明细也在 productScopeBar 内，导致 productGroupRelationItem strong 继承了不换行规则。
同时 productGroupRelationMap 固定 4 列，productGroupAsinTable 设置 min-width: 620px，在窄屏下只能横向滚动。
```

### 解决方案

```text
1. 展开明细里的 productGroupRelationItem 文本单独覆盖为可换行。
2. 商品组关系摘要从固定 4 列改成 auto-fit 自适应分组。
3. 移动端分析路径允许换行，不再强制横向滚动。
4. 移动端广告 ASIN 表格改成纵向信息卡，保留广告 ASIN、花费、订单、销售额、ACOS、策略字段。
```

### 后续防错

```text
顶部短标签的 nowrap / ellipsis 规则不能直接作用到展开明细、证据卡、表格或长业务文本。
长 ASIN、搜索词、策略说明、证据解释默认要允许换行或改成移动端纵向结构。
浏览器 QA 至少检查 1440、1280 和 390 三个宽度的 overflow 与 overlap。
```

## 2026-06-16：语义组有汇总但没有复核队列，运营仍会回到单词处理

### 问题

`/api/search-intents` 已经能生成搜索词语义组，但如果前端只在具体 `search_term_opportunity` 里展示单个词，运营还是会按孤立搜索词逐条处理。

这会偏离广告 AI 信号工作台的业务本质：AI 应该先把同类意图、广告表现和 ABA 市场热度整理成可复核证据，再让人工决定下一步，而不是只给一张搜索词列表。

### 原因

语义组汇总缺少三个可操作字段：

```text
1. 语义来源：规则语义 / 人工语义 / 未分组
2. Top 搜索词：订单、花费、ACOS、来源行数
3. ABA 命中：同周期 Top1000 是否命中、排名是多少
```

前端也没有独立的语义组复核入口，因此运营无法从“语义组 -> Top 搜索词 -> 具体机会”推进。

### 解决

`SearchIntentSummary` 返回 `semantic_source`、`aba_match_count` 和 `top_search_terms`。

左侧信号队列上方新增“语义组复核”卡片，展示：

```text
语义组名称
语义来源
汇总订单 / 花费 / ACOS / ABA 命中数
Top 搜索词及 ABA 排名
```

### 防错规则

语义组不是报表维度，而是人工处理前的诊断入口。

后续新增搜索词机会时，必须能回答：

```text
1. 这个机会属于哪个语义组？
2. 这个语义组下还有哪些 Top 搜索词？
3. 是否有 ABA 同周期热度证据？
4. 人工处理入口仍然只允许记录观察、标记已处理、加入复盘、忽略本次。
```

## 2026-06-16：全局语义组卡片在商品范围下点击后不能变成空队列

### 问题

语义组复核卡片来自 `/api/search-intents`，口径是广告搜索词分析层的全局语义组。

当页面当前处于某个 Parent ASIN / ASIN 商品范围时，该商品范围内不一定有对应 `search_term_opportunity`。如果直接在当前商品范围内筛选，点击“海滩出行用品”这类全局语义组后会变成空队列。

这会让运营误以为该语义组没有机会信号，而实际全量排查范围下存在 `beach essentials` 机会。

### 原因

语义组复核卡片和左侧信号队列原本使用不同范围：

```text
语义组复核：全局广告搜索词语义组
信号队列：当前 Product Scope 下的 AI 信号
```

两者直接联动时，如果不处理范围差异，就会把“当前商品范围无命中”误展示成“该语义组无机会”。

### 解决

点击语义组卡片时：

```text
1. 先在当前商品范围内筛 search_term_opportunity。
2. 如果当前范围无命中，再在全量信号里查找同语义组搜索词机会。
3. 如果全量范围有命中，自动切到“全量排查”并选中对应信号。
4. 筛选仍只作用于 search_term_opportunity + search_term，不误收广告组、广告商品或数据质量信号。
```

### 防错规则

语义组是分析层入口，不是商品范围入口。

凡是全局语义组要联动商品范围内的队列，必须明确：

```text
当前商品范围是否有命中？
如果没有，是否需要切到全量排查？
切到全量排查后，人工处理对象是否仍落到具体 SearchTerm 信号？
是否仍禁止自动新增关键词、否词、调价或暂停？
```

## 2026-06-16：新 ABA 已导入但业务信号未消费 ABA 证据

### 问题

```text
用户补充的新 ABA 报告 ABA搜索词报表-000049101-20260616173512.xlsx 已经导入本地快照，并生成 Top1000 索引。

但 run_signal_scan 读取 aba_row_count=1000 后，当前 45 条业务信号里 data_sources 包含 ABA导出的数量为 0。
如果只看“ABA 已导入”，容易误以为市场机会证据已经进入业务分析闭环。
```

### 原因

```text
当前 ABA 消费规则主要依赖：
1. 同站点。
2. 同周期或最新同词兜底。
3. normalized_query 精确匹配。
4. 特定触发条件，例如 ABA 热词有广告点击但无订单。

本轮只读核验发现：
广告搜索词 264 个唯一词与 ABA Top1000 只有 3 个精确交集：baby、baby essentials、beach essentials。
beach essentials 的 ABA 排名为 208，但对应广告搜索词已有转化，不符合“有点击无转化”类异常。
大量长尾词如 beach essentials for toddlers 1-3 不能自动继承 beach essentials 的 ABA 排名。
```

### 解决方向

```text
不要重复导入 ABA，也不要把 ABA 做成普通报表。

下一步应把 ABA 热度作为语义组/搜索词机会的证据增强：
1. SearchTerm 仍是人工处理对象。
2. 语义组只作为分析上下文和复盘回看上下文。
3. ABA 根词命中只能生成“人工复核机会”或证据说明，不能自动加词、否词、调价或暂停。
4. 精确词匹配、语义组匹配和长尾包含关系必须分层展示，不能混成高置信结论。
```

### 防错规则

```text
以后看到 aba_row_count > 0，不能直接认为 ABA 已经支撑业务洞察。
必须继续检查：
1. 有多少信号真正包含 ABA导出 data_sources。
2. ABA 是精确搜索词证据，还是语义组/根词上下文证据。
3. 周期是否一致或是否需要降置信。
4. 运营最终能执行的人工动作和复盘对象是什么。
```

### 本轮解决

```text
已将 ABA 热词短语上下文接入搜索词机会证据链。

实现边界：
1. 精确 normalized_query 匹配仍按原规则进入 ABA 排名证据。
2. 短语上下文只允许多词 ABA 热词被广告搜索词包含，例如 beach essentials -> beach essentials for toddlers 1-3。
3. 单词 ABA 热词不做泛化，避免 baby 这类词误匹配过多搜索词。
4. 短语上下文只补充 ABA语义参考词、ABA语义参考排名、ABA周期、ABA匹配边界，不提升长尾词置信度。
5. 人工动作仍是观察、复盘或人工评估，不出现自动加词、否词、调价或暂停。
```

### 验证事实

```text
真实扫描结果：
ready_for_signals=true
aba_row_count=1000
signal_count=45
包含 ABA导出 data_sources 的信号数=3

其中 beach essentials for toddlers 1-3：
object_type=search_term
signal_category=search_term_opportunity
confidence=medium
ABA语义参考词=beach essentials
ABA语义参考排名=208
ABA匹配边界=短语包含匹配，仅作为语义组市场热度背景，不代表精确搜索词份额或本店广告归因。
```

## 2026-06-16：复盘待办只读证据快照但缺少上下文摘要

### 问题

```text
人工动作已经能把语义组、搜索词和 ABA 短语上下文写入 evidence_snapshot。

但复盘待办侧如果只原样展示前几条证据，运营仍需要自己判断：
1. 当时属于哪个语义组。
2. ABA 参考词和排名是什么。
3. 这类语义组是否重复进入复盘。
4. 系统是否只是提示人工复核，而不是自动改规则或执行广告。
```

### 原因

```text
上一轮解决的是“写入留痕”，本轮前还没有“复盘读取模型”。
如果只依赖 evidence_snapshot 展示，复盘侧缺少面向业务判断的聚合字段。
这会削弱“人工确认 -> 7/14 天复盘 -> 规则改进”的闭环。
```

### 解决

```text
新增 ReviewContext：
1. 从 evidence_snapshot 读取语义组、搜索词、ABA 参考词、ABA 排名、ABA 周期和匹配边界。
2. 在同市场人工记录中统计同一语义组 / ABA 参考词重复次数。
3. 生成 repeat_summary，提示人工判断规则是否需要调整。
4. 明确 can_auto_change_rules=false、can_auto_execute_ads=false。
5. 前端在处理留痕和复盘待办里展示复盘上下文。
```

### 防错规则

```text
以后做复盘能力时，不能只问“记录有没有写入”。
还必须检查：
1. 复盘侧是否能读回当时的对象、窗口和证据上下文。
2. 上下文是否保持 SearchTerm / 语义组 / ABA 的对象边界。
3. 重复出现是否只触发人工复核提示。
4. 是否仍禁止自动加词、否词、调价、暂停或自动改规则。
```

### 验证事实

```text
backend/tests/test_manual_actions.py 新增复盘上下文读取测试：
同一语义组已有 2 次人工留痕时，复盘待办返回 repeat_search_intent_count=2，并返回人工规则复核提示。

验证命令：
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_actions.py
结果：27 passed。

$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py
结果：24 passed。

cd frontend; npm run test:review-ui
结果：通过。
```

## 2026-06-17：复盘保存前检查只在前端展示，后端未校验请求期望

### 问题

```text
右侧复盘区已有“保存前检查”清单，能提醒用户确认复盘对象、窗口、指标口径和规则反馈边界。

但本轮前保存复盘记录时，前端请求只提交 review_note / reviewer_name。
后端只根据 URL 中的 signal_id 和 review_window 重算效果并保存，没有校验前端当前待办对应的 action_id、object_id 或 review_window。

如果前端状态错位或用户在不同对象之间快速切换，存在把复盘结果写到错误对象或错误窗口的风险。
```

### 原因

```text
上一轮重点是“复盘侧能读回上下文”，前端 checklist 已能显示保存前检查。
但 checklist 没有进入写入契约。
保存接口缺少只读期望字段，因此无法识别“用户看到的对象”和“后端即将保存的对象”是否一致。
```

### 解决

```text
保存复盘记录前，前端请求必须携带：
1. expected_action_id
2. expected_object_type
3. expected_object_id
4. expected_review_window
5. expected_can_auto_change_rules=false
6. expected_can_auto_execute_ads=false

后端保存前校验这些字段与 ReviewEffectResult 一致。
不一致时返回 409 review_record_preflight_mismatch。
请求声称允许自动改规则或自动执行广告时返回 409 review_record_forbidden_effect。
```

### 防错规则

```text
以后所有“保存复盘记录 / 规则反馈候选”相关写入，都不能只依赖前端文案提醒。

必须同时满足：
1. 前端展示用户将要保存的对象、窗口和证据。
2. 请求携带同一组期望字段。
3. 后端用当前可计算事实重新校验期望字段。
4. 写入结果能读回 action_id / object_id / review_window / 指标窗口。
5. 仍然禁止自动改规则、自动加词、否词、调价或暂停广告。
```

### 验证事实

```text
新增后端测试：
test_review_record_route_rejects_mismatched_preflight_expectation

红测事实：
对象期望为 wrong search term 时，原接口返回 200。

修复后：
同一请求返回 409，detail=review_record_preflight_mismatch，且不生成 review_records.jsonl。

验证命令：
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_actions.py
结果：28 passed。

$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py
结果：24 passed。

cd frontend; npm run test:review-ui
结果：通过。

cd frontend; npm run build
结果：通过。
```

## 2026-06-17：规则反馈候选不能脱离人工动作证据上下文

### 问题

```text
复盘记录可以说明某次人工处理后的结果是 improved / no_change / worse / unclear。
但如果规则反馈候选只按单条 review_records 列表展示，运营无法判断它属于哪个语义组、ABA 参考词或同类问题。
这样容易把规则反馈做成浅层列表，而不是可落地的人工复核入口。
```

### 原因

```text
review_records 保存的是复盘结果和指标窗口。
语义组、搜索词、ABA 参考词和 ABA 周期主要存在于 manual_actions 的 evidence_snapshot / review_context 中。
如果生成规则反馈时不回查 manual_actions，就会丢掉规则复核最关键的业务上下文。
```

### 解决

```text
生成 review_feedback 时必须用 review_record.action_id 关联 manual_actions。
规则反馈候选分组优先按“语义组”聚合，其次按“ABA语义参考词”聚合。
候选只输出人工复核建议和边界，不自动改规则，不自动执行广告动作。
```

### 防错规则

```text
以后扩展规则反馈候选时必须满足：
1. 候选来源可追溯到 review_record_id 和 action_id。
2. 候选分组必须说明语义组或 ABA 参考词来源。
3. ABA 只能作为站点级参考，不得当作店铺或商品归因。
4. 规则反馈只进入解释层和人工复核，不自动修改规则或广告动作。
```

## 2026-06-17：规则反馈候选为空不能直接解释为无问题

### 问题

```text
当前真实运行态中没有 review_records.jsonl，且 15 条可复盘人工动作的 evidence_snapshot 数量为 0。
如果前端只显示“暂无规则反馈候选”，用户可能误以为系统已经判断没有需要复核的规则问题。
```

### 原因

```text
规则反馈候选依赖两类输入：
1. review_records：说明人工处理后的结果是 improved / no_change / worse / unclear。
2. manual_actions.evidence_snapshot：说明该复盘属于哪个对象、语义组或 ABA 参考词。

当前真实数据两类输入都不完整：
没有保存复盘结论，也没有人工动作证据快照。
```

### 解决

```text
review_feedback 增加 manual_action_context_coverage。
closure_checklist 增加“复盘输入证据”检查项。
无候选时先说明输入覆盖不足，而不是只给空状态。
```

### 防错规则

```text
以后判断规则反馈候选为空时，必须同时检查：
1. review_records 是否存在。
2. review_records 是否能关联 action_id。
3. manual_actions 是否有 evidence_snapshot。
4. evidence_snapshot 是否包含语义组或 ABA 参考词。
5. 如果任一项不足，只能提示“输入不足”，不能提示“无规则问题”。
```

## 2026-06-17：人工动作证据快照不能依赖语义组筛选状态

### 问题

```text
真实 SearchTerm 机会信号已经带有语义组和 ABA 短语上下文。
但如果前端只在用户先点击语义组筛选后才生成 evidence_snapshot，运营直接处理该信号时，人工动作可能缺少语义组、ABA参考词、ABA周期和匹配边界。
```

### 原因

```text
旧前端证据快照构造依赖 selectedSearchIntentLabel。
这把“用户是否点过语义组筛选”误当成“当前信号是否具备语义组和 ABA 证据”的前提。
从业务对象看，证据已经在 signal facts 中，不能依赖某个 UI 筛选状态。
```

### 解决

```text
从当前 selectedSignal.evidence.facts 直接抽取语义组、搜索词、ABA语义参考词、ABA语义参考排名、ABA周期和 ABA匹配边界。
selectedSearchIntentLabel 只作为可选补充，不再作为生成证据快照的必要条件。
后端保存链路继续原样保存 evidence_snapshot，不自动执行任何广告动作。
```

### 防错规则

```text
以后所有人工动作证据快照必须优先来自信号自身 evidence / facts。
前端筛选、tab、卡片选中状态只能作为展示上下文，不能作为证据是否存在的唯一来源。
如果用户直接从信号队列处理 SearchTerm，仍必须保留 SearchTerm、语义组、ABA 参考词、ABA 周期和匹配边界。
```

## 2026-06-17：复盘待办读回不能只验普通 evidence_snapshot

### 问题

```text
人工动作写入后，普通 evidence_snapshot 能读回并不代表 SearchTerm + ABA 语义上下文也能读回。
如果只测广告商品覆盖这类普通证据，后续改路由或模型时可能丢掉 review_context，而测试仍然通过。
```

### 原因

```text
复盘待办有两层信息：
1. evidence_snapshot：人工动作时保存的证据快照。
2. review_context：从证据快照提取出的语义组、搜索词、ABA 参考词、ABA 周期、匹配边界和重复次数。

规则反馈和 7/14 天复盘真正依赖的是第二层业务上下文。
```

### 解决

```text
路由级测试必须覆盖：
POST /api/signals/{signal_id}/manual-actions 写入 SearchTerm 证据快照。
GET /api/signals/{signal_id}/review-todos 读回 evidence_snapshot 和 review_context。
GET /api/review-todos?market_id=1 全局待办也能读回同一上下文。
```

### 防错规则

```text
以后凡是修改 manual_actions、review_todos、review_context 或 API 路由，必须同时验证：
1. SearchTerm 对象身份没有丢。
2. 语义组没有丢。
3. ABA 参考词、排名、周期和匹配边界没有丢。
4. can_auto_change_rules 和 can_auto_execute_ads 仍为 false。
```

## 2026-06-17：复盘待办数量不标范围会误导用户

### 问题

```text
页面当前商品范围显示“复盘待办 6 条 / 到期 0 条”，但全局 API 返回 10 条待办。
如果页面不说明这是当前范围过滤后的数量，用户可能误以为全局只有 6 条复盘待办。
```

### 原因

```text
前端先读取全量 reviewTodos，再按 product scope 和 signal_id 过滤成 reviewTodosForCurrentScope。
原有提示只处理“当前范围 0 条、全局有待办”的情况；当当前范围有待办但少于全局待办时，没有展示范围差异。
```

### 解决

```text
buildReviewTodoScopeHint 增加 currentTotal > 0 且 currentTotal < globalTotal 的提示：
当前范围有 N 条复盘待办；全局共 M 条，另有 M-N 条在其他范围。
```

### 防错规则

```text
以后凡是页面同时存在“全局数据”和“当前范围过滤数据”，数量文案必须标明口径。
不能把当前范围数量当成全局事实展示；尤其是复盘待办、信号队列、人工处理记录和规则反馈候选。
```

## 2026-06-17：人工动作请求体不能只藏在页面事件函数里

### 问题

```text
handleManualAction 可以直接内联构造 createSignalManualAction 的请求体。
这样虽然当前能提交 evidence_snapshot，但测试很难直接锁住“人工动作 payload 必须携带证据快照”的合同。
后续如果页面事件函数被重构，可能漏传 evidence_snapshot，而纯证据快照测试仍然通过。
```

### 原因

```text
证据快照构造和请求体构造是两层合同：
1. buildSignalManualActionEvidenceSnapshot 证明能从信号 facts 中提取 SearchTerm / 语义组 / ABA 上下文。
2. 人工动作请求 payload 证明这些上下文真的会随 POST /manual-actions 提交。

只测试第一层不足以证明真实按钮提交不会丢证据。
```

### 解决

```text
新增 buildManualActionRequestPayload。
handleManualAction 和前端测试共用该函数。
测试必须断言 action_type、action_note、operator_name 和 evidence_snapshot 同时存在。
```

### 防错规则

```text
以后凡是修改人工动作按钮、createSignalManualAction 调用或 evidence_snapshot 字段，必须同时验证：
1. 请求 payload 中 evidence_snapshot 不为空。
2. SearchTerm、语义组、ABA 参考词、ABA 排名、ABA 周期和 ABA 匹配边界没有丢。
3. 该 payload 仍然只记录人工动作，不包含自动调价、暂停、否词或新增关键词能力。
```

## 2026-06-17：人工动作预检对象不能只停留在页面展示

### 问题

```text
页面能展示后端 manual action preflight target，但人工动作 POST payload 此前没有携带 expected_object_type / expected_object_id。
如果信号选择、范围过滤或后端重算 target 在点击前后发生变化，用户看到的预检对象和后端实际写入对象可能缺少强校验。
```

### 原因

```text
预检展示和写入提交是两层合同：
1. preflight 说明“当前准备写入哪个对象”。
2. POST /manual-actions 必须声明“用户确认写入哪个对象”。

此前只完成第一层，后端写入时只根据当前 signal.primary_object 落库，没有拿用户刚确认的 expected object 做一致性检查。
```

### 解决

```text
前端 buildManualActionRequestPayload 携带 expected_object_type / expected_object_id。
前端同时固定 expected_can_auto_change_rules=false、expected_can_auto_execute_ads=false。
后端 ManualActionRequest 接收这些字段，并在 save_manual_action 前校验 expected object 与实际落库 object 一致。
不一致时返回 409 manual_action_preflight_mismatch。
任何自动改规则或自动执行广告动作标记为 true 时，返回 409 manual_action_forbidden_effect。
```

### 防错规则

```text
以后凡是修改 manual action preflight、manual action POST、对象选择或右侧人工确认区，必须同时验证：
1. 前端 payload 携带 expected_object_type 和 expected_object_id。
2. 后端会拒绝 expected object 与实际落库 object 不一致的请求。
3. expected_can_auto_change_rules 和 expected_can_auto_execute_ads 必须保持 false。
4. 预检展示、提交 payload、落库记录三者的 object_type / object_id 必须一致。
```

## 2026-06-17：人工动作写后不能只显示已记录

### 问题

```text
人工动作写入成功后，如果页面只显示“已记录”和本地读回结果，用户仍不知道后端是否完成 post_write 复盘前置验收。
特别是复盘类动作必须确认 7d / 14d 待办已生成，ignore 必须确认当前待办为 0。
```

### 原因

```text
后端已有 expect_written=true 的只读预检能力，但前端成功路径此前没有调用。
这导致“写入前预期”和“写入后验收”断开，用户只能看到保存成功，不能确认复盘闭环是否真的具备前置条件。
```

### 解决

```text
前端新增 buildManualActionPostWritePreflightRequest。
人工动作保存成功后，用刚落库 record 的 object_type / object_id / action_type 请求 post_write 预检。
manualActionPreflightStatusText 根据 mode 区分 pre_write 和 post_write 文案。
右侧消息同时展示点击后读回结果和写后验收结果。
```

### 防错规则

```text
以后凡是修改人工动作保存成功路径，必须同时验证：
1. 保存成功后会调用 expectWritten=true 的 post_write 只读验收。
2. post_write 使用刚落库 record 的 object_type / object_id，不使用页面旧选择状态猜测。
3. 复盘类动作需要验收 7d / 14d 待办，ignore 需要验收 0 条待办。
4. 写后验收仍然只是检查 manual_actions / review_todos，不保存 review_records，不执行广告动作。
```

## 2026-06-17：post_write 读取失败不能误报人工留痕保存失败

### 问题

```text
人工动作保存成功后，如果 post_write 只读验收接口读取失败，页面原先会进入 handleManualAction 的总 catch。
用户看到的是“人工处理记录保存失败”，但实际可能是 manual_action 已经保存，只是写后验收状态没有读到。
```

### 原因

```text
createSignalManualAction、manual_actions 读回、review_todos 读取和 post_write preflight 读取被放在同一个 try/catch。
这些动作的业务含义不同：
1. createSignalManualAction 失败才代表保存失败。
2. manual_actions / review_todos 读回失败代表读回待核对。
3. post_write preflight 失败代表验收读取失败，不等于保存失败。
```

### 解决

```text
前端新增 manualActionPostWritePreflightReadErrorText。
manualActionPostWriteReadbackMessage 支持拼接 post_write 读取失败文案。
handleManualAction 在保存成功后把 fetchManualActionPreflight(expectWritten=true) 放入独立 try/catch。
post_write 读取失败时继续显示“已记录”和点击后读回结果，并提示稍后刷新核对复盘待办。
```

### 防错规则

```text
以后凡是修改人工动作保存成功路径，必须区分：
1. 保存失败：createSignalManualAction 失败。
2. 读回失败：manual_actions 或 review_todos 读取失败。
3. 写后验收读取失败：post_write preflight 读取失败。

保存成功后的任何只读验收失败，都不能覆盖“已记录”事实。
post_write 读取失败只能提示待核对，不得提示人工处理记录保存失败。
```

### 验证方式

```text
npm run test:review-ui
npm run test:snapshot-ui
npm run build
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_verifies_post_write_review_todos -q

浏览器只读检查：
页面无后端断连，当前商品组信号可见，console error / warn 为 0，没有自动广告执行按钮。
```
## 2026-06-17：广告 ASIN 证据不能只停留在触发搜索词行

### 问题

```text
B07BS9754Q 的下一未留痕候选在页面上只显示搜索词上下文 3 条。
只读核对真实 30 天快照后发现，该 ASIN 的广告商品投放行所在广告组实际有 18 个搜索词上下文，其中运行态 ABA Top1000 命中 1 条 beach essentials。
如果只展示触发证据行，用户会以为系统只识别了 3 个搜索词，分析深度不足。
```

### 原因

```text
原逻辑把候选自带 evidence_drilldown 中的 search_term_context_count 当成完整上下文。
该字段实际更接近“触发推荐证据里的搜索词行数”，不是“同广告组完整搜索词上下文”。
同时 scripts/inspect_signal_triage.py 只给 sales_product 下一候选加载完整 signal_rows，导致广告 ASIN 的只读排查脚本也会低估上下文。
```

### 解决

```text
广告 ASIN 合并证据时，按广告活动 + 广告组从完整 signal_rows 中聚合同广告组搜索词。
新增 search_term_market_context 证据块，展示同广告组搜索词数、ABA Top1000 命中数、Top 高花费词和归因边界。
CLI 收口复用后端服务证据钻取函数，避免脚本和 API 口径分叉。
```

### 防错规则

```text
以后凡是广告 ASIN 证据钻取，必须区分两类口径：
1. 触发证据行数：用于说明为什么生成候选。
2. 完整同广告组上下文：用于说明该广告 ASIN 所在投放容器周边有哪些搜索词和市场背景。

搜索词上下文不能自动归因到单个 ASIN。
ABA 不能当作店铺或 ASIN 数据，只能按站点 + 周期 + normalized_query 作为市场背景。
完整 ABA 30 万词文件不能在接口请求里每次全量读取；长尾匹配必须先做轻量索引。
```

## 2026-06-17：前端不能把触发证据行数展示成完整上下文数量

### 问题

```text
后端已经能为 B07BS9754Q 返回完整同广告组搜索词上下文 18 条和 ABA Top1000 命中 1 条。
但前端中间证据区仍使用“关键证据 3 条”这类泛化文案。
用户容易把 3 条触发证据行误读为系统只识别到 3 条搜索词，从而认为分析深度不足。
```

### 原因

```text
前端原本把优先阅读的 keyEvidenceFacts 叫作“关键证据”。
该集合本质是触发候选的优先证据行，不等于完整业务上下文。
同时 recommendedEvidenceDrilldownText 使用“证据块”和“证据行合计”，没有把触发证据和上下文证据分层表达。
```

### 解决

```text
前端统一改为：
1. “触发证据”表示生成候选的优先证据行。
2. “触发证据行合计”表示触发行的聚合指标。
3. “业务证据与上下文”表示完整投放容器、搜索词市场背景、ABA 背景和边界说明。

search_term_market_context 被提升为优先可见业务证据块。
```

### 防错规则

```text
以后凡是前端展示证据数量，必须标明口径：
1. 触发证据行：说明为什么生成信号或候选。
2. 完整上下文：说明同广告组、同商品组或站点级市场背景。
3. 完整证据：说明当前信号 facts 总数。

不能用“关键证据”这类模糊文案同时指代触发证据和完整上下文。
搜索词和 ABA 仍不能自动归因到单个广告 ASIN。
```

### 验证方式

```text
npm run test:signal-ui
npm run build

浏览器只读检查：
中间证据区显示“触发证据 3 条触发证据行”。
业务证据区显示“搜索词市场背景：同广告组搜索词 18 条 / ABA Top1000 匹配 1 条”。
console error / warn 为 0。
```

## 2026-06-17：右侧建议处理区不能缺少人工动作处理依据

### 问题

```text
右侧建议处理区已经显示建议动作、风险、不确定性、预检对象和人工按钮。
但在 B07BS9754Q 这类广告 ASIN 待处理对象上，用户点击人工动作前看不到“为什么建议这样处理”的结构化依据。
这会让建议区退化成泛化操作面板，不能支撑广告 AI 信号工作台的业务判断。
```

### 原因

```text
页面已有 selectedManualActionEvidenceSnapshot，并且人工动作 payload 会携带这份证据快照。
但右侧建议处理卡片没有消费它，只展示 selectedSignal.risk、selectedSignal.uncertainty 和 manual_action_preview。
因此“页面所见”和“后续留痕证据”之间缺少写入前的可见连接。
```

### 解决

```text
新增 manualActionEvidenceReasonText，从 evidence_snapshot 中提取写入前处理依据。
右侧建议处理卡片新增“处理依据”，优先展示广告商品覆盖、广告指标汇总、搜索词市场背景。
处理依据与人工动作写入使用同一份 selectedManualActionEvidenceSnapshot，不新增第二套解释模型。
```

### 防错规则

```text
以后凡是右侧人工动作区允许点击记录观察、标记已处理或加入复盘，点击前必须能看到处理依据。
处理依据必须来自信号证据或分诊业务上下文，不能从建议文案重新编造。
搜索词市场背景和 ABA 仍只能作为上下文，不自动归因到广告 ASIN。
```

### 验证方式

```text
npm run test:review-ui
npm run build
git diff --check

浏览器只读检查：
右侧建议处理区显示“处理依据”。
处理依据包含“广告商品覆盖”和“搜索词市场背景：同广告组搜索词 18 条 / ABA Top1000 匹配 1 条”。
console error / warn 为 0。
```

## 2026-06-17：深度检查不能用全局分数掩盖商品组覆盖和复盘缺口

### 问题

```text
inspect_analysis_depth.py 原先只输出全局 depth_score。
在当前 30 天快照中，全局数据覆盖已经较完整，但 Parent ASIN B00K4W4AAA 内部仍需要区分 12 个销售子 ASIN 和 3 个有广告商品行的 ASIN。
同时复盘层 ready_count=0 时，脚本仍可能返回 ready 并提示“选择 P0 信号做规则深化”。
这会让目标模式误判当前已经可以进入规则改进。
```

### 原因

```text
全局深度评分只能说明快照总体可分析，不等于某个 Parent ASIN 下所有子 ASIN 都有广告数据。
复盘层 gap 只参与扣分，但 ready 判定只看 depth_score 和 coverage_gaps，没有要求所有 framework_layers 都无 gap。
因此“数据覆盖足够”和“复盘闭环完成”被混成了一个状态。
```

### 解决

```text
inspect_analysis_depth.py 增加 --product-scope-id。
传入 parent_asin:B00K4W4AAA 时输出 product_scope 覆盖切片：
- child_asin_count
- sales_child_asin_count
- advertised_child_asin_count
- matched_child_asin_count
- sales_only_child_asins
- unadvertised_child_asins
- analysis_boundary

同时修正 ready 判定：
只要任一 framework layer 仍有 gap，就不能返回 ready。
复盘层 ready_count=0 时，next_action 必须优先等待复盘窗口和补复盘结果，不能提示直接做规则深化。
```

### 防错规则

```text
以后判断数据深度时必须分三层：
1. 全局快照覆盖：说明 API 和 ABA 数据量是否足够。
2. 商品组覆盖：说明 Parent ASIN 下销售 ASIN 和广告 ASIN 的命中关系。
3. 复盘 readiness：说明是否已有 ready 复盘结果支撑规则反馈。

没有广告商品行的子 ASIN 不进入广告 AI 信号队列，只能作为销售承接或广告覆盖缺口背景。
没有 ready 复盘结果时，不允许把系统状态表述为可进入规则改进。
```

### 验证方式

```text
python -m pytest backend/tests/test_analysis_depth_cli.py -q
python -m py_compile scripts/inspect_analysis_depth.py backend/tests/test_analysis_depth_cli.py
python scripts/inspect_analysis_depth.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA
git diff --check

真实输出确认：
- status=needs_depth
- depth_score=90
- B00K4W4AAA child_asin_count=12
- advertised_child_asin_count=3
- ready_count=0
```

## 2026-06-17：cross 数据质量待办不能驱动广告指标复盘等待摘要

### 问题

```text
review readiness 里同时存在 cross / ABA 数据质量待办和 advertised_product 复盘待办。
原先等待摘要直接按所有 not_ready effect 的 due_at 排序，导致最早复盘日期被 cross / ABA 待办提前到 2026-06-21。
但广告指标真正最早可复核对象是 advertised_product，日期是 2026-06-22。
```

### 原因

```text
系统把“待办事实”和“广告处理前后指标复盘窗口”混在一起排序。
cross / data_quality 可以说明数据需要补齐或复查，但它没有广告处理前后指标窗口，不能作为广告效果复盘日期。
同类等待逻辑还分散在服务层、review-readiness CLI 和 signal-triage CLI，容易出现输出不一致。
```

### 解决

```text
新增指标复盘对象集合：
- search_term
- advertised_product
- sales_product
- placement

等待摘要和 blocker 的 earliest_due_date 只从上述对象计算。
cross / ABA 数据质量待办仍保留在 effects 里，但只作为数据补齐或导入复查提示，不驱动广告指标复盘窗口。
```

### 防错规则

```text
以后所有“最早复核处理后指标”的文案，只能由指标复盘对象决定。
cross / data_quality 不得进入广告处理前后指标窗口计算。
rule_improvement、review_wait_summary、signal-triage blockers 必须使用同一等待口径。
如果三处输出日期不一致，优先视为口径错误，不要继续推进规则改进。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_triage_cli.py -q
python -m pytest backend/tests/test_review_readiness_cli.py -q
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_triage_service.py -q
python scripts\inspect_signal_triage.py --market-id 1 --top 5 --product-scope-id parent_asin:B00K4W4AAA
python scripts\inspect_review_readiness.py --market-id 1

真实输出必须满足：
- review_wait_summary.earliest_due_date=2026-06-22。
- signal-triage blocker 也显示 2026-06-22。
- cross / ABA 待办仍保留在 effects 中，但不驱动广告指标复盘等待摘要。
```

## 2026-06-17：写前人工动作预检必须显示不保存复盘结论

### 问题

```text
B07BS9754Q 的人工动作预检已经返回 ready_for_explicit_manual_write、will_write=false，并且 forbidden_effects 包含“不保存 review_records”。
但页面写前预检文案只显示“不执行广告动作”，没有直接显示“不保存复盘结论”。
这会让运营把“加入复盘”误读成点击后马上产生 ReviewRecord 或处理效果结论。
```

### 原因

```text
manualActionPreflightStatusText 原先只在 post_write 模式下把“不保存 review_records”转成“不保存复盘结论”。
pre_write 模式虽然展示了预计留痕和待办数量，但没有展示 ReviewRecord 边界。
同时“写后预期”主要藏在 tooltip title 中，首屏可见性不够稳定。
```

### 解决

```text
manualActionPreflightStatusText 在 pre_write 和 post_write 两种模式下统一读取 forbidden_effects。
只要后端返回“不保存 review_records”，前端预检文案就显示“不保存复盘结论”。
右侧人工动作区把写后预期汇总改为可见文本：复盘类 -> 7d / 14d；忽略 -> 0 条；不保存结论。
```

### 防错规则

```text
凡是页面允许人工点击记录观察、标记已处理、加入复盘或忽略本次，写前必须同时说明：
1. 是否写 manual_actions。
2. 是否生成 7d / 14d review_todos。
3. 不自动执行广告动作。
4. 不保存 ReviewRecord / 不保存复盘结论。

“加入复盘”只能表示进入复盘待办，不能暗示已经有处理改善、无变化或恶化结论。
```

### 验证方式

```text
npm run test:review-ui
npm run build

浏览器只读检查：
- B07BS9754Q 可见。
- 后端预检通过可见。
- 预检文案包含“不执行广告动作”和“不保存复盘结论”。
- 写后预期可见：“复盘类 -> 7d / 14d；忽略 -> 0 条；不保存结论。”
- console error / warn 为空。
```

## 2026-06-17：广告 ASIN 证据快照不能截断上下文边界

### 问题

```text
B07BS9754Q 这类广告 ASIN 候选需要同时展示广告商品覆盖、广告聚合指标、主要花费来源、搜索词市场背景和上下文边界。
前端业务证据列表已经能保留 context_boundary，但人工动作 payload 证据快照默认只保留 4 条。
当搜索词市场背景存在时，第 5 条“上下文边界”会从 evidence_snapshot 中被截断。
```

### 原因

```text
业务展示层和人工动作快照层使用了两个独立截断上限。
signalTriageBusinessEvidenceItems 已经把 context_boundary 当成必须保留项处理。
buildManualActionEvidenceSnapshot 仍沿用通用 4 条默认上限，没有意识到广告 ASIN 的边界说明是复盘必要证据。
```

### 解决

```text
业务证据可见项继续优先显示前 4 条核心证据，并在必要时额外保留 context_boundary。
人工动作证据快照默认上限调整为 5。
测试覆盖 B07 广告 ASIN 场景，要求 evidence_snapshot 同时保留：
1. 广告商品覆盖。
2. 广告聚合指标。
3. 主要花费来源。
4. 搜索词市场背景。
5. 上下文边界。
```

### 防错规则

```text
凡是人工动作 evidence_snapshot 用于后续 7/14 天复盘，就不能只按固定数量截断指标证据。
边界证据必须和指标证据一起保存。
广告 ASIN 的搜索词、ABA、广告位上下文只能作为证据背景或不确定性说明，不能被截断到后续复盘不可见。
以后新增证据块时，必须检查“展示层可见”和“payload 快照可保存”两层合同。
```

### 验证方式

```text
npm run test:signal-ui
npm run test:review-ui

断言重点：
- B07 广告 ASIN 业务证据可见项包含“上下文边界”。
- B07 广告 ASIN 人工动作 evidence_snapshot 长度为 5。
- evidence_snapshot 第 5 条为“上下文边界”，detail 包含“不能自动归因”。
```

## 2026-06-17：处理依据不能只展示指标而漏掉上下文边界

### 问题

```text
B07BS9754Q 的人工动作证据快照已经包含“上下文边界”。
但右侧人工确认区的“处理依据”由 manualActionEvidenceReasonText 生成，默认只取 3 条高优先级证据。
结果点击前依据文案只显示广告商品覆盖、搜索词市场背景、广告聚合指标，没有显示“搜索词和广告位不能自动归因到该广告 ASIN”。
```

### 原因

```text
证据快照保存合同和处理依据展示合同是两层逻辑。
上一轮只保证 evidence_snapshot 不截断上下文边界，但没有同步检查“点击前运营最先看到的处理依据”是否也承接边界证据。
manualActionEvidenceReasonPriority 也没有把“广告聚合指标”和“上下文边界”列入明确优先级。
```

### 解决

```text
manualActionEvidenceReasonText 继续默认只展示 3 条核心依据。
新增“广告聚合指标”和“上下文边界”的排序优先级。
当 evidence_snapshot 存在“上下文边界”时，额外追加该条边界，不把“主要花费来源”塞进摘要。
```

### 防错规则

```text
广告 ASIN 人工动作点击前的处理依据必须同时覆盖：
1. 广告商品覆盖或广告指标。
2. 搜索词 / ABA 市场背景。
3. 归因边界或上下文边界。

证据快照保存了边界，不代表运营点击前已经看见边界。
以后新增 evidence_snapshot 关键块时，必须检查：
- 证据链是否可见。
- 人工动作 payload 是否保存。
- 右侧处理依据是否承接。
- 复盘待办是否能读回。
```

### 验证方式

```text
npm run test:review-ui
npm run test:signal-ui
npm run build

断言重点：
- B07 广告 ASIN 处理依据包含“上下文边界：搜索词 18 条 / 广告位 0 条”。
- B07 广告 ASIN 处理依据不包含“主要花费来源”，避免摘要过载。
```

## 2026-06-17：复盘类人工动作不能空证据写入

### 问题

```text
前端已经会构造 evidence_snapshot，但后端 ManualActionRequest 默认允许 evidence_snapshot 为空。
POST /api/signals/{signal_id}/manual-actions 旧逻辑只校验禁止自动改规则、禁止自动执行广告动作和 expected object。
如果有人绕过前端直接请求 API，observe / handled / add_to_review 仍可能写成空证据人工动作，并派生 7d / 14d ReviewTodo。
```

### 原因

```text
证据快照最初是前端和测试层逐步补上的合同。
后端保存路由没有把“复盘类动作必须有点击时证据”作为写入边界。
这会让 ManualAction -> ReviewTodo 的继承链条形式上成立，但复盘时没有当时证据可回看。
```

### 解决

```text
POST /api/signals/{signal_id}/manual-actions 增加写入门槛：
- observe / handled / add_to_review 必须带非空 evidence_snapshot。
- 缺失时返回 409 / manual_action_missing_evidence_snapshot。
- ignore 不生成当前复盘待办，本轮不强制证据快照。

测试同步更新：
- 空证据 add_to_review 必须拒绝。
- B07BS9754Q 的 5 条 evidence_snapshot 必须从 action 读回到 ReviewTodo。
- 旧路由测试不再用空证据模拟复盘类动作。
```

### 防错规则

```text
凡是会生成 7d / 14d ReviewTodo 的人工动作，必须有 evidence_snapshot。
前端生成证据不等于后端安全；写入接口也必须校验。
后续任何新增人工动作入口都必须同时验证：
1. evidence_snapshot 非空。
2. object_type / object_id 与预检一致。
3. 不自动执行广告动作。
4. 不保存 ReviewRecord。
5. ReviewTodo 能继承证据快照。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_api_routes.py -q
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_actions.py -q -k "api or route or manual_action"

断言重点：
- 空 evidence_snapshot 的 add_to_review 返回 409。
- B07BS9754Q 的 add_to_review 返回 5 条 evidence_snapshot。
- B07BS9754Q 的 7d / 14d ReviewTodo 都能读回“上下文边界”。
```

## 2026-06-17：历史复盘待办缺证据不能被误读为新写入失败

### 问题

```text
当前真实运行态 /api/review-todos?market_id=1 返回 10 条复盘待办。
只读核对发现 10 条都缺 evidence_snapshot，且 0 条带 review_context。
如果页面只显示复盘待办数量，不说明证据状态，运营和开发者容易误判为“新后端证据门槛没有生效”。
```

### 原因

```text
这些待办来自历史旧人工留痕。
旧 ManualAction 创建时还没有强制 evidence_snapshot 合同，所以派生出的 ReviewTodo 缺少点击时证据快照。
上一轮新增的后端门槛只约束新写入的 observe / handled / add_to_review，不会自动补齐历史数据。
```

### 解决

```text
前端复盘队列摘要增加证据状态说明：
- 存在缺口时显示“当前 X/Y 条待办来自历史旧留痕缺少证据快照；新人工动作已要求保存证据快照。”
- 不存在缺口时显示“当前待办已带点击时证据快照。”

测试补充历史旧待办缺 evidence_snapshot 的断言，避免后续把这类提示删掉。
```

### 防错规则

```text
历史 ReviewTodo 缺 evidence_snapshot，不等于新写入合同失败。
不能为了让队列看起来完整而伪造或回填历史证据。
缺证据待办只能做指标事实复盘，不能反推当时处理依据，也不能自动生成规则结论。
判断新合同是否有效，必须用新 POST 写入或后端测试验证 409 / manual_action_missing_evidence_snapshot。
```

### 验证方式

```text
npm run test:review-ui
npm run test:signal-ui
npm run build

只读 API 覆盖核对：
/api/review-todos?market_id=1 返回 10 条待办，with_evidence_snapshot=0，missing_evidence_snapshot=10，with_review_context=0，is_due_count=0。

Browser 只读核对：
http://localhost:5175/ 页面可见“历史旧留痕缺少证据快照”和“新人工动作已要求保存证据快照”，console error / warn 为 0。
```

## 2026-06-17：分析深度下一步不能把复盘未到期误写成补数据覆盖

### 问题

```text
真实运行态中 Parent ASIN B00K4W4AAA 的 30 天快照和商品范围覆盖已经足够：
depth_score=90，coverage_gaps=[]，对象层、问题层、证据层、判断层、建议层均为 ok。
唯一缺口是复盘层：ready_count=0，review_record_count=0，最早复盘窗口到 2026-06-22。

但 inspect_analysis_depth.py 的 next_action 仍输出“优先补数据覆盖和复盘窗口”，容易让用户误以为当前主要任务是继续扩数据，而不是等待复盘窗口。
```

### 原因

```text
旧 _next_action 只看 review_layer.ready_count。
只要 ready_count=0，就返回固定补数据文案，没有读取 inspect_review_readiness.py 已经计算出的 review_wait_summary。
这把“覆盖不足”和“复盘窗口未到期”混成了同一个建议。
```

### 解决

```text
build_analysis_depth_payload 将 review_payload 传给 _next_action。
当 coverage_gaps 为空且 review_wait_summary.status=waiting_review_window 时，next_action 改为输出：
- 当前数据覆盖已满足基础深度。
- 瓶颈是复盘窗口未到期。
- 窗口、最早 due_date、下一对象。
- 不拉取快照、不保存复盘结论、不自动改规则、不自动执行广告动作。

覆盖不足场景仍保留补数据建议。
```

### 防错规则

```text
深度检查的 next_action 必须先区分：
1. 数据覆盖不足。
2. 证据 / 判断 / 建议层不足。
3. 复盘窗口未到期。
4. ready 复盘存在但未保存 ReviewRecord。

当前覆盖足够但复盘未到期时，不得提示“补数据覆盖”作为主动作。
未到期前不得提示拉快照、保存复盘结论、自动改规则或自动执行广告动作。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_analysis_depth_cli.py -q

只读脚本核对：
$env:PYTHONPATH='backend'; python scripts\inspect_review_readiness.py --market-id 1 --compact
$env:PYTHONPATH='backend'; python scripts\inspect_analysis_depth.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA

断言重点：
- review_readiness.status=not_ready。
- rule_improvement.status=waiting_review_window。
- next_action 包含 2026-06-22、B016EXMVZS、不保存复盘结论。
- next_action 不包含“补数据覆盖”。
```

## 2026-06-17：源码口径正确但旧后端进程会让页面继续显示旧复盘日期

### 问题

```text
源码直接调用 app.services.signal_triage.build_signal_triage_payload 已经返回正确广告指标复盘口径：
earliest_due_date=2026-06-22，next_object_type=advertised_product，next_object_id=B016EXMVZS。

但 http://127.0.0.1:8000/api/signal-triage?market_id=1 首次仍返回：
earliest_due_date=2026-06-21，next_object_type=cross，next_object_id=aba_search_term_snapshot。

如果只看页面，会误以为前端或服务层仍未修复 cross 待办污染广告指标复盘日期。
```

### 原因

```text
8000 端口运行的是旧 uvicorn 进程。
源码已修复并且单元测试已覆盖，但旧服务进程没有重启，导致浏览器页面继续消费旧 API 口径。
```

### 解决

```text
重启 8000 端口后端服务。
重启后 /api/signal-triage?market_id=1 返回 2026-06-22 / advertised_product / B016EXMVZS。
浏览器刷新后，右侧“规则改进门槛”显示 2026-06-22、B016EXMVZS、不拉取快照、不保存复盘结论、不自动改规则、不自动执行广告动作。
```

### 防错规则

```text
当源码直接调用结果、测试结果和页面结果不一致时，先检查本地后端进程是否 stale。
必须同时核对三层：
1. 源码直接调用服务函数。
2. 运行中 API 响应。
3. 浏览器页面 DOM。

服务重启后仍不一致，才进入代码修复；不要在旧进程导致的问题上盲目改前端或业务逻辑。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_signal_triage_service.py -q -k "review_wait_summary_ignores_cross_todos_for_metric_window or review_readiness_next_action_waits_when_review_effects_are_not_due"

源码直接调用：
app.services.signal_triage.build_signal_triage_payload(selected_market_id=1, top=5)

运行中 API：
GET /api/signal-triage?market_id=1

Browser：
刷新 http://localhost:5175/，检查右侧“规则改进门槛”。
```

### 补充防错入口

```text
已新增只读 smoke：
$env:PYTHONPATH='backend'; python scripts\smoke_review_wait_contract.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --api-url "http://127.0.0.1:8000/api/signal-triage?market_id=1&top=5&product_scope_id=parent_asin%3AB00K4W4AAA"

用途：
同时比较 review_readiness、signal_triage_service 和运行中 API 的 status、earliest_due_date、next_object_type、next_object_id、forbidden_actions。
如果输出 mismatch，先检查后端进程是否 stale 或 API 是否走了旧代码，再决定是否改业务代码。
```

## 2026-06-17：人工动作预检脚本和后端服务不能保留两套实现

### 问题

```text
scripts/inspect_manual_action_preflight.py 曾经复制了一份 backend/app/services/manual_action_preflight.py 的预检逻辑。

这会带来两个风险：
1. CLI、API、页面读到的预检口径可能逐步漂移。
2. 新增 evidence_snapshot_preview 这类合同字段时，容易只改服务或只改脚本，导致验收命令和页面 API 不一致。
```

### 原因

```text
早期为了快速跑通 CLI，只读脚本里保留了完整业务逻辑。
后续 API 也需要同一口径时，逻辑没有及时收口到 service 层。
```

### 解决

```text
scripts/inspect_manual_action_preflight.py 改为调用 backend/app/services/manual_action_preflight.py。
脚本只保留参数解析、JSON 输出和测试可替换依赖 wrapper。
核心人工动作预检逻辑只维护一份。
```

### 防错规则

```text
后续新增或修改人工动作预检字段时，只改 backend/app/services/manual_action_preflight.py。
CLI 脚本不得重新复制业务判断。
脚本 wrapper 传 action_root / review_root 时，只有非空路径才透传；不能把 None 当成自定义路径。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_action_preflight_cli.py -q
python -m py_compile scripts\inspect_manual_action_preflight.py backend\app\services\manual_action_preflight.py
$env:PYTHONPATH='backend'; python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id B07BS9754Q --expected-object-type advertised_product --action-type add_to_review
```

## 2026-06-17：右侧预检展示证据和点击 payload 不能两套来源

### 问题

```text
右侧“后端只读预检”已经展示 B07BS9754Q 将保存 5 条 evidence_snapshot_preview。
但前端点击人工动作时，buildManualActionRequestPayload 仍优先使用 selectedManualActionEvidenceSnapshot。
这会造成用户看到的是后端预检的 5 条证据，而实际提交 payload 可能只有前端旧证据或另一套合并证据。
```

### 原因

```text
证据快照预览是后续新增的 API 合同字段。
前端展示层先接入了 evidence_snapshot_preview，但写入 payload 生成函数没有同步改为同源读取预检证据。
```

### 解决

```text
buildManualActionRequestPayload 优先使用 preflight.evidence_snapshot_preview.items。
只有预检没有可保存证据，或 will_save_on_authorized_write=false 时，才回退到前端传入的 evidenceSnapshot。
新增 review-ui 测试锁定：B07BS9754Q payload 必须提交 5 条后端预检证据，并且不能被前端旧证据覆盖。
```

### 防错规则

```text
人工动作按钮前展示的证据快照和点击时提交的 evidence_snapshot 必须同源。
后端预检返回 evidence_snapshot_preview.items 时，前端 payload 必须优先使用它。
后续改预检展示字段时，必须同时验证 buildManualActionRequestPayload。
```

### 验证方式

```text
npm run test:review-ui
npm run build
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_actions.py backend/tests/test_api_routes.py -q -k "manual_action or evidence_snapshot or route"
$env:PYTHONPATH='backend'; python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id B07BS9754Q --expected-object-type advertised_product --action-type add_to_review
```

## 2026-06-17：直接 API 不能提交与后端预检不一致的人工动作证据快照

### 问题

```text
上一轮已经让前端 buildManualActionRequestPayload 优先使用后端 evidence_snapshot_preview.items。
但后端 POST /api/signals/{signal_id}/manual-actions 仍只校验证据非空和对象期望一致。
如果有人绕过前端直接调用 API，可以提交一条非空但不匹配当前后端预检的旧 evidence_snapshot，并让 ReviewTodo 继承错误证据。
```

### 原因

```text
“页面展示证据”和“后端写入证据”是两层合同。
前端同源只能保证正常按钮路径，不足以保证 API 写入边界。
后端原先没有按 expected_product_scope_id / expected_object_type / expected_object_id / action_type 重跑只读预检，也没有比较提交证据和预检证据是否一致。
```

### 解决

```text
ManualActionRequest 增加 expected_product_scope_id。
复盘类动作 observe / handled / add_to_review 写入前，后端重新调用 build_manual_action_preflight_payload。
若预检 blocked、目标对象不一致、action_type 不一致，返回 409 / manual_action_preflight_mismatch。
若提交的 evidence_snapshot 与 preflight.evidence_snapshot_preview.items 的 label / value / detail / source 不一致，返回 409 / manual_action_evidence_snapshot_mismatch。
若复盘类动作缺 expected_object_type 或 expected_object_id，返回 409 / manual_action_preflight_required。
ignore 仍不生成复盘待办，不进入本轮证据快照强校验。
```

### 防错规则

```text
前端生成正确 payload 不等于后端写入安全。
以后凡是人工动作会生成 7d / 14d ReviewTodo，后端必须校验：
1. 目标 object_type / object_id 与当前信号一致。
2. 请求携带的 expected_product_scope_id 能重跑同一只读预检。
3. 提交的 evidence_snapshot 与后端预检 evidence_snapshot_preview.items 同源一致。
4. expected_can_auto_change_rules 和 expected_can_auto_execute_ads 必须为 false。
5. 不允许通过直接 API 写入旧证据、空证据或错对象证据。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_api_routes.py::test_manual_action_api_rejects_evidence_snapshot_that_does_not_match_preflight -q
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_actions.py backend/tests/test_api_routes.py -q -k "manual_action or evidence_snapshot or route"
npm run test:review-ui
npm run build
$env:PYTHONPATH='backend'; python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id B07BS9754Q --expected-object-type advertised_product --action-type add_to_review
```
## 2026-06-17：CLI 人工动作写入口不能丢失证据快照

### 问题

```text
后端 API 已经校验复盘类人工动作的 evidence_snapshot 必须和 preflight.evidence_snapshot_preview.items 同源。
但 scripts/apply_manual_action_once.py 作为 CLI 写入口，旧逻辑只保存目标对象、动作类型、店铺站点和备注，没有把 preflight 证据快照写入 save_manual_action。
如果未来通过 CLI 授权写入，review_todo 会继承空证据，后续 7d / 14d 复盘无法回看当时处理依据。
```

### 原因

```text
人工动作写入口不止页面和 API。CLI 虽然复用了 preflight 和授权码，但保存调用没有同步升级到“对象 + 证据同源”合同。
这属于入口合同漂移，不是 UI 展示问题。
```

### 解决

```text
scripts/apply_manual_action_once.py 写入前从 preflight.evidence_snapshot_preview.items 提取 label / value / detail / source。
observe / handled / add_to_review 缺少证据快照时返回 blocked / missing_evidence_snapshot，不调用 save_manual_action。
save_manual_action 现在接收同源 evidence_snapshot。
backend/tests/test_manual_action_apply_cli.py 增加授权写入保存证据快照、缺失证据快照阻断写入的测试。
```

### 防错规则

```text
任何新增人工动作写入口，只要会生成 7d / 14d review_todos，就必须同时满足：
1. 先跑只读 preflight。
2. 明确人工授权后才允许写入。
3. 保存的 evidence_snapshot 必须来自同一 preflight 目标。
4. 缺少 evidence_snapshot 时阻断 observe / handled / add_to_review。
5. 不能保存 review_records，不能执行广告动作。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_action_apply_cli.py -q
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_action_apply_cli.py backend/tests/test_manual_action_preflight_cli.py backend/tests/test_manual_actions.py backend/tests/test_api_routes.py -q -k "manual_action or preflight or evidence_snapshot"
$env:PYTHONPATH='backend'; python -m py_compile scripts\apply_manual_action_once.py backend\tests\test_manual_action_apply_cli.py
```
## 2026-06-17：全局复盘待办明细不能丢失留痕证据快照

### 问题

```text
后端 ReviewTodo 已继承 ManualAction.evidence_snapshot。
但前端全局复盘待办明细 buildReviewTodoQueueDetails 旧逻辑只展示 review_context，没有展示 evidence_snapshot。
这会导致运营切到全量排查查看复盘待办时，只看到对象、状态、到期日和复盘上下文，看不到当时人工留痕的证据快照。
```

### 原因

```text
证据快照写入链路已经补齐，但全局明细展示仍沿用早期“只看复盘上下文”的口径。
这是视图消费字段落后于数据合同，不是后端数据缺失。
```

### 解决

```text
buildReviewTodoQueueDetails 的 contextText 改为复用 manualActionEvidenceSnapshotText(todo)。
该函数会优先输出 evidence_snapshot，并在存在 review_context 时追加复盘上下文。
测试补充全局复盘待办明细必须包含“留痕证据快照”和具体 ABA 证据标签。
```

### 防错规则

```text
以后凡是展示 ReviewTodo 明细，不只要展示状态和到期日，还必须优先展示或可访问 evidence_snapshot。
如果待办缺少 evidence_snapshot，才展示历史缺口提示，不能把缺口静默吞掉。
前端不要重新拼一套证据文案，优先复用 manualActionEvidenceSnapshotText。
```

### 验证方式

```text
npm run test:review-ui
npm run build
```
## 2026-06-17：ReviewRecord 不能只凭 ready effect 保存

### 问题

```text
save_review_record 已经拒绝 not_ready effect。
但旧逻辑中，只要 effect.status=ready，即使请求没有携带 expected_action_id / expected_object_type / expected_object_id / expected_review_window，也会保存 ReviewRecord。
这会让直接 API 或脚本绕过页面预检，把 ready 复盘效果保存到未确认的动作、对象或复盘窗口上。
```

### 原因

```text
ready 只证明前后指标窗口可以计算，不证明“当前保存请求确认的是同一条人工动作、同一个业务对象、同一个复盘窗口”。
ReviewRecord 是规则反馈样本来源，比 ReviewTodo 更接近最终规则改进，因此保存门槛必须比只读效果计算更严格。
```

### 解决

```text
save_review_record 新增预检期望必填校验。
缺少 expected_action_id、expected_object_type、expected_object_id、expected_review_window 任一项时，返回 review_record_preflight_required。
API 路由将该错误映射为 409。
后端成功保存用例全部显式携带预检期望字段。
```

### 防错规则

```text
以后任何保存 ReviewRecord 的入口，都必须同时满足：
1. ReviewEffectResult.status = ready。
2. expected_action_id 与 ready effect 的 action_id 一致。
3. expected_object_type / expected_object_id 与 ready effect 一致。
4. expected_review_window 与请求窗口一致。
5. expected_can_auto_change_rules 和 expected_can_auto_execute_ads 必须为 false。

没有这些字段，只能展示 ready 效果待人工确认，不能保存 ReviewRecord，也不能进入规则反馈样本。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_actions.py::test_review_record_rejects_missing_preflight_expectation -q
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_actions.py backend/tests/test_api_routes.py -q -k "review_record or review_records or review_effect"
$env:PYTHONPATH='backend'; python -m pytest backend/tests/test_manual_actions.py backend/tests/test_api_routes.py -q -k "manual_action or review_record or review_effect or evidence_snapshot"
npm run test:review-ui
```

## 2026-06-17：推荐对象复盘按钮不能串用下一个候选 preview

### 问题

```text
浏览器运行态检查发现：
“查看复盘对象”按钮文案和 aria-label 指向已留痕推荐对象 B016EXMW02，
但点击后中间诊断标题仍停在下一个未留痕候选 B07BS9754Q。

这会让运营误以为已经进入复盘对象，实际仍在看候选信号。
```

### 原因

```text
后端 signal-triage 在推荐对象已有人工作业时，会把顶层 manual_action_preview 指向 next_unhandled_candidate。
前端 buildBackendRecommendedManualActionCandidate 旧逻辑优先使用顶层 manual_action_preview.signal_id，
导致推荐对象卡片显示 B016EXMW02，但实际绑定的 signal 是 B07BS9754Q。

这是前端消费 API 口径时没有校验 preview 与 recommended_candidate 是否同一对象。
```

### 解决

```text
buildBackendRecommendedManualActionCandidate 改为优先绑定 recommended_candidate.signal_id。
只有 manual_action_preview 的 signal_id / object_id 与 recommended_candidate 一致时，才复用该 preview。
如果 preview 指向下一个候选，则推荐对象卡片使用 recommended_candidate / 当前 signal 构造只读预览。
resolveSignalSelectionId 不再覆盖仍在当前队列内的显式选择，避免用户点开的复盘对象被自动切回下一个候选。
```

### 防错规则

```text
以后任何前端推荐卡、复盘对象卡、候选卡都必须先确认：
1. 卡片展示的 object_label。
2. 点击后绑定的 signal_id。
3. 只读 preview 的 object_id。
三者必须属于同一业务对象。

如果 API 同时返回 recommended_candidate 和 next_unhandled_candidate，前端不能把顶层 preview 直接当成推荐对象 preview。
```

### 验证方式

```text
npm run test:signal-ui
npm run test:review-ui
npm run build
浏览器运行态：
1. 进入 http://127.0.0.1:5175/
2. 经营商品入口选择 Parent ASIN B00K4W4AAA
3. 点击“查看复盘对象”
4. 中间诊断标题切到“广告 ASIN B016EXMW02 转化稳定，适合进入商品机会观察”
5. 页面仍保留“下一个未留痕候选 B07BS9754Q”，且不自动执行广告动作
```

## 2026-06-17：Parent ASIN 入口不能被误读为全量子 ASIN 广告分析

### 问题

```text
用户视角复核发现：
Parent ASIN B00K4W4AAA 下有 12 个销售表现子 ASIN，但当前广告 AI 信号队列只应继续分析有广告证据支撑的广告 ASIN 候选。
如果页面只写“当前商品组 AI 信号”，用户容易误以为 12 个子 ASIN 都应该进入广告分析，进而把未投放子 ASIN 也当成广告诊断对象。
```

### 原因

```text
Parent ASIN 是经营范围入口，不是广告投放对象。
销售表现子 ASIN 和广告 ASIN 属于不同对象层级；未投放子 ASIN 可以出现在销售表现计数中，但不能直接包装成广告 AI 信号。
原文案强调“当前商品组”，但没有把“有广告证据”和“未投放子 ASIN 不直接进入广告诊断”放到用户第一阅读路径里。
```

### 解决

```text
Parent ASIN 入口说明补充“下钻有广告证据的广告 ASIN”。
当前商品组 AI 信号说明补充“只展示当前 Parent ASIN 范围内有广告证据的异常、机会和待确认事项”。
商品组边界说明补充“广告 ASIN 只代表当前投放覆盖，未投放子 ASIN 不进入广告信号队列”。
前端纯函数测试补充上述边界断言。
```

### 防错规则

```text
以后任何 Parent ASIN / ASIN 入口相关文案和页面结构都必须同时说明：
1. Parent ASIN 是经营入口，不是广告对象。
2. 销售表现子 ASIN 数量不等于广告分析候选数量。
3. 广告 AI 信号队列优先展示有广告证据支撑的广告 ASIN / 商品信号。
4. 未投放子 ASIN 不直接进入广告诊断。
5. 搜索词和广告位只作为上下文，不自动归因到 ASIN。
```

### 验证方式

```text
npm run test:signal-ui
npm run test:review-ui
npm run build
浏览器运行态：
1. 打开 http://127.0.0.1:5175/
2. 选择 Parent ASIN B00K4W4AAA
3. 首屏应看到“有广告证据”和“未投放子 ASIN 不直接进入广告诊断”
4. 展开“商品边界与广告 ASIN 对比”后，应看到“未投放子 ASIN 不进入广告信号队列”
5. 控制台无 error / warn
```

## 2026-06-17：Parent ASIN 经营销售不能复用混合订单和销售字段

### 问题

```text
用户补充真实广告操作路径后，核对发现 Parent ASIN B00K4W4AAA 的经营盘子应先看整体销售，再下钻广告证据。
但 /api/product-scope 返回的 parent_asin:B00K4W4AAA 订单和销售额只有 13519 / 149023.34。
真实快照 sales_product_daily_metrics 复算结果是 24591 单、销售额 262341.49。
```

### 原因

```text
旧 product_scope 聚合把 parent_asin_product_map 当成去重依据。
如果父 ASIN 映射中已经存在某个子 ASIN，sales_product_daily_metrics 中相同 parent_asin + asin 的销售表现行会被跳过。
这会把 Parent ASIN 经营盘子的整体销售截断成部分销售。
同时 ProductScopeOption 只有 spend/orders/sales 三个混合字段，无法表达“经营销售口径”和“广告表现口径”是两条不同证据链。
```

### 解决

```text
ProductScopeOption 新增 sales_orders、sales_amount、ad_spend、ad_orders、ad_sales、metric_boundary。
Parent ASIN 聚合改为：
1. sales_product_daily_metrics 负责经营订单和销售额。
2. advertised_products 负责广告花费、广告订单和广告销售。
3. parent_asin_product_map 只补充子 ASIN 映射，只有在缺少销售 API 行时才兜底经营销售指标。
前端商品组概览改为读取显式字段，并展示 metric_boundary。
```

### 防错规则

```text
以后任何 Parent ASIN 经营盘子指标都必须区分：
1. 经营订单 / 经营销售额：优先来自 sales_product_daily_metrics。
2. 广告花费 / 广告订单 / 广告销售：来自 advertised_products。
3. parent_asin_product_map 不能让 sales_product_daily_metrics 行被跳过。
4. 不允许用 spend/orders/sales 三个混合字段单独解释 Parent ASIN 经营结论。
5. 前端展示 Parent ASIN 指标时必须带口径说明或 metric_boundary。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_product_scope.py -q
frontend 目录下 npm run test:signal-ui
frontend 目录下 npm run build
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "product_scope or signal_triage"
$env:PYTHONPATH='backend'; python -m py_compile backend\app\models\product_scope.py backend\app\services\product_scope.py
```

## 2026-06-17：广告组问题定位不能只停留在主要花费来源

### 问题

```text
用户补充真实操作路径后，发现当前工作台虽然能从 Parent ASIN 下钻到广告 ASIN，并展示主要花费广告活动 / 广告组，但没有结构化说明“具体该先查哪个广告组、问题可能来自搜索词、广告位还是广告商品承接”。
如果只展示一段长摘要或主要花费来源，运营仍然需要自己重新拆广告组、广告位、投放词和搜索词，AI 分诊价值不足。
```

### 原因

```text
recommended_evidence_drilldown 里已有 top_spend_ad_group、ad_product_rows、search_term_context_count 和 placement_context_count。
但这些字段没有被组合成广告组诊断结论，也没有进入业务证据卡的第一阅读路径。
这是“数据已经有，但没有按用户下钻路径组织”的问题。
```

### 解决

```text
后端新增 ad_group_diagnosis。
每个推荐广告 ASIN 会按广告商品投放行聚合广告组维度指标：花费、点击、订单、销售额、ACOS、CVR、同广告组搜索词数量、广告位数量。
business_evidence_blocks 新增“广告组问题定位”，直接提示优先下钻广告组和诊断焦点。
前端业务证据卡把“广告组问题定位”放在搜索词市场背景之前，并保留搜索词市场背景可见。
```

### 防错规则

```text
以后任何 Parent ASIN -> 广告 ASIN 的分诊详情，都不能只说主要花费广告活动。
至少要能回答：
1. 优先下钻哪个广告组？
2. 这个广告组的广告商品花费、订单、销售额是什么？
3. 同广告组有多少搜索词上下文？
4. 同广告组有多少广告位上下文？
5. 当前更应该先查广告商品承接、搜索词、广告位，还是补数据？
6. 该定位只用于人工诊断，不允许自动调价、自动否词、自动加词。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q -k "recommended_evidence_drilldown or ad_group"
frontend 目录下 npm run test:signal-ui
frontend 目录下 npm run build
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "signal_triage"
```
## 2026-06-17：优先广告组诊断不能混用其他广告组搜索词

### 问题

```text
用户补充真实广告操作路径后，核对发现：
系统已经能定位优先下钻广告组，但搜索词市场背景仍按同一广告 ASIN 的多个广告组合并输出。
例如 B016EXMW02 的优先广告组只有 18 条搜索词，但旧搜索词块会展示 93 条相关广告组搜索词合集。
这会让运营误以为 93 条搜索词都属于当前优先广告组，从而误判某个广告组的问题来源。
```

### 原因

```text
recommended_evidence_drilldown 合并同 ASIN 候选时，会汇总多个广告商品投放行。
_ad_product_search_term_aba_context 旧逻辑按这些投放行的全部 campaign/ad_group 上下文聚合搜索词。
但用户实际排查某个广告组时，需要先看优先下钻广告组的投放词、搜索词和广告位，而不是同 ASIN 所有广告组的混合搜索词。
```

### 解决

```text
搜索词上下文新增 focus_ad_group 参数，优先按主要花费广告组收敛。
返回 basis=priority_ad_group_search_terms 和 context_ad_group_label，前端文案展示为“优先广告组搜索词”。
搜索词块新增高花费词、有效词、无订单消耗词、ABA 匹配词和下一步人工复核焦点。
如果优先广告组没有搜索词，则不拿其他广告组搜索词补位，避免跨广告组误解释。
```

### 防错规则

```text
以后任何“广告组问题定位”后的搜索词解释都必须优先绑定同一个广告组。
如果搜索词来自多个广告组，必须显式说明是合集，不能写成当前优先广告组证据。
如果优先广告组搜索词为 0，只能提示搜索词上下文缺口或先看广告商品承接，不能用其他广告组搜索词替代。
搜索词和 ABA 仍只作为上下文和市场背景，不能自动归因到广告 ASIN，也不能触发自动否词、自动加词或自动调价。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q -k "search_term_aba_context or recommended_evidence_drilldown or ad_group"
frontend 目录下 npm run test:signal-ui
frontend 目录下 npm run build
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "signal_triage"
只读核对 parent_asin:B00K4W4AAA 的 B016EXMW02：search_term_basis=priority_ad_group_search_terms，context_count=18。
```

## 2026-06-17：广告位数据不能被误判为广告组级缺失

### 问题

```text
用户真实排查广告时会从 Parent ASIN 经营盘子下钻到广告 ASIN、广告组、广告位、投放商品、投放词和搜索词。
系统在“广告组问题定位”中显示优先广告组广告位 0 条，容易被理解为没有广告位数据。
只读核对后发现快照中实际存在 ad_placement_daily_metrics 150 行，问题不是广告位完全缺失，而是当前广告位数据没有广告组字段。
```

### 原因

```text
积加 sp_placements 标准化后的字段只有 campaign_id、campaign_name、placement、spend、orders、sales、clicks 等广告活动级字段。
旧逻辑按 campaign + ad_group 关联广告位，因此没有 ad_group_id / ad_group_name 的广告位行都会被排除。
如果直接把这些广告位算成同广告组广告位，会制造错误归因；如果只显示 0 条，又会掩盖同广告活动下其实有广告位数据。
```

### 解决

```text
保留 placement_count 表示同广告组广告位数量。
新增 campaign_placement_count 表示同广告活动广告位数量。
新增 placement_context_level 标记当前广告位证据粒度：ad_group / campaign / missing。
业务证据块显示“广告组级广告位 0 条 / 同广告活动广告位 N 条”，并在诊断焦点中提示不能直接归因到该广告组。
```

### 防错规则

```text
以后任何广告位证据都必须先检查字段粒度。
没有 ad_group_id 或 ad_group_name 时，不能写成“同广告组广告位”，只能写成“同广告活动广告位”。
同广告活动广告位只能用于人工复核流量位置结构，不能直接归因到某个广告组、广告 ASIN 或自动广告动作。
如果既没有广告组级广告位，也没有活动级广告位，才允许提示广告位数据缺口。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q -k "ad_group or placement"
frontend 目录下 npm run test:signal-ui
frontend 目录下 npm run build
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "signal_triage"
只读核对 parent_asin:B00K4W4AAA：B016EXMW02 同广告活动广告位 3 条，B07BS9754Q 同广告活动广告位 4 条。
```

## 2026-06-17：投放词不能伪装成完整关键词库

### 问题

```text
用户希望继续补齐广告排查路径中的投放词层。
当前真实快照没有独立 keywords 或 product_targetings 标准化表，只有 ad_search_term_daily_metrics 中的 keyword_text、keyword_id、target_id、source_report_type。
如果直接把这些字段展示成完整关键词库，会误导用户以为系统已经掌握广告组全部投放词。
同时，B07BS9754Q 的优先广告组没有搜索词表现行时，旧边界容易继承同 ASIN 其他广告组搜索词数量。
```

### 原因

```text
积加 sp_search_targeting_terms 和 sp_search_keyword_terms 当前被标准化进搜索词表现表。
这些字段能说明“搜索词表现行背后的投放词结构”，但不能覆盖未产生搜索词表现的全部投放词。
广告架构上，广告组是投放容器；一个广告 ASIN 可能出现在多个广告组中，不能跨广告组补搜索词或投放词。
```

### 解决

```text
新增 targeting_context，只按优先广告组聚合搜索词表现行里的投放词字段。
文案使用“投放词结构”，不使用“关键词库”。
业务证据明确提示：投放词来自搜索词表现行，不代表完整关键词库，不能自动加词、否词或调价。
优先广告组没有搜索词表现行时，搜索词数量显示 0，不再拿其他广告组补位。
```

### 防错规则

```text
以后只有独立关键词或商品定向主数据表时，才允许称为关键词库或完整投放词列表。
来自搜索词表现行的 keyword_text、keyword_id、target_id 只能称为投放词结构或投放词证据。
任何广告组诊断都必须优先绑定同一广告组，不能跨广告组补搜索词、投放词或广告位。
AI 只能给人工复核建议，不能自动加词、否词或调价。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q -k "search_term_aba_context or targeting or ad_group or empty_search_term_boundary"
frontend 目录中 npm run test:signal-ui
frontend 目录中 npm run build
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "signal_triage"
只读核对 parent_asin:B00K4W4AAA：B016EXMW02 投放词 1 个 / 搜索词表现行 18 条；B07BS9754Q 搜索词 0 条。
```

## 2026-06-17：单周期稳定转化不能包装成可处理机会

### 问题

```text
用户审阅 Web 结果后指出：当前数据口径混乱，系统给出的信息、机会和证据链没有实际落地意义。
审计发现系统生成了多条“广告 ASIN 转化稳定，适合进入商品机会观察”和“广告位转化稳定，存在预算倾斜机会”。
这些信号看起来像机会，但无法直接指导运营做人工动作。
```

### 原因

```text
旧规则只凭单周期点击、订单、ACOS 就把稳定表现升级成机会信号。
但广告 ASIN 放量判断需要库存、利润、价格、主推策略、搜索词相关性等独立证据。
广告位预算倾斜判断需要同广告活动广告位对比、预算目标和广告组 / Campaign 粒度边界。
当前这些证据并未完整接入，因此旧结论超出了数据能力。
```

### 解决

```text
暂停生成 advertised_product_opportunity。
暂停生成广告位稳定转化预算倾斜机会。
保留异常、销售承接不足、搜索词机会、搜索词表现分化等更接近人工动作和复盘的信号。
旧测试改为防错测试，要求单周期稳定转化不生成可处理机会。
```

### 防错规则

```text
以后不能把“表现好”直接写成“机会”。
机会信号必须同时具备：明确经营对象、明确经营问题、至少一个可复核证据源、明确人工动作、明确 7/14 天复盘指标。
广告 ASIN 放量机会必须补齐库存、利润、价格、主推策略或搜索词相关性等独立证据，否则只能作为观察事实。
广告位预算倾斜机会必须有同活动广告位对比和预算目标，否则不能进入 Web 待处理信号。
如果当前范围没有可落地信号，页面应显示无信号或数据 / 规则缺口，不能用伪机会填充。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q -k "placement or advertised_product or opportunity"
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "signal_triage or signals"
真实快照复核：信号总数从 45 降到 30，不再包含 advertised_product_opportunity 和稳定转化广告位机会。
```

## 2026-06-17：人工处理队列缺少统一可落地准入闸门

### 问题

```text
用户指出 Web 结果数据口径混乱，信息、机会和证据链没有实际落地意义。
上一轮虽然暂停了 advertised_product_opportunity 和稳定转化广告位机会，但人工处理队列入口仍缺少统一准入规则。
只要某条信号带 requires_manual_confirmation=true，就可能进入候选队列，未来新增浅层信号仍可能污染 Web 结果。
```

### 原因

```text
build_review_candidates_payload 旧逻辑只判断站点、对象类型、data_quality 和是否需要人工确认。
它没有统一检查：信号类别是否允许进入人工处理、是否有明确经营问题、动作类型是否允许、证据数量是否足够、数据来源是否可追踪。
这会把“信号列表”和“可人工处理候选列表”混为一谈。
```

### 解决

```text
signal_triage 新增人工处理队列准入闸门。
准入条件：支持对象、允许信号类别、明确经营问题、允许人工动作、evidence_count >= 3、存在可追踪数据来源、requires_manual_confirmation=true。
advertised_product_opportunity 明确禁入人工处理队列。
advertised_product_efficiency 补充经营问题映射为“花费浪费”。
inspect_review_candidates 复用后端同一套准入原因。
候选分层文案改为“广告 ASIN 待处理”，避免把候选包装成机会。
```

### 防错规则

```text
以后 Web 人工处理队列不等于全部 AI 信号列表。
任何新信号进入队列前，必须同时回答：主对象是谁、经营问题是什么、证据来源是什么、人工动作是什么、7/14 天复盘看什么。
没有可追踪数据来源、证据数量不足、动作类型不在允许列表或经营问题未分类时，只能被排除并进入 excluded_summary，不允许进入候选队列。
表现观察不能通过 summary 文案包装成机会；广告 ASIN 放量类机会在库存、利润、价格、主推策略或独立市场证据未接入前不得进入队列。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_review_candidates_cli.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "signal_triage or signals"
真实快照复核：market_id=1 当前候选类别只包含 product_ad_coverage、advertised_product_efficiency、search_term_opportunity、search_term_performance_split；Parent ASIN B00K4W4AAA 当前仍为 0 个候选。
```

## 2026-06-17：Parent ASIN 广告指标不能回退为经营指标

### 问题

```text
用户指出 Web 结果数据口径混乱、证据链缺少落地意义。
浏览器复核 B00K4W4AAA 商品组概览时发现，“广告证据下钻”曾把 Parent ASIN 的经营订单/经营销售额显示成广告订单/广告销售额。
这会误导用户认为广告贡献了完整经营销售，直接破坏广告诊断口径。
```

### 原因

```text
前端 scopeAdOrders / scopeAdSales 旧逻辑在缺少 ad_orders / ad_sales 时回退到 orders / sales。
对于 Parent ASIN，orders / sales 表示经营订单和经营销售额，不是广告指标。
同时本地 8000 后端进程是旧进程，/api/product-scope 未返回最新 ProductScopeOption 中的 ad_orders / ad_sales 字段，触发了前端错误回退。
```

### 解决

```text
scopeAdOrders / scopeAdSales 只允许 advertised_asin 层在缺广告字段时回退到 orders / sales。
Parent ASIN 层缺 ad_orders / ad_sales 时显示 0，不再用经营指标补位。
空队列解释前置显示，明确销售表现子 ASIN、当前投放广告 ASIN和未投广告子 ASIN边界。
重启本地后端 8000，让 /api/product-scope 返回 ad_orders=281、ad_sales=2715.06 等当前模型字段。
```

### 防错规则

```text
以后所有前端广告指标展示必须优先使用 ad_* 字段。
Parent ASIN 的 orders / sales 默认是经营销售口径，不能直接放进广告证据区。
缺广告指标时宁可显示缺口或 0，也不能用经营指标补位。
浏览器看到的数据和 Python 只读服务输出不一致时，必须优先怀疑本地后端旧进程或接口合同未刷新。
```

### 验证方式

```text
frontend 目录中 npm run test:signal-ui
frontend 目录中 npm run build
只读检查 /api/product-scope 中 parent_asin:B00K4W4AAA 的 sales_orders / sales_amount / ad_spend / ad_orders / ad_sales。
浏览器复核 localhost:5175 商品组概览，确认广告订单显示 281、广告销售额显示 2715.06。
```

## 2026-06-17：销售表现广告指标与 SP 广告商品指标混用导致伪机会

### 问题

```text
用户指出 Web 结果数据口径混乱，给出的信息、机会和证据链无实际落地意义。
审计发现 product_ad_coverage 会在销售表现 ad_orders 已经明显存在的情况下，仅因 SP 广告商品 orders 偏低，就生成“销售强但广告弱”机会。
这种机会无法直接落地，因为系统没有先确认两个 API 字段是否同口径、同覆盖范围。
```

### 原因

```text
旧规则把 sales_product_daily_metrics.orders 当作销售强度，把 advertised_products.orders 当作广告承接强弱。
但 sales_product_daily_metrics 本身还带 ad_orders / ad_sales / ad_spend。
当 sales_product_daily_metrics.ad_orders 与 advertised_products.orders 明显冲突时，说明当前快照覆盖或字段口径不一致。
此时继续生成 product_ad_coverage，会把数据质量问题误报成业务机会。
```

### 解决

```text
新增产品广告承接口径冲突判断。
当销售表现 ad_orders > 1 且匹配到的 SP 广告商品 orders <= 1 时，不生成 product_ad_coverage。
改为生成 1 条 data_quality 信号，说明销售表现广告订单与 SP 广告商品订单冲突，需要先核对字段口径和快照覆盖。
真实快照下原 10 条 product_ad_coverage 被拦截，Web 人工处理队列不再展示销售强广告弱伪机会。
```

### 防错规则

```text
以后任何信号不得跨表直接比较同名或近似指标，必须先确认字段口径、对象粒度和覆盖范围一致。
sales_product_daily_metrics 中的 ad_orders / ad_sales 不能被忽略；它们与 advertised_products 冲突时，优先生成数据质量信号。
产品广告承接机会必须同时满足：销售商品强、广告商品弱、销售表现广告指标不反证该判断。
如果证据本身互相冲突，不能通过 summary 或 suggested_action 包装成业务建议。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q -k "ad_metrics_conflict"
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "signal_triage or signals"
真实快照只读扫描应显示 product_ad_coverage_count=0、metric_conflict_count=1。
真实 Web 队列只读扫描应显示 candidate_categories 不包含 product_ad_coverage。
```

## 2026-06-17：ASIN 型搜索词被误包装成关键词扩量机会

### 问题

```text
用户指出当前 Web 结果数据口径混乱，机会和证据链缺少实际落地意义。
审计 search_term_opportunity 发现，ASIN 形态搜索词会被输出为“加入精准关键词候选”或普通长尾词机会。
这会误导运营把商品定向或自动投放上下文中的 ASIN 对象，当成普通关键词去处理。
```

### 原因

```text
旧规则只按订单、花费和 ACOS 判断搜索词机会，没有先判断 search_term 是否为 ASIN 形态。
聚合逻辑会覆盖单条信号的动作描述，把同一搜索词跨投放上下文合并后重新写成“精准关键词候选”。
人工分诊允许动作白名单没有包含新的商品定向复核动作，导致正确动作一度无法进入候选队列。
```

### 解决

```text
新增 ASIN 型搜索词识别规则。
ASIN 型搜索词统一输出 review_product_targeting_search_term，建议人工复核商品定向价值。
单条强转化、长尾低花费高转化、跨投放上下文聚合都保留对象边界证据。
人工分诊白名单加入 review_product_targeting_search_term，真实候选队列恢复到 20 条，其中 3 条为 ASIN 型搜索词商品定向复核。
```

### 防错规则

```text
以后任何 search_term_opportunity 都必须先判断搜索词形态。
普通搜索词可以进入关键词机会；ASIN 型搜索词不得建议加词，只能进入商品定向或自动投放上下文人工复核。
聚合函数不得丢失对象边界证据，也不得用普通搜索词动作覆盖 ASIN 型搜索词动作。
新增 action_type 后必须同步检查人工分诊准入，否则正确信号会从 Web 队列消失。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q -k "asin_like"
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "signal_triage or signals"
真实 Web 队列只读扫描应显示 action_mix 包含 review_product_targeting_search_term=3，且 ASIN 型搜索词描述不包含“精准关键词”。
```

## 2026-06-17：广告 ASIN 异常缺下游证据却提示调整投放结构

### 问题

```text
用户指出 Web 结果信息和证据链缺少实际落地意义。
审计 advertised_product_efficiency 发现，部分广告 ASIN 只有广告商品直接指标，没有同广告组搜索词和广告位上下文。
旧建议仍提示“对应搜索词”和“调整投放结构”，这超出了当前证据能支持的判断范围。
```

### 原因

```text
旧规则只要广告商品点击充足但订单弱，或 ACOS 偏高，就统一输出同一套建议文案。
ad_product_context_evidence 在下游上下文为空时没有显式输出证据缺口。
候选 evidence_drilldown 只显示搜索词 0 条 / 广告位 0 条，没有把它转化为用户可理解的业务判断边界。
```

### 解决

```text
广告 ASIN 异常在缺少同广告组搜索词和广告位上下文时，新增“下钻证据缺口”证据。
建议文案收敛为先检查库存、价格、Listing、主推策略，并确认是否补齐搜索词或广告位数据。
候选下钻 business_evidence_blocks 增加 downstream_context_gap，前端可直接展示该证据缺口。
真实队列中 2 条缺下游上下文的 B0GL8QDHW5 候选已出现 downstream_context_gap。
```

### 防错规则

```text
以后广告 ASIN 效率异常必须区分直接指标和下游原因解释。
只有广告商品直接指标时，可以判断承接异常，但不能解释为具体搜索词、广告位或投放结构问题。
当同广告组搜索词和广告位上下文为 0 时，必须显式展示下钻证据缺口。
建议文案不得在证据缺失时引导用户调整投放结构，只能提示人工核对承接或补齐下游证据。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q -k "advertised_product_low_order_anomaly"
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q -k "direct_rows_for_coverage_ratio"
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "signal_triage or signals"
真实 Web 队列只读扫描应显示缺下游上下文的广告 ASIN 候选带 downstream_context_gap，且建议描述不包含“对应搜索词”或“调整投放结构”。
```

## 2026-06-17：广告 ASIN 有搜索词上下文但缺广告位证据仍提示检查投放结构

### 问题

```text
用户指出 Web 结果数据口径混乱，信息、机会和证据链无实际落地意义。
继续审计 advertised_product_efficiency 发现，部分候选有同广告组搜索词上下文，但广告位上下文为 0。
旧建议文案仍写“同广告组搜索词和广告位上下文”，并提示“检查投放结构”，容易让用户误以为广告位证据存在。
```

### 原因

```text
上一轮只处理了搜索词和广告位同时缺失的全缺口场景。
规则把“存在任意下游上下文”简化成“下游上下文完整”，没有区分只有搜索词、只有广告位和两者都存在。
前端业务证据块排序也没有显式包含新缺口类 block，存在被展示上限截断的风险。
```

### 解决

```text
广告 ASIN 异常增加部分缺口判断：有搜索词上下文但广告位为 0 时，新增“广告位证据缺口”。
suggested_action 收敛为先查库存、价格、Listing、主推策略和同广告组搜索词；广告位证据缺失时先补齐广告位数据。
候选 business_evidence_blocks 增加 placement_context_gap。
前端业务证据排序显式加入 placement_context_gap 和 downstream_context_gap，保证缺口信息可见。
```

### 防错规则

```text
以后不能把“有任意下游上下文”当作“下游证据完整”。
广告 ASIN 诊断必须分别表达搜索词上下文数量和广告位上下文数量。
只有搜索词上下文时，可以建议复核搜索词消耗 / 相关性，但不能判断广告位问题，也不能提示已有广告位上下文。
关键证据缺口 block 必须进入前端显式排序，不能依赖 fallback。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q -k "missing_placement_context"
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q -k "missing_placement_context"
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "signal_triage or signals"
frontend 目录中 npm run test:signal-ui
frontend 目录中 npm run build
真实 Web 队列只读扫描应显示 6 条部分缺口 advertised_product_efficiency 候选带 placement_context_gap，且 bad_action_count=0。
```

## 2026-06-17：搜索词候选复用广告 ASIN 证据模板

### 问题

```text
用户指出 Web 结果数据口径混乱，信息、机会和证据链没有实际落地意义。
审计 search_term_opportunity / search_term_performance_split 后发现，搜索词候选虽然对象是 search_term，但 evidence_drilldown 复用了广告 ASIN 模板。
页面和右侧处理依据出现“广告商品投放行 0 条”“广告聚合指标 0”“主要花费来源未知”，与搜索词机会判断矛盾。
```

### 原因

```text
_candidate_evidence_drilldown 只区分 sales_product 和广告商品模板；只要 source_rows 中有 ad_search_term_daily_metrics，就落入广告商品证据模板。
recommended_evidence_drilldown 和 next_unhandled_evidence_drilldown 对 search_term 仍进入同 ASIN 合并逻辑，导致后端预检 evidence_snapshot_preview 也返回广告商品 0 指标。
前端右侧“处理依据”优先展示本地 fallback 证据，没有优先使用后端 preflight evidence_snapshot_preview，导致用户点击前看到的依据和写入时保存的依据可能不一致。
```

### 解决

```text
后端新增搜索词专用 evidence_drilldown：
- 只汇总 ad_search_term_daily_metrics。
- 展示搜索词表现、投放上下文、投放词结构、广告位证据缺口和对象边界。
- search_term 不再进入广告 ASIN 合并模板。
- 复盘反馈 evidence_groups 中 search_term_daily_metrics 显示为“搜索词指标”。

前端新增 buildManualActionDisplayEvidenceSnapshot：
- 右侧处理依据优先展示后端 preflight evidence_snapshot_preview。
- 预检无证据时才回退到本地 fallback。
```

### 防错规则

```text
搜索词候选不能显示广告商品覆盖、广告聚合指标或主要花费来源，除非主对象确实是 advertised_product。
SearchTerm 是投放证据对象，默认只能定位广告活动、广告组、投放词结构和搜索词表现，不能自动归因到单个广告 ASIN。
右侧处理依据必须优先来自后端预检证据快照，保证“点击前看到的依据”和“授权后保存的 evidence_snapshot”同源。
新增任何对象类型的 drilldown 时，必须检查候选层、推荐层、next_unhandled 层、复盘反馈层和前端处理依据是否同口径。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "signal_triage or signals or preflight"
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_review_candidates_cli.py -q
frontend 目录中 npm run test:review-ui
frontend 目录中 npm run test:signal-ui
frontend 目录中 npm run build
真实接口 /api/manual-action/preflight 对 search_term:1:beach essentials 返回“搜索词表现 / 投放上下文 / 投放词结构 / 对象边界”，bad_ad_product_template=False。
浏览器 DOM 切到 all 后右侧处理依据显示“搜索词表现：花费 34.11 / 订单 21 / 销售额 193.20”，不再显示广告聚合指标 0。
```

## 2026-06-17：低样本普通搜索词被包装成可处理机会

### 问题

```text
用户指出 Web 结果数据口径混乱，给出的信息、机会和证据链无实际落地意义。
审计搜索词机会后发现，普通搜索词在单一投放上下文中仅 1 次点击、2-3 个订单、无 ABA 支撑时，仍会生成 promote_search_term 或 review_long_tail_search_term。
这会把低样本偶然正反馈包装成“加入精准关键词候选”或“长尾机会观察”，导致人工处理队列噪音过高。
```

### 原因

```text
旧规则只按订单、花费和 ACOS 判断机会。
强机会和长尾机会没有共同的最小样本准入。
ABA 精确 / 短语市场证据只作为补充证据，没有参与判断低样本搜索词是否有处理价值。
```

### 解决

```text
新增搜索词机会准入：
普通搜索词必须满足点击数达到最小样本，或命中 ABA 精确 / ABA 短语市场证据，才允许生成机会。
ASIN 型搜索词保留为商品定向复核，不进入普通关键词扩量口径。
真实候选中 signal_count 从 21 降到 17，candidate_count 从 20 降到 16；普通低样本词已从 Web 队列中移除。
```

### 防错规则

```text
以后不能只凭订单和低 ACOS 生成普通搜索词机会。
单一 API 行、低点击、无 ABA 或多上下文支撑的普通搜索词，只能作为低样本现象，不能进入人工处理机会队列。
ASIN 型搜索词必须继续和普通关键词分开处理，只能作为商品定向或自动投放上下文复核。
搜索词机会规则新增或改动时，必须同时检查强机会、长尾机会、聚合机会和人工分诊候选数量。
```

### 验证方式

```text
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q -k "low_sample or long_tail or search_term_opportunity"
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_detection.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_signal_triage_service.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -q -k "signal_triage or signals or preflight"
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_review_candidates_cli.py -q
python -m py_compile backend\app\services\signal_detection.py
真实接口 /api/signal-triage?market_id=1&product_scope_id=all 应显示 signal_count=17、candidate_count=16，低样本普通搜索词不再出现在 top_candidates。
浏览器 DOM 应显示 17 条信号，且不包含 boys polarized sunglasses age 6-8、childrens sunglasses、4 year old sunglasses boy、best beach vacation essentials。
```

## 2026-06-17：候选层丢失不确定性导致 Web 机会看起来无边界

### 问题

```text
用户指出当前 Web 结果数据口径混乱，给出的信息、机会和证据链无实际落地意义。
继续审计后发现，规则层的 AiSignal 已有 uncertainty，但候选层和摘要层没有透传。
低样本长尾搜索词、ABA 短语背景词、单一 API 数据源信号在 Web 候选里会被展示成普通“机会”，用户看不到“只能观察、不能直接放量、ABA 不能归因到店铺商品”的边界。
```

### 原因

```text
_candidate_payload 只保留 summary、evidence_count、freshness_status、evidence_drilldown 和 suggested_action，没有保留 uncertainty。
_candidate_summary 也没有保留 uncertainty。
前端摘要类型缺少 uncertainty，导致顶部深度摘要无法提示关键不确定性。
```

### 解决

```text
backend/app/services/signal_triage.py：
- _candidate_payload 增加 uncertainty。
- _candidate_summary 增加 uncertainty。

scripts/inspect_signal_triage.py：
- _candidate_summary 同步增加 uncertainty。

frontend/src/pages/SignalTriageWorkbench/signalUi.ts：
- recommended_candidate / next_unhandled_candidate 增加 uncertainty。
- 深度摘要加入“不确定性”。
```

### 防错规则

```text
任何新信号字段只要影响业务判断，必须同时检查规则层、候选 payload、推荐摘要、next_unhandled、CLI 验收和前端展示。
不确定性、归因边界、证据来源属于 AI 信号的核心业务字段，不能只放在详情或调试输出里。
搜索词机会尤其必须把低样本、ABA 站点级背景、不可自动归因到广告 ASIN 的边界放到候选层。
```

### 验证方式

```text
backend 目录中 python -m pytest tests\test_signal_triage_service.py -q -k "search_term_candidate_keeps_uncertainty"
backend 目录中 python -m pytest tests\test_signal_triage_service.py -q
backend 目录中 python -m pytest tests\test_signal_detection.py -q
backend 目录中 python -m pytest tests\test_api_routes.py -q -k "signal_triage or signals or preflight"
backend 目录中 python -m py_compile app\services\signal_triage.py ..\scripts\inspect_signal_triage.py
frontend 目录中 npm run build
真实接口 /api/signal-triage?market_id=1&product_scope_id=all&top=5 的 recommended_candidate 和 next_unhandled_candidate 必须包含 uncertainty。
```

## 2026-06-17：推荐对象证据被下一候选证据覆盖

### 问题

```text
真实推荐对象 B07QXY6573 已经有人工留痕和复盘待办。
后端为了继续推进人工处理，会同时返回 next_unhandled_candidate = beach essentials。
前端 signalTriageBusinessEvidenceItems 在 recommended_manual_status.has_manual_action=true 时全局切换到 next_unhandled_evidence_drilldown。
这会导致用户点回 B07QXY6573 时，中间业务证据条不再展示 B07QXY6573 的广告组问题定位、投放词结构和搜索词分化判断。
```

### 原因

```text
前端把“下一步人工处理入口”和“当前选中信号证据”混成同一个全局状态。
推荐对象和下一候选是两个不同业务对象：
- B07QXY6573：广告 ASIN / 广告组 / 投放词 / 搜索词分化复核
- beach essentials：搜索词机会 / 多投放上下文 / ABA 背景
两者不能共用同一组业务证据条。
```

### 解决

```text
signalTriageBusinessEvidenceItems 增加显式 target：
- recommended：取 recommended_evidence_drilldown
- next_unhandled：取 next_unhandled_evidence_drilldown
- active：保留旧的全局推荐逻辑

SignalTriageWorkbench 按 selectedSignal.id 判断：
- 选中 recommended_candidate 时展示推荐对象证据
- 选中 next_unhandled_candidate 时展示下一候选证据
- 其他信号不强行展示全局证据
```

### 防错规则

```text
推荐对象、下一候选、当前选中信号必须分开建模。
任何证据条、预检依据、人工动作快照都必须绑定具体 signal_id / object_type / object_id。
不能因为推荐对象已留痕，就把推荐对象的诊断证据从详情区替换成下一候选证据。
下一候选可以在右侧或顶部提示，但不能覆盖当前选中信号的中间证据链。
```

### 验证方式

```text
frontend 目录中 npm run test:signal-ui
frontend 目录中 npm run build
build_signal_triage_payload(selected_market_id=1, product_scope_id='all', top=5) 应同时返回：
- recommended_candidate.object_label = B07QXY6573
- recommended_evidence_drilldown.business_evidence_blocks 包含 综合判断 / 广告组问题定位 / 投放词结构
- next_unhandled_candidate.object_label = beach essentials
- next_unhandled_evidence_drilldown.business_evidence_blocks 包含 搜索词表现 / 投放上下文 / 投放词结构
```

## 2026-06-17：业务证据平铺导致诊断不可落地

### 问题

```text
用户阅读当前 web 结果后反馈：数据口径混乱，系统给出的信息、机会、证据链没有实际落地意义。

具体到 B07QXY6573：
后端已经返回综合判断、广告组问题定位、投放词结构、搜索词市场背景、广告位证据缺口和上下文边界。
但前端把这些内容作为普通业务证据条平铺在“证据链”内部，没有组织成运营可执行的排查路径。
```

### 原因

```text
前端展示把“证据存在”误当成“用户能行动”。
广告运营实际路径是：
先看 Parent / 广告商品整体问题，再下钻广告组、广告位、投放商品、投放词、搜索词，最后判断广告组哪里出了问题。

旧展示只证明系统有证据，没有告诉用户下一步先查哪里，也没有突出广告组、投放词、搜索词和广告位之间的边界。
```

### 解决

```text
新增前端纯函数 signalTriageDiagnosisPathItems：
- 只消费已有 business_evidence_items。
- 不新增后端结论。
- 固定组织顺序：综合判断 -> 广告组问题定位 -> 投放词结构 -> 搜索词市场背景 -> 广告位证据缺口 -> 上下文边界。

SignalDiagnosis 在“原因”和“证据链”之间新增“广告诊断路径”。
完整证据链保留，但从主要行动入口降级为支撑材料。
```

### 防错规则

```text
以后不能只判断“页面有没有展示证据”。
必须判断证据是否回答了用户的运营路径：
- 先看哪个业务对象？
- 下钻到哪个广告组？
- 应优先复核哪个投放词或搜索词？
- 哪些证据只能作背景，不能直接归因？
- 系统建议是否仍然停留在人工确认范围内？

前端展示如果不能支撑上述问题，即使数据块很多，也仍然属于不可落地。
```

### 验证方式

```text
frontend 目录中 npm run test:signal-ui
frontend 目录中 npm run build
build_signal_triage_payload(selected_market_id=1, product_scope_id='all', top=5) 的 recommended_evidence_drilldown 必须包含诊断路径所需节点。
http://127.0.0.1:5175/ 应返回 200。
```

## 2026-06-17：路径节点主值仍是统计口径

### 问题

```text
前端已经把“广告诊断路径”前置，但后端返回的部分节点 value 仍只表达统计数量：
- 优先广告组投放词 1 个 / 搜索词表现行 4 条
- 优先广告组搜索词 4 条 / ABA Top1000 匹配 0 条

这类主值能说明数据量，但不能直接告诉运营下一步先看哪个投放词或搜索词。
```

### 原因

```text
后端把可操作对象放在 detail 中，把 value 当成摘要计数。
但前端诊断路径的主行最容易被用户看到，主行如果不露出行动对象，路径仍然会变成“信息展示”，不是“排查路径”。
```

### 解决

```text
新增 _evidence_focus_pair_text，从有效项和无订单消耗项中各取第一优先对象。

targeting_context.value 改为：
优先广告组投放词 N 个：有效 {targeting}

search_term_market_context.value 改为：
优先广告组搜索词 N 条：有效 {search_term} / 无订单 {search_term} / ABA Top1000 匹配 N 条

完整高花费、有效、无订单、ABA 明细继续留在 detail。
```

### 防错规则

```text
诊断路径的 value 必须优先回答“先看谁”，不能只回答“有多少行”。
行数、覆盖率、样本量可以保留，但不能替代可复核对象。
如果某个节点存在有效对象和问题对象，主值至少要露出第一优先有效对象和第一优先问题对象。
如果结论涉及广告操作，detail 必须继续说明只能人工复核，不能自动加词、否词、调价或暂停广告。
```

### 验证方式

```text
backend 目录中 python -m pytest tests\test_signal_triage_service.py -q -k "ad_product_drilldown_adds_same_ad_group_search_term_aba_context"
backend 目录中 python -m pytest tests\test_signal_triage_service.py -q
backend 目录中 python -m py_compile app\services\signal_triage.py
真实接口 /api/signal-triage?market_id=1&product_scope_id=all&top=5 的 targeting_context.value 和 search_term_market_context.value 应直接包含有效词或无订单词。
```

## 2026-06-17：空信号状态掩盖历史人工留痕缺证据

### 问题

```text
当前商品组筛选下可能出现“暂无需处理信号”，但后端 review_feedback.closure_checklist 同时提示：
人工动作 15 条，证据快照 0 条。

如果页面只展示“无信号”，用户会误以为当前广告对象没有问题。
实际阻塞点是历史人工动作缺 evidence_snapshot，无法支撑 7/14 天复盘。
```

### 原因

```text
空队列说明只看商品组内当前信号数量、策略压制、数据不足和范围外信号。
它没有读取后端复盘闭环检查项。

但从业务闭环看：
人工动作 -> evidence_snapshot -> review_todos -> 7/14 天复盘
是一条链路。
历史人工动作没有 evidence_snapshot 时，即使当前候选为空，也不能说闭环健康。
```

### 解决

```text
buildProductScopeSignalExplanation 增加 signalTriageSummary 输入。
当 closure_checklist 中 manual_action_context 为 blocked 时，空队列说明追加：
复盘输入证据缺口：{后端 evidence 文本}

页面调用处传入 signalTriageSummary，让空队列状态能暴露后端事实。
```

### 防错规则

```text
以后看到“无信号”必须继续检查：
- signal_row_count 是否大于 0
- candidate_count 是否为 0
- review_feedback.closure_checklist 是否有 blocked
- manual_action_context 是否提示 evidence_snapshot 为 0

空队列不是业务结论，只是当前筛选下没有可处理候选。
如果历史人工动作缺证据快照，必须提示复盘输入证据缺口。
禁止把这类状态解释成广告数据没有问题。
```

### 验证方式

```text
frontend 目录中 npm run test:signal-ui
frontend 目录中 npm run build
浏览器打开 http://127.0.0.1:5175/，当前商品组空队列说明应包含“复盘输入证据缺口”。
```

## 2026-06-17：人工动作快照先保存泛化指标而非行动路径

### 问题

```text
人工动作预检快照直接按 business_evidence_blocks 原始顺序保存。
广告 ASIN 场景中，原始顺序通常先是广告商品覆盖、广告聚合指标、主要花费来源。

这些指标有价值，但不能直接回答运营复盘时最关键的问题：
- 当时综合判断是什么？
- 问题定位在哪个广告组？
- 先看哪个投放词？
- 先看哪个搜索词？
- 哪些证据只是上下文，不能自动归因？
```

### 原因

```text
系统把“有证据”误当成“证据顺序可落地”。
人工动作快照不是普通证据仓库，它会被 ReviewTodo 继承，用于 7/14 天复盘回看当时判断依据。

如果快照前段只保存泛化指标，复盘时用户仍要重新在完整证据里找行动线索，闭环落地弱。
```

### 解决

```text
在 manual_action_preflight 中增加行动化证据排序：
综合判断 -> 广告组问题定位 -> 投放词结构 -> 搜索词市场背景 -> 广告位证据缺口 / 下钻证据缺口 -> 上下文边界 -> 排查路径。

只有存在这些行动化块时才重排。
覆盖率、聚合指标、主要花费来源等泛化指标仍保存，只是排在后面。
```

### 防错规则

```text
人工动作 evidence_snapshot_preview 的前几条必须优先回答“为什么要人工处理”。
不能只保存覆盖率、花费、订单、ACOS 这类泛化指标。

广告 ASIN 类信号至少优先保留：
- 综合判断
- 广告组问题定位
- 投放词结构
- 搜索词市场背景
- 广告位或下钻证据缺口
- 上下文边界

任何涉及广告操作的证据 detail 必须继续说明只允许人工复核，不能自动加词、否词、调价或暂停广告。
```

### 验证方式

```text
backend 目录中 python -m pytest tests\test_manual_action_preflight_cli.py -q -k "targets_next_unhandled_advertised_product"
backend 目录中 python -m pytest tests\test_manual_action_preflight_cli.py -q
backend 目录中 python -m pytest tests\test_manual_action_apply_cli.py -q
backend 目录中 python -m pytest tests\test_api_routes.py -q -k "manual_action"
backend 目录中 python -m py_compile app\services\manual_action_preflight.py
```

## 2026-06-17：全量排查入口直接输出可处理广告建议

### 问题

```text
用户指出当前 Web 结果数据口径混乱，信息、机会和证据链没有实际落地意义。

只读检查发现：
/api/signal-triage?product_scope_id=all 旧运行态会直接输出 recommended_candidate、manual_action_preview、top_candidates 和 next_action。

这会让全量混合视图看起来可以直接给广告处理建议，但 all 入口没有先锁定 Parent ASIN / ASIN 经营对象。
```

### 原因

```text
系统把“存在候选”误当成“可处理建议已成立”。

从 Amazon 广告操作路径看，运营通常先确定 Parent ASIN / ASIN 经营对象，再进入广告 ASIN、广告组、广告位、投放商品、投放词和搜索词。
全量排查入口混合了商品、未归因、数据质量和复盘待办，只能用于排查数据关系，不能承担业务建议入口。
```

### 解决

```text
新增经营对象准入闸门：
- 只有 parent_asin: / ad_asin: 作用域允许输出广告可处理建议；sales_asin: 只能作为销售背景或覆盖缺口上下文。
- all / 空作用域 / unattributed 只返回 product_scope_gate 和候选池数量。
- 非可处理作用域下清空 recommended_candidate、manual_action_preview、next_unhandled_candidate、candidate_layers 和 top_candidates。
- 前端本地推荐和 CLI 预检同步使用同一口径。
```

### 防错规则

```text
任何广告处理建议必须先有明确经营对象。

全量排查只能回答：
- 当前有多少候选池
- 为什么不能直接处理
- 下一步应选择哪个 Parent ASIN / ASIN 入口

不能在 all / unattributed 入口输出：
- 推荐处理对象
- 人工动作预览
- 机会扩量建议
- 否词、加词、调价、暂停等广告动作建议
```

### 验证方式

```text
backend 目录中 python -m pytest tests\test_signal_triage_service.py -q
backend 目录中 python -m pytest tests\test_signal_triage_cli.py -q
backend 目录中 python -m pytest tests\test_api_routes.py -q -k signal_triage
backend 目录中 python -m py_compile app\services\signal_triage.py ..\scripts\inspect_signal_triage.py
frontend 目录中 npm run test:signal-ui
frontend 目录中 npm run build

重启后端后请求：
/api/signal-triage?market_id=1&product_scope_id=all&top=5

期望：
status=requires_product_scope
signal_status.candidate_count=0
product_scope_gate.candidate_pool_count 保留真实候选池数量
recommended_candidate=null
manual_action_preview=null
top_candidates=[]
```

## 2026-06-17：Parent ASIN 空队列容易被误解为没有广告数据

### 问题

```text
parent_asin:B00K4W4AAA 当前没有可处理候选，但该 Parent 实际有销售和广告数据。

如果空队列只显示“暂无可处理命中”，用户仍然无法判断：
- 系统是否读到了 12 个子 ASIN
- 是否识别到当前投放广告 ASIN
- 广告花费、广告订单、广告销售是否存在
- 空队列是数据缺口还是规则未命中
```

### 原因

```text
空队列解释停留在状态层，没有把 Parent ASIN 的广告承接口径直接展示出来。
用户需要自己从顶部商品范围、诊断依据和全量候选之间拼关系，容易误判为系统漏数或证据链无意义。
```

### 解决

```text
Parent ASIN 空队列解释增加广告承接口径：
- 当前投放广告 ASIN 数
- 广告花费
- 广告订单
- 广告销售

有广告数据但未命中规则时，明确说明只能保持观察。
没有广告 ASIN 时，明确说明不能生成广告处理建议。
```

### 防错规则

```text
Parent ASIN 空队列不能只写“暂无候选”。

必须至少说明：
- 销售子 ASIN 数
- 当前投放广告 ASIN 数
- 广告花费 / 广告订单 / 广告销售
- 未投放子 ASIN 不进入广告信号队列
- 搜索词和广告位不能强行归入 ASIN
```

### 验证方式

```text
frontend 目录中 npm run test:signal-ui
frontend 目录中 npm run build

测试应覆盖：
Parent ASIN 少信号解释包含“广告承接口径”、花费、广告订单和广告销售。
```

## 2026-06-17：锁定 Parent 后深度检查仍混入全局信号

### 问题

```text
用户指出当前 Web 结果数据口径混乱、机会和证据链缺少实际落地意义。

只读复核发现：
parent_asin:B00K4W4AAA 当前商品范围下没有 AI 信号，但深度检查仍用全局 17 条信号计算问题层、证据层和建议层。

结果会把当前 Parent 的空结果误包装成全局“机会扩量 / 销售承接不足 / 人工建议”，用户无法判断当前 Parent 到底发生了什么。
```

### 原因

```text
inspect_analysis_depth.py 只把 product_scope_id 用于 product_scope 覆盖检查，没有把信号集合同步过滤到当前商品范围。
对象层、问题层、证据层和建议层使用了不同范围的数据，导致结论跨范围混用。
```

### 解决

```text
传入 product_scope_id 时：
- 先构建 product_scope_summary。
- 复用 signal_triage 的商品范围过滤逻辑。
- framework_layers 只使用当前商品范围内的 signals。
- 当前范围没有 AI 信号时，next_action 指向广告 ASIN / 广告组 / 投放词 / 搜索词 / 广告位只读下钻。
```

### 防错规则

```text
只要页面或脚本已经锁定 Parent ASIN / ASIN：
- 不得用全局 signal_count、candidate_count、problem_type_counts 解释当前对象。
- 不得把全局候选包装成当前 Parent 的机会。
- 当前范围无信号时，要说明“有商品/广告数据但未命中信号”，并给出下钻复核路径。
```

### 验证方式

```text
python -m pytest backend\tests\test_analysis_depth_cli.py -q

测试应覆盖：
- 当前 Parent 商品范围成立时，对象层不报“经营商品入口覆盖不足”。
- 当前 Parent 没有命中信号时，问题层和建议层不能显示全局机会与建议。
- next_action 指向当前 Parent 的广告下钻，而不是全局复盘窗口。
```

## 2026-06-17：Parent 空队列只有汇总，没有可落地广告下钻路径

### 问题

```text
用户指出当前 Web 结果数据口径混乱，信息、机会和证据链缺少实际落地意义。

B00K4W4AAA 当前没有可处理候选，但已有 3 个广告 ASIN 和广告组、搜索词、广告位上下文。
如果页面只显示广告 ASIN 数、花费、订单、销售，用户仍然不知道下一步该查哪个广告 ASIN、哪个广告组、哪些搜索词。
```

### 原因

```text
此前空队列解释只做到 Parent 汇总层：
- 说明有多少广告 ASIN。
- 说明合计花费、订单和销售。

但没有把 Amazon 广告操作路径继续落到：
- 广告 ASIN
- 广告组
- 投放词 / 搜索词
- 广告位上下文

这会让“暂无候选”看起来像没有业务结论，而不是一个可复核的广告诊断状态。
```

### 解决

```text
signal triage 增加 product_scope_drilldown：
- Parent / ASIN 作用域下输出实际投放广告 ASIN。
- 每个广告 ASIN 选择优先广告组。
- 优先广告组输出搜索词数量、有效搜索词、无订单花费搜索词。
- 广告位只输出同广告组或同广告活动上下文。
- 前端空队列解释消费该字段，显示“广告下钻”路径。
```

### 防错规则

```text
Parent ASIN 无候选时，不能只输出“暂无候选”或泛泛机会。

必须说明：
- 当前是否有广告 ASIN。
- 哪些广告 ASIN 有广告数据。
- 优先下钻哪个广告组。
- 搜索词和广告位是上下文，不是单 ASIN 自动归因。
- 没有可处理信号时只能观察或人工复核，不能自动执行广告动作。
```

### 验证方式

```text
backend 目录中 python -m pytest tests\test_signal_triage_service.py -q
backend 目录中 python -m pytest tests\test_signal_triage_cli.py tests\test_review_candidates_cli.py -q
frontend 目录中 npm run test:signal-ui
frontend 目录中 npm run build

真实快照验证：
python scripts\inspect_signal_triage.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --top 5

期望：
product_scope_drilldown.advertised_asin_count=3
items[0].asin=B016EXMVZS
items[0].top_ad_group.search_term_count=18
boundary 明确搜索词和广告位不能自动归因到单个 ASIN
```

## 2026-06-17：广告下钻只在空状态文案中展示，未进入中间证据链

### 问题

```text
product_scope_drilldown 已经能返回 Parent 下的广告 ASIN、广告组、有效搜索词、无订单花费词和归因边界。

但如果这些事实只出现在左侧空队列解释中，中间诊断区仍然显示“未选择信号”，用户还是无法把它当成正式证据链来复核。
```

### 原因

```text
中间 diagnosisPanel 原先只支持 selectedSignal。
当当前 Parent 作用域 signal_count=0 时，selectedSignal 为空，页面直接显示 EmptyState。

这导致“API 有广告下钻证据，但中间证据链不可见”的产品断层。
```

### 解决

```text
新增 productScopeDrilldownEvidenceItems：
- 将 product_scope_drilldown 转成和推荐证据相同形态的证据项。
- 无 selectedSignal 但有下钻数据时，中间 diagnosisPanel 显示“广告下钻证据链”。
- 证据项包括广告 ASIN 覆盖、优先广告 ASIN、优先广告组、有效搜索词、无订单花费词和归因边界。
```

### 防错规则

```text
凡是用于解释当前 Parent / ASIN 的业务证据，不能只放在左侧空状态或折叠诊断依据里。

如果该证据支撑用户下一步判断，必须进入中间诊断可见区。

前端只能消费后端下钻事实，不重新计算搜索词或广告位归因。
```

### 验证方式

```text
frontend 目录中 npm run test:signal-ui
frontend 目录中 npm run build

测试应覆盖：
product_scope_drilldown 能生成广告 ASIN 覆盖、优先广告组、有效搜索词、无订单花费词和归因边界证据项。
```

## 2026-06-17：广告组下钻缺少投放商品结构，容易误归因搜索词

### 问题

```text
Parent 无候选时，页面已经能展示优先广告 ASIN、优先广告组、有效搜索词和无订单花费词。

但如果不展示“该广告组投放了几个广告 ASIN”，用户会自然把同广告组搜索词理解成当前 ASIN 的独占搜索词表现。
这在多商品广告组里会制造错误归因。
```

### 原因

```text
product_scope_drilldown 原先只从当前广告 ASIN 的 advertised_products 行选出 top_ad_group。
它没有回到全部 advertised_products 行统计同广告组内还有哪些广告 ASIN。

因此证据链能回答“先查哪个广告组”，但不能回答“这个广告组是不是多商品容器”。
```

### 解决

```text
top_ad_group 新增：
- ad_group_advertised_asin_count
- ad_group_advertised_asins
- ad_group_attribution_boundary

前端中间证据链新增“广告组投放结构”节点，展示同广告组广告 ASIN 数、ASIN 列表和归因边界。
```

### 防错规则

```text
凡是展示同广告组搜索词、投放词或广告位上下文，必须同时说明广告组投放商品结构。

如果同广告组有多个广告 ASIN：
- 搜索词不能自动归因到单个广告 ASIN。
- 广告位不能自动归因到单个广告 ASIN。
- 建议只能是人工复核或记录观察，不能自动否词、自动拆广告组或自动调价。

如果同广告组只识别到 1 个广告 ASIN：
- 仍然按广告组上下文解释，不能绕过人工确认。
```

### 验证方式

```text
backend 目录中 python -m pytest tests\test_signal_triage_service.py -q
frontend 目录中 npm run test:signal-ui

真实 API 验证：
/api/signal-triage?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&top=5

期望：
items[0].top_ad_group.ad_group_advertised_asin_count=2
items[0].top_ad_group.ad_group_advertised_asins 包含 B016EXMVZS、B016EXMW02
items[0].top_ad_group.ad_group_attribution_boundary 明确不能自动归因到单个 ASIN
```
## 2026-06-17：非数据质量信号缺少对象层级判断边界

### 问题

```text
用户指出当前 Web 结果数据口径混乱，信息、机会和证据链缺少实际落地意义。

复核发现：
- 前端详情页的“判断边界”只对 data_quality 信号生效。
- 广告商品、搜索词、广告位、广告组和机会信号虽然有证据链，但没有统一说明当前对象层级能判断什么、不能判断什么。
- 结果是用户容易把搜索词、广告位或广告组上下文误读成可直接执行的广告动作或商品级结论。
```

### 原因

```text
后端候选摘要已经提供 problem_type / attribution_boundary / review_path，但前端详情页没有把对象层级边界作为所有信号的基础信息展示。
旧实现把“判断边界”当成数据质量专用区块，导致非数据质量信号仍像普通机会或异常文案。
```

### 解决

```text
signalDecisionBoundary 扩展到关键对象类型：
- ad_group：广告组是投放容器，不能直接归因为单个商品。
- search_term：搜索词只能作为投放判断对象，不能直接当作商品结论。
- placement：广告位只说明流量位置和预算效率。
- advertised_product：广告商品可以承接广告指标，但销售承接要继续看销售商品和同组上下文。
- sales_product：销售商品说明经营表现，广告动作必须回到广告 ASIN、广告组、投放词和搜索词复核。
- opportunity：只能进入人工观察或小流量验证，不自动执行广告动作。
```

### 防错规则

```text
所有信号详情都必须展示对象层级判断边界。

如果信号对象是广告组、搜索词或广告位：
- 不能直接写成商品级结论。
- 不能直接引导自动否词、自动拆广告组、自动调价。
- 必须说明人工复核路径。

如果信号是机会：
- 必须说明机会落在哪个对象层级。
- 必须说明只能人工观察或验证。
- 缺少对象、证据源或人工动作路径时，应降级为观察或数据质量提示。
```

### 验证方式

```text
frontend 目录：
npm run test:signal-ui
npm run build

浏览器验证：
http://localhost:5175/
全量排查下选中真实广告商品信号，详情页应出现：
“广告商品可以承接广告指标，但销售承接仍要结合销售商品和同组上下文复核。”
```

## 2026-06-17：机会队列标签泛化，掩盖对象层级

### 问题

```text
用户指出当前 Web 结果数据口径混乱，给出的信息、机会和证据链缺少实际落地意义。

复核发现：
- 当前真实扫描有 7 条 opportunity。
- 7 条 opportunity 全部是 search_term_opportunity。
- 7 条 opportunity 的 object_type 全部是 search_term。
- 但前端队列和分诊说明此前把所有 opportunity 统一显示为“机会”。

这会让用户无法第一眼判断：
- 这是搜索词机会，还是广告商品机会。
- 是 ABA 站点级市场机会，还是店铺广告表现机会。
- 是广告组结构复核，还是广告位预算效率复核。
```

### 原因

```text
signalCategoryLabel 只判断 signal_type=opportunity，没有继续读取 signal_category 和 object_type。
筛选层的“机会”可以是聚合入口，但单条信号不能只显示聚合标签。
单条信号必须先暴露对象层级，否则证据链会被用户误读成可直接操作的商品级结论。
```

### 解决

```text
signalCategoryLabel 按对象层级输出机会标签：
- search_term_opportunity -> 搜索词机会
- advertised_product_opportunity -> 广告商品机会
- aba_market_opportunity -> ABA市场机会
- placement_efficiency / placement -> 广告位机会
- ad_group_structure / ad_group -> 广告组结构机会
- 其他 opportunity -> 市场机会
```

### 防错规则

```text
以后新增或修改机会信号时：
- 筛选按钮可以叫“机会”，但单条信号卡片、详情分诊和建议区不能只叫“机会”。
- 单条机会必须显示对象层级。
- 单条机会必须能回答：看哪个对象、依据哪些证据、人工可以做什么、后续复盘看什么。
- 回答不出来时，不得进入 AI 信号队列，只能进入候选观察或数据质量提示。
```

### 验证方式

```text
frontend 目录：
npm run test:signal-ui
npm run build

真实数据复核：
python scripts\run_signal_scan.py --market-id 1

期望：
- 真实 search_term_opportunity 在 Web 队列中显示为“搜索词机会”。
- 广告商品、ABA、广告位、广告组结构机会不能被统一显示为泛化“机会”。
```

## 2026-06-17：ABA 短语背景放行低点击普通搜索词机会

### 问题

```text
用户指出当前 Web 结果数据口径混乱，机会和证据链缺少实际落地意义。

复核真实扫描发现：
- adult sunglasses womens sporty 只有 1 次点击、2 个订单。
- beach essentials for toddlers 1-3 只有 2 次点击、2 个订单。
- 这类普通搜索词因为命中 ABA 热词短语背景，被生成 search_term_opportunity。

这会让用户误以为站点级 ABA 背景足以支撑店铺广告机会判断。
```

### 原因

```text
has_actionable_search_term_opportunity_support 旧逻辑把 aba_support_row 或 aba_phrase_context_row 当作机会准入条件。
但 ABA 是站点级市场数据，只能说明市场热度背景，不能替代当前店铺广告侧样本量。
当广告侧只有 1-2 次点击时，订单和 ACOS 都可能是低样本偶然现象，不应该进入 AI 信号队列。
```

### 解决

```text
普通搜索词机会新增广告侧最小样本闸门：
- 非 ASIN 型搜索词必须 clicks >= SEARCH_TERM_MIN_ACTIONABLE_CLICKS。
- ABA 精确匹配或短语匹配只增强证据链和置信度，不单独放行低点击普通词。
- ASIN 型搜索词保留商品定向复核路径，但建议动作必须是 review_product_targeting_search_term，不能写成普通关键词扩量。
```

### 防错规则

```text
以后新增或修改搜索词机会规则时：
- ABA 不能作为普通搜索词机会的唯一准入证据。
- 低点击普通词即使命中 ABA 热词，也只能进入候选观察或语义背景，不能进入 AI 信号队列。
- 如果要进入 AI 信号队列，必须先满足广告侧最小样本，再补 ABA、语义组或多投放上下文证据。
- ASIN 型搜索词必须和普通关键词机会分开，走商品定向复核路径。
```

### 验证方式

```text
backend 目录：
python -m pytest tests\test_signal_detection.py -q

真实数据复核：
python scripts\run_signal_scan.py --market-id 1

期望：
- 低点击普通长尾词不再生成 search_term_opportunity。
- ABA 短语背景仍可在满足最小广告侧样本后进入证据链。
- 真实扫描 opportunity_count 从 7 收敛到 5。
```

## 2026-06-17：搜索词机会缺少人工动作路径和复盘指标

### 问题

```text
用户指出当前 Web 结果的数据口径混乱，给出的信息、机会和证据链没有实际落地意义。

复核真实扫描发现：
- 当前仍有 5 条 search_term_opportunity。
- 这些机会已经区分普通搜索词和 ASIN 型搜索词。
- 但 evidence.facts 没有统一说明人工动作路径和 7/14 天复盘指标。

这会让用户只能看到“有机会”，却不知道下一步应该记录观察、加入关键词候选、小流量测试，还是商品定向复核。
```

### 原因

```text
旧逻辑把 suggested_action 当成唯一处理说明。
但 suggested_action 更像摘要，不足以成为证据链中的可复盘口径。

机会信号如果缺少复盘指标，用户无法判断建议是否带来改善，也无法形成“AI 信号 -> 人工确认 -> 7/14 天复盘”的闭环。
```

### 解决

```text
搜索词机会新增统一证据项：
- 人工动作路径
- 复盘指标

覆盖场景：
- 普通搜索词机会：人工确认后加入精准关键词候选或小流量观察。
- ASIN 型搜索词机会：只进入商品定向或自动投放上下文复核。
- 合并搜索词机会：逐广告活动、广告组和广告商品复核，不能用合并结果直接同步放量。
- 长尾机会：小流量观察，7/14 天复盘点击、订单、ACOS、CVR 和是否重复出现。
```

### 防错规则

```text
以后新增或修改 opportunity 信号时：
- 不能只写 summary / why / suggested_action。
- 必须在 evidence.facts 中提供“人工动作路径”和“复盘指标”。
- ASIN 型搜索词必须出现商品定向复核路径。
- 合并机会必须说明逐广告活动、广告组和广告商品复核。
- 缺少复盘指标的机会不得进入建议优先留痕对象。
```

### 验证方式

```text
backend 目录：
python -m pytest tests\test_signal_detection.py -q

真实数据复核：
python scripts\run_signal_scan.py --market-id 1

期望：
- signal_count 不因补充证据而增加。
- opportunity_count 维持当前真实口径。
- 每条 opportunity 的 evidence.facts 都包含“人工动作路径”和“复盘指标”。
```

## 2026-06-17：证据链停留在展示层，未进入人工动作和复盘上下文

### 问题

```text
用户指出当前 Web 结果数据口径混乱，给出的信息、机会和证据链没有实际落地意义。

本轮复核发现两个层面的问题：
1. “人工动作路径”和“复盘指标”虽然已进入 search_term opportunity 的 facts，但没有贯穿到 evidence_drilldown、manual_action evidence_snapshot 和 review todo 的 ReviewContext。
2. B00K4W4AAA 范围内有广告 ASIN、广告组和搜索词上下文数据，但 candidate_count=0，manual_action_preflight 仍 missing_target。
```

### 原因

```text
旧实现把证据展示、人工留痕、复盘待办当成相邻模块处理，没有把“可行动字段”作为跨模块合同。

同时，product_scope_drilldown 和 candidate 仍是两个层级：
- product_scope_drilldown 说明经营对象下有哪些广告数据。
- candidate 才说明哪些对象可以进入人工处理。

当前页面容易把前者误读成后者，造成“看起来分析很多，但不能落地”的体验。
```

### 解决

```text
已修复最小闭环：
- signal_triage 搜索词证据钻取追加 manual_action_path / review_metrics block。
- ReviewContext 增加 manual_action_path / review_metrics。
- review_context_for_manual_action 从人工留痕快照提取“人工动作路径”和“复盘指标”。
- manual_action_preflight 优先保留这两类可行动证据。
- 前端 ReviewContextForUi 和 ReviewContext 支持这两个字段。
- reviewContextText 显示“动作路径”和“复盘指标”。
- buildSignalManualActionEvidenceSnapshot 从 signal facts 补充这两条留痕证据。
```

### 防错规则

```text
以后凡是新增机会或异常信号，不能只检查页面是否显示 facts。

必须同时确认：
- evidence_drilldown 能看到可行动证据。
- manual_action 写入时能保存这份证据。
- review todo 能读出动作路径和复盘指标。
- candidate_count=0 时不得伪装成可处理建议。
- product_scope_drilldown 只能作为经营对象诊断，不等于人工处理候选。
```

### 验证方式

```text
后端：
python -m pytest tests\test_manual_actions.py tests\test_signal_triage_service.py -q

前端：
npm run test:signal-ui
npm run build

真实范围复核：
python scripts\inspect_signal_triage.py --market-id 1 --top 10 --product-scope-id parent_asin:B00K4W4AAA
python scripts\inspect_manual_action_preflight.py --market-id 1 --top 10 --product-scope-id parent_asin:B00K4W4AAA

当前事实：
- B00K4W4AAA 有 product_scope_drilldown。
- candidate_count=0。
- manual_action_preflight blocked：missing_target。
```

## 2026-06-17：把 sales-only ASIN 当成广告可行动范围

### 问题

```text
用户指出当前 Web 结果数据口径混乱，给出的信息、机会和证据链无实际落地意义。

本轮复核发现：
- product scope 共 241 个。
- 其中 194 个 sales_asin 没有被明确降级为销售背景。
- 前端 preferredProductScopeId / isActionableProductScope / buildProductScopeOptionGroups 会把 sales_asin 放入可行动商品范围。
- 后端和 CLI 的 ACTIONABLE_PRODUCT_SCOPE_PREFIXES 也包含 sales_asin。

结果是：
没有广告数据的销售 ASIN 看起来也能进入广告 AI 分析和人工动作预览，污染广告分析口径。
```

### 原因

```text
旧实现把“经营商品范围”和“广告分析对象”混成一个 product_scope。

第一性原理上：
- SalesProduct 是经营商品。
- AdvertisedProduct 是广告投放商品。
- sales-only ASIN 没有广告商品、广告组、搜索词、广告位证据。
- 没有广告证据时，不能往下做广告问题诊断或广告调整建议。

sales-only ASIN 只能作为：
- Parent ASIN 经营盘子背景。
- 广告覆盖缺口的候选背景。
- 不可直接人工执行广告动作的解释对象。
```

### 解决

```text
已修复：
- 前端默认选项不再回退到 sales_asin。
- 前端可行动范围只允许 parent_asin 和 advertised_asin。
- 前端下拉分组改为“广告分析入口”，不再展示 sales_asin。
- sales_asin 标签改为“销售背景”。
- 后端服务和 CLI 的可行动前缀移除 sales_asin。
- sales_asin 直接请求时返回 requires_product_scope，并提示“销售背景 ASIN”。
```

### 防错规则

```text
以后涉及 product_scope 时必须先问：
- 这个范围有没有广告数据？
- 这个范围是经营背景，还是广告分析对象？
- 如果没有 advertised_products 证据，是否只能作为覆盖缺口背景？

禁止：
- 把 sales_asin 直接放入广告 AI 信号可行动入口。
- 把 sales-only 子 ASIN 包装成广告调整建议。
- 在没有广告商品证据时生成 manual_action_preview。

允许：
- Parent ASIN 页面展示 sales-only 子 ASIN 作为经营盘子背景。
- Parent ASIN 范围内出现“销售强但广告覆盖弱”的覆盖缺口候选。
- 但后续广告处理必须落到 Parent / ad_asin / 广告组 / 投放词 / 搜索词 / 广告位证据。
```

### 验证方式

```text
后端：
python -m pytest tests\test_signal_triage_service.py tests\test_signal_triage_cli.py -q

前端：
npm run test:signal-ui
npm run build

真实数据复核：
python scripts\inspect_signal_triage.py --market-id 1 --top 5 --product-scope-id sales_asin:B016EXMW1G

期望：
- sales_asin 返回 requires_product_scope。
- can_write_manual_action=false。
- message 包含“销售背景 ASIN”。
- 真实分布中 sales_asin|requires_product_scope=194。
```

## 2026-06-17：原型仍以信号列表优先导致业务路径不清

### 问题

```text
用户指出当前 Web 结果数据口径混乱，给出的信息、机会和证据链无实际落地意义。
随后用户要求围绕目标重新绘制原型图，而不是盲目开展项目。

这说明当前原型虽然有 AI 信号队列、证据链和建议处理，但首屏仍容易让用户误解：
- 系统已经发现了可处理机会。
- 经营商品、广告商品、广告组、搜索词和 ABA 处在同一分析层级。
- 只要展示了信息，就能继续给广告调整建议。
```

### 原因

```text
旧原型把“AI 信号队列”作为第一入口，用户必须点开后才能理解对象边界。

从第一性原理看，广告诊断应先回答：
- 当前经营入口是什么？
- 哪些子 ASIN 真有广告数据？
- 广告组是否多商品？
- 搜索词和广告位是否只能作为上下文？
- ABA 是否只是站点级市场背景？
- 这条判断是否能进入人工动作和 7/14 天复盘？

如果这些问题没有在首屏路径中表达清楚，继续开发功能只会放大错误口径。
```

### 解决

```text
已新增目标版原型：
- design/ai-copilot-workbench-v2.html
- design/ai-copilot-workbench-v2.png

v2 从“信号列表优先”调整为“广告诊断路径优先”：
Parent ASIN 经营盘
→ 广告 ASIN 覆盖
→ 广告组结构
→ 广告位 / 投放词
→ 搜索词 / ABA 证据
→ AI 信号准入
→ 人工确认 / 复盘

右侧建议处理增加门禁表达：
candidate_count=0 时，只展示观察和下钻路径，不生成广告调整建议。
```

### 防错规则

```text
以后做工作台页面或信号展示时：
- 不能先展示机会列表再让用户猜对象关系。
- Parent ASIN 页面必须先展示广告覆盖和证据层级。
- 搜索词、广告位、ABA 默认是证据和上下文，不能直接包装成商品级建议。
- 没有主对象、经营问题、证据强度、归因边界和复盘指标时，不得进入建议处理。
- 原型方向未确认前，不继续扩大功能实现。
```

### 验证方式

```text
人工视觉检查：
打开 design/ai-copilot-workbench-v2.png。

期望：
- 首屏能看到当前诊断上下文。
- 左侧是广告诊断路径，而不是单纯信号列表。
- 中间有对象证据矩阵。
- 右侧在无候选时明确不生成广告调整建议。
- 页面没有自动调价、自动暂停、自动否词、自动新增关键词等按钮。
```

## 2026-06-17：v2 准入门禁只停留在原型图，Web 仍可能让用户误判

### 问题

```text
v2 原型已经明确 candidate_count=0 时不能生成广告调整建议。

但如果当前 Web 只在空队列里显示“暂无信号”或只展示广告下钻证据，用户仍可能误解为：
- 系统没有读取到数据。
- 或系统读到数据但暗示存在某种机会。
- 或当前广告下钻证据可以直接进入人工动作。
```

### 原因

```text
准入门禁如果只存在于设计图或右侧文案中，不会成为真实交互约束。

第一性原理上：
- 先判断能不能进入 AI 信号准入。
- 再展示证据链。
- 最后才允许建议处理和人工确认。

没有 candidate、manual_action_preview 或复盘对象时，页面必须先解释为什么不能行动。
```

### 解决

```text
已新增前端准入卡片：
- buildProductScopeAdmissionCard
- ProductScopeAdmissionCardPanel

当前 Web 在无选中信号时优先展示：
- AI 信号准入状态
- 结论
- 下一步
- 人工动作边界
- 广告下钻证据摘要

candidate_count=0 时明确展示：
不能写人工动作，只允许查看经营诊断、广告下钻证据和数据缺口。
```

### 防错规则

```text
以后只要页面展示 product_scope_drilldown，就必须同时展示 actionability_status。

禁止：
- 只展示广告 ASIN / 广告组 / 搜索词证据，但不说明能否行动。
- candidate_count=0 时显示建议处理按钮或广告调整建议。
- 把“暂无信号”写成像系统没读到数据。

允许：
- 显示经营诊断、广告覆盖和证据缺口。
- 引导用户继续下钻或记录观察。
- 等后端返回 ready_for_manual_confirmation 后再开放人工确认入口。
```

### 验证方式

```text
前端：
npm run test:signal-ui
npm run build

浏览器：
打开 http://localhost:5175/
选择 Parent ASIN B00K4W4AAA。

期望：
- 中间诊断区出现“AI 信号准入：暂不进入人工动作”。
- 展示 candidate_count=0 的禁写边界。
- 展示广告 ASIN 覆盖、优先广告 ASIN 和优先广告组。
- 不出现自动广告执行动作。
```

## 2026-06-18：顶部和左侧不展示诊断路径会削弱落地判断

### 问题

```text
上一轮 Web 已经在中间诊断区展示 AI 信号准入门禁。

但顶部商品范围区和左侧队列仍没有直接展示：
- 当前诊断上下文。
- 当前诊断路径。
- 当前 Parent ASIN 下实际广告 ASIN 覆盖。
- candidate_count=0 时为什么只能诊断。

用户进入页面后仍需要展开详情或读中间区域，才能理解当前不是可行动建议，而是经营对象诊断。
```

### 原因

```text
v2 原型要求路径优先，但 Web 实现还把路径信息放在折叠详情或中间证据区。

第一性原理上，广告 AI 工作台必须先回答：
当前看哪个经营对象？
哪些对象有广告数据？
证据能下钻到哪个广告组、搜索词和广告位？
能不能进入人工动作？

如果这些信息不在首屏，运营会把空队列误读为没数据，或把广告下钻误读为广告调整建议。
```

### 解决

```text
已新增：
- buildDiagnosisContextSummary
- buildDiagnosisPathSummary
- 顶部“当前诊断上下文”
- 左侧“当前诊断路径”

Parent ASIN B00K4W4AAA 下现在可见：
- 销售表现子 ASIN 12 个。
- 广告 ASIN 3 个。
- 优先广告组 RBK004-kids sunglasses-广泛。
- 搜索词 18 条 / 广告位 0 条。
- AI 准入只能诊断，候选 0 个。
- candidate_count=0，不能写人工动作。
```

### 防错规则

```text
以后只要页面进入 Parent ASIN / ASIN 经营入口，首屏必须展示：
- 当前诊断上下文。
- 当前诊断路径。
- 广告 ASIN 覆盖。
- AI 准入状态。
- 可处理候选数。

禁止：
- 只在折叠详情里展示分析路径。
- 只展示信号队列而不说明当前层级。
- candidate_count=0 时只写“暂无信号”。
- 把搜索词或广告位上下文包装成单个 ASIN 的直接归因。
```

### 验证方式

```text
前端：
npm run test:signal-ui
npm run build

浏览器：
打开 http://localhost:5175/
选择 Parent ASIN B00K4W4AAA。

期望：
- 顶部可见“当前诊断上下文”。
- 左侧可见“当前诊断路径”。
- 可见 Parent ASIN B00K4W4AAA、3 个广告 ASIN、只能诊断、候选 0 个。
- 可见 RBK004-kids sunglasses-广泛。
- 新增区域没有横向溢出。
```

## 2026-06-18：中间诊断区只展示证据条仍不足以落地

### 问题

```text
顶部和左侧已经能说明当前诊断上下文和路径。

但中间诊断区如果只展示：
- AI 信号准入卡片
- 广告下钻证据链

用户仍需要自己拼接：
Parent ASIN 是经营入口，
广告 ASIN 是有广告数据的对象，
广告组只是容器，
搜索词和广告位只是上下文，
candidate_count=0 时不能人工动作。

这会让页面看起来“有证据”，但证据关系仍不够直观。
```

### 原因

```text
广告 AI 工作台的第一性原理不是展示更多指标，而是回答：
当前对象是谁？
哪些证据能直接归属到这个对象？
哪些只是上下文？
现在能不能进入人工处理？

证据条只能表达单点事实，不能稳定表达对象层级和准入门禁。
```

### 解决

```text
已在 Web 中间诊断区接入对象证据矩阵：
- 经营入口
- 广告 ASIN
- 广告组
- 搜索词/广告位
- AI 准入

当前 B00K4W4AAA 页面可见：
- 对象证据矩阵。
- Parent ASIN -> 广告 ASIN -> 广告组 -> 搜索词/广告位 -> AI 准入。
- candidate_count=0，不能写人工动作。
- 搜索词和广告位不能自动归因到单个 ASIN。
```

### 防错规则

```text
Parent ASIN / ASIN 诊断中间区必须展示对象证据矩阵。

禁止：
- 只堆证据条或指标卡。
- 把广告组写成商品。
- 把搜索词或广告位写成单个 ASIN 的直接归因。
- candidate_count=0 时在右侧生成广告调整建议。
```

### 验证方式

```text
前端：
npm run test:workbench-structure
npm run test:signal-ui
npm run build

浏览器：
打开 http://localhost:5175/
选择 Parent ASIN B00K4W4AAA。

期望：
- 中间区可见“对象证据矩阵”。
- 可见 5 个证据层级。
- 可见 RBK004-kids sunglasses-广泛。
- 可见 candidate_count=0。
- 桌面和 390 x 844 窄屏均无横向溢出。
```
## 2026-06-18：候选存在不等于业务可落地

### 问题

```text
页面或脚本只显示有 AI 信号候选时，容易误判为版本已经能落地。
但真实落地至少还需要：
- 稳定人工处理对象
- 可追溯证据来源
- 人工动作路径
- 7/14 天复盘指标
- 归因边界

本轮真实审计发现：修复前 market_id=1 全局 14 条候选中只有 5 条满足落地合同，9 条缺少人工动作路径和复盘指标。
同时 Parent ASIN B00K4W4AAA 仍然 candidate_count=0，不能因为全局候选 ready 就说该 Parent ASIN 已落地。
```

### 原因

```text
旧口径只审计“是否有候选”，没有审计“候选是否能进入人工确认和 7/14 天复盘”。
部分 advertised_product_efficiency 候选已经有对象、证据来源和归因边界，但证据链没有稳定暴露人工动作路径和复盘指标。
```

### 解决

```text
新增 scripts/inspect_candidate_actionability_contract.py，只读审计每条候选的落地合同。
后端 candidate payload 统一补齐 manual_action_path 和 review_metrics，避免不同信号规则漏填。
真实审计修复后：market_id=1 全局 candidate_count=14，ready_contract_count=14。
```

### 防错规则

```text
以后判断版本能否落地，不能只看页面是否有信号或候选数。
必须同时检查：
- 当前用户选择的 product_scope 是否有候选
- 候选是否通过 actionability contract
- 是否存在 7/14 天复盘指标
- 是否明确禁止自动广告动作

全局 ready 不代表某个 Parent ASIN ready。
Parent ASIN candidate_count=0 时，页面只能展示诊断、下钻路径和空态原因，不能生成广告调整建议。
```

### 验证方式

```text
python scripts\inspect_candidate_actionability_contract.py --market-id 1
期望：status=ready 且 ready_contract_count=candidate_count。

python scripts\inspect_candidate_actionability_contract.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA
期望：如 candidate_count=0，则必须展示 empty 原因，不能展示人工动作建议。
```

## 2026-06-18：Parent ASIN 空候选不等于没有广告数据

### 问题

```text
用户质疑当前版本是否真的能落地时，B00K4W4AAA 的 candidate_count=0 容易被误读为：
- 没有广告数据。
- 系统不可用。
- 当前商品没有分析价值。

但真实下钻显示该 Parent ASIN 有广告 ASIN、广告花费、广告订单和搜索词上下文。
空候选的真实含义是：当前规则没有命中可进入人工确认的广告对象级候选。
```

### 原因

```text
旧的 no_actionable_candidate 文案只说“没有候选”或“只能诊断”，没有把广告 ASIN 数、花费、订单和规则未命中原因展示出来。
这会让运营无法判断下一步是补数据、下钻证据，还是调整规则口径。
```

### 解决

```text
后端 actionability_status 在 product_scope_drilldown ready 时，会输出：
- 广告 ASIN 数。
- 广告花费。
- 广告订单。
- 搜索词上下文。
- 无订单花费词样本。
- empty_reason_code=rules_not_triggered_with_ad_data。

右侧空态继续禁止人工动作和广告调整建议，只允许诊断、下钻或补充规则/策略口径。
```

### 防错规则

```text
以后看到 Parent ASIN / ASIN candidate_count=0 时，必须先区分：
1. 没有广告 ASIN 投放行。
2. 有广告数据但规则未命中。
3. 有候选但被人工动作合同拦截。

禁止：
- 把 candidate_count=0 简写成“暂无信号”。
- 把有广告数据但规则未命中的状态说成“数据不足”。
- 为了让页面看起来可落地而伪造广告调整建议。
```

### 验证方式

```text
python scripts\inspect_signal_triage.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA

期望：
- actionability_status.status=no_actionable_candidate。
- message 包含广告 ASIN、广告花费、广告订单和“现有信号规则没有命中”。
- next_step 只引导下钻或补规则/策略口径，不生成广告调整建议。
```

## 2026-06-18：运行中后端未更新会导致页面落地判断失真

### 问题

```text
脚本已返回新的 Parent ASIN 空候选原因，但浏览器使用的后端 API 一开始仍返回旧文案。

这会让用户看到的页面与当前代码、测试和只读脚本结论不一致，从而误判版本是否能落地。
```

### 原因

```text
后端 uvicorn 进程没有使用 reload，旧进程启动时间早于最新代码变更。
前端 5175 仍正常运行，但实际 API 默认请求 127.0.0.1:8000。
如果只看页面，不核对后端进程和 API 返回，会把旧运行态当成当前版本事实。
```

### 解决

```text
已重启 127.0.0.1:8000 后端服务。
重启后 /api/signal-triage 返回：
- 广告 ASIN 3 个。
- 广告花费 764.11。
- 广告订单 281。
- empty_reason_code=rules_not_triggered_with_ad_data。
```

### 防错规则

```text
以后判断 Web 版本是否落地，必须同时核验：
1. 前端服务是否可访问。
2. 后端 API 是否返回当前代码口径。
3. CLI 脚本和 API 返回是否一致。

禁止只凭浏览器截图判断当前代码是否已经生效。
```

### 验证方式

```text
GET http://127.0.0.1:8000/api/signal-triage?market_id=1&product_scope_id=parent_asin:B00K4W4AAA

期望：
- actionability_status.status=no_actionable_candidate。
- actionability_status.empty_reason_code=rules_not_triggered_with_ad_data。
- message 包含广告 ASIN 3 个、广告花费 764.11、广告订单 281。
```

## 2026-06-18：原型偏对象矩阵会削弱业务落地路径

### 问题

```text
v2 原型已经说明 candidate_count=0 时不能生成广告调整建议，但中间区仍偏“对象证据矩阵”。
这会让用户看懂对象边界，却不一定知道下一步应如何按运营习惯继续排查广告问题。
```

### 原因

```text
真实运营通常先看 Parent ASIN 整体销售，再进入广告 ASIN、广告组、广告位、投放词和搜索词。
如果第一屏只展示对象定义，而没有把广告组和搜索词证据放入同一条排查路径，页面仍会像分析说明页，而不是可落地的工作台。
```

### 解决

```text
新增 v3 原型：
- 左侧固定为运营决策路径。
- 中间展示广告 ASIN 承接、广告组问题定位、有效搜索词和无订单花费词对比、广告位 / ABA 边界。
- 右侧展示 AI 准入门禁、人工动作禁用原因和 7/14 天复盘口径。
```

### 防错规则

```text
以后设计首屏时，不能只证明“对象边界是对的”。
必须同时证明：
1. 用户从哪里进入。
2. 用户下一步看哪个广告对象。
3. 哪些证据足以行动，哪些只能观察。
4. 为什么能或不能进入人工处理和复盘。
```

### 验证方式

```text
打开 design/ai-copilot-workbench-v3.html。
期望首屏可见：
- Parent ASIN 销售盘。
- 广告 ASIN 承接。
- 广告组问题定位。
- 有效搜索词和无订单花费词对比。
- AI 准入门禁。
- 禁止自动广告动作和复盘口径。
```

## 2026-06-18：搜索词列表重复 key 警告

```text
问题：
浏览器 QA 中发现“搜索词问题定位”模块出现 React 重复 key 警告。

原因：
同一个广告组内可能出现重复搜索词文本，原 key 只使用 rowId + 分组 + label，不能保证唯一。

解决：
搜索词列表 key 改为 rowId + 分组 + index + label。
新增 signalWorkbenchStructure 静态测试，防止 key 模板退回到只使用 label。

后续防错：
真实广告数据里搜索词文本不保证唯一，前端列表 key 不能只依赖搜索词文本。
```

## 2026-06-18：PowerShell 重启后端时不要使用 `$PID` 作为循环变量

```text
问题：
重启 127.0.0.1:8000 后端时，脚本使用 foreach ($pid in $pids)，触发 PowerShell 只读变量冲突，旧 uvicorn 进程未被正确停止。

原因：
$PID 是 PowerShell 内置只读变量，不能作为循环变量覆盖。
旧进程未停止会导致浏览器和 API 仍返回旧 actionability_status，误判当前代码没有生效。

解决：
改用 $procId 等非保留变量名停止监听 8000 的进程，再启动 uvicorn。

后续防错：
判断运行态是否生效时，必须同时核对：
1. 当前监听 8000 的进程。
2. API 是否返回新字段。
3. 浏览器刷新后是否看到新字段。
```

## 2026-06-18：历史人工动作证据快照为空不能误判新写入链路失败

```text
问题：
复核 /api/review-todos?market_id=1 时，历史 review_todos 中 evidence_snapshot 为空，容易误判当前人工动作闭环仍无法支撑复盘证据追溯。

原因：
这些记录来自早期写入，当时还没有强制保存 preflight evidence_snapshot。
当前代码已经要求复盘类人工动作必须带 evidence_snapshot；CLI 和 API 都会阻止空证据写入。

解决：
用临时 action_root / review_root 对 ad_asin:B0GL8QDHW5 做隔离写入验收：
- preflight evidence_snapshot_preview.item_count=10。
- execute 后 written_record.evidence_snapshot=10 条。
- post_write_validation.target_review_todo_count=2。
- review_records=0，符合复盘窗口未到期前不保存结论的规则。

后续防错：
判断人工复盘闭环时必须区分：
1. 历史遗留 manual_actions 是否缺 evidence_snapshot。
2. 当前新写入链路是否强制 evidence_snapshot。
3. review_todos 是否从最新 manual_action 继承 evidence_snapshot。
4. review_records 是否只在 7d / 14d 窗口 ready 后保存。
```

## 2026-06-18：推荐对象切换时必须先核对 Parent 子 ASIN 范围

```text
问题：
复核 parent_asin:B07C32KY1R 时，recommended_candidate 为 B07QXY6573，但 manual_action_preview 指向 B0GL8QDHW5，表面看像人工动作目标越过了当前 Parent 范围。

原因：
B07QXY6573 已有人工留痕后，系统会把下一次可写 manual_action_preview 切到 next_unhandled_candidate。
只看推荐对象和预览对象不同，不能直接判断为越界，必须核对当前 Parent 的 child_asins。

事实：
product_scope 显示 B0GL8QDHW5 属于 parent_asin:B07C32KY1R 的 child_asins。
因此本次不是跨 Parent 越界，而是同 Parent 内下一未处理候选。

后续防错：
凡是发现 recommended_candidate、next_unhandled_candidate 和 manual_action_preview 不一致，必须先检查：
1. recommended 对象是否已有 manual_action / review_todos。
2. next_unhandled_candidate 是否在当前 product_scope 的 child_asins 或 ad_asin 范围内。
3. preflight target 是否与 manual_action_preview 一致。
4. 不得只按 ASIN 不同就判断为对象越界。
```

## 2026-06-18：有广告数据但无候选时不能只显示空态

```text
问题：
B00K4W4AAA 有广告 ASIN、广告组、搜索词和广告位上下文，但 candidate_count=0。
如果页面只显示“暂不进入人工动作”或“规则未命中”，用户仍不知道是没数据、规则太严，还是系统没有能力分析。

原因：
no_actionable_candidate 只说明动作准入失败，不等于诊断解释完成。
对于真实运营，必须说明每个广告 ASIN 为什么未触发异常、为什么机会规则暂缓，以及下一步应继续看哪个广告组或搜索词。

解决：
product_scope_drilldown 新增 candidate_gap_analysis：
- anomaly_check 解释广告 ASIN 异常为什么未命中。
- opportunity_check 解释稳定转化机会为什么暂缓。
- result 固定为诊断不准入。
- boundary 明确 candidate_count=0，不写人工动作。

后续防错：
1. 有广告下钻数据但 candidate_count=0 时，必须展示候选缺口解释。
2. 候选缺口解释不能生成广告调整建议。
3. 稳定转化机会必须等库存、利润、价格、主推策略等独立证据补齐后再进入候选。
4. 不允许为了让页面“看起来能处理”而伪造观察型信号或人工动作入口。
```

## 2026-06-18：Parent 范围不能只按 ASIN 过滤搜索词候选

```text
问题：
B00K4W4AAA 全局已有搜索词候选，例如 beach essentials、boys sunglasses、baby sunglasses。
但 Parent 范围此前 candidate_count=0，用户从 Parent 经营入口进入时看不到这些同广告组搜索词候选。

原因：
搜索词行通常不带 ASIN。
旧过滤只看 signal evidence 里的 asin / parent_asin，导致搜索词候选无法通过 Parent child_asins 匹配进入商品范围。

解决：
在 signal_triage 过滤层用当前快照 advertised_products 行建立广告组上下文映射：
Parent child ASIN -> advertised_products 广告组上下文 -> search_term source_rows。
搜索词候选命中同广告组上下文时进入 Parent 范围。

边界：
这不是把搜索词归因到单个 ASIN。
它只表示该搜索词候选发生在当前 Parent 相关广告 ASIN 所在的广告组上下文里。
人工处理前必须复核投放词、搜索词、广告商品和策略边界。

后续防错：
1. Parent 范围过滤搜索词候选时，不能只依赖 ASIN 字段。
2. 搜索词候选必须单独分层为“搜索词候选”，不能写成“广告 ASIN 待处理”。
3. 推荐理由必须包含“广告组上下文”和“不能自动归因到单个 ASIN”。
4. 不得因此自动否词、自动加词或自动调价。
```

## 2026-06-18：前端商品范围队列必须接入后端准入候选

```text
问题：
B00K4W4AAA 后端已经返回 ready_for_manual_confirmation，candidate_count=3。
但前端队列仍可能显示 Parent ASIN 广告分诊 0 条，用户会误判当前版本不能落地。

原因：
前端 productScopedSignals 只按本地 ASIN / parent_asin 过滤。
搜索词候选通常不带 ASIN，只通过后端分诊合同确认属于当前 Parent 的广告组上下文。
如果前端只使用本地过滤结果，就会把后端已准入的 search_term 候选挡在队列外。

解决：
新增 mergeBackendTriageSignals。
前端展示队列使用“本地商品范围信号 + 后端分诊摘要里的 recommended / next_unhandled / candidate_layers signal_id”。
只合并后端已经准入的真实 signal，不伪造归因。

后续防错：
1. 判断页面是否落地时，必须同时看 API actionability_status 和前端队列展示。
2. 后端 ready 候选必须能在当前 Parent 队列里被选中。
3. 合并候选只发生在展示层，不改变 signalProductScopeIds 的对象归因规则。
4. 搜索词候选仍必须展示归因边界，不能转成自动加词、否词、调价或暂停动作。
```

## 2026-06-18：人工确认前不能只展示证据摘要

```text
问题：
beach essentials 的后端 preflight 已经返回 8 条 evidence_snapshot_preview。
但前端右侧此前主要展示一段证据摘要，用户无法逐条审计将被保存的证据、来源和边界。

原因：
证据快照已经进入 preflight 合同，但 UI 只消费 manualActionPreflightEvidenceSnapshotText 的压缩文案。
这会让人工确认看起来像“系统说可以点”，而不是“运营确认这些证据后再点”。

解决：
新增 manualActionPreflightEvidenceRows，并在右侧人工确认区展示“将保存的证据快照”列表：
- label / value / detail / source。
- 后端 boundary。
- 不改变写入逻辑，不自动执行广告动作。

后续防错：
1. 人工确认前必须能看到将随 manual_action 保存的证据快照。
2. 证据列表必须保留来源字段，不能只展示结论。
3. 搜索词候选必须展示对象边界，不能把搜索词写成单 ASIN 归因。
4. preflight 证据只用于人工确认和后续复盘回看，不代表自动加词、否词、调价或暂停。
```

## 2026-06-18：人工按钮点击前必须展示写后验收合同

```text
问题：
右侧人工确认区已有证据快照预览，但用户点击前仍不够明确：点击后到底要读回几条人工留痕、几条复盘待办、是否会保存复盘结论、是否可能触发广告动作。

原因：
这些信息已经存在于后端 preflight 的 expected_after_write 和 forbidden_effects 中，但前端此前主要用固定文案展示“写后预期”，没有把当前对象的验收数量前置展示。

解决：
新增 manualActionPostWriteContractItems。
右侧人工确认区展示“点击后验收合同”：
- 人工留痕：预计 1 条。
- 复盘待办：预计 2 条，7d / 14d，并继承证据快照。
- 复盘结论：不保存。
- 广告动作：禁止自动执行。

后续防错：
1. 人工动作按钮前必须展示写后验收合同。
2. 验收数量必须来自后端 preflight，不使用前端硬编码替代当前对象事实。
3. 复盘结论必须明确晚于 7/14 天完整窗口，不能在点击动作时保存。
4. 写后合同必须同时展示禁止自动广告动作，避免用户误解为系统会自动执行调整。
```

## 2026-06-18：人工动作按钮必须校验预检目标和当前候选一致

```text
问题：
右侧人工确认按钮此前只检查 preflight 是否存在、是否 blocked、是否 will_write=false。
如果用户快速切换候选，理论上可能出现旧 preflight 还在前端状态中，而当前选中信号已经变成新候选。

原因：
preflight 是异步读取结果。
仅靠 useEffect 取消请求不能覆盖所有前端状态过期风险。
按钮门禁层缺少“preflight.target 是否等于当前 selectedBackendManualActionPreview”的最终校验。

解决：
manualActionButtonGate 新增 expectedTarget。
按钮渲染和 handleManualAction 都传入当前候选 objectType / objectId。
如果 preflight.target.object_type / object_id 与当前候选不一致，按钮禁用并提示等待预检刷新。

后续防错：
1. 人工动作写入前必须同时满足 preflight ready 和目标一致。
2. 前端不能只因为 preflight.ready 就允许按钮点击。
3. expected_object_type / expected_object_id 必须来自后端 preflight.target，且 preflight.target 必须与当前候选一致。
4. 候选切换、筛选切换、Parent ASIN 切换后，人工按钮必须等待新 preflight。
5. 该规则只保护人工留痕对象一致性，不允许自动加词、否词、调价或暂停广告。
```

## 2026-06-18：人工动作 smoke 不能只输出大 payload

```text
问题：
apply_manual_action_once.py 写入后虽然返回 written_record 和 post_write_validation，但目标模式复核时需要人工从大 payload 中手动找：
- 是否写入 1 条 manual_action。
- 是否生成 2 条 7d / 14d review_todos。
- evidence_snapshot 是否写入。
- review_records 是否仍为 0。
- 是否仍禁止广告动作。

原因：
写后验收事实存在，但没有被收口成一个稳定断言字段。
这会让每轮复盘依赖人工读长 JSON，容易漏看 review_records 或广告动作边界。

解决：
apply_manual_action_once.py 新增 smoke_assertions：
- status。
- target。
- manual_action_written。
- target_manual_action_count。
- target_review_todo_count。
- target_review_record_count。
- written_evidence_snapshot_count。
- review_todos_expected_to_inherit_evidence_snapshot。
- review_records_not_saved。
- ad_actions_not_executed。

后续防错：
1. 判断人工动作 MVP 是否跑通，优先看 smoke_assertions，不只看 status=written_and_verified。
2. written_and_verified 必须同时满足人工留痕、7d/14d 待办、证据快照和无 review_records。
3. 任何 smoke 输出都必须继续展示不执行广告动作。
4. 隔离 smoke 必须使用 action_root / review_root，不能污染默认运行态。
5. smoke 通过不等于广告建议有效；效果结论必须等复盘窗口完整后再写 review_records。
```

## 2026-06-18：人工动作写后提示不能只说已记录

```text
问题：
人工动作写入后，前端原本能提示“已记录”和“7d / 14d 已读回”，但没有把用户真正关心的验收事实集中展示：
- 当前处理对象是否还是同一个搜索词。
- 证据快照是否保存。
- 7d / 14d 复盘待办是否生成。
- review_records 是否仍为 0。
- 是否没有执行广告动作。

原因：
manualActionPostWriteReadbackMessage 只拼接了 readbackText 和 postWritePreflight 状态。
这些字段虽然分散存在，但没有形成运营可扫读的写后验收摘要。

解决：
新增写后读回摘要：
- object_type / object_id。
- evidence_snapshot 数量。
- review_todo 窗口。
- review_records 数量。
- 不执行广告动作。

后续防错：
1. 写后提示必须回答“写给谁、保存了什么、进入哪个复盘窗口、有没有复盘结论、有没有广告副作用”。
2. review_records 仍只能在复盘窗口到期并计算效果后保存，点击人工动作时必须显示为 0 或未保存。
3. 搜索词对象的写后提示不能暗示已经对某个广告 ASIN 自动归因。
4. 不执行广告动作必须持续出现在写后验收链路里。
5. 浏览器截图失败不能当成页面失败；优先用 DOM、控制台、交互状态、测试和构建交叉验证。
```

## 2026-06-18：第一屏不能把真实运营路径藏在折叠区

```text
问题：
页面已有“当前分析路径”，但它藏在“经营盘子与广告证据下钻”的 details 内。
用户第一眼仍容易先看到信号队列和建议处理，误以为系统是在做普通信号列表，而不是从 Parent ASIN 经营盘开始诊断广告问题。

原因：
当前信息架构虽然已经有 Parent ASIN、广告 ASIN、广告组、搜索词和人工确认链路，但首屏显性结构仍偏信号队列。
业务路径不显性时，用户很难判断数据口径、对象关系和建议是否能落地。

解决：
在诊断入口边界区新增“Parent ASIN 经营诊断路径”：
Parent ASIN 销售盘 -> 广告 ASIN 覆盖 -> 广告组结构 -> 广告位 / 投放词 -> 搜索词 / ABA 证据 -> AI 信号诊断 -> 人工确认 / 7-14 天复盘。

后续防错：
1. 第一屏必须让用户看到业务阅读路径，不能只看到信号队列。
2. 信号队列必须服务于当前 Parent ASIN 经营诊断，不能替代经营路径。
3. 广告组只作为容器，不能在路径中被包装成商品。
4. 搜索词、ABA 和广告位只能作为证据上下文，不能直接当作 ASIN 归因。
5. 路径显性化不等于自动广告动作；所有处理仍必须人工确认并进入 7/14 天复盘。
```
## 2026-06-18：首屏不能只有经营路径没有经营数值

```text
问题：
上一轮已经把 Parent ASIN 经营诊断路径前置到第一屏，但核心销售盘指标和广告 ASIN 对比仍主要藏在 details 中。
这样用户第一眼能看到“应该按什么路径查”，却看不到“当前 Parent ASIN 到底卖得怎么样、哪些广告 ASIN 有投放证据、广告表现如何”。

原因：
路径说明只解决信息架构问题，不能替代业务判断。
Amazon 广告运营通常先看 Parent ASIN 整体销售，再看该 Parent 下有广告数据的广告 ASIN、广告组、投放词、搜索词和广告位。
如果首屏没有核心经营数值，用户仍会觉得页面像浅层信号列表，而不是可落地的广告 AI 工作台。

解决：
复用 productScopeGroupOverview 新增首屏经营摘要：
- Parent ASIN 销售盘。
- 当前投放广告 ASIN 数。
- 广告花费、广告订单、广告销售额。
- 首屏广告 ASIN 对比：广告 ASIN、花费、订单、销售额、ACOS、策略。
- 明确只展示有广告证据的广告 ASIN，未投放子 ASIN 不进入广告诊断。

后续防错：
1. 第一屏必须同时展示业务路径和核心经营事实，不能只展示路径说明。
2. 广告诊断只能进入有广告证据的广告 ASIN，不得把 Parent 下所有销售子 ASIN 都放进广告分析。
3. 广告 ASIN 对比表必须使用广告商品粒度指标，不得用 Parent 销售盘数值替代。
4. 搜索词和广告位只能作为广告组或投放上下文证据，不能直接归因到单个 ASIN。
5. 首屏摘要不等于自动广告建议，所有处理仍必须进入人工确认和 7/14 天复盘。
```
## 2026-06-18：能跑的原型不能直接等同于能落地

```text
问题：当前页面已经能展示 Parent ASIN 经营入口、广告 ASIN 摘要、候选信号、证据和人工确认区，但如果直接回答“已经能落地”，会掩盖真实业务风险。

原因：落地不是页面能渲染，而是用户能稳定完成业务判断：
- 先看 Parent ASIN 整体销售表现。
- 再看有广告证据的广告 ASIN。
- 再下钻广告组、广告位、投放商品、投放词和搜索词。
- 再根据证据链判断问题来源。
- 最后只做人工确认，并进入 7/14 天复盘。

本轮事实：首屏已经新增“查看广告组、搜索词和广告位证据”入口，且浏览器确认真实快照、12 个销售子 ASIN、3 个广告 ASIN、beach essentials、待人工确认和无写入痕迹可见。

风险：浏览器自动化未稳定证明点击后的滚动位置变化；搜索词解释深度、广告组问题定位和复盘效果回看仍不足以支撑生产级广告调整判断。

解决：把当前版本定位为内部 MVP 验证版，而不是生产落地版。下一轮继续强化证据下钻和可见交互反馈。

后续防错：
1. 不得把“页面能跑”当成“业务能落地”。
2. 不得把首屏有数值当成“证据链完整”。
3. 不得把 AI 建议直接包装成广告操作结论。
4. 每轮都必须继续回答：数据口径是否清晰、数据关系是否清楚、是否真的能落到业务动作和复盘。
```
## 2026-06-18：首屏下钻按钮不能只有跳转意图没有反馈

```text
问题：首屏已经有“查看广告组、搜索词和广告位证据”按钮，但此前点击后缺少明确状态反馈。用户可能不知道页面是否已经进入广告诊断区，也无法判断系统是否做了写入动作。

原因：只有 scrollIntoView 不能稳定表达业务状态。尤其在首屏和诊断区同屏可见时，滚动距离可能很小，用户感知不到变化。

解决：点击后设置 isEvidenceDrilldownFocused，在广告诊断工作台上方显示“已进入广告诊断工作台”状态条，并给三列诊断区加 focusedFromFirstScreen 高亮。

边界：状态条只说明进入诊断区，不代表已经执行广告动作，也不代表已经写入 manual_action。

后续防错：
1. 关键业务入口点击后必须有可见反馈，不能只依赖滚动。
2. 反馈文案必须继续强调只允许人工确认，不执行广告调整。
3. 交互反馈不能改变数据口径、归因边界或人工写入门禁。
4. 浏览器验证要同时检查状态条、真实数据上下文、无写入提示和控制台 fresh error/warn。
```
## 2026-06-18：范围级广告证据链不能被单条信号分支挡住

```text
问题：
浏览器验证发现首屏数据和下钻按钮都存在，但新增的“广告证据链导览”没有出现在真实 DOM 中。

原因：
导览最初渲染在 ProductScopeDrilldownEvidencePanel 内部；当页面存在 selectedSignal 时，中间区优先渲染 SignalDiagnosis，导致范围级证据链被条件分支挡住。

解决：
把 ProductScopeEvidenceRouteGuidePanel 移到 diagnosisPanel 顶层。
这样无论当前是单条信号诊断，还是 Parent 范围无候选诊断，用户都能先看到 Parent ASIN -> 广告 ASIN -> 广告组 -> 搜索词 / 广告位 -> 人工确认门禁。

边界：
导览只解释诊断路径，不改变候选准入，不写人工动作，不自动执行广告调整。

后续防错：
1. 范围级证据链必须渲染在单条信号诊断分支之前。
2. 只要是 Parent ASIN 范围诊断，必须先展示对象边界和归因边界。
3. 浏览器验证不能只看按钮存在，还要检查真实 DOM 里是否出现证据链导览。
4. 搜索词和广告位仍只能作为同广告组上下文，不能自动归因到单个 ASIN。
```
## 2026-06-18：广告组问题定位不能只藏在空候选分支

```text
问题：
页面已经有广告组问题定位和搜索词问题定位组件，但浏览器验证发现真实页面当前选中信号时看不到广告组人工复核判断。

原因：
广告组问题定位原本渲染在 ProductScopeDrilldownEvidencePanel 内部。
当 selectedSignal 存在时，中间区渲染 SignalDiagnosis，空候选面板不显示，导致范围级广告组判断被挡住。

解决：
把 ProductScopeAdGroupDiagnosisPanel 提升到 diagnosisPanel 顶层。
同时新增广告组人工复核判断，直接展示：
- 优先复核哪个广告组。
- 有效词和无订单花费词数量。
- 同广告组广告 ASIN 数。
- 当前人工确认门禁。

边界：
广告组仍是投放容器，不是商品。
搜索词和广告位仍只能说明同广告组上下文，不能自动归因到单个 ASIN。
判断块只服务人工复核，不自动执行广告动作。

后续防错：
1. 范围级广告组判断必须渲染在 selectedSignal 分支之前。
2. 广告组诊断必须同时说明证据、判断和人工门禁，不能只列指标。
3. 浏览器验收必须检查真实 DOM 中是否存在“广告组人工复核判断”。
4. 任何广告组判断都必须继续显示禁止自动调价、暂停、否词和新增关键词的边界。
```
## 2026-06-18：右侧人工候选不能脱离中间广告组诊断

```text
问题：
中间诊断区已经告诉用户优先复核哪个广告组，但右侧人工确认区原本只展示候选对象、处理依据和预检。
用户仍需要自己推断“右侧这个候选为什么承接中间那个广告组问题”。

原因：
中间广告组判断和右侧 manual_action_preview 是两条展示链路。
虽然后端已返回 ad_group_advertised_asins、搜索词上下文和候选对象，但前端没有把它们拼成用户可读的关系说明。

解决：
新增“人工候选与广告组关系”：
- 广告 ASIN 候选命中优先广告组投放商品清单时，说明它出现在该广告组投放商品中。
- 搜索词候选说明它使用优先广告组同组搜索词上下文复核。
- 关系说明复用中间广告组判断，不重新生成新的业务事实。

边界：
该关系只说明复核上下文，不代表自动归因。
搜索词和广告位不能自动归因到单个 ASIN。
右侧人工动作仍只允许记录观察、标记已处理、加入复盘或忽略本次，不自动执行广告动作。

后续防错：
1. 右侧人工候选必须能解释和中间诊断对象的关系。
2. 如果解释不了关系，不能把候选包装成可行动建议，只能提示证据缺口。
3. 桥接说明必须消费后端已返回字段，不得在前端虚构广告组归因。
4. 浏览器验收必须检查真实 DOM 中的“人工候选与广告组关系”。
```
## 2026-06-18：复盘待办不能被误读为复盘结论

```text
问题：
当前系统已经有人工留痕和 7/14 天复盘待办，但如果界面只显示“复盘状态”或“待办数量”，用户容易误以为系统已经判断了处理有效或无效。

原因：
manual_actions、review_todos、review_records 是三层不同事实：
- manual_actions 只表示人工做过记录。
- review_todos 只表示未来要复盘。
- review_records 才表示已经保存过复盘结论。

解决：
右侧新增“人工确认复盘闭环”摘要，把三层状态拆开显示。
当前未写入时明确显示：未记录、未生成、未保存。
即使 reviewEffect ready，也必须提示仍需人工保存 review_records 后才形成结论。

边界：
该摘要只解释闭环状态，不触发写入，不生成待办，不保存复盘记录，不自动执行广告动作。

后续防错：
1. 不得把 review_todos 数量当成复盘结论。
2. 不得在未保存 review_records 前展示“处理有效”“处理无效”这类结论。
3. 页面上凡涉及复盘，都要能解释 manual_actions、review_todos、review_records 的区别。
4. 浏览器验收必须检查三层闭环文案和禁止自动执行广告动作。
```
## 2026-06-18：选中信号局部闭环不能替代全局复盘可验证性

```text
问题：
上一轮右侧已经能解释选中信号的 manual_actions、review_todos、review_records 三层闭环，但它只能说明当前选中对象。
真实运行态里全局已有 17 条人工留痕、0 条已保存复盘、0 个 ready 复盘，最早到 2026-06-22 才能复核。
如果页面不展示全局门槛，用户可能误以为当前版本已经具备复盘结论能力。

原因：
选中信号状态和当前经营范围的 review_status 是两个层级：
- 选中信号闭环回答“这个对象有没有留痕、待办、结论”。
- 全局复盘可验证性回答“当前范围有没有任何可保存复盘结论”。
只看前者会漏掉“已有人工留痕但窗口未到期”的事实。

解决：
右侧复盘状态顶部新增“全局复盘可验证性”。
当前展示：已有 17 条人工留痕，ready 复盘 0 个，最早 2026-06-22 后再复核。
同时显示已存复盘 0 条、未就绪 10 个、下一项 advertised_product / B016EXMVZS。

边界：
该提示只读展示 review_status。
未到期前不拉取快照、不保存复盘结论、不自动改规则、不自动执行广告动作。

后续防错：
1. 判断项目是否能落地时，必须同时看全局 review_status 和选中信号闭环。
2. ready_count=0 时，不得展示“处理有效”“处理无效”或“规则可调整”。
3. review_record_count=0 时，不得把人工留痕或复盘待办包装成已验证结论。
4. 浏览器验收必须检查全局复盘门槛、最早到期日和禁止动作。
```
## 2026-06-18：复盘等待态不能只有状态没有下一步

```text
问题：
右侧已经能显示“复盘等待窗口”和最早复盘日期，但如果只告诉用户“未到期”，仍然不够落地。
运营需要知道等待期现在该做什么、到期后怎么查、ready 后才能保存什么。

原因：
状态提示回答“当前是什么”，但没有回答“接下来怎么推进”。
在目标模式里，如果等待状态没有下一步路径，容易出现两类错误：
- 为了推进而提前拉快照或保存复盘结论。
- 因为窗口未到期而停滞，不继续维护人工候选和证据链。

解决：
在“全局复盘可验证性”内新增“复盘下一步路径”：
1. 现在：查看人工留痕和复盘待办，不拉取快照、不保存复盘结论。
2. 到期后：2026-06-22 后只读检查复盘效果，确认 7/14 天窗口是否完整。
3. ready 后：人工确认后再保存 review_records，不自动改规则或执行广告动作。

边界：
该路径只是下一步说明，不触发任何写入，不生成新待办，不保存复盘记录，不执行广告动作。

后续防错：
1. 复盘等待状态必须同时显示下一步路径。
2. 未到期前不得把“到期后检查”做成现在可执行的拉快照或保存结论动作。
3. ready 后也必须人工确认才能保存 review_records。
4. 浏览器验收必须检查“现在 / 到期后 / ready 后”三步和禁止动作文案。
```
## 2026-06-18：首屏经营摘要不能只有数值没有诊断路径

```text
问题：
首屏已经展示 Parent ASIN 销售盘、12 个销售表现子 ASIN、3 个广告 ASIN 和广告 ASIN 对比表。
但如果没有明确下一步诊断路径，用户仍可能把它理解成普通经营报表，而不是广告 AI 信号工作台。

原因：
经营事实回答“当前盘子是什么”，但不回答“运营下一步怎么排查”。
如果首屏只展示数值，用户仍需要自己推断 Parent ASIN、广告 ASIN、广告组、搜索词、广告位和人工复盘之间的关系。

解决：
在 Parent ASIN 首屏经营摘要内新增“首屏诊断路径”：
1. 先看销售盘。
2. 再看有广告证据的广告 ASIN。
3. 定位广告组，明确广告组是投放容器。
4. 复核搜索词和广告位上下文，明确不能直接归因到单个 ASIN。
5. 进入人工确认和 7/14 天复盘。

边界：
该路径只解释使用顺序，不新增后端字段，不写 manual_actions，不保存 review_records，不执行广告动作。

后续防错：
1. 首屏经营摘要必须同时回答“当前事实”和“下一步怎么排查”。
2. 有销售表现但无广告证据的子 ASIN 不能进入广告诊断路径。
3. 搜索词和广告位只能作为上下文证据，不能自动归因到单个 ASIN。
4. 浏览器验收必须检查“首屏诊断路径”以及 5 步文案。
```
## 2026-06-18：首屏不能让用户自己判断 MVP 是否可落地

```text
问题：
首屏已经展示 Parent ASIN 经营路径、经营数值、广告 ASIN 对比和复盘等待状态，但用户仍会问“这个版本真的能落地吗”。
这说明页面虽然有信息，但没有把当前范围的落地门槛显性化。

原因：
落地判断至少包含四层事实：
- 经营口径是否清楚。
- 是否有广告证据可下钻。
- 是否有 AI 候选能进入人工确认。
- 是否已有 review_records 支撑复盘结论。

如果这些事实分散在不同区域，用户需要自己拼接，容易把页面能跑误读成业务闭环已完成。

解决：
在 Parent ASIN 首屏经营摘要内新增 “MVP 落地门禁”：
- 经营口径：销售表现子 ASIN 是经营背景。
- 广告证据：只有有广告证据的 ASIN 进入广告诊断。
- AI 准入：候选仍需人工复核，不自动执行广告动作。
- 复盘门槛：review_records 未保存前不能说处理有效或无效。

后续防错：
1. 判断版本能否落地时，先看落地门禁，不只看页面是否有数据。
2. 销售子 ASIN、广告 ASIN、AI 候选和复盘结论必须分层展示。
3. candidate_count > 0 只表示有候选可人工复核，不代表可以自动广告调整。
4. review_records=0 时，不得展示处理有效、无效或规则可调整结论。
5. 浏览器验收必须检查 “MVP 落地门禁” 和无人工写入痕迹。
```

## 2026-06-18：广告 ASIN 下钻不能只改变筛选器

```text
问题：
点击或切换到广告 ASIN B016EXMVZS 后，页面虽然会变成广告 ASIN 范围，但用户需要从多段上下文里拼接当前直读指标和同广告组上下文边界。
这容易让用户误以为搜索词、投放词或广告位已经自动归因到该 ASIN。

原因：
原有 ASIN 诊断入口只在 description 和分析路径里说明“搜索词和广告位仍只作为同广告组上下文”。
该说明不够醒目，也没有把当前选中的具体 ASIN、可直接读取的广告商品指标、不能自动归因边界和后端预检目标放在同一个提示里。

解决：
ProductScopeSelectionSummary 新增 scopeSyncNotice。
广告 ASIN / 销售 ASIN 范围下展示“广告 ASIN 范围已同步”：
- 当前已切到具体 ASIN。
- 只直接读取该 ASIN 可匹配的广告商品指标。
- 搜索词、投放词和广告位仍是同广告组上下文，不能自动归因到该 ASIN。
- 人工动作仍以后端预检 object_type / object_id 为准。

后续防错：
1. 广告 ASIN 下钻必须有显性范围同步提示，不能只依赖筛选器值变化。
2. 搜索词、投放词和广告位不能因为页面筛选到某个 ASIN 就被包装成单 ASIN 归因。
3. 可处理候选为 0 时，只能展示诊断和证据边界，不能补造人工动作入口。
4. 人工动作目标始终以后端预检为准，不用页面筛选标签替代 object_type / object_id。
```
## 2026-06-18：广告 ASIN 候选 0 不能只显示空结果

```text
问题：
切换到广告 ASIN B016EXMVZS 后，系统可能出现 candidate_count=0。
如果页面只显示“没有候选”或右侧为空，用户会难以判断这是系统没算出来、数据缺失，还是证据不足不能写人工动作。

原因：
广告 ASIN 指标、广告组、搜索词和广告位是不同证据层。
当广告 ASIN 层没有命中异常，稳定转化机会规则又因库存、利润、价格、主推策略等独立证据不足而暂缓时，系统只能诊断，不能生成可处理候选。
搜索词和广告位仍是同广告组上下文，不能自动归因到单个 ASIN。

解决：
新增“广告 ASIN 候选缺口解释”：
- 明确当前 ASIN candidate_count=0。
- 说明哪些检查没有触发候选。
- 说明哪些证据只支持诊断，不支持人工动作。
- 说明下一步应继续下钻广告 ASIN、广告组、有效搜索词和无订单花费词。
- 明确不写人工动作、不自动执行广告动作。

边界：
候选缺口解释只解释为什么不能进入人工确认，不生成新候选，不改变后端准入规则，不保存 manual_actions，不保存 review_records。

后续防错：
1. candidate_count=0 时必须解释原因，不能只显示空状态。
2. 不能把“稳定转化但证据不足”包装成扩量建议。
3. 不能把同广告组搜索词和广告位自动归因到单个 ASIN。
4. 右侧人工动作必须以后端候选为准，前端不能为了可落地感伪造处理入口。
```
## 2026-06-18：顶部拉取快照不能被误解为复盘推进动作

```text
问题：
复盘窗口未到期时，页面顶部仍有“拉取快照”按钮。
用户可能把它误解为当前应该拉新快照来推进复盘，从而绕过 7/14 天窗口门槛。

原因：
快照拉取和复盘检查是两类不同动作：
- 快照拉取用于人工低频补齐真实 API 数据。
- 复盘检查必须等人工动作后的 7/14 天窗口完整，再只读比较处理前后指标。

如果这两个动作在 UI 上没有显性边界，就会把“数据补齐”误读成“复盘已可推进”。

解决：
新增顶部“快照复盘门禁”：
- review_wait_summary.status = waiting_review_window 时，禁用顶部“拉取快照”按钮。
- 显示最早复盘日期。
- 明确未到期前不拉取快照、不保存复盘结论、不自动改规则、不自动执行广告动作。

后续防错：
1. 复盘等待态下，顶部快照动作必须受 review_wait_summary 约束。
2. 未到期前只能刷新本地信号和查看人工留痕，不能拉新快照制造复盘结论。
3. 到期后也必须先只读检查复盘效果，ready 后由人工保存 review_records。
4. 浏览器验收必须检查“快照复盘门禁”、拉取快照 disabled 状态和自动广告动作按钮为 0。
```
## 2026-06-18：effects 明细不能替代复盘身份审计

```text
问题：
inspect_review_readiness 已经输出 effects 明细，里面有 action_id、object_id 和 review_window。
但如果只靠人眼查看 effects，目标模式里容易误判“字段看起来存在”就等于“复盘读回身份已经可验收”。

原因：
effects 是逐条复盘效果候选，不是审计结论。
真正的复盘保存前门槛需要同时回答：
- action_id 是否缺失。
- object_id 是否缺失或仍像快照行 ID。
- review_window 是否缺失。
- 当前是否已有 ready 复盘效果。
- 当前是否允许保存 review_records。

解决：
新增 review_identity_audit：
- 机器可读统计缺失 action_id / object_id / review_window 的数量。
- 机器可读统计 unstable_object_id_count。
- 输出 readback_keys 作为 2026-06-22 后复盘读回检查入口。
- ready_review_count=0 时 can_save_review_records_now=false。
- 区分 earliest_due_date 和 earliest_metric_due_date，避免把 cross 数据质量待办日期误当成广告指标复盘日期。

后续防错：
1. 判断复盘链路是否可落地时，必须先看 review_identity_audit，不能只看 effects。
2. ready_for_readback 只表示身份可读回，不表示复盘效果 ready。
3. 广告指标复盘日期必须优先看 earliest_metric_due_date，不能直接拿 cross 待办日期判断。
4. can_save_review_records_now=false 时，不得保存 review_records。
5. review_records 保存仍必须由人工确认，不能自动改规则或自动执行广告动作。
```
## 2026-06-18：右侧复盘区不能隐藏读回身份门禁

```text
问题：
后端已经有 review_identity_audit，但如果右侧复盘区不展示，用户仍只能看到人工留痕和等待窗口。
这会让用户难以判断当前是“身份可读回但效果未到期”，还是“复盘链路本身缺键不可用”。

原因：
复盘落地需要三层同时成立：
- 原人工动作身份可读回。
- 7/14 天广告指标窗口完整。
- 人工确认后才保存 review_records。
缺少任何一层，当前版本都不能宣称完整业务闭环落地。

解决：
右侧“复盘状态”接入 review_identity_audit：
- 显示读回身份是否可审计。
- 显示缺失 action_id / object_id / review_window 数量。
- 显示历史对象 ID 风险。
- ready_review_count=0 时明确当前不能保存复盘记录。
- 明确 ready_for_readback 不等于复盘效果 ready。

后续防错：
1. UI 不能只展示复盘待办数量，必须展示复盘身份门禁。
2. ready_for_readback 只能作为读回门禁，不得作为处理有效、无效或规则调整依据。
3. review_records=0 时，页面不能输出“建议已验证有效”的表达。
4. 完整落地验收必须等 ready_review_count > 0 且人工确认保存 review_records 后再判断。
```
## 2026-06-18：首屏不能把诊断 MVP 包装成完整闭环

```text
问题：
首屏已有经营摘要、诊断路径、落地门禁和复盘门禁，但用户仍可能问“这个版本真的能落地吗”。
这说明页面缺少一个直接的版本状态判定，用户仍需要自己把 candidate_count、人工留痕和复盘状态拼起来。

原因：
当前系统至少有三种不同落地状态：
- 诊断 MVP：有真实广告证据，但没有可写人工候选。
- 人工留痕 MVP：有候选可以人工确认，但复盘未 ready。
- 完整复盘闭环：有 ready 复盘，并且人工保存了 review_records。
如果 UI 只展示单项门禁，容易把诊断 MVP 误读成完整业务闭环。

解决：
Parent ASIN 首屏经营摘要新增“诊断 MVP 状态判定”：
- candidate_count=0 时显示诊断 MVP，说明只能下钻证据，不能写人工动作。
- candidate_count>0 且可写人工动作时显示人工留痕 MVP，说明不是完整复盘闭环。
- ready 复盘和 review_records 同时存在时才显示复盘闭环可验证。
- 没有广告证据时显示数据准备中。

后续防错：
1. 回答“能否落地”时必须先看 mvpStatus，不只看页面是否有数据。
2. 诊断 MVP 不能展示广告调整建议或人工动作入口。
3. 人工留痕 MVP 不能展示处理有效 / 无效结论。
4. 完整复盘闭环必须有 ready 复盘和已保存 review_records，不能只靠 manual_actions。
```

## 2026-06-18：CLI 分诊不能维护第二套候选口径

```text
问题：
浏览器和 /api/signal-triage 显示 Parent ASIN B00K4W4AAA 下有 3 个搜索词候选，但 scripts\inspect_signal_triage.py 显示 candidate_count=0。
这会让落地性判断出现相反结论：页面是人工留痕 MVP，CLI 却误判为只能诊断。

原因：
CLI 脚本维护了第二套候选来源，从 scripts\inspect_review_candidates.py 读取候选；工作台 API 使用 backend/app/services/signal_triage.py 的服务层候选口径。
服务层已能识别搜索词上下文候选，旧 CLI 候选来源没有同步，导致同一 Parent ASIN 出现两个事实。

解决：
scripts\inspect_signal_triage.py 改为复用服务层 build_review_candidates_payload。
新增 test_signal_triage_cli_uses_service_candidate_source，保证 CLI 加载时使用服务层候选构建器。

后续防错：
1. 候选准入只允许服务层作为唯一事实来源，CLI 只能包装服务层结果。
2. 判断能否落地时，必须同时核对工作台 API 和 CLI；二者不一致时先查口径，不下业务结论。
3. 搜索词候选、广告 ASIN 候选和广告组结构候选必须分层展示，不能混成“广告 ASIN 建议”。
4. 搜索词候选即使可进入人工确认，也不能自动加词、否词、调价或归因到单个广告 ASIN。
```

### 问题：商品入口加载态短暂显示全量排查，容易误导默认业务口径

#### 现象

```text
浏览器刷新后，商品范围接口尚未返回前，经营商品入口下拉会短暂使用 all 作为 fallback。
用户可能看到“辅助入口：全量排查（商品 + 未归因 + 数据质量）”，误以为当前版本默认仍以全量排查为经营诊断口径。
```

#### 原因

```text
前端在 productScope 为空时使用 all 作为本地占位。
这和项目规则冲突：全量排查只能作为辅助入口，默认经营诊断必须优先 Parent ASIN / ASIN，尤其是有广告证据的 Parent ASIN。
```

#### 解决方案

```text
1. 默认经营入口选择规则改为优先选择有广告证据的 Parent ASIN。
2. 若 productScope 尚未加载，前端使用 loading_product_scope 占位，文案为“正在读取经营商品入口”。
3. 加载期间禁用经营商品入口下拉，避免用户把加载态当成可选业务口径。
4. loading 占位单独归入“经营入口状态”，不混入广告分析入口或辅助排查入口。
```

#### 后续防错规则

```text
所有入口 fallback 都不能使用会改变业务含义的真实选项。
加载态必须是加载态，不能伪装成全量排查、未归因或任一真实业务对象。
默认入口要优先能形成广告诊断闭环的经营对象：有广告证据的 Parent ASIN 优先于普通 Parent ASIN，普通 Parent ASIN 优先于广告 ASIN，全量排查最后兜底。
```
## 2026-06-18：可验证 MVP 不能包装成完整落地版本

```text
问题：
当前版本已经有真实 30 天快照、Parent ASIN 经营入口、AI 信号候选、人工动作预检和证据快照预览，容易被误认为已经完整落地。

原因：
完整业务落地至少需要三层同时成立：
1. 真实广告数据能生成可解释候选。
2. 人工授权后 manual_actions / review_todos / evidence_snapshot 能读回。
3. 7/14 天窗口到期后，人工保存 review_records，才能判断建议有效、无效或不确定。

当前只满足第 1 层，并且第 2 层停在写入前预检；第 3 层 review_records 仍为 0。

防错规则：
1. 回答“能否落地”时必须区分诊断 MVP、人工留痕 MVP、完整复盘闭环。
2. review_records=0 时，不得宣称建议有效、规则可调整或广告动作可执行。
3. ready_for_explicit_manual_write 只表示可在人工授权后写入留痕，不表示建议已经生效。
4. 后续页面必须继续显式展示不自动调价、不自动暂停、不自动否词、不自动加词。
5. 完整落地判断必须以写入后读回和复盘记录为准，不能只看页面有数据或有候选。
```
## 2026-06-18：post-write 不能只看留痕和待办数量

```text
问题：
写入后验收如果只检查 target_manual_action_count=1 和 target_review_todo_count=2，可能出现“数量正确但证据快照没有读回”的假闭环。

原因：
7/14 天复盘需要解释为什么当时建议人工处理、证据来自哪里、当时的投放上下文是什么。
如果 manual_action 或 review_todos 没有 evidence_snapshot，后续 review_records 即使保存，也缺少可解释依据。

解决：
post_write_checks 增加：
- target_manual_action_evidence_snapshot_counts
- target_review_todo_evidence_snapshot_counts

复盘类人工动作写入后：
- 目标 manual_action 缺少 evidence_snapshot 时阻断 post_write_verified。
- 目标 7d / 14d review_todos 缺少 evidence_snapshot 时阻断 post_write_verified。

防错规则：
1. 判断 post-write 是否通过时，必须同时检查数量、窗口、证据快照和 review_records=0。
2. evidence_snapshot_count=0 的人工留痕不能作为高质量复盘样本。
3. review_todos 没有继承 evidence_snapshot 时，不能进入完整复盘闭环。
4. 证据快照读回只证明复盘有依据，不证明广告处理有效；效果结论仍必须等 review_records。
```
## 2026-06-18：授权脚本 smoke 不能只证明写入前证据存在

```text
问题：
apply_manual_action_once.py 写入后的 smoke_assertions 原本只返回 written_evidence_snapshot_count 和 review_todos_expected_to_inherit_evidence_snapshot。
这只能说明“写入时拿到了证据”和“理论上待办应继承证据”，不能说明 post-write 读回真的拿到了证据。

原因：
完整人工留痕 MVP 的验收应以写入后读回为准。
如果只看写入前 preview，可能出现保存或派生待办过程中丢失 evidence_snapshot，却仍被 smoke 摘要误判为通过。

解决：
smoke_assertions 新增：
- post_write_manual_action_evidence_snapshot_count
- post_write_review_todo_evidence_snapshot_counts
- post_write_review_todos_with_evidence_snapshot

这些字段全部来自 post_write_validation.post_write_checks。

防错规则：
1. 授权写入后的验收摘要必须优先看 post_write_* 字段，不只看 written_evidence_snapshot_count。
2. review_todos_expected_to_inherit_evidence_snapshot 只能表达预期，不代表已读回成功。
3. post_write_review_todos_with_evidence_snapshot 必须等于目标复盘待办数量，才能说 7d / 14d 待办证据完整。
4. 这些字段只证明证据链完整，不证明广告建议有效；效果仍必须等 review_records。
```

## 2026-06-18：UI 不能隐藏 post-write 证据读回

```text
问题：
后端和授权脚本已经能返回 post_write_checks 里的证据快照读回计数，但如果右侧 UI 只展示“1 条留痕、2 条待办”，运营仍然无法判断复盘待办是否真的带着当时的证据链。

原因：
完整人工留痕 MVP 至少要让用户看见三层事实：
1. manual_actions 是否写入并可读回。
2. 7d / 14d review_todos 是否生成并可读回。
3. manual_actions 与 review_todos 是否都能读回 evidence_snapshot。
只展示前两层会把“数量写入成功”误包装成“复盘证据完整”。

解决：
右侧写后验收文案在 post_write 模式下必须消费 post_write_checks：
- target_manual_action_evidence_snapshot_counts
- target_review_todo_evidence_snapshot_counts

防错规则：
1. post-write UI 文案不得只展示 target_manual_action_count 和 target_review_todo_count。
2. 只要后端返回证据读回计数，UI 必须展示留痕证据数量和 7d / 14d 待办证据数量。
3. pre_write 阶段只能展示预计，不得提前表达证据已经保存。
4. 证据读回只能证明复盘有依据，不能证明广告建议有效；review_records=0 时仍不得输出处理有效或无效结论。
```

## 2026-06-18：运行中的后端旧进程会隐藏新 API 合同字段

```text
问题：
manual_action_preflight.py 当前代码已经返回 target_manual_action_evidence_snapshot_counts 和 target_review_todo_evidence_snapshot_counts，但 8000 运行态 API 起初没有返回这两个字段。

原因：
后端 uvicorn 进程是在本轮代码变更前启动的旧进程，没有自动加载最新服务代码。只看运行态 API 会误判为“后端合同未实现”，只看脚本直接调用又会误判为“页面已可用”。

解决：
重启 8000 后端后，运行态 API 与当前代码一致，post_write_checks 已包含证据读回计数字段。

防错规则：
1. 修改后端 API 合同后，必须重启或确认 reload 生效，再做运行态 API 验证。
2. 判断 API 合同是否落地时，要同时看当前代码、脚本直接调用和运行态 HTTP 响应。
3. 运行态 API 缺字段时，先排查 stale process，不要直接改业务逻辑或前端兜底。
4. 运行态验证完成后，要复核默认 data/runtime 是否被污染；隔离演练必须使用临时 action_root / review_root。
```

## 2026-06-18：板块不能只用裸指标填充

```text
问题：
用户指出当前项目基础结构看起来没问题，但仔细看每个板块内容仍然敷衍，只是拿一些指标填充页面，没有反复思考这些指标为什么目标服务。

原因：
现有工作台已经有 Parent ASIN、广告 ASIN、广告组、搜索词、证据链和人工确认等结构，但不少内容仍停留在“展示指标”层面。
如果没有明确业务问题、对象粒度、指标目的、能证明什么、不能证明什么和人工下一步，运营看完仍无法落地判断。

解决：
/api/signal-triage 新增 diagnosis_contract，用 beach essentials 真实推荐信号先打深一条标杆链路。
每个合同块必须包含：
- business_question
- object_grain
- metrics
- current_judgement
- proves
- does_not_prove
- next_manual_step

前端中间诊断区展示“指标目的和诊断合同”，不再只展示裸指标。

防错规则：
1. 新增或调整任何诊断板块时，必须先回答“这个板块服务哪个业务判断”。
2. 指标必须带 purpose；不能只展示花费、点击、订单、ACOS、CVR。
3. 每个板块必须写清能证明什么和不能证明什么，尤其是搜索词、广告位、广告组不能自动归因到单个 ASIN。
4. 下一步只能落到人工动作和复盘路径，不能包装成自动加词、自动调价、自动否词或自动暂停。
5. 先用一条真实链路打深，再复制到其他信号类型；不要一次性铺很多浅层板块。
```

## 2026-06-18：左侧信号队列不能只按异常 / 机会技术桶组织

```text
问题：
左侧信号队列原先按 anomaly / opportunity / data_quality 筛选。
这能说明规则输出类型，但不能直接回答运营真正关心的经营问题：当前是扩量机会、花费浪费、投放结构、数据质量，还是复盘待办。

原因：
“异常 / 机会”是技术归类，不是业务入口。
如果队列入口停留在技术桶，用户需要自己把 signal_type、signal_category、对象类型和证据链重新翻译成业务判断，容易继续出现“结构看着没问题，但内容敷衍”的问题。

解决：
signalQueueKind 改为经营问题分类：
- opportunity_expansion -> 机会扩量
- spend_waste -> 花费浪费
- structure_boundary -> 投放结构
- data_quality -> 数据质量
- review -> 复盘

左侧筛选按钮和 signal pill 使用经营问题标签；分诊口径仍保留原始 signal_type / signal_category，便于排查但不作为用户主入口。

防错规则：
1. 左侧队列入口必须先服务经营问题，不能只展示技术信号类型。
2. 原始 signal_type / signal_category 可以作为辅助口径，但不能替代用户可理解的业务分类。
3. 数据质量和复盘必须保持独立，不得被包装成花费浪费或机会扩量。
4. 投放结构类信号必须提示广告组、广告位、搜索词的对象边界，不能自动归因到单个 ASIN。
5. 后续新增信号时必须先定义它属于哪个经营问题队列，再决定前端展示。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-18：诊断合同不能只写不能证明，还要写证据缺口

```text
问题：
diagnosis_contract 已经能说明业务问题、指标目的、能证明和不能证明。
但广告组、投放词、广告位这些层级如果只写“不能自动归因”或“不能判断广告位影响”，用户仍不知道下一步要补什么证据。

原因：
“不能证明”是边界提示，不是行动路径。
运营要落地，需要知道当前缺的是投放商品清单、主推策略、投放词维护状态、广告商品承接，还是同周期广告位数据。

解决：
diagnosis_contract.sections 新增：
- evidence_gap：当前缺哪类证据或哪类证据不够。
- required_evidence：补齐什么证据后才能继续判断。

前端诊断合同面板同步展示“证据缺口”和“需要补证”。

防错规则：
1. 任何诊断块只写“不能证明”都不够，必须同时说明证据缺口。
2. 广告组块必须说明广告组是容器，以及是否缺投放商品清单、主推策略或商品级承接确认。
3. 搜索词块必须说明投放词、广告商品和策略证据缺口，不能直接包装成自动加词或调价。
4. 广告位块必须说明是否缺同周期 ad_placement_daily_metrics，缺失时不能判断广告位影响。
5. required_evidence 只能指向补证或人工复核，不能指向自动广告动作。

验证方式：
python -m pytest tests\test_signal_triage_service.py -k diagnosis_contract -q
npm run test:signal-ui
npm run build
运行态 /api/signal-triage 的 beach essentials 合同块必须带 evidence_gap 和 required_evidence。
```

## 2026-06-18：人工确认预检快照必须承接诊断合同

```text
问题：
中间诊断区已经通过 diagnosis_contract 展示业务问题、指标目的、证据缺口和需要补证。
但右侧人工确认区的 evidence_snapshot_preview 原先只来自 business_evidence_blocks。
如果用户授权写入，ManualAction.evidence_snapshot 可能缺少诊断合同里的证据缺口和补证路径。

原因：
诊断合同和人工预检快照是两个链路：
- diagnosis_contract 用于解释为什么这些指标存在、能证明什么、不能证明什么、缺什么证据。
- evidence_snapshot_preview 用于人工点击前预览，并在授权后随 manual_action 保存。

如果预检只消费 business_evidence_blocks，就会出现“页面中间能看到缺口，但复盘回看证据快照看不到缺口”的断链。

解决：
manual_action_preflight 的 evidence_snapshot_preview 追加 diagnosis_contract 摘要：
- 诊断证据缺口：聚合 sections.evidence_gap。
- 需要补证：聚合 sections.required_evidence。
- source 统一标记为 diagnosis_contract。
- detail 明确只用于人工复核和复盘，不自动加词、调价、否词或暂停广告。

后续防错规则：
1. 凡是影响人工动作判断的诊断合同字段，都必须进入 preflight 证据快照或明确说明为什么不保存。
2. evidence_snapshot_preview 不能只保存业务指标，还必须保存关键边界、证据缺口和补证路径。
3. 前端提交 manual_action 时必须继续优先使用后端 preflight.evidence_snapshot_preview.items，不能重新拼一套前端证据。
4. 诊断合同更新后，必须同步检查人工预检快照测试，避免展示层和保存层分叉。
5. 保存证据快照只证明“当时为什么人工处理”，不证明广告建议有效；效果仍必须等 review_records。

验证方式：
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_snapshots_diagnosis_contract_gaps -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_manual_action_preflight_cli.py -q
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_api_routes.py -k "manual_action or preflight or evidence_snapshot" -q
运行态 /api/manual-action/preflight 的 beach essentials 预检快照必须包含“诊断证据缺口”和“需要补证”。
```

## 2026-06-18：右侧预检不能把补证路径埋在长列表里

```text
问题：
后端 preflight 已经返回“诊断证据缺口”和“需要补证”，但如果右侧只展示完整证据快照列表，运营仍需要在 10 条证据中自己找当前不能判断的原因和下一步补证路径。

原因：
完整 evidence_snapshot_preview 主要服务保存和复盘回看，不等于右侧操作栏的阅读优先级。
人工确认前的第一性问题是“现在能不能判断、缺什么证据、人工下一步是什么”，而不是先把全部证据平铺出来。

解决：
前端新增 manualActionPreflightPriorityEvidenceRows，只从后端 preflight.evidence_snapshot_preview.items 中提取：
- 诊断证据缺口
- 需要补证

右侧“后端只读预检”后先展示“先看证据缺口”，再展示完整“将保存的证据快照”。

防错规则：
1. 右侧人工确认区必须先暴露影响人工判断的证据缺口和补证路径。
2. 优先提示只能消费后端 preflight 快照，不得由前端另造一套业务事实。
3. 完整证据快照列表仍用于保存和复盘回看，不能替代点击前的关键判断提示。
4. 补证路径只能指向人工复核或数据补齐，不能包装成自动加词、自动调价、自动否词或自动暂停。

验证方式：
npm run test:review-ui
npm run test:signal-ui
npm run build
运行态 /api/manual-action/preflight 必须返回“诊断证据缺口”和“需要补证”。
```

## 2026-06-18：人工动作按钮区不能只靠按钮标签表达动作边界

```text
问题：
右侧人工确认区虽然按钮文案是“记录观察 / 标记已处理 / 加入复盘 / 忽略本次”，但如果按钮组附近没有明确边界，用户仍可能把“建议处理”理解成系统会自动执行广告动作。

原因：
广告 AI 信号工作台的第一性边界是“AI 只诊断和建议，广告动作必须人工执行”。
单个按钮标签只能说明动作名称，不能完整说明系统影响范围。
尤其是“加入复盘”容易被误读成系统已经修改广告并等待效果验证。

解决：
右侧按钮组上方固定展示：
只保存人工留痕和复盘待办，不执行广告动作。

同时用结构测试锁定 aria-label="人工留痕动作" 和该边界文案，避免后续页面整理时删掉。

防错规则：
1. 人工动作按钮区必须有明确边界文案，不能只保留四个按钮。
2. “加入复盘”只能生成复盘待办或留痕，不代表系统已经执行广告调整。
3. 任意新按钮或新文案都不能暗示自动调价、自动暂停、自动加词或自动否词。
4. 前端文案必须和后端 preflight 的 will_write=false / requires_explicit_authorization 边界一致。

验证方式：
npm run test:workbench-structure
npm run test:review-ui
npm run test:signal-ui
npm run build
运行态 B00K4W4AAA / beach essentials 预检必须保持 will_write=false，且无人工点击不写入。
```

## 2026-06-18：复盘窗口不能用字符串排序

```text
问题：
post-write preflight 的 target_review_windows 曾返回 ["14d","7d"]。
这不是业务顺序，用户阅读上会误解为先看 14 天再看 7 天。

原因：
代码使用普通字符串排序。
字符串排序下 "14d" 会排在 "7d" 前面。

解决：
manual_action_preflight.py 新增 REVIEW_WINDOW_ORDER=("7d","14d")，target_review_windows 按业务顺序输出。
非标准窗口只作为兜底追加到后面。

防错规则：
1. 所有复盘窗口展示和 API 输出必须按 7d -> 14d 的业务阅读顺序。
2. 不能用普通字符串排序处理复盘窗口。
3. post-write 验收中的 target_review_windows、ReviewTodo 展示和前端文案必须保持同一顺序。
4. 复盘窗口顺序只服务阅读和验收，不代表系统自动判断效果；review_records 仍必须等窗口完整后人工保存。

验证方式：
python -m pytest tests/test_manual_action_preflight_cli.py::test_manual_action_preflight_verifies_post_write_review_todos -q
python -m pytest tests/test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot -q
运行态 /api/manual-action/preflight 的 target.review_windows 必须为 ["7d","14d"]。
```

## 2026-06-18：复盘待办不能被表达成效果结论

```text
问题：
点击后读回和复盘状态区如果只展示“已生成 7d / 14d 待办”，用户容易误解为系统已经判断处理有效，或者已经完成广告调整后的效果验证。

原因：
review_todos 只是排程对象，不是结果对象。
真正的效果结论只能来自到期后的人工复盘和 review_records。
如果文案只强调待办数量，没有强调未到期不判效，就会混淆 manual_actions、review_todos、review_records 三个对象。

解决：
前端 reviewUi 增加统一边界文案：
复盘待办只表示进入排程，未到期不判断效果。

该边界接入：
- 点击后读回一致消息
- 写后读回摘要
- post-write preflight 状态
- 人工确认复盘闭环台账

防错规则：
1. 任何“已生成复盘待办”的用户可见文案，都必须说明这只是排程，不是效果结论。
2. 没有 review_records 前，不能展示改善、无变化或恶化。
3. 复盘待办到期只代表可以人工复核，不代表系统自动保存结论。
4. 该边界不能被包装成自动广告动作。

验证方式：
npm run test:review-ui
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-18：左侧信号队列不能只展示严重度和置信度

```text
问题：
左侧队列如果只展示对象类型、严重度、置信度、证据数量和状态，用户仍然要点开详情才知道这条信号到底服务哪个经营问题。
这会让队列退回“指标告警列表”，而不是 AI Copilot 的业务分诊入口。

原因：
严重度和置信度只能说明优先级和证据强弱，不能说明业务问题。
Amazon 广告诊断的第一性路径必须先回答：
这是机会扩量、花费浪费、投放结构、数据质量，还是复盘问题？
当前对象是经营商品、广告商品、搜索词、广告位还是广告组容器？
这个对象能证明什么，不能证明什么？

解决：
buildSignalQueueMeta 增加经营问题和 decision 边界：
- primary 首项固定展示“经营问题 xxx”。
- decision 汇总 signalDecisionBoundary 和 buildSignalTriggerRationale(...).actionBoundary。
- 左侧信号行增加“业务问题和对象边界”可见文案。

防错规则：
1. 左侧队列必须先表达经营问题，不能只保留 severity / confidence。
2. 队列层的边界说明必须复用已有信号规则和对象边界，不得前端另造业务事实。
3. 搜索词、广告位、广告组的队列文案必须提示不能自动归因到单个广告 ASIN。
4. 队列文案只能指向人工复核、记录或复盘，不能包装成自动广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
运行态页面必须能渲染 .signalRowDecisionContext。
```

## 2026-06-18：中间关键指标不能裸展示成 BI 指标

```text
问题：
中间诊断区如果只展示花费、订单、销售额、ACOS，用户会把 AI 信号详情误读成普通广告报表。
这些指标本身不能回答“为什么异常、能证明什么、不能证明什么、运营下一步做什么”。

原因：
关键指标只有在绑定业务问题和对象粒度后才有诊断意义。
搜索词机会里的订单、花费、销售额、ACOS只能说明当前投放上下文下的表现，不能自动证明应该加词、调价、否词，也不能自动归因到单个广告 ASIN。

解决：
新增 buildSignalMetricDecisionItems，复用后端 diagnosis_contract.metrics 和合同字段，将关键指标渲染为：
- 服务判断
- 能证明
- 不能证明
- 人工下一步

防错规则：
1. 中间诊断区的核心指标必须说明指标目的，不能只展示数字。
2. 指标解释必须优先消费 diagnosis_contract，不得由前端另造业务事实。
3. 没有 diagnosis_contract 时只能使用保守兜底边界，不能包装成自动广告动作。
4. 搜索词、广告位、广告组相关指标必须继续提示不能自动归因到单个广告 ASIN。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:review-ui
npm run build
后端 /api/signal-triage 的 B00K4W4AAA / beach essentials 链路必须返回 diagnosis_contract。
页面必须能渲染 .metricDecisionCell。
```

## 2026-06-18：单条历史留痕不能静默缺少证据快照

```text
问题：
历史 manual_actions 可能没有 evidence_snapshot。
如果右侧“处理留痕”在这种情况下不展示任何证据说明，用户容易误以为旧留痕已经保存了点击时证据，或误以为当前证据闭环已完成。

原因：
manual_actions 只记录人工动作。
evidence_snapshot 才记录点击时的证据摘要。
旧记录没有 evidence_snapshot 时，不能反推当时为什么处理，也不能证明当前建议有效或当前闭环已落地。

解决：
manualActionEvidenceSnapshotText 在 evidence_snapshot 为空时固定返回：
历史旧留痕缺少证据快照；只能说明曾有人工动作或复盘上下文，不能证明当前证据闭环。

防错规则：
1. 单条历史动作缺 evidence_snapshot 时必须显式提示，不能返回空文本。
2. 不能自动补写旧 manual_actions 的 evidence_snapshot。
3. 旧留痕不能被当成 review_records 或建议有效性证据。
4. 新人工动作仍必须通过 preflight 保存 evidence_snapshot，后续 ReviewTodo 继承同一份证据快照。

验证方式：
npm run test:review-ui
npm run test:signal-ui
npm run test:workbench-structure
npm run build
只读核对 data/runtime/manual_actions/manual_actions.jsonl 中旧记录的 evidence_snapshot 数量。
```

## 2026-06-18：没有当前人工留痕时不能只展示普通空状态

```text
问题：
当前信号没有匹配到人工处理记录时，右侧“处理留痕”如果只展示“暂无人工处理记录”，用户不知道这是未产生证据快照留痕、后端预检尚未转成人工授权写入，还是系统没有可操作事项。

原因：
AI 信号工作台的闭环不是“有无记录”本身，而是：
preflight -> 人工动作 -> manual_actions 证据快照 -> review_todos 复盘排程 -> review_records 复盘结论。
空状态如果不说明缺哪一层证据，会让用户误以为空状态等于不需要处理，或误以为系统已经自动判断完成。

解决：
新增 manualActionEmptyStateText：
没有当前留痕时固定说明“当前未产生证据快照留痕”，并引导先读取后端预检、确认 will_write=false、再由人工选择动作。

新增 reviewTodoEmptyStateText：
没有复盘待办时区分“还没有人工留痕”和“已有留痕但没有 7/14 天待办”，并明确不能保存复盘结论。

防错规则：
1. 处理留痕为空时必须说明“当前未产生证据快照留痕”，不能只写“暂无”。
2. 空状态必须把用户引回 preflight 和人工确认，不得暗示自动写入或自动执行广告动作。
3. 没有 review_todos 时，不能展示可保存复盘结论。
4. ReviewTodo 只是排程，ReviewRecord 才是效果结论。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui
npm run build
npx --no-install playwright screenshot --browser=chromium --channel=chrome --wait-for-selector text=当前未产生证据快照留痕 http://localhost:5175/
```

## 2026-06-18：人工处理路径不能让用户自行拼接

```text
问题：
右侧人工确认区同时有后端预检、证据预览、按钮、处理留痕和复盘待办。
如果没有一个清晰顺序，用户需要自行判断当前应该先看哪块、下一步点击什么、点击后系统会生成什么。

原因：
Amazon 广告 AI 信号工作台的闭环是顺序链路：
后端只读预检 -> 证据快照 -> 人工点击留痕 -> 7/14 天复盘待办 -> 到期后人工保存复盘结论。
这些对象如果分散展示，容易被误读成普通信息卡，或被误解成系统已经自动执行广告动作。

解决：
新增 buildManualActionPathSteps，把当前状态压缩成 4 步：
1. 后端预检
2. 证据快照
3. 人工留痕
4. 7/14 天复盘

页面新增 aria-label="人工处理路径" 的紧凑步骤条，放在后端只读预检之后、人工按钮之前。

防错规则：
1. 右侧人工确认区必须能展示当前处于哪一步，不能只靠按钮和零散说明。
2. 后端预检通过只能说明 will_write=false 且可人工授权，不代表自动写入。
3. 证据快照只在人工点击后保存，不能提前当作已留痕。
4. 7/14 天复盘只表示排程，未到期不判断效果，不保存 review_records。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui
npm run build
npx --no-install playwright screenshot --browser=chromium --channel=chrome --wait-for-selector [aria-label="人工处理路径"] http://localhost:5175/
```

## 2026-06-18：点击后读回不能只靠一句紧凑文案

```text
问题：
人工动作点击后的读回状态如果只用一句“预期/读回”表达，用户仍需要自行拆解：
到底是人工留痕已读回、7/14 天待办已读回，还是复盘结论已保存。

原因：
ManualAction、ReviewTodo、ReviewRecord 是三个不同对象。
如果 UI 把它们压成一句话，用户容易把 ReviewTodo 误读成效果结论，或把留痕误读成广告动作已执行。

解决：
新增 buildManualActionReadbackPathItems，把点击后读回拆成三段：
1. 留痕读回
2. 待办读回
3. 复盘结论

页面新增 aria-label="点击后读回路径"，直接展示三段状态。

防错规则：
1. 点击后读回必须区分 ManualAction、ReviewTodo、ReviewRecord。
2. ReviewTodo 只能说明进入 7/14 天排程，不能证明广告效果。
3. ReviewRecord 未保存时，不能展示效果判断。
4. 任何读回状态都不得暗示系统自动执行广告动作。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui
npm run build
Python Playwright 打开 http://localhost:5175/，等待 aria-label="点击后读回路径"，点击“机会扩量”筛选后确认该区域仍可见。
```

## 2026-06-18：人工动作读回必须核对稳定对象身份

```text
问题：
右侧人工确认区如果只显示“预检通过”“留痕已读回”“待办已生成”，但不展示 signal_id、object_type、object_id 和 market_id，用户仍可能把搜索词、广告 ASIN、广告组或其他站点对象混在一起。

原因：
人工处理闭环依赖稳定对象身份：
当前信号 -> 后端预检目标 -> ManualAction -> ReviewTodo -> ReviewRecord。
任意一层对象身份不一致，7/14 天复盘就可能落到错误对象上。

解决：
新增 buildManualActionIdentityGateItems，把右侧身份门禁拆成四段：
1. 当前信号
2. 预检目标
3. 留痕读回
4. 复盘待办

页面新增 aria-label="人工动作对象身份门禁"，用于展示 signal_id / object_type / object_id / market_id 的一致性。

防错规则：
1. 人工动作前后必须能读回稳定对象身份，不能只读回状态词。
2. signal_id、object_type、object_id、market_id 任一不一致时必须提示待核对。
3. 搜索词、广告 ASIN、广告组不能在人工留痕和复盘待办里混用。
4. 身份门禁只做只读核对，不触发广告动作，不保存 review_records。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui
npm run build
Python Playwright 打开 http://localhost:5175/，等待 aria-label="人工动作对象身份门禁"，点击“机会扩量”筛选后确认该区域仍可见。
```

## 2026-06-18：搜索词展示标签不能当作稳定复盘 ID

```text
问题：
页面右侧“人工动作对象身份门禁”真实读取到后端 preflight 后，仍显示“预检目标 / 待核对”，原因是 object_id 不一致：
当前信号显示 beach essentials，后端预检目标是 search_term:1:beach essentials。

原因：
reviewSignalStableObjectForReviewRecord 优先取 primary_object.label，再取 primary_object.object_id。
对搜索词来说，label 是用户可读展示名，object_id 才是复盘链路稳定 ID。
把 label 作为门禁比较 ID，会误判后端预检目标不一致。

解决：
reviewSignalStableObjectForReviewRecord 改为优先使用 primary_object.object_id，缺失时才退回 label。
补充 prefixed search_term object_id 测试，确认：
1. 当前信号展示 search_term / search_term:1:beach essentials。
2. 预检目标显示身份一致。
3. 不再出现 object_id 不一致。

防错规则：
1. label / object_label 只用于用户可读展示，不能作为 ManualAction、ReviewTodo、ReviewRecord 的稳定对象 ID。
2. 搜索词对象进入人工动作链路时，优先使用后端返回的稳定 object_id，例如 search_term:1:beach essentials。
3. 身份门禁比较顺序必须是稳定 object_id 优先，label 只能兜底。
4. 如果页面显示“待核对”，先核对字段口径，再判断是否真是后端 target 缺失。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
$env:PYTHONPATH='backend'; pytest backend\tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_snapshots_diagnosis_contract_gaps backend\tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot -q
Python Playwright 打开 http://localhost:5175/，确认 .manualActionIdentityGate 显示“预检目标 / 身份一致”，且无 object_id 不一致。
```

## 2026-06-18：不能用一种动作的 preflight 放行另一种人工动作

```text
问题：
页面当前只读取推荐动作 add_to_review 的 preflight，但右侧同时展示记录观察、标记已处理、加入复盘、忽略本次四个按钮。
如果按钮门禁只检查对象 ID 和 will_write=false，就可能用 add_to_review 的 preflight 放行 observe / handled / ignore。

原因：
manualActionButtonGate 原先没有比较 preflight.target.action_type 和当前按钮 actionType。
现有测试也允许 observe 在 add_to_review preflight 下通过。
这会造成 preflight 展示的写后预期、ReviewTodo 生成规则和用户实际点击动作不一致。

解决：
manualActionButtonGate 增加 action_type 一致性检查。
SignalTriageWorkbench 在按钮渲染和点击处理时传入当前按钮 actionType。
动作不一致时禁用按钮，并显示“后端预检动作与当前按钮不一致”。

防错规则：
1. 人工动作按钮必须匹配当前 preflight.target.action_type。
2. 不能用 add_to_review 的 preflight 放行 observe / handled / ignore。
3. 如果要支持四个按钮都可点击，必须为每个动作读取对应 action_type 的 preflight。
4. 禁用态必须说明原因，不能让用户误以为系统自动处理或按钮失效。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
$env:PYTHONPATH='backend'; pytest backend\tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot -q
Python Playwright 打开 http://localhost:5175/，确认 add_to_review 可点击，observe / handled / ignore 因预检动作不一致被禁用。
```

## 2026-06-18：四个人工动作不能共用推荐动作的 preflight

```text
问题：
上一轮为了防止 add_to_review preflight 放行 observe / handled / ignore，先把动作不一致的按钮禁用了。
但从业务规则看，记录观察、标记已处理、加入复盘、忽略本次都是允许的人工动作。
如果页面只读取推荐动作 add_to_review 的 preflight，其他三个合法人工动作会被错误阻断。

原因：
前端只有 manualActionPreflight 单份状态。
按钮门禁已经会校验 action_type，但没有按按钮动作读取对应的后端只读预检。
因此“防止错配”的正确方案不是长期禁用其他按钮，而是为每个 action_type 各自读取 preflight。

解决：
新增 manualActionPreflightsByAction / manualActionPreflightErrorsByAction。
页面选中信号后并行读取 add_to_review、observe、handled、ignore 四份 pre_write preflight。
按钮渲染和点击处理都通过 manualActionPreflightForAction / manualActionPreflightErrorForAction 读取当前动作对应的预检。
没有对应动作预检时，只允许 fallback action_type 完全匹配才回退，否则保持未就绪。

防错规则：
1. 四个允许人工动作必须各自有 action_type 维度的 preflight。
2. 不能把推荐动作 preflight 作为其他按钮的默认门禁。
3. 按钮 actionType、preflight.target.action_type、写入 payload.action_type 必须一致。
4. 如果某个动作预检读取失败，只禁用该动作，并说明读取失败，不影响其他动作的预检结果。
5. 忽略本次仍是人工留痕动作，不生成当前 7/14 天复盘待办，但不能被 add_to_review 的待办规则误禁用。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
$env:PYTHONPATH='backend'; pytest backend\tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot -q
Python Playwright 打开 http://localhost:5175/，确认四个按钮 disabled=false，且页面不含“后端预检动作与当前按钮不一致”。
```

## 2026-06-18：人工动作按钮不能只展示静态意图

```text
问题：
四个人工动作都可用后，按钮第二行仍主要依赖静态文案：
add_to_review 显示 7d/14d 待办，observe / handled 显示留痕 + 复盘，ignore 显示 0 条待办。
这些文案虽然方向正确，但没有明确来自后端 preflight 的 expected_after_write。
用户点击前无法确认当前动作到底会写几条 ManualAction、几条 ReviewTodo。

原因：
按钮层使用的是 manualActionCompactIntent 静态映射。
后端 preflight 已经返回 current_counts 和 expected_after_write，但前端没有把它转成按钮级写后预期。
这会让按钮看起来像普通状态按钮，而不是有明确写入合同的人工动作入口。

解决：
新增 manualActionButtonExpectationText。
从 current_counts.target_manual_action_count / target_review_todo_count 和 expected_after_write 计算写后增量。
按钮副文案优先展示该动作 preflight 的真实预期：
- 复盘类动作：留痕 +1，复盘待办 +2。
- ignore：留痕 +1，复盘待办 +0。
按钮 title / aria-label 同时保留动作解释和写后预期。

防错规则：
1. 人工动作按钮第二行优先使用后端 preflight 的 expected_after_write。
2. 静态文案只能作为 preflight 未读取时的兜底，不能替代写入合同。
3. ignore 必须明确展示复盘待办 +0，避免被误解为也进入 7/14 天复盘。
4. ReviewTodo 仍然只表示排程，不代表效果结论。
5. 按钮展示写后预期不等于自动写入，仍必须由人工点击触发。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
$env:PYTHONPATH='backend'; pytest backend\tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot -q
Python Playwright 打开 http://localhost:5175/，确认四个按钮显示各自“预期写入”，且 ignore 显示“复盘待办 +0”。
```

## 2026-06-19：post_write 读回不能只覆盖推荐动作

```text
问题：
B00K4W4AAA / beach essentials 的写后读回原先重点覆盖 add_to_review。
但右侧允许记录观察、标记已处理、加入复盘、忽略本次四种动作，如果只验证推荐动作，就可能在后续改动中把 observe / handled / ignore 的读回口径重新混掉。

原因：
pre_write 阶段已经按 action_type 分开，但 post_write 阶段同样需要按当前点击动作验收。
ignore 的 ReviewTodo 期望是 0，复盘类动作的 ReviewTodo 期望是 2，两者不能共用同一读回断言。

解决：
新增隔离 API 回归，使用临时 action_root / review_root 循环验证 observe / handled / add_to_review / ignore。
每个动作写入后都用 expect_written=true 读取 post_write preflight，断言 action_type、ManualAction 数量、ReviewTodo 数量、证据快照和 review_records。

防错规则：
1. post_write 读回必须使用当前 record.action_type。
2. add_to_review / observe / handled 写后应读回 7d / 14d 两条待办。
3. ignore 写后应读回 0 条当前复盘待办。
4. post_write 阶段不保存 ReviewRecord，不判断处理效果。
5. 验证人工动作写入必须使用隔离 action_root / review_root，不能污染默认运行数据。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_api_routes.py::test_beach_essentials_manual_action_post_write_readback_distinguishes_action_types -q
```

## 2026-06-21：复盘未到期不能被包装成可保存结论

```text
问题：
右侧复盘区如果只提示“窗口尚未完整”或“到期后再判断”，用户仍可能误以为系统已经具备保存 ReviewRecord 的条件，只是暂时没有展示结果。
这会让 ReviewTodo 被误读成复盘结论，导致人工处理闭环看起来比真实数据更完整。

原因：
ReviewTodo 是 7/14 天复盘排程，ReviewRecord 才是到期后基于处理前后指标保存的结论。
当前默认数据只读检查显示 ready=0、review_records=0、earliest_metric_due_date=2026-06-22。
在处理后指标窗口未完整前，not_ready 只能证明“不能判断”，不能证明有效或无效。

解决：
reviewCheckpointText 的未到期分支明确补充“未到期前不保存复盘记录”。
reviewUi 测试增加反向断言，确保未到期状态不出现“可以保存复盘记录”。
后端现有测试继续验证 not_ready effect 会拒绝保存 ReviewRecord。

防错规则：
1. ReviewTodo 只代表排程，不代表效果结论。
2. not_ready ReviewEffect 只能说明证据窗口不足，不能给出有效/无效判断。
3. 未到期文案必须直接说明“不保存复盘记录”，不能只写等待或观察。
4. 默认运行数据在 ready=0 时不允许保存 ReviewRecord。
5. 复盘结论必须等处理后指标窗口、对象身份、原始证据快照都满足后再进入保存链路。

验证方式：
npm run test:review-ui
python -B -m pytest -p no:cacheprovider tests\test_manual_actions.py::test_review_record_rejects_not_ready_effect tests\test_manual_actions.py::test_review_record_route_rejects_not_ready_effect -q
```

## 2026-06-21：复盘等待态禁止动作不能在前端单独硬编码

```text
问题：
前端“复盘等待窗口”的下一步文案原本只硬编码了“不拉取快照、不保存复盘结论”。
但后端 review_wait_summary.forbidden_actions 已经返回完整门禁：不拉取快照、不保存复盘结论、不自动改规则、不自动执行广告动作。
如果前端单独维护一份简化文案，后端门禁扩展后页面会漏掉关键边界。

原因：
复盘等待态属于业务门禁，不是纯展示文案。
门禁来源应该是后端 review_readiness / signal_triage_service 聚合后的 review_wait_summary。
前端只负责组织信息层级，不应该重新定义哪些动作被禁止。

解决：
buildReviewReadinessGateSummary 等待态复用 waitSummary.forbidden_actions 生成 boundary 和“现在”步骤。
测试补充断言：等待态下一步必须包含“不自动执行广告动作”。
smoke_review_wait_contract 验证 review_readiness 与 signal_triage_service 的 forbidden_actions 一致。

防错规则：
1. 复盘等待态的禁止动作以后端 review_wait_summary.forbidden_actions 为准。
2. 前端不能只展示部分 forbidden_actions。
3. 如果后端没有返回 forbidden_actions，前端才使用安全兜底：不拉取快照、不保存复盘结论、不自动改规则、不自动执行广告动作。
4. 任何 ready 前状态都不能保存 ReviewRecord、自动改规则或自动执行广告动作。

验证方式：
npm run test:signal-ui
python scripts\smoke_review_wait_contract.py --market-id 1
```

## 2026-06-21：最早复盘日期必须区分广告指标和 cross 数据质量待办

```text
问题：
复盘等待窗口如果只写“最早复盘”，用户可能把 cross / 数据质量待办的 due_date 当作广告指标复盘日期。
当前只读数据里 review_identity_audit.earliest_due_date=2026-06-21，但真正广告指标窗口 earliest_metric_due_date=2026-06-22。
如果页面不标明口径，运营可能在广告指标未 ready 时误以为可以保存效果结论。

原因：
ReviewTodo 可以覆盖不同对象类型。
cross / aba_search_term_snapshot 这类数据质量对象不适用广告指标前后对比。
广告指标复盘必须看 review_wait_summary 中过滤后的广告指标对象和日期，不能直接拿所有待办的最早 due_date。

解决：
工作台复盘等待主门禁把“最早复盘”改成“最早广告复盘”。
到期后步骤改为“只读检查广告指标复盘效果”。
测试断言等待态必须包含“最早广告复盘”和“广告指标复盘效果”。

防错规则：
1. 页面展示复盘日期时必须说明口径：广告指标复盘 / 数据质量复查 / cross 待办。
2. 保存 ReviewRecord 前只能使用广告指标 ready 窗口，不能使用 cross 数据质量 due_date。
3. review_wait_summary.earliest_due_date 是主门禁广告指标日期；review_identity_audit.earliest_due_date 只能作为身份审计背景。
4. 任何未 ready 的 cross 待办都不能包装成广告调整有效、无变化或恶化结论。

验证方式：
npm run test:signal-ui
python -B -m pytest -p no:cacheprovider tests\test_review_readiness_cli.py::test_review_identity_audit_separates_metric_due_date_from_cross_due_date tests\test_review_readiness_cli.py::test_review_wait_summary_ignores_cross_todos_for_metric_window -q
```

## 2026-06-21：下一复盘对象不能只藏在等待态详情长句里

```text
问题：
复盘等待窗口虽然已经在 detail 里包含“下一项：advertised_product / B016EXMVZS”，但该信息混在长句里，不利于运营扫读。
用户真正需要的是到期后先看哪个对象，而不是只知道某天后再复核。

原因：
review_wait_summary 已经返回 next_object_type / next_object_id / next_object_label。
前端如果只把这些字段拼进 detail，会让关键对象信息弱于日期和数量，降低复盘路径的可执行性。

解决：
buildReviewReadinessGateSummary 等待态新增指标行：
下一广告对象 = advertised_product / B016EXMVZS。
测试断言该字段必须出现在 items 中。

防错规则：
1. 复盘等待摘要必须同时展示日期和下一对象。
2. 下一对象必须来自后端 review_wait_summary，不能由前端猜测。
3. 下一对象只表示到期后复核目标，不代表当前 ready，也不代表可以保存 ReviewRecord。
4. 如果后端无法识别下一对象，前端只能显示“待识别”，不能编造 ASIN 或对象类型。

验证方式：
npm run test:signal-ui
python scripts\smoke_review_wait_contract.py --market-id 1
```

## 2026-06-21：复盘等待摘要不能缺少下一窗口

```text
问题：
复盘等待摘要已经展示最早广告复盘日期和下一广告对象，但没有把下一复盘窗口作为主字段输出。
用户到期后仍需要判断应该看 7d 还是 14d，容易把“某天后复核”理解成泛化动作。

原因：
review_wait_summary 只聚合了 earliest_due_date、next_object_type、next_object_id。
但 ReviewTodo 的稳定复盘键是 signal_id + action_id + object_type + object_id + review_window。
缺少 review_window 会让复盘路径少一个关键维度。

解决：
review_wait_summary 增加 next_review_window。
scripts\inspect_review_readiness.py、backend signal_triage service 和 smoke_review_wait_contract 保持同一合同。
前端等待门禁新增“下一窗口”，顶部快照边界同步展示 7 天广告指标复盘效果。

防错规则：
1. 复盘等待摘要必须同时展示日期、对象和窗口。
2. next_review_window 必须来自后端 ReviewTodo / ReviewEffect，不得由前端根据日期推断。
3. next_review_window 只表示下一次复核窗口，不代表 ready，也不代表可以保存 ReviewRecord。
4. smoke 合同必须覆盖该字段，防止脚本和服务再次分叉。

验证方式：
npm run test:signal-ui
npm run test:snapshot-ui
python scripts\smoke_review_wait_contract.py --market-id 1
python -B -m pytest -p no:cacheprovider tests\test_review_readiness_cli.py -q
npm run build
```

## 2026-06-21：到期后证据缺口不能继续显示为等待窗口

```text
问题：
前端 buildReviewReadinessGateSummary 原先只要 manual_action_count > 0 且 ready_count=0，就统一展示“复盘等待窗口”。
这在窗口未到期时正确，但到期后如果后端返回 blocked_by_data_gap，页面仍可能提示像是在等待日期，而不是告诉运营缺哪类复盘证据。

原因：
waiting_review_window 和 blocked_by_data_gap 是两个不同业务状态。
前者是时间窗口未完整，不能拉快照或保存结论。
后者是复盘窗口到期后仍缺处理前 / 处理后指标、快照或对象证据，需要补证据后再判断。

解决：
后端 review_wait_summary 和 scripts\inspect_review_readiness.py 在 blocked_by_data_gap 时返回 gap_reasons、message、next_step。
前端 buildReviewReadinessGateSummary 增加 blocked 分支，标题展示“复盘证据缺口”，并把缺口原因和补证据步骤放到主路径。
blocked_by_data_gap 的 forbidden_actions 只禁止保存复盘结论、自动改规则、自动执行广告动作，不再默认禁止人工低频补快照。

防错规则：
1. waiting_review_window 只能表达窗口未到期。
2. blocked_by_data_gap 必须表达缺口原因和下一步补证据方式。
3. 到期后 still not_ready 不能继续包装成“等待窗口”。
4. 缺少处理后快照时，下一步必须提醒先查询积加 API 限流规则，再人工低频补快照。
5. not_ready 只能说明证据不足，不能证明处理有效、无效或恶化。
6. ready 前仍不能保存 ReviewRecord、自动改规则或自动执行广告动作。

验证方式：
npm run test:signal-ui
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_review_wait_summary_explains_data_gap_after_due -q
python -B -m pytest -p no:cacheprovider tests\test_review_readiness_cli.py::test_review_wait_summary_explains_data_gap_after_due -q
python scripts\smoke_review_wait_contract.py --market-id 1
```

## 2026-06-21：ready 状态不能单独作为保存 ReviewRecord 的前端门禁

```text
问题：保存 ReviewRecord 的按钮如果只看 selectedReviewEffect.status === "ready"，会把“数据窗口可评估”误表达成“可以直接保存复盘结论”。这会绕过用户保存前检查清单，削弱人工确认边界。

原因：ReviewEffect.ready 只说明处理前后指标窗口满足复盘判断条件，不代表用户已经核对业务对象、证据口径、能证明什么、不能证明什么和下一步动作。ReviewRecord 是人工复盘记录，不是系统自动生成的广告动作。

解决：前端增加 canSaveReviewRecordWithPreflight。保存按钮和 handleSaveReviewRecord 共同检查 ReviewEffect ready 与 preflight checklist 非空，缺任一条件都不能保存。

防错规则：
1. ready 不是保存 ReviewRecord 的唯一条件。
2. 保存前必须存在人工核对清单。
3. 前端展示保存按钮的条件必须和 handleSaveReviewRecord 的保护条件一致。
4. not_ready、blocked_by_data_gap、waiting_review_window 都不能保存 ReviewRecord。
5. ReviewRecord 保存链路不得被包装成自动调价、自动加词、自动否词或自动执行广告动作。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：复盘日期不能把全部待办和广告指标窗口混用

```text
问题：review_identity_audit.earliest_due_date 表示所有待办最早到期日，其中包含 ABA 数据质量 / cross 待办；但广告复盘保存应该看广告指标对象的最早可复盘日期。如果只看 earliest_due_date，用户可能误以为 2026-06-21 就可以保存广告 ReviewRecord。

原因：复盘身份审计最初关注“能否按原动作读回对象”，所以保留所有待办日期；广告指标复盘等待则关注 advertised_product / sales_product / search_term / placement 的前后指标窗口。这两个口径没有在输出合同里显式分开。

解决：inspect_review_readiness.py 新增 earliest_any_due_date 和 date_boundary，保留 earliest_due_date 兼容旧调用；保存广告复盘记录时明确以 earliest_metric_due_date 为准。前端复盘读回身份门禁展示 date_boundary。

防错规则：
1. earliest_due_date / earliest_any_due_date 只能表示所有待办最早到期日。
2. 保存广告 ReviewRecord 前必须看 earliest_metric_due_date 和 review_wait_summary.earliest_due_date。
3. cross / 数据质量待办不能触发广告指标复盘保存。
4. ready_for_readback 只表示可按原动作读回对象，不表示复盘效果 ready。
5. 未出现 ready 复盘前，不保存复盘结论，不自动改规则，不自动执行广告动作。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_review_readiness_cli.py::test_review_identity_audit_separates_metric_due_date_from_cross_due_date -q
npm run test:signal-ui
npm run build
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：规则反馈候选不能只依赖 ManualAction 旁路证据

```text
问题：ReviewRecord 已经保存 evidence_snapshot，但 review_feedback.candidate_groups 原先仍要求能通过 action_id 找到对应 ManualAction。只要 ManualAction 旁路上下文缺失，已经保存的复盘记录就不能进入语义组或 ABA 参考词的规则反馈候选。

原因：上一轮把证据快照持久化到 ReviewRecord，但候选分组逻辑还停留在“从人工动作找语义组”的旧入口。这样会让最终复盘事实依赖旁路数据，和 ReviewRecord 自解释目标不一致。

解决：candidate_groups 生成时优先读取 ManualAction.evidence_snapshot；如果 action_id 找不到 ManualAction，则退回读取 ReviewRecord.evidence_snapshot。新增测试覆盖 manual_actions 为空、ReviewRecord 自身带语义组和 ABA 边界时仍能生成 search_intent 候选。

防错规则：
1. ReviewRecord 保存后必须能独立支撑复盘事实回看。
2. 规则反馈候选不能只依赖 ManualAction 旁路数据。
3. ManualAction 可作为动作源头优先来源，但不能成为 ReviewRecord 规则反馈的唯一来源。
4. candidate_groups 只能进入解释层和人工复核，不自动改规则，不自动执行广告动作。
5. 真实 review_records=0 时不能伪造规则反馈候选。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_review_readiness_summarizes_saved_review_records_as_rule_feedback tests\test_signal_triage_service.py::test_review_readiness_groups_saved_review_record_snapshot_without_manual_action -q
python -B -m pytest -p no:cacheprovider tests\test_api_routes.py::test_signal_triage_rule_feedback_after_saving_ready_review_record tests\test_review_readiness_cli.py::test_review_readiness_script_feedback_checks_review_record_trace -q
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：ReviewRecord 后端不能把缺失的禁止自动动作声明默认为 false

```text
问题：ReviewRecordRequest 原先把 expected_can_auto_change_rules / expected_can_auto_execute_ads 默认成 false。这样调用方即使没有显式确认“不会自动改规则、不会自动执行广告动作”，后端也会当作 false 接受。前端清单已经要求用户确认边界，但后端合同仍可能被直接 API 调用绕过。

原因：false 默认值无法区分“调用方明确确认 false”和“调用方没有传这个字段”。ReviewRecord 是人工复盘记录，保存前必须显式确认 no-auto 边界，不能靠默认值替用户确认。

解决：ReviewRecordRequest 将两个字段改为可缺失；save_review_record 要求二者必须显式为 false。缺失返回 review_record_preflight_required，传 true 返回 review_record_forbidden_effect。前端 API 类型同步改为必填 boolean。

防错规则：
1. ReviewRecord 保存请求必须显式带 expected_can_auto_change_rules=false。
2. ReviewRecord 保存请求必须显式带 expected_can_auto_execute_ads=false。
3. 后端不能用默认 false 替调用方完成 no-auto 确认。
4. 缺失 no-auto 字段属于预检不完整，不得保存 ReviewRecord。
5. 传入 true 属于越权，不得保存 ReviewRecord。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_manual_actions.py::test_review_record_rejects_missing_forbidden_effect_expectation tests\test_manual_actions.py::test_review_record_route_rejects_missing_forbidden_effect_expectation -q
npm run build
```

## 2026-06-21：保存前清单非空不等于可以保存 ReviewRecord

```text
问题：如果 canSaveReviewRecordWithPreflight 只检查 checklist.length > 0，后续改动可能只保留一个表面清单项，页面仍展示“保存复盘记录”。这会让保存前人工核对退化成形式门禁。

原因：ReviewRecord 保存前至少要确认对象、人工动作证据、复盘窗口、指标口径、复盘结论边界和规则反馈边界。缺任一项，用户都可能误把局部指标变化当作完整业务归因，或误以为系统会自动改规则和执行广告动作。

解决：前端定义必需核对项集合，canSaveReviewRecordWithPreflight 只有在 ReviewEffect ready 且所有必需核对项都存在时才返回 true。保存前清单新增“确认复盘结论边界”，明确该记录只保存当前对象、当前动作和当前 7/14 天窗口的指标变化。

防错规则：
1. checklist 非空不能代表可保存 ReviewRecord。
2. 保存按钮必须要求全部必需核对项齐全。
3. 保存前必须说明复盘结论能证明什么，也必须说明不能证明所有业务变化来自本次人工处理。
4. 缺少“复盘结论边界”时，即使 ReviewEffect ready，也不能展示保存按钮。
5. ReviewRecord 仍只是一条人工复盘记录，不触发自动改规则或自动执行广告动作。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：ManualAction 后端不能把缺失的禁止自动动作声明默认为 false

```text
问题：ManualActionRequest 原先把 expected_can_auto_change_rules / expected_can_auto_execute_ads 默认成 false。直接 API 调用方即使没有显式确认“不自动改规则、不自动执行广告动作”，后端也会按 false 接受并写入人工动作。

原因：false 默认值无法区分“调用方明确确认 false”和“调用方没有传这个字段”。ManualAction 是人工动作留痕入口，包含记录观察、标记已处理、加入复盘、忽略本次，必须统一守住 no-auto 边界。

解决：ManualActionRequest 将两个字段改为可缺失；create_signal_manual_action 要求二者必须显式为 false。缺失返回 manual_action_preflight_required，传 true 返回 manual_action_forbidden_effect。前端 API 类型同步改为必填 boolean，测试补齐成功路径的显式 false，并新增缺失字段拒绝写入用例。

防错规则：
1. ManualAction 写入请求必须显式带 expected_can_auto_change_rules=false。
2. ManualAction 写入请求必须显式带 expected_can_auto_execute_ads=false。
3. 后端不能用默认 false 替调用方完成 no-auto 确认。
4. 缺失 no-auto 字段属于预检不完整，不得保存人工动作。
5. 传入 true 属于越权，不得保存人工动作。
6. ignore 也属于人工动作写入，不能绕过 no-auto 合同。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_manual_actions.py::test_manual_action_route_rejects_missing_forbidden_effect_expectation tests\test_manual_actions.py::test_manual_action_route_rejects_preflight_object_mismatch -q
npm run build
```
## 2026-06-21：规则反馈候选分组不能只依赖边界文案

```text
问题：review_feedback.candidate_groups 原先只有 boundary 文案，虽然 records[] 已经有结构化 action_boundary，但分组层仍可能让后续逻辑依赖中文文本判断“能复核什么、禁止做什么”。

原因：规则反馈候选分组是给人工规则复核看的高层入口，如果这一层只有文案，后续做排序、筛选、测试或界面改版时容易回到解析展示文本的脆弱模式。

解决：candidate_groups[] 新增结构化 action_boundary，复用 allowed_reviews、forbidden_actions 和 boundary。前端类型同步该字段，渲染时优先展示结构化动作边界，原 boundary 只作为展示兜底或补充说明。

防错规则：
1. 规则反馈候选分组必须携带结构化 action_boundary。
2. boundary 文案只能用于展示兜底，不能作为业务逻辑判断来源。
3. allowed_reviews 只能表达人工复核方向，例如复核阈值、复核证据来源、复核建议动作。
4. forbidden_actions 必须明确禁止自动改规则和自动执行广告动作。
5. 没有已保存 ReviewRecord 时，不能生成规则反馈候选，只能展示等待窗口或证据缺口。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_review_readiness_summarizes_saved_review_records_as_rule_feedback -q
npm run test:signal-ui
npm run build
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：规则反馈样本不能只回看证据摘要

```text
问题：review_feedback.records 已经能显示 evidence_drilldown.summary 和 evidence_groups，但这些更像证据摘要。运营进入人工规则复核时，还需要知道该复盘样本原本沿哪条广告诊断路径产生，否则容易只看 worse / improved 标签和指标变化就讨论规则。

原因：ReviewRecord 保存的是复盘事实，不保存完整原始诊断语境。如果规则反馈层只展示复盘结果和证据摘要，用户仍要回到信号详情里手动拼出 Parent ASIN、广告 ASIN、广告组、投放词、搜索词和广告位之间的关系。

解决：review_feedback.records[] 新增 diagnosis_path，从原始 signal evidence_drilldown.business_evidence_blocks 派生排查路径、路径证据、边界和人工下一步。前端规则反馈记录行展示该字段，用于回看原始广告诊断路径。

防错规则：
1. 规则反馈样本必须能回到原始 AI 信号证据链。
2. evidence summary 不能替代诊断路径，尤其不能替代广告 ASIN、广告组、投放词、搜索词和广告位的关系说明。
3. diagnosis_path 只能从原始 evidence_drilldown 派生，不能从复盘备注或前端文案反推。
4. 诊断路径只服务人工复核，不代表可以自动改规则或自动执行广告动作。
5. 没有原始 evidence_drilldown 时，允许 diagnosis_path 为空，但不能伪造路径。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_review_readiness_summarizes_saved_review_records_as_rule_feedback -q
npm run test:signal-ui
npm run build
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：保存 ReviewRecord 前不能只核对指标窗口

```text
问题：ReviewRecord 保存前清单原先只覆盖复盘对象、人工动作证据、复盘窗口、指标口径、复盘结论边界和规则反馈边界。用户仍可能在未回看原始广告诊断路径的情况下，仅凭处理前后指标变化保存复盘结论。

原因：复盘指标只能回答“处理前后窗口有没有变化”，不能回答“当初为什么判断这条广告链路有问题”。如果缺少 Parent ASIN -> 广告 ASIN -> 广告组 -> 投放词 / 搜索词 / 广告位路径，ReviewRecord 后续进入规则反馈时会变成浅层指标复盘。

解决：ReviewRecord 保存前清单新增必选 diagnosis_path，标题为“回看原始诊断路径”。该项从人工动作 evidence_snapshot 读取排查路径和支撑证据；如果历史待办没有路径，必须显示“缺少原始诊断路径”，并限制为只能核对指标事实，不能扩展成规则判断或自动广告动作。

防错规则：
1. 保存 ReviewRecord 前必须出现 diagnosis_path 检查项。
2. diagnosis_path 只能来自人工动作留痕证据快照，不能从复盘结果或前端文案反推。
3. 指标窗口变化不能替代原始广告诊断链。
4. 缺少原始诊断路径时必须显示证据缺口，不能伪造 Parent ASIN、广告 ASIN、广告组、投放词、搜索词或广告位关系。
5. 保存复盘只形成事实记录和人工规则反馈候选，不自动改规则，不自动执行广告动作。

验证方式：
npm run test:review-ui
npm run build
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：人工动作预检快照不能把排查路径排在末尾

```text
问题：business_evidence_blocks 已经包含 diagnosis_path / 排查路径，但 manual_action_preflight 的 ACTIONABLE_EVIDENCE_BLOCK_ORDER 把 diagnosis_path 排在最后。人工动作预检和证据摘要通常优先展示前几条证据，导致用户点击前先看到综合判断、广告组、投放词、搜索词和缺口，却不一定先看到原始广告诊断路径。

原因：之前把排查路径当作普通证据块排序，而不是人工留痕和后续复盘的主轴。这样会和 ReviewRecord 保存前必须回看 diagnosis_path 的规则分裂：保存前要求看路径，但人工动作源头快照里路径被埋到末尾。

解决：将 ACTIONABLE_EVIDENCE_BLOCK_ORDER 中的 diagnosis_path 前置为第一项。测试补齐预检、manual_action 写入和 7d / 14d review_todos 读回，要求 evidence_snapshot 第一条都是“排查路径”。

防错规则：
1. 人工动作预检快照第一条必须是原始广告诊断路径。
2. 预检快照、manual_action.evidence_snapshot 和 review_todos.evidence_snapshot 必须继承同一条排查路径。
3. 综合判断、广告组问题定位、投放词结构、搜索词背景和广告位缺口都只能作为路径下的支撑证据，不能替代路径本身。
4. 排查路径只服务人工确认和复盘回看，不代表系统可以自动调价、加词、否词或暂停广告。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_manual_action_preflight_cli.py -q
python -B -m pytest -p no:cacheprovider tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot tests\test_api_routes.py::test_beach_essentials_manual_action_post_write_readback_distinguishes_action_types -q
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：保存 ReviewRecord 不能绕过 ReviewTodo 证据快照

```text
问题：前端保存前清单已经要求回看“排查路径”，manual_action_preflight 也把排查路径前置到 evidence_snapshot。但直接调用 ReviewRecord 保存接口时，旧后端合同只校验 ready effect、对象身份、复盘窗口和 no-auto 字段，没有要求请求携带当时 ReviewTodo 的证据快照。

原因：ReviewRecord 保存接口把“用户已回看原始诊断路径”当成前端 UI 责任，没有在后端确认 expected_evidence_snapshot 是否存在、是否包含排查路径、是否和当前 ReviewTodo.evidence_snapshot 一致。这样直接 API 调用可能绕过原始广告诊断链，只凭指标窗口保存复盘结论。

解决：ReviewRecordRequest 新增 expected_evidence_snapshot。create_signal_review_record 在 ready 状态下先校验证据快照：缺失返回 review_record_preflight_required，缺少排查路径返回 review_record_missing_diagnosis_path，和 ReviewTodo 不匹配返回 review_record_preflight_mismatch，快照签名不一致返回 review_record_evidence_snapshot_mismatch。save_review_record 服务层也要求 expected_evidence_snapshot 至少包含“排查路径”。

防错规则：
1. 保存 ReviewRecord 的请求必须携带 expected_evidence_snapshot。
2. expected_evidence_snapshot 必须包含 label=排查路径 且 value 非空。
3. 如果请求声明 action_id / object_type / object_id / review_window，后端必须找到同一条 ReviewTodo。
4. 请求证据快照必须和 ReviewTodo.evidence_snapshot 一致，不能接受前端缓存或临时拼接的旧证据。
5. ReviewRecord 仍只保存人工复盘事实，不保存自动广告动作，不自动改规则。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_manual_actions.py::test_review_record_persists_ready_effect_and_can_read_latest tests\test_manual_actions.py::test_review_record_rejects_missing_diagnosis_path tests\test_manual_actions.py::test_review_record_route_saves_ready_effect_and_signal_reads_latest_result tests\test_manual_actions.py::test_review_record_route_rejects_mismatched_preflight_expectation tests\test_manual_actions.py::test_review_record_route_rejects_mismatched_evidence_snapshot tests\test_manual_actions.py::test_review_record_route_rejects_missing_forbidden_effect_expectation tests\test_manual_actions.py::test_review_record_route_rejects_not_ready_effect -q
python -B -m pytest -p no:cacheprovider tests\test_api_routes.py::test_signal_triage_rule_feedback_after_saving_ready_review_record -q
npm run test:review-ui
npm run build
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：人工动作快照不能缺少 AI 准入理由

```text
问题：manual_action_preflight 已经能保存排查路径、诊断证据、证据缺口和补证要求，但 evidence_snapshot_preview 里没有独立保存“AI 准入”事实。用户点击后，ManualAction 和 ReviewTodo 可以回看证据路径，却不能直接回看当时为什么 candidate_count 允许进入人工确认。

原因：AI 准入事实原先停留在 signal_triage.actionability_status 和页面状态里，没有进入人工动作证据快照。后续复盘或规则反馈如果只看 ManualAction / ReviewTodo，会缺少“当初为什么允许人工留痕”的判断边界。

解决：manual_action_preflight 新增 ai_admission_gate 快照块，排序放在“排查路径”之后。该块从 actionability_status 和 signal_status 读取 status、manual_gate_title、candidate_count、can_write_manual_action、boundary 和 next_step，并明确 source=actionability_status。

防错规则：
1. 可进入人工确认的预检快照必须包含“AI 准入”。
2. “AI 准入”必须说明 status、candidate_count 和是否允许人工留痕。
3. “AI 准入”只能证明允许进入人工确认，不能证明系统会自动执行广告动作。
4. ManualAction 和 ReviewTodo 必须继承该准入事实，便于后续 ReviewRecord 和规则反馈回看。
5. candidate_count=0 或未进入经营对象范围时，不能伪造人工动作准入快照。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_manual_action_preflight_cli.py -q
python -B -m pytest -p no:cacheprovider tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot tests\test_api_routes.py::test_beach_essentials_manual_action_post_write_readback_distinguishes_action_types tests\test_manual_actions.py::test_review_record_route_saves_ready_effect_and_signal_reads_latest_result -q
npm run test:review-ui
npm run build
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-type search_term --expected-object-id "search_term:1:beach essentials" --action-type add_to_review
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：保存 ReviewRecord 不能缺少 AI 准入理由

```text
问题：manual_action_preflight 已经把“AI 准入”写入 evidence_snapshot，但 ReviewRecord 保存合同只强制检查“排查路径”。如果历史或异常待办只有排查路径、没有 AI 准入，仍可能只凭指标窗口保存复盘结论。

原因：上一轮只把 AI 准入补进人工动作源头快照，没有把它升级为 ReviewRecord 保存门禁。这样 ManualAction -> ReviewTodo -> ReviewRecord 的最后一步仍可能丢掉“为什么允许进入人工确认”的关键事实。

解决：ReviewRecord 保存合同新增 AI 准入校验。save_review_record 和 create_signal_review_record 都要求 expected_evidence_snapshot 包含 label=AI 准入 且 value 非空。前端保存前清单新增“回看 AI 准入理由”，canSaveReviewRecordWithPreflight 把 ai_admission 作为必需项。

防错规则：
1. 保存 ReviewRecord 的请求必须携带“排查路径”。
2. 保存 ReviewRecord 的请求必须携带“AI 准入”。
3. “AI 准入”只能证明允许进入人工确认，不能证明系统自动执行广告动作。
4. 缺少 AI 准入时返回 review_record_missing_ai_admission，不得降级为普通指标复盘保存。
5. ReviewTodo.evidence_snapshot 与请求 expected_evidence_snapshot 仍必须一致，不能接受前端临时拼接的准入文案。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_manual_actions.py -q
python -B -m pytest -p no:cacheprovider tests\test_api_routes.py::test_signal_triage_rule_feedback_after_saving_ready_review_record tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot tests\test_api_routes.py::test_beach_essentials_manual_action_post_write_readback_distinguishes_action_types -q
cd frontend
npm run test:review-ui
npm run build
cd ..
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：ReviewRecord 保存后不能丢失证据快照

```text
问题：ReviewRecord 保存接口已经校验 expected_evidence_snapshot 必须包含“排查路径”和“AI 准入”，但 ReviewRecord 模型本身没有保存 evidence_snapshot。保存后的 review_records.jsonl 只能看到复盘指标和结论，不能独立回看当时为什么进入人工确认。

原因：上一轮只把证据快照作为保存门禁，没有把同一份已校验快照写入最终复盘记录。这样 review_feedback.records 仍需要依赖当前信号或人工动作旁路数据才能解释原始路径和 AI 准入，长期会造成规则反馈样本证据断链。

解决：ReviewRecord 增加 evidence_snapshot。save_review_record 把已校验的 expected_evidence_snapshot 写入记录。review_feedback.records 和 inspect_review_readiness 输出 evidence_snapshot_count、diagnosis_snapshot、ai_admission_snapshot。前端 ReviewRecord 读回判断要求指标窗口和证据快照同时存在，并在规则反馈样本池展示“保存快照”。

防错规则：
1. ReviewRecord 保存前必须校验证据快照。
2. ReviewRecord 保存后也必须持久化同一份证据快照。
3. ReviewRecord.evidence_snapshot 必须能回看“排查路径”和“AI 准入”。
4. 证据快照只说明当时的人工判断依据，不是新的广告事实，不自动改规则，不自动执行广告动作。
5. 规则反馈样本不能只展示 improved / worse 和指标窗口，必须能回看保存时的证据快照。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_manual_actions.py::test_review_record_persists_ready_effect_and_can_read_latest tests\test_manual_actions.py::test_review_record_route_saves_ready_effect_and_signal_reads_latest_result -q
python -B -m pytest -p no:cacheprovider tests\test_api_routes.py::test_signal_triage_rule_feedback_after_saving_ready_review_record tests\test_signal_triage_service.py::test_review_readiness_summarizes_saved_review_records_as_rule_feedback tests\test_review_readiness_cli.py::test_review_readiness_script_feedback_checks_review_record_trace -q
npm run test:review-ui
npm run test:signal-ui
npm run build
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：脚本和后端复盘日期合同不能分叉

```text
问题：inspect_review_readiness.py 已经输出 review_identity_audit，并区分 earliest_any_due_date 与 earliest_metric_due_date；但后端 build_signal_triage_payload 的 review_status 只透传 review_wait_summary，真实 /api/signal-triage 页面合同拿不到同样的日期边界。

原因：上一轮先修 CLI 和前端 helper，没有同步检查服务层 API 输出。这样页面可能只能看到广告指标等待日期，或拿不到全部待办 / 广告指标日期差异，从而把数据质量 / cross 待办日期误读成广告复盘可保存日期。

解决：build_review_readiness_payload 新增 review_identity_audit，build_signal_triage_payload.review_status 透传该字段；服务层测试覆盖日期边界和 API 透传。

防错规则：
1. CLI 新增复盘 readiness 字段时，必须同步检查后端服务层是否输出同名合同。
2. 前端类型或 helper 依赖的新字段，必须有真实 API 或服务层测试覆盖。
3. earliest_due_date / earliest_any_due_date 只能表示全部待办最早日期，不能用作广告指标 ReviewRecord 保存日期。
4. 广告指标复盘保存前只看 earliest_metric_due_date，并且仍必须通过 ready effect、排查路径和 AI 准入门禁。
5. 页面不能用缺失字段自行拼接业务边界，应优先消费后端明确返回的 date_boundary。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_review_identity_audit_separates_metric_due_date_from_cross_due_date tests\test_signal_triage_service.py::test_signal_triage_exposes_review_identity_audit_from_readiness -q
python -B -m pytest -p no:cacheprovider tests\test_review_readiness_cli.py::test_review_identity_audit_separates_metric_due_date_from_cross_due_date -q
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：历史复盘待办缺证据快照时不能只提示等待窗口

```text
问题：真实运行态中 10 条 ReviewTodo 都缺少 evidence_snapshot。旧 readiness 只提示“最早 2026-06-22 后复盘”，但到期后保存 ReviewRecord 仍会因为缺少“排查路径”和“AI 准入”被后端门禁拒绝。

原因：等待窗口和证据快照是两个独立门槛。7/14 天到期只能说明可以检查处理后指标，不能证明当时人工动作的原始诊断路径和 AI 准入理由仍可读回。

解决：review_identity_audit 增加 missing_evidence_snapshot_count、missing_diagnosis_path_count、missing_ai_admission_count。规则反馈 readiness 增加 blocked_by_review_evidence_gap。CLI、后端 API 和前端身份门禁统一展示证据快照缺口。

防错规则：
1. ReviewTodo 到期不等于可以保存 ReviewRecord。
2. 保存 ReviewRecord 前必须同时满足指标窗口 ready、目标对象稳定、expected_evidence_snapshot 可读回。
3. 历史人工动作缺少 evidence_snapshot 时，不得用当前页面文案静默补成原始证据。
4. 缺少“排查路径”或“AI 准入”时，只能重新人工留痕或人工确认补录，不能自动改规则，不能自动执行广告动作。
5. readiness 输出必须优先暴露证据快照缺口，不能只提示等待日期。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_review_readiness_blocks_legacy_todos_without_evidence_snapshot -q
python -B -m pytest -p no:cacheprovider tests\test_review_readiness_cli.py::test_review_readiness_script_blocks_legacy_todos_without_evidence_snapshot -q
npm run test:signal-ui
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：历史证据快照不能用当前信号静默补录

```text
问题：历史人工动作缺少 evidence_snapshot 后，容易误以为只要当前页面还能生成排查路径和 AI 准入，就可以把这些文案补回旧 manual_actions 或旧 ReviewTodo。

原因：当前信号预检生成的是“现在的诊断证据”，不是当时人工点击时保存的原始证据。若静默补回旧记录，会污染 ReviewRecord 和后续规则反馈样本，让系统误以为历史动作当时具备完整证据链。

解决：新增 inspect_review_evidence_repair.py 只读预检。它按历史 action 分组检查缺口，只判断当前是否能重建同对象证据预览，并固定输出 will_write=false、can_patch_legacy_record=false。真实运行态显示 5 个历史动作都不能由当前信号直接重建同对象证据。

防错规则：
1. 当前信号证据不能伪装成历史点击时保存的 evidence_snapshot。
2. 缺 evidence_snapshot 的旧 ReviewTodo 不能直接保存 ReviewRecord。
3. 补录只能作为人工确认流程，不得静默修改 manual_actions.jsonl。
4. 当前预检目标和历史对象不匹配时，只能作废旧待办或走人工确认补录。
5. 所有补录预检默认只读，不自动改规则，不自动执行广告动作。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_review_evidence_repair_cli.py -q
python scripts\inspect_review_evidence_repair.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --compact
```

## 2026-06-21：诊断合同不能把字符串列表证据当成结构化对象过滤掉

```text
问题：beach essentials 的 recommended_evidence_drilldown.ad_groups 已经返回 2 个广告组名称，但 diagnosis_contract.ad_group_boundary 显示“广告组数 0”，并提示广告组标签不足。

原因：ad_groups 在当前服务输出里是字符串列表，例如广告组名称；诊断合同层使用 _dict_list 读取，只保留 dict，导致字符串证据被全部过滤掉。结果不是数据缺失，而是类型契约读取错误。

解决：新增 _string_list 读取 helper，diagnosis_contract 的广告组边界按字符串列表消费 ad_groups，并在 current_judgement 中展示广告组名称。服务层测试补充断言：beach essentials 链路必须显示 2 个广告组，并列出两个广告组名称。

防错规则：
1. 证据下钻字段如果是字符串列表，诊断合同层不能用 dict list helper 读取。
2. 诊断合同的指标值必须和 recommended_evidence_drilldown 的真实字段类型一致。
3. 搜索词上下文存在时，广告组边界不能误报成 0 个广告组。
4. 广告组名称只能说明投放容器上下文，不能把搜索词自动归因到单个广告 ASIN。
5. 后续新增 diagnosis_contract 字段时，必须补真实字段类型的测试，不只测字段存在。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_signal_triage_payload_adds_diagnosis_contract_for_search_term_path -q
python -B -m py_compile app\services\signal_triage.py
python -c "调用 build_signal_triage_payload(selected_market_id=1, product_scope_id='parent_asin:B00K4W4AAA') 并抽检 ad_group_boundary"
```

## 2026-06-21：作废历史复盘待办不能混成广告人工动作

```text
问题：历史 ReviewTodo 缺 evidence_snapshot 时，需要让坏待办退出 ReviewRecord 保存链路。但如果把这个动作塞进 ManualAction.action_type，容易被误读成“忽略本次信号”或新的广告处理动作。

原因：作废旧待办治理的是复盘链路，不是广告投放对象。它既不代表系统忽略当前 AI 信号，也不代表运营执行了广告动作，更不能补写历史 evidence_snapshot。

解决：新增 ReviewTodoDecisionRecord，持久化到 review_todo_decisions.jsonl。build_review_todos 读取该记录后过滤对应 action_id / review_window 的旧待办。API 和 CLI 都要求显式预检，CLI 默认 dry-run，真实写入必须提供授权码。

防错规则：
1. 作废历史待办只影响 ReviewTodo 生成，不删除 ManualAction。
2. 作废历史待办不能保存 ReviewRecord，也不能自动改规则。
3. 作废记录必须固定 can_auto_change_rules=false、can_auto_execute_ads=false。
4. 目标待办已经包含“排查路径”和“AI 准入”时，不能按缺证据旧待办作废。
5. 作废后必须重新人工留痕生成新的带 evidence_snapshot 的 ReviewTodo，不能把作废当成复盘闭环完成。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_review_todo_void_cli.py -q
python -B -m pytest -p no:cacheprovider tests\test_manual_actions.py::test_review_todo_decision_voids_one_legacy_window_without_deleting_action tests\test_manual_actions.py::test_review_todo_decision_voids_all_windows_when_window_is_none tests\test_api_routes.py::test_review_todo_void_api_removes_legacy_todos_and_blocks_review_record -q
python scripts\apply_review_todo_void_once.py --market-id 1 --action-id manual-action-36ab1ca4680846e2899f747c43c0e800 --expected-object-type advertised_product --expected-object-id B016EXMVZS --compact
```

## 2026-06-21：历史待办治理不能停留在 CLI/API，必须在工作台只读可见

```text
问题：历史缺 evidence_snapshot 的 ReviewTodo 已经能通过 CLI/API 做只读预检和显式作废，但如果工作台页面不展示这个治理状态，用户仍会在右侧建议区看到无法落地的复盘待办，误以为系统只是“后端服务未连接”或普通空状态。

原因：用户操作发生在 AI Copilot 工作台，不在命令行。历史证据缺口是复盘保存链路的关键阻断项，必须和人工确认区同屏表达；但真实作废又是运行态决策，不能因为要可见就直接给页面加一键写入。

解决：新增 /api/review-evidence-repair 只读接口，前端通过 fetchReviewEvidenceRepair 拉取历史待办治理预检，并用 buildReviewEvidenceRepairSummary 渲染“历史待办治理”。页面只展示缺口数量、影响待办、样例对象、人工下一步和边界，不调用 /api/review-todos/void。

防错规则：
1. 工作台可以展示历史待办治理状态，但默认只能只读。
2. 页面不得静默补写旧 ManualAction.evidence_snapshot 或旧 ReviewTodo.evidence_snapshot。
3. 页面不得把历史待办作废包装成广告动作、规则修复或复盘闭环完成。
4. /api/review-todos/void 只能作为显式治理接口，不能被普通信号详情自动触发。
5. 历史治理提示必须说明 will_write=false、不能补写历史证据、不能自动执行广告动作。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_api_routes.py::test_review_evidence_repair_route_returns_readonly_preview -q
python -B -m pytest -p no:cacheprovider tests\test_review_evidence_repair_cli.py tests\test_api_routes.py::test_review_evidence_repair_route_returns_readonly_preview -q
npm run test:workbench-structure
npm run test:signal-ui
npm run build
python scripts\inspect_review_evidence_repair.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --compact
```

## 2026-06-21：广告 ASIN 覆盖层不能只显示数量，必须说明业务判断和归因边界

```text
问题：ad_asin_coverage 原来只显示广告 ASIN 数、直接广告商品行和广告 ASIN 候选层。它能说明“有多少”，但不能回答用户真正关心的“哪些 ASIN 有广告证据、权重多大、推荐搜索词能不能归因、下一步看哪里”。

原因：诊断合同虽然有固定字段，但广告 ASIN 层没有复用 product_scope_drilldown 已经计算出的花费、订单和优先 ASIN，导致内容仍接近裸指标。搜索词候选 direct_ad_product_row_count=0 时，如果不写清楚归因边界，用户容易误以为 beach essentials 可以直接落到某个广告 ASIN。

解决：新增广告 ASIN 合同 helper，将广告 ASIN 数、花费、订单、优先复核 ASIN、直接广告商品行和候选层一起输出，并把 current_judgement、evidence_gap、required_evidence、next_manual_step 改成业务判断。测试样本拆开搜索词信号证据和全量广告商品快照，固定“Parent 范围有广告 ASIN，但搜索词推荐对象不能直接归因”的口径。

防错规则：
1. 诊断合同不能只展示数量指标，必须说明指标服务的业务问题。
2. Parent ASIN 下的未投放子 ASIN 只能作为经营背景，不能进入广告诊断。
3. 搜索词候选 direct_ad_product_row_count=0 时，不能归因到单个广告 ASIN。
4. 广告 ASIN 覆盖层必须优先指向有花费和订单的广告 ASIN 与广告组，而不是停留在 Parent ASIN。
5. 测试样本要区分信号证据和全量快照，避免把广告商品行塞进搜索词信号后掩盖归因边界。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_signal_triage_payload_adds_diagnosis_contract_for_search_term_path -q
python -B -m py_compile app\services\signal_triage.py
npm run test:signal-ui
python -c "调用 build_signal_triage_payload(selected_market_id=1, product_scope_id='parent_asin:B00K4W4AAA') 并抽检 ad_asin_coverage"
```

## 2026-06-21：广告组边界不能只显示容器名，必须说明结构证据和归因边界

```text
问题：ad_group_boundary 原来只显示广告组数、搜索词上下文和广告组结构候选层。它能说明搜索词出现在哪些广告组，但不能说明这些广告组内投放了几个 ASIN、是否多商品、广告位证据是否能解释广告组表现，也不能给运营指出下一步应打开哪个广告组核对什么。

原因：product_scope_drilldown 已经计算出 ad_group_diagnosis，但 diagnosis_contract 没有消费这些结构证据。更细一点，UI 层为了简洁只保留前 6 个广告组，可能把推荐搜索词所在广告组截掉，导致合同层只剩广告组名称，缺投放商品结构。

解决：product_scope_drilldown 新增 ad_group_diagnosis_context，最多保留 12 条轻量广告组诊断上下文；diagnosis_contract.ad_group_boundary 按推荐搜索词所在广告组匹配结构证据，输出已匹配广告组结构、多商品广告组、最大同组 ASIN、有效/无订单词和广告位层级，并把 current_judgement、evidence_gap、required_evidence、next_manual_step 改成业务判断。

防错规则：
1. 广告组边界不能只展示广告组名称或数量。
2. 广告组是投放容器，不是商品；多商品广告组下搜索词不能自动归因到单个 ASIN。
3. 诊断合同要区分广告组级广告位证据和广告活动级广告位证据，不能把后者当成前者。
4. UI 列表截断不能影响诊断合同匹配推荐对象所需的结构证据。
5. 广告组层下一步只能提示人工核对投放商品清单、投放词、同组 ASIN 和主推策略，不能包装成自动调价、自动暂停、自动否词或自动加词。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_signal_triage_payload_adds_diagnosis_contract_for_search_term_path -q
python -B -m py_compile app\services\signal_triage.py
npm run test:signal-ui
运行中 API 抽检 /api/signal-triage?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&top=5
```

## 2026-06-21：没有已保存 ReviewRecord 时不能命名为已保存复盘样本池

```text
问题：前端 buildRuleFeedbackPrioritySummary 在 review_feedback.total=0 但 closure_checklist 存在时，仍把卡片标题显示为“已保存复盘样本池”。这会让用户误以为已有可反哺规则的复盘样本。

原因：closure_checklist 只是复盘输入证据和保存门槛检查，不等于已保存 ReviewRecord。ready 复盘效果也只是可人工保存复盘记录，不能直接变成规则反馈候选。

解决：当 saved ReviewRecord 数量为 0 时，前端标题改为“复盘样本池门槛”，basis 明确“暂无已保存 ReviewRecord；不形成规则反馈候选”，records 和 candidateGroups 固定为空。只有 review_feedback.total > 0 时才展示“已保存复盘样本池”。

防错规则：
1. 规则反馈候选只能来自已保存 ReviewRecord，不能来自 ready ReviewEffect。
2. closure_checklist 只能说明保存门槛，不能包装成规则反馈样本。
3. ReviewEffect.status=ready 只表示可人工保存复盘记录，不表示规则有效或无效。
4. 前端标题必须区分“复盘样本池门槛”和“已保存复盘样本池”。
5. 没有已保存 ReviewRecord 时，页面不得展示 records / candidateGroups 类型的规则反馈候选。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_review_readiness_shows_manual_action_context_coverage_without_saved_review_records -q
```

## 2026-06-21：规则反馈单条记录不能隐去证据快照缺口

```text
问题：规则反馈样本池虽然有 closure_checklist 汇总“复盘证据快照 partial”，但单条 records 文案在某条 ReviewRecord 缺少 evidence_snapshot、排查路径或 AI 准入时可能不显示缺口。用户逐条看样本时会误以为该样本完整可复用。

原因：ruleFeedbackSavedSnapshotText 只有在存在 snapshot 数量或诊断 / AI 准入文本时才输出保存快照片段。缺失时返回空字符串，导致单条记录层无法暴露样本质量问题。

解决：单条记录始终输出保存快照状态；如果缺少 evidence_snapshot、排查路径或 AI 准入，直接显示“缺口：保存快照 / 排查路径 / AI 准入”。完整样本继续显示保存快照数量、排查路径、AI 准入和来源。

防错规则：
1. 规则反馈样本池的汇总清单不能替代单条记录质量说明。
2. 每条 ReviewRecord 样本都必须说明保存快照是否存在。
3. 缺少排查路径或 AI 准入时，单条记录必须显式显示缺口。
4. 缺证据样本只能作为待补证据或低质量样本，不应被包装成完整规则反馈样本。
5. 即使样本结果是 improved，也不能绕过 evidence_snapshot、排查路径和 AI 准入检查。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
GET /api/signal-triage?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&top=5
```

## 2026-06-21：右侧建议区不能脱离诊断合同只展示泛化人工动作

```text
问题：右侧“建议处理 / 人工确认”原来能展示建议动作、风险、不确定性和后端预检对象，但没有直接承接 diagnosis_contract 的业务问题、当前判断、能证明、不能证明和人工下一步。用户在点击人工动作前仍可能只看到“加入复盘”这类泛化动作，而看不到为什么该动作可落地。

原因：中间诊断区已经打深了证据合同，但右侧人工确认区主要消费 manual_action_preview 和 actionability_status；manual_action_preflight 的证据快照也只保存证据缺口和需要补证，缺少当前判断和反证边界。这样会导致人工留痕和后续复盘缺少“当时为什么允许进入人工确认”的完整上下文。

解决：manual_action_preflight 的 evidence_snapshot_preview 新增人工确认判断依据、能证明的事实、不能证明的边界、人工下一步；前端右侧新增“人工确认证据依据”，从 diagnosis_contract 读取业务问题、当前判断和动作边界。

防错规则：
1. 右侧建议区必须承接 diagnosis_contract，不能只展示建议动作、风险和预检对象。
2. 人工留痕证据快照必须保存当前判断和反证边界，不能只保存“缺什么证据”。
3. 搜索词机会进入人工确认时，必须同时说明不能自动加词、调价、否词，也不能自动归因到单个广告 ASIN。
4. 前端可以组织展示诊断合同，但不能自行编造业务结论；业务判断优先来自后端 diagnosis_contract。
5. “加入复盘”只表示人工留痕和 7/14 天待办，不代表建议有效，也不代表执行了广告动作。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_snapshots_diagnosis_contract_gaps -q
python -B -m pytest -p no:cacheprovider tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot -q
npm run test:signal-ui
npm run test:review-ui
npm run test:workbench-structure
npm run build
运行中 API 抽检 /api/manual-action/preflight
```

## 2026-06-21：搜索词机会不能只用 ACOS / CVR 包装成扩量建议

```text
问题：search_term_opportunity 原来主要显示花费、点击、订单、ACOS、CVR。它能说明“这个词表现不错”，但不能回答“为什么值得人工复核、是否有市场热度、是否已有投放词承接、能否归因到广告 ASIN、运营下一步做什么”。

原因：搜索词只是广告表现证据，不是商品对象。ABA 是站点级市场背景，也不是店铺或商品数据。若只看 ACOS / CVR，很容易把搜索词机会包装成自动加词或自动调价建议，偏离 AI Copilot 的人工确认边界。

解决：diagnosis_contract.search_term_opportunity 新增广告表现、效率、投放上下文、投放词证据、ABA 市场热度和广告组承接边界，并把判断写清楚：可进入人工扩量复核，但不能自动加词、调价、否词，也不能自动归因到单个广告 ASIN。

防错规则：
1. 搜索词机会必须同时说明广告表现、投放词承接、市场背景和归因边界。
2. ABA 只能作为站点级市场背景，不能当店铺、广告组或商品数据。
3. 多商品广告组下，搜索词不能自动归因到单个广告 ASIN。
4. ACOS / CVR 只能作为人工复核参考，不能直接生成自动广告动作。
5. 下一步必须落到人工核对投放词、同组 ASIN、主推策略和 7/14 天复盘。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_signal_triage_payload_adds_diagnosis_contract_for_search_term_path -q
python -B -m py_compile app\services\signal_triage.py
npm run test:signal-ui
运行中 API 抽检 /api/signal-triage?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&top=5
```

## 2026-06-21：广告位证据不能把广告活动级背景当广告组级归因

```text
问题：placement_gap 原来只按“广告位上下文数量”判断，容易把两种完全不同的情况混在一起：一种是真的没有广告位证据，另一种是有同广告活动广告位背景，但缺广告组级或搜索词直连广告位证据。

原因：ad_placement_daily_metrics 可能只有 campaign_id / campaign_name，无法按 campaign_id + ad_group_id 对齐到具体广告组。若把这种活动级背景当成广告组级证据，页面会给出无法落地的广告位判断。

解决：diagnosis_contract.placement_gap 新增证据粒度诊断，分别输出推荐对象广告位、匹配广告组、广告组级广告位、广告活动级背景和缺广告位广告组；当只有活动级背景时，明确写出“只能辅助判断活动层流量位置，不能证明搜索词或广告组由广告位导致”。

防错规则：
1. 广告活动级广告位只能作为背景，不能替代广告组级归因。
2. 判断广告组级广告位影响，必须有 campaign_id + ad_group_id 对齐证据。
3. 搜索词推荐对象没有直连广告位时，不能把广告位包装成搜索词原因。
4. 有活动级广告位背景但缺广告组级证据时，下一步只能提示人工核对或补证据。
5. 广告位层不得输出自动调价、自动加词、自动否词或自动暂停广告动作。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_signal_triage_payload_adds_diagnosis_contract_for_search_term_path -q
python -B -m py_compile app\services\signal_triage.py
npm run test:signal-ui
运行中 API 抽检 /api/signal-triage?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&top=5
```

## 2026-06-21：ready 复盘效果不能替代 ReviewTodo 证据签名

```text
问题：ready ReviewEffect 说明处理前后指标窗口可复核，但如果前端没有显式核对 ReviewTodo 的 action_id、对象、窗口和 evidence_snapshot，用户可能误把页面缓存、其他待办证据或重新计算证据保存成 ReviewRecord。

原因：后端已经会校验 expected_evidence_snapshot 与当前 ReviewTodo 的签名是否一致，但前端保存前检查清单原来只强调复盘对象、人工动作证据、诊断路径、AI 准入、指标窗口和规则反馈边界，没有把“这份证据必须来自同一 ReviewTodo”单独作为保存门槛展示。

解决：前端保存前检查新增 todo_evidence_signature 门槛；只有 ReviewTodo 的 action_id / object_type / object_id / review_window 与 ready effect 对齐，且 evidence_snapshot 非空时才允许保存 ReviewRecord。错配时展示 todo_evidence_signature_missing，并说明不能用页面缓存、其他待办或当前重新计算证据保存。

防错规则：
1. ready ReviewEffect 只证明指标窗口可复核，不能证明证据快照属于同一待办。
2. ReviewRecord 的 evidence_snapshot 必须来自对应 ReviewTodo，不得来自页面缓存或重新计算结果。
3. 保存按钮门槛必须同时检查 action_id、object_type、object_id、review_window 和 evidence_snapshot。
4. 前端门槛只是用户可见提示，后端签名校验仍必须保留。
5. 证据错配时只能阻止保存并要求重新读回待办，不能补写或伪造历史证据。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
python -B -m pytest -p no:cacheprovider tests\test_manual_actions.py::test_review_record_route_rejects_mismatched_evidence_snapshot -q
```

## 2026-06-21：规则反馈候选组不能只显示 ABA 参考词

```text
问题：规则反馈候选组已经能按语义组 / ABA 参考词聚合已保存 ReviewRecord，但前端文案只展示“ABA参考：beach essentials”和通用动作边界，没有显式展示后端返回的 ABA匹配边界。用户可能把 ABA 参考词误读成店铺级广告证据或商品归因证据。

原因：后端 candidate_groups 已有 aba_match_boundary 字段，但前端 API 类型和 RuleFeedbackCandidateGroupForUi 没接入，ruleFeedbackCandidateGroupText 也没有渲染这个字段。

解决：前端类型补充 aba_match_boundary，候选组文案新增“ABA边界”。优先展示后端返回的边界；如果有 ABA 参考词但边界缺失，则兜底提示 ABA 只作为站点级市场背景，不能当作店铺、广告组、商品或搜索词归因证据。

防错规则：
1. 规则反馈候选组展示 ABA 参考词时，必须同时展示 ABA 边界。
2. ABA 只能作为站点级市场背景，不能包装成店铺级复盘证据。
3. 语义组 / ABA 参考词只用于人工复核阈值、证据来源和建议动作，不自动改规则。
4. 候选组不能输出自动调价、自动暂停、自动否词、自动加词或自动广告动作。
5. 前端不能因为字段缺失而沉默展示 ABA 参考词，必须有兜底边界文案。

验证方式：
npm run test:signal-ui
python -B -m pytest -p no:cacheprovider tests\test_signal_triage_service.py::test_review_readiness_summarizes_saved_review_records_as_rule_feedback tests\test_signal_triage_service.py::test_review_readiness_groups_saved_review_record_snapshot_without_manual_action -q
npm run test:workbench-structure
npm run build
```

## 2026-06-21：复盘等待窗口不能掩盖历史证据快照缺口

```text
问题：真实运行态同时存在“7/14 天复盘窗口未到期”和“历史 ReviewTodo 缺 evidence_snapshot”。如果首层只展示 waiting_review_window，用户会误以为等到 2026-06-22 后即可保存 ReviewRecord。

原因：review_wait_summary 原来只根据 effect 是否到期生成等待摘要；review_identity_audit 虽然已经发现 missing_evidence_snapshot，但没有反向覆盖等待摘要。前端也只是把 identity audit 作为附属信息展示，没有让 blocked 门禁压过等待状态。

解决：后端和 inspect_review_readiness 脚本在生成 review_identity_audit 后，用审计问题覆盖 review_wait_summary 为 blocked_by_review_evidence_gap；前端 buildReviewReadinessGateSummary 增加兜底，只要 identityAudit.status=blocked，就优先展示“复盘证据门禁阻断”。

防错规则：
1. ReviewTodo 缺 evidence_snapshot、排查路径或 AI 准入时，首层复盘状态必须 blocked，不能只显示等待窗口。
2. earliest_due_date 只能作为上下文，不能替代 evidence_snapshot 门禁。
3. 缺历史证据时不能保存 ReviewRecord，也不能把当前页面证据伪装成历史点击证据。
4. 修复历史缺口只能走重新人工留痕、人工确认补录或显式作废旧待办，不能静默补写。
5. API、CLI 和 UI 的复盘门禁口径必须一致。

验证方式：
npm run test:signal-ui
python -m pytest tests\test_signal_triage_service.py::test_review_readiness_blocks_legacy_todos_without_evidence_snapshot tests\test_review_readiness_cli.py::test_review_readiness_script_blocks_legacy_todos_without_evidence_snapshot
npm run build
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：历史待办治理不能只有策略说明而没有 dry-run 路径

```text
问题：复盘证据门禁已经能识别历史 ReviewTodo 缺 evidence_snapshot，但治理卡片如果只提示“作废旧待办或补录策略”，用户仍无法判断下一步该核对哪条 action、哪个对象、哪个窗口，也无法安全执行。

原因：review_evidence_repair 原来能列出历史缺口和推荐下一步，但没有把现有 apply_review_todo_void_once.py 的 dry-run / execute / 授权码路径接到治理 payload 和前端摘要里。策略正确，但落地链路不够短。

解决：每个可作废的历史缺口项新增 void_plan，包含 dry_run_command、execute_command、required_authorization_code 和边界说明；compact CLI 和前端治理摘要同步展示 dry-run 路径。UI 不提供直接写按钮，execute 仍必须显式授权。

防错规则：
1. 历史 ReviewTodo 治理必须先给 dry-run 路径，不能只写抽象策略。
2. 作废旧待办只能写 review_todo_decisions，不能修改 manual_actions。
3. 缺旧 evidence_snapshot 时不能静默补写，也不能保存对应 ReviewRecord。
4. 前端只能展示治理计划和边界，不自动执行作废。
5. 每条治理计划必须同时包含 action_id、expected_object_type、expected_object_id 和授权码，避免作废错对象。

验证方式：
npm run test:signal-ui
python -m pytest tests\test_review_evidence_repair_cli.py tests\test_review_todo_void_cli.py tests\test_manual_actions.py::test_review_todo_decision_voids_one_legacy_window_without_deleting_action tests\test_manual_actions.py::test_review_todo_decision_voids_all_windows_when_window_is_none tests\test_api_routes.py::test_review_todo_void_api_removes_legacy_todos_and_blocks_review_record
npm run build
python scripts\inspect_review_evidence_repair.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --compact
```

## 2026-06-21：人工留痕写入后验收不能跟随推荐队列漂移

```text
问题：beach essentials 写入人工留痕后，推荐队列会移动到下一个未处理候选 boys sunglasses。如果 manual_action_preflight 的 post-write 验收继续从当前 next_unhandled_candidate 取目标，就会把刚写入的 beach essentials 判成 object_id_mismatch。

原因：写入前预检适合读取当前推荐队列，但写入后验收的主对象应该是 expected_object_id / expected_object_type 对应的已写入 ManualAction / ReviewTodo。原逻辑没有区分“当前待处理候选”和“刚写入待验收对象”。

解决：manual_action_preflight 在 expect_written=true 且传入 expected_object_id / expected_object_type 时，优先从已写入的 ManualAction、ReviewTodo、ReviewRecord 读回目标对象；pre-write 也支持在候选集合中按 expected_object_id 锁定对象。post-write 的 evidence_snapshot_preview 改为读回已保存的 evidence_snapshot，不重新生成当前候选证据。

防错规则：
1. pre-write 可以跟随当前推荐队列，但如果调用方传了 expected_object_id，必须优先锁定该候选。
2. post-write 必须验收刚写入对象，不能跟随 next_unhandled_candidate。
3. post-write evidence_snapshot_preview 必须来自已保存 ManualAction，不能重新生成当前候选证据。
4. 推荐队列移动是正常业务行为，不能因此把已写入对象判成失败。
5. next_unhandled_candidate 和已加入复盘对象必须在 UI 文案中分层展示。

验证方式：
python -m pytest tests\test_manual_action_preflight_cli.py tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot tests\test_review_evidence_repair_cli.py tests\test_review_todo_void_cli.py -q
GET /api/manual-action/preflight?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&expected_object_type=search_term&expected_object_id=search_term:1:beach essentials&action_type=add_to_review&expect_written=true&top=5
GET /api/signal-triage?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&top=5
```

## 2026-06-21：Windows PowerShell UTF8 BOM 会让 JSONL 第一行被加载器跳过

```text
问题：修复 manual_actions.jsonl 的中文 action_note 时，使用 Windows PowerShell Set-Content -Encoding UTF8 重写文件，结果文件第一行带 BOM。manual_actions 加载器按 utf-8 逐行 json.loads，第一行触发 Unexpected UTF-8 BOM，被跳过，manual_action_count 从 18 变成 17。

原因：Windows PowerShell 5.1 的 UTF8 默认带 BOM，而 JSONL 逐行解析不接受首行 BOM。这个问题不会报服务崩溃，但会让运行态统计少一条记录，属于隐蔽数据质量问题。

解决：运行时 JSONL 修复必须用无 BOM UTF-8 写回，例如 System.Text.UTF8Encoding(false)。修复后 load_manual_actions(market_id=1) 恢复 18 条，beach essentials 目标 action 仍为 1 条。

防错规则：
1. 不用 Set-Content -Encoding UTF8 重写 JSONL 运行数据。
2. 重写 JSONL 后必须立刻用后端 loader 验证记录数，而不是只数文件行数。
3. 发现 manual_action_count 异常下降时，先检查首行 BOM 和 json.loads 失败。
4. 中文 action_note / operator_name 修复要同时检查 API 读回结果。

验证方式：
load_manual_actions(market_id=1)
GET /api/signal-triage?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&top=5
GET /api/manual-action/preflight?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&expected_object_type=search_term&expected_object_id=search_term:1:beach essentials&action_type=add_to_review&expect_written=true&top=5
```

## 2026-06-21：复盘对象不能和下一待处理候选混在同一层表达

```text
问题：beach essentials 写入人工留痕后进入 waiting_review_window，同时推荐队列移动到下一条未处理候选 boys sunglasses。如果前端只显示“当前信号 / 复盘等待 / 下一候选”而不分层，用户会误以为 boys sunglasses 已经加入复盘，或 beach essentials 仍是当前待处理候选。

原因：复盘链路和候选队列是两条不同业务链路。ReviewTodo 读回的是已写入 ManualAction 的对象；next_unhandled_candidate 表示队列里下一条可人工确认对象。二者同时出现在 /api/signal-triage 中，但含义不同。

解决：前端复盘状态卡新增“复盘对象与候选队列”摘要，分别展示已加入复盘对象、复盘窗口、证据快照、下一待处理候选和对象关系。真实页面现在显示 beach essentials 为复盘对象，boys sunglasses 为下一候选，并明确“不同对象，分开处理”。

防错规则：
1. waiting_review_window 只能说明已人工留痕对象等待 7/14 天效果窗口，不能说明 next_unhandled_candidate 已处理。
2. next_unhandled_candidate 只能作为下一轮人工确认候选，不能继承上一个对象的 ReviewTodo 或复盘结论。
3. 页面同时出现复盘对象和下一候选时，必须显式展示对象关系。
4. 复盘对象和下一候选不能混合归因，不能因此自动加词、否词、调价或保存 ReviewRecord。
5. 前端读取 readback_keys 时必须识别真实字段 due_at，不能只按假设字段 due_date 展示窗口。

验证方式：
npm run test:signal-ui
npm run build
浏览器只读检查 http://localhost:5175/ 中的“复盘对象与候选队列”
GET /api/signal-triage?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&top=5
```

## 2026-06-21：诊断合同不能继续绑定已进入复盘等待的推荐对象

```text
问题：beach essentials 已经人工留痕并进入 waiting_review_window 后，后端 diagnosis_contract 仍使用 recommended_candidate / recommended_evidence_drilldown，前端也只在选中 recommended_candidate 时展示合同。结果是下一条可处理候选 boys sunglasses 缺少完整“指标目的 / 能证明 / 不能证明 / 人工下一步”，而已进入复盘的 beach essentials 仍像当前待处理对象。

原因：recommended_candidate 和 next_unhandled_candidate 是两个不同阶段的对象。recommended_candidate 可以是已处理并等待复盘的对象；next_unhandled_candidate 才是当前可进入人工确认的候选。diagnosis_contract 原来没有随着 manual_action_preview 的实际目标切换。

解决：后端在存在 next_unhandled_candidate 时，让 diagnosis_contract 使用 next_unhandled_candidate 和 next_unhandled_evidence_drilldown，并返回 signal_id / object_id。前端用 diagnosis_contract.signal_id 决定是否展示诊断合同和人工确认证据。

防错规则：
1. diagnosis_contract 必须绑定当前可人工确认对象，而不是固定绑定 recommended_candidate。
2. 当 recommended_candidate 已有人工留痕且存在 next_unhandled_candidate 时，合同、预检和右侧人工确认证据必须指向 next_unhandled_candidate。
3. 已进入 ReviewTodo 等待窗口的对象只能展示复盘等待和读回身份，不能继续包装成人工确认前诊断对象。
4. 前端不能只用 recommended_candidate.signal_id 判断是否展示诊断合同，必须优先使用 diagnosis_contract.signal_id。
5. 合同目标切换后仍不得输出自动加词、否词、调价或暂停广告动作。

验证方式：
python -m pytest tests\test_signal_triage_service.py::test_signal_triage_points_to_next_unhandled_candidate_when_recommended_is_waiting tests\test_signal_triage_service.py::test_signal_triage_payload_adds_diagnosis_contract_for_search_term_path -q
npm run test:workbench-structure
npm run test:signal-ui
npm run build
浏览器只读检查 http://localhost:5175/ 中诊断合同和人工确认证据是否显示 boys sunglasses
GET /api/signal-triage?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&top=5
```

## 2026-06-21：人工预检不能把诊断合同展示 ID 当写入 ID

```text
问题：boys sunglasses 的 diagnosis_contract.object_id 是展示型搜索词文本，而 manual_action_preview.object_id 是快照行级写入 ID gerpgo_market_1_20260616_120443:507942344。如果后续预检或保存链路误用 diagnosis_contract.object_id，就可能出现预检目标找不到候选、保存身份漂移，或复盘待办无法读回。

原因：诊断合同服务的是用户阅读和业务判断，object_id 可以是更易懂的业务标签；人工动作保存服务的是可读回的稳定身份，必须使用 manual_action_preview.object_id / object_type / action_type。

解决：新增 test_manual_action_preflight_snapshots_next_unhandled_diagnosis_contract，模拟 beach essentials 已进入复盘等待、boys sunglasses 为下一候选，断言预检目标使用 manual_action_preview.object_id，并且 evidence_snapshot_preview 中的 diagnosis_contract 块包含 boys sunglasses、不包含 beach essentials。

防错规则：
1. 人工预检和保存链路必须以 manual_action_preview.object_id 为写入身份。
2. diagnosis_contract.object_id 只可作为诊断合同展示对象，不能直接替代人工动作写入 ID。
3. 当 recommended_candidate 已进入复盘等待且存在 next_unhandled_candidate 时，预检目标、诊断合同快照和右侧人工确认证据必须一致指向 next_unhandled_candidate。
4. evidence_snapshot_preview 必须保留 diagnosis_contract 的判断依据、能证明、不能证明、人工下一步、证据缺口和需要补证。
5. 预检通过不代表执行广告动作；仍然只允许记录观察、标记已处理、加入复盘或忽略本次。

验证方式：
python -m pytest tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_snapshots_next_unhandled_diagnosis_contract tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_snapshots_diagnosis_contract_gaps -q
GET /api/manual-action/preflight?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&expected_object_type=search_term&expected_object_id=gerpgo_market_1_20260616_120443%3A507942344&action_type=add_to_review&expect_written=false&top=5
```

## 2026-06-21：忽略本次不能跳过人工预检证据快照

```text
问题：ignore 不生成 7/14 天复盘待办，如果后端只对 observe / handled / add_to_review 校验证据快照，ignore 就可能写入一条没有证据的人工留痕。

原因：之前把“是否生成复盘待办”和“是否需要人工预检证据”混成同一个 REVIEWABLE_ACTION_TYPES 判断。ignore 的确不是复盘类动作，但仍是人工动作，仍需要可回看的证据链。

解决：/api/signals/{signal_id}/manual-actions 对四个动作统一要求 evidence_snapshot，并统一调用 _validate_manual_action_preflight 校验对象、动作类型和证据快照签名。ignore 仍保持 target_review_todo_count = 0。

防错规则：
1. 记录观察、标记已处理、加入复盘、忽略本次都必须先通过 preflight。
2. ignore 不生成当前 7/14 天复盘待办，但必须保存 evidence_snapshot。
3. 后端不能只用 REVIEWABLE_ACTION_TYPES 判断是否需要证据快照。
4. 前端构造人工动作 payload 时必须优先使用当前 action_type 对应 preflight 的 evidence_snapshot_preview.items。
5. 忽略本次不代表误报、删除信号或自动执行广告动作，只代表当前不进入复盘待办。

验证方式：
python -m pytest tests\test_api_routes.py tests\test_manual_action_preflight_cli.py -q
npm run test:review-ui
GET /api/manual-action/preflight?market_id=1&product_scope_id=parent_asin:B00K4W4AAA&expected_object_type=search_term&expected_object_id=gerpgo_market_1_20260616_120443%3A507942344&action_type=ignore&expect_written=false&top=5
```

## 2026-06-21：右侧主预检面板不能固定展示推荐动作

```text
问题：四个人工动作已经分别读取 preflight，但右侧“后端只读预检 / 将保存的证据快照 / 点击后验收合同”主面板仍固定展示推荐动作的 manualActionPreflight。用户查看 ignore 时，可能误以为 ignore 也会生成 7/14 天待办，或误以为所有按钮保存同一份动作预期。

原因：manualActionPreflight 原本只承载推荐动作预检。后续补齐 manualActionPreflightsByAction 后，按钮本身已经 action-specific，但主面板仍未切到当前动作的 preflight。

解决：前端新增 manualActionPreviewActionType。按钮 focus / hover 时切换当前预览动作；主预检、路径、身份门禁、证据缺口、证据快照和点击后合同统一读取 selectedManualActionPreviewPreflight。manualActionPreflightErrorForAction 同步修正，避免某个动作成功时继承推荐动作错误。

防错规则：
1. 右侧主预检面板必须展示当前预览 action_type，不得默认把推荐动作当作所有按钮口径。
2. 证据快照预览、点击后合同和身份门禁必须使用同一份 selectedManualActionPreviewPreflight。
3. focus / hover 只改变预览，不触发写入。
4. 写入仍必须由按钮自己的 action_type、preflight gate 和 evidence_snapshot payload 决定。
5. action-specific error 为 null 时，不能继承 fallback error。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-21：复盘记录保存入口不能只靠按钮是否出现表达门槛

```text
问题：ReviewRecord 保存按钮只在 effect ready 且保存前 checklist 完整时出现，逻辑上安全，但按钮不出现时用户看不出是未到期、未 ready、缺诊断路径、缺 AI 准入，还是已有匹配复盘记录。

原因：保存门槛只存在于 canSaveReviewRecordWithPreflight 的布尔判断里，没有被常驻表达成用户可读的业务状态。对运营来说，“没有按钮”不是一个可执行的解释。

解决：新增 buildReviewRecordSaveGateSummary，把 ReviewTodo、ReviewEffect、保存前 checklist 和已匹配 ReviewRecord 汇总成 blocked / ready / saved 三类门槛提示。右侧复盘待办区常驻展示“复盘保存门槛”，保存按钮改为读取 gate.canSave，已有匹配 ReviewRecord 时不再显示重复保存入口。

防错规则：
1. ReviewRecord 保存入口必须说明当前不能保存的具体原因，不能只隐藏按钮。
2. ready 只代表可以人工复核，不代表自动保存结论。
3. 保存前必须同时满足 ready 复盘效果和完整保存前 checklist。
4. 已有同一 action_id / object_id / review_window 的匹配 ReviewRecord 时，不再展示重复保存入口。
5. 保存 ReviewRecord 只沉淀人工复盘结论和证据快照，不自动改规则，不自动执行广告动作。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
GET http://localhost:5175/
```

## 2026-06-21：诊断合同不能只平铺字段，必须前置证据强度和证明边界

```text
问题：后端 diagnosis_contract 已经有业务问题、能证明、不能证明、证据缺口和人工下一步，但前端原来主要按判断块平铺展示。用户要读完多个小字段后才知道整体证据强度和业务边界，容易退化成“指标展示”。

原因：证据解释没有在第一眼形成摘要。证据强度散在置信度、原因文本和诊断合同细节里，没有被组织成一个面向运营判断的入口。

解决：新增 buildSignalDiagnosisEvidenceSummary，从现有 signal confidence / evidence_count / data_sources / diagnosis_contract 汇总“证据强度和判断边界摘要”。中间诊断合同区先展示摘要，再展示具体判断块。

防错规则：
1. 诊断合同区必须先回答证据强度、能证明什么、不能证明什么，再展示细节。
2. 证据强度只能来自现有 signal 置信度、证据数量、证据来源和诊断合同，不额外创造事实。
3. `does_not_prove` 必须在摘要层可见，不能藏在长列表末尾。
4. 证据缺口和人工下一步必须与右侧人工确认证据口径一致。
5. 摘要不能输出自动加词、自动否词、自动调价或自动暂停广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
GET http://localhost:5175/
```

## 2026-06-21：人工证据快照不能缺少搜索词和广告位边界

```text
问题：右侧人工确认区已经能展示搜索词边界和广告位边界，但后端 evidence_snapshot_preview 之前只保存诊断合同、泛化边界和指标。人工动作写入后，7/14 天复盘待办可能只继承泛化证据，缺少当时最关键的搜索词和广告位边界。

原因：前端展示和后端保存不是同一件事。复盘只应该信任点击时保存并校验过的 evidence_snapshot，如果边界只存在于当前页面文案，后续复盘无法证明当时用户看到了什么证据层级。

解决：manual-action preflight 在 evidence_snapshot_preview 中新增“搜索词边界”和“广告位边界”，来源为 product_scope_drilldown.ad_group_diagnosis。前端处理依据摘要将这两类边界提升为优先展示项。

防错规则：
1. 右侧展示过的关键边界，若会影响人工动作判断，必须进入后端预检 evidence_snapshot_preview。
2. 搜索词边界必须随人工留痕保存，不能只作为前端临时文案。
3. 广告位边界必须说明广告组级、活动级背景或缺口，不能在复盘时只剩广告位数量。
4. manual_actions 和 review_todos 必须继承同一份预检快照，不能写入前端旧快照或重建快照。
5. 保存边界证据只服务人工复盘，不代表自动加词、否词、调价、广告位加价或暂停广告。

验证方式：
python -m pytest tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot tests\test_api_routes.py::test_beach_essentials_manual_action_post_write_readback_distinguishes_action_types -q
python -m pytest tests\test_manual_action_preflight_cli.py -q
npm run test:review-ui
npm run test:workbench-structure
npm run build
GET http://localhost:5175/
GET /api/manual-action/preflight 确认 evidence_snapshot_preview 包含“搜索词边界”和“广告位边界”
```

## 2026-06-21：右侧人工确认不能丢失中间诊断边界

```text
问题：中间广告组卡片已经说明搜索词业务判断和广告位证据层级，但右侧人工确认区只展示候选对象、广告组关系和通用边界。用户点人工动作前可能看不到搜索词不能归因、广告位证据不足等关键约束。

原因：右侧人工区是最终动作入口。如果这里只保留泛化建议，用户会把中间诊断里已经讲清楚的对象边界遗忘，页面就会从 AI Copilot 工作台退回“建议按钮集合”。

解决：ManualActionCandidateAdGroupBridge 增加 searchTermBoundary、placementBoundary 和 manualNextStep；右侧“人工候选与广告组关系”新增“人工点击前边界复核”，在按钮前展示搜索词边界、广告位边界和人工下一步。

防错规则：
1. 右侧人工动作入口必须承接中间诊断的关键边界，不能只展示候选对象和按钮。
2. 搜索词边界必须说明能证明什么、不能证明什么，尤其不能自动加词、否词、调价或归因到单个广告 ASIN。
3. 广告位边界必须说明证据层级，不能把活动级背景当广告组级判断。
4. 人工下一步只能指向记录观察、标记已处理、加入复盘或忽略本次前的人工复核。
5. 右侧按钮只保存人工留痕或复盘待办，不执行广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
GET http://localhost:5175/
```

## 2026-06-21：点击后读回短文案不能把复盘待办写得像效果结论

```text
问题：右侧人工动作点击后读回路径已有详细说明，但最短状态行曾写成“预期：复盘类 7d/14d；读回一致：留痕 + 7d/14d，未存结论”。运营扫读时容易把 7d/14d 理解成效果窗口已经成立，而不是复盘待办排程。

原因：详细文案和短文案的信息强度不一致。详细区说明了待办只是排程，但 compact 文案为了短而省略了“待办 / 排程 / 未到 ready”的业务边界。

解决：manualActionPostWriteExpectationSummaryText 明确写成“复盘类只生成 7d/14d 待办（排程）”；manualActionReadbackCompactText 在最短读回文案中展示“7d/14d 待办”“仅排程”“未存结论”；ignore 的读回文案补充“不代表误报”。

防错规则：
1. 所有出现 7d/14d 的人工动作读回文案，都必须同时说明它是复盘待办或排程。
2. 没有 ReviewRecord 前，不能把待办展示成“已改善”“已无效”“已恶化”。
3. 忽略本次只表示当前不进入复盘待办，不代表误报、删除信号或清空证据。
4. compact 文案不能牺牲对象边界，最短状态也要区分 manual_actions、review_todos、review_records。
5. 文案修正不得改变写入链路，不得新增自动广告动作。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-21：广告证据链导览不能只列路径节点

```text
问题：Parent ASIN 经营入口下，广告证据链导览已经能列出经营入口、广告 ASIN、广告组、搜索词/广告位和 AI 准入，但如果只展示路径节点，运营仍然需要自己判断这条链路能不能落地到人工复核。

原因：路径节点说明“看哪里”，但没有先回答“为什么看这层、这层能证明什么、不能证明什么、下一步人工做什么”。这会让工作台退化成指标导航，而不是 AI 信号诊断。

解决：新增 buildProductScopeEvidenceRouteDecision，基于 ProductScopeEvidenceMatrix 生成路径可落地判断；前端在广告证据链导览顶部展示业务问题、当前判断、能证明、不能证明和人工下一步。

防错规则：
1. Parent ASIN 路径下，导览必须先回答当前广告链路是否足够支撑人工复核。
2. 未投放子 ASIN 不能因为属于 Parent ASIN 就进入广告信号分析。
3. 搜索词、广告位和 ABA 市场热度不能自动归因到单个广告 ASIN。
4. AI 准入为 candidate_count=0 或只能诊断时，页面只能引导只读诊断或补证据，不能包装成人工动作写入。
5. 路径摘要只能复用现有证据矩阵，不额外创造后端事实或自动广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
GET http://localhost:5175/
```

## 2026-06-21：广告 ASIN 对比表不能只展示指标

```text
问题：Parent ASIN 首屏和商品组详情的广告 ASIN 对比表已经展示广告 ASIN、花费、订单、销售额、ACOS 和策略，但这些指标本身不能直接告诉运营哪个 ASIN 值得先下钻，也不能说明下钻判断的边界。

原因：广告 ASIN 表如果只展示指标，就会退化成普通报表。运营仍然需要自己推理“高花费低订单是否该看搜索词”“有策略说明时是否不能判异常”“搜索词和广告位是否能归因到该 ASIN”。

解决：ProductGroupAdAsinRow 增加 decision 字段；首屏和详情表新增“下钻判断”列，展示状态、原因和下一步。判断只来自广告商品粒度指标和策略说明，不把搜索词、广告位或 ABA 归因到该 ASIN。

防错规则：
1. 广告 ASIN 对比表必须说明为什么这个 ASIN 值得或不值得下钻，不能只展示花费、订单、销售额和 ACOS。
2. 下钻判断只能使用广告商品粒度指标和明确策略说明。
3. 搜索词、广告位和 ABA 只能作为后续上下文复核，不能在广告 ASIN 表里写成确定归因。
4. 有主推款或策略说明时，集中消耗不能直接判异常，必须写成按策略复核。
5. 表格点击只切换诊断范围，不代表自动调价、自动加词、自动否词或自动暂停广告。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
GET http://localhost:5175/
```

## 2026-06-21：广告组问题定位不能只展示指标和复核文案

```text
问题：广告组问题定位卡片已经展示花费、订单、ACOS、广告 ASIN 数、搜索词数和人工复核判断，但用户仍需要自己判断问题到底落在搜索词意图分化、无订单花费词、广告 ASIN 混合还是广告位证据缺口。

原因：广告组是投放容器，不是商品。只展示广告组汇总指标会掩盖投放商品、搜索词和广告位之间的关系，容易让运营把广告组汇总结果误读成单个 ASIN 结论。

解决：ProductScopeAdGroupDiagnosisRow 增加 problemLocator；广告组卡片新增“广告组问题落点”，先展示业务问题、当前判断、问题落点、为何拆开看、不能证明和人工下一步。

防错规则：
1. 广告组问题定位必须先说明问题落点，不能只展示花费、订单、ACOS 和搜索词数量。
2. “建议拆开看”只能表示人工复核路径，不能表示自动拆广告组。
3. 多广告 ASIN 广告组下，搜索词和广告位不能自动归因到单个广告 ASIN。
4. 没有搜索词或广告位证据时，必须展示证据缺口，不能用泛化指标填充。
5. 广告组卡片里的人工下一步只能指向记录观察、标记已处理、加入复盘或忽略本次前的人工复核，不得出现自动广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
GET http://localhost:5175/
```

## 2026-06-21：搜索词问题定位不能只列有效词和无订单花费词

```text
问题：搜索词问题定位已经能列出有效搜索词和无订单花费词，但如果不说明这些词服务什么业务判断，用户仍然需要自己判断它们代表放量机会、浪费风险还是仅仅是广告组上下文。

原因：搜索词是用户查询表现，投放词是触发来源，广告组是容器。只列搜索词和指标会让页面退回普通报表，也容易让运营把搜索词误读成自动加词、自动否词或单个广告 ASIN 归因。

解决：ProductScopeSearchTermDiagnosis 增加 decision；搜索词卡片新增“搜索词业务判断”，展示业务问题、当前判断、投放词证据、能证明、不能证明和人工下一步。搜索词条目补充投放词展示。

防错规则：
1. 搜索词问题定位必须说明业务判断，不能只列有效词和无订单花费词。
2. 投放词只能解释搜索词触发来源，不能自动包装成加词建议。
3. 搜索词不能自动归因到单个广告 ASIN。
4. 无订单花费词只能进入人工复核，不等于自动否词。
5. 搜索词层的下一步只能指向人工复核、记录观察或加入复盘，不得出现自动广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
GET http://localhost:5175/
```

## 2026-06-21：广告位证据不能只展示数量或活动级背景

```text
问题：广告组卡片已经展示广告位数量和活动广告位数量，但如果不说明证据层级，用户可能把活动级广告位背景误读成广告组级广告位判断，也可能把广告位数量误读成可直接调整广告位加价的依据。

原因：广告位只说明流量位置表现。广告活动级广告位背景不能替代广告组级证据，广告组级广告位也不能自动归因到单个搜索词或广告 ASIN。

解决：ProductScopeAdGroupDiagnosisRow 增加 placementDecision；广告组卡片新增“广告位证据判断”，展示业务问题、当前判断、证据层级、能证明、不能证明、证据缺口和人工下一步。

防错规则：
1. 广告位层必须说明证据层级，区分广告组级、活动级背景和缺口。
2. 活动级广告位背景不能替代广告组级判断。
3. 广告位不能自动归因到单个搜索词或广告 ASIN。
4. 广告位证据只能指导人工复核，不能包装成自动调整广告位加价、预算或竞价。
5. 缺少广告位证据时必须展示证据缺口，不能用搜索词或广告 ASIN 指标替代广告位判断。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
GET http://localhost:5175/
```

## 2026-06-21：保存 ReviewRecord 不能缺少搜索词和广告位边界

```text
问题：人工动作证据快照已经保存了“搜索词边界”和“广告位边界”，但 ReviewRecord 保存前门槛仍只要求“排查路径”和“AI 准入”。这会让 ready 复盘效果在缺关键边界时仍可能被保存为规则反馈样本。

原因：ReviewEffect.ready 只证明处理前后指标窗口可比较，不证明当时点击人工动作时已经看过搜索词归因边界和广告位证据层级。搜索词和广告位都是广告排查关键证据，缺失时不能落地到可靠复盘。

解决：后端 save_review_record 和 API preflight 增加搜索词边界、广告位边界硬校验；前端保存前检查新增两项必检；测试样本同步要求四类关键证据齐全。

防错规则：
1. ReviewRecord 保存必须同时具备排查路径、AI 准入、搜索词边界和广告位边界。
2. ReviewEffect.ready 不能单独作为保存 ReviewRecord 的依据。
3. 缺搜索词边界时，不能把搜索词表现归因到单个广告 ASIN，也不能沉淀成加词、否词或调价规则反馈。
4. 缺广告位边界时，不能把活动级广告位背景当成广告组级判断，也不能沉淀成广告位调整反馈。
5. 前端 checklist 只是用户表达，后端必须保留同等硬校验。

验证方式：
python -m pytest tests\test_manual_actions.py -q
python -m pytest tests\test_api_routes.py -q
npm run test:review-ui
npm run test:workbench-structure
```

## 2026-06-21：复盘等待不能把旧待办缺边界误报成只等窗口

```text
问题：ReviewRecord 保存已经要求搜索词边界和广告位边界，但 review readiness / repair 治理层仍只识别缺 evidence_snapshot、排查路径和 AI 准入。当前真实旧待办有 15 条 evidence_snapshot，缺搜索词边界和广告位边界，如果只显示等待 7/14 天窗口，用户会误以为到期后可以保存复盘记录。

原因：保存合同升级后，读回门禁和历史待办治理没有同步升级。ReviewTodo 的 evidence_snapshot 是否完整，必须在等待层和治理层提前暴露，不能把失败留到 ReviewRecord POST。

解决：review readiness 审计新增搜索词边界和广告位边界计数与 issue；review_wait_summary 和 rule_improvement 把这些缺口标成 blocked_by_review_evidence_gap；review_evidence_repair 的必需标签升级为四项；前端读回身份门禁同步展示两个新缺口。

防错规则：
1. ReviewRecord 保存合同升级时，review readiness、repair 脚本和前端门禁必须同步升级。
2. 缺搜索词边界或广告位边界的 ReviewTodo，不能显示成单纯等待窗口。
3. 当前可重建证据预览只能作为人工补录参考，不能静默 patch 旧 ManualAction 或 ReviewTodo。
4. 已有旧人工留痕时，不能重复写入同一对象来绕过历史缺口；需要人工决定补录流程或作废旧待办。
5. 治理层不保存 ReviewRecord，不自动改规则，不执行广告动作。

验证方式：
python -m pytest tests\test_signal_triage_service.py tests\test_review_readiness_cli.py tests\test_review_evidence_repair_cli.py -q
npm run test:signal-ui
python scripts\inspect_review_readiness.py --market-id 1 --compact
python scripts\inspect_review_evidence_repair.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --compact
```

## 2026-06-21：历史待办治理不能只显示命令而不解释证据可修复性

```text
问题：历史待办治理卡片能显示旧待办对象、dry-run 命令和作废路径，但用户仍需要自己判断缺的是搜索词边界、广告位边界，当前预检只是可重建预览，还是可以重新留痕。

原因：后端 repair payload 已返回 issue_types、current_preflight、patch_policy，但前端样例行只消费对象、窗口、动作和 recommended_next_step，信息密度不够，容易把治理流程看成技术命令集合。

解决：前端 buildReviewEvidenceRepairSummary 渲染证据缺口、当前预检状态、四类关键证据是否存在、是否可重建预览、是否可重新留痕、是否可修补历史记录和 patch_policy。

防错规则：
1. 历史待办治理必须说明缺什么证据，不能只显示 action_id 和 dry-run 命令。
2. 可重建证据预览不等于可静默 patch 旧 evidence_snapshot。
3. 当前不可重新留痕时，必须说明已有旧留痕挡住写入，不能靠重复点击绕过门禁。
4. 作废旧待办不等于复盘完成，仍需要新的完整证据快照和新的 ReviewTodo。
5. 治理卡片不得包装成自动修补、自动改规则或自动执行广告动作。

验证方式：
npm run test:signal-ui
npm run build
```

## 2026-06-21：后端短 next_step 不能覆盖前端完整证据门槛

```text
问题：运行态 /api/signal-triage 已返回 blocked_by_review_evidence_gap，但 review_wait_summary.next_step 只写“补录证据快照”，没有明确“完整证据快照必须包含排查路径、AI 准入、搜索词边界和广告位边界”。前端原来优先使用后端 next_step，导致页面补证据步骤可能丢失四类关键证据要求。

原因：后端 next_step 是状态摘要，不是完整 UI 门槛文案。前端把后端短文案当成最终用户动作说明，缺少本地兜底。

解决：buildReviewReadinessGateSummary 在证据门禁阻断时调用 reviewEvidenceGateRepairStep，即使后端 next_step 较短，也会追加“补录或重新留痕必须形成完整证据快照，包含排查路径、AI 准入、搜索词边界和广告位边界”。新增 test:signal-ui-runtime 用真实 API 验证该链路。

防错规则：
1. 后端状态摘要可以短，但前端人工动作路径必须保留完整证据门槛。
2. blocked_by_review_evidence_gap 的“补证据”步骤必须点名排查路径、AI 准入、搜索词边界和广告位边界。
3. 运行态 smoke 必须覆盖真实 API payload 到 UI helper 的翻译结果，避免只靠 fixture 通过。
4. 缺四类关键证据时，不保存 ReviewRecord，不自动改规则，不执行广告动作。
5. 当前页面证据只能作为人工补录参考，不能伪装成历史点击证据。

验证方式：
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
python scripts\smoke_review_wait_contract.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --api-url http://127.0.0.1:8000/api/signal-triage?market_id=1&top=5&product_scope_id=parent_asin%3AB00K4W4AAA
```

## 2026-06-21：旧待办作废后不能被旧人工留痕永久挡住

```text
问题：旧 ReviewTodo 已经因为缺搜索词边界和广告位边界被显式作废，但重新写入完整证据人工留痕时，preflight 仍因为同对象存在旧 ManualAction 返回 duplicate_manual_action。结果是旧待办没有了，新待办也无法生成，复盘链路停在中间态。

原因：原重复判断只看同对象是否已有 ManualAction，没有区分“有效复盘待办仍存在”和“旧待办已作废且旧 evidence_snapshot 不满足四类关键证据”。写后验收也默认同对象只能有 1 条人工留痕，不能容纳保留旧审计记录 + 新完整记录的恢复场景。

解决：manual_action_preflight 增加恢复口径：当同对象历史动作缺排查路径、AI 准入、搜索词边界、广告位边界中的任一关键证据，且当前没有有效 ReviewTodo 时，允许重新写入一次同 action_type 的完整证据留痕。写后验收在多条同对象记录并存时，只把包含四类关键证据的记录作为有效人工留痕，旧缺证据记录继续保留为审计事实。

防错规则：
1. 作废旧 ReviewTodo 不等于删除旧 ManualAction；旧动作仍要保留审计。
2. 旧动作缺四类关键证据且有效待办已作废时，可以重新留痕，但不能放开普通重复写入。
3. 写后验收必须确认新的 ReviewTodo 继承完整 evidence_snapshot，而不是只看同对象记录数量。
4. 新旧 ManualAction 并存时，页面和脚本必须优先读回具备四类关键证据的有效记录。
5. 任何恢复流程仍不得保存 ReviewRecord、自动改规则或执行广告动作；必须等 7/14 天窗口到期。

验证方式：
python -m pytest tests\test_manual_action_preflight_cli.py tests\test_review_todo_void_cli.py tests\test_review_evidence_repair_cli.py -q
python scripts\apply_manual_action_once.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-type search_term --expected-object-id "search_term:1:beach essentials" --action-type add_to_review --compact
python scripts\apply_manual_action_once.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-type search_term --expected-object-id "search_term:1:beach essentials" --action-type add_to_review --execute --authorization-code "WRITE_ONCE:search_term:search_term:1:beach essentials" --action-note "旧待办作废后重新写入完整证据快照，供 7d/14d 人工复盘。" --compact
npm run test:signal-ui-runtime
```

## 2026-06-21：复盘待办队列不能只显示数量

```text
问题：左侧 AI 信号队列里的复盘待办摘要如果只显示“复盘待办 N 条 / 到期 M 条”，用户容易把待办理解成系统已经可以复盘、可以拉取快照、可以保存结论或可以自动优化广告。

原因：ReviewTodo 是人工动作派生的排程对象，不是 ReviewRecord，也不是规则改进结果。此前右侧详情已说明等待窗口和禁止动作，但左侧队列第一眼只强调数量和下一项，边界不够前置。

解决：buildReviewTodoQueueSummary 在有待办时补充状态边界：未到期前不拉取复盘快照、不保存 ReviewRecord、不自动改规则、不执行广告动作；到期后仍需人工确认后保存 ReviewRecord，不自动改规则或执行广告动作。

防错规则：
1. 复盘待办摘要必须说明 ReviewTodo 只是排程，不代表处理效果。
2. 未到期状态必须明确不拉取复盘快照、不保存 ReviewRecord。
3. 到期状态也必须明确人工确认，不自动改规则或执行广告动作。
4. 左侧队列和右侧详情必须使用一致的复盘边界口径。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-21：复盘对象分离摘要不能只显示证据数量

```text
问题：“复盘对象与候选队列”摘要能区分 beach essentials 和 boys sunglasses，但证据快照只显示“2 个待办 / N 条证据”。如果只看数量，用户仍无法判断这些证据是否满足保存 ReviewRecord 前必须回看的排查路径、AI 准入、搜索词边界和广告位边界。

原因：review_identity_audit.readback_keys 已经返回四类关键边界布尔字段，但前端 reviewEvidenceSnapshotText 只消费 evidence_snapshot_count，没有消费关键边界状态。

解决：reviewReadbackKeys 读取四类关键边界字段；reviewEvidenceSnapshotText 在全部复盘待办均满足四类关键边界时显示“四类边界齐全”，有明确 false 时显示“关键边界缺口”。

防错规则：
1. evidence_snapshot_count 只能说明证据条数，不能单独证明复盘证据可保存。
2. 复盘对象摘要必须同时说明证据数量和四类关键边界状态。
3. next_unhandled_candidate 不能继承 ReviewTodo 的证据状态；两条链路必须分开读。
4. 保存 ReviewRecord 前仍以后端校验为准，前端摘要只用于人工理解和预警。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-22：repair 预检不能只认旧四项证据门禁

```text
问题：
ReviewRecord 保存门禁已经要求 search_term 带投放词证据、ABA 背景、证据缺口和动作边界。
但 /api/review-evidence-repair 仍只把排查路径、AI 准入、搜索词边界、广告位边界当成可重建预览标准。
这会让历史待办治理看起来有 blocked 状态，却不能给用户展示具体补录预检路径。

原因：
1. repair 服务最初只为 legacy evidence_snapshot 整份缺失或对象错配设计。
2. search_term 复核链加深后，只更新了 ReviewRecord 保存门禁和读回审计，没有同步更新 repair 预检的 issue 类型和必需标签。
3. 当前 beach essentials 的旧待办不是整份空快照，而是缺四类 search_term 专属证据标签。

解决：
1. REPAIR_ISSUE_TYPES 纳入 missing_targeting_evidence、missing_aba_context、missing_evidence_gap、missing_action_boundary。
2. search_term repair 预检必需标签升级为八项：排查路径、AI 准入、搜索词边界、广告位边界、投放词证据、ABA 背景、证据缺口、动作边界。
3. current_preflight 返回新四项 has_* 字段，前端历史待办治理摘要可以展示有/缺哪些证据。
4. 真实接口已能返回 beach essentials 的具体 repair item：可重建 21 条证据预览，但因已有旧人工留痕不能重复写入。

防错规则：
1. 任何 evidence_snapshot 合同升级，都必须同步更新 repair 预检、前端摘要、runtime 真实接口测试。
2. search_term 补录治理必须检查投放词证据、ABA 背景、证据缺口和动作边界。
3. 可重建证据预览只说明当前同一对象可形成参考证据，不允许静默补写历史 evidence_snapshot。
4. 已有旧人工留痕时，不得通过重复写入绕过门禁；只能人工决定补录流程或 dry-run 作废旧待办。

验证方式：
python -m pytest tests/test_review_evidence_repair_cli.py -q
python -m pytest tests/test_api_routes.py -q
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-21：复盘待办下一项不能只是静态文本

```text
问题：左侧复盘待办摘要已经能显示“下一项”和到期日，但如果下一项只是静态文本，用户看到 beach essentials 等待复盘后，还要自己在信号队列里找对应对象和证据链。

原因：ReviewTodo 是从 ManualAction 派生的排程对象，真正的判断依据在当时选中的 AiSignal 和 evidence_snapshot。摘要只显示对象名，会让“等待复盘”和“回看证据”断开。

解决：工作台新增 handleLocateReviewSignal；复盘待办摘要的下一项在有 signal_id 时渲染为定位按钮，全局复盘待办明细行也复用同一定位函数。点击只切换选中信号并回到工作台，不写入人工动作、不保存 ReviewRecord、不拉取复盘快照、不执行广告动作。

防错规则：
1. 复盘待办摘要显示下一项时，必须能回到对应 signal_id 的信号详情。
2. 定位复盘对象只用于回看证据链，不能触发 ManualAction、ReviewRecord 或快照拉取。
3. 当前范围找不到对应 signal_id 时，可以切到全量排查定位，但不能混用下一未处理候选的证据。
4. 左侧摘要和全局明细必须使用同一定位逻辑，避免两处行为不一致。
5. 复盘窗口未到期前仍只能等待，不保存复盘结论、不自动改规则、不执行广告动作。

验证方式：
npm run test:workbench-structure
npm run test:review-ui
npm run build
```

## 2026-06-21：下一候选预检不能继承已处理对象的 ReviewTodo

```text
问题：推荐对象 beach essentials 已经有 7d / 14d ReviewTodo 时，如果下一候选 boys sunglasses 的 preflight 只看队列状态或全局复盘状态，就可能误把 beach 的待办当成 boys 的待办。

原因：
1. 推荐对象复盘链路和下一候选人工确认链路同时出现在同一个工作台页面。
2. 后端 preflight 必须按 expected_object_type / expected_object_id / shop_id / market_id 精确过滤目标对象。
3. 页面上的“等待复盘”和“可人工确认”是两个任务，不是同一对象的连续文案。

解决：
1. 在 test_manual_action_preflight_snapshots_next_unhandled_diagnosis_contract 中补入 beach essentials 既有 ReviewTodo。
2. 验证 boys sunglasses 的 target_review_todo_count 仍为 0，expected_after_write 才为 2。
3. 验证 boys 的 evidence_snapshot_preview 不继承 beach 的复盘等待证据。

防错规则：
1. next_unhandled_candidate 的 preflight 只能统计当前 expected object 的 ManualAction / ReviewTodo / ReviewRecord。
2. 推荐对象已有 ReviewTodo 不能阻止下一候选进入只读预检。
3. 下一候选通过 preflight 不等于已经人工确认写入，仍需显式人工动作。
4. 任何情况下都不自动执行广告动作，不自动保存 ReviewRecord。

验证方式：
python -m pytest tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_snapshots_next_unhandled_diagnosis_contract -q
python -m pytest tests\test_manual_action_preflight_cli.py -q
npm run test:signal-ui-runtime
```

## 2026-06-21：未到期复盘不能暗示保存 ready 结论

```text
问题：当前运行态没有历史证据快照缺口，但复盘窗口仍未到期，ready_count=0。页面治理卡片和规则反馈门槛如果继续展示“或保存 ready 的 ReviewRecord / 保存 ready 复盘记录”，会让运营误以为无历史缺口等于可以保存复盘结论。

原因：
1. ready_no_legacy_gap 只表示历史 evidence_snapshot 治理通过，不代表复盘效果 ready。
2. waiting_review_window 表示 7/14 天窗口未到期，当前只能等待和回看点击时证据。
3. 前端 fallback 文案没有区分 ready_count=0 和 ready_count>0。

解决：
1. review_evidence_repair 在无历史缺口且 waiting_review_window / ready_count=0 时，只返回“继续等待复盘窗口，未到期前不保存 ReviewRecord”。
2. 历史待办治理卡片 fallback 去掉“保存 ready”暗示。
3. 复盘样本池门槛按 ready_count 区分动作边界：ready_count=0 时明确不保存复盘记录。
4. 中间诊断区标题统一为“复盘点击时证据快照”，强调这是点击时 evidence_snapshot，不是当前效果结论。

防错规则：
1. ready_no_legacy_gap 不等于 ready ReviewRecord。
2. waiting_review_window 状态下不得展示“保存 ready”作为当前可执行动作。
3. 没有 ready 复盘前，规则反馈区只能展示门槛和等待状态，不能暗示可沉淀复盘样本。
4. 点击时 evidence_snapshot 只能用于复盘回看，不代表效果改善或规则有效。

验证方式：
$env:PYTHONPATH='backend'; python -m pytest backend\tests\test_review_evidence_repair_cli.py -q
npm run test:signal-ui
npm run test:workbench-structure
$env:PYTHONPATH='backend'; python -m py_compile backend\app\services\review_evidence_repair.py scripts\inspect_review_evidence_repair.py
```

## 2026-06-21：推荐对象状态不能混入下一候选动作

```text
问题：推荐对象 beach essentials 已经人工留痕并等待 7/14 天复盘，但 recommendedManualStatusText 会优先使用顶层 summary.next_action。顶层 next_action 同时包含“继续人工确认下一个未留痕候选 boys sunglasses”，导致推荐对象状态句里混入下一候选动作。

原因：
1. summary.next_action 描述的是队列级下一步，不是推荐对象自身状态。
2. recommended_manual_status.next_action 描述的是推荐对象自身人工留痕和复盘待办状态。
3. 页面已有独立“下一个未留痕候选”卡，继续把下一候选写进推荐对象状态会造成对象边界混乱。

解决：
1. recommendedManualStatusText 只使用 recommended_manual_status.next_action 和自身 fallback。
2. 下一候选动作继续由 buildNextUnhandledManualActionCandidate 和独立候选卡表达。
3. 测试增加反向断言，防止推荐对象状态再次包含“下一个未留痕候选”。

防错规则：
1. 推荐对象状态只能描述推荐对象自身的 ManualAction / ReviewTodo / ready review 状态。
2. 队列级 next_action 可以说明下一候选，但不能覆盖对象级状态。
3. next_unhandled_candidate 必须作为独立任务卡或独立定位入口出现，不能混入已复盘对象状态。
4. 已进入复盘窗口的对象不能被包装成新的人工动作目标。

验证方式：
npm run test:signal-ui
浏览器运行态验证 recommendedStatusMixesNextCandidate=false，hasSeparateNextCandidate=true。
点击“定位候选”后验证 selectedBoys=true、hasBeachReviewSnapshot=false；/api/manual-action/preflight 返回 ready_for_explicit_manual_write，目标对象为 boys sunglasses。
```

## 2026-06-21：运行态测试不能停留在旧治理状态

```text
问题：signalUi.runtime.test.ts 仍断言旧的 blocked_by_legacy_evidence_gap / evidence_snapshot_object_mismatch 状态，但当前真实运行态已经通过作废旧待办和重新留痕恢复为 waiting_review_window。旧断言会把已经修复的状态当成失败，反向误导后续开发。

原因：
1. runtime 测试直接依赖本地 JSONL 和真实 API 状态，状态会随着人工留痕、作废待办和复盘窗口推进而变化。
2. 旧测试关注“对象错配治理缺口”，但当前最重要的风险已经变成“已复盘对象和下一候选是否分离”。
3. 只保留旧断言会迫使代码回到已修复前的状态。

解决：
1. 重写 signalUi.runtime.test.ts，改为验证当前运行态合同。
2. 测试直接请求 /api/signal-triage、/api/review-evidence-repair 和 /api/manual-action/preflight。
3. 验证 beach essentials 已留痕等待复盘、boys sunglasses 是下一候选、对象不同、推荐对象状态不混入下一候选。
4. 验证下一候选预检 ready_for_explicit_manual_write，目标对象和 evidence_snapshot_preview 与 boys sunglasses 一致。

防错规则：
1. runtime 测试必须跟随当前真实业务阶段，不得把旧治理状态硬编码成永恒目标。
2. 当真实状态从 blocked 变成 waiting_review_window 后，测试重点应转向等待窗口、对象分离和下一候选预检。
3. 如果未来 boys sunglasses 被人工留痕，runtime 测试也应再升级到新的下一候选或新的等待复盘合同。

验证方式：
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
```

## 2026-06-24：前端搜索词复核链不能忽略后端广告组边界合同

```text
问题：
beach essentials 的 recommended_diagnosis_contract 已经包含广告组边界、同组广告 ASIN、投放词和广告位证据缺口。
但 buildSearchTermOpportunityReviewChain 旧逻辑只从 business_evidence_blocks 找广告组块；推荐候选的 blocks 没有显式广告组合流判断块时，前端可能退回“广告组合流判断待补 / 同组投放商品表现待补”。

原因：
1. 后端诊断合同和前端复核链是两层结构，前端只复用了 parent_asin_scope、ad_asin_coverage、placement_gap 和 search_term_opportunity。
2. ad_group_boundary 已经是解释广告组容器、同组广告 ASIN 和广告位边界的权威合同，但没有进入 buildSearchTermOpportunityReviewChain。
3. 这会造成用户看到“证据待补”，实际后端已经有足够广告组边界证据，降低诊断可信度。

解决：
1. buildSearchTermOpportunityReviewChain 新增读取 ad_group_boundary 诊断合同。
2. 广告组合流判断优先使用 ad_group_boundary 的 currentJudgement / doesNotProve / evidenceGap。
3. 同组投放商品表现优先使用 ad_group_boundary 的 currentJudgement。
4. 运行态测试锁定 beach essentials 复核链必须展示真实广告组、B016EXMVZS / B07BS9754Q / B016EXMW02 和广告位证据层级，且不能出现待补文案。

防错规则：
1. 前端复核链要优先复用 diagnosis_contract 中的权威对象边界，不要只依赖 business_evidence_blocks。
2. 后端合同已有某层证据时，前端不得显示该层“待补”。
3. SearchTerm 复核链至少要能回看：Parent ASIN、广告 ASIN、广告组容器、同组投放商品、投放词、广告位边界、人工下一步。

验证方式：
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
```

## 2026-06-21：标签齐全但对象错配的旧动作不应阻止重新留痕

```text
问题：
旧 ReviewTodo 已因为 evidence_snapshot_object_mismatch 被显式作废，但同一对象下仍存在 ManualAction。
preflight 的重复写入判断只看 evidence_snapshot 是否包含“排查路径 / AI 准入 / 搜索词边界 / 广告位边界”四类标签。
结果是：旧动作标签看似齐全，但证据文本不能回看 beach essentials；旧待办已作废后，系统仍因为 duplicate_manual_action 阻止重新留痕，形成“不能复盘，也不能重写”的状态。

原因：
1. 有效复盘证据的定义不完整，只检查标签，不检查对象引用。
2. ReviewTodo 作废只隐藏旧待办，不删除 ManualAction；这符合事实留痕原则，但 preflight 必须识别旧动作是否仍能作为有效复盘证据。
3. 搜索词场景里 object_id、object_label 和证据文本必须一致，否则会把 boys sunglasses / sunglasses for kids 的证据误当 beach essentials 的复盘依据。

解决：
1. manual_action_preflight._has_required_review_evidence_snapshot 增加对象引用一致性判断。
2. 证据快照必须能命中当前 object_id、object_label 或 object_id 尾部稳定词，才算有效复盘证据。
3. 标签齐全但对象引用不一致的旧 ManualAction 不再阻止重新人工留痕。
4. 重新留痕仍必须走 dry-run、授权码、execute 和 post-write 验收，不允许绕过 preflight。

防错规则：
1. evidence_snapshot_count 和四类标签齐全只能说明证据结构完整，不能单独证明证据可复盘。
2. 可复盘证据必须同时满足：标签齐全、对象身份一致、ReviewTodo 继承快照、复盘窗口到期。
3. 作废旧 ReviewTodo 不能静默 patch ManualAction，也不能保存 ReviewRecord。
4. 重新留痕只生成新的 ManualAction / ReviewTodo，不自动执行广告动作。

验证方式：
python -m pytest backend\tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_allows_rewrite_when_complete_labels_belong_to_other_object -q
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-type search_term --expected-object-id "search_term:1:beach essentials" --action-type add_to_review --expect-written --compact
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-21：治理预览不能漏掉证据快照对象不一致

```text
问题：review readiness 已经识别 evidence_snapshot_object_mismatch，说明 ReviewTodo 的证据快照不能回看目标对象；但 inspect_review_evidence_repair.py 只把缺 evidence_snapshot、排查路径、AI 准入、搜索词边界、广告位边界当成可治理缺口，导致真实运行态错误返回 ready_no_legacy_gap。

原因：
1. review_evidence_repair.REPAIR_ISSUE_TYPES 没有包含 evidence_snapshot_object_mismatch。
2. apply_review_todo_void_once.py 判断“完整证据”时只看四类标签，没有检查对象引用。
3. 结果是证据条数和标签齐全但对象错配的 ReviewTodo 既不能保存 ReviewRecord，又没有治理入口。

解决：
1. 将 evidence_snapshot_object_mismatch 纳入 repair 预览。
2. repair 预览增加 has_object_reference，只有能回看目标 object_id、object_label 或搜索词稳定文本时，才视为可用证据预览。
3. 作废 dry-run 增加对象引用一致性门禁：标签齐全但对象错配时允许进入 dry-run 作废；真正 execute 仍需显式授权码。

防错规则：
1. 复盘治理缺口必须同时覆盖“缺证据”和“证据对象不一致”。
2. 证据标签齐全不等于证据可复盘；必须能回看目标对象。
3. 对象错配待办可以显式作废，但不能静默 patch evidence_snapshot。
4. 作废只写 review_todo_decisions，不修改 manual_actions，不保存 ReviewRecord，不执行广告动作。

验证方式：
python -m pytest tests\test_review_evidence_repair_cli.py tests\test_review_todo_void_cli.py -q
python -m pytest tests\test_review_readiness_cli.py tests\test_api_routes.py tests\test_signal_triage_service.py tests\test_manual_action_preflight_cli.py tests\test_review_evidence_repair_cli.py tests\test_review_todo_void_cli.py -q
python scripts\inspect_review_evidence_repair.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --compact
python scripts\apply_review_todo_void_once.py --market-id 1 --action-id manual-action-22d68ef169c9447e90c197393144dad3 --expected-object-type search_term --expected-object-id "search_term:1:beach essentials" --compact
```

## 2026-06-21：前端治理卡片不能继续显示旧的 ready_no_legacy_gap 口径

```text
问题：后端 review_evidence_repair 已经能返回 blocked_by_legacy_evidence_gap 和 evidence_snapshot_object_mismatch，但前端运行态测试和治理卡片仍按旧口径期待 ready_no_legacy_gap，页面会继续掩盖对象错配。

原因：
1. ReviewEvidenceRepairItem.current_preflight 类型没有声明 has_object_reference。
2. reviewEvidenceSnapshotText 只看排查路径、AI 准入、搜索词边界、广告位边界，没有消费对象引用。
3. reviewRepairIssueText 没有把 evidence_snapshot_object_mismatch 翻译成用户可读文案。
4. signalUi.runtime.test.ts 仍断言“没有历史证据快照缺口”。

解决：
1. 前端类型补齐 has_object_reference。
2. 证据快照摘要出现对象引用 false 时显示“对象引用缺口”。
3. 历史待办治理样本行显示“证据快照对象不一致”和“对象引用可回看 / 对象引用错配”。
4. 运行态测试改为验证 blocked 状态、对象错配、dry-run 授权码和禁止自动动作边界。

防错规则：
1. 前端不能只根据证据条数或四类标签判断 ReviewTodo 可复盘。
2. 后端新增治理 issue_type 后，前端必须有用户可读翻译。
3. 运行态测试必须跟随真实 API 状态，不能保留旧 fixture 期待。
4. 治理卡片只能展示 dry-run / 授权码 / 禁止动作边界，不得静默执行作废或补写。

验证方式：
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
python -m pytest tests\test_review_evidence_repair_cli.py tests\test_review_todo_void_cli.py -q
```

## 2026-06-21：复盘证据快照不能混入下一候选对象

```text
问题：历史 ReviewTodo 的复盘对象是 search_term:1:beach essentials，但 evidence_snapshot 中混入 boys sunglasses，且缺少 beach essentials 对象引用。页面如果只看证据条数和四类边界，会误以为快照完整。

原因：
1. build_signal_triage_payload 的全局 diagnosis_contract 指向 next_unhandled_candidate，容易被推荐对象或复盘对象误用。
2. manual_action_preflight 之前无条件读取全局 diagnosis_contract，没有按 target 选择候选级合同。
3. 候选匹配逻辑优先 stable_object_id，导致同一作用域下可能把下一候选的 evidence_drilldown 取给复盘对象。
4. ReviewRecord 保存前只校验排查路径、AI 准入、搜索词边界和广告位边界，没有校验证据快照能否回看复盘对象本身。

解决：
1. /api/signal-triage 增加 recommended_diagnosis_contract 和 next_unhandled_diagnosis_contract，前端按选中对象读取候选级合同。
2. manual_action_preflight 按 target 匹配 diagnosis_contract 和 evidence_drilldown，带身份但不匹配的合同直接拒绝。
3. review_identity_audit 增加 has_object_reference / missing_object_reference_count，并输出 evidence_snapshot_object_mismatch。
4. ReviewRecord 保存前新增对象引用一致性硬门禁，快照不含目标 object_id、object_label 或搜索词稳定文本时拒绝保存。

防错规则：
1. evidence_snapshot 完整不只看条数和四类标签，还必须能回看目标对象。
2. recommended_candidate、next_unhandled_candidate、ReviewTodo 不能共用一份无身份的诊断合同。
3. 历史 evidence_snapshot 对象不一致时，不能静默改写，只能走显式治理、补录或重新人工留痕。
4. 对象不一致的 ReviewTodo 不得保存 ReviewRecord，也不得进入规则反馈样本。
5. 任何治理流程仍不得自动改规则、自动调价、自动否词、自动加词或自动执行广告动作。

验证方式：
python -m pytest tests/test_review_readiness_cli.py tests/test_api_routes.py tests/test_signal_triage_service.py tests/test_manual_action_preflight_cli.py -q
npm run test:signal-ui
npm run test:workbench-structure
npm run build
python scripts\inspect_review_readiness.py --market-id 1 --compact
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-type search_term --expected-object-id "search_term:1:beach essentials" --action-type add_to_review --compact
```

## 2026-06-21：复盘对象定位后中间区不能只看当前 signal facts

```text
问题：复盘待办下一项已经可以定位到对应信号，但中间诊断区仍主要展示 selectedSignal 的当前 facts 和 diagnosis_contract。用户从 beach essentials 复盘待办点进来时，如果看不到当时人工点击保存的 evidence_snapshot，仍无法确认为什么这个对象进入 7/14 天复盘。

原因：AiSignal 当前事实、ManualAction 点击证据和 ReviewTodo 待办排程是三层不同对象。只定位 signal_id 不能自动证明用户看到了点击时证据快照；证据快照若只藏在右侧处理留痕小段里，主诊断区的证据链仍不完整。

解决：SignalDiagnosis 增加“复盘点击时证据快照”面板，优先展示 ReviewTodo.evidence_snapshot，缺失时退回 ManualAction.evidence_snapshot。面板明确说明这是人工点击时证据，不是当前实时广告事实，也不是效果结论。

防错规则：
1. 从复盘待办定位到信号后，中间诊断区必须能看到点击时 evidence_snapshot。
2. evidence_snapshot 必须和当前 signal facts 分开表达，不能混成当前实时广告指标。
3. ReviewTodo.evidence_snapshot 优先于 ManualAction.evidence_snapshot，因为 ReviewRecord 保存前校验最终要读待办继承的快照。
4. 缺 evidence_snapshot 时只能提示证据缺口，不能用当前页面证据伪装成历史点击证据。
5. 展示 evidence_snapshot 不代表复盘已完成；未到期前仍不保存 ReviewRecord、不自动改规则、不执行广告动作。

验证方式：
npm run test:workbench-structure
npm run test:review-ui
npm run build
```

## 2026-06-21：运行态测试不能只验证候选分离

```text
问题：signalUi.runtime.test.ts 之前主要验证 beach essentials 与 boys sunglasses 的推荐对象 / 下一候选分离。这个合同还不够，因为页面可能在对象分离正确的情况下，继续退回成裸指标展示，无法回答业务判断。

原因：
1. 运行态 smoke 没有约束 Parent ASIN -> 广告 ASIN -> 广告组 -> 投放词 -> 搜索词 -> 广告位缺口 -> 人工复盘这条完整路径。
2. diagnosis_contract 虽然存在，但测试没有检查每个诊断块是否包含对象边界、能证明什么、不能证明什么和人工下一步。
3. 对广告 AI 信号工作台而言，“字段存在”不等于“业务可落地”。

解决：
1. 运行态 smoke 增加 product_scope_drilldown 断言，确认 Parent ASIN 入口只承接有广告证据的广告 ASIN。
2. 运行态 smoke 增加 next_unhandled_evidence_drilldown 断言，确认下一候选有排查路径、指标目的、上下文、边界、广告位缺口和复盘路径。
3. 运行态 smoke 增加 next_unhandled_diagnosis_contract 断言，确认六个诊断块绑定下一候选对象并保留人工动作边界。

防错规则：
1. 运行态测试不能只断言候选分离，还必须断言业务诊断链路完整。
2. 搜索词信号必须说明广告组、投放词、广告 ASIN 边界，不能自动归因到单个广告商品。
3. 广告位缺失时必须作为证据缺口展示，不能用泛化指标代替。
4. 测试和页面文案都必须保留“不能自动执行广告动作”的边界。

验证方式：
npm run test:signal-ui-runtime
```

## 2026-06-21：接口字段完整不等于前端业务表达完整

```text
问题：上一轮运行态 smoke 已经证明 next_unhandled_diagnosis_contract 字段完整，但如果不验证前端 helper 的消费结果，页面仍可能退回成只显示裸指标或只显示部分证据块。

原因：
1. API 合同、UI helper、组件展示是三层不同链路。
2. 接口字段完整只能证明后端有数据，不能证明中间诊断区和右侧人工确认能读出业务问题、指标目的和人工下一步。
3. 纯静态结构测试能证明组件位置存在，但不能证明真实 payload 能通过这些 helper。

解决：
1. signalUi.runtime.test.ts 使用真实 /api/signal-triage 响应调用 signalTriageDiagnosisContractItems。
2. 运行态验证 buildManualConfirmationEvidenceItems 输出固定人工确认结构。
3. 运行态验证 buildSignalDiagnosisEvidenceSummary 和 buildSignalMetricDecisionItems 能解释证据强度、指标用途、能证明和不能证明。

防错规则：
1. 新增诊断合同字段后，必须同时验证 API 字段、前端 helper 输出和组件展示位置。
2. 前端不能只显示指标值，必须保留指标目的、证据边界和人工下一步。
3. helper 层测试要区分数据值和组件标签，不能把 UI 标签误当数据合同。
4. 运行态测试仍不得触发真实人工写入或任何广告动作。

验证方式：
npm run test:signal-ui-runtime
npm run test:workbench-structure
```
## 2026-06-21：signal-triage 有下一候选但 /api/signals 缺同 ID 时，前端不能丢候选

```text
问题：
/api/signal-triage 能返回 next_unhandled_candidate，例如 B00K4W4AAA 链路下的 boys sunglasses；但 /api/signals 当前不一定包含同一个 signal_id。原前端 mergeBackendTriageSignals 只从 /api/signals 查找同 ID，找不到就跳过，导致页面可能无法自然定位下一待处理对象。

原因：
1. 前端隐含假设：signal-triage 的 candidate signal_id 必定已经存在于 /api/signals。
2. 真实运行态里，signal-triage 是人工复核候选来源，/api/signals 是信号列表来源，两个接口不是强同步关系。
3. 右侧建议区之前也基于 normalizedSignals 计算，即使展示队列补齐候选，建议区仍可能读不到该候选。

解决：
1. 前端增加 SignalTriageCandidateForUi，承接后端 candidate_summary 已返回的候选摘要字段。
2. mergeBackendTriageSignals 在 /api/signals 找不到同 ID 时，用 signal-triage 候选生成最小只读信号。
3. 工作台右侧推荐和下一候选改用 displayProductScopedSignals，保证补齐后的候选能进入人工确认路径。
4. 增加 signalUi 单测和真实运行态测试，覆盖“列表缺同 ID但 triage 有候选”的路径。

防错规则：
1. 不要假设 /api/signals 与 /api/signal-triage 的 signal_id 天然完全同步。
2. signal-triage 返回的 recommended_candidate / next_unhandled_candidate 必须能独立支撑页面定位。
3. 合成候选只能用于展示、诊断和人工预检，不得写库，不得自动执行广告动作。
4. 以后新增候选来源时，必须同时验证左侧队列、中间诊断和右侧人工确认是否都能消费。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
浏览器只读检查 http://localhost:5175/
```

## 2026-06-21：推荐对象 ID 不能从已切换到下一候选的 manual_action_preview 推断

```text
问题：
B00K4W4AAA 链路下，recommended_candidate 是 beach essentials，且该对象已经人工留痕并等待 7/14 天复盘。
同一响应里的顶层 manual_action_preview 已经指向 next_unhandled_candidate boys sunglasses。
如果前端把 manual_action_preview.signal_id 优先当成推荐对象 ID，旧选中态 beach essentials 就不会自动切到 boys sunglasses。

原因：
1. recommended_candidate 表示已推荐或已留痕的业务对象。
2. manual_action_preview 表示当前可人工写入的动作预览；当推荐对象已留痕时，它可能转向下一候选。
3. 原 recommendedManualActionSignalId 优先读取 manual_action_preview.signal_id，混淆了“推荐对象身份”和“下一可写对象身份”。

解决：
1. recommendedManualActionSignalId 改为优先读取 recommended_candidate.signal_id。
2. resolveSignalSelectionId 在推荐对象已留痕且有 next_unhandled_candidate 时，将旧推荐对象选中态切到下一候选。
3. 如果用户当前选中其它有效信号，保留用户选择，不强行跳转。
4. 增加单测和真实运行态 smoke，覆盖 beach essentials -> boys sunglasses 的默认定位。

防错规则：
1. recommended_candidate、manual_action_preview、next_unhandled_candidate 是三个语义不同的对象入口，不能用一个 ID 推断全部身份。
2. 顶层 manual_action_preview 只能代表当前可写预览，不天然代表推荐对象。
3. 已人工留痕并等待复盘的对象不能阻塞下一未留痕候选进入中间诊断和右侧人工确认。
4. 默认定位优化只改变阅读路径，不自动写入 ManualAction，不生成 ReviewTodo，不执行广告动作。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-21：中间诊断区不能把业务判断包装成技术合同语言

```text
问题：
next_unhandled_diagnosis_contract 字段和 helper 输出已经完整，但中间诊断区标题仍使用“诊断合同”这类内部技术语言。
用户真正需要的是判断链：业务问题、指标目的、能证明、不能证明、人工下一步。
如果页面只说“合同”，会降低运营阅读效率，也容易退回到字段展示。

原因：
1. API 合同命名服务开发者，不等于用户可见的信息架构。
2. 中间区是运营主阅读区，应该先表达业务判断，而不是暴露内部数据结构。
3. 结构测试之前没有强约束“业务判断与指标目的”这些可见层级。

解决：
1. 中间诊断区标题改为“业务判断与指标目的”。
2. 增加引导文案：先回答这条信号要判断什么，再说明指标能证明什么、不能证明什么，以及人工下一步。
3. 结构测试补充“业务判断与指标目的 / 指标目的 / 当前判断 / 能证明 / 不能证明 / 人工下一步”断言。

防错规则：
1. 用户可见区域不要直接暴露内部 API 字段概念。
2. 每个诊断块必须服务业务判断，不能只展示字段或裸指标。
3. 前端结构测试不仅要检查组件存在，还要检查关键业务表达存在。
4. 该表达只用于人工判断，不代表自动写入 ManualAction 或自动执行广告动作。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-21：人工预检证据快照不能只优先展示补证缺口

```text
问题：
boys sunglasses 的 /api/manual-action/preflight 已经返回完整 evidence_snapshot_preview，包含排查路径、AI 准入、搜索词边界、广告位边界、广告位证据缺口和需要补证。
但前端优先展示区只置顶“诊断证据缺口 / 需要补证”，用户点击“加入复盘”前不一定第一眼看到搜索词边界、广告位边界和人工动作边界。

原因：
1. 后端证据完整只能证明数据合同存在，不能证明前端阅读路径合理。
2. 人工确认前最关键的是边界判断：搜索词不能自动归因到 ASIN，广告位缺口不能包装成广告位结论。
3. 原优先规则把“补证”当成最高优先，但忽略了“为什么不能直接动作”的边界证据。

解决：
1. manualActionPreflightPriorityEvidenceRows 优先展示 AI 准入、搜索词边界、广告位边界、广告位证据缺口、诊断证据缺口、需要补证和人工下一步。
2. 右侧标题改为“先看边界与缺口”，避免把这一区域理解成普通缺口列表。
3. 运行态测试用真实 B00K4W4AAA -> boys sunglasses 预检响应验证快照和 helper 输出。

防错规则：
1. 预检快照完整不等于页面可落地，必须验证前端是否把关键边界放到用户第一眼。
2. 搜索词边界、广告位边界、AI 准入和人工动作边界优先级高于普通指标展示。
3. 广告位缺失只能说明证据缺口，不能自动推导广告位调整建议。
4. 预检仍是只读，不能自动写 ManualAction、ReviewTodo 或执行广告动作。

验证方式：
npm run test:review-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-21：复盘待办读回不能只看 7d / 14d 窗口

```text
问题：
页面原先可以显示 7d / 14d 待办已读回，但没有直接说明这些 ReviewTodo 是否继承了可复盘的 evidence_snapshot。
如果只看窗口标签，用户会误以为待办已经可用于复盘；实际还需要确认排查路径、AI 准入、搜索词边界和广告位边界是否随待办保存。

原因：
1. ReviewTodo 是复盘排程，不是复盘结论。
2. ReviewTodo.evidence_snapshot 才是后续 ReviewRecord 保存前要回看的输入证据。
3. 同一对象可能存在多条历史 ManualAction，旧留痕证据条数可能和最新待办继承的证据条数不同。

解决：
1. 待办读回文案增加 7d / 14d evidence_snapshot 条数。
2. 待办读回文案检查排查路径、AI 准入、搜索词边界和广告位边界。
3. runtime 测试用真实 beach essentials 链路验证当前 7d / 14d 待办各继承 17 条证据快照。

防错规则：
1. 不能只凭 7d / 14d 窗口存在判断复盘输入完整。
2. ReviewTodo.evidence_snapshot 缺四类关键边界时，必须提示证据缺口。
3. 旧 ManualAction 的证据数量不能替代当前待办继承的证据数量。
4. 待办已读回仍不代表效果改善，未到期前不能保存 ReviewRecord，也不能自动改规则或执行广告动作。

验证方式：
npm run test:review-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-21：ReviewRecord 保存前不能只看证据标签完整

```text
问题：
ReviewTodo.evidence_snapshot 已经能保存排查路径、AI 准入、搜索词边界和广告位边界，但前端保存 ReviewRecord 的门槛没有显式检查这份证据快照是否能回看当前复盘对象。
这会出现“证据条数和标签都完整，但证据其实来自其他候选对象”的假闭环。

原因：
1. 四类关键标签只能证明证据结构完整，不能证明证据归属对象正确。
2. ReviewEffect.ready 只能证明处理前后指标窗口完整，不能证明证据快照属于当前对象。
3. 后端已经拒绝 evidence_snapshot_object_mismatch，但前端没有在保存前把这个边界讲给用户。

解决：
1. ReviewRecordPreflightCheck 增加 todo_object_reference / todo_object_reference_missing。
2. canSaveReviewRecordWithPreflight 把待办证据对象引用纳入必需保存条件。
3. 对象引用检查支持 object_id、object_label，以及 search_term:1:xxx 这类 ID 的尾部搜索词文本。
4. 前端测试增加“标签齐全但对象引用错配时不能保存 ReviewRecord”。

防错规则：
1. 证据快照完整 = 关键标签完整 + 对象引用可回看，缺一不可。
2. 不能用证据条数、ready effect 或四类标签替代对象归属校验。
3. 前端保存按钮和 handleSaveReviewRecord 必须使用同一个 preflight 门槛。
4. 对象错配只能阻断保存或要求人工治理，不能静默改写历史 evidence_snapshot。

验证方式：
npm run test:review-ui
npm run test:signal-ui-runtime
$env:PYTHONPATH='.'; pytest tests\test_api_routes.py -k review_record_rejects_evidence_snapshot_object_mismatch
npm run build
```

## 2026-06-21：人工动作写后读回不能只显示待办窗口和标签完整

```text
问题：
写后读回路径可以显示 7d / 14d ReviewTodo 已读回，并显示排查路径、AI 准入、搜索词边界和广告位边界齐全。
但如果不显示对象引用状态，用户仍可能把“标签齐全的其他候选证据”误认为当前复盘对象的证据。

原因：
1. ReviewTodo 是复盘排程，不能只用窗口和标签证明可复盘。
2. 对象引用错配在保存 ReviewRecord 前会被阻断，但读回摘要如果不提前展示，会让问题暴露太晚。
3. 用户判断能否进入复盘，需要同时看到证据条数、对象身份和关键边界。

解决：
1. reviewTodoEvidenceSnapshotReadbackText 成功态增加“可回看对象引用”。
2. 缺口态增加“缺对象引用”，和 AI 准入 / 搜索词边界 / 广告位边界一起展示。
3. 测试 fixture 必须包含稳定 object_id / object_label 以及证据快照中的对象文本。
4. runtime 测试用真实 beach essentials 链路验证读回摘要包含对象引用。

防错规则：
1. 写后读回摘要也必须遵循“证据完整 = 对象引用 + 四类关键边界”。
2. 不允许用 7d / 14d 待办存在、证据条数或标签齐全替代对象归属判断。
3. 发现对象引用缺口只能提示治理或阻断保存，不能静默补写历史 evidence_snapshot。
4. ReviewTodo 读回仍只是排程和证据回看，不代表效果改善，也不能自动执行广告动作。

验证方式：
npm run test:review-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：搜索词人工留痕不能使用快照行 ID 当复盘对象

```text
问题：
boys sunglasses 人工预检时，target.object_id 仍是 gerpgo_market_1_20260616_120443:507942344。
当用业务对象 boys sunglasses 做预检时，会触发 object_id_mismatch。
即使页面能展示搜索词，后续 ManualAction / ReviewTodo / ReviewRecord 仍可能保存一次性快照行 ID，7/14 天后无法稳定回到同一搜索词。

原因：
1. manual_action_preview 生成对象 ID 时，搜索词沿用了 primary_object.object_id，也就是快照行 ID。
2. create_signal_manual_action 写入前也使用 manual_action_object_id(primary_object)，搜索词没有稳定 ID 规则。
3. 预检、API 路由、复盘效果和 ReviewRecord 保存分散使用字符串相等，不能识别自然搜索词、稳定 ID 和旧 snapshot_id:搜索词 是同一对象。

解决：
1. 搜索词人工动作对象统一规整为 search_term:{market_id}:{搜索词}。
2. 保留 source_object_id 追溯旧快照行 ID，但不把它当复盘对象 ID。
3. 新增 manual_action_object_id_aliases / manual_action_object_id_matches，统一处理自然搜索词、稳定 ID 和旧 snapshot_id:搜索词 的等价匹配。
4. 人工动作写入、预检、复盘效果、ReviewTodo 匹配、ReviewRecord 保存都接入同一等价对象规则。

防错规则：
1. search_term 的 ManualAction.object_id / ReviewTodo.object_id / ReviewRecord.object_id 必须是稳定搜索词 ID，不能是快照行 ID。
2. 快照行 ID 只能作为 source_object_id 或证据来源追溯，不能作为复盘对象。
3. API 路由不得直接用 request.expected_object_id == object_id 判断搜索词对象，必须走统一等价匹配。
4. 测试里允许输入自然搜索词或旧 snapshot_id:搜索词，但写入结果必须规整成 search_term:{market_id}:{搜索词}。

验证方式：
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id "boys sunglasses" --expected-object-type search_term --action-type add_to_review --top 5 --compact
python -m pytest（workdir=backend）
npm run test:review-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-22：搜索词预检快照不能混用商品范围首个广告组口径

```text
问题：
boys sunglasses 预检快照里，诊断合同写“广告组级广告位 0 条 / 同广告活动广告位 3 条”，但“广告位边界”块写成同活动 4 条。
这是因为预检快照先使用 product_scope_drilldown 的第一个广告组汇总，再叠加当前搜索词 diagnosis_contract。
用户会看到同一证据快照里两个广告位口径，无法判断哪个服务当前搜索词。

原因：
1. _product_scope_boundary_snapshot_blocks 默认读取商品范围第一个 ad_group_diagnosis。
2. 对 search_term 目标来说，商品范围首个广告组不一定等于当前搜索词所在广告组。
3. “广告组合流判断 / 搜索词边界 / 广告位边界”必须服务当前复盘对象，不能用范围汇总冒充对象证据。

解决：
1. search_term 目标的“广告组合流判断 / 搜索词边界 / 广告位边界”改为从当前目标 diagnosis_contract 生成。
2. 广告位边界直接使用合同里的“广告位证据”指标，boys sunglasses 固定显示广告组级广告位 0 条 / 同广告活动广告位 3 条。
3. 搜索词边界 detail 同时保留指标目的和“不能自动归因到单个 ASIN、不能自动加词/否词/调价”的硬边界。

防错规则：
1. 人工动作 evidence_snapshot_preview 必须以 target 为中心生成证据块。
2. product_scope 汇总只能作为背景，不能覆盖 search_term 目标级边界。
3. 同一证据快照里不能出现同一指标的两个冲突口径；如果有范围口径和目标口径，目标口径优先。
4. 目标级证据块不能只展示指标 purpose，必须同时保存不能证明什么。

验证方式：
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id "boys sunglasses" --expected-object-type search_term --action-type add_to_review --top 5 --compact
python -m pytest（workdir=backend）
npm run test:signal-ui-runtime
npm run test:review-ui
npm run build
git diff --check
```

## 2026-06-22：广告组问题不能直接归因到单个广告 ASIN

```text
问题：
广告组诊断已经能展示问题落点、搜索词和广告位上下文，但如果不先说明问题归属层级，用户容易把广告组下的花费、订单、搜索词或广告位证据直接理解成某个广告 ASIN 的问题。

原因：
1. Amazon 广告架构中，广告组是投放容器，一个广告组可以包含一个或多个广告 ASIN。
2. 搜索词和广告位证据通常落在广告组、活动或投放对象上下文，不天然归属于单个广告 ASIN。
3. 如果页面只展示 ACOS、CVR、花费、点击等指标，仍然回答不了“问题到底该归到广告 ASIN、广告组结构、搜索词还是证据缺口”。

解决：
1. productScopeAdGroupDiagnosisRows 增加 ownershipDecision。
2. 广告组诊断面板新增“广告组问题归属判定”，先展示业务问题、归属结论、证据路径和不能证明的内容。
3. 多广告 ASIN 广告组默认先归属到广告组容器和搜索词上下文，不拆到单个广告 ASIN。
4. 人工下一步只允许记录观察、加入复盘、标记处理或忽略本次，不包装成自动调价、自动拆组、自动加词或自动否词。

防错规则：
1. 广告组诊断必须先说明广告组内广告 ASIN 数量和证据路径，再展示问题落点。
2. 搜索词、投放词和广告位证据不能自动归因到单个广告 ASIN。
3. 缺少广告位证据或只有活动级广告位时，只能显示证据缺口，不能推导广告位影响。
4. 前端结构测试要约束“归属判定 / 证据路径 / 不能证明 / 人工下一步”同时存在。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：广告组合流判断不能只停留在前端复盘卡片

```text
问题：
前端 ReviewTodo 回读和 ReviewRecord 保存门禁已经把“广告组合流判断”视为搜索词复核链必备证据。
但后端 review_identity_audit 和历史待办治理 API 仍只识别投放词证据、ABA 背景、证据缺口和动作边界。
结果是当前 B00K4W4AAA / beach essentials 历史 ReviewTodo 缺少广告组合流判断时，修复工具不会报告该缺口。

原因：
1. _review_todo_evidence_snapshot_audit 没有 has_ad_group_synthesis。
2. _review_identity_audit_issues 没有 missing_ad_group_synthesis。
3. review_evidence_repair 的 search_term required labels 没有“广告组合流判断”。
4. 前端人工确认证据链也没有把该判断作为点击前 evidence_snapshot 对齐项。

解决：
1. 后端审计新增 has_ad_group_synthesis / missing_ad_group_synthesis_count / missing_ad_group_synthesis issue。
2. 历史待办治理把“广告组合流判断”纳入 search_term 必备修复标签。
3. 前端复盘对象队列、历史治理、人工确认证据写入核对统一展示五项搜索词复核链。
4. 当前真实修复检查返回 repair_issue_count=10，issue_types 包含 missing_ad_group_synthesis。

防错规则：
1. 搜索词复盘链必须同时包含投放词证据、广告组合流判断、ABA 背景、证据缺口和动作边界。
2. 前端展示新增必备证据时，后端 review_identity_audit、repair API、runtime 测试必须同步更新。
3. 旧 evidence_snapshot 缺证据只能提示历史缺口，不能用当前页面证据静默补写历史点击证据。
4. 可重建证据预览不等于可重复写入人工动作；已有留痕时必须人工决定补录或作废旧待办。

验证方式：
python -m pytest tests\test_review_evidence_repair_cli.py
python -m pytest tests\test_signal_triage_service.py -k "review_readiness or review_evidence"
python -m pytest tests\test_api_routes.py -k "manual_action_preflight or review_evidence or signal_triage"
npm run test:signal-ui
npm run test:review-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_review_evidence_repair.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --top 10 --compact
git diff --check
```

## 2026-06-22：复盘门禁边界不能只写基础证据缺口

```text
问题：
后端 review_identity_audit 已经能返回 missing_ad_group_synthesis。
但页面“复盘证据门禁阻断”的 boundary 仍只写 evidence_snapshot、排查路径、AI 准入、搜索词边界和广告位边界。
运营会看到缺口总数变多，却不能从门禁边界读出搜索词复核链还缺广告组合流判断、ABA 背景、证据缺口和动作边界。

原因：
1. buildReviewIdentityAuditSummary 已接入五项复核链。
2. buildReviewReadinessGateSummary 的 boundary 和默认 next_step 仍停留在早期基础四项文案。
3. 单测仍断言旧短句“搜索词边界或广告位边界”，没有覆盖五项链路。

解决：
1. 复盘身份摘要把“广告组合 2”改为“广告组合流判断 2”。
2. 复盘门禁 boundary 明确列出投放词证据、广告组合流判断、ABA 背景、证据缺口和动作边界。
3. 补证据 next step 同步列出完整证据快照要求。
4. runtime 测试和单元测试都锁定 boundary 必须包含广告组合流判断。

防错规则：
1. 后端新增 review_identity_audit issue 后，必须检查页面 gate 的 primary、detail、boundary、nextSteps 是否都能让用户读懂。
2. 不能只在明细摘要里出现缺口，门禁边界也必须说明为什么不能保存 ReviewRecord。
3. 搜索词复盘链缺口必须按五项展示，不得退回“搜索词边界 / 广告位边界”这种基础口径。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:review-ui
npm run build
浏览器 DOM 验收 http://localhost:5175/，确认复盘门禁 boundary 包含“广告组合流判断”。
```

## 2026-06-22：广告组合流判断不能只存在 evidence_snapshot JSON

```text
问题：
后端已经能把“广告组合流判断”写入 ManualAction 和 ReviewTodo 的 evidence_snapshot。
但如果前端复盘待办摘要和 ReviewRecord 保存前门禁不消费该标签，运营到 7/14 天后仍然只能看到搜索词复核链缺口或裸指标，不能直接读出同广告组广告 ASIN、单 ASIN 归因边界和广告位证据缺口。

原因：
1. evidence_snapshot 是机器可回读证据，不等于用户可理解的复盘说明。
2. ReviewTodo 证据回读原先只要求投放词证据、ABA 背景、证据缺口和动作边界。
3. ReviewRecord 保存前检查原先没有 ad_group_synthesis 门禁，可能让缺少广告组合流判断的搜索词复盘进入保存流程。

解决：
1. ReviewTodo 回读摘要新增“广告组合流判断”行，直接展示快照中的 value 和 detail。
2. 搜索词复核链必备标签加入“广告组合流判断”。
3. ReviewRecord 保存前检查新增 ad_group_synthesis / ad_group_synthesis_missing。
4. review-ui 和 runtime 测试锁定缺少该标签时必须 blocked。

防错规则：
1. 关键证据不能只在 JSON 里存在，必须进入用户可读摘要。
2. 关键证据不能只展示，必须进入保存前门禁。
3. 搜索词复盘必须回到广告组和同组广告 ASIN，不能把搜索词指标自动归因到单个广告 ASIN。
4. 历史 ReviewTodo 缺证据时只能提示证据缺口，不能用当前证据静默补写历史快照。

验证方式：
npm run test:review-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-22：证据快照扩展后写入读回测试不能停留在旧合同

```text
问题：
preflight 已新增“广告组合流判断”，真实证据快照从 21 条变为 22 条。
但 API roundtrip 测试仍断言 21 条，并只检查旧索引位置，导致测试合同没有覆盖新证据是否进入 ManualAction 和 ReviewTodo。

原因：
1. preflight、ManualAction 写入和 ReviewTodo 派生是三段链路。
2. 只验证 preflight 有新标签，不能证明人工点击后还保留该标签。
3. 旧测试过度依赖证据条数和固定索引，新增证据项后容易误报失败，也容易漏掉写入后丢证据。

解决：
1. API roundtrip 测试升级为 22 条证据合同。
2. 测试同时检查 preflight、ManualAction.evidence_snapshot 和 ReviewTodo.evidence_snapshot 都包含“广告组合流判断”。
3. 使用隔离临时 action_root / review_root 执行一次脚本级写入，验证 post-write 读回 7d / 14d 证据数量一致。

防错规则：
1. 每新增一个点击前关键证据标签，必须同步验证写入后 ManualAction 能读回。
2. 复盘类动作还必须验证 7d / 14d ReviewTodo 继承同一 evidence_snapshot。
3. 测试可以检查关键顺序，但业务判断必须优先按 label 查找，不能只依赖固定索引。
4. 写入验证必须使用隔离临时目录，除非用户明确授权写入默认运行数据。

验证方式：
python -m pytest tests\test_api_routes.py -k "beach_essentials_manual_action"（workdir=backend）
python -c 调用 build_manual_action_apply_payload 隔离写入并断言 22 条证据
python -m pytest backend\tests\test_manual_action_preflight_cli.py
git diff --check
```

## 2026-06-22：广告组合流判断不能只停留在点击前页面

```text
问题：
右侧页面已经展示“人工点击前广告组合流复核”，但后端 manual-action preflight 的 evidence_snapshot_preview 如果没有对应标签，后续 ManualAction / ReviewTodo 读回时仍看不到当时的完整判断链。

原因：
1. 页面广告组合流判断来自前端 ProductScopeAdGroupEvidenceSynthesis。
2. 点击时保存的 evidence_snapshot 来自后端 manual_action_preflight。
3. 两条链路如果没有同名证据项，运营会看到“页面讲清楚了，但复盘快照只剩零散指标”。

解决：
1. manual_action_preflight 新增“广告组合流判断”证据块。
2. 该证据块合并广告 ASIN、投放词、搜索词和广告位粒度。
3. 证据块明确不能自动归因到单个广告 ASIN，不能自动加词、否词、调价或调整广告位。
4. 测试锁定 B00K4W4AAA 样本下 preflight 必须返回该标签。

防错规则：
1. 页面新增关键业务判断后，必须检查后端 evidence_snapshot_preview 是否保存同等判断。
2. ManualAction / ReviewTodo 可回看证据必须包含业务判断、对象边界、能证明和不能证明的内容。
3. 不能把前端展示当成已经进入复盘证据链；必须以后端 preflight 或写入后读回为准。
4. 所有广告组合流判断只服务人工复核，不得包装成自动广告动作。

验证方式：
python -m pytest backend\tests\test_manual_action_preflight_cli.py
python -c 只读调用 build_manual_action_preflight_payload(selected_market_id=1, product_scope_id="parent_asin:B00K4W4AAA")
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：Parent ASIN 首屏不能只展示广告 ASIN 数量

```text
问题：
Parent ASIN 首屏已经能展示 12 个销售表现子 ASIN / 3 个当前投放广告 ASIN。
但如果只展示数量，用户仍可能把 3 个广告 ASIN 误读成完整变体集合，或者把 9 个未投放子 ASIN 也当成广告诊断对象。

原因：
1. 销售表现子 ASIN 来自经营口径，广告 ASIN 来自 advertised_products 投放行，二者不是同一个集合。
2. 广告 ASIN 覆盖率只说明当前广告投放覆盖，不能证明 Parent ASIN 完整子商品数量。
3. 未投放子 ASIN 没有广告花费、订单、搜索词和广告位证据，无法继续做广告信号诊断。

解决：
1. ProductScopeGroupOverview 增加 adCoverageDecision。
2. Parent ASIN 首屏和商品组详情展示“广告 ASIN 覆盖准入判断”。
3. 覆盖判断固定说明能证明、不能证明和人工下一步。
4. 测试覆盖 3/12 覆盖率、未覆盖子 ASIN 和补证路径。

防错规则：
1. Parent ASIN 首屏不能只展示广告 ASIN 数量，必须解释覆盖率和未覆盖对象边界。
2. 未投放子 ASIN 不能进入广告诊断，只能作为经营背景、覆盖缺口或补证对象。
3. 广告 ASIN 数量不能用来推断 Parent ASIN 完整子商品数量。
4. 进入广告诊断前，必须先确认 advertised_products 投放行、非 SP 来源或商品映射证据。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-21：人工动作身份门禁不能只核对对象字段

```text
问题：
右侧“人工动作对象身份门禁”可以核对当前信号、预检目标、人工留痕和复盘待办的 signal_id / object_id / market_id。
但如果门禁只看对象字段，不展示 ReviewTodo.evidence_snapshot 状态，用户仍然不知道该待办是否有可复盘证据。

原因：
1. 对象字段一致只证明待办属于同一对象，不证明证据快照可回看。
2. 证据快照完整性如果只出现在读回摘要里，用户需要跨区块拼接身份和证据状态。
3. ReviewTodo 进入 ReviewRecord 保存前必须同时满足身份一致和证据完整，两者不应割裂展示。

解决：
1. buildManualActionIdentityGateItems 的“复盘待办”明细加入 reviewTodoEvidenceSnapshotReadbackText。
2. 身份门禁 happy path 显示“待办证据快照”和“可回看对象引用”。
3. 缺证据快照时，身份门禁直接显示“待办证据快照待核对”和“缺证据快照 / 对象引用”。

防错规则：
1. 身份门禁不能只做 ID 比对，也要展示待办证据快照是否可回看。
2. 同一对象不等于可保存 ReviewRecord；还必须有对象引用和关键边界证据。
3. 缺证据快照只能提示缺口或治理，不能静默补历史 evidence_snapshot。
4. 复盘待办仍只是排程，不代表效果改善或广告动作已执行。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：右侧风险和不确定性不能脱离诊断合同

```text
问题：
中间诊断区已经通过 diagnosis_contract 展示业务问题、对象粒度、能证明、不能证明、证据缺口和人工下一步。
但右侧“建议处理”里的风险和不确定性仍直接渲染 selectedSignal.risk / selectedSignal.uncertainty。
这会让运营在点击人工动作前看到的仍是泛化 AI 文案，而不是可落地的反证和证据缺口。

原因：
1. 中间诊断合同和右侧人工操作栏的数据来源没有统一。
2. selectedSignal.risk / uncertainty 可能来自后端候选 fallback 或 attribution_boundary，适合兜底，不适合作为人工动作主依据。
3. 右侧是人工确认入口，必须优先回答“不能证明什么”和“还缺什么证据”，不能只展示泛化风险。

解决：
1. 新增 buildManualActionDecisionFactItems。
2. 处理依据优先用点击前可见证据摘要；没有证据摘要时用 diagnosis_contract 当前判断和人工下一步。
3. 风险优先用 diagnosis_contract.doesNotProve。
4. 不确定性优先用 diagnosis_contract.evidenceGap + requiredEvidence。
5. 无诊断合同时才回退到 signal.risk / signal.uncertainty / signalDecisionBoundary。

防错规则：
1. 右侧人工确认区必须优先消费 diagnosis_contract，不能直接把泛化 risk / uncertainty 当作主判断。
2. 搜索词机会必须明确不能证明自动加词、自动调价、自动否词，也不能自动归因到单个 ASIN。
3. 不确定性必须尽量表达证据缺口和需要补证，而不是写成笼统风险。
4. 前端结构测试必须防止 actionFactList 重新裸渲染 selectedSignal.risk / selectedSignal.uncertainty。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：广告组证据不能只并列展示

```text
问题：
广告组问题定位已经展示问题归属、问题落点、搜索词判断和广告位证据。
但如果没有一个合流判断，用户仍要自己把广告 ASIN、投放词、搜索词和广告位缺口拼成业务结论。
这会让页面看起来结构完整，但实际落地时仍像指标堆叠。

原因：
1. 广告组是投放容器，不是产品。
2. 广告 ASIN、投放词、搜索词和广告位各自只能证明一部分事实。
3. 用户需要的是“这些证据共同指向什么人工复核问题”，不是更多并列信息块。

解决：
1. 新增 productScopeAdGroupEvidenceSynthesis。
2. 在广告组问题定位区新增“广告组证据合流判断”。
3. 合流判断固定展示业务问题、证据链、能证明、不能证明、证据缺口和人工下一步。
4. 当前样本明确显示：搜索词分化可复核，但广告位只有活动级背景，不能做广告组级广告位结论。

防错规则：
1. 广告组诊断不能只展示广告商品、搜索词、广告位三个独立块。
2. 必须说明这些证据是否合流到同一个可人工复核问题。
3. 广告位只有活动级背景时，必须显示 partial 或缺口，不能包装成广告组级广告位结论。
4. 合流判断只能服务人工记录观察、标记已处理、加入复盘或忽略本次，不得生成自动广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：复盘待办不能只显示排程状态

```text
问题：
页面已经能生成和读取 ReviewTodo，但如果复盘待办区只展示到期日、对象和证据快照摘要，用户仍然不知道这条待办是否继承了完整的业务判断和证据边界。
这会导致历史待办缺少投放词证据、ABA 背景、证据缺口、动作边界时，运营到保存 ReviewRecord 前才发现不能复盘。

原因：
1. ReviewTodo 是 ManualAction.evidence_snapshot 的继承节点，不只是一个 7/14 天排程。
2. ReviewRecord 保存门禁已经检查关键标签，但它发生在复盘效果 ready 之后。
3. 用户路径上需要先知道“这条待办能不能复盘”，再看处理前后指标。

解决：
1. 新增 buildReviewTodoEvidenceReadbackSummary，提前检查 ReviewTodo.evidence_snapshot。
2. 右侧复盘待办区新增“复盘待办证据回读核对”卡片。
3. 对 search_term 待办强制检查投放词证据、ABA 背景、证据缺口和动作边界。

防错规则：
1. 有 ReviewTodo 只代表有复盘排程，不代表证据链完整。
2. search_term ReviewTodo 缺关键标签时必须显示 blocked，不能误导用户保存 ReviewRecord。
3. 不允许用当前页面证据静默补写历史 ReviewTodo.evidence_snapshot。
4. ReviewTodo 仍然只服务人工复盘，不自动改规则，也不执行广告动作。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-21：已保存 ReviewRecord 不等于证据可回看

```text
问题：页面可以显示“已保存复盘记录”，但如果只看 action_id、object_id、review_window 和指标窗口，仍可能忽略 evidence_snapshot 是否真的指回当前对象。

原因：
1. 指标窗口一致只能证明可做前后对比，不能证明复盘结论沿用了当时的诊断证据。
2. 证据标签齐全也不够，必须能从证据文本里回看当前 object_id、object_label 或稳定搜索词文本。
3. 如果复盘记录混入其他候选对象证据，用户会误以为规则反馈样本已经可信。

解决：
1. ReviewRecord 读回状态要求同时具备对象引用、排查路径、AI 准入、搜索词边界和广告位边界。
2. 复盘结论读回区展示“复盘证据快照：7d N 条”或“复盘证据快照待核对”。
3. 测试增加“标签齐全但对象引用错配”的反例，防止假闭环。

防错规则：
1. 已保存 ReviewRecord 只代表人工结论已保存，不代表证据可用于规则反馈。
2. 规则反馈候选必须能追溯到同一复盘对象的证据快照。
3. 缺对象引用或关键边界时，只能提示人工核对，不能自动改规则或执行广告动作。

验证方式：
npm run test:review-ui
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-22：中间诊断摘要不能缺少 stable object 回读

```text
问题：
中间“业务判断与指标目的”可以展示业务问题、证据强度、能证明、不能证明和人工下一步。
但如果不展示当前诊断对象，用户仍然不知道这条判断最终会不会落到右侧同一个 ManualAction / ReviewTodo / ReviewRecord 对象上。

原因：
1. 指标强度只说明证据是否支持判断，不说明判断对象是谁。
2. 搜索词、广告 ASIN、广告组和 Parent ASIN 是不同对象层级，不能靠页面上下文猜归属。
3. 右侧复盘链路已经要求 stable object，同一约束也必须在中间诊断摘要可见。

解决：
1. SignalDiagnosisEvidenceSummary 增加 objectReadback。
2. buildSignalDiagnosisEvidenceSummary 从 evidence.primary_object / signal.object_type 读出对象类型、对象标识和诊断粒度。
3. UI 摘要新增“诊断对象”一行，说明该对象必须与右侧 ManualAction / ReviewTodo / ReviewRecord 使用同一 stable object。
4. 测试覆盖 beach essentials fixture 和真实 next_unhandled_candidate 链路。

防错规则：
1. 中间诊断摘要不能只输出裸指标、证据强度或泛化业务问题。
2. 每个可进入人工复盘的诊断摘要必须读出 stable object 或明确提示“诊断对象待核对”。
3. 对象缺失时只能提示人工核对，不能把诊断结论直接带入复盘保存。
4. 搜索词证据不能自动归因到 Parent ASIN 或单个广告 ASIN。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-22：复盘效果窗口不能散落展示

```text
问题：
右侧复盘待办区已经能展示待办、复盘效果、保存门槛和指标表。
但处理前窗口、处理后窗口、指标覆盖、下一步和边界散落在多个 span、表格和保存门槛里。
用户容易只看到“已到期”或“ready”，却没看清楚是否真的具备完整处理前后指标，或是否只是可以人工保存 ReviewRecord。

原因：
1. ReviewTodo、ReviewEffect 和 ReviewRecord 是三个不同层级。
2. reviewEffectSummaryText 只透传后端 message，不能独立承担复盘口径解释。
3. 保存门槛强调能否保存，但没有先把处理前 / 处理后窗口整理成稳定阅读块。

解决：
1. 新增 buildReviewEffectWindowLedger。
2. 固定展示状态、处理前窗口、处理后窗口、指标口径、下一步和边界。
3. 右侧复盘待办区在“复盘保存门槛”之前新增“复盘效果窗口口径”卡片。
4. 未 ready 时明确不能保存 ReviewRecord；ready 时明确只保存当前对象、当前动作、当前窗口的复盘记录。

防错规则：
1. 复盘状态必须先说明处理前 / 处理后窗口是否完整，再谈保存 ReviewRecord。
2. ReviewTodo 到期不等于 ReviewEffect ready。
3. ReviewEffect ready 不等于自动保存 ReviewRecord。
4. ReviewRecord 保存后也只进入规则反馈候选，不自动改规则、不执行广告动作。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：ready_for_explicit_manual_write 不能被误读成已处理

```text
问题：
/api/manual-action/preflight 返回 ready_for_explicit_manual_write 时，表示只读预检通过、具备人工授权写入条件。
但 ready 字面上容易被用户理解成系统已经处理完成。
如果页面不展示当前写入数量和授权后预期结果，用户可能误以为 ManualAction、ReviewTodo 或 ReviewRecord 已经生成。

原因：
1. preflight 是只读检查，不是写入动作。
2. will_write=false 且 requires_explicit_authorization=true 时，后端明确不会写 manual_actions。
3. 右侧人工确认区原先更强调按钮可用状态，没有把“当前未写入”和“授权后才写入”放在同一层解释。

解决：
1. 新增 manualActionAuthorizationReadinessSummary，把 preflight 转成可读的待授权写入状态。
2. 页面新增“人工动作待授权写入状态”卡片。
3. 卡片同时展示对象、当前 ManualAction / ReviewTodo 数量、授权后预期数量、证据快照数量和边界。
4. 文案明确说明：ready 只代表可人工确认，不代表已处理；未授权前不写 manual_actions，不生成 ReviewTodo，不保存 ReviewRecord，不执行广告动作。

防错规则：
1. 任何 ready_for_explicit_manual_write 都必须同时展示当前状态和授权后预期结果。
2. 不能只用绿色 ready 表达人工动作状态。
3. preflight ready 不等于 ReviewRecord 可保存，也不等于广告动作已执行。
4. 证据快照预览只能说明授权后可保存和继承，不能说明当前已经落库。

验证方式：
npm run test:review-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-22：推荐诊断对象和可写候选切换时必须显式提示

```text
问题：
当前真实链路里，recommended_candidate 是 beach essentials，但它已经有人工留痕和 ReviewTodo。
后端会把 manual_action_preview 切到 next_unhandled_candidate boys sunglasses。
如果右侧按钮区不说明目标切换，用户会误以为仍在给 beach essentials 写人工动作，实际却可能写到 boys sunglasses。

原因：
1. 推荐诊断对象、已留痕复盘对象、下一未留痕候选是三个不同职责。
2. 后端切换 manual_action_preview 是为了避免重复写同一对象，但 UI 如果只显示按钮目标，会丢掉“为什么切换”的业务解释。
3. 搜索词对象里还可能出现原始快照行 ID，若不展示 object_label，运营难以识别实际对象。

解决：
1. 新增 manualActionQueueTargetSwitchSummary。
2. 页面在人工确认区展示“人工动作目标切换提示”。
3. 提示同时展示诊断推荐对象和当前可写候选。
4. 可写候选同时显示对象 ID 和可读对象名。
5. 选中已留痕推荐对象时提示 blocked；选中下一候选时提示 ready。

防错规则：
1. recommended_candidate 已有人工留痕且存在 next_unhandled_candidate 时，右侧必须说明两个对象不是同一个。
2. 不能把推荐对象的诊断证据写到另一个可写候选。
3. 只展示原始 object_id 不够，搜索词对象必须尽量同时展示 object_label。
4. ready_for_explicit_manual_write 只代表可人工确认，不代表系统已经处理完成。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-22：搜索词机会不能只看广告表现，必须同时说明投放词和 ABA 边界

```text
问题：
搜索词候选可以展示花费、订单、销售额、ACOS 和 CVR，但如果不说明投放词证据和 ABA 市场背景边界，用户仍可能把“有订单”误读成可以自动加词、自动调价，或把 ABA 当成店铺数据。

原因：
1. 广告搜索词表现来自店铺广告上下文，投放词来自关键词 / 商品定向 / 自动投放上下文，ABA 来自站点级市场导出。
2. ABA 只能按站点 + 周期 + 标准化搜索词匹配，不能证明店铺搜索量、广告组问题或单个广告 ASIN 归因。
3. 只有裸指标时，页面回答不了“为什么值得人工复核”“还缺什么证据”“人工下一步是什么”。

解决：
1. 新增 buildSearchTermOpportunityReviewChain，复用 diagnosis_contract 和 business_evidence_blocks。
2. 中间诊断区新增“搜索词机会复核链”。
3. 固定展示投放词证据、ABA 背景、当前判断、能证明、不能证明、证据缺口、需要补证、人工下一步和动作边界。
4. 如果诊断合同只命中广告组边界，也不能把面板标题写成广告组结论；fallback 仍保持“搜索词机会复核链”和 SearchTerm 粒度。

防错规则：
1. 搜索词机会不能只展示花费、订单、ACOS、CVR。
2. ABA 必须标明是站点级市场背景，不能当店铺、产品、广告组或广告 ASIN 数据。
3. 投放词证据必须说明来自当前投放上下文，不代表完整关键词库。
4. 搜索词机会只能进入人工记录观察、标记已处理、加入复盘或忽略本次；不得自动加词、否词、调价或暂停广告。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
浏览器只读检查 http://localhost:5175/
```

## 2026-06-22：右侧人工确认不能丢失搜索词机会复核链

```text
问题：
中间诊断区已经展示“搜索词机会复核链”，但右侧人工确认区只展示 diagnosis_contract 的业务问题、当前判断、能证明、不能证明和人工下一步。
这样用户在最后点击记录观察、标记已处理、加入复盘或忽略本次时，看不到投放词证据、ABA 背景、证据缺口和动作边界。

原因：
1. buildManualConfirmationEvidenceItems 只消费 diagnosis_contract，没有消费 business_evidence_blocks 生成的 SearchTermOpportunityReviewChain。
2. 中间诊断和右侧人工确认形成两套口径，容易出现“中间讲边界、右侧变成泛化动作”的断点。
3. 右侧是人工点击前最后一层门禁，缺少同源证据会削弱落地性。

解决：
1. buildManualConfirmationEvidenceItems 增加 searchTermOpportunityReviewChain 入参。
2. 工作台用 selectedDiagnosisContractItems + selectedTriageBusinessEvidenceItems 生成 selectedSearchTermOpportunityReviewChain。
3. 右侧人工确认证据依据追加投放词证据、ABA 背景、证据缺口和动作边界。
4. runtime 测试用真实 /api/signal-triage 下一候选验证右侧能拿到同一条复核链。

防错规则：
1. 中间诊断新增的核心判断链，如果会影响人工动作，右侧人工确认区也必须消费同源边界。
2. 右侧不能只展示裸指标或泛化建议，必须展示点击前能证明、不能证明和动作边界。
3. ABA 仍只能作为站点级市场背景，不能被右侧包装成店铺或广告组证据。
4. 右侧按钮只能保存人工留痕或复盘待办，不得自动加词、否词、调价或暂停广告。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
浏览器只读检查 http://127.0.0.1:5175/
```

## 2026-06-22：页面展示说清楚但保存快照可能丢失搜索词复核链

```text
问题：
右侧人工确认区已经展示投放词证据、ABA 背景、证据缺口和动作边界，但 ManualAction 写入时实际优先保存后端 preflight.evidence_snapshot_preview.items。
如果后端预检快照没有这些字段，7/14 天 ReviewTodo 复盘时仍只能回看旧的排查路径、AI 准入、搜索词边界和广告位边界，无法复原当时为什么允许人工处理 beach essentials。

原因：
1. 页面展示层和保存快照层不是同一个生成入口。
2. buildManualActionRequestPayload 在有 preflight 且 will_save_on_authorized_write 不为 false 时，会优先使用后端预检快照。
3. 后端预检快照之前只保存投放词结构、搜索词边界、广告位边界和诊断合同摘要，缺少更适合运营复盘扫读的投放词证据、ABA 背景、证据缺口和动作边界标签。

解决：
1. 在 backend/app/services/manual_action_preflight.py 为 search_term 目标补充搜索词复核链快照项。
2. 投放词证据优先取 diagnosis_contract.metrics 中的精确值，保证能看到 beach essentials。
3. ABA 背景显式写成 ABA 排名或 ABA 背景缺口，并继续声明 ABA 不能当店铺、产品、广告组或广告 ASIN 数据。
4. 动作边界随快照保存，明确只允许记录观察、标记已处理、加入复盘或忽略本次，不得自动加词、否词、调价或暂停广告。
5. 前端 preflight 优先证据排序同步加入这些标签，避免用户在点击前看不到保存依据。

防错规则：
1. 影响人工动作的页面证据，必须确认是否也进入 preflight.evidence_snapshot_preview。
2. ReviewTodo 继承的是 ManualAction evidence_snapshot，不会自动从当前页面重新生成证据。
3. ABA 可以保存为判断背景，但不能被解释成店铺表现、广告组表现或广告 ASIN 归因。
4. 保存快照里的“动作边界”不能省略，否则后续复盘容易把人工留痕误读成自动动作建议。

验证方式：
python -m pytest tests\test_api_routes.py -q
python -m pytest backend\tests\test_manual_action_preflight_cli.py -q
npm run test:review-ui
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：ReviewRecord 保存前不能漏掉搜索词复核链

```text
问题：
ManualAction 和 ReviewTodo 已经可以保存投放词证据、ABA 背景、证据缺口和动作边界。
但 ReviewRecord 保存前门禁仍只检查排查路径、AI 准入、搜索词边界、广告位边界和对象引用。
这会导致 search_term 待办即使缺少完整搜索词复核链，也可能被误判为可保存复盘结论。

原因：
1. 上一轮只补齐了人工动作写入和 ReviewTodo 继承，没有同步更新 ReviewRecord 保存前检查。
2. 后端 review_status 读回审计和 CLI readiness 审计仍按旧四类标签判断证据完整。
3. 历史 beach essentials ReviewTodo 只有 17 条旧 evidence_snapshot，缺少投放词证据、ABA 背景、证据缺口和动作边界。

解决：
1. ReviewRecord 保存前检查对 object_type=search_term 增加四类必需门禁：投放词证据、ABA 背景、证据缺口和动作边界。
2. POST ReviewRecord 在 expected_object_type=search_term 时缺任一标签直接拒绝保存。
3. review_status 和 inspect_review_readiness.py 同步输出四类缺口统计和 issue 类型。
4. runtime 测试把当前历史 beach essentials 待办断言为 blocked，而不是 waiting 或可保存。

防错规则：
1. evidence_snapshot 合同加深时，必须同时更新 ManualAction 写入、ReviewTodo 读回、ReviewRecord 保存门禁、API 校验和 CLI 审计。
2. search_term 专属证据链不能套到 advertised_product 门禁上。
3. 历史 evidence_snapshot 缺项只能阻断或要求人工治理，不能静默补写历史 evidence_snapshot。
4. ABA 仍只能作为站点级市场背景，不能包装成店铺、广告组或 ASIN 归因证据。

验证方式：
python -m pytest tests\test_api_routes.py -q
python -m pytest tests\test_signal_triage_service.py -q
python -m pytest backend\tests\test_review_readiness_cli.py -q
npm run test:review-ui
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-22：ready_no_legacy_gap 不能被误读成复盘可保存

```text
问题：
/api/review-evidence-repair 返回 ready_no_legacy_gap，表示当前没有整份历史 evidence_snapshot 缺失。
但同一范围下 /api/signal-triage 的 review_wait_summary 和 rule_improvement_status 都是 blocked_by_review_evidence_gap。
如果前端只把 ready_no_legacy_gap 展示成 ready，用户会误以为历史待办治理已经不阻断复盘，忽略搜索词复核链仍缺投放词证据、ABA 背景、证据缺口和动作边界。

原因：
1. ReviewEvidenceRepair 的职责是治理 legacy 空快照或对象错配，不等于 ReviewRecord 保存门禁。
2. ReviewRecord 保存门禁已经升级到 search_term 专属证据链，但历史待办治理摘要仍按旧语义显示“没有历史证据快照缺口”。
3. UI 没有同时表达“legacy 治理无缺口”和“复盘证据门禁仍阻断”这两个事实。

解决：
1. buildReviewEvidenceRepairSummary 增加 rule_improvement_status 判断。
2. 当 status=ready_no_legacy_gap 且 rule_improvement_status=blocked_by_review_evidence_gap 时，摘要状态改为 blocked。
3. 文案明确说明：旧快照不是整份缺失，但缺搜索词复核链标签，具体缺口以复盘证据门禁为准。
4. runtime 测试用真实接口锁定当前 beach essentials 状态，避免再次把阻断展示成 ready。

防错规则：
1. ready_no_legacy_gap 只说明 legacy 治理无整份缺口，不能代表 ReviewRecord 可保存。
2. 是否能保存 ReviewRecord 必须以 review_wait_summary / review_identity_audit / 保存前 preflight 为准。
3. 页面同时出现多个状态源时，必须解释它们的职责边界，不能只取最乐观状态。
4. 搜索词复核链缺口只能阻断或人工治理，不能静默补写历史 evidence_snapshot。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-22：dry-run 作废路径不能只展示脚本

```text
问题：
历史待办治理已经返回 void_plan，但前端样本项只展示授权码和 dry-run 命令。
用户能看到脚本，却不一定能看清楚作废的是哪个对象、覆盖哪些 7/14 天复盘窗口、真实写入边界是什么。

原因：
1. 后端 void_plan 已经包含 expected_object_type、expected_object_id、review_windows 和 boundary。
2. 前端 ReviewEvidenceRepairPayloadForUi 没声明这些字段，buildReviewEvidenceRepairSummary 也没有把它们组织成业务核对路径。
3. 只展示脚本容易让用户误以为页面在引导直接执行，而不是先做只读治理预检。

解决：
1. 前端 void_plan 类型补齐 action_id、expected_object_type、expected_object_id、review_window、review_windows。
2. 新增 reviewRepairVoidPlanText，按“作废对象 / 复盘窗口 / 授权码 / dry-run / 写入边界”展示。
3. UI 样本只展示 dry-run，不展示 execute_command，不出现 --execute。
4. runtime 测试锁定当前 beach essentials 链路必须显示只写 review_todo_decisions、不修改 manual_actions、不保存 ReviewRecord。
5. 页面渲染新增 dry-run 核对清单，不再只把脚本放进长文本列表。

防错规则：
1. 任何可执行脚本进入 UI 前，必须先展示对象、窗口、授权和写入边界。
2. 页面可以展示 dry-run 命令，但不能把 execute 命令作为普通样本内容展示。
3. ReviewTodo 作废治理不等于广告动作，不等于 ReviewRecord 保存，也不等于历史 evidence_snapshot 补写。
4. 右侧窄栏不能只展示长脚本文本，必须把对象、窗口、授权码、dry-run 和写入边界拆成可扫描字段。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-22：旧待办治理不能把“补录”写成当前可执行路径

```text
问题：
历史待办治理和规则改进门槛已经能提示旧 ReviewTodo 缺证据，但部分文案仍写“补录流程 / 补录策略 / 人工确认补录”。
当前项目实际可验证的 MVP 写入能力只有 review_todo_decisions 作废记录，以及后续重新人工留痕生成新的 ReviewTodo。
如果页面或 API 把“补录”写成下一步，用户会误以为系统可以安全补写历史 evidence_snapshot。

原因：
1. ReviewTodoDecision 数据模型只支持让旧待办退出 ReviewRecord 保存链路，不支持补写历史证据。
2. apply_review_todo_void_once.py 只写 review_todo_decisions，不修改 manual_actions，不保存 ReviewRecord。
3. 当前可重建证据预览只是人工核对材料，不是补写历史 evidence_snapshot 的执行入口。
4. 后端 review_evidence_repair、signal_triage 和前端治理卡片曾使用不同的治理文案，容易让用户误读。

解决：
1. review_evidence_repair 的 patch_policy 改为“人工核对参考”，明确 MVP 不提供补写历史 evidence_snapshot 的执行入口。
2. review_evidence_repair 的 next_action / recommended_next_step 改为：先 dry-run 作废旧待办，再重新人工留痕生成新的 ReviewTodo。
3. signal_triage.rule_improvement.next_step 和 review_wait_summary.next_step 同步改为同一口径。
4. 前端 buildReviewEvidenceRepairSummary 把“补录判断”改成“补证边界”，说明可重建预览不是补写入口。
5. 测试锁定目标代码中不再出现“补录流程 / 补录策略 / 人工补录参考”等旧口径。

防错规则：
1. 没有真实写入链路的动作，不能写成用户下一步。
2. 当前页面证据、preflight 证据和可重建预览都不能补写历史 evidence_snapshot。
3. 旧待办治理当前只允许 dry-run 作废旧待办，然后重新人工留痕；不保存 ReviewRecord，不自动改规则，不执行广告动作。
4. 如果未来要做人工补录，必须先新增独立数据模型、授权门禁、读回验证和测试，不能复用当前文案暗示已经可做。

验证方式：
python -m pytest tests\test_signal_triage_service.py -k "review_evidence or review_readiness"
python -m pytest tests\test_review_evidence_repair_cli.py
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
真实 API 校验 /api/review-evidence-repair 与 /api/signal-triage：包含 dry-run 作废旧待办，不包含补录。
```

## 2026-06-22：已留痕旧对象和待授权新对象不能混读

```text
问题：
推荐对象 beach essentials 已有人工留痕和复盘待办，但当前下一候选 boys sunglasses 仍只是待授权对象。
如果右侧只展示“目标切换”和“预检 ready”，用户容易把旧对象的 ReviewTodo / evidence_snapshot / 复盘状态误读成新对象已经处理。

原因：
1. 推荐对象状态和下一候选状态来自同一份 signal-triage 摘要，但业务含义不同。
2. preflight ready 只说明可人工授权，不代表已经写入 manual_actions。
3. 旧对象的 ManualAction / ReviewTodo 不能证明新对象已处理，也不能被新对象复用。

解决：
1. 新增 manualActionReviewRouteSplitSummary，把“已留痕旧对象”和“待授权新对象”拆成两条轨道。
2. 右侧人工确认区新增“人工确认双轨分流”卡片，放在按钮和只读预检之前。
3. 测试覆盖本地样本、页面结构和真实运行态接口，锁定 ready 只代表可人工授权。

防错规则：
1. recommended_manual_status 只归属于 recommended 对象。
2. next_unhandled_candidate 必须通过自己的 preflight 和人工点击后，才算进入 ManualAction / ReviewTodo 链路。
3. 不允许把旧对象 evidence_snapshot、ReviewTodo 或 ReviewRecord 状态迁移、借用或展示成新对象状态。
4. UI 遇到“推荐已处理 + 下一候选待处理”时，必须先展示对象分流，再展示写入按钮。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：页面证据依据和点击时证据快照不能断开

```text
问题：
右侧已经展示“人工确认证据依据”和“将保存的证据快照”，但没有明确核对两者是否一致。
如果运营读到的业务问题、能证明/不能证明、投放词证据、ABA 背景、证据缺口和动作边界没有进入 evidence_snapshot，后续 ReviewTodo / ReviewRecord 就无法回看当时为什么允许人工确认。

原因：
1. 页面证据依据来自 diagnosis_contract 和搜索词复核链。
2. 点击时保存的 evidence_snapshot 来自后端 manual-action preflight。
3. 两者来源不同，必须显式核对，否则容易出现“页面说得很完整，复盘快照只剩裸指标”的断链。

解决：
1. 新增 manualConfirmationEvidenceReadinessSummary，核对业务判断、证明边界和搜索词复核链是否进入将保存的 evidence_snapshot。
2. 右侧新增“人工确认证据写入核对”卡片，放在人工确认证据依据之后、按钮和预检信息之前。
3. runtime 测试用真实 boys sunglasses 链路验证当前 21 条 evidence_snapshot 已覆盖关键判断标签。

防错规则：
1. 右侧展示给运营看的关键判断，必须能进入 ManualAction.evidence_snapshot，否则不能说已形成可复盘留痕。
2. search_term 人工确认必须同时保存投放词证据、ABA 背景、证据缺口和动作边界。
3. preflight ready 只说明可人工授权；证据写入核对 ready 才说明当前可回看的证据合同完整。
4. 不允许用页面当前证据静默补写历史 evidence_snapshot。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：中间诊断和人工留痕证据不能各讲各的

```text
问题：
中间诊断区已经展示业务问题、能证明、不能证明、证据缺口和人工下一步。
右侧也已经展示人工确认证据依据和 evidence_snapshot 写入核对。
但如果两者没有同步提示，运营仍可能不知道“中间读到的诊断”是否会随人工点击进入后续 ReviewTodo / ReviewRecord 可回看的证据链。

原因：
1. 中间诊断摘要来自 diagnosis_contract。
2. 右侧人工确认证据来自 buildManualConfirmationEvidenceItems。
3. 点击时将保存的证据来自 manual-action preflight。
4. 三者来源不同，只靠相似文案不能证明证据合同贯通。

解决：
1. 新增 manualConfirmationDiagnosisBridgeSummary，核对中间诊断、右侧证据依据和写入核对结果。
2. 右侧新增“诊断到人工留痕同步”卡片，展示业务问题、证明边界、证据缺口和点击快照是否贯通。
3. runtime 测试用真实 boys sunglasses 链路验证当前中间诊断和 21 条 preflight evidence_snapshot 已贯通。

防错规则：
1. 中间诊断区新增关键判断时，必须同步检查它是否进入右侧人工依据或点击时 evidence_snapshot。
2. 页面显示“可复盘留痕”前，必须同时满足诊断可读、人工依据可读、preflight 快照可读。
3. 未授权前仍不能写 manual_actions，不能生成 ReviewTodo，不能保存 ReviewRecord，也不能执行广告动作。
4. 不能用当前页面证据静默补写历史待办或历史人工动作的 evidence_snapshot。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：广告组合流判断不能停留在中间诊断区

```text
问题：
中间广告组问题定位已经新增“广告组证据合流判断”，能说明广告 ASIN、投放词、搜索词和广告位是否共同指向一个可人工复核问题。
但右侧“人工候选与广告组关系”仍只展示候选对象、搜索词边界和广告位边界，没有直接展示合流状态、合流证据链和合流证据缺口。
运营点击人工动作前，仍需要自己把中间卡片和右侧按钮区拼起来。

原因：
1. 上一轮新增 ProductScopeAdGroupEvidenceSynthesis 只接入了中间广告组诊断区。
2. buildManualActionCandidateAdGroupBridge 仍沿用 actionableReview、searchTermDecision 和 placementDecision，没有消费 evidenceSynthesis。
3. 右侧人工确认区是实际动作入口，如果不承接中间判断，用户会误以为候选对象只和搜索词/广告位相关，而不是处在完整广告组合流判断下。

解决：
1. ManualActionCandidateAdGroupBridge 增加合流状态、合流判断、合流证据链、合流证明边界和合流证据缺口。
2. buildManualActionCandidateAdGroupBridge 复用 topGroup.evidenceSynthesis，右侧展示“证据合流：搜索词分化优先”等中间判断。
3. 右侧“人工候选与广告组关系”卡片新增“人工点击前广告组合流复核”清单。
4. 前端测试锁定右侧必须能读到合流状态、投放词/搜索词证据链、广告组级广告位缺口和禁止自动动作边界。

防错规则：
1. 中间诊断区新增关键判断后，必须检查右侧人工确认入口是否承接同一判断。
2. 右侧点击前承接不等于后端 evidence_snapshot 已保存该标签，不能把页面展示包装成复盘快照合同。
3. 广告组合流判断只能说明候选与同广告组上下文的复核关系，不能自动归因到单个广告 ASIN。
4. 任何涉及加词、否词、调价、广告位调整的文案，都必须保持人工复核口径，不得写成自动执行。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：旧待办作废建议和执行门禁不能使用两套证据口径

```text
问题：
/api/review-evidence-repair 提示 beach essentials 的旧待办可以 dry-run 作废。
但实际运行 scripts/apply_review_todo_void_once.py 时，脚本返回 review_todo_has_required_evidence，阻止作废。
这会造成页面建议看似可执行，但运营真正执行 dry-run 时被挡住。

原因：
1. review-evidence-repair 已按搜索词深证据合同判断，要求投放词证据、广告组合流判断、ABA 背景、证据缺口和动作边界。
2. apply_review_todo_void_once.py 仍只检查基础 4 标签：排查路径、AI 准入、搜索词边界、广告位边界。
3. /api/review-todos/void 和复盘记录保存校验也漏掉了“广告组合流判断”。
4. 结果是建议层、脚本层和 API 层对“证据是否足够复盘”的判断不一致。

解决：
1. 作废脚本对 search_term 使用 9 类深证据标签，非 search_term 保持基础标签。
2. /api/review-todos/void 和 ReviewRecord 前置校验补齐“广告组合流判断”。
3. 新增测试锁定：搜索词只有基础 4 标签时可以作废；具备 9 类深证据和对象引用时才不能作废。
4. 重新运行真实 dry-run，确认返回 dry_run_ready 且 will_write=false。

防错规则：
1. 页面建议、dry-run 脚本和 API 写入门禁必须共享同一个业务证据合同。
2. 搜索词复盘不能只看基础 4 标签；必须能解释广告组、投放词、ABA、证据缺口和动作边界。
3. 如果某个入口提示可执行，必须至少用 dry-run 或 API 预检验证该路径真实可走。
4. 作废旧 ReviewTodo 不等于补写历史证据，不等于保存 ReviewRecord，也不等于执行广告动作。

验证方式：
python -m pytest tests\test_review_todo_void_cli.py
python -m pytest tests\test_review_evidence_repair_cli.py
python -m pytest tests\test_api_routes.py -k "review_todo_void_api or review_record_rejects_evidence_snapshot_object_mismatch or without_action_boundary or without_ad_group_synthesis or beach_essentials_manual_action_api_roundtrip"
python scripts\apply_review_todo_void_once.py --market-id 1 --action-id manual-action-d958bfd5cef04a538547bb4c19c48e6f --expected-object-type search_term --expected-object-id "search_term:1:beach essentials" --compact
```

## 2026-06-22：dry-run 计划状态不能只藏在 void_plan.status 里

```text
问题：
右侧历史待办治理卡片已经展示作废对象、复盘窗口、授权码和 dry-run 命令。
但 void_plan.status=dry_run_available 没有显式展示。
运营可能先看到授权码和命令，误以为旧待办已经作废，或者误以为当前可以直接保存 ReviewRecord。

原因：
1. 后端把作废计划状态放在 void_plan.status。
2. 前端 reviewRepairVoidPlanItem 只派生对象、窗口、授权码、dry-run 命令和边界。
3. 状态阶段没有被放到卡片第一行，用户需要从上下文推断“还没执行”。

解决：
1. ReviewEvidenceRepairVoidPlanSummary 新增 statusText。
2. dry_run_available 显示为“dry-run 可预检；真实作废未执行”。
3. 右侧 dry-run 核对清单在作废对象前新增“计划状态”行。
4. 普通单测、真实接口 runtime 测试和结构测试都锁定该状态必须可见。

防错规则：
1. 涉及 dry-run / execute / post-write 的卡片，必须显式展示当前阶段。
2. 授权码只能说明执行时需要什么，不能替代执行状态。
3. 页面展示作废计划时，必须同时说明不修改 manual_actions、不保存 ReviewRecord、不执行广告动作。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-22：manual_action_preflight 不能用基础 4 标签判断搜索词完整留痕

```text
问题：
隔离验证中，旧 7d / 14d ReviewTodo 已通过 apply_review_todo_void_once.py 作废。
但再次运行 apply_manual_action_once.py dry-run 时，preflight 仍返回 duplicate_manual_action。
这会导致“旧待办已清理，但同一个 beach essentials 搜索词仍不能重新加入复盘”的状态冲突。

原因：
1. review-evidence-repair 和 review-todo-void 已经把 search_term 完整证据定义为 9 类深证据。
2. manual_action_preflight._has_required_review_evidence_snapshot 仍只检查基础 4 标签：排查路径、AI 准入、搜索词边界、广告位边界。
3. 旧版浅证据刚好具备基础 4 标签，于是被误判为完整人工留痕。
4. 结果是 target_review_todo_count=0 之后仍被 duplicate_manual_action 阻挡。

解决：
1. manual_action_preflight 新增 SEARCH_TERM_REQUIRED_REVIEW_EVIDENCE_LABELS。
2. search_term 必须具备基础 4 标签 + 广告组合流判断、投放词证据、ABA 背景、证据缺口、动作边界，才算完整留痕。
3. 非 search_term 继续使用基础 4 标签，避免扩大改动。
4. 新增隔离测试证明：旧 4 标签留痕 + 已作废 ReviewTodo 可以重新 dry-run，并授权写入新的 22 项 evidence_snapshot 与 7d / 14d ReviewTodo。

防错规则：
1. search_term 的“可复盘完整证据”只能使用深证据合同，不能退回基础 4 标签。
2. 作废 ReviewTodo 只说明待办阻挡被清理，不说明旧 ManualAction 已经完整。
3. preflight / apply / void / API roundtrip 必须使用同一证据完整定义。
4. 真实写入前优先用隔离目录或 dry-run 验证，不污染真实人工留痕。

验证方式：
python -m pytest tests\test_manual_action_preflight_cli.py -k "rewrite_after_voided or complete_labels_belong or post_write_accepts_new_complete_action_after_legacy_gap"
python -m pytest tests\test_manual_action_apply_cli.py
python -m pytest tests\test_review_todo_void_cli.py
python -m pytest tests\test_manual_action_preflight_cli.py
python -m pytest tests\test_api_routes.py -k "review_todo_void_api or without_ad_group_synthesis or beach_essentials_manual_action_api_roundtrip"
git diff --check
```

## 2026-06-22：历史待办治理卡片不能只展示命令，必须展示重入复盘路径

```text
问题：
右侧历史待办治理卡片已经展示 dry-run 状态、作废对象、复盘窗口、授权码和命令。
但如果没有明确展示“作废后如何重新留痕”，运营仍可能误以为执行作废就等于复盘完成，或误以为可以直接保存 ReviewRecord。

原因：
1. ReviewTodoDecision 只清理旧待办阻挡，不修改旧 ManualAction。
2. 作废旧待办后，还必须重新跑人工动作预检，并由新的 ManualAction 生成新的 7d / 14d ReviewTodo。
3. 原卡片更像技术命令清单，业务阶段不够显性。

解决：
1. ReviewEvidenceRepairVoidPlanSummary 新增 afterVoidText 和 recreateText。
2. dry-run 核对清单新增“作废后”和“重新留痕”两行。
3. search_term 重入复盘文案明确 9 类深证据要求：排查路径、AI 准入、搜索词边界、广告位边界、广告组合流、投放词、ABA、证据缺口和动作边界。
4. 前端 helper 测试、真实 runtime 测试和结构测试都锁定该路径必须可见。

防错规则：
1. 涉及历史治理的卡片必须先讲业务阶段，再展示授权码或命令。
2. 作废旧待办不等于复盘完成，不等于保存 ReviewRecord。
3. 重新加入复盘必须来自新的人工留痕和新的 ReviewTodo，不能补写旧 evidence_snapshot。
4. 搜索词重入复盘必须显式提醒深证据合同，不能退回基础 4 标签。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-22：首屏路径不能只做粗提示，必须能读通业务诊断链路

```text
问题：
首屏已有 Parent ASIN 经营摘要和诊断路径，但原 pathSteps 只有“先看销售盘、再看广告 ASIN、定位广告组、复核流量证据、进入人工闭环”5 个粗步骤。
用户仍需要自己把 Parent ASIN、广告 ASIN、广告组、投放词、搜索词、广告位、AI 信号、人工确认和复盘拼起来。

原因：
1. 原路径更像页面提示，没有明确每一层回答的业务问题。
2. AI 准入、人工确认、7/14 天复盘被压成一个“人工闭环”，用户难以判断当前系统到底到了哪一步。
3. 粗路径容易让人误读为 Parent ASIN 下所有子 ASIN 都进入广告诊断，或误读为搜索词/广告位可以直接归因到单个 ASIN。

解决：
1. ProductScopeFirstScreenSummary 新增 pathSummary。
2. pathSteps 改为 7 个业务节点：Parent ASIN 经营盘、广告 ASIN 覆盖、广告组结构、投放词 / 搜索词 / 广告位、AI 信号诊断、人工确认、7/14 天复盘。
3. 首屏直接渲染 pathSummary，用户不展开详情也能读通路径。
4. 测试锁定 7 个节点、广告组不是产品、搜索词/广告位只做上下文证据、人工动作不能自动执行。

防错规则：
1. 首屏路径必须按业务对象和人工阶段拆开，不能用一句“人工闭环”概括。
2. Parent ASIN 只能作为经营入口；广告诊断必须下钻到有广告证据的广告 ASIN。
3. 搜索词、ABA 和广告位只能提供上下文或市场证据，不能直接当作 ASIN 归因。
4. AI 信号之后必须进入人工确认和 7/14 天复盘，不能包装成自动广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
浏览器核对 http://localhost:5175/ 首屏诊断路径 stepCount=7
```

## 2026-06-22：中间诊断区不能和首屏路径使用两套业务链路

```text
问题：
首屏已经展示 Parent ASIN -> 广告 ASIN -> 广告组 -> 投放词 / 搜索词 / 广告位 -> AI 信号诊断 -> 人工确认 -> 7/14 天复盘。
但中间“广告证据链导览”仍只有 5 层：经营入口、广告 ASIN、广告组、搜索词/广告位、人工确认。
用户进入中间诊断区后，仍可能看不到 AI 信号诊断和复盘阶段，导致首屏和中间区口径不一致。

原因：
1. 对象证据矩阵是 5 个底层数据层，这个口径本身没错。
2. 但 buildProductScopeEvidenceRouteGuide 直接把矩阵 rows 映射为导览步骤，导致业务流程导览也被限制成 5 层。
3. AI 信号诊断和 7/14 天复盘属于业务阶段，不是新的 API 数据层，不能放进矩阵，但必须出现在导览中。

解决：
1. ProductScopeEvidenceRouteGuideStep.layerId 增加 ai_signal、manual_confirmation、review。
2. buildProductScopeEvidenceRouteGuide 保持矩阵 5 行不变，但导览输出 7 个节点。
3. 中间导览明确展示 AI 信号诊断、人工确认和 7/14 天复盘。
4. 样式改为 auto-fit 网格，避免 7 个节点挤压。
5. 单测、结构测试和浏览器核对都锁定中间导览 stepCount=7。

防错规则：
1. 数据层矩阵可以是 5 层，但业务导览必须承接完整 7 节点链路。
2. 不得把人工确认或复盘伪装成 API 数据层。
3. 首屏路径、中间导览和右侧人工确认必须使用同一条业务链路。
4. 当首屏路径变更时，必须同步检查中间 ProductScopeEvidenceRouteGuide 和相关测试。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
浏览器核对 http://localhost:5175/ 广告证据链导览 stepCount=7
```

## 2026-06-22：搜索词机会指标不能合并到泛化指标组

```text
问题：
beach essentials 搜索词机会已经有 diagnosis_contract，但关键指标被后端合并成“广告表现”和“效率”。
前端的指标目的展示按花费、订单、销售额、ACOS 查找合同说明时，容易找不到精确指标，只能退回泛化 fallback。
这样页面看起来有“业务判断与指标目的”，但用户细看仍像是在罗列指标。

原因：
1. 后端合同没有把花费、订单、销售额、ACOS、CVR 作为独立诊断指标返回。
2. 前端不应该自己猜指标业务含义，否则会形成前后端两套口径。
3. 合并指标适合摘要，不适合解释“每个数字为什么服务业务判断”。

解决：
1. search_term_opportunity 合同改为逐项返回花费、订单、销售额、ACOS、CVR。
2. 每个指标都带 purpose：花费用于排除零成本偶然样本，订单用于证明真实店内广告转化，ACOS 只作人工效率参考且不是自动调价依据。
3. 后端测试锁定 beach essentials 的关键指标和值。
4. 前端运行态测试锁定真实 API 返回的指标目的会被 UI helper 消费，而不是退回默认文案。

防错规则：
1. 需要展示在“指标目的”旁边的关键数字，后端 diagnosis_contract 必须用独立 metric 返回。
2. 摘要指标可以合并，但判断合同里的关键指标不能只写成“广告表现 / 效率”。
3. 前端只呈现合同含义，不补写业务解释。
4. 搜索词机会只能进入人工复核，不得包装成自动加词、自动调价、自动否词或单 ASIN 自动归因。

验证方式：
python -m pytest tests\test_signal_triage_service.py -k diagnosis_contract
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-22：右侧预检区不能裸露后端动作码

```text
问题：
右侧人工确认按钮已经是“记录观察 / 标记已处理 / 加入复盘 / 忽略本次”四个业务动作。
但处理依据里的预检摘要仍直接展示后端 action_type，例如 add_to_review。
这不是自动广告动作，但它属于工程字段，运营读起来不像明确的业务下一步。

原因：
1. manual_action_preview.action_type 是后端协议字段，不是用户界面语言。
2. 页面已经有 manualActionLabel，但预检摘要没有复用它。
3. ignore 的预期写入文案没有显式重复“不执行广告动作”，按钮小字可能弱化动作边界。

解决：
1. 前端新增 manualActionDisplayLabel，把后端 actionType 显示为中文人工动作。
2. 右侧预检摘要展示“加入复盘 / 复盘对象 / 对象ID / 窗口 / 不执行广告动作”。
3. manualActionButtonExpectationText 的 ignore 分支补齐“不执行广告动作，不保存复盘结论”。
4. 写后预期和写后预期汇总都补齐“不执行广告动作”。

防错规则：
1. 用户可见的右侧动作区不得直接裸露后端 action_type，除非同时给出中文业务动作。
2. 所有人工动作预期都必须说明不执行广告动作；ignore 也不能省略。
3. 预检只能说明 will_write=false、写入目标和复盘待办预期，不能被描述成广告后台动作。
4. 若后端新增人工动作类型，必须先补中文动作映射和边界文案，再允许展示。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：右侧处理依据不能被证据摘要吞掉业务问题

```text
问题：
右侧风险和不确定性已经开始承接 diagnosis_contract。
但“处理依据”仍优先展示 evidenceReason。
当 evidenceReason 存在时，用户会先看到证据摘要，却看不到该证据服务的业务问题和当前判断。

原因：
1. buildManualActionDecisionFactItems 把 evidenceReason 放在 diagnosis_contract.businessQuestion 和 currentJudgement 之前。
2. 证据摘要适合做支撑材料，不适合作为人工动作的第一句话。
3. 右侧窄栏如果不先说明业务问题，用户会把它读成泛化 AI 建议，而不是可复核的业务判断。

解决：
1. 新增 manualActionDecisionBasisText。
2. 有 diagnosis_contract 时，处理依据固定按“业务问题 / 当前判断 / 证据摘要或关键指标 / 人工下一步”输出。
3. 无 diagnosis_contract 时才回退到原 evidenceReason、why、summary。
4. 前端测试和结构测试锁定业务问题、当前判断、证据摘要必须出现在右侧处理依据中。

防错规则：
1. 右侧处理依据有诊断合同时，必须先回答业务问题和当前判断。
2. 证据摘要只能作为支持材料，不能吞掉业务问题。
3. 任何人工动作展示都必须说明下一步是人工处理，不得包装成自动广告动作。
4. 后续新增信号类型时，必须先补 diagnosis_contract，再允许右侧展示处理依据。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
git diff --check
浏览器运行态核对 http://localhost:5175/
```

## 2026-06-22：搜索词机会合同不能把广告位缺口留给独立板块

```text
问题：
boys sunglasses 的 placement_gap 板块已经能说明广告组级广告位 0 条、同广告活动广告位 3 条。
但 search_term_opportunity 主合同只说订单、花费、ACOS、投放词、广告组和 ABA。
右侧人工确认优先消费 search_term_opportunity 时，用户可能看不到广告位缺口，把低 ACOS 搜索词误读成可直接扩量。

原因：
1. 搜索词机会合同和广告位缺口合同分成两个 section，但右侧处理依据只取优先 section。
2. 广告位层级属于搜索词机会能否落地的关键反证，不能只放在另一个板块。
3. 前端不应该跨 section 猜业务口径，后端 diagnosis_contract 必须把主判断需要的反证说完整。

解决：
1. search_term_opportunity 新增“广告位证据”指标。
2. current_judgement 写入广告位层级，例如“广告组级广告位 0 条 / 同广告活动广告位 3 条”。
3. proves / does_not_prove / evidence_gap / next_manual_step 同步补广告位边界。
4. 后端测试锁定搜索词机会主合同必须带广告位证据和 ad_placement_daily_metrics 需求。

防错规则：
1. 搜索词机会主合同必须包含广告位层级，不能要求用户从 placement_gap 另行拼口径。
2. 只有活动级广告位时，只能作为背景，不能证明广告位造成搜索词表现差异。
3. 缺广告组级广告位时，人工下一步必须先核对广告位层级，再决定是否记录观察或加入复盘。
4. 前端继续只呈现 diagnosis_contract，不补写后端没有返回的业务解释。

验证方式：
python -m pytest tests\test_signal_triage_service.py -k diagnosis_contract
python -m pytest tests\test_signal_triage_service.py
python 只读调用 build_signal_triage_payload 核对 boys sunglasses
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run test:workbench-structure
npm run build
git diff --check
```

## 2026-06-22：前端不能把来源快照行 ID 当人工复盘对象

```text
问题：
后端搜索词 preflight target 同时有稳定 object_id 和 source_object_id。
前端类型原来只表达 object_id，没有表达 source_object_id，容易让后续开发看不出“复盘对象”和“来源追溯 ID”的边界。

原因：
1. 搜索词旧对象曾使用 snapshot_id:row_id 作为对象 ID。
2. 修复后稳定对象应是 search_term:{market_id}:{搜索词}，旧行 ID 只能保留为 source_object_id。
3. 如果前端未来从 manual_action_preview、selectedSignal 或候选行重新拼 expected_object_id，就可能绕过后端预检目标。

解决：
1. 前端 ManualActionPreflightTarget / ManualActionPreflightForUi.target 补充 source_object_id。
2. buildManualActionRequestPayload 继续只使用 preflight.target.object_id / object_type 生成提交预期对象。
3. review-ui 测试锁定：source_object_id 为 gerpgo_market_1_20260616_120443:507942344 时，payload expected_object_id 仍是 search_term:1:boys sunglasses。

防错规则：
1. 人工动作提交 payload 的 expected_object_id 必须来自后端 preflight.target.object_id。
2. source_object_id 只用于来源追溯，不能进入 expected_object_id。
3. 前端不得从候选对象、信号对象或旧快照行 ID 自行拼复盘对象。
4. 搜索词复盘对象必须使用稳定业务 ID，不能回退到一次性快照行 ID。

验证方式：
npm run test:review-ui
npm run build
git diff --check
```

## 2026-06-22：隔离写入演练不能只看 post-write 汇总数字

```text
问题：
apply_manual_action_once.py 原有 smoke_assertions 主要看 post-write preflight 的汇总计数。
如果 ReviewTodo 实际记录没有继承正确 object_id、action_id 或 evidence_snapshot，单靠汇总数字不一定能立刻暴露。

原因：
1. post-write 汇总能证明目标数量和证据条数，但不能直接证明实际 ReviewTodo 行的对象身份。
2. 搜索词链路同时存在 stable object_id 和 source_object_id，需要确认旧快照行 ID 没有进入复盘对象。
3. 复盘闭环最终依赖 ReviewTodo 记录，而不是只依赖 preflight 摘要。

解决：
1. 隔离 action_root 写入后，脚本直接 build_review_todos 读取实际待办。
2. smoke_assertions 增加实际 ReviewTodo 对象 ID、证据条数、action_id 继承和 evidence_snapshot 继承检查。
3. 增加 source_object_id_not_used_as_review_object 断言。
4. 增加测试锁定 search_term:1:boys sunglasses 的隔离写入会生成 7d / 14d 两条同对象 ReviewTodo。

防错规则：
1. 涉及人工留痕到复盘待办的演练，不能只看 target_review_todo_count。
2. 必须读回实际 ReviewTodo，核对 object_id、action_id 和 evidence_snapshot。
3. source_object_id 只能用于来源追溯，不能进入 ManualAction 或 ReviewTodo 的复盘对象字段。
4. 隔离演练必须使用临时 action_root / review_root，不能污染默认人工记录。

验证方式：
python -m pytest tests\test_manual_action_apply_cli.py -q
python -m pytest tests\test_manual_action_apply_cli.py tests\test_manual_actions.py tests\test_api_routes.py -q
scripts\apply_manual_action_once.py 隔离 dry-run + execute
git diff --check
```

## 2026-06-22：ReviewRecord 服务层不能只相信保存请求里的证据快照

```text
问题：
API 路由层已经会用 ReviewTodo 的 evidence_snapshot 做签名校验，但 save_review_record 服务函数本身只校验了少量必需标签。
如果未来有脚本、测试或内部调用绕过路由，仍可能把另一套看似完整但并非当前人工动作继承的证据快照保存成 ReviewRecord。

原因：
1. ReviewEffectResult 原来只带动作对象、窗口和前后指标，没有带 ManualActionRecord.evidence_snapshot。
2. save_review_record 无法证明 expected_evidence_snapshot 是否来自同一条人工动作。
3. 搜索词复盘比普通对象更容易混入口径，需要动作边界、ABA 背景、投放词证据、证据缺口等业务证据。

解决：
1. ReviewEffectResult 新增 evidence_snapshot，并由 build_review_effect_result 从 ManualActionRecord 继承。
2. save_review_record 保存前比较 expected_evidence_snapshot 与 effect.evidence_snapshot 的签名。
3. save_review_record 对搜索词对象补充广告组合流判断、投放词证据、ABA 背景、证据缺口、动作边界门禁。
4. API 路由补齐相关 409 错误透传。

防错规则：
1. ReviewRecord 必须复盘同一条 ManualAction 的原始 evidence_snapshot。
2. 前端和脚本不能用页面缓存、selectedSignal 或临时拼装证据替代 ReviewTodo / ReviewEffectResult 继承的证据快照。
3. 搜索词复盘缺少动作边界时不能保存，因为这会把 AI 建议误包装成自动广告动作。
4. 服务层也必须有门禁，不能只依赖 API 路由层。

验证方式：
python -m pytest tests\test_manual_actions.py -q
python -m pytest tests\test_api_routes.py -q
python -m pytest tests\test_manual_action_apply_cli.py tests\test_manual_actions.py tests\test_api_routes.py -q
npm run build（workdir=frontend）
git diff --check
```

## 2026-06-22：经营商品入口不能混放多种业务口径

```text
问题：
顶部筛选器原名为“经营商品入口”，但下拉选项同时包含 Parent ASIN、广告 ASIN、全量排查、未归因数据和销售 ASIN 背景。
用户会误以为这些都是同一种经营商品入口，进而把没有广告数据的销售对象也当成广告诊断对象。

原因：
1. 后端 product_scope 需要返回多种 option 支撑诊断、下钻和排查。
2. 前端原先把 Parent ASIN 和 advertised_asin 合并进“广告分析入口”分组。
3. 字段名“经营商品入口”过窄，无法解释 all / unattributed / sales_asin 这些辅助口径。

解决：
1. 顶部字段名改为“诊断入口”。
2. Parent ASIN 分组命名为“经营入口（Parent ASIN）”。
3. advertised_asin 分组命名为“广告下钻入口（仅已投广告 ASIN）”。
4. all / unattributed / sales_asin 统一放入“辅助排查入口（非广告动作对象）”。

防错规则：
1. Parent ASIN 是经营盘子入口，不等于广告动作对象。
2. 广告诊断下钻只能落到有广告数据的广告 ASIN、广告组、投放词、搜索词和广告位。
3. sales_asin 只能作为销售背景或覆盖缺口，不应被包装成广告诊断入口。
4. all / unattributed 只能用于排查，不能作为默认经营诊断口径。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
rg 检查旧混合标签
git diff --check
```

## 2026-06-22：语义组复核不能反向改写诊断入口

```text
问题：
用户点击“语义组复核”下的数据后，顶部“诊断入口”筛选器的值会变化。
这会让用户误以为当前 Parent ASIN、广告 ASIN 或辅助排查口径被系统自动替换，破坏广告诊断链路。

原因：
1. frontend/src/pages/SignalTriageWorkbench/SignalTriageWorkbench.tsx 的 handleSelectSearchIntent 先在当前 displayProductScopedSignals 中筛选语义组。
2. 如果当前入口无命中，旧逻辑会继续到 normalizedSignals 全量信号中查找。
3. 一旦全量信号存在该语义组，旧逻辑会 setSelectedProductScopeId("all")，导致顶部诊断入口自动切到全量排查。

解决：
1. handleSelectSearchIntent 只在当前诊断入口下筛选语义组。
2. 删除 normalizedSignals 全量回退和 setSelectedProductScopeId("all")。
3. 增加结构测试，锁定语义组复核不再改写 selectedProductScopeId。

防错规则：
1. 顶部“诊断入口”是上游业务口径，只能由用户明确选择。
2. 语义组复核、证据卡片、下钻卡片只能筛选或聚焦当前入口内的数据，不能反向切换上游口径。
3. 如果当前入口下没有命中，应展示“当前入口无命中”的证据缺口；如需跨入口查看，必须提供显式按钮和说明。
4. 不允许为了让卡片有内容而自动切到 all，这会把全量排查伪装成当前 Parent ASIN 诊断。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
git diff --check
```

## 2026-06-22：Parent ASIN 下钻不能缺少投放词结构证据

```text
问题：
Parent ASIN 下钻原先能看到广告 ASIN、广告组、搜索词和广告位，但投放词结构表达不足。
从 Amazon 广告架构看，运营判断广告组问题时需要同时看广告组内投放商品、投放词、搜索词和广告位；只看搜索词会让证据链断层。

原因：
1. 后端 top_ad_group 已经能从搜索词表现行读取广告组上下文，但没有把投放词上下文放到 product_scope_drilldown。
2. 前端诊断路径和对象证据矩阵仍以“搜索词/广告位”作为流量层，缺少“投放词 -> 搜索词”的业务关系。
3. 投放词来自搜索词表现行中的 keyword_text / target_id，只能作为人工复核证据，不能被误当成完整关键词库。

解决：
1. backend/app/services/signal_triage.py 为 top_ad_group 增加 targeting_context。
2. frontend/src/api.ts 和 signalUi.ts 增加投放词上下文类型和 UI helper。
3. 诊断路径、对象证据矩阵、下钻证据改为展示“投放词/搜索词/广告位”。
4. 页面明确投放词边界：不代表完整关键词库，不能自动加词、否词或调价。

防错规则：
1. Parent ASIN 经营入口下钻广告问题时，流量层至少要表达投放词、搜索词和广告位三类证据。
2. 搜索词不能自动归因到单个广告 ASIN；投放词也不能自动变成广告动作。
3. 投放词结构来自搜索词表现数据时，必须标注“不是完整关键词库”。

验证方式：
python -m pytest backend\tests\test_signal_triage_service.py -q -k "product_scope or diagnosis_contract"
python -m pytest backend\tests\test_signal_triage_service.py backend\tests\test_signal_triage_cli.py -q
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
git diff --check
```

## 2026-06-22：候选定位和复盘定位不能隐式切到全量排查

```text
问题：
上一轮修复了“语义组复核”点击会改变诊断入口，但推荐候选、下一个未留痕候选和复盘待办定位仍可能在当前入口无命中时自动切到 all。
这会让用户点击一个定位按钮后丢失当前 Parent ASIN / 广告 ASIN 语境。

原因：
1. handleLocateReviewSignal 在当前 displayProductScopedSignals 无目标信号时会 setSelectedProductScopeId("all")。
2. 推荐候选和下一个候选按钮内联了同样逻辑：当前入口无目标信号时直接切到 all。
3. 这类按钮的用户意图是“定位信号”，不是“切换诊断入口”；自动切换会混淆业务口径。

解决：
1. 新增 handleLocateSignalInCurrentScope，统一负责当前入口内定位。
2. 当前入口无命中时只写提示：诊断入口已保持不变，需要跨范围查看时使用显式全量排查入口。
3. 删除推荐候选、下一个候选、复盘待办定位中的隐式 setSelectedProductScopeId("all")。
4. 结构测试锁定源码中不再出现 setSelectedProductScopeId("all")。

防错规则：
1. 只有顶部诊断入口下拉、广告 ASIN 显式下钻、带“切到全量排查”文案的按钮可以改变 product_scope。
2. 推荐候选、下一个候选、复盘待办定位只允许在当前 product_scope 内定位信号。
3. 当前 product_scope 无命中时必须展示范围缺口，不能为了定位成功而自动切 all。
4. all 是辅助排查入口，不是下游卡片的自动兜底口径。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run build
rg 检查 SignalTriageWorkbench.tsx 不再出现 setSelectedProductScopeId("all")
git diff --check
```

## 2026-06-22：人工动作写入不能使用前端临时证据兜底

```text
问题：
buildManualActionRequestPayload 原先在后端 preflight 没有返回 evidence_snapshot_preview.items 时，会回退使用 input.evidenceSnapshot。
这会让页面展示用的语义组证据、selectedSignal 证据或前端合并证据进入 manual_actions.evidence_snapshot。

原因：
1. 前端展示证据和后端确认可保存证据的职责不同。
2. evidenceSnapshot 是页面当前上下文的解释材料，不一定经过后端目标对象、product_scope 和动作边界校验。
3. 如果没有 evidence_snapshot_preview，后续 ReviewTodo / ReviewRecord 可能继承一份无法证明来自后端预检合同的证据。

解决：
1. buildManualActionRequestPayload 删除 input.evidenceSnapshot 写入 fallback。
2. manualActionButtonGate 要求 preflight.evidence_snapshot_preview.items 存在且 will_save_on_authorized_write 不为 false。
3. 缺少可保存预览时禁用人工动作按钮，并提示不能用前端临时证据写入人工留痕。
4. 测试锁定：无后端可保存预览时 payload.evidence_snapshot 为空，按钮不可点击。

防错规则：
1. 前端 fallback evidence 只能用于展示，不能用于持久化。
2. manual_actions.evidence_snapshot 只能来自后端 manual-action preflight 的 evidence_snapshot_preview。
3. 人工动作按钮 ready 必须同时满足目标一致、动作一致、will_write=false 和可保存证据预览存在。
4. 任何绕开后端预检证据预览的写入，都视为复盘证据链污染风险。

验证方式：
npm run test:review-ui
npm run test:signal-ui
```

## 2026-06-22：复盘等待摘要的缺证据口径必须跨入口一致

```text
问题：
boys sunglasses 人工闭环写入后，smoke_review_wait_contract 发现 review_readiness CLI 和 signal_triage_service 对历史 beach essentials 待办的缺证据摘要不一致。
CLI 只提示缺少投放词证据、ABA 背景、证据缺口和动作边界；服务层还提示缺少广告组合流判断。

原因：
1. 服务层已经从 evidence_snapshot 读取 has_ad_group_synthesis，并生成 missing_ad_group_synthesis。
2. CLI 只读取了投放词、ABA、证据缺口和动作边界，没有把“广告组合流判断”纳入 readback key、计数、issue 和文案映射。
3. 两个入口对同一批 ReviewTodo 的口径漂移，会让运营误以为缺口少了一类。

解决：
1. scripts/inspect_review_readiness.py 增加 has_ad_group_synthesis 读回。
2. review_identity_audit_summary 增加 missing_ad_group_synthesis_count。
3. review_identity_audit_issues 增加 missing_ad_group_synthesis issue。
4. review_audit_issue_reason 增加“缺少广告组合流判断”映射。
5. backend/tests/test_review_readiness_cli.py 增加回归测试，锁定摘要文案和审计 issue。

防错规则：
1. 新增 evidence_snapshot 必要标签时，CLI、服务层、smoke 对比和测试必须同步更新。
2. 复盘等待摘要必须以 ReviewTodo 读回事实为准，不允许一个入口少报证据缺口。
3. 历史待办缺证据时，只能提示作废旧待办后重新人工留痕，不能静默补写旧 evidence_snapshot。

验证方式：
python scripts\smoke_review_wait_contract.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA
python -m pytest backend\tests\test_review_readiness_cli.py backend\tests\test_review_wait_contract_smoke.py -q
```

## 2026-06-22：搜索词复盘证据不能漏保存“需要补证”

```text
问题：
搜索词人工确认和 ReviewTodo 复盘读回如果只保存投放词证据、广告组合流判断、ABA 背景、证据缺口和动作边界，却不保存“需要补证”，7/14 天后就无法证明当时运营知道还缺哪些证据。
这会让复盘从“回看当时判断边界”退化成“只看后置指标变化”，容易把搜索词裸指标误判成自动加词、否词或调价依据。

原因：
1. “证据缺口”和“需要补证”是两个不同问题：前者说明当前不能证明什么，后者说明后续还要人工补哪类证据。
2. 之前部分入口只要求证据缺口和动作边界，没有把“需要补证”纳入 ReviewTodo / ReviewRecord 必需合同。
3. 前端优先证据默认只展示 6 条，会截断证据缺口、需要补证或动作边界，用户第一眼看不到复盘关键项。

解决：
1. 后端搜索词必需证据标签加入“需要补证”。
2. ReviewRecord 保存前门禁同时校验证据缺口、需要补证和动作边界。
3. signal_triage、review_readiness CLI、review_evidence_repair 统一返回 missing_required_evidence。
4. 前端 ReviewTodo 回读、ReviewRecord 保存前清单、历史治理卡片、人工确认优先证据行统一展示“需要补证”。
5. 优先证据默认展示 9 条，保证搜索词复核链关键项不会被截断。

防错规则：
1. 搜索词复核链必须同时包含：投放词证据、广告组合流判断、ABA 背景、证据缺口、需要补证、动作边界。
2. 新增或调整 evidence_snapshot 必需标签时，必须同步更新服务层、CLI、前端 helper、ReviewRecord 门禁和测试。
3. 真实接口状态如果已经 ready，不要继续用旧“历史缺链路 blocked”假设写 runtime 测试。
4. 复盘窗口未到期时，只能等待和只读核对；不能拉快照、不能保存 ReviewRecord、不能自动执行广告动作。

验证方式：
npm run test:review-ui
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
python -m pytest backend/tests/test_manual_action_preflight_cli.py backend/tests/test_manual_actions.py backend/tests/test_api_routes.py backend/tests/test_review_evidence_repair_cli.py backend/tests/test_review_readiness_cli.py backend/tests/test_review_todo_void_cli.py -q
```

## 2026-06-22：广告商品复盘不能只保存覆盖指标

```text
问题：
广告商品对象进入人工复盘时，如果只保存“广告商品覆盖”或广告 ASIN 指标，7/14 天后无法证明当时是否回看过广告组容器、搜索词 / 广告位上下文、证据缺口和动作边界。
这会把 advertised_product 复盘退化成浅指标对比，容易误导用户把搜索词或广告位背景自动归因到单个广告 ASIN。

原因：
1. 之前搜索词复核链已经要求投放词证据、广告组合流判断、ABA 背景、证据缺口、需要补证和动作边界。
2. advertised_product 只使用基础证据门禁，缺少广告商品覆盖、广告组合流判断、证据缺口、需要补证和动作边界的强制保存。
3. 前端保存前清单和 ReviewTodo 读回没有按 advertised_product 对象展示广告商品复核链。

解决：
1. advertised_product 的 preflight 必需证据加入广告商品覆盖、广告组合流判断、证据缺口、需要补证和动作边界。
2. ReviewRecord 服务层和 API 层保存前同步校验 advertised_product 复盘链。
3. 前端 ReviewTodo 读回、ReviewRecord 保存前清单、人工确认证据对齐摘要都识别广告商品复核链。
4. 只有出现“广告商品覆盖”时才触发广告商品复核链，避免误伤 SearchTerm 复核链。

防错规则：
1. 广告商品复盘对象是 advertised_product，广告组、搜索词和广告位只能作为上下文证据。
2. 广告商品复盘必须保存广告商品覆盖、广告组合流判断、证据缺口、需要补证和动作边界。
3. 新增对象类型的 evidence_snapshot 必需标签时，必须同步更新 preflight、ReviewRecord 门禁、前端保存前清单和测试。

验证方式：
npm run test:review-ui
python -m pytest tests/test_manual_actions.py tests/test_api_routes.py tests/test_manual_action_preflight_cli.py -q
npm run build
python -m py_compile app/services/manual_action_preflight.py app/services/manual_actions.py app/api/routes.py
git diff --check
```

## 2026-06-23：人工处理依据不能由前端语义上下文兜底

```text
问题：
右侧人工确认区虽然写入 payload 已经只使用后端 preflight 的 evidence_snapshot_preview，但“处理依据”展示仍可能从前端合并证据中取摘要。
当用户处于语义组聚焦状态时，这会让“语义组 / ABA 背景 / 页面临时证据”看起来像可保存依据。

原因：
1. 前端展示证据用于帮助用户理解当前上下文，不等于后端确认可保存证据。
2. 语义组是 SearchTerm 机会的二级筛选和复核背景，不是人工动作对象。
3. 如果处理依据允许前端临时证据兜底，用户会把“看到证据”误读成“可以写入复盘证据”。

解决：
1. 新增 manualActionSavableEvidenceReasonText。
2. 处理依据只在后端 preflight 返回可保存 evidence_snapshot_preview 时展示证据摘要。
3. 不可保存时展示“等待后端可保存 evidence_snapshot_preview；页面语义组和临时证据只用于只读核对，不写入人工动作”。
4. 测试锁定：不可保存预检不会把语义组文本暴露为处理依据。

防错规则：
1. 前端语义组、页面合并证据和 fallback evidence 只能用于只读核对。
2. “处理依据”“将保存的证据快照”“manual_actions.evidence_snapshot”都必须以后端 preflight 可保存预览为准。
3. 语义组不是人工动作对象，不能被包装成可写留痕对象。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui
npm run build
```

## 2026-06-23：语义组板块名称不能弱化真实数据口径

```text
问题：
左侧语义组相关板块实际只服务当前诊断入口内的 SearchTerm 机会聚焦，但用户可见主标题仍写成“搜索词机会二级筛选”。
这会弱化“语义组不是经营商品入口、不是人工动作对象、不是商品归因”的真实边界。

原因：
1. 后续代码已经把点击逻辑收敛为 displayProductScopedSignals 内过滤，但标题仍保留早期功能型描述。
2. “二级筛选”说明了交互层级，却没有说明业务对象和数据粒度。
3. 用户在排查广告问题时更需要知道当前看的是 SearchTerm 语义聚焦，而不是泛化筛选器。

解决：
1. 左侧板块标题和 aria-label 改为“搜索词语义聚焦”。
2. 板块说明改为“当前诊断入口内的 SearchTerm 机会聚合”。
3. 空态和卡片用途说明同步改为语义聚焦口径。
4. 结构测试锁定旧标题不再作为主入口名称出现。

防错规则：
1. 语义组相关 UI 必须显式说明它只服务当前诊断入口内的 SearchTerm 机会。
2. 不要把语义组包装成经营商品入口、广告对象或人工动作对象。
3. 涉及语义组的展示文案必须同时说明不改变诊断入口、不证明单个 ASIN 归因、ABA 仅作站点级背景。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
```

## 2026-06-23：对象级复盘证据门禁不能只在保存时生效

```text
问题：
ReviewRecord 保存门禁已经要求 advertised_product 和 placement 保留深证据，但历史待办审计和前端复盘门禁摘要仍主要按 search_term 统计“投放词证据 / 证据缺口 / 需要补证 / 动作边界”。
这会导致广告商品或广告位待办缺少广告商品覆盖、广告位表现、证据缺口、需要补证或动作边界时，保存时会被拦住，但用户在治理摘要里不一定能提前读出缺口。

原因：
1. 搜索词复核链先被打深，后续 advertised_product 和 placement 保存门禁补强后，没有同步扩展 review_identity_audit。
2. review_evidence_repair 仍只按 search_term 选择深证据必需标签。
3. 前端复盘身份门禁仍使用“搜索词复核链”作为跨对象摘要名称。

解决：
1. review_identity_audit 按 object_type 审计对象复核链。
2. advertised_product 审计广告商品覆盖、广告组合流判断、同组投放商品表现、证据缺口、需要补证和动作边界。
3. placement 审计广告位表现、证据缺口、需要补证和动作边界。
4. review_evidence_repair 按 object_type 选择 search_term / advertised_product / placement 必需证据标签。
5. 前端复盘身份门禁摘要改为“对象复核链”，并展示广告商品覆盖和广告位表现缺口。

防错规则：
1. 新增或强化某个 object_type 的 ReviewRecord 保存必需标签时，必须同步更新 manual_action_preflight、save_review_record、API 预检、review_identity_audit、review_evidence_repair、前端摘要和测试。
2. 广告位表现只能说明流量位置层级，不能自动归因到单个搜索词、广告组或广告 ASIN。
3. 广告商品覆盖只能证明广告 ASIN 有投放证据，不能把搜索词或广告位背景自动归因到单个 ASIN。
4. 历史待办缺对象复核链时，只能提示 dry-run 作废旧待办并重新人工留痕，不能补写历史 evidence_snapshot。

验证方式：
python -m pytest tests/test_signal_triage_service.py tests/test_review_readiness_cli.py tests/test_review_evidence_repair_cli.py -q
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:review-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：SearchTerm 语义筛选不能暗示可直接进入人工动作

```text
问题：
SearchTerm 机会筛选的承接提示原先写成“右侧人工动作也必须落到这条 SearchTerm 的稳定对象”。
这句话虽然表达了对象必须稳定，但在语义组筛选场景下，容易让用户误以为点击语义组后就可以直接进入人工动作。

原因：
1. 语义组只是当前诊断入口下的 SearchTerm 机会筛选上下文。
2. 人工动作是否可写，必须由后端 preflight 的 target 和 evidence_snapshot_preview 决定。
3. 页面文案如果跳过“若进入人工动作”和“后端预检确认”，会弱化语义组不是人工动作对象的边界。

解决：
1. buildSearchIntentReviewCards 的用途说明改为先打开具体 SearchTerm 信号核对证据。
2. buildSearchIntentFocusContext 的承接说明改为“若进入人工动作，右侧必须以后端预检确认的 SearchTerm 稳定对象为准”。
3. 前端测试锁定旧强暗示不再出现。

防错规则：
1. 语义组 / SearchTerm 筛选上下文只能缩小当前入口下的 SearchTerm 机会队列。
2. 语义组不是人工动作对象，不能直接写入 manual_actions。
3. 任何人工动作文案都必须以具体信号对象和后端 preflight 为准。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：当前诊断路径不能停在 AI 准入

```text
问题：
左侧当前诊断路径只展示到“AI 准入”，没有继续展示“人工确认”和“7/14 天复盘”。
这会让用户误以为 AI 准入就是广告诊断闭环终点。

原因：
1. 首屏路径和广告证据链导览已经补齐人工确认与复盘，但左侧 QueueDiagnosisPathPanel 使用的 buildDiagnosisPathSummary 仍停留在早期 5 步结构。
2. 目标 MVP 要跑通的是 Parent ASIN -> 广告证据 -> AI 诊断 -> 人工确认 -> 复盘闭环，不是只展示 AI 候选状态。
3. 如果路径不显示 ReviewTodo / ReviewRecord 门槛，用户会把“可进入人工确认”误解为“已产生业务结论”。

解决：
1. buildDiagnosisPathSummary 的 description 补齐“AI 准入 -> 人工确认 -> 7/14 天复盘”。
2. steps 增加“人工确认”和“7/14 天复盘”。
3. boundary 明确未完成人工留痕和 ready 复盘前不能保存 ReviewRecord 或判断处理有效。
4. 前端测试锁定路径长度、无候选状态、ready 候选状态和 ReviewRecord 边界。

防错规则：
1. 任何诊断路径文案都不能只停在 AI 准入。
2. 当前路径必须同时说明人工确认和 7/14 天复盘门槛。
3. ready 只代表可人工确认或可人工保存，不能代表系统自动执行或自动判断处理有效。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：语义组复核不能被理解成语义资产入口

```text
问题：
用户指出“语义组复核”的作用应该是从 Parent ASIN 的角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果继续使用“语义组复核”或“语义聚合”作为用户可见主名称，用户会误以为系统在复核语义资产、自然搜索词或 Parent ASIN 下全部变体，而不是广告搜索词表现。

原因：
1. 后端 /api/search-intents 已经按 product_scope_id 过滤当前诊断入口下的 ad_search_term_daily_metrics。
2. 前端标题仍偏“语义组”表达，弱化了 Parent ASIN 视角和广告搜索词事实来源。
3. 语义标签只是分组字段，不是经营对象、广告对象或人工动作对象。

解决：
1. 用户可见标题改为“Parent ASIN 广告搜索词聚合复核”。
2. 副标题和用途说明改为“从当前 Parent ASIN 视角聚合广告中的用户搜索词表现”。
3. search_intent 展示标签从“语义聚合”改为“搜索词聚合”。
4. 测试锁定旧的“搜索词表现复核（语义聚合）”主标题不再作为入口出现。

防错规则：
1. 该板块只能说明当前 Parent ASIN 诊断入口下的广告用户搜索词表现，不能说明 Parent ASIN 下全部自然搜索或市场搜索表现。
2. 语义标签 / intent_label 只是 SearchTerm 聚合维度，不能被包装成可人工处理对象。
3. 点击搜索词聚合只能筛选当前入口下的 SearchTerm 信号，不能改写顶部诊断入口。
4. 进入人工动作前，必须打开具体 SearchTerm 信号，并以后端 preflight 的稳定对象和 evidence_snapshot_preview 为准。

验证方式：
GET /api/search-intents?market_id=1&product_scope_id=parent_asin:B00K4W4AAA
npm run test:signal-ui
npm run test:workbench-structure
```

## 2026-06-23：推荐候选与下一个未处理候选的诊断合同选择不一致

```text
问题：
运行 python -m pytest tests\test_manual_actions.py tests\test_signal_detection.py tests\test_signal_triage_service.py 时，138 个测试通过，1 个测试失败：
test_signal_triage_points_to_next_unhandled_candidate_when_recommended_is_waiting。
该测试期望 payload["diagnosis_contract"]["signal_id"] 是 sig-unhandled，实际返回 sig-handled。

原因：
当前 build_signal_triage_payload 在推荐候选已有人工动作、下一个未处理候选才应进入人工确认时，顶层 diagnosis_contract 仍可能保留推荐候选。
这与本轮“广告搜索词聚合上下文”文案改动无关，但会影响右侧人工确认应读取哪个诊断合同。

解决：
已修复。build_signal_triage_payload 现在在推荐候选已有人工动作、且存在 next_unhandled_diagnosis_contract 时，把顶层 diagnosis_contract 指向 next_unhandled_diagnosis_contract。
推荐候选未进入人工留痕时，顶层 diagnosis_contract 仍保留 recommended_diagnosis_contract。
测试同步补充 recommended_diagnosis_contract、next_unhandled_diagnosis_contract、diagnosis_contract 三方一致性断言。

防错规则：
1. 推荐候选已有人工动作且存在 next_unhandled_candidate 时，右侧人工确认和顶层 diagnosis_contract 的对象选择必须一致。
2. 后续修复前，不要把该失败归因到广告搜索词聚合命名或文案改动。
3. 修复时应补充推荐候选、下一个未处理候选和手工预检对象三者一致性的测试。

验证方式：
python -m pytest tests\test_signal_triage_service.py::test_signal_triage_top_level_diagnosis_contract_tracks_recommended_candidate tests\test_signal_triage_service.py::test_signal_triage_points_to_next_unhandled_candidate_when_recommended_is_waiting -q
python -m pytest tests\test_signal_triage_service.py -q
python -m pytest tests\test_manual_actions.py tests\test_signal_detection.py -q
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-23：广告搜索词聚合摘要不能缺广告 ASIN 和投放词上下文

```text
问题：
“Parent ASIN 广告搜索词聚合复核”已经能按 Parent ASIN 作用域聚合广告搜索词，但摘要原先主要展示广告活动、广告组和搜索词表现行。
这仍然不够落地，因为运营判断广告搜索词表现时，需要知道这组搜索词关联了哪些广告 ASIN、是否跨多个广告组、是否有投放词证据，以及能不能归因到单个广告商品。

原因：
1. search_intent_summaries 原先只接收搜索词表现行，广告商品上下文没有传入聚合层。
2. ad_context 只统计 campaign / ad_group / search term row，弱化了 Parent ASIN -> 广告 ASIN -> 广告组 -> 投放词 / 搜索词 的业务路径。
3. evidence_gap 只判断投放词、ABA 和广告位缺口，没有提前提示缺广告 ASIN 覆盖或多广告 ASIN 归因边界。

解决：
1. search_intent_summaries 增加 context_rows，只读接收同一市场快照上下文。
2. /api/search-intents 的站点视图和商品作用域视图都传入 signal_rows 作为 context_rows。
3. ad_context 增加关联广告 ASIN 数、投放词 / 投放对象数和多广告 ASIN 归因边界。
4. evidence_gap 增加缺广告 ASIN 覆盖、同广告组多广告 ASIN 的边界提示。

防错规则：
1. 搜索词聚合不是语义资产看板，必须服务 Parent ASIN 下广告搜索词表现复核。
2. 搜索词聚合摘要至少要说明广告活动、广告组、广告 ASIN、投放词 / 投放对象、搜索词表现行和归因边界。
3. 同广告组多广告 ASIN 时，只能提示不能自动归因，不能把搜索词表现写成某个广告 ASIN 的确定结论。
4. 不能为了展示深度新增聚合上下文人工动作；人工动作仍必须落到具体 SearchTerm 稳定对象。

验证方式：
python -m pytest tests\test_signal_detection.py::test_search_intent_summary_exposes_review_terms_and_aba_matches tests\test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope -q
python -m pytest tests\test_signal_detection.py tests\test_signal_triage_service.py -q
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：搜索词聚合主语仍可能弱化“表现复核”

```text
问题：
用户再次明确“语义组复核”的作用应是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
如果页面主标题继续强调“聚合复核”，用户仍可能把它理解成语义组资产或聚合对象本身，而不是广告搜索词表现分析任务。

原因：
1. 之前已经避免了“语义组”主语，但“聚合复核”仍偏技术口径。
2. data_grain 写“搜索词表现行”，但没有直接强调这些行必须来自广告中实际产生表现的用户搜索词。
3. 用户真正关心的是 SearchTerm 表现，而不是聚合动作本身。

解决：
1. 左侧主标题改为“Parent ASIN 广告搜索词表现复核”。
2. 副标题、用途说明和 data_grain 明确“广告中实际产生表现的用户搜索词”。
3. search_intent 展示标签改为“搜索词表现聚合”，保留“广告搜索词聚合上下文”作为证据标签，不把它包装成动作对象。

防错规则：
1. 用户可见主标题应回答业务任务：分析广告搜索词表现，而不是管理语义组。
2. 搜索词表现复核只看当前 Parent ASIN / 诊断入口广告上下文中实际产生表现的 SearchTerm 行。
3. 聚合标签只能作为上下文和筛选维度，不能改变诊断入口，不能成为人工动作对象。

验证方式：
python -m pytest tests\test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope -q
npm run test:signal-ui
npm run test:workbench-structure
```

## 2026-06-23：SearchTerm 快照行 ID 不能作为长期复盘对象

```text
问题：
真实快照中部分长尾 SearchTerm 信号的 primary_object.object_id 是 gerpgo 快照行 ID，例如 gerpgo_market_1_20260616_120443:507938343。
后端人工动作 preflight 会把同一搜索词规范成 search_term:1:b01fay0yl0。
如果前端按钮门禁直接比较这两个 object_id，会误判预检目标和当前信号不一致。

原因：
1. 快照行 ID 是来源行追溯，不是长期复盘对象。
2. SearchTerm 的稳定对象应由 market_id + search_term 构成。
3. 前端此前在 manualActionExpectedTargetForSignal、ReviewTodo 匹配和 ReviewRecord 读回目标里优先使用 primary_object.object_id。

解决：
1. 前端 SearchTerm 稳定对象统一生成为 search_term:{market_id}:{search_term}。
2. 原始快照行 ID、label 和 search_term 只作为 alias 参与读回匹配。
3. 人工动作身份门禁用稳定 ID + alias 集合匹配，避免裸搜索词、稳定 ID 和快照行 ID 形态差异造成误阻断。

防错规则：
1. SearchTerm 的人工动作、ReviewTodo 和 ReviewRecord 对象必须使用稳定搜索词 ID。
2. 快照行 ID 只能作来源追溯，不得作为长期复盘对象。
3. 前端门禁和读回逻辑必须兼容历史裸搜索词，但最终写入以后端 preflight target 为准。

验证方式：
npm run test:review-ui
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：SearchTerm 诊断链不能让投放词越过广告组边界

```text
问题：
中间“广告搜索词表现复核链”和右侧人工确认预览原先按 Parent ASIN -> 广告 ASIN -> 投放词 -> 广告组 -> 同组投放商品 -> 广告位 -> ABA 展示。
这个顺序会让用户先看投放词，再补看广告组和同组广告 ASIN，容易把投放词当成可以脱离广告组容器直接处理的对象。

原因：
1. 早期为了补齐投放词证据，把 targetingEvidence 插在广告 ASIN 后面。
2. Amazon 广告架构里，广告组是投放容器，一个广告组可以投放多个商品。
3. SearchTerm 和投放词表现通常落在广告活动 / 广告组上下文，不能先跳过广告组边界做关键词动作判断。

解决：
1. 中间 SearchTerm 诊断链顺序改为 Parent ASIN -> 广告 ASIN -> 广告组合流判断 -> 同组投放商品表现 -> 投放词证据 -> 广告位边界 -> ABA 背景。
2. 右侧人工确认预览同步使用同一顺序。
3. 投放词证据说明补充“当前广告组投放上下文”，防止被误读成完整关键词库或可自动加词依据。
4. 前端结构测试、helper 测试和 runtime 测试锁定新顺序。

防错规则：
1. SearchTerm 详情必须先说明广告组容器和同组投放商品，再说明投放词。
2. 投放词只能作为当前广告组投放上下文证据，不能绕过人工确认变成自动加词、否词或调价动作。
3. 中间诊断链和右侧人工确认预览必须保持同一业务顺序，否则复盘证据会变成另一套读法。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-23：SearchTerm 预检保存快照不能和表现复核链脱节

```text
问题：
真实 B00K4W4AAA / beach essentials 的 manual-action preflight 已经能生成广告组合流判断和同组投放商品表现，但投放词证据、ABA 背景仍排在诊断合同判断、能证明、不能证明、人工下一步之后。
这会导致用户在中间区按广告架构读 SearchTerm 表现复核，保存到 ReviewTodo 后又按另一套合同字段顺序回看证据。

原因：
1. manual_action_preflight.py 只有一套通用 ACTIONABLE_EVIDENCE_BLOCK_ORDER。
2. 通用排序把 diagnosis_contract_* 放在 search_term_review_* 前面，适合通用合同说明，但不适合 SearchTerm 的运营排查链路。
3. 前端已经把“语义组复核”收口成 Parent ASIN 广告搜索词表现复核，但后端保存顺序还没有完全跟上。

解决：
1. 为 object_type=search_term 增加专用 SEARCH_TERM_ACTIONABLE_EVIDENCE_BLOCK_ORDER。
2. SearchTerm 预检保存顺序固定为：排查路径、AI 准入、广告组合流判断、同组投放商品表现、投放词证据、搜索词边界、广告位边界、ABA 背景，再进入诊断判断和反证。
3. 前端搜索词聚合筛选兼容“广告搜索词聚合上下文”和历史“语义组”标签，但人工动作仍必须落到具体 SearchTerm。
4. 后端 API 路由测试锁定 ReviewTodo 继承同一保存顺序。

防错规则：
1. SearchTerm 的 evidence_snapshot_preview、ManualAction.evidence_snapshot 和 ReviewTodo.evidence_snapshot 必须和中间广告搜索词表现复核链保持同一业务顺序。
2. 搜索词聚合上下文只能作为筛选维度和只读背景，不能成为人工动作对象。
3. 任何兼容“语义组”的代码都必须同时支持用户可见的“广告搜索词聚合上下文”，避免旧字段重新成为页面主语。
4. 不得把 SearchTerm 证据快照改成合同字段优先；合同说明应排在广告结构证据之后。

验证方式：
python -m pytest tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_snapshots_diagnosis_contract_gaps -q
python -m pytest tests\test_api_routes.py -q
python -m pytest tests\test_manual_action_preflight_cli.py -q
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-23：SearchTerm 复盘读回不能回退成语义组或裸指标

```text
问题：
用户再次确认“语义组复核”的作用应该是从 Parent ASIN 的角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果 ReviewTodo / ReviewRecord 读回仍先看投放词或只看搜索词指标，运营会在复盘时把广告搜索词聚合上下文误读成可处理对象。

原因：
1. 左侧表现复核和中间诊断链已经收口为 Parent ASIN 广告搜索词表现复核，但复盘读回是另一条 helper 链路。
2. 复盘待办原先只要求投放词证据、广告组合流、同组投放商品、ABA、证据缺口和动作边界，没有把搜索词边界与广告位边界纳入 SearchTerm 复核链。
3. ReviewRecord 保存前检查原先在 SearchTerm 场景下仍先回看投放词，再回看广告组合流和同组投放商品，读法和运营排查路径不一致。

解决：
1. SearchTerm ReviewTodo 的“搜索词复核链”补齐广告组合流判断、同组投放商品表现、投放词证据、搜索词边界、广告位边界、ABA 背景、证据缺口、需要补证和动作边界。
2. SearchTerm ReviewRecord 保存前检查顺序改为：广告组合流判断 -> 同组投放商品表现 -> 投放词证据 -> 搜索词边界 -> 广告位边界 -> ABA 背景 -> 证据缺口 -> 需要补证 -> 动作边界。
3. 测试样本锁定 evidence_snapshot 和 checklist 顺序，确保 7/14 天后复盘仍沿 Parent ASIN 广告搜索词表现链读回。

防错规则：
1. “语义组”只能作为搜索意图聚合维度，不能成为 ReviewTodo、ReviewRecord 或人工动作对象。
2. SearchTerm 复盘读回必须先回到广告组容器和同组投放商品，再看投放词、搜索词边界、广告位和 ABA。
3. 左侧聚合、中间诊断、右侧人工确认、ReviewTodo 和 ReviewRecord 必须使用同一条广告搜索词表现复核链。

验证方式：
npm run test:review-ui
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-23：规则反馈和已保存复盘记录不能使用 SearchTerm 筛选口径

```text
问题：
ReviewTodo / ReviewRecord 保存前读回已经按广告搜索词表现复核链收口，但已保存 ReviewRecord 状态文本和规则反馈样本池仍可能显示“SearchTerm 筛选”或只显示搜索词边界 / 广告位边界。
这会让运营误以为复盘沉淀的是一个筛选器或语义上下文，而不是具体 SearchTerm 人工复核证据链。

原因：
1. reviewRecordEvidenceSnapshotReadbackText 原先只按通用快照标签检查对象引用、排查路径、AI 准入、搜索词边界和广告位边界。
2. SearchTerm 场景下还需要回看广告组合流判断、同组投放商品表现、投放词证据和 ABA 背景。
3. 规则反馈 candidate group 的 search_intent 分组文案仍使用“SearchTerm 筛选”，偏技术口径，容易被误解成独立处理对象。

解决：
1. SearchTerm 已保存 ReviewRecord 的快照读回补齐广告组合流判断、同组投放商品表现、投放词证据、搜索词边界、广告位边界和 ABA 背景。
2. 前后端规则反馈样本上下文统一改为“广告搜索词表现复核”。
3. 前端旧“语义组人工动作”兼容文案直接映射为“广告搜索词聚合上下文人工动作”，不再保留 SearchTerm 筛选中间口径。

防错规则：
1. 规则反馈只进入解释层和人工复核优先级，不是广告处理对象。
2. SearchTerm 已保存复盘记录必须能回看完整广告搜索词表现复核链，不能只看搜索词边界和广告位边界。
3. 用户可见文案不得把 search_intent 说成 SearchTerm 筛选、语义组动作或可执行广告对象。

验证方式：
npm run test:review-ui
npm run test:signal-ui
npm run test:workbench-structure
python -m pytest tests\test_signal_triage_service.py::test_review_readiness_summarizes_saved_review_records_as_rule_feedback tests\test_signal_triage_service.py::test_review_readiness_groups_saved_review_record_snapshot_without_manual_action -q
```

## 2026-06-23：语义组复核不能被做成抽象语义资产

```text
问题：
用户明确指出，“语义组复核”的作用应该是从 Parent ASIN 的角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果页面或规则反馈继续展示“搜索词上下文 1 条 / 广告位上下文 0 条”这类裸统计，用户无法判断这些数据服务什么业务问题。

原因：
1. 早期为了兼容 search_intent / semantic_group 字段，部分 UI 和后端证据组沿用了技术口径。
2. review_feedback.evidence_groups 只展示指标和上下文条数，没有表达 Parent ASIN -> 广告组 -> 投放词 -> 搜索词 -> 广告位 -> ABA 的复核链。
3. 语义组如果被用户看成独立对象，容易误导为可以直接处理的广告对象。

解决：
1. SearchTerm 场景下的 evidence_groups 改为“Parent ASIN 广告搜索词表现复核”证据组。
2. 证据组必须展示当前诊断入口下广告用户搜索词表现行、广告组边界、投放词上下文、广告位边界、复盘证据链覆盖和归因边界。
3. 前端展示标题改为“广告搜索词复核证据”，不再用泛化“证据上下文”隐藏业务目的。

防错规则：
1. “语义组”只能作为广告搜索词聚合上下文和筛选维度，不能成为人工动作、ReviewTodo 或 ReviewRecord 对象。
2. 用户可见文案必须说明它服务于 Parent ASIN 广告搜索词表现复核。
3. 规则反馈样本必须能看出是否覆盖广告组、同组投放商品、投放词、搜索词边界、广告位和 ABA 背景。

验证方式：
python -m pytest tests\test_signal_triage_service.py::test_review_readiness_summarizes_saved_review_records_as_rule_feedback tests\test_signal_triage_service.py::test_review_readiness_groups_saved_review_record_snapshot_without_manual_action -q
npm run test:signal-ui
npm run build
```

## 2026-06-23：广告搜索词聚合卡片不能只展示搜索词裸指标

```text
问题：
左侧 Parent ASIN 广告搜索词表现复核卡片的 topTerms 原先只展示搜索词、订单、花费、ACOS 和 ABA 排名。
这会让用户看到的是搜索词榜单，而不是广告搜索词表现复核入口。

原因：
1. search_intent_top_terms 只聚合了搜索词指标和 ABA 背景，没有把逐词广告组和投放词带到 SearchIntentTopTerm。
2. 前端 buildSearchIntentReviewCards 只能输出裸指标，用户需要回头读 ad_context 才能拼出广告组和投放词关系。
3. Amazon 广告架构中，搜索词必须放回广告活动、广告组和投放词上下文里看，不能孤立判断。

解决：
1. SearchIntentTopTerm 新增 ad_group_names 和 targeting_texts。
2. search_intent_top_terms 从 ad_search_term_daily_metrics 行提取逐词广告组和投放词 / 投放对象。
3. 左侧 topTerms 展示“广告组 / 投放词”，缺失时明确显示待补齐。

防错规则：
1. Parent ASIN 广告搜索词表现复核卡片里的单条搜索词，必须能读到广告组和投放词上下文。
2. 缺广告组或投放词时只能显示证据缺口，不能假装可以判断扩量或止损。
3. 搜索词表现不能脱离广告组容器讨论，也不能自动归因到单个广告 ASIN。

验证方式：
python -m pytest tests\test_signal_detection.py::test_search_intent_summary_exposes_review_terms_and_aba_matches tests\test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope -q
npm run test:signal-ui
npm run build
```

## 2026-06-23：SearchTerm 复盘快照不能丢失逐投放上下文

```text
问题：
页面中间诊断区已经能展示 SearchTerm 的逐投放上下文，但右侧人工确认预检快照只保存了搜索词边界、投放词证据和广告组汇总。
如果运营加入 7/14 天复盘，后续读回时无法直接看到同一搜索词在不同广告活动、广告组和投放词下的表现差异。

原因：
1. evidence_snapshot_preview 原先只从 business_evidence_blocks、诊断合同和复盘链条生成快照。
2. recommended_evidence_drilldown.search_term_rows 已经有逐投放表现行，但没有写入人工动作快照。
3. 页面展示和复盘保存之间存在证据粒度落差，容易让复盘退回裸搜索词合计指标。

解决：
1. SearchTerm 预检快照新增“逐投放上下文”。
2. 该块只读取当前目标 SearchTerm 的 ad_search_term_daily_metrics 表现行。
3. 保存广告活动、广告组、投放词、点击、花费、订单、销售额、ACOS、CVR 和归因边界。

防错规则：
1. 用户从 Parent ASIN 广告搜索词表现复核进入具体 SearchTerm 后，人工动作和 ReviewTodo 快照必须保留逐投放上下文。
2. 逐投放上下文只能证明广告活动 / 广告组 / 投放词下的搜索词表现，不能证明单个广告 ASIN 归因。
3. 保存快照不能只保存合计指标；否则 7/14 天复盘无法判断当时业务判断是否完整。

验证方式：
python -m py_compile backend\app\services\manual_action_preflight.py
python -m pytest backend\tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot backend\tests\test_api_routes.py::test_beach_essentials_manual_action_post_write_readback_distinguishes_action_types backend\tests\test_manual_action_apply_cli.py::test_manual_action_apply_rewrites_after_voided_legacy_todo_with_isolated_roots -q
```

## 2026-06-23：旧 ReviewTodo 不能丢失 Parent ASIN 广告搜索词聚合上下文

```text
问题：
用户重新明确，“语义组复核”的作用应该是从 Parent ASIN 的角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
当前真实 beach essentials 复盘待办来自旧保存快照，evidence_snapshot 没有“语义组”，导致 review_context.search_intent_label 为 null。
这会让复盘列表退化成只显示 SearchTerm，丢失用户从 Parent ASIN 广告搜索词表现复核进入的聚合上下文。

原因：
1. 新版 manual_action_preflight 已经会保存“语义组”，但历史 manual_actions.jsonl 不会自动补齐。
2. manual_actions.review_context_for_manual_action 原先只读取 evidence_snapshot 的“语义组”标签。
3. 旧记录没有该标签时，系统没有用 SearchTerm 本身按现有规则补出“规则语义：海滩出行用品”。

解决：
1. ReviewTodo 读回时优先读取“语义组”，并兼容“广告搜索词聚合上下文”标签。
2. 对旧 SearchTerm 留痕，如果快照缺语义组，则按 resolved_search_intent_label 从 SearchTerm 推导语义组。
3. 同类搜索词聚合重复次数改为按读回后的语义组上下文统计，兼容新旧记录。

防错规则：
1. 语义组复核的业务口径是 Parent ASIN 广告上下文中的用户搜索词表现聚合。
2. 保存阶段和读回阶段都必须保留语义组上下文；旧记录缺标签时要用规则推导，但不得改写原始 evidence_snapshot。
3. 语义组只能用于复盘同类搜索词表现，不能替代顶部诊断入口，不能触发自动加词、否词、调价或暂停广告。

验证方式：
python -m py_compile backend\app\services\manual_actions.py
python -m pytest tests\test_manual_actions.py::test_review_todos_extract_search_intent_and_aba_context tests\test_manual_actions.py::test_review_todos_infer_search_intent_for_legacy_search_term_action tests\test_manual_actions.py::test_review_todos_extract_manual_action_path_and_review_metrics -q
读取 build_review_todos(market_id=1)：beach essentials 旧待办返回 search_intent_label=规则语义：海滩出行用品。
```

## 2026-06-23：ReviewRecord 不能只保存对象 ID 和快照条数

```text
问题：
ReviewTodo 已经能读回“广告搜索词聚合上下文”，但 ReviewRecord 原先没有 review_context 字段。
如果运营保存复盘记录后，页面和规则反馈候选只能看到 object_id、复盘结果和 evidence_snapshot 条数，仍然会丢失 Parent ASIN 广告搜索词聚合路径。

原因：
1. ReviewRecord 只保存 evidence_snapshot，没有结构化复盘上下文。
2. 前端 reviewRecordReadbackStatus 只检查快照标签，不展示“这条复盘属于哪个同类搜索词聚合”。
3. 规则反馈候选 buildRuleFeedbackCandidate 只拼复盘结果、备注和对象读回，没有拼复盘上下文。

解决：
1. ReviewRecord 新增 review_context，只作为读回辅助，不替代 evidence_snapshot。
2. 保存 ReviewRecord 时写入 review_context；加载旧 ReviewRecord 时按 evidence_snapshot / SearchTerm 稳定文本推导，不改写历史文件。
3. 前端保存后读回状态和规则反馈候选都展示“复盘上下文：广告搜索词聚合上下文”。

防错规则：
1. Parent ASIN 广告搜索词聚合上下文必须贯穿 ReviewTodo、ReviewRecord 和规则反馈候选。
2. ReviewRecord.review_context 是读回上下文，不是新的广告事实，不得用于自动改规则或执行广告动作。
3. evidence_snapshot 仍是点击时证据快照，不能为了补上下文而伪造或改写历史快照。

验证方式：
python -m py_compile backend\app\models\manual_actions.py backend\app\services\manual_actions.py
python -m pytest tests\test_manual_actions.py::test_review_todos_infer_search_intent_for_legacy_search_term_action tests\test_manual_actions.py::test_review_record_persists_ready_effect_and_can_read_latest -q
npm run test:review-ui
```

## 2026-06-23：语义组复核不能被误解为诊断入口或广告动作对象

```text
问题：
用户明确指出，“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果把语义组做成独立诊断入口、商品归因入口或人工动作对象，会偏离广告 AI 信号工作台的业务本质。

原因：
1. Parent ASIN 是经营商品入口，负责限定当前分析上下文。
2. 语义组只是当前 Parent ASIN 广告上下文下的广告用户搜索词聚合层。
3. 用户真正需要诊断和处理的是具体 SearchTerm 及其广告组、投放词、广告位背景，而不是对语义组本身执行广告动作。

解决：
1. 运行态验证点击语义组后顶部诊断入口保持 parent_asin:B00K4W4AAA 不变。
2. 语义组点击只改变同类 SearchTerm 队列筛选，并选中具体 SearchTerm beach essentials。
3. 页面文案继续明确“用途、数据口径、能证明、不能证明、人工下一步”。

防错规则：
1. 语义组复核必须绑定当前 Parent ASIN 上下文，不能替代顶部诊断入口。
2. 语义组只能聚合广告中实际产生表现的用户搜索词，不能代表 Parent ASIN 全量自然搜索或全部子 ASIN。
3. 语义组只能引导用户打开具体 SearchTerm 诊断，不能生成自动加词、否词、调价或暂停动作。

验证方式：
浏览器验证 http://localhost:5175/：点击“规则语义：海滩出行用品”后，诊断入口仍为 parent_asin:B00K4W4AAA，页面显示语义组筛选态并选中 beach essentials SearchTerm。
```

## 2026-06-23：SearchTerm 复盘快照不能丢失语义组来源

```text
问题：
beach essentials 的人工确认预检快照原先保存了搜索词、Parent ASIN、广告 ASIN、逐投放上下文、广告位背景和 ABA 背景，但没有保存“语义组”。
如果运营从“规则语义：海滩出行用品”点击进入 beach essentials 并加入 7/14 天复盘，后续复盘只能看到单个 SearchTerm，无法回看当时的广告搜索词聚合入口。

原因：
1. raw ad_search_term_daily_metrics 行里的 intent_label 可能是“未分组搜索词”。
2. 页面展示的“规则语义：海滩出行用品”来自 resolved_search_intent_label 的规则解析。
3. signal_triage 的搜索词 drilldown row summary 原先没有把解析后的 intent_label 带入人工预检。
4. manual_action_preflight 原先没有把语义组保存成 evidence_snapshot 标签，导致 ReviewTodo 的语义组上下文统计可能缺失。

解决：
1. _search_term_row_summary 复用 resolved_search_intent_label(row)，输出与页面一致的 intent_label。
2. SearchTerm evidence_snapshot_preview 新增“语义组”块，保存“规则语义：海滩出行用品”。
3. SearchTerm 必备复盘证据标签新增“语义组”，避免后续完整性校验忽略聚合入口。

防错规则：
1. 用户从 Parent ASIN 广告搜索词表现复核进入具体 SearchTerm 后，人工动作和 ReviewTodo 快照必须保存语义组来源。
2. 语义组只能作为复盘同类搜索词表现和重复上下文的证据，不能替代顶部诊断入口。
3. 语义组不能变成自动加词、否词、调价或暂停广告的依据。

验证方式：
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id "beach essentials" --expected-object-type search_term --action-type add_to_review --top 5 --compact
返回 evidence_snapshot_preview.item_count=29，并包含“语义组=规则语义：海滩出行用品”。
```

## 2026-06-23：活动级广告位背景不能替代广告组级归因

```text
问题：
SearchTerm 诊断合同已经说明有 6 条同广告活动广告位背景，但右侧人工确认快照原先只保存“广告位边界”和“广告位证据缺口”。
用户 7/14 天复盘时无法看到这些活动级广告位背景分别是哪几个广告位、花费和订单如何，也容易把“有广告位背景”误解成“能判断广告位影响”。

原因：
1. 当前 ad_placement_daily_metrics 快照行只有 campaign_id / campaign_name，没有 ad_group_id。
2. SearchTerm 表现行能落到广告组和投放词，但广告位行只能按同 campaign 匹配。
3. 如果只保存广告位计数，复盘时无法区分“活动级背景”和“广告组级归因证据”。

解决：
1. SearchTerm 预检快照新增“广告位活动级背景”。
2. 该块按当前 SearchTerm 所在 campaign 匹配 ad_placement_daily_metrics。
3. 快照 detail 明确这些行只有 campaign_id，只能作为同广告活动背景，不能替代广告组级广告位归因。

防错规则：
1. 只有 campaign_id 的广告位数据必须标记为活动级背景。
2. 没有 ad_group_id 或 SearchTerm 直连广告位证据时，不得判断广告位造成该搜索词或广告组表现差异。
3. 复盘快照可以保存活动级广告位背景，但必须同时保存“不能替代广告组级归因”的边界。

验证方式：
python -m py_compile backend\app\services\manual_action_preflight.py
python -m pytest backend\tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot backend\tests\test_api_routes.py::test_beach_essentials_manual_action_post_write_readback_distinguishes_action_types backend\tests\test_manual_action_apply_cli.py::test_manual_action_apply_rewrites_after_voided_legacy_todo_with_isolated_roots -q
```

## 2026-06-23：中间诊断区不能把语义组呈现成独立对象

```text
问题：
即使左侧板块已经叫“Parent ASIN 广告搜索词表现复核”，用户点击聚合卡片后，中间承接条如果只写“当前筛选 / 二级筛选”，仍容易把语义组理解成一个独立诊断对象。

原因：
1. 语义组这个词天然像一个对象名。
2. 当前业务里它只是当前 Parent ASIN 广告搜索词表现行的分析分组。
3. 用户真正要分析的是具体 SearchTerm 在广告组、投放词、广告位背景下的表现。

解决：
1. 中间承接条标题改为“Parent ASIN 广告搜索词聚合承接”。
2. 标签名改为“聚合标签”，并明确来源是当前 Parent ASIN 关联广告中实际产生表现的用户搜索词行。
3. 边界说明明确聚合标签不是经营商品、广告组或人工动作对象，人工动作仍落到 SearchTerm 稳定对象。

防错规则：
1. 页面里出现语义组、搜索意图或聚合标签时，必须同步说明它来自 Parent ASIN 广告 SearchTerm 表现聚合。
2. 聚合标签只能缩小同类 SearchTerm 复核范围，不能切换顶部诊断入口。
3. 聚合标签不能成为自动加词、否词、调价或暂停广告的动作对象。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：ReviewRecord 读回不能只显示抽象上下文

```text
问题：
ReviewRecord 虽然已经能读回 review_context，但如果只显示“广告搜索词聚合上下文”和“搜索词”，用户仍需要自己推理它和 Parent ASIN、具体 SearchTerm、复盘动作之间的关系。

原因：
1. 复盘阶段用户关注的是处理结果是否有效，以及同类搜索词规则口径是否需要人工复核。
2. 聚合标签只是 Parent ASIN 广告搜索词表现背景，不能成为复盘对象。
3. SearchTerm 才是人工处理和复盘对象，规则反馈也只能人工核对，不能自动改规则或执行广告。

解决：
1. ReviewRecord / ManualAction 读回文案统一展示“Parent ASIN 广告搜索词聚合上下文”。
2. 搜索词读回统一展示为“具体 SearchTerm”。
3. 当同时存在聚合上下文和 SearchTerm 时，追加“人工复盘下一步：按同类广告搜索词表现核对规则口径，不把聚合标签当作广告动作对象”。

防错规则：
1. 复盘读回必须同时保留聚合背景和具体 SearchTerm，不只显示对象 ID。
2. 规则反馈候选只能提示人工复核规则口径，不能表达为自动改规则。
3. 聚合标签永远不能作为自动加词、否词、调价或暂停广告的对象。

验证方式：
npm run test:review-ui
npm run test:signal-ui
npm run build
```

## 2026-06-23：前端不能把后端 SearchTerm 诊断合同压扁成 4 个基础指标

```text
问题：
后端 beach essentials 诊断合同已经包含 CVR、投放上下文、投放词证据、广告位证据、ABA 市场热度和广告组承接边界，但前端关键指标区原先只展示花费、订单、销售额、ACOS。
用户看到的仍像浅层指标卡，难以理解为什么这条 SearchTerm 支撑扩量复核。

原因：
1. buildSignalMetricDecisionItems 固定从 signal.evidence.metrics 取 4 个基础指标。
2. 后端诊断合同里的 SearchTerm 扩展指标已经在数据里，但没有被前端关键指标区承接。
3. 这会造成“后端有深度，前端第一眼仍像浅分析”的割裂。

解决：
1. 前端保留基础 4 指标。
2. 当存在 search_term_opportunity 合同时，额外读取 CVR、投放上下文、投放词证据、广告位证据、ABA市场热度、广告组承接边界。
3. 扩展指标继续展示服务判断、能证明、不能证明和人工下一步，不生成自动广告动作。

防错规则：
1. 后端 diagnosis_contract 已经提供业务判断指标时，前端不能只展示裸 metrics。
2. SearchTerm 机会判断必须同时展示投放上下文、投放词、广告位层级和 ABA 背景，不能只看 ACOS。
3. 扩展指标只能服务人工复核，不能包装成自动加词、否词、调价或暂停广告。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-23：逐投放上下文不能只是明细表

```text
问题：
beach essentials 的逐投放上下文能列出精准组和扩展组，但如果每行只写“有订单承接”，用户仍需要自己判断先看哪个广告组。

原因：
1. SearchTerm 表现可能跨多个广告活动、广告组和投放词。
2. 运营要做的是排查顺序判断，而不是只读明细行。
3. 如果没有复核优先级，同一 SearchTerm 下的广告组差异仍不够落地。

解决：
1. 逐投放上下文行新增 reviewPriority 和 reviewReason。
2. 排序后第一条有订单行标记为“优先复核广告组”。
3. 其他有订单行标记为“对照复核广告组”，无订单有花费行标记为“止损复核候选”。

防错规则：
1. 逐投放上下文必须说明人工复核顺序，不能只展示明细指标。
2. 复核优先级只能代表人工核对顺序，不能代表自动加词、否词、调价或暂停广告。
3. 即使某个广告组排第一，也不能把 SearchTerm 自动归因到单个广告 ASIN。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-24：语义组复核不能只靠长文说明对象边界

```text
问题：
用户明确指出，“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果中间承接条只用一段长文说明“搜索意图分组不是动作对象”，用户仍需要自己推理当前 Parent ASIN、聚合分组和具体 SearchTerm 的关系。

原因：
1. 语义组天然像一个独立对象名称，容易被误读成诊断入口或人工动作对象。
2. 点击语义组后中间会选中具体 SearchTerm，如果页面不显式展示入口路径，用户会误以为经营对象发生了变化。
3. 长文说明不如结构化路径稳定，后续前端调整时也更容易回退成二级筛选口径。

解决：
1. SearchIntentFocusContext 增加 pathItems。
2. 点击语义组后的承接条固定展示“经营诊断入口 / 搜索词聚合口径 / 当前诊断对象”。
3. 关系说明明确为“从 Parent ASIN 视角聚合广告中实际产生表现的用户搜索词行”。

防错规则：
1. 页面出现语义组、搜索意图分组或聚合口径时，必须同时绑定 Parent ASIN 经营入口和具体 SearchTerm 诊断对象。
2. 语义组只能作为广告搜索词表现聚合和筛选维度，不能成为经营入口、广告组入口或人工动作对象。
3. 用户可见层不能只靠长文解释对象边界，关键路径必须结构化展示。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-24：无可写预检对象不能显示成后端读取中

```text
问题：
当当前 SearchTerm 已有人工留痕，或当前信号没有后端可写预检对象时，右侧人工确认区仍可能显示“后端预检：正在读取”。
这会让用户误以为后端接口卡住，而不是对象已经留痕或当前对象不适合写入。

原因：
manualActionPreflightStatusText(null) 的默认文案是读取中。
前端调用时没有先判断 selectedBackendManualActionPreview 是否存在，也没有把 latestManualAction 作为空预检状态的解释依据。

解决：
1. SignalTriageWorkbench 在没有 selectedBackendManualActionPreview 时，不再调用读取中文案。
2. 如果 latestManualAction 存在，显示“当前 SearchTerm 已有人工留痕；后端预检不再生成重复写入对象”。
3. 如果没有可写对象，显示“当前信号没有后端可写预检对象；不会自动写入人工动作”。
4. 同步把承接路径标签改为“搜索词聚合口径”，避免用户误解为语义对象复核。

防错规则：
1. null preflight 只有在存在后端可预检对象时才表示读取中。
2. 已留痕、重复阻断、无可写对象必须显示业务事实，不能伪装成 loading。
3. 搜索意图分组只聚合 Parent ASIN 下广告 SearchTerm 表现，人工动作对象必须落到具体 SearchTerm。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run build
Playwright + 本机 Chrome 运行态检查 http://127.0.0.1:5175/
```

## 2026-06-24：右侧复盘读回不能让稳定 ID 抢占 SearchTerm 主语

```text
问题：
右侧人工动作目标切换、身份门禁、复盘待办和历史待办治理里，SearchTerm 对象可能优先显示为 search_term:1:beach essentials。
这对系统门禁是稳定对象，但对运营用户来说不像业务对象，容易削弱“我正在复盘 beach essentials 这个搜索词”的判断。

原因：
1. 读回文案直接拼 object_type / object_id。
2. SearchTerm 的 object_id 已升级为稳定 ID，适合校验，不适合作为第一主语。
3. object_label 已经有具体搜索词文本，但展示层没有统一优先使用。

解决：
1. reviewUi.ts 新增 SearchTerm 身份展示 helper。
2. signalUi.ts 同步为人工动作目标切换和历史待办治理 sample 使用同一展示规则。
3. 用户可见层显示“具体 SearchTerm：beach essentials；稳定对象：search_term / search_term:1:beach essentials”。

防错规则：
1. SearchTerm 场景下，具体搜索词文本必须先于稳定 object_id 展示。
2. 稳定 object_id 必须保留为校验字段，不能删除或隐藏到无法核对。
3. ManualAction、ReviewTodo、ReviewRecord、历史待办治理和作废计划必须使用同一对象展示口径。

验证方式：
npm run test:signal-ui
npm run test:review-ui
npm run test:workbench-structure
npm run build
Playwright + 本机 Chrome 运行态检查 http://127.0.0.1:5175/
```

## 2026-06-24：历史待办治理样本不能继续渲染成一整段长文本

```text
问题：
历史待办治理 sampleItems 原样渲染成一个 li，里面同时包含对象、复盘窗口、动作 ID、证据缺口、预检状态、作废计划、授权码、dry-run 命令和禁止动作边界。
信息虽然完整，但运营很难从右侧面板第一眼读出“缺什么证据”和“下一步人工做什么”。

原因：
1. 后端 summary 为了保留审计信息，把多个治理事实串成一条文本。
2. 前端只用 <li>{item}</li> 渲染，没有按业务判断结构拆分。
3. 历史待办治理属于复盘闭环关键步骤，不能让用户在长文本里找证据缺口和 dry-run 边界。

解决：
1. SignalTriageWorkbench 新增 ReviewRepairSampleItem。
2. 样本按“对象主行 / meta / 证据缺口与预检 / 作废计划 / 授权与命令”展示。
3. dry-run 命令和授权码用 code 块换行展示，保留命令但不暗示自动执行。

防错规则：
1. 历史待办治理样本不得再裸渲染为一整段文本。
2. 作废计划和命令只能作为人工核对信息展示，不得自动执行。
3. 缺证据的旧 ReviewTodo 仍不能补写历史 evidence_snapshot，只能作废旧待办后重新人工留痕。

验证方式：
npm run test:workbench-structure
npm run build
Playwright + 本机 Chrome 运行态检查 http://127.0.0.1:5175/
```

## 2026-06-24：不能把“语义组复核”理解成语义资产管理入口

```text
问题：
“语义组复核”这个旧称容易让用户误解为系统在复核语义资产、语义标签或可执行对象，而不是从 Parent ASIN 角度聚合广告中的用户搜索词表现。

原因：
1. 后端历史字段仍需要兼容 intent_label / 语义组。
2. 用户真正的业务任务是先看 Parent ASIN，再看该 Parent ASIN 相关广告中的用户搜索词表现。
3. 如果前端继续把它称为语义组，用户会误以为点击后会改变诊断入口或生成语义组人工动作。

解决：
1. 用户可见主标题保持“Parent ASIN 广告搜索词表现复核”。
2. 数据口径明确为当前 Parent ASIN 相关广告上下文中的 ad_search_term_daily_metrics 用户搜索词表现行。
3. 新增结构测试，禁止页面源码重新出现“语义组复核”旧称。

防错规则：
1. 搜索意图分组只能作为广告 SearchTerm 聚合上下文，不是经营商品、广告组或人工动作对象。
2. 点击聚合组只能筛选当前诊断入口下的 SearchTerm 信号，不得改变顶部 Parent ASIN / ASIN 诊断入口。
3. 后续若必须展示历史“语义组”字段，用户可见层必须翻译为“搜索意图分组”或“广告搜索词表现复核背景”。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
python 只读请求 /api/search-intents?market_id=1&product_scope_id=parent_asin:B00K4W4AAA
```

## 2026-06-24：搜索意图聚合信号不能包装成广告操作候选

```text
问题：
后端 search_intent 聚合信号仍使用“语义标签”作为证据标签，并在 suggested_action.description 中写“进入人工否词或降竞价候选”。
这会让用户把搜索意图分组误读成可执行广告对象，也会削弱“AI 只做诊断、证据、人工复核和复盘”的产品边界。

原因：
1. 早期实现把搜索意图聚合当作语义分桶异常，语言更接近语义资产和广告操作候选。
2. 前端主标题已经改成 Parent ASIN 广告搜索词表现复核，但后端聚合信号文案没有同步收口。
3. 搜索意图分组可以辅助定位问题，但不能直接变成否词、降竞价或加词建议。

解决：
1. search_intent 信号证据标签改为“搜索意图分组”。
2. suggested_action 改为“人工复核这组广告搜索词表现”。
3. 建议描述只保留人工核对 Parent ASIN 商品、广告组策略、搜索词是否偏泛，以及记录观察 / 复盘依据。
4. 新增后端测试，禁止该信号再出现“语义标签 / 否词 / 降竞价 / 候选”旧语言。

防错规则：
1. 搜索意图分组只是广告 SearchTerm 聚合上下文，不是人工动作对象。
2. 聚合信号只能引导打开具体 SearchTerm 或记录复盘观察，不能包装成广告后台动作候选。
3. 后端信号文案必须和前端“Parent ASIN 广告搜索词表现复核”用户语言保持一致。

验证方式：
python -m pytest tests/test_signal_detection.py
python -m pytest tests/test_signal_triage_service.py -q
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run test:workbench-structure
npm run build
python -m compileall app
rg 旧词扫描 backend/app frontend/src
```

## 2026-06-24：SearchTerm 详情不能只有证据清单，必须有复核路径

```text
问题：
广告搜索词表现复核链虽然已经展示 Parent ASIN、广告 ASIN、广告组、同组投放商品、投放词、广告位和 ABA 背景，但如果没有总括路径，用户容易把它读成一组证据块，而不是一条运营排查路线。

原因：
1. 组件按顺序渲染了证据，但没有显式说明“先看什么，再看什么，最后进入什么人工动作”。
2. 用户实际操作广告时会先看 Parent ASIN 销售盘，再下钻有广告数据的广告 ASIN、广告组、投放商品、投放词、广告位和具体 SearchTerm。
3. 如果没有路径提示，SearchTerm、广告组、广告 ASIN 容易被误读为同一层对象。

解决：
1. SearchTermOpportunityReviewChain 增加 reviewPath。
2. 中间诊断区在对象粒度后展示复核路径。
3. 测试锁定复核路径和证据块展示顺序。

防错规则：
1. SearchTerm 详情必须先说明复核路径，再展示分层证据。
2. 复核路径必须从 Parent ASIN 销售盘开始，经过有广告数据的广告 ASIN、广告组容器、同组投放商品、投放词、广告位边界和具体 SearchTerm，最后落到人工确认和 7/14 天复盘。
3. 不得把广告组、投放词、广告位或搜索意图分组包装成人工动作对象。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
python 只读请求 /api/signal-triage?market_id=1&top=5&product_scope_id=parent_asin:B00K4W4AAA
```

## 2026-06-24：右侧人工确认不能缺 SearchTerm 复核路径

```text
问题：
用户再次明确“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
中间诊断区已经有 SearchTerm 复核路径，但右侧人工确认区如果只展示分层证据，用户点击人工动作前仍可能把搜索意图分组误读成处理对象。

原因：
1. buildManualConfirmationEvidenceItems 已复用 SearchTermOpportunityReviewChain 的分层证据，但没有把 reviewPath 放入右侧证据项。
2. 左侧聚合、中间诊断、右侧人工确认如果路径表达不一致，会让用户误以为人工动作绑定的是语义组。
3. 运营实际动作前需要确认 Parent ASIN、广告 ASIN、广告组、同组投放商品、投放词、广告位和具体 SearchTerm 的完整链路。

解决：
1. 右侧人工确认证据新增“复核路径”。
2. 测试锁定右侧证据顺序为业务判断、人工下一步、复核路径，再进入 Parent ASIN、广告 ASIN、广告组、投放词、广告位、ABA、证据缺口和动作边界。
3. 运行态测试确认真实候选进入右侧人工确认时能读出 Parent ASIN 销售盘和具体 SearchTerm。

防错规则：
1. 搜索意图分组只能作为当前 Parent ASIN 广告 SearchTerm 的聚合维度，不能成为人工动作对象。
2. 右侧人工动作前必须能回看同一条 Parent ASIN -> 广告 ASIN -> 广告组 -> 投放词 -> 广告位 -> SearchTerm -> 复盘路径。
3. 左侧聚合、中间诊断、右侧人工确认不得使用不同对象口径描述同一条 SearchTerm 信号。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-24：广告商品和广告位信号不能只有对象边界，必须有复核路径

```text
问题：
SearchTerm 已补齐复核路径，但 advertised_product 和 placement 仍只有对象边界说明。
如果用户点到广告商品或广告位信号时只看到指标、对象和一句边界，仍不清楚应按什么顺序从 Parent ASIN / 当前经营入口下钻到人工确认和 7/14 天复盘。

原因：
1. 早期对象上下文只负责说明“能不能归因”，没有说明“怎么复核”。
2. 广告商品和广告位都是容易跨层误读的对象：广告商品容易被当成经营商品，广告位容易被当成 ASIN 归因证据。
3. 右侧人工确认区如果不带对象路径，用户点击人工动作前仍可能只基于裸指标做判断。

解决：
1. advertised_product 对象上下文新增复核路径：Parent ASIN 销售盘 -> 当前广告 ASIN -> 广告组容器 -> 同组投放商品表现 -> 投放词 / 搜索词上下文 -> 广告位边界 -> 人工确认 -> 7/14 天复盘。
2. placement 对象上下文新增复核路径：当前经营入口 -> 广告活动 / 广告组 -> 广告位表现 -> 广告 ASIN 和搜索词承接核对 -> 证据缺口 -> 人工确认 -> 7/14 天复盘。
3. 右侧人工确认证据在广告商品 / 广告位信号下同步展示“复核路径”。

防错规则：
1. 广告商品信号必须说明广告 ASIN 是投放商品，不等同于经营商品。
2. 广告位信号必须说明广告位只代表流量位置或广告活动上下文，不能自动归因到 Parent ASIN 或单个 ASIN。
3. 人工确认前必须能看到对象复核路径；没有路径时不能只靠裸指标包装成可处理建议。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-24：广告组信号不能只有容器边界，必须有复核路径

```text
问题：
ad_group 信号已经能说明“广告组是投放容器”，但没有展示从 Parent ASIN 到广告组、同组投放商品、投放词 / 搜索词 / 广告位证据、主推款策略边界、人工确认和 7/14 天复盘的完整路径。

原因：
1. 早期对象上下文只强调广告组不能当商品，没有说明运营应该按什么顺序复核。
2. 多商品广告组下，用户很容易把搜索词或广告位表现误归因到单个 ASIN。
3. 缺少主推款策略边界时，系统只能提示人工确认结构待确认，不能直接包装成拆广告组或自动动作。

解决：
1. ad_group 对象上下文新增复核路径：Parent ASIN 销售盘 -> 广告 ASIN 覆盖 -> 当前广告组容器 -> 同组投放商品表现 -> 投放词 / 搜索词 / 广告位证据 -> 主推款策略边界 -> 人工确认 -> 7/14 天复盘。
2. 右侧人工确认区在 ad_group 信号下同步展示“复核路径”。
3. 前端测试锁定“当前广告组容器”“同组投放商品表现”“主推款策略边界”和“不能自动拆广告组”。

防错规则：
1. 广告组信号必须先说明广告组是投放容器，不是商品。
2. 多商品广告组必须先看同组广告商品表现和主推款策略边界，不能把搜索词或广告位自动归因到单个 ASIN。
3. 缺少主推款策略资料时，只能进入人工复核或复盘观察，不能自动拆广告组或输出自动广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：“语义组复核”容易被误读成独立对象，必须回到 Parent ASIN 广告搜索词表现口径

```text
问题：
如果页面或讨论中继续使用“语义组复核”作为主概念，用户容易以为这里是在维护语义资产，或以为语义组本身可以成为人工动作对象。

原因：
1. 语义 / 搜索意图只是搜索词表现聚合维度，不是广告架构里的可操作对象。
2. 用户真实操作路径是先看 Parent ASIN，再看该 Parent ASIN 下的广告数据、广告组、投放商品、投放词和搜索词。
3. 如果不强调 Parent ASIN 入口和广告搜索词表现行口径，页面会退回“指标堆叠”，不能支撑具体广告诊断。

解决：
1. 产品定义统一为“Parent ASIN 广告搜索词表现复核”。
2. 数据口径限定为当前 Parent ASIN 关联广告上下文中的 ad_search_term_daily_metrics 用户搜索词表现行，按搜索意图 / 语义规则聚合。
3. 点击分组只筛选当前入口下同类 SearchTerm 信号，并选中具体 SearchTerm 进入诊断，不改变顶部诊断入口，不生成语义组人工动作。

防错规则：
1. 页面主标题不要退回“语义组复核”；可以保留“搜索意图分组”作为字段解释，但不能当作业务主对象。
2. 搜索词表现分组必须说明能证明什么、不能证明什么和人工下一步。
3. ABA 只能作为站点级背景，不能把 ABA 或语义组当作店铺、商品或广告组归因。

验证方式：
python -c 调用 build_search_intent_summaries(selected_market_id=1, product_scope_id='parent_asin:B00K4W4AAA')
backend/tests/test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope
frontend/tests/signalUi.test.ts 中 buildSearchIntentReviewCards / buildSearchIntentFocusContext 相关断言
```

## 2026-06-24：旧搜索词聚合标签只能兼容识别，不能作为用户可见主口径

```text
问题：
系统为了兼容历史人工动作和旧证据快照，仍需要识别“语义组”“Parent ASIN 搜索词表现聚合”“广告搜索词聚合上下文”等旧标签。
如果这些旧标签直接出现在页面对象上下文或证据标签里，用户仍可能误以为这是一个独立语义对象，而不是 Parent ASIN 下广告 SearchTerm 表现复核。

原因：
1. 历史记录和旧证据快照需要 alias 兼容，不能简单删除旧标签。
2. 兼容识别层和用户可见展示层原先没有完全分开。
3. “广告搜索词聚合上下文”这类说法仍偏技术描述，不如“Parent ASIN 广告搜索词表现复核”贴近用户操作路径。

解决：
1. 前端 evidenceFactDisplayLabel 和 manualActionEvidenceDisplayLabel 将旧标签统一显示为“搜索意图分组”。
2. search_intent 对象上下文显示“Parent ASIN 广告搜索词表现复核”，不再展示“广告搜索词聚合上下文用于...”。
3. filterSignalsBySearchIntent 继续兼容旧标签，保证历史记录能筛选和读回。
4. 测试锁定旧标签兼容识别和新显示口径。

防错规则：
1. 旧标签可以出现在 alias 列表和测试样本里，但不能作为页面主标题或对象上下文标题。
2. 用户可见主概念统一使用“Parent ASIN 广告搜索词表现复核”。
3. 字段层需要说明分组时使用“搜索意图分组”，不要退回“语义组复核”。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:review-ui
npm run build
rg 巡检“语义组复核 / 聚焦语义组 / 当前语义组留痕 / 广告搜索词聚合上下文用于”
```

## 2026-06-24：搜索词聚合不能停留在语义或意图视角，用户可见口径必须是搜索词表现分组

```text
问题：
用户明确指出“语义组复核”的作用应该是从 Parent ASIN 的角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果页面继续显示“搜索意图分组”作为主口径，仍会让用户把它理解成语义资产或聚类对象，而不是广告 SearchTerm 表现分析入口。

原因：
1. search_intent 是内部分析类型，不等于 Amazon 广告架构里的业务操作对象。
2. “搜索意图分组”虽然比“语义组”更准确，但仍偏技术聚类视角，没有直接表达广告表现分析目的。
3. 用户真实路径是 Parent ASIN -> 广告数据 -> 广告组 / 投放商品 / 投放词 / 搜索词 / 广告位 -> 判断问题 -> 人工记录和复盘。

解决：
1. 用户可见文案统一改为“搜索词表现分组”。
2. 后端输出给页面的证据标签、解释文案和 preflight evidence_snapshot 同步使用“搜索词表现分组”。
3. “语义组 / 搜索意图分组 / Parent ASIN 搜索词表现聚合 / 广告搜索词聚合上下文”只作为兼容识别别名保留。
4. 前端显示层继续把旧标签归一为“搜索词表现分组”。

防错规则：
1. 内部聚合方法不能成为用户主对象。
2. 搜索词聚合板块必须围绕 Parent ASIN 下广告 SearchTerm 表现，说明数据口径、能证明什么、不能证明什么和人工下一步。
3. 人工动作对象仍是具体 SearchTerm 或后端预检稳定对象，不能是搜索词表现分组本身。
4. 后续出现“语义组复核 / 搜索意图分组”作为主标题或人工动作对象时，必须改回搜索词表现分组或 Parent ASIN 广告搜索词表现复核。

验证方式：
npm run test:signal-ui
npm run test:review-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
python -m pytest tests\test_signal_detection.py tests\test_signal_triage_service.py tests\test_manual_actions.py tests\test_api_routes.py -q
python -m compileall app
```

## 2026-06-24：搜索词表现分组不能只改前端文案，后端合同也必须说明真实数据口径

```text
问题：
如果前端已经显示“搜索词表现分组”，但后端 SearchIntentSummary 的 data_grain / proves 仍停留在泛化“当前广告上下文”，用户还是无法判断这组指标到底来自 Parent ASIN、站点全量、ABA，还是纯语义聚合。

原因：
1. 前端文案只能降低误读，不能替代 API 内容合同。
2. 搜索词表现分组的业务价值来自当前 Parent ASIN 关联广告上下文中的真实 ad_search_term_daily_metrics 行。
3. 如果后端不说明数据表、入口边界和不能证明的内容，页面仍可能退回“指标填充”。

解决：
1. Parent ASIN search-intents 的 data_grain 明确为当前 Parent ASIN 关联广告上下文中的 ad_search_term_daily_metrics 用户搜索词表现行，按搜索词表现分组聚合。
2. search_intent_summaries 按 data_grain 生成 proves / does_not_prove。
3. 单元测试锁定 Parent ASIN 数据口径、能证明的事实和不能证明的边界。

防错规则：
1. 搜索词表现分组的 API 返回必须说明数据来源和对象边界。
2. 只改页面标题不算完成口径修正。
3. Parent ASIN 场景下不能返回泛化“当前广告上下文”来代替真实入口边界。

验证方式：
python -m pytest tests\test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope -q
python -m pytest tests\test_signal_triage_service.py -q
npm run test:signal-ui-runtime
```

## 2026-06-24：广告组诊断不能只写容器边界，必须写同组商品表现和主推策略边界

```text
问题：
广告组诊断如果只说“广告组是投放容器”，但不展示同组广告 ASIN 的花费、订单和主推策略边界，用户仍然无法判断这个广告组到底哪里需要人工复核。

原因：
1. 广告组是容器，不是商品；容器边界只能防误读，不能支撑运营判断。
2. 用户判断广告组问题时需要先看同组投放商品表现，再判断消耗集中是否来自主推款策略。
3. 如果缺主推款策略资料，系统只能提示人工确认，不能自动拆广告组或包装成广告动作。

解决：
1. ad_group_boundary diagnosis_contract 新增“同组投放商品表现”，使用既有 advertised_product_performance。
2. ad_group_boundary diagnosis_contract 新增“主推策略边界”，说明策略备注候选、多商品广告组和禁止自动拆组。
3. current_judgement 同步读出同组商品表现和主推策略边界。

防错规则：
1. 广告组诊断必须同时出现容器边界、同组投放商品表现、主推策略边界。
2. 多商品广告组下不能把搜索词或广告位自动归因到单个广告 ASIN。
3. 缺主推策略资料时只能进入人工核对或复盘观察，不能输出自动拆广告组、自动调价、自动否词或自动加词。

验证方式：
python -m pytest tests\test_signal_triage_service.py::test_signal_triage_payload_adds_diagnosis_contract_for_search_term_path -q
python -m pytest tests\test_signal_triage_service.py -q
npm run test:signal-ui-runtime
```

## 2026-06-24：点击搜索词表现分组不能改变经营诊断入口

```text
问题：
用户指出“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
如果点击搜索词表现分组后改变“经营诊断入口”筛选器，用户会误以为系统从 Parent ASIN 切换到了语义组或搜索词聚合对象。

原因：
1. 搜索词表现分组只是当前 Parent ASIN 广告 SearchTerm 表现的聚合筛选，不是 Amazon 广告架构里的经营入口。
2. 顶部经营诊断入口承载 Parent ASIN / ASIN 对象边界，不能被语义聚合点击隐式改写。
3. 如果状态联动散在组件里，后续容易再次把分组点击写成 setSelectedProductScopeId(intentLabel) 这类错误逻辑。

解决：
1. 新增 resolveSearchIntentFocusSelection，把分组点击结果抽成纯 helper。
2. handleSelectSearchIntent 只更新 selectedSearchIntentLabel、selectedSearchIntentScopeId 和 selectedId，不触碰 selectedProductScopeId。
3. 测试锁定点击分组绑定当前 Parent ASIN scope、选中具体 SearchTerm，再次点击同一分组只清空焦点。

防错规则：
1. 搜索词表现分组只能筛选当前诊断入口内的 SearchTerm 信号。
2. 搜索词表现分组不能作为经营诊断入口值，也不能作为人工动作对象。
3. 后续不得出现 setSelectedProductScopeId(intentLabel)、setSelectedProductScopeId(card.intentLabel) 或把分组值写入顶部入口筛选器的逻辑。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：运行态服务旧进程会掩盖搜索词表现分组新口径

```text
问题：
用户再次强调“语义组复核”的本质是从 Parent ASIN 角度聚合广告用户搜索词，帮助分析搜索词表现。
源码和后端单测已经返回 ad_search_term_daily_metrics 口径，但运行态 /api/search-intents 仍一度返回旧文案“当前 Parent ASIN 相关广告上下文中实际产生表现的用户搜索词行”。

原因：
1. 机器上同时存在旧 8000/5175 和新 8010/5174 两套 dev server。
2. 用户浏览器停在 5175，仍连接旧 8000 后端。
3. 只看源码或单测会误判页面已经更新，必须同时检查 tmp/local-dev/last-start.json 和真实 API 返回。

解决：
1. 停掉本项目占用 8000、8010、5175、5174 的监听进程。
2. 重新运行 scripts/start_local_dev.ps1，让当前服务回到 8000/5175。
3. frontend/tests/signalUi.runtime.test.ts 增加运行态断言，要求搜索词表现分组 dataGrain 包含 ad_search_term_daily_metrics，并读出广告用户搜索词表现。

防错规则：
1. 遇到页面文案或接口口径和源码不一致，先查运行服务端口，不直接改业务代码。
2. 搜索词表现分组必须锁定为当前 Parent ASIN 关联广告上下文中的 ad_search_term_daily_metrics 用户搜索词表现行。
3. 运行态测试必须覆盖 API 返回和前端 helper 读出，不能只覆盖静态文案。

验证方式：
python -m pytest tests\test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope -q
npm run test:signal-ui-runtime
```

## 2026-06-24：后端 preflight 有完整 SearchTerm 证据，但右侧优先摘要可能漏关键链路

```text
问题：
后端 evidence_snapshot_preview 已经包含具体搜索词、搜索词表现分组、广告组合流、同组商品、逐投放上下文、投放词、广告位边界和广告位活动级背景。
但前端 manualActionPreflightPriorityEvidenceRows 旧优先标签没有覆盖全部 SearchTerm 复核链，用户在右侧人工确认前可能看不到完整业务上下文。

原因：
1. 后端证据合同越来越完整，但前端优先摘要标签仍偏通用动作核对。
2. 默认只展示 12 条优先证据时，部分 SearchTerm 关键证据容易被证据缺口、动作边界等通用项挤掉。
3. 如果右侧摘要漏掉分组、同组商品或广告位活动级背景，用户仍可能基于裸指标保存人工动作。

解决：
1. manualActionPreflightPriorityEvidenceLabels 增加搜索词、搜索词表现分组、同组投放商品表现、逐投放上下文和广告位活动级背景。
2. manualActionPreflightPriorityEvidenceRows 默认上限调整为 16 条，保证 SearchTerm 业务链路优先露出。
3. 运行态测试锁定右侧 preflight 优先证据必须包含这些关键标签。

防错规则：
1. SearchTerm 人工确认前的优先摘要必须覆盖：搜索词、搜索词表现分组、广告组、同组商品、逐投放上下文、投放词、搜索词边界、广告位边界。
2. 广告位只有 campaign 级背景时，必须显示“广告位活动级背景”，不能让用户误认为已有广告组级归因。
3. 后端新增 evidence_snapshot 标签后，要同步检查前端优先摘要是否会漏掉关键业务链路。

验证方式：
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
```

## 2026-06-24：SearchTerm 历史待办治理证据合同不能和 ReviewRecord 保存门禁分叉

```text
问题：
ReviewRecord 保存门禁已经要求 SearchTerm 复盘证据包含 Parent ASIN入口和广告 ASIN承接。
但 review_evidence_repair.py 中 SEARCH_TERM_REPAIR_LABELS 旧口径只要求排查路径、AI 准入、搜索词边界、广告位边界、投放词证据、广告组、ABA 和动作边界，没有要求搜索词表现分组、Parent ASIN入口和广告 ASIN承接。
这会让历史 SearchTerm 待办陷入矛盾状态：保存 ReviewRecord 会被拒绝，但 apply_review_todo_void_once.py 可能误判为“证据完整”而阻止 dry-run 作废。

原因：
1. 人工动作预检、历史证据修复检查、复盘待办作废脚本各自维护必备 evidence label。
2. SearchTerm 复盘链路后来补深了 Parent ASIN 和广告 ASIN 证据，但历史治理常量没有同步。
3. 测试只覆盖了搜索词边界、广告位边界、投放词、广告组和 ABA，没有专门锁定 Parent ASIN / 广告 ASIN 缺口。

解决：
1. review_evidence_repair.py 直接复用 manual_action_preflight.py 中的 REQUIRED_REVIEW_EVIDENCE_LABELS、SEARCH_TERM_REQUIRED_REVIEW_EVIDENCE_LABELS、ADVERTISED_PRODUCT_REQUIRED_REVIEW_EVIDENCE_LABELS 和 PLACEMENT_REQUIRED_REVIEW_EVIDENCE_LABELS。
2. 补齐 test_review_evidence_repair_cli.py 中 SearchTerm 可重建证据 fixture 的搜索词表现分组、Parent ASIN入口和广告 ASIN承接。
3. 补充 test_review_todo_void_cli.py 用例，确认缺 Parent ASIN / 广告 ASIN 的 SearchTerm 待办仍可 dry-run 作废，而完整证据待办会被阻断。

防错规则：
1. SearchTerm 复盘治理的必备证据必须与人工动作预检合同共用同一来源，不再手写另一套常量。
2. 判断历史待办是否可作废时，必须同时校验搜索词表现分组、Parent ASIN入口、广告 ASIN承接、广告组合流、同组商品表现、投放词、搜索词边界、广告位边界、ABA、证据缺口、需要补证和动作边界。
3. 缺 Parent ASIN 或广告 ASIN 证据时，不能保存 ReviewRecord，也不能阻止历史待办 dry-run 作废。
4. 当前证据预览只能用于重新人工留痕，不能补写历史 evidence_snapshot。

验证方式：
python -m pytest tests\test_review_evidence_repair_cli.py -q
python -m pytest tests\test_review_todo_void_cli.py -q
python -m pytest tests\test_manual_actions.py -q
python -m pytest tests\test_review_readiness_cli.py -q
python -m pytest tests\test_manual_action_apply_cli.py -q
python -m pytest tests\test_api_routes.py -q
python -m compileall app ..\scripts\apply_review_todo_void_once.py ..\scripts\inspect_review_evidence_repair.py
```

## 2026-06-24：“语义组复核”命名容易偏离广告搜索词表现主口径

```text
问题：
“语义组复核”这个旧叫法容易让用户误解为自然搜索语义分析、泛关键词分类或新的主入口。
但当前项目是广告 AI 信号工作台，该板块的业务本质应是从 Parent ASIN 角度聚合广告中的用户搜索词表现，帮助用户分析同类 SearchTerm 表现。

原因：
1. 历史 evidence label 中存在“语义组 / 搜索意图分组 / 广告搜索词聚合上下文”等兼容叫法。
2. 如果 UI 把它做成独立语义入口，用户会误以为点击后应该切换经营诊断入口或生成语义组人工动作。
3. 如果不强调 ad_search_term_daily_metrics，容易把 ABA 站点级背景或自然搜索词误当成广告主口径。

解决：
1. 用户可见名称固定为“Parent ASIN 广告搜索词表现复核”，分组名称固定为“搜索词表现分组”。
2. 数据粒度固定为当前 Parent ASIN 关联广告上下文中的 ad_search_term_daily_metrics 用户搜索词表现行。
3. 点击分组只筛选当前诊断入口内的 SearchTerm 信号，不改变经营诊断入口，不生成语义组人工动作。
4. ABA 只能作为站点级背景，不能证明单个 ASIN 归因，也不能替代广告搜索词表现。

防错规则：
1. 后续 UI 不再使用“语义组复核”作为用户可见板块名。
2. SearchTerm 聚合板块必须回答扩量、止损、观察三个业务判断，不能只展示裸指标。
3. 所有人工动作仍必须落到具体 SearchTerm / 广告商品 / 广告位等可复盘对象，不能落到搜索词表现分组本身。

验证方式：
rg -n "语义组复核|Parent ASIN 广告搜索词表现复核|搜索词表现分组" frontend\src backend\app frontend\tests backend\tests
```

## 2026-06-24：搜索词表现复核默认裁剪会制造“数据量不足”的假象

```text
问题：
B00K4W4AAA 的 /api/search-intents 已经返回 7 个广告搜索词表现分组。
但前端 buildSearchIntentReviewCards 默认只展示 4 个，会让用户以为当前 Parent ASIN 的搜索词表现数据量仍然不足。

原因：
1. 前端 helper 早期为了控制列表长度写死默认 limit=4。
2. 当前真实 Parent ASIN 的搜索词表现分组只有 7 个，全部展示仍属于可控 MVP 范围。
3. 如果默认隐藏后 3 个分组，用户无法完整判断 Parent ASIN 下广告 SearchTerm 表现分布。

解决：
1. buildSearchIntentReviewCards 默认上限从 4 调整为 8。
2. 运行态测试增加断言：当真实搜索词表现分组数小于等于 8 个时，默认卡片数必须等于真实分组数。
3. 继续保持“搜索词表现分组不是人工动作对象”，只是当前 Parent ASIN 下广告 SearchTerm 的分析分组。

防错规则：
1. 不要用过小的默认展示上限制造数据量不足的错觉。
2. 如果真实分组数量超过 8，再设计排序、折叠或分页，并明确显示总数和当前展示数。
3. 搜索词表现复核的裁剪逻辑必须由业务阅读路径驱动，不能为了页面短而隐藏关键证据。

验证方式：
npm run test:signal-ui-runtime
npm run test:signal-ui
npm run build
```

## 2026-06-24：右侧 preflight 的广告组合流判断不能退回搜索词机会指标

```text
问题：
中间广告搜索词表现复核链已经使用 ad_group_boundary 诊断合同展示广告组容器、同组广告 ASIN 和广告位证据缺口。
但右侧 preflight evidence_snapshot_preview 的“广告组合流判断”旧逻辑优先读取 search_term_opportunity.current_judgement，导致同名标签在中间和右侧表达不同口径。

原因：
1. _target_search_term_boundary_snapshot_blocks 早期只围绕具体 SearchTerm 的机会判断生成“广告组合流判断”。
2. 后续中间复核链已经补深 ad_group_boundary，但 preflight 同名标签没有同步调整。
3. _search_term_review_chain_snapshot_blocks 只在缺“广告组合流判断”时兜底，不能覆盖已经生成但层级不对的同名标签。

解决：
1. SearchTerm preflight 的“广告组合流判断”优先读取 ad_group_boundary.current_judgement。
2. 同步把广告位证据层级合并进该块 value，保留缺广告组级广告位时不能下结论的边界。
3. detail 继续明确不能把搜索词表现自动归因到单个广告 ASIN，也不能把广告组当产品。
4. 写前 preflight 和写后 evidence_snapshot 测试同时锁定两个广告组、三个广告 ASIN 和广告位层级必须出现在“广告组合流判断”里。

防错规则：
1. 中间诊断链和右侧 preflight 使用同名 evidence label 时，必须来自同一业务层级。
2. “广告组合流判断”必须优先回答广告组容器、同组广告 ASIN、广告位证据缺口，不应退回搜索词机会裸指标。
3. 如果后端诊断合同新增或调整 section，必须同时检查 preflight 证据块是否仍按正确优先级取值。
4. 历史旧 evidence_snapshot 不能静默补写，只能通过重新人工留痕生成新证据链。

验证方式：
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id "search_term:1:beach essentials" --expected-object-type search_term --action-type add_to_review --top 5 --compact
python -m pytest tests\test_manual_action_preflight_cli.py tests\test_api_routes.py -q
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：历史 ReviewTodo 缺证据不能误判为当前 preflight 缺能力

```text
问题：
inspect_review_readiness 显示 beach essentials 的 7/14 天 ReviewTodo 因缺“同组投放商品表现”被阻断。
但当前 manual_action preflight 已经能生成 29 条 evidence_snapshot_preview，并包含“同组投放商品表现”。

原因：
1. ReviewTodo 继承的是人工点击当时保存的 evidence_snapshot。
2. 历史最新 manual_action 只有 22 条证据，尚未保存独立的“同组投放商品表现”块。
3. 当前 preflight 能生成完整证据，只能用于新的人工留痕，不能静默补写历史 evidence_snapshot。

解决：
1. 保持历史 evidence_snapshot 不可补写。
2. 对历史缺证据 ReviewTodo 使用 apply_review_todo_void_once 显式作废。
3. 作废后再用 apply_manual_action_once 重新人工留痕，让新 ReviewTodo 继承包含“同组投放商品表现”的 29 条证据。
4. 当前已用临时目录演练：void 后 apply dry-run 变为 ready，不修改真实运行态数据。

防错规则：
1. 看到 review_readiness 缺证据时，先区分“历史留痕缺证据”和“当前 preflight 缺能力”。
2. 不允许把当前页面证据或当前 preflight 证据补写进历史 manual_action / ReviewTodo。
3. 只有显式作废旧待办并重新人工留痕，才能让后续 7/14 天复盘读回新证据链。

验证方式：
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id "search_term:1:beach essentials" --expected-object-type search_term --action-type add_to_review --top 5 --compact
python scripts\apply_review_todo_void_once.py --market-id 1 --action-id manual-action-a41040dc336e41a2a1d4c7a5ec854cfd --expected-object-type search_term --expected-object-id "search_term:1:beach essentials" --compact
python -m pytest tests\test_manual_action_apply_cli.py::test_manual_action_apply_rewrites_after_voided_legacy_todo_with_isolated_roots -q
```

## 2026-06-24：SearchTerm preflight 缺“需要补证”兜底会造成写前 ready、写后 blocked

```text
问题：
boys sunglasses 旧待办作废后，第一次重新人工留痕写入了 23 条 evidence_snapshot，但 inspect_review_readiness 仍显示 blocked，原因是新 ReviewTodo 缺少“需要补证”2 条。

原因：
1. _search_term_review_chain_snapshot_blocks 早期只把 diagnosis_contract_required_evidence 合并进“证据缺口”。
2. 当某个 SearchTerm 诊断合同没有独立 required_evidence 字段时，preflight 仍显示 ready，但写后 ReviewTodo 门禁要求独立“需要补证”标签。
3. 结果是写前 dry-run ready，写后 post_write blocked，说明 preflight 合同和复盘门禁不一致。

解决：
1. SearchTerm review chain 增加 search_term_review_required_evidence 兜底块。
2. 只有当诊断合同没有独立“需要补证”时才生成兜底，避免 beach essentials 从 29 条重复增加到 30 条。
3. 作废已写入但 blocked 的 manual-action-64b4b2e82b9d4d8bbe653f2e681b857f，再重新留痕 boys sunglasses，生成 24 条 evidence_snapshot。
4. 重新运行 inspect_review_readiness 后，review_identity_audit.status=ready_for_readback，missing_required_evidence_count=0。

防错规则：
1. SearchTerm 写前 preflight 的证据标签必须覆盖 ReviewTodo 保存门禁需要的标签。
2. dry-run ready 不够，真实写入后必须执行 post-write 验收和 inspect_review_readiness。
3. 如果已经写入但 post-write blocked，不修改或删除历史 ManualAction；显式作废其 ReviewTodo 后重新留痕。
4. “需要补证”不是文案装饰，它是 7/14 天复盘回看缺口和下一步路径的必备证据。

验证方式：
python -m pytest tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_uses_target_diagnosis_contract_when_global_points_to_next tests\test_manual_action_preflight_cli.py::test_manual_action_preflight_snapshots_next_unhandled_diagnosis_contract -q
python -m pytest tests\test_manual_action_apply_cli.py::test_manual_action_apply_rewrites_after_voided_legacy_todo_with_isolated_roots tests\test_review_todo_void_cli.py -q
python scripts\inspect_review_readiness.py --market-id 1 --compact
```

## 2026-06-24：Parent ASIN 广告搜索词表现复核不能只写“搜索词表现分组”

```text
问题：
用户再次明确，“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
当前页面主标题已收口为 Parent ASIN 广告搜索词表现复核，但部分 data_grain、helper 默认值和测试仍写“按搜索词表现分组聚合”，没有说明分组方法是标准化搜索词/语义标签，也没有明确排除未投放子 ASIN 和自然搜索词。

原因：
1. 旧“语义组复核”改名后，数据口径文字没有同步打深。
2. “搜索词表现分组”只说明结果形态，没说明源数据来自 ad_search_term_daily_metrics。
3. 不排除未投放子 ASIN 和自然搜索词时，用户会误以为这里代表 Parent ASIN 全量搜索需求。

解决：
1. /api/search-intents 的 Parent ASIN data_grain 改为：当前 Parent ASIN 关联广告上下文中的 ad_search_term_daily_metrics 用户搜索词表现行，按标准化搜索词/语义标签聚合；不包含未投放子 ASIN 或自然搜索词。
2. 前端标题说明、panel context、SearchIntent card、signalMetricBoundary 同步强调“按标准化搜索词/语义标签聚合广告中的用户搜索词表现”。
3. 运行态测试同步更新为当前事实：历史证据链已治理完成，状态是等待 7/14 天复盘窗口，不再断言旧 blocked 缺口或旧 22 条证据快照。

防错规则：
1. 用户可见主语必须是 Parent ASIN 广告搜索词表现复核，不能退回“语义组复核”。
2. 语义标签只能作为聚合方法，不能成为诊断入口、经营商品、广告组或人工动作对象。
3. 搜索词聚合口径必须同时说明源表、Parent ASIN 广告上下文、排除未投放子 ASIN / 自然搜索词。
4. 运行态测试出现旧 evidence_snapshot 数量时，应先确认当前真实 ReviewTodo 是否已重新留痕，不能把历史旧快照当新事实。

验证方式：
python -m pytest tests\test_signal_triage_service.py::test_search_intent_summaries_respect_parent_asin_product_scope -q
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：历史“语义组”兼容标签不能在右侧人工确认读回中露出

```text
问题：
用户已确认“语义组复核”的真实业务作用是从 Parent ASIN 角度聚合广告中的用户搜索词表现。
当前主面板已经改为 Parent ASIN 广告搜索词表现复核，但右侧人工确认 / 复盘读回里仍可能从历史 evidence_snapshot 或 repeat_summary 读到旧“语义组”标签。

原因：
1. 旧 evidence_snapshot 需要兼容读取，不能直接删除“语义组”标签匹配。
2. manualActionEvidenceSnapshotText 已做 label 转换，但 manualActionEvidenceReasonText 直接输出 item.label。
3. reviewContextDisplayText 只转换了“搜索意图分组”和“Parent ASIN 搜索词表现聚合”，漏掉“语义组”。

解决：
1. manualActionEvidenceReasonText 输出前统一走 manualActionEvidenceDisplayLabel。
2. reviewContextDisplayText 增加“语义组”到“搜索词表现分组”的显示层转换。
3. review-ui 测试补充断言：历史“语义组”证据和上下文读回时不能显示“语义组：”，必须显示“搜索词表现分组”。

防错规则：
1. 历史兼容标签可以留在匹配列表，但用户可见层必须转换为当前业务主语。
2. 当前业务主语是 Parent ASIN 广告搜索词表现复核 / 搜索词表现分组，不再使用“语义组复核”。
3. 搜索词表现分组不是人工动作对象；人工动作对象必须是具体 SearchTerm 或稳定广告对象。

验证方式：
npm run test:review-ui
npm run test:signal-ui-runtime
python scripts\inspect_manual_action_preflight.py --market-id 1 --product-scope-id parent_asin:B00K4W4AAA --expected-object-id "search_term:1:beach essentials" --expected-object-type search_term --action-type add_to_review --top 5 --compact
```

## 2026-06-24：广告搜索词表现复核链不能只给长文本证据

```text
问题：
广告搜索词表现复核链已经包含广告组合流判断、同组投放商品表现和广告位边界，但中间诊断区主要以长文本列表展示，用户仍需要自己推理每层证据为什么存在、能证明什么和下一步做什么。

原因：
1. buildSearchTermOpportunityReviewChain 先解决了链路完整性，没有把每层拆成独立判断卡。
2. 广告组合流判断、同组投放商品表现和广告位边界各自服务不同问题，但 UI 上都只是一个 label + span。
3. 裸证据容易让运营把广告组、搜索词或广告位误读成单个广告 ASIN 的归因证据。

解决：
1. SearchTermOpportunityReviewChain 增加 reviewLayers。
2. 每层固定包含 purpose、evidence、proves、doesNotProve、nextManualStep。
3. 中间诊断区渲染“广告搜索词复核分层判断”，先展示分层判断，再展示完整证据链。

防错规则：
1. 任何搜索词复核链新增证据层时，必须同步说明用途、能证明、不能证明和人工下一步。
2. 广告组、搜索词和广告位默认是上下文证据，不能直接包装成单个广告 ASIN 归因。
3. 分层判断只服务人工复核和 7/14 天复盘，不允许生成自动加词、否词、调价、暂停广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：搜索词人工确认不能缺少逐投放上下文和广告位边界

```text
问题：
右侧“人工确认证据写入核对”已经检查 Parent ASIN入口、广告 ASIN承接、投放词证据、广告组合流判断、同组投放商品表现、ABA 背景、证据缺口、需要补证和动作边界，但没有把“逐投放上下文”和“广告位边界”作为搜索词复核链必需项。

原因：
1. 上一轮先打深中间诊断区分层判断，没有同步收紧右侧人工确认到复盘的证据门禁。
2. 搜索词合计指标容易被误读成可直接复盘，但复盘真正需要回看当时的广告组、投放词、广告 ASIN 和广告位证据层级。
3. 少了逐投放上下文，运营无法知道当时先复核哪条广告搜索词表现行；少了广告位边界，后续容易把广告位缺口包装成广告位结论。

解决：
1. buildManualConfirmationEvidenceItems 增加“逐投放上下文”。
2. manualConfirmationEvidenceReadinessSummary 将“逐投放上下文”和“广告位边界”纳入搜索词复核链必需标签。
3. review-ui / signal-ui / runtime / structure 测试同步加严，ready 必须包含这两个标签，缺口必须暴露这两个缺失。

防错规则：
1. 搜索词人工留痕要进入 7/14 天复盘，必须同时保留逐投放上下文和广告位边界。
2. 搜索词表现合计不能替代广告组、投放词、广告 ASIN 和广告位层级证据。
3. 任何后续改动若新增或重排搜索词复核链，必须同步检查 manualConfirmationEvidenceReadinessSummary 和 ReviewRecord 保存前门禁。

验证方式：
npm run test:signal-ui
npm run test:review-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
```

## 2026-06-24：搜索词表现分组不能绕过逐投放上下文保存复盘

```text
问题：
用户进一步确认“语义组复核”的真实作用应是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果 ReviewRecord 保存时只要求搜索词合计、广告 ASIN承接、广告组合流和投放词证据，但不要求“逐投放上下文”，系统仍可能保存一条无法落地到广告组 / 投放词 / 广告 ASIN 的复盘记录。

原因：
1. 前端人工确认门禁已经要求“逐投放上下文”，但后端服务层和 API 预检还没有同步要求。
2. 搜索词表现分组是聚合分析视角，不是人工动作对象。
3. 缺逐投放上下文时，7/14 天后只能复盘搜索词合计变化，不能复盘当时具体该看哪条广告投放链路。

解决：
1. save_review_record 对 search_term ReviewRecord 增加“逐投放上下文”必需检查。
2. _validate_review_record_evidence_snapshot_preflight 同步返回 review_record_missing_ad_context_rows。
3. _review_todo_missing_required_evidence 把“逐投放上下文”纳入旧待办缺证判断。
4. 服务层和 API 层新增缺逐投放上下文拒绝保存测试。

防错规则：
1. 搜索词表现分组只用于 Parent ASIN 下广告搜索词聚合分析，不是可保存复盘的动作对象。
2. 搜索词 ReviewRecord 必须保留逐投放上下文，否则不能视为完整广告复盘证据。
3. 后续若调整前端搜索词复核链，必须同时检查后端 ReviewRecord 保存门禁。

验证方式：
python -B -m pytest -p no:cacheprovider tests\test_manual_actions.py::test_review_record_rejects_search_term_snapshot_without_ad_context_rows_at_service_layer tests\test_api_routes.py::test_review_record_rejects_search_term_snapshot_without_ad_context_rows tests\test_manual_actions.py::test_review_record_persists_ready_effect_and_can_read_latest tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot -q
python -B -m pytest -p no:cacheprovider tests\test_api_routes.py::test_review_todo_void_api_removes_legacy_todos_and_blocks_review_record tests\test_api_routes.py::test_review_record_rejects_evidence_snapshot_object_mismatch tests\test_api_routes.py::test_review_record_rejects_search_term_snapshot_without_action_boundary tests\test_api_routes.py::test_review_record_rejects_search_term_snapshot_without_required_evidence tests\test_api_routes.py::test_review_record_rejects_search_term_snapshot_without_ad_group_synthesis -q
```

## 2026-06-24：搜索词表现分组点击不能造成经营诊断入口漂移感

```text
问题：
用户指出点击“语义组复核 / 搜索词表现分组”下的数据后，诊断入口筛选器的值看起来会变化。
这会让用户误以为搜索词表现分组是一个新的经营入口或诊断入口。

原因：
1. 搜索词表现分组点击本身没有直接 setSelectedProductScopeId，但旧实现允许 selectedProductScopeId 为 null。
2. 当 selectedProductScopeId 为 null 时，顶部筛选器 value 由 preferredProductScopeId(productScopeOptions) 隐式推导。
3. 默认推荐入口、数据刷新和选中信号变化叠在一起时，用户容易把“队列焦点变化”理解为“诊断入口变化”。

解决：
1. 新增 resolveProductScopeSelectionId，保持有效的当前经营入口。
2. loadSignals 后显式 setSelectedProductScopeId(nextActiveProductScopeId)，把默认入口固化为当前入口。
3. signal-ui 测试验证当前入口有效时保持不变，入口无效时才回到推荐入口。
4. workbench-structure 测试继续验证搜索词表现分组不写 setSelectedProductScopeId。

防错规则：
1. 经营诊断入口是用户分析边界，必须是显式状态，不要长期依赖隐式默认值。
2. 搜索词表现分组只允许筛选当前入口内的同类 SearchTerm 信号，不能修改 ProductScope。
3. 后续任何点击行为如果要改变经营诊断入口，必须是明确的入口按钮或顶部筛选器操作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-24：左侧 AI 信号队列不能被读成 Parent ASIN 全部子 ASIN 清单

```text
问题：
用户此前指出项目容易把 Parent ASIN 下所有变体都展示出来，但广告 AI 信号工作台应以广告数据为核心。
左侧队列虽然已按当前诊断入口收敛，但空态和解释里仍有“当前商品组”这类宽泛主语，容易让用户误读为系统在列出 Parent ASIN 下全部子 ASIN。

原因：
1. Parent ASIN 是经营入口，销售表现可识别多个子 ASIN。
2. 左侧 AI 信号队列的对象应是有广告证据并命中规则的信号，不是销售子 ASIN 清单。
3. “商品组”这个词没有明确区分销售背景和广告证据，容易让未投放子 ASIN 看起来也需要广告诊断。

解决：
1. Parent ASIN 队列标题改为“Parent ASIN 广告证据信号”。
2. 空态标题改为“当前 Parent ASIN 暂无可处理广告信号 / 暂无可行动候选”。
3. 解释文案改为“Parent ASIN 广告口径”，明确未投放子 ASIN 只作经营背景，不进入广告信号队列。
4. signal-ui 测试同步断言广告证据范围和未投放子 ASIN 边界。

防错规则：
1. 左侧 AI 信号队列只展示当前经营入口下有广告证据并命中规则的信号。
2. Parent ASIN 下全部子 ASIN 只能作为销售背景或覆盖率解释，不能直接变成广告信号队列对象。
3. 后续若出现“商品组”文案，必须确认它不会被用户理解为全部子 ASIN 清单。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-24：搜索词表现判断不能只停留在聚合卡片

```text
问题：
搜索词表现分组卡片已经能给出扩量复核 / 止损复核 / 观察复核判断，但点击进入具体 SearchTerm 后，中间诊断区和右侧人工确认区原本只知道分组标签和具体 SearchTerm，不知道这条分组为什么被优先打开。

原因：
1. SearchIntentReviewCard 里已有 operationDecisionLabel / operationDecisionReason / operationDecisionTone。
2. buildSearchIntentFocusContext 和 buildManualConfirmationEvidenceItems 原本没有接收当前激活卡片。
3. 用户点击后容易看到裸 SearchTerm 证据，却缺少“为什么现在要看这条词”的业务判断承接。

解决：
1. 页面新增 activeSearchIntentReviewCard，只读匹配当前 activeSearchIntentLabel。
2. buildSearchIntentFocusContext 增加可选卡片参数，在中间承接条展示“运营判断”和“优先 SearchTerm”。
3. buildManualConfirmationEvidenceItems 增加可选卡片参数，在右侧人工确认证据中展示“搜索词表现判断”。
4. 测试锁定判断只用于人工复核优先级，不自动执行广告动作。

防错规则：
1. 搜索词表现分组的扩量 / 止损 / 观察判断必须能贯通到具体 SearchTerm 诊断和人工确认。
2. 该判断只能服务人工复核优先级，不能变成自动加词、否词、调价或暂停。
3. 后续如果新增搜索词聚合卡片字段，必须检查中间诊断区和右侧人工确认区是否需要同步承接。

验证方式：
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:workbench-structure
npm run build
```

## 2026-06-24：人工预检快照不能丢失搜索词表现判断

```text
问题：
用户再次明确“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
前端已经能在聚合卡片、中间承接和右侧人工确认区展示扩量 / 止损 / 观察判断，但后端 evidence_snapshot_preview 原本只保存“搜索词表现分组”，没有保存“搜索词表现判断”。
如果用户 7/14 天后复盘，只能看到当时属于哪个搜索词表现分组，却看不到为什么当时优先选择这条 SearchTerm。

原因：
1. 搜索词表现分组是聚合维度，不是最终复盘判断。
2. 人工确认和复盘读回以后端 preflight evidence_snapshot_preview 为准，不能只依赖前端临时状态。
3. 旧快照顺序缺少扩量 / 止损 / 观察判断，导致复盘证据链无法回答“为什么选它”。

解决：
1. 后端 search_term evidence_snapshot_preview 在“搜索词表现分组”后新增“搜索词表现判断”。
2. 判断来自当前 preflight 目标 SearchTerm 的广告表现行：有订单低 ACOS 为扩量复核，高花费无订单为止损复核，其余为观察复核。
3. ManualAction 和 7/14 天 ReviewTodo 读回同步保存该判断。
4. 前端优先证据摘要识别“搜索词表现判断”，但不把它设为页面证据硬门禁；后端 preflight 可以比页面临时证据更完整。
5. 测试把新预检证据快照数量锁定为 30；历史已保存待办仍允许读回 29 条，不做隐式迁移。
6. 测试断言该判断只用于人工复核优先级，不自动加词、否词、调价或暂停。

防错规则：
1. 搜索词表现分组只能说明 Parent ASIN 下广告 SearchTerm 的聚合维度。
2. 搜索词表现判断必须随人工预检快照保存，否则 7/14 天复盘缺少选择理由。
3. 历史 29 条快照不能被测试或 UI 假定已经变成 30 条；只能保证新 preflight 和新写入包含判断。
4. 任何判断文案都不能包装成自动广告动作，仍只能落到记录观察、标记已处理、加入复盘、忽略本次。

验证方式：
python -m py_compile backend\app\services\manual_action_preflight.py
python -B -m pytest -p no:cacheprovider tests\test_api_routes.py::test_beach_essentials_manual_action_api_roundtrip_reads_back_evidence_snapshot -q
npm run test:signal-ui-runtime
npm run test:review-ui
npm run test:signal-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-24：Parent ASIN 入口不能混用经营和广告指标裸标签

```text
问题：
用户再次明确“语义组复核”的作用应该是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
如果 Parent ASIN 第一屏仍写“销售入口 / 订单 / 销售额”，而广告 ASIN 表格也写“订单 / 销售额”，用户会把经营销售口径、广告商品口径和搜索词表现聚合口径混在一起。

原因：
1. Parent ASIN 是经营销售入口，不是广告动作对象。
2. 广告 ASIN 表格来自 advertised_products 广告证据，订单和销售额应明确为广告订单、广告销售额。
3. 搜索词表现分组只是当前 Parent ASIN 广告上下文下的 SearchTerm 聚合视角，不能被裸指标误导成独立业务对象。

解决：
1. Parent ASIN 路径改为“经营销售入口”。
2. Parent ASIN 经营背景改为“经营订单 / 经营销售额”。
3. 广告 ASIN 表头改为“广告订单 / 广告销售额”。
4. 前端测试和结构测试同步锁定新口径。

防错规则：
1. 经营指标必须显式带“经营”或“销售表现”语义。
2. 广告指标必须显式带“广告”语义。
3. 搜索词表现分组只能解释当前 Parent ASIN 广告用户搜索词表现，不得成为诊断入口或人工动作对象。
```

## 2026-06-24：SearchTerm 复盘读回不能漏掉搜索词表现判断

```text
问题：
后端新 preflight 已经把“搜索词表现判断”写入 SearchTerm evidence_snapshot，但前端复盘读回和 ReviewRecord 保存前门禁只检查搜索词表现分组、广告组合流、同组投放商品、逐投放上下文、投放词、搜索词边界、广告位边界和 ABA 背景。
这样 7/14 天后用户可能只能看到“这条词属于哪个分组”，却看不到当时为什么把它判为扩量、止损或观察候选。

原因：
1. “搜索词表现分组”是聚合维度，不是复盘选择理由。
2. “搜索词表现判断”是人工复核优先级，必须随 ReviewTodo / ReviewRecord 读回。
3. 复盘保存门禁如果只检查条数和结构标签，会漏掉真正回答业务判断的证据。

解决：
1. SearchTerm ReviewTodo 证据回读把“搜索词表现判断”列为必备标签。
2. SearchTerm ReviewRecord 读回成功文案把“搜索词表现判断”列入可回看链路。
3. ReviewRecord 保存前检查新增“回看搜索词表现判断”。
4. “语义组复核”前端表达改为 Parent ASIN 广告搜索词表现聚合，避免用户误解为复核语义标签。
5. review-ui 和运行态 smoke 测试补齐完整链路与缺失阻断。

防错规则：
1. SearchTerm 复盘不能只回看分组，必须回看当时扩量 / 止损 / 观察判断。
2. 搜索词表现判断只服务人工复核优先级，不允许变成自动加词、否词、调价或暂停广告。
3. Parent ASIN 广告搜索词表现复核是广告用户搜索词表现聚合视角，不是语义标签复核、经营商品入口或人工动作对象。
4. 后续新增 evidence_snapshot 标签时，必须同步检查 ReviewTodo 读回、ReviewRecord 读回、保存前门禁和运行态 smoke。
```

## 2026-06-24：搜索词表现复核不能停在分组筛选，必须能回到复盘结论

```text
问题：
用户再次校准“语义组复核”的作用：它应该从 Parent ASIN 的角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果页面只提供搜索词表现分组筛选，但复盘保存时不能按顺序读回当时判断和处理前后指标，用户仍然无法判断这个分析是否真的落地。

原因：
1. 搜索词表现分组是聚合视角，不是人工动作对象。
2. 具体 SearchTerm 才是人工确认、复盘待办和 ReviewRecord 的稳定对象。
3. 复盘保存如果只看处理后指标，会把结果倒推成原因，丢失当时为什么处理这条 SearchTerm 的证据链。

解决：
1. ReviewRecord 保存区新增“复盘保存顺序核对”。
2. 保存前按“当时判断 -> 指标窗口 -> 保存结论”展示。
3. 当时判断来自 ReviewTodo 业务判断读回，指标窗口来自 ReviewEffect ready 结果，保存结论仍只允许人工写入 ReviewRecord。
4. 测试锁定复盘保存顺序核对和页面渲染。

防错规则：
1. Parent ASIN 广告搜索词表现复核只负责聚合广告用户搜索词表现。
2. 搜索词表现分组不能升级成人工动作对象，也不能自动加词、否词、调价或暂停广告。
3. 保存 ReviewRecord 前必须能读回当时判断和处理前后指标窗口，不能从结果反推原因。
```

## 2026-06-24：点击搜索词表现复核后不能让用户误读为诊断入口切换

```text
问题：
用户曾指出点击“语义组复核”数据后，“诊断入口”筛选器的值看起来会变。
当前代码没有直接把搜索词表现分组写入 ProductScope，但选中具体 SearchTerm 后，中间诊断对象变化，用户仍可能误读为经营诊断入口被切换。

原因：
1. 搜索词表现分组和具体 SearchTerm 都出现在第一屏左侧与中间区。
2. 没有结构化读回时，用户需要自己判断“入口筛选器未变、只是队列二级筛选变化”。
3. 这会重新引发把搜索词表现分组当成经营商品入口或人工动作对象的风险。

解决：
1. 新增“诊断入口锁定核对”。
2. 点击搜索词表现复核后展示经营诊断入口、搜索词表现分组和优先诊断对象。
3. 文案明确搜索词表现分组不会写回 ProductScope；切换 Parent ASIN、ASIN 或广告对象必须使用诊断入口筛选器或明确入口按钮。

防错规则：
1. 搜索词表现复核点击只能筛当前入口内的同类 SearchTerm 信号。
2. 任何会改变 ProductScope 的动作必须是显式入口动作。
3. 页面只要选中 SearchTerm，就必须同时说明当前经营诊断入口仍保持不变。
```

## 2026-06-24：搜索词表现复核不能只解释分组，必须解释具体 SearchTerm 承接理由

```text
问题：
用户明确指出“语义组复核”的作用应该是从 Parent ASIN 的角度聚合广告中的用户搜索词，帮助用户分析搜索词表现。
如果页面只讲搜索词表现分组的数据口径，而不解释为什么当前中间诊断落到某条具体 SearchTerm，用户仍会觉得这个板块只是语义标签或裸指标筛选。

原因：
1. “语义组”是旧字段或规则来源，不是用户要复核的业务对象。
2. 搜索词表现分组只回答“这一组广告搜索词表现如何”，不能单独回答“为什么先看这条 SearchTerm”。
3. 人工动作和 7/14 天复盘必须落到具体 SearchTerm 稳定对象，不能落到分组。

解决：
1. 新增“具体 SearchTerm 复核理由”。
2. 在中间诊断区按 Parent ASIN 聚合视角、当前判断、优先打开理由、当前中间诊断、人工下一步展示。
3. 明确边界：不能证明单个 ASIN 归因，不能自动加词、否词、调价或暂停广告。
4. 测试锁定正常命中和用户手动切换到非优先 SearchTerm 的 warning 状态。

防错规则：
1. 用户可见主概念必须是“Parent ASIN 广告搜索词表现复核”，不能再把“语义组复核”作为主入口。
2. 任何搜索词表现聚合都必须说明它聚合的是当前 Parent ASIN 关联广告中的用户搜索词表现行。
3. 点击聚合项后必须解释具体 SearchTerm 承接理由，否则不能直接进入人工建议。
```

## 2026-06-24：搜索词人工动作前不能缺少对象和判断读回

```text
问题：
中间诊断区已经能解释为什么从 Parent ASIN 广告搜索词表现复核进入某条具体 SearchTerm，但右侧人工确认区如果直接展示动作按钮，用户在点击记录观察、标记已处理或加入复盘前，仍可能不知道将保存的是分组、SearchTerm 还是后端预检对象。

原因：
1. 诊断阅读和人工动作位于不同面板，用户容易在切换视线时丢失上下文。
2. 搜索词表现分组只是分析视角，不能成为人工动作对象。
3. 人工动作和 7/14 天复盘必须落到具体 SearchTerm，并且实际写入以后端 preflight evidence_snapshot_preview 为准。

解决：
1. 新增“人工留痕对象读回”。
2. 在人工动作按钮前展示入口上下文、复盘对象、当时判断、选择理由和保存后用途。
3. 对优先 SearchTerm 不一致场景展示 warning，提示按当前 SearchTerm 证据留痕。
4. 测试锁定该读回必须先于人工动作按钮渲染。

防错规则：
1. 搜索词表现复核进入人工确认前，必须先读回具体 SearchTerm 和当时判断。
2. 前端读回只做点击前核对，不能替代后端 preflight evidence_snapshot_preview。
3. 搜索词表现分组不能写成人工动作对象，也不能触发自动加词、否词、调价或暂停广告。
```

## 2026-06-24：前端读回不能替代后端预检快照

```text
问题：
右侧已经新增“人工留痕对象读回”，但如果没有同屏核对后端 preflight evidence_snapshot_preview，用户仍可能把页面临时读回理解成“这些证据已经会被保存到人工动作和复盘待办”。

原因：
1. 前端读回来自页面当前上下文和搜索词表现聚合卡片。
2. 7/14 天复盘真正能回看的证据来自后端 preflight evidence_snapshot_preview。
3. 如果后端快照缺少搜索词、Parent ASIN 广告搜索词表现复核或搜索词表现判断，保存后的复盘链路仍然不完整。

解决：
1. 新增“后端预检一致性核对”，在人工动作按钮前展示。
2. 核对后端快照是否包含搜索词、Parent ASIN 广告搜索词表现复核和搜索词表现判断。
3. 若后端预检未返回或证据不完整，展示 waiting / blocked 状态，并说明不能把前端读回当成复盘证据。
4. 最终写入仍以后端 preflight 为准，不新增自动广告动作。

防错规则：
1. 前端读回只负责提示用户当前看到的入口和对象，不能当作已保存证据。
2. SearchTerm 人工留痕必须以后端 evidence_snapshot_preview 作为复盘证据来源。
3. 新增或调整搜索词表现证据时，必须同步检查人工动作读回、后端预检一致性和 ReviewTodo / ReviewRecord 读回。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
```

## 2026-06-24：点击搜索词表现聚合卡片后右侧不能静默换对象

```text
问题：
用户确认“语义组复核”的真实作用应是从 Parent ASIN 角度聚合广告中的用户搜索词，帮助分析搜索词表现。
如果点击 B00K4W4AAA 下的 beach essentials 聚合卡片后，中间诊断选中 beach essentials，但右侧人工确认静默切到下一未处理候选，用户会以为自己在处理 SearchTerm，实际却可能保存到别的对象。

原因：
1. 搜索词表现分组是聚合入口，不是人工动作对象。
2. 推荐 SearchTerm 可能已经留痕，系统仍会存在下一未处理候选。
3. 如果运行态测试只检查“有下一候选可写”，会漏掉点击聚合卡片后的对象一致性。

解决：
1. 运行态测试锁定 B00K4W4AAA -> beach essentials 标杆链路。
2. 点击聚合卡片后必须保持 Parent ASIN 诊断入口不变。
3. 选中信号必须是 beach essentials 这条具体 SearchTerm。
4. beach essentials 已留痕时，右侧人工预检必须阻断重复写入，不能静默切到下一未处理候选。

防错规则：
1. Parent ASIN 广告搜索词表现复核只能做广告搜索词聚合视角。
2. 点击聚合项后，中间诊断和右侧人工确认必须读回同一个具体 SearchTerm。
3. 若该 SearchTerm 已留痕，必须显示阻断或已处理状态，不能替用户换对象保存。

验证方式：
npm run test:signal-ui-runtime
```

## 2026-06-24：右侧优先证据不能截断动作边界

```text
问题：
SearchTerm 人工确认的后端 evidence_snapshot_preview 已经包含搜索词、搜索词表现分组、搜索词表现判断、广告上下文、证据缺口、需要补证和动作边界。
但右侧优先证据列表如果默认截断过早，用户可能看得到指标和证据缺口，却看不到动作边界。

原因：
1. 完整 SearchTerm 证据链较长，优先证据列表原默认展示 16 条。
2. 动作边界排在需要补证之后，容易被截断。
3. 动作边界是人工确认前的关键业务边界，缺失会让页面像自动优化建议，而不是人工留痕入口。

解决：
1. manualActionPreflightPriorityEvidenceRows 默认展示调整为 18 条。
2. 运行态测试锁定 SearchTerm 优先证据顺序必须从 AI 准入、具体搜索词、搜索词表现分组和判断，一路读到需要补证和动作边界。
3. 保持后端 evidence_snapshot_preview 为复盘证据来源，前端只做可见排序和展示。

防错规则：
1. SearchTerm 人工确认前必须能看到动作边界。
2. 优先证据列表不能只展示指标、缺口和上下文，必须露出“不能自动加词、否词、调价、暂停广告”的边界。
3. 后续新增 SearchTerm 证据标签时，必须复核 manualActionPreflightPriorityEvidenceRows 的展示上限和顺序。

验证方式：
npm run test:signal-ui-runtime
npm run test:review-ui
npm run test:workbench-structure
```

## 2026-06-24：语义组复核不能被用户理解成独立处理对象

```text
问题：
用户指出“语义组复核”的作用应该是从 Parent ASIN 的角度聚合出广告中的用户搜索词，帮助用户分析搜索词表现。
如果页面主语仍停留在“语义组”或“搜索词表现分组”，用户容易把它理解成一个新的业务对象或处理对象。

原因：
1. 语义组只是搜索词归类方法，不是 Amazon 广告架构里的稳定对象。
2. 业务入口应是 Parent ASIN / 广告 ASIN，分析对象应是广告用户搜索词表现，人工动作对象应是具体 SearchTerm。
3. “搜索词表现分组”可以解释聚合结果，但不适合作为主板块标题或激活态主语。

解决：
1. 用户可见主语收口为“Parent ASIN 广告搜索词表现复核”。
2. 激活态和入口锁定说明明确：该复核只聚合当前 Parent ASIN 视角下广告中的用户搜索词表现行。
3. 继续保持点击复核卡不切换 ProductScope，人工写入不落到分组。

防错规则：
1. 新页面主语使用“Parent ASIN 广告搜索词表现复核”，不要再使用“语义组复核”作为主入口。
2. “搜索词表现分组”只允许作为证据标签、兼容旧留痕或解释聚合结果。
3. 所有搜索词聚合入口必须说明来源是广告用户搜索词表现行，不是自然搜索、ABA 店铺数据或单个 ASIN 归因。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
```

## 2026-06-24：证据完整不能变成看报纸式决策负担

```text
问题：
用户指出当前结果增加了决策时间和成本；如果要看 10 个 Parent ASIN 的广告数据，就像看 10 张报纸。
这说明页面虽然补齐了证据链，但把证据阅读和判断归纳的负担推给了用户。

原因：
1. 证据链越补越完整，但默认视图没有先回答“现在该不该处理、为什么、下一步做什么”。
2. 中间诊断默认展示过多“能证明 / 不能证明 / 证据缺口 / 需要补证”，用户需要自己二次归纳。
3. 右侧优先证据为了不漏动作边界曾被拉长，反而接近完整报告，降低了人工确认效率。

解决：
1. 中间诊断改为摘要优先：默认展示业务问题、诊断对象、人工下一步和证据缺口。
2. “能证明 / 不能证明 / 强度依据”和每个诊断块的完整解释改为可展开详情。
3. 右侧 preflight 优先证据默认压缩为 8 条，先展示 AI 准入、搜索词、搜索词表现判断、人工下一步、同组投放商品表现、证据缺口、需要补证和动作边界。
4. 完整 evidence_snapshot_preview 不删除，只放在完整证据区和复盘留痕链路中。

防错规则：
1. 不要把“更多证据默认展示”当成产品升级。
2. 每个 Parent ASIN 和 SearchTerm 默认先给决策摘要，再给可展开证据。
3. 优先视图服务分诊，完整证据服务核对和复盘；两者不能混成一张长报告。
4. 当用户需要同时看多个 Parent ASIN 时，默认应像异常清单，而不是像逐页读报表。

验证方式：
npm run test:workbench-structure
npm run test:review-ui
npm run test:signal-ui-runtime
浏览器只读检查摘要文案和 details 展开结构。
```

## 2026-06-24：复盘证据不能只验证预检预览，必须验证写后继承

```text
问题：
人工动作保存前的 evidence_snapshot_preview 已经能展示完整证据，但如果缺少写后回归门禁，后续改 UI 摘要或证据排序时，可能只保证预览完整，却让 ReviewTodo 读回的历史证据变少或缺关键广告证据链。

原因：
1. preflight 预览代表“将要保存什么”，post-write 才代表“实际保存并可复盘回读什么”。
2. 7/14 天复盘依赖 ManualAction.evidence_snapshot 继承到 ReviewTodo，而不是依赖当前页面重新生成。
3. 只看证据条数不足以证明业务可复盘，必须看同组商品、投放上下文、投放词、搜索词边界和广告位边界是否仍在。

解决：
1. 新增 post-write 回归测试，锁定 SearchTerm 人工动作写入后 ReviewTodo 继承完整 evidence_snapshot。
2. 测试要求写后 evidence_snapshot_preview 为 saved，且边界说明“不重新生成当前候选证据”。
3. 测试锁定同组投放商品表现、逐投放上下文、投放词证据、搜索词边界、广告位边界的存在和顺序。

防错规则：
1. 后续调整人工动作、复盘待办或证据折叠时，必须同时跑 post-write preflight 测试。
2. 不能用当前候选重新生成的证据替代历史点击证据。
3. 复盘链路必须证明“当时为什么处理这个 SearchTerm”，不能只证明“现在还能生成一份类似证据”。

验证方式：
python -m pytest backend\tests\test_manual_action_preflight_cli.py -q
```

## 2026-06-24：首页摘要和默认诊断入口不能脱节

```text
问题：
左侧已经出现“今日先看什么”和 Parent ASIN 优先处理清单，但首次加载如果仍按普通默认 Parent ASIN 打开，中间诊断可能展示的不是优先清单第一项。
用户看到摘要和详情不一致时，仍需要自己重新判断该打开哪个 Parent ASIN，阅读成本没有真正下降。

原因：
1. 普通默认入口只保证有一个可用 Parent ASIN，不保证它是今天最该处理的对象。
2. 优先清单已经按到期复盘、高优先信号、打开信号和广告花费排序，但首次加载没有复用这套排序。
3. 用户要看 10 个 Parent ASIN 时，默认入口不对齐会把分诊工作重新推给用户。

解决：
1. 新增 resolveProductScopePrioritySelectionId。
2. 无手动选择时使用今日优先 Parent ASIN 作为默认诊断入口。
3. 有有效手动选择时保留用户选择，不自动抢焦点。
4. 历史选择失效时回到优先 Parent ASIN，而不是普通第一个 Parent ASIN。

防错规则：
1. 首页任何“今日先看什么”摘要必须和默认诊断入口保持一致。
2. 自动默认只允许发生在用户未明确选择或历史选择失效时。
3. 优先入口只改变阅读顺序，不能改变人工动作对象、复盘对象或自动执行广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-24：Parent ASIN 纵向草图不能直接落成长报表

```text
问题：
用户提出筛选器 -> Parent ASIN 销售表现 -> 广告组数据 -> 广告组下具体数据 -> AI 广告数据分析的框架。
这个顺序符合运营路径，但如果原样做成纵向大块页面，用户看多个 Parent ASIN 时仍然需要逐页读销售和广告报表，决策成本不会下降。

原因：
1. Parent ASIN 销售表现是经营入口，不等于广告诊断对象。
2. 广告组是投放容器，广告组下还有投放商品、投放词、搜索词和广告位等不同证据层。
3. AI 如果放在最底部，只是在用户读完报表后总结，不能提前缩小人工复核范围。
4. 工作台价值是先告诉用户今天先看什么、为什么、下一步人工做什么，不是把所有数据默认摊开。

解决：
1. 将“Parent ASIN 诊断详情摘要”改为“Parent ASIN 运营诊断路径”。
2. 四段路径固定为 Parent ASIN 销售表现入口 -> AI 广告诊断摘要 -> 广告组优先排序 -> 广告组下具体广告数据。
3. 页面新增横向路径顺序，先让用户扫读路径，再展开证据。
4. 每段默认前置“人工下一步”，能证明 / 不能证明作为核对信息保留。

防错规则：
1. 草图中的四段只能作为运营诊断路径，不应直接变成四个默认展开报表区。
2. AI 必须在路径中提前缩小复核范围，不能只做页面底部总结。
3. 销售表现、广告 ASIN、广告组、搜索词和广告位必须保持对象边界，不混成同一层筛选。
4. 完整证据可以展开和保存到复盘快照，但首屏必须先服务分诊和人工下一步。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run test:review-ui
npm run build
```

## 2026-06-24：广告组列表不能先让用户读裸指标再自己判断

```text
问题：
广告组问题定位虽然已经有优先广告组和业务判断，但列表行默认先展示花费、ACOS、搜索词、广告 ASIN、广告位等指标。
这会让用户先读指标，再自行推理问题类型和下一步，仍然像在读广告组小报表。

原因：
1. 指标对 AI 诊断是证据，不是默认阅读入口。
2. 广告组是投放容器，裸指标容易让用户误以为可以直接落到商品或自动动作。
3. 用户真正需要先知道的是问题类型、证据强度和人工下一步。

解决：
1. 广告组优先判断新增三段读回：问题类型、证据强度、人工下一步。
2. 广告组列表行改为先展示业务判断，再展示广告组证据摘要。
3. 证据缺口、上下文边界和下一步复核焦点继续保留。

防错规则：
1. 广告组层默认视图必须先给问题 / 证据 / 下一步，不要先给裸指标。
2. 指标只能作为证据摘要或下钻依据，不能替代业务判断。
3. 广告组仍是容器，不能把广告组指标直接包装成商品归因或自动操作建议。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:review-ui
npm run build
```

## 2026-06-24：广告组详情不能以具体数据作为主语

```text
问题：
当前广告组详情层仍使用“具体数据”和“广告组下具体广告数据”这类主语。
这会让用户以为进入广告组后要继续读完整明细表，再自己判断问题。

原因：
1. 广告组是投放容器，广告组下的投放商品、投放词、搜索词和广告位是复核证据，不是默认阅读主语。
2. 如果详情层先强调具体数据，用户会重新回到 BI 报表心智。
3. AI 信号工作台应该先说明问题落点、证据缺口和人工下一步，再给证据摘要。

解决：
1. 四段运营诊断路径第 4 段改为“当前广告组复核路径”。
2. 当前广告组详情标题和 aria-label 改为“当前广告组复核路径”。
3. 新增“问题落点 / 证据缺口 / 人工下一步”三段判断。
4. 广告组指标区改为“当前广告组证据摘要”，不再作为详情层主语。
5. 结构测试锁定顺序：三段复核判断 -> 复核顺序 -> 证据摘要。

防错规则：
1. 广告组详情层主语必须是复核路径，不是具体数据。
2. 投放商品、投放词、搜索词和广告位只能作为证据层展示，不能默认包装成报表区。
3. 指标必须服务业务判断，不能替代业务判断。
4. 禁止把广告组证据自动归因到单个 ASIN，禁止自动执行广告动作。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:review-ui
npm run build
```

## 2026-06-24：搜索词复核卡片列表不能替代判断摘要

```text
问题：
Parent ASIN 广告搜索词表现复核区虽然已经展示卡片，但用户仍需要逐张卡片读业务问题、指标目的和证据缺口，才能知道先看扩量、止损还是观察补证。
这会让搜索词复核层变成另一个小报表，继续增加决策成本。

原因：
1. 卡片是证据下钻入口，不是面板级判断入口。
2. 多个 Parent ASIN 场景下，用户需要先看搜索词层的分布和优先对象，而不是逐个读完整卡片。
3. 搜索词表现分组不是人工动作对象，必须先说明排序、证据缺口和人工下一步，避免被误解成自动加词或否词入口。

解决：
1. 新增 buildSearchIntentReviewDecisionSummary。
2. 搜索词复核区在卡片列表前新增判断摘要。
3. 摘要固定展示有效词扩量、浪费词止损、证据缺口观察三类分布。
4. 摘要读回优先 SearchTerm、证据缺口、人工下一步和动作边界。
5. 结构测试锁定摘要必须先于卡片列表。

防错规则：
1. 搜索词复核层必须先给判断摘要，再给卡片证据。
2. 卡片指标只能服务扩量、止损、观察补证判断，不能替代判断。
3. 搜索词表现分组不能写成人工动作对象，不能改变 Parent ASIN / 广告 ASIN / 广告组诊断入口。
4. 禁止把搜索词复核摘要包装成自动加词、否词、调价或暂停广告动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run test:review-ui
npm run build
```

## 2026-06-24：Parent ASIN 首页不能只给优先列表

```text
问题：
用户如果要同时看多个 Parent ASIN，首页只给“今日优先处理清单”仍然会让用户逐行阅读，每个 Parent ASIN 都像一张小报纸。

原因：
1. 优先列表能说明排序，但不能先把对象按处理阶段归类。
2. 多 Parent ASIN 场景下，用户第一眼需要知道哪些到期复盘、哪些待人工确认、哪些观察、哪些暂不展开。
3. 如果首页没有分诊桶，用户会过早进入单个 Parent ASIN 的纵向详情，阅读成本继续上升。

解决：
1. 新增 Parent ASIN 分诊桶：复盘优先、人工确认、保持观察、暂不展开。
2. 分诊桶复用已有 ProductScopePriorityQueueItem，不新增排序公式和后端字段。
3. 首页摘要先展示分诊桶，再展示 Parent ASIN 优先处理清单。

防错规则：
1. 首页入口必须先回答“这批 Parent ASIN 分别处于什么处理阶段”，再展示逐行列表。
2. 分诊桶只能表达人工处理阶段，不能暗示自动广告动作。
3. 没有广告证据的 Parent ASIN 只能作为经营背景，不能进入广告诊断动作。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
```

## 2026-06-25：Parent ASIN 作战流不能把筛选器当装饰

```text
问题：
用户草图提出“筛选器 -> Parent ASIN 销售表现 -> 广告组数据 -> 广告组下具体数据 -> AI 广告数据分析”的路径。
如果直接照图做成纵向大块页面，筛选器会变成装饰控件，后面仍然是四块报表，用户看 10 个 Parent ASIN 仍像看 10 张报纸。

原因：
1. 筛选器本质是对象口径门禁，不只是 UI 控件。
2. Parent ASIN 销售表现、广告组、搜索词、广告位、ABA 属于不同对象层，不能放在同一层报表语义里。
3. 没有先说明“当前口径能证明什么、不能证明什么”，用户仍要自己读指标判断关系。

解决：
1. Parent ASIN 运营诊断路径新增“筛选器与口径锁定”第 0 层。
2. 单屏诊断框架固定为五层：口径锁定、销售入口、广告组优先排序、广告组下具体数据、AI 人工动作判断。
3. 第 0 层明确：广告组不是产品，销售子 ASIN 不等于广告 ASIN，ABA 不是店铺专属数据。
4. 测试锁定“不能把筛选器、销售、广告组、明细和 AI 分析堆成长报表”。

防错规则：
1. 筛选器必须说明经营对象、站点、周期和数据来源口径。
2. 所有下钻板块必须回答业务问题、当前判断、能证明什么、不能证明什么、人工下一步。
3. 具体数据只能服务判断，不能替代判断。
4. 禁止把筛选器后的页面做成普通 BI 报表或自动广告动作入口。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
```

## 2026-06-25：广告组下具体数据不能直接变成明细表

```text
问题：
广告组展开区已经有投放商品、投放词、搜索词和广告位明细，但如果用户必须逐块阅读这些明细，仍然会增加决策时间。

原因：
1. 具体数据是证据，不是第一屏判断。
2. 投放商品、投放词、搜索词和广告位属于不同证据层，用户需要先知道问题优先落在哪一层。
3. 如果没有先说明“不能证明什么”，用户容易把搜索词或广告位误读成单个广告 ASIN 归因。

解决：
1. 当前广告组复核区新增“广告组下具体数据四要素摘要”。
2. 四要素固定为：问题落点、证据强度、不能证明、人工下一步。
3. 摘要渲染在复核顺序、广告组证据摘要和运营检查清单之前。
4. 明细仍保留为下钻证据，不替代人工确认和 7/14 天复盘。

防错规则：
1. 广告组下具体数据必须先给四要素摘要，再展示明细。
2. 具体数据只能解释问题落点和证据强度，不能自动归因到单个广告 ASIN。
3. 人工下一步只能是记录观察、标记已处理、加入复盘或忽略本次，不得包装成自动广告动作。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
```

## 2026-06-26：Parent ASIN 队列行不能把先读结论埋在材料里

```text
问题：
左侧已经有 Parent ASIN 首页分诊摘要和分诊桶，但每条 Parent ASIN 行仍同时展示状态、判断、证据、人工下一步和默认下钻。
用户看 10 个 Parent ASIN 时，仍可能逐行读完整材料，再自己提炼“到底先点哪个、点进去做什么”。

原因：
1. 首页摘要解决了全局排序，但行内还缺少第一眼命令。
2. 状态、判断、证据和下钻都是必要材料，但不应该抢在“先读结论”前面。
3. 如果行内第一层仍是材料包，用户会回到读报表心智，AI 工作台的分诊价值会被削弱。

解决：
1. 每条 Parent ASIN 队列行新增“只看这一句”。
2. 将 workflowStatus.nextStep 提前到行内先读结论。
3. 原“下一步”摘要改为“人工”，只承接右侧允许的人工动作。
4. 结构测试锁定：先读结论必须早于状态原因、判断和证据摘要。

防错规则：
1. Parent ASIN 队列行必须先回答“打开这行后做什么”，再给判断和证据。
2. 行内证据摘要可以保留，但不能替代先读结论。
3. 不要因为行内阅读成本高就推倒三栏工作台或改成 BI 大盘。
4. 搜索词、广告位和广告组仍是证据层，不能因为文案压缩而变成商品归因或自动动作对象。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:review-ui
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
浏览器打开 http://127.0.0.1:5175/，确认左侧今日 Parent ASIN 优先处理清单显示“只看这一句”。
```

## 2026-06-26：中间诊断详情不能把四问藏在合同面板后面

```text
问题：
中间诊断区已经有“业务判断与指标目的”面板，但用户点进信号后第一眼仍先看到诊断主视角、对象上下文和元信息。
用户需要继续阅读材料，才能提炼当前判断、证据能证明什么、不能证明什么、人工下一步是什么。

原因：
1. 完整诊断合同适合做证据说明，不适合作为第一眼判断入口。
2. AI 信号工作台的核心不是堆指标，而是缩短用户从信号到人工判断的路径。
3. 如果四问摘要位置过后，用户仍会回到读报表和读材料的心智。

解决：
1. 中间诊断详情新增“诊断先读四问”。
2. 四问固定为：当前判断、能证明、不能证明、人工下一步。
3. 四问复用现有 diagnosisEvidenceSummary，不新增后端事实，不改变 diagnosis_contract。
4. 四问位置前置到标题后、诊断主视角前，完整合同和证据链继续保留。

防错规则：
1. 中间诊断详情必须先给当前判断、能证明、不能证明、人工下一步。
2. 完整合同和证据链不能替代第一眼判断。
3. 不要因为阅读成本高就推倒 AI Copilot 三栏工作台，也不要把人工动作包装成自动广告动作。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run test:review-ui
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
浏览器打开 http://127.0.0.1:5175/，确认“诊断先读四问”早于“诊断主视角”和“业务判断与指标目的”。
```

## 2026-06-26：右侧人工确认不能和中间诊断使用两套四问语言

```text
问题：
中间诊断已经前置“当前判断 / 能证明 / 不能证明 / 人工下一步”，但右侧人工确认前仍使用“当前在确认什么 / 证据能证明什么 / 现在人工做什么”等另一套说法。
用户在最后点击人工动作前，需要重新理解同一条证据链，增加决策成本。

原因：
1. 右侧已有人工确认前四问和预检机制，但它更像操作前说明，语言没有完全承接中间诊断。
2. 同一条 AI 信号链路如果每个板块换一套术语，会让用户以为对象或口径发生变化。
3. 人工按钮前必须强调诊断判断和证据边界，而不是只强调按钮推荐。

解决：
1. 右侧“人工确认前四问”标签统一为：当前判断、能证明、不能证明、人工下一步。
2. 当前判断优先读取 selectedSignal.summary。
3. 人工下一步优先读取 selectedDiagnosisEvidenceSummary.nextManualStep，建议按钮动作只作为 detail。
4. 结构测试锁定新标签和取值来源，旧标签不再作为默认语言。

防错规则：
1. 中间诊断、右侧人工确认和复盘回看必须尽量使用同一套四问语言。
2. 右侧按钮前默认层必须先回答业务判断和证据边界，再展示完整预检和按钮。
3. 统一语言不等于放宽动作边界；仍只允许记录观察、标记已处理、加入复盘和忽略本次。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:review-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
浏览器打开 http://127.0.0.1:5175/，确认右侧“人工确认前四问”使用当前判断、能证明、不能证明、人工下一步。
```

## 2026-06-26：复盘回看不能和诊断链路使用另一套四问语言

```text
问题：
中间诊断和右侧人工确认已经统一“当前判断 / 能证明 / 不能证明 / 人工下一步”，但复盘回看仍使用“当时确认什么 / 当时证据证明什么 / 当时证据不能证明什么”。
用户到 7/14 天复盘时需要重新翻译同一条证据链，容易把 ReviewTodo evidence_snapshot 读回误解为当前实时诊断。

原因：
1. 复盘区已有 evidence_snapshot、诊断路径读回和 ReviewRecord 保存门禁，但默认层文案没有承接前面的四问语言。
2. “证据证明什么”比“能证明”更像结论断言，容易弱化反证和归因边界。
3. 复盘区必须强调点击时证据快照，而不是当前实时页面重新判断。

解决：
1. 复盘回看四问统一为：当时判断、当时能证明、当时不能证明、现在复盘做什么。
2. 标题辅助语改为“沿点击时证据快照”。
3. 不改后端，不补写历史 evidence_snapshot，不改变 ReviewRecord 保存门禁。
4. 结构测试锁定新标签。

防错规则：
1. 诊断、人工确认和复盘回看必须沿同一套四问语言递进。
2. 复盘只能读回点击时 evidence_snapshot 和 ready 窗口，不能把当前实时证据伪装成历史判断。
3. 统一语言不等于自动判断建议有效；未 ready 前只能说待复盘或证据不足。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:review-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
浏览器打开 http://127.0.0.1:5175/，确认复盘区显示“当时判断 / 当时能证明 / 当时不能证明 / 现在复盘做什么”。
```

## 2026-06-26：复盘保存不能把对象一致性藏在完整账本里

```text
问题：
ReviewRecord 保存门禁已经检查 ReviewTodo 的对象引用和证据签名，但默认业务判断层没有直接告诉用户“是否还是同一个对象”。
用户保存复盘结论前，如果只看到 ready / blocked，不一定能判断当前证据是否来自同一个 object_id / object_label 和点击时 evidence_snapshot。

原因：
1. todo_object_reference 和 todo_evidence_signature 属于正确的底层门禁，但位置偏技术。
2. 复盘保存是人工闭环的最后一步，默认层必须先回答业务风险，而不是让用户展开检查清单后自己翻译。
3. 如果对象一致性不前置，后续容易把当前页面缓存、其他对象证据或重新计算结果误认为可保存复盘依据。

解决：
1. 在复盘保存业务判断默认层新增“对象一致性”。
2. 直接读取已有 todo_object_reference / todo_evidence_signature 检查结果，不新增后端字段。
3. 明确提示不能用其他对象或当前页面缓存保存复盘。
4. 结构测试锁定对象一致性文案和两个 preflight id。

防错规则：
1. 复盘保存默认层必须直接说明对象一致性，不能只藏在完整 preflight 清单里。
2. 任何保存 ReviewRecord 的 UI 都必须回看 ReviewTodo 的点击时 evidence_snapshot 和稳定对象引用。
3. 不要因为暴露对象一致性就扩大成新表、新后端合同或自动广告动作。
4. 每轮发现问题时围绕最终目标结构化推进，不要矫枉过正或推翻已有 MVP 主线。

验证方式：
npm run test:workbench-structure
npm run test:review-ui
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
浏览器打开 http://127.0.0.1:5175/，确认当前复盘窗口未到期时不渲染保存结论区、不保存复盘结论、不自动改规则、不自动执行广告动作；保存判断展示由结构测试覆盖。
```

## 2026-06-26：Parent ASIN 队列默认全量展开会把工作台变回报表

```text
问题：
Parent ASIN 首页已经有“今日先看什么”和分诊桶，但下方优先处理清单默认显示全部 Parent ASIN。
用户面对 10 个 Parent ASIN 时仍会像看 10 张报表，需要自己逐行比较，决策时间和成本没有真正降下来。

原因：
1. 结构上已有分诊摘要，但默认筛选仍是 all。
2. 完整队列对横向核对有价值，但不应该是第一阅读路径。
3. AI 信号工作台的首屏任务是先给“今天先看谁”，不是把所有对象都摊开。

解决：
1. 新增 UI 级筛选 focus，默认只展示排序第一的 Parent ASIN。
2. 分诊桶新增“今日优先”按钮，明确默认只打开这一条。
3. “全部”入口保留，用于横向核对完整排序队列。
4. 结构测试锁定默认 focus 和按需查看全部的文案。

防错规则：
1. 多 Parent ASIN 场景默认只展示今日优先对象，完整队列必须按需展开。
2. 收敛默认展示不等于删除数据；全部和分诊桶入口必须保留。
3. 不要因为列表太长就推翻 AI Copilot 三栏工作台，也不要把分诊做成 BI 大盘。
4. 每轮优化都围绕最终目标结构化推进：先判断、再下钻证据、最后人工确认和复盘。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:review-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
浏览器打开 http://127.0.0.1:5175/，确认默认队列 1 行，点击“全部”恢复 10 行，再切回“今日优先”恢复 1 行。
```

## 2026-06-26：中间诊断候选全铺开会增加用户决策成本

```text
问题：
Parent ASIN 队列默认已收敛到“今日优先”，但点击进入中间诊断后，广告组和搜索词候选仍默认铺开。
用户虽然少看了 Parent ASIN 列表，却又被迫阅读多个广告组和多个搜索词组，仍像看一张长报纸。

原因：
1. 中间诊断保留了完整证据访问，但默认层没有区分“当前先看什么”和“完整候选在哪里”。
2. 广告组优先判断固定取第一行，可能和用户当前聚焦广告组不一致。
3. 搜索词复核路径默认展示全部语义组，增加了人工判断前的阅读成本。

解决：
1. 广告组优先判断改为优先跟随当前 selectedId，找不到时才回退第一行。
2. 当前聚焦广告组在默认层展示，其他广告组进入折叠候选区。
3. 单屏搜索词和搜索词复核区只默认展示第一条路径，其他搜索词组折叠保留。
4. CSS 补齐默认路径卡片和折叠入口样式，避免结构正确但视觉层级不清。

防错规则：
1. AI 工作台默认层必须回答“当前先看什么”，不能把所有候选都当成默认内容。
2. 收敛默认展示不等于删除证据；完整候选必须可展开追溯。
3. 当前聚焦对象必须与中间优先判断一致，不能左侧选 A、中间默认讲 B。
4. 每轮发现问题时围绕最终目标结构化推进，不要矫枉过正，不推翻三栏 Copilot 工作台，不扩大成 BI 大盘。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:review-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
浏览器打开 http://127.0.0.1:5175/，确认广告组默认只看当前聚焦，其他 5 个广告组候选折叠；搜索词默认只看第一条路径，其他搜索词组折叠。
```

## 2026-06-26：右侧四个人工动作默认并列会增加二次决策成本

```text
问题：
右侧已经有“本次建议人工动作”，但默认层仍展示 4 个动作选择依据和 4 个动作按钮。
用户看完中间证据链后，还要重新比较“加入复盘 / 记录观察 / 标记已处理 / 忽略本次”，增加人工确认前的决策成本。

原因：
1. 允许动作集合需要完整保留，但不应该全部作为默认主路径。
2. 推荐动作已经由后端预检、button gate 和诊断证据推导，默认层应优先承接这一个动作。
3. 四个动作依据适合作为改判说明，不适合作为默认阅读内容。

解决：
1. 默认按钮区只渲染推荐动作按钮。
2. 其他 3 个允许动作进入折叠区，运营需要改判时再展开。
3. 四个动作选择依据进入折叠区，默认只读推荐动作卡和推荐按钮。
4. 结构测试锁定推荐动作优先、其他动作折叠和选择依据折叠。

防错规则：
1. 右侧默认层必须回答“当前建议用户点哪个人工动作”，不能把所有允许动作并列成默认选择题。
2. 收敛默认动作不等于删除人工改判权；其他允许动作必须保留在二级入口。
3. 选择依据和完整预检适合折叠保留，默认层只保留业务判断、推荐动作和关键边界。
4. 不要因为右侧动作太多就改后端动作集合或加入自动广告执行。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:review-ui
npm run test:signal-ui-runtime
npm run build
python scripts\inspect_api_snapshot.py --snapshot-root data\api_snapshots
python scripts\run_signal_scan.py --market-id 1
浏览器打开 http://127.0.0.1:5175/，确认右侧默认只显示 1 个推荐动作按钮，动作选择依据和其他动作入口默认折叠。
```

## 2026-06-26：右侧对象双轨提示会让用户误以为要重新判断写入对象

```text
问题：
右侧人工确认默认层同时展示“已推荐/已留痕对象”和“当前可写候选”。
这虽然在工程上保护了对象边界，但用户第一眼会以为自己还需要判断两个对象谁才应该被处理，增加授权前成本。

原因：
1. 系统需要保留已留痕对象、复盘读回对象和本次可写对象的安全边界。
2. 但默认层没有把主语收敛到“本次按钮写入对象”，而是把边界说明提前变成了主内容。
3. 对象边界是必要证据，不应该成为用户默认阅读路径上的二次选择题。

解决：
1. 默认卡片改为“本次按钮写入对象”。
2. 默认第一行展示按钮实际写入对象，第二行展示证据边界。
3. 详细“已留痕对象 / 本次写入对象 / 人工下一步”进入折叠的对象边界核对。
4. 后端 preflight、ManualAction payload、evidence_snapshot 和复盘读回逻辑不变。

防错规则：
1. 右侧默认层必须先回答“本次按钮会写给谁”，再让用户看对象边界。
2. 保护对象边界不等于把所有技术边界放到第一层；低频核对信息应折叠保留。
3. 发现默认层太重时，优先调整信息层级，不要立刻改数据模型、删证据或推翻三栏工作台。
4. 每轮复盘围绕最终目标、MVP 链路、数据口径、对象关系、证据链和用户下一步动作结构化判断，避免矫枉过正。

验证方式：
npm run test:workbench-structure
npm run test:signal-ui
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-26：SearchTerm 复核路径不能把广告承接压成一句话

```text
问题：
点击 SearchTerm 证据聚焦后，中间执行路径只有“核对广告承接”一句话。
用户仍然需要自己推理广告活动、广告组、投放词、同组广告 ASIN、广告位之间的关系，决策成本偏高。

原因：
1. Amazon 广告架构里，广告组是容器，不是商品。
2. 一个广告组可能投放多个广告 ASIN，SearchTerm 表现不能自动归因到单个 ASIN。
3. 广告位可以解释流量位置和表现分布，但不能单独证明商品承接问题。
4. 把这些层压成一句“广告承接”，会让 AI Copilot 工作台退回到用户自己读报表。

解决：
1. 把执行路径拆成：锁定入口、锁定 SearchTerm、核对广告组 / 投放词、核对同组广告 ASIN、核对广告位、选择人工动作。
2. SearchTerm 焦点说明明确沿广告组、投放词、同组广告 ASIN、广告位逐层核对证据。
3. 页面文案明确同组广告 ASIN 只能作为候选承接，不能直接归因到单个 ASIN。
4. 保留原后端数据口径和人工动作集合，不新增自动广告动作。

防错规则：
1. SearchTerm 证据链必须回到广告组和投放词，再看同组 advertised products，不能直接跳到单个 ASIN 结论。
2. 广告位只能作为流量位置证据，不能被包装成商品承接归因。
3. 发现阅读路径不清时，优先拆清默认判断路径，不要立刻改数据模型、删除证据或把页面改成 BI 大盘。
4. 每轮复盘围绕最终目标、MVP 链路、数据口径、对象关系、证据链和用户下一步动作结构化判断，避免矫枉过正。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-26：Parent ASIN 销售盘不能被降级成普通背景说明

```text
问题：
Parent ASIN 销售表现入口原先写成“销售背景（不直接诊断）”。
虽然对象边界是对的，但用户会觉得这一层没有回答经营问题，只是在做免责说明；这会增加进入广告诊断前的决策成本。

原因：
1. 销售表现确实不能直接归因到广告组、搜索词或广告位。
2. 但运营真实路径是先看 Parent ASIN 整体销售盘，再判断广告承接是否值得下钻。
3. 如果只强调“不直接诊断”，会把销售盘从业务分母降级成无行动价值的背景信息。

解决：
1. 把第一层改为“经营销售盘（业务分母）”。
2. 在 Parent ASIN 概览中展示广告订单占比和广告销售占比。
3. 业务问题改为“整体销售盘和广告承接占比，是否支持进入广告诊断”。
4. 仍保留边界：只有有 advertised_products 证据的广告 ASIN 才进入广告诊断，未投放子 ASIN 不进入广告分析。

防错规则：
1. 销售表现不能直接生成广告动作，但必须服务经营入口判断。
2. Parent ASIN 销售盘应该回答经营规模、广告承接占比和可下钻广告 ASIN，而不是只说“背景”。
3. 不要因为强调对象边界，就把所有上游经营数据都降级成不可行动信息。
4. 不要矫枉过正把销售盘改成 BI 报表；它只服务 AI Copilot 的广告诊断路径。

验证方式：
npm run test:signal-ui
npm run test:workbench-structure
npm run test:signal-ui-runtime
npm run build
```

## 2026-06-26：语义组复核不能扩大成可写人工动作对象

```text
问题：
用户从“语义组复核”进入具体搜索词后，容易误以为右侧人工动作会处理当前看到的聚合组。
如果系统为了降低摩擦而把语义组或任意可见 SearchTerm 都包装成可写对象，就会造成“看 A 证据、写 B 对象”或“把聚合上下文当业务对象”的错误。

原因：
1. 语义组是从 Parent ASIN 角度聚合广告用户搜索词表现的分析资产，不是 Amazon 广告架构里的可写对象。
2. SearchTerm 表现必须落到具体稳定对象 ID，并由后端 preflight 读回 target 后才能允许人工留痕。
3. 后端已有推荐候选或下一未处理候选时，前端不能为了当前可见搜索词临时生成写入对象。

解决：
1. 保持语义组只负责定位和缩小分析范围。
2. 非后端可写候选的具体 SearchTerm，manualActionPreviewForSelectedSignal 返回 null。
3. 右侧对象链路显示 waiting，提示当前信号没有后端可写预检对象。
4. 按钮门禁继续以后端 preflight target 和 selectedSignal expectedTarget 一致为准。

防错规则：
1. 语义组、search intent、Parent ASIN 搜索词聚合都不能作为 ManualAction 写入对象。
2. 降低用户决策成本不等于扩大可写范围；优先做摘要、优先级和路径说明，不要绕过对象门禁。
3. 发现对象口径混乱时，先检查主链路和 preflight target，不要立刻推翻工作台结构或重建数据模型。
4. 每轮复盘围绕最终目标、MVP 链路、数据口径、对象关系、证据链和用户下一步动作结构化判断，避免矫枉过正。

验证方式：
npm run test:signal-ui
```

## 2026-06-26：多 Parent ASIN 场景不能让用户像逐张读报表

```text
问题：
真实 market_id=1 下存在 10 个以上 Parent ASIN。
如果“今日先看谁”的答案只出现在左侧队列深处，用户仍然会按 Parent ASIN 逐个打开销售盘、广告组、搜索词和广告位，决策成本接近读 10 张报表。

原因：
1. 系统已有 Parent ASIN 优先级排序和分诊桶，但关键结论位置太靠后。
2. 当前业务路径要求先从 Parent ASIN 经营入口判断是否值得下钻，再进入广告 ASIN、广告组、投放词、搜索词和广告位证据。
3. 降低阅读成本不能靠新增 BI 总览或堆更多指标，而应先把“先看谁、为什么、打开后看哪层、不要做什么”提前。

解决：
1. 新增第一屏 Parent ASIN 分诊指令条。
2. 复用现有 ProductScopePriorityDecisionSummary，不新增数据口径和排序算法。
3. 第一屏只给紧凑决策，完整排序依据仍保留在左侧 Parent ASIN 分诊面板。
4. 移动端必须允许 Parent ASIN 长文本换行，不能让按钮或长 ASIN 撑出横向滚动。

防错规则：
1. 多 Parent ASIN 的第一屏必须先回答“今日只打开哪一个”，再让用户读单个对象证据链。
2. 不能为了减少阅读成本把页面改成 BI 大盘；仍然要回到 AI Copilot 的分诊、证据解释、人工确认和复盘闭环。
3. 全局分诊只决定阅读顺序，不替代销售表现、广告 ASIN、广告组、投放词、搜索词和广告位证据。
4. 每次新增第一屏信息都要检查桌面和移动端是否有文本溢出、按钮重叠或信息层级过重。

验证方式：
npm run test:workbench-structure
npm run build
Chrome 桌面 / 移动端可视核对
```

## 2026-06-26：第一屏默认动作不能只切换 Parent ASIN 筛选器

```text
问题：
第一屏已经提示“今日只打开优先 Parent ASIN”，但按钮原先只切换诊断入口筛选器。
当当前对象本身已经是今日优先 Parent ASIN 时，点击会直接 return，用户不会进入广告证据下钻，仍然要自己寻找广告组、投放词、搜索词和广告位证据。

原因：
1. 全局分诊回答了“先看谁”，但默认按钮没有承接“打开后看哪层”。
2. handleSelectProductScopePriority 只适合队列和左侧分诊面板的普通选择，不适合第一屏默认作战动作。
3. 如果切换 Parent ASIN 后立刻滚动，可能滚到旧 DOM 或空工作台，需要等 signalTriageSummary 返回并确认 scope 一致。

解决：
1. 第一屏按钮改为“打开并定位广告证据”。
2. 新增 pendingEvidenceDrilldownScopeId，切换 Parent ASIN 后等待对应 signalTriageSummary 返回再聚焦。
3. 当前对象已经是优先 Parent ASIN 时，直接复用 handleOpenProductScopeEvidenceDrilldown。
4. 左侧分诊面板和队列行仍只做选择，不强制滚动，避免普通浏览路径被打断。

防错规则：
1. 第一屏默认动作必须完成它承诺的业务路径：从“先看哪个 Parent ASIN”进入“默认广告证据下钻”。
2. 普通队列选择和第一屏默认动作要分开；不要把所有点击都强制滚动。
3. 异步切换对象后定位证据，必须等待 scope 摘要一致，不能滚动到旧对象证据。
4. 每轮修阅读路径时围绕最终目标和 MVP 主链路收敛，不要因为一个按钮不顺就推翻三栏工作台或重建数据模型。

验证方式：
npm run test:workbench-structure
npm run build
git diff --check
Chrome DOM 冒烟：点击“打开并定位广告证据”后出现“已定位默认广告组证据”。
```

## 2026-06-26：优先广告组默认卡不能让用户从广告位缺口里倒推证据顺序

```text
问题：
进入默认广告组下钻后，页面已有问题落点、证据强度、人工下一步，也有后续四层复核路径。
但优先广告组默认卡顶部先出现广告位证据状态，四层复核顺序藏在后面的“当前广告组复核路径”里，用户可能先看到广告位缺口，再误以为当前问题就是广告位问题。

原因：
1. 广告位只是流量位置证据，不能单独证明广告组或商品承接问题。
2. 当前真实样本的优先层是搜索词意图分化，但广告位缺口位置过早，容易抢占用户注意力。
3. 四层复核顺序已经存在，问题不是缺模型，而是默认信息层级不够顺。

解决：
1. 在优先广告组默认卡里新增“优先广告组证据读取顺序”。
2. 复用已有 buildProductScopeAdGroupChecklistItems 和 buildProductScopeAdGroupReviewPriority。
3. 顺序固定为投放商品、投放词、搜索词、广告位，并展示当前优先层。
4. 该顺序放在闭环状态之后、广告位证据状态之前。

防错规则：
1. 优先广告组默认卡必须先告诉用户证据阅读顺序，再展示单层证据缺口。
2. 广告位缺口只能表示当前不能判断广告位影响，不能包装成广告位异常结论。
3. 不要因为默认卡信息层级不顺就重写后端诊断、扩大 API 同步范围或新增自动广告动作。
4. 每轮看到局部问题时，先检查是否可以通过信息层级收口，避免矫枉过正。

验证方式：
npm run test:workbench-structure
npm run build
git diff --check
Chrome DOM 冒烟：优先广告组证据读取顺序位于广告位状态之前。
```

## 2026-06-26：投放词缺口不能被误读成完整关键词库缺失

```text
问题：
默认广告组下存在“投放词证据缺口”。
如果页面只写“未标记投放词”或“等待投放词证据”，用户容易误以为系统已经判断完整关键词库缺失，进而期待自动加词或否词建议。

原因：
1. 当前 MVP 的投放词证据来自广告搜索词表现行上的 keyword_text / target_id。
2. SP 关键词详情和商品定向详情第一阶段暂缓同步。
3. 搜索词表现行没有带出明确投放上下文，只能说明当前广告组下搜索词样本缺少投放词来源，不能证明关键词库覆盖问题。

解决：
1. 优先广告组证据读取顺序里增加“投放词口径”。
2. fallback 文案改成“投放词缺口：未带 keyword_text / target_id”。
3. 保留边界：只用 keyword_text / target_id 解释投放上下文，不能当作完整关键词库。
4. 结构测试禁止回退到模糊的“未标记投放词”。

防错规则：
1. 投放词缺口只能说明当前搜索词表现行没有带出 keyword_text / target_id，不等于关键词库缺失。
2. 暂缓 SP 关键词详情时，不得高置信判断“关键词库未覆盖”。
3. 投放词证据不足时，只能先做搜索词表现人工复核，不得自动加词、否词或调价。
4. 发现字段缺口时先说清字段来源和同步边界，不要扩大成新模块或新数据源。

验证方式：
npm run test:workbench-structure
npm run build
git diff --check
Chrome DOM 冒烟：优先路径显示“投放词口径”，明细显示“投放词缺口：未带 keyword_text / target_id”。
```

## 2026-06-26：搜索词复核上下文不能冒充人工动作对象

```text
问题：
右侧“搜索词人工留痕对象读回”会读取 evidence_snapshot 里的搜索词字段，并把复盘对象显示为 SearchTerm。
当当前按钮真实写入对象是广告商品 B0GL8QDHW5 时，页面可能出现“SearchTerm：B0GL8QDHW5”，让用户误以为广告 ASIN 是搜索词。

原因：
1. 搜索词复核上下文和人工动作写入对象共用一个读回卡片。
2. buildSignalManualActionEvidenceSnapshot 曾把非 search_term primary_object.label 兜底成 searchTerm。
3. 读回卡片没有优先使用后端 preflight target 的 object_type / object_id / object_label。

解决：
1. SearchIntentManualActionReadbackInput 增加 actionObjectType / actionObjectId / actionObjectLabel。
2. 读回卡片优先显示真实动作对象；广告商品显示为“广告商品：ASIN”，只有 search_term 才显示 SearchTerm。
3. buildSignalManualActionEvidenceSnapshot 只在 primary_object.object_type 为 search_term 时，才把 primary_object.label 当成搜索词。
4. SignalTriageWorkbench 调用时传入 selectedManualActionPreviewPreflight.target，保持右侧标签与按钮实际写入对象一致。

防错规则：
1. evidence_snapshot 是证据解释，不是人工动作对象身份的权威来源。
2. 人工动作对象必须以后端 preflight target 为准。
3. Parent ASIN 搜索词表现复核只能作为上下文，不得写成 ProductScope、广告商品或 SearchTerm 动作对象。
4. 每轮发现口径错误时先修最小对象边界，不要因为局部错误推翻三栏工作台或重建流程。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
git diff --check
Chrome DOM 冒烟：右侧显示“广告商品：B0GL8QDHW5”，且不再出现“SearchTerm：B0GL8QDHW5”。
```

## 2026-06-26：右侧人工动作默认层不能再暗示四个动作并列选择

```text
问题：
右侧已经有“本次建议人工动作”和推荐动作主按钮，但默认边界文案仍写“4 项 / 需要人工点击才会留痕”，折叠摘要也写“展开 4 个人工动作选择依据”。
这会让用户误以为看完诊断后还要重新比较四个动作，增加人工确认前的二次决策成本。

原因：
1. 工程上保留四个允许动作是必要边界。
2. 但默认阅读层应该服务“本次建议点哪个动作”，不是展示动作全集。
3. 把动作全集放到默认文案里，会让 AI Copilot 工作台退回到用户自己做选择题。

解决：
1. 人工确认边界改为“先点本次建议，其他动作仅作备选”。
2. 动作角标改为“建议优先”。
3. 动作选择依据折叠摘要改为“其他动作仅在人工判断不一致时展开”。
4. 折叠内推荐动作标记“本次建议”，其他可用动作标记“备选动作”。

防错规则：
1. 右侧默认层必须先回答“本次建议点击哪个人工动作”。
2. 四个允许动作必须保留，但不应在默认层制造并列选择成本。
3. 推荐优先只是展示层级，不得绕过人工确认，也不得自动执行广告动作。
4. 每轮发现决策成本问题时，优先调整信息层级，不要删除安全边界或重建流程。

验证方式：
npm run test:workbench-structure
npm run test:review-ui
npm run build
git diff --check
Chrome DOM 冒烟：右侧显示“先点本次建议，其他动作仅作备选”，推荐卡为“加入复盘”，其他动作在折叠入口中。
```

## 2026-06-26：人工动作写后默认层不能只说明待办生成状态

```text
问题：
用户点击“加入复盘”后，如果默认层只说明 ReviewTodo 是否生成，仍然不能直接回答运营最关心的问题：7/14 天后到底看什么指标、按什么边界复盘。

原因：
1. ReviewTodo 是系统状态，不是业务判断。
2. evidence_snapshot 是审计明细，不能要求用户点击后再读完整快照才能知道下一步。
3. 当前真实 snapshot 不一定有独立“复盘指标”字段，不能把缺失字段伪装成真实证据。

解决：
1. 写后默认层从“复盘待办生成了吗”改为“复盘看什么”。
2. 优先从 ReviewTodo / 最新人工动作 / preflight preview 的 snapshot 中提取“复盘指标”。
3. 没有独立“复盘指标”字段时，显示明确 fallback：7/14 天后对比花费、订单、销售额、ACOS、CVR 和广告位 / 搜索词边界。
4. 明确未到完整窗口前不保存改善、无变化或恶化结论。

防错规则：
1. 写后默认层必须回答用户后续复盘动作，而不只是系统写入状态。
2. fallback 只能作为复盘口径说明，不能包装成真实证据字段。
3. 未到 7/14 天窗口前，不得提前生成复盘结论。
4. 发现默认层不清楚时，优先调整信息层级，不要推翻人工确认流程或新增自动广告动作。

验证方式：
npm run test:workbench-structure
npm run test:review-ui
npm run build
Chrome DOM 冒烟：写后读回显示“复盘看什么”和默认复盘指标；当前 snapshot 未出现独立“复盘指标”证据项。
```

## 2026-06-26：人工动作写后消息不能只像系统账本

```text
问题：
人工动作保存后的 inline 消息会读回证据快照、复盘排程、复盘结论和禁止广告动作。
这些信息必要，但如果它们排在第一层，用户点击后仍要自己判断后续复盘看什么。

原因：
1. 写后消息复用了读回一致性和 post_write preflight 状态，天然偏系统账本。
2. 默认读回摘要已经前置“复盘看什么”，但 inline 消息没有同步这层业务语言。
3. 当前真实 evidence_snapshot 不一定有“复盘指标”，不能把 fallback 当成真实证据字段。

解决：
1. 写后消息新增“写后复盘：复盘看什么”前置段。
2. 优先读取 evidence_snapshot 中的“复盘指标”。
3. 没有该字段时，只显示 fallback 复盘口径：花费、订单、销售额、ACOS、CVR 和广告位 / 搜索词边界。
4. 原有证据快照、复盘排程、复盘结论和禁止广告动作读回保留在后半段。

防错规则：
1. 写后消息第一层必须回答业务下一步，不能只展示系统写入数量。
2. fallback 只是复盘口径说明，不是新增真实证据。
3. 保留审计账本，不删除 evidence_snapshot / ReviewTodo / ReviewRecord 边界。
4. 每轮收口文案时围绕目标链路推进，不因局部不顺推翻人工确认模型。

验证方式：
npm run test:review-ui
npm run test:workbench-structure
npm run build
```
