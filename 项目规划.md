# Amazon广告AI信号工作台项目规划

更新日期：2026-06-14

## 1. 项目定位

本项目是面向 Amazon 广告运营的 AI 信号工作台。

积加 ERP 已经提供普通广告数据展示和基础分析，本项目不重复做报表，而是专注于：

- 自动识别异常和机会点
- 说明哪里异常、为什么异常、证据是什么
- 给出建议处理方式和置信度
- 支持人工确认、记录和复盘

AI 只做诊断、证据整理、建议生成和复盘辅助，不自动执行广告动作。

## 2. 明确不做

- 不自动改竞价
- 不自动暂停或开启广告
- 不自动新增关键词
- 不自动新增否定关键词
- 不绕过人工确认
- 不替代积加 ERP 的普通广告 BI 报表
- 不复用旧项目的产品中心模型
- 不把广告组当成产品
- 不把 ABA 当成开放平台 API 自动同步源

## 3. 旧项目隔离原则

旧项目只允许作为只读参考：

- 积加开放平台 API 路径参考
- 已验证字段和关联键参考
- 已踩过的数据边界错误参考

旧项目不能作为以下内容的参考：

- 对象模型
- 页面结构
- 产品中心逻辑
- 广告组绑定单产品逻辑
- 产品归因逻辑

正式开发时必须从新项目的数据模型和产品设计出发，避免被旧项目带偏。

## 3.1 当前项目内部样例隔离原则

当前新项目已有代码只允许作为技术壳参考。

允许参考：

```text
React / Vite 运行方式
FastAPI / Pydantic / pytest 运行方式
依赖版本
启动、构建、测试命令
```

不能参考：

```text
旧 TodaySignals 页面结构
RB831 样例数据
旧样例 UI 布局
旧样例业务对象
旧样例字段缺口
```

第一版原型必须独立实现 `SignalTriageWorkbench`，不能改造旧样例页面。

## 4. 数据源分层

### 4.1 积加开放平台 API

作为稳定主数据源。

第一阶段同步范围详见：`API同步范围.md`。

原型使用真实 API 快照，方案详见：`真实数据快照方案.md`。

已验证可用的数据方向：

- 店铺和站点信息
- 销售表现
- SP 广告活动
- SP 广告商品
- SP 广告位
- SP 商品定向
- SP 关键词
- SP 用户搜索词-投放
- SP 用户搜索词-关键词

这些数据适合进入自动同步链路。

### 4.2 积加 ERP 人工导出

作为增强数据源。

当前已确认的导出数据：

- ABA 搜索词报表
- 关键词监控报表
- 用户搜索词报表-按天
- 销售表现-父 ASIN

这类数据不能默认每日稳定更新，必须记录导入时间、来源文件和数据周期。

### 4.3 人工维护数据

作为业务语义源。

包括：

- 产品介绍
- 核心卖点
- 目标人群
- 适用场景
- 产品核心词
- 禁投词
- 竞品词
- 投放策略资料：主推款 ASIN、策略标签、生效周期

## 5. ABA 数据定位

当前结论：积加开放平台公开 API 暂不能直接获取 ABA 搜索词数据，只能从积加 ERP 导出。

导入流程详见：`ABA导入流程.md`。

ABA 在本项目中定位为：

```text
人工导入的市场机会增强数据
```

ABA 不进入自动 API 同步主链路。

ABA 数据只能按以下关系参与分析：

```text
站点 + 周期 + 标准化搜索词
```

ABA 本身不属于某个店铺。它只能和某个店铺下的广告搜索词、产品语义、销售表现做匹配后，形成机会信号。

当前样本文件事实：

- 文件：`ABA搜索词报表-Top30万搜索词.xlsx`
- 工作表：`关键词搜索热度`
- 数据量：300111 行
- 站点：US
- 周期：2026-05-10 到 2026-05-16
- 字段包括：关键词、搜索量排名、排名变化、前三 ASIN、前三 ASIN 点击份额、前三 ASIN 转化份额
- `搜索查询量` 列为空，不能用于估算搜索量

## 6. 核心对象模型

### 6.1 Shop

店铺。

多店铺作为顶部筛选器存在，不做单独入口页。

所有 AI 信号必须带店铺上下文：

```text
shop_id / shop_name
```

### 6.2 Marketplace

站点。

所有广告、销售、搜索词、ABA 匹配都必须带站点上下文：

```text
market_id / marketplace / country
```

### 6.3 SalesProduct

经营商品或销售商品。

代表实际经营和销售表现中的商品对象。

常见关联键：

```text
asin / msku / sku / market_id
```

### 6.4 AdvertisedProduct

广告商品。

代表广告中实际投放的商品对象。

常见关联键：

```text
campaign_id / group_id / ad_id / asin / msku / market_id
```

### 6.5 AdGroup

广告组。

广告组只是投放容器，不是产品。

用户当前业务里常见情况是：

```text
一个广告组至少包含一个商品，也可能投放多个商品
```

所以不能把广告组绑定成单个产品。

### 6.6 SearchTerm

广告搜索词。

代表用户实际搜索词及其广告表现。

### 6.7 AbaSearchTermSnapshot

ABA 搜索词周快照。

代表市场整体搜索热度、前三 ASIN、点击份额和转化份额。

### 6.8 KeywordRankSnapshot

关键词排名监控快照。

代表某关键词下自然排名、广告排名、视频广告位置等信息。

### 6.9 ProductSemanticProfile

人工维护的产品语义资料。

用于判断搜索词与产品的相关性。

### 6.10 AiSignal

AI 识别出的异常或机会点。

### 6.11 Evidence

信号证据。

每个 AI 信号必须能回到具体证据。

## 7. 核心关联关系

逻辑表结构详见：`数据模型与表结构.md`。

```text
SalesProduct
  ← asin / msku / sku →
AdvertisedProduct

AdvertisedProduct
  ← campaign_id / group_id / ad_id →
Campaign / AdGroup

SearchTerm
  ← campaign_id / group_id / keyword_id / target_id →
Keyword / ProductTargeting

SearchTerm
  ← normalized_query + marketplace + week →
AbaSearchTermSnapshot

SearchTerm
  ← normalized_query + asin + date/week →
KeywordRankSnapshot

AiSignal
  ← evidence_id →
Evidence
```

## 7.1 Amazon 广告深度分析框架

深度分析不是堆更多图表或更多信号，而是让每条判断都能落到固定分析链路：

```text
分析对象
→ 经营问题
→ 证据链
→ 判断与反证
→ 人工建议
→ 复盘验证
```

### 7.1.1 分析对象层

默认经营入口优先级：

```text
Parent ASIN / ASIN
→ AdvertisedProduct
→ SearchTerm
→ Placement
→ AdGroup / Campaign
→ DataQuality / Cross
```

硬规则：

- Parent ASIN / ASIN 是经营诊断主入口。
- AdvertisedProduct 是广告商品表现入口。
- SearchTerm、Placement、AdGroup 默认是证据和上下文，不直接等同于商品归因。
- AdGroup 是投放容器，不是产品。
- DataQuality / Cross 信号只解释数据链路，不直接输出经营异常结论。

### 7.1.2 经营问题层

每条分析必须归入一个明确经营问题：

| 问题类型 | 说明 | 典型对象 |
| --- | --- | --- |
| 花费浪费 | 有花费但订单、销售或点击承接不足 | SearchTerm / AdvertisedProduct / Placement |
| 销售承接不足 | 广告带来流量但 Listing、价格、库存或商品承接弱 | ASIN / AdvertisedProduct |
| 机会扩量 | 小样本高转化、热词相关、广告覆盖弱 | SearchTerm / ABA / ASIN |
| 投放结构失衡 | 广告组、广告位、活动结构导致判断混乱或预算倾斜 | AdGroup / Placement / Campaign |
| 市场竞争压力 | ABA、排名或竞品份额显示市场机会或压力 | AbaSearchTermSnapshot / KeywordRankSnapshot |
| 数据质量缺口 | 数据过期、缺失、口径冲突或无法归因 | DataQuality / Cross |
| 处理后复盘 | 人工动作后 7 天 / 14 天指标变化 | AiSignal / ManualAction |

### 7.1.3 判断层

任何深度分析结论必须同时说明：

```text
样本量是否足够
时间窗口是否完整
证据源是否独立
是否存在过期数据
是否存在主推款或人工策略边界
是否存在广告组 / 搜索词 / 广告位归因不确定
```

不能只因为单个指标异常就输出高置信结论。

### 7.1.4 复盘层

分析框架最终必须回到复盘：

```text
人工动作
→ 稳定业务对象 ID
→ 处理前 7/14 天窗口
→ 处理后 7/14 天窗口
→ ready 后人工保存复盘记录
```

没有 ready 复盘结果时，只能说“待复盘”或“证据不足”，不能说建议有效或无效。

## 8. 多店铺站点规则

v1 不做跨店铺总览。

店铺只作为顶部筛选器，不做单独店铺选择页。

首页顶部筛选器：

```text
店铺 / 站点 / 产品线 / 产品 / 周期 / 数据新鲜度
```

硬规则：

```text
每条信号必须带 shop_id / shop_name。
证据只能在同一店铺上下文内关联。
ABA 例外：ABA 是站点级外部数据，只能按 站点 + 周期 + 搜索词 匹配。
```

## 9. AI Copilot 工作台信息架构

v1 第一屏采用 AI Copilot 工作台，而不是普通数据大盘或聊天框。

已选原型参考：

```text
design/ai-copilot-workbench-v1.png
```

已选 UI 视觉策略：

```text
信号分诊工作台
```

页面结构：

```text
顶部：店铺 / 站点 / 产品线 / 产品 / 周期 / 数据新鲜度
左侧：今日 AI 信号队列
中间：选中信号的诊断、原因和证据链
右侧：建议处理、风险、置信度、人工确认
```

核心流程：

```text
AI 发现信号
→ 用户点开信号
→ 看哪里异常或哪里有机会
→ 看广告搜索词、ABA、销售、排名等证据
→ AI 给建议
→ 人工确认、记录、复盘
```

## 10. 信号卡字段

左侧信号卡只展示决策必要信息：

```text
严重度
信号类型
店铺
站点
对象类型
对象名称
核心问题一句话
影响指标
置信度
状态
数据新鲜度
```

状态包括：

```text
待确认 / 观察中 / 已处理 / 已忽略
```

## 11. 信号详情结构

点开信号后按固定顺序展示：

```text
1. 结论
2. 原因
3. 证据链
4. 影响范围
5. 反证或不确定性
```

每条证据必须标明来源：

```text
积加API / ABA导出 / 关键词监控 / 销售表现 / 人工语义
```

## 12. 右侧人工处理动作

只允许人工动作：

```text
记录观察
标记已处理
加入复盘
忽略本次
```

不出现自动执行动作。

## 13. AI 信号类型体系

信号体系一开始完整设计，开发分批实现。

信号统一结构：

```text
信号大类
信号类型
触发条件
所需数据
证据字段
建议动作
置信度规则
实现优先级
```

### 13.1 数据质量信号

- ABA 数据过期
- API 数据未更新
- API 快照无可分析数据
- 店铺或站点数据缺失
- 广告搜索词缺失
- 销售表现缺失
- 同一对象多来源指标冲突

### 13.2 广告效率异常

- 高花费无订单
- 高点击低转化
- 高曝光低点击
- CPC 异常升高
- ACOS 异常升高
- ROAS 异常下降
- 花费突然放大
- 曝光突然下降
- 订单突然下降

### 13.3 搜索词异常与机会

- 搜索词高花费无订单
- 搜索词高点击低 CVR
- 搜索词有订单但未重点投放
- 搜索词语义相关但广告覆盖弱
- 搜索词语义不相关但持续消耗
- 同类搜索词整体表现分化
- 新出现有效搜索词
- 长尾词低花费高转化

### 13.4 ABA 市场机会

- ABA 热词未覆盖
- ABA 热词有广告但份额弱
- ABA 前三 ASIN 竞品强势
- ABA 高热词与产品语义高度相关
- ABA 热词自然排名弱
- ABA 热词广告有点击但无转化
- ABA 趋势上升词

### 13.5 产品 / 广告商品信号

- 产品销售强但广告弱
- 产品广告强但销售承接弱
- 广告商品高消耗低产出
- 广告商品自然订单强但广告依赖高
- 同一产品不同 ASIN 表现差异大
- 产品语义与投放词不匹配

### 13.6 广告组结构信号

- 一个广告组内多商品表现分化
- 广告组内商品语义不一致
- 广告组消耗集中在少数商品，但必须先读取主推款策略资料；已确认主推款时不生成异常或待确认信号
- 广告组搜索词无法归属到具体商品
- 广告组结构影响诊断可信度

### 13.7 广告位信号

- Top of Search 花费高但转化差
- Product Pages 表现优于搜索顶部
- Rest of Search 低曝光但高转化
- 某广告位 ACOS 异常升高
- 广告位流量结构变化

### 13.8 排名与自然流量信号

- 自然排名强但广告仍高消耗
- 自然排名弱但 ABA 热度高
- 广告排名有但自然排名缺失
- 关键词排名下降
- 关键词排名上升但广告未承接
- 视频广告位置出现但转化弱

### 13.9 复盘效果信号

- 处理后 7 天花费下降
- 处理后 7 天订单改善
- 处理后 14 天 ACOS 改善
- 建议处理无明显效果
- 被忽略信号后续恶化
- 重复出现同类问题

## 14. 统一置信度规则

```text
高：至少两个独立证据源一致
中：一个强证据源 + 指标明显异常
低：只有单一数据源，或 ABA / 导出数据过期
```

## 15. 统一严重度规则

```text
高：影响花费、订单、ACOS、核心产品或高热搜索词
中：影响局部广告组、部分搜索词，观察价值高
低：数据不足、轻微波动、仅提示观察
```

## 16. v1 最小闭环

原型技术栈详见：`原型技术栈.md`。

先用 RBK004 验证：

```text
产品介绍
→ 销售表现
→ 广告商品
→ 广告搜索词
→ ABA 搜索词机会
→ 关键词排名监控
→ AI 信号
→ 人工确认 / 记录
→ 7 天 / 14 天复盘
```

### 16.1 MVP 收敛原则

MVP 不以信号数量为目标，以一个真实店铺站点能跑通人工决策闭环为目标。

当前 MVP 必须证明：

```text
选择店铺 / 站点
→ 读取最新真实 API 快照
→ 生成少量可解释 AI 信号
→ 点开信号能看到对象边界、原因、关键证据、完整证据、置信度和建议动作
→ 人工选择处理状态
→ 系统保存处理记录
→ 后续能按 7 天 / 14 天复盘处理效果
```

当前 MVP 暂不继续扩大：

```text
不继续堆信号数量
不做普通 BI 大盘
不做复杂语义聚类
不做跨店铺总览
不做自动广告动作
不做生产级权限和工作流
```

下一阶段优先级：

```text
第一优先：人工处理留痕最小闭环
第二优先：复盘记录和复盘指标对比
第三优先：在已有闭环稳定后再补新信号类型
```

## 17. 后续待细化

- AI 信号类型矩阵已落地：`AI信号类型矩阵.md`
- 数据模型与表结构已落地：`数据模型与表结构.md`
- ABA 导入流程已落地：`ABA导入流程.md`
- API 同步范围已落地：`API同步范围.md`
- 真实数据快照方案已落地：`真实数据快照方案.md`
- 原型技术栈已落地：`原型技术栈.md`
- Product Design 选中原型的可开发页面规格已落地：`开发原型.md`
- 开发原型实现方案已落地：`开发原型.md`
- 当前项目内部样例隔离规则已落地：`AGENTS.md` / `开发原型.md`
- 积加开放平台同步脚本实现流程
- 第一阶段开发任务拆分
