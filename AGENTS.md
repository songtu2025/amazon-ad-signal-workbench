# AGENTS.md

## MUST FOLLOW RULES

- 所有回复、任务清单、进度说明、代码注释和文档内容，均使用中文。
- 遵循 KISS 原则，优先做最小可验证闭环。
- 事实优先。如果代码、数据、API、导出文件和预期不一致，以实际结果为准。
- 在开始设计方案或实现代码之前，必须先充分调研当前相关文件和数据来源。
- 需求不明确时先停止并确认，不要猜测式开发。
- 总是先给出文字版方案，获得用户确认后再实施。非常小的只读检查可以例外。
- 每次只做一个明确任务，不顺手做无关重构。
- 每一轮任务必须回写任务进度，至少更新 `开发经验记录.md` 中的当前进展、事实结论和下一步。
- 每一轮任务开始前必须调用 Product Design 工作流做设计简报校准；已有确认方向时用回放确认，不重复发散。
- 禁止自动执行广告动作。
- 禁止复用旧项目错误模型。
- 禁止把旧项目当作新项目的页面结构或对象模型参考。
- 禁止把 ERP 页面功能当成开放平台 API 能力，必须以开放平台文档或真实请求验证为准。
- 禁止把页面名称当成数据事实，必须检查字段来源、API、数据库表和导入脚本。

## 项目定位

本项目是 Amazon 广告 AI 信号工作台。

积加 ERP 已经提供普通广告数据展示和基础分析，本项目不重复做 BI 报表，而是专注：

- 自动识别异常和机会点
- 说明哪里异常、为什么异常、证据是什么
- 给出建议处理方式和置信度
- 支持人工确认、记录和复盘

AI 只允许做：

- 异常识别
- 机会识别
- 证据整理
- 原因解释
- 建议生成
- 可信度说明
- 人工确认后的记录与复盘

AI 不允许做：

- 自动执行广告动作
- 自动改竞价
- 自动暂停或开启广告
- 自动新增关键词
- 自动新增否定关键词
- 绕过人工确认流程

## 新旧项目隔离

新项目路径：

```text
D:\DataProject\coedx_project\Amazon广告AI信号工作台
```

旧项目路径：

```text
D:\DataProject\coedx_project\Amazon广告分析
```

旧项目只允许作为只读参考：

- 积加开放平台 API 路径参考
- 已验证字段和关联键参考
- 已踩过的数据边界错误参考

旧项目不能作为以下内容参考：

- 对象模型
- 页面结构
- 产品中心逻辑
- 产品归因逻辑
- 广告组绑定单产品逻辑

如果必须读取旧项目，必须明确说明读取目的，并且不得修改旧项目文件。

## 当前项目内部样例隔离

当前项目已有样例页面和样例数据不能作为产品原型依据。

允许参考：

- React / Vite / TypeScript 运行方式
- FastAPI / Pydantic / pytest 运行方式
- 依赖版本
- 启动、构建、测试命令

禁止参考：

- `TodaySignals` 页面结构
- RB831 样例数据
- 旧样例组件拆分方式
- 旧样例样式层级
- 旧样例信号字段缺口

开发原型必须以以下文件为准：

- `项目规划.md`
- `项目规范.md`
- `开发原型.md`
- `AI信号类型矩阵.md`
- `数据模型与表结构.md`
- `真实数据快照方案.md`

如果现有代码和上述文档冲突，以文档和真实数据边界为准。

## 每次开始任务前必须读取

修改新项目任何文件前，必须先读取：

- `AGENTS.md`
- `项目规划.md`
- `项目规范.md`
- `开发原型.md`
- `开发经验记录.md`
- `AI信号类型矩阵.md`
- `数据模型与表结构.md`
- `API同步范围.md`
- `ABA导入流程.md`
- `真实数据快照方案.md`
- `原型技术栈.md`
- `问题解决记录.md`

如果任务涉及具体实现，还要按需读取：

- 当前相关代码文件
- 当前相关测试文件
- 当前相关数据样本
- 当前相关脚本

不要一次性读取无关大文件。

## 文档规则

- 禁止无要求创建文档。
- 用户明确要求写文档时，只写当前任务必要内容。
- 能用代码、配置、脚本表达的，不写复杂冗余说明。
- 文档必须服务开发和决策，不写空泛总结。

已确认的管理文档：

- `项目规划.md`：项目定位、数据边界、对象模型、工作台方向、信号体系
- `项目规范.md`：开发、数据、UI、测试、文档维护规范
- `开发原型.md`：原型定位、交互范围、验收标准
- `开发经验记录.md`：进度、决策变更、高频错误、可复用经验
- `AI信号类型矩阵.md`：AI 信号触发条件、证据字段、建议动作、置信度、严重度和测试样例
- `数据模型与表结构.md`：核心对象、逻辑表结构、来源批次、证据和复盘数据模型
- `API同步范围.md`：积加开放平台第一阶段同步范围、暂缓范围、信号降级规则
- `ABA导入流程.md`：ABA 人工导入、字段映射、校验、入库和信号降级规则
- `真实数据快照方案.md`：低频真实 API 快照、样本店铺、凭据、缓存和 mock 兜底规则
- `原型技术栈.md`：前端、后端、数据层、依赖和测试技术选型
- `问题解决记录.md`：项目问题、原因、解决方案和后续防错规则

当任务改变项目规划、项目规范、原型范围、数据边界或开发经验时，必须同步更新对应文档。

当任务暴露出新的错误、用户纠偏或重复风险时，必须同步更新 `问题解决记录.md`。

## Product Design 规则

每轮任务开始前必须先走 Product Design 设计确认。

执行方式：

- 如果是页面、原型、交互、视觉、信息架构任务，必须按 Product Design 简报流程确认后再实施。
- 如果是数据、后端、脚本任务，至少回放当前已确认的产品简报，确认不会偏离 AI Copilot 工作台方向。
- 本轮 Product Design 校准结论必须写入任务进度记录。

已选方向：

```text
AI Copilot 工作台
```

已选原型参考：

```text
design/ai-copilot-workbench-v1.png
```

v1 第一屏结构：

```text
顶部：店铺 / 站点 / 产品线 / 产品 / 周期 / 数据新鲜度
左侧：AI 信号队列
中间：信号诊断和证据链
右侧：建议处理和人工确认
```

禁止把 v1 做成：

- 普通数据大盘
- 聊天框优先
- 旧项目产品中心
- 只展示数据不展示证据的报表页

## 数据源规则

### 积加开放平台 API

可作为稳定主数据源。

已确认方向：

- 店铺和站点信息
- 销售表现
- SP 广告活动
- SP 广告商品
- SP 广告位
- SP 商品定向
- SP 关键词
- SP 用户搜索词-投放
- SP 用户搜索词-关键词

进入自动同步链路前，必须确认：

- API 路径
- 请求参数
- 返回字段
- 关联键
- 数据周期
- 是否真实可请求成功

### 积加 ERP 人工导出

只作为增强数据源。

当前包括：

- ABA 搜索词报表
- 关键词监控报表
- 用户搜索词报表-按天
- 销售表现-父 ASIN

人工导入数据必须记录：

- 来源文件
- 导入时间
- 数据开始日期
- 数据结束日期
- 店铺或站点归属
- 字段映射版本

## ABA 数据规则

当前结论：

```text
积加开放平台公开 API 暂不能直接获取 ABA 搜索词数据，只能从积加 ERP 导出。
```

ABA 定位：

```text
人工导入的市场机会增强数据
```

硬规则：

- ABA 不进入自动 API 同步主链路。
- ABA 不天然归属于某个店铺。
- ABA 只能按 `站点 + 周期 + 标准化搜索词` 参与匹配。
- ABA 不能直接当作店铺、产品或广告组数据。
- ABA 过期时，相关信号置信度必须降低。

当前样本事实：

- 文件：`ABA搜索词报表-Top30万搜索词.xlsx`
- 工作表：`关键词搜索热度`
- 站点：US
- 周期：2026-05-10 到 2026-05-16
- 数据量：300111 行
- `搜索查询量` 列为空，不能用来估算搜索量

## 对象模型规则

必须区分以下对象：

```text
Shop                  店铺
Marketplace           站点
SalesProduct          经营商品 / 销售商品
AdvertisedProduct     广告商品
AdGroup               广告组
SearchTerm            广告搜索词
AbaSearchTermSnapshot ABA 搜索词快照
KeywordRankSnapshot   关键词排名快照
ProductSemanticProfile 产品语义资料
AiSignal              AI 信号
Evidence              证据
```

关键边界：

- `AdGroup` 是投放容器，不是产品。
- 一个广告组可以投放多个商品。
- `SalesProduct` 和 `AdvertisedProduct` 必须分开。
- `ProductTargeting` 是商品定向，不是广告组内投放商品。
- 搜索词语义聚合是独立分析资产，不等于关键词本身。

## 多店铺站点规则

店铺作为顶部筛选器，不做单独店铺入口页。

每条 AI 信号必须带：

```text
shop_id / shop_name
market_id / marketplace / country
```

证据默认只能在同一店铺上下文内关联。

ABA 例外：

```text
ABA 是站点级外部数据，只能按 站点 + 周期 + 搜索词 匹配。
```

v1 不做跨店铺总览。跨店铺对比必须单独设计，不得混入普通信号。

## AI 信号规则

每个 AI 信号必须包含：

```text
signal_type
signal_category
shop_id
market_id
object_type
object_id
severity
confidence
summary
reason
evidence_count
data_sources
freshness_status
suggested_action
manual_status
review_result
```

每个信号详情必须按固定顺序展示：

```text
1. 结论
2. 原因
3. 证据链
4. 影响范围
5. 反证或不确定性
```

每条证据必须标明来源：

```text
积加API / ABA导出 / 关键词监控 / 销售表现 / 人工语义
```

信号体系采用：

```text
完整信号体系设计 + 分批实现
```

不要只做零散规则。每类信号必须定义：

```text
信号大类
信号类型
触发条件
所需数据
证据字段
建议动作
置信度规则
实现优先级
```

## 统一置信度规则

```text
高：至少两个独立证据源一致
中：一个强证据源 + 指标明显异常
低：只有单一数据源，或 ABA / 导出数据过期
```

## 统一严重度规则

```text
高：影响花费、订单、ACOS、核心产品或高热搜索词
中：影响局部广告组、部分搜索词，观察价值高
低：数据不足、轻微波动、仅提示观察
```

## 人工处理规则

系统只允许以下人工动作：

```text
记录观察
标记已处理
加入复盘
忽略本次
```

禁止出现以下自动动作：

```text
自动调价
自动暂停
自动否词
自动新增关键词
```

## 开发流程

每个开发任务遵循：

```text
1. 读取 AGENTS.md 和相关规划文档
2. 明确当前任务目标
3. 调用 Product Design 做本轮设计简报校准
4. 列出允许修改范围
5. 给出文字方案
6. 用户确认
7. 最小实现
8. 最小验证
9. 更新开发经验记录
10. 简短总结
```

如果任务涉及数据链路，必须先做只读验证，再写入或建模。

如果验证命令失败，必须停止并说明事实，不能继续猜测式开发。

## 测试和验证规则

根据任务风险选择最小验证。

优先验证：

- Python 语法编译
- 单元测试
- 关键脚本 smoke 测试
- 前端 build
- 浏览器可视检查
- 数据样本只读检查

不要为了形式跑大量无关测试。

验证失败时必须说明失败命令和关键错误。

## Git 和变更规则

- 不要回滚用户或其他进程的改动。
- 不使用 `git reset --hard`。
- 不使用破坏性清理命令。
- 不提交无关格式化。
- 不把旧项目文件混入新项目提交。

## 高频错误提醒

### 错误 1：把广告组当产品

正确做法：

```text
广告组只作为容器。
商品分析必须落到广告商品或销售商品。
```

### 错误 2：把 ERP 页面功能当开放平台 API 能力

正确做法：

```text
ERP 能展示，不代表开放平台能取。
必须查开放平台 API 或真实请求结果。
```

### 错误 3：把 ABA 当店铺数据

正确做法：

```text
ABA 是站点级市场数据。
只能通过站点、周期、搜索词与店铺数据关联。
```

### 错误 4：先写页面再补数据模型

正确做法：

```text
先确认数据源、对象边界和关联关系，再开发页面。
```

### 错误 5：AI 建议越权成自动执行

正确做法：

```text
AI 只做诊断、证据整理、建议和复盘。
广告动作必须人工确认。
```

## 任务结束输出规则

任务结束时只做简短总结，说明：

- 改了什么
- 验证了什么
- 还有什么未完成或风险

不要写冗长总结。
